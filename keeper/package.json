{
  "name": "@abrigo-somnia/keeper",
  "version": "0.1.0",
  "private": true,
  "description": "Off-chain key-holding keeper for the agentic data cooperative (TypeScript, zero-dep).",
  "type": "module",
  "engines": {
    "node": ">=23"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "te": "node src/teClient.ts",
    "vercel-build": "node scripts/build-vercel-output.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.0",
    "fast-check": "^4.8.0"
  }
}

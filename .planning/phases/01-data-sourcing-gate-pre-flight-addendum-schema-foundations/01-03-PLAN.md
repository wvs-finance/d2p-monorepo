---
phase: 01-data-sourcing-gate-pre-flight-addendum-schema-foundations
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - research/data_sourcing_matrix.yaml
  - research/DATA_SOURCING.md
  - tests/test_data_sourcing_matrix.py
  - tests/test_sufficiency_bars.py
autonomous: true
requirements: [DATA-SOURCE-01]
user_setup:
  - service: ormi-0xgraph
    why: "INDEX-01 (Phase 3) subgraph host — provisional free Developer pick; sign-off needed only if Phase-1 modeling shows the 300k-entity cap is exceeded (→ Ormi Production $75/mo)"
    env_vars: []
    dashboard_config:
      - task: "At signup, DISABLE free-tier auto-upgrade (Ormi auto-upgrades to Production $75/mo at 300k entities — would breach the no-spend-without-sign-off rule mid-backfill)"
        location: "Ormi 0xGraph dashboard — billing settings"
must_haves:
  truths:
    - "A consumer can load research/data_sourcing_matrix.yaml and find every mandatory capability row, each with source_url + utc_fetch_ts, and each sufficiency-bar row carrying a pass/fail value."
    - "A consumer can read research/DATA_SOURCING.md and find the cheapest-sufficient PROVISIONAL recommendation (Ormi free Developer $0), the cost-of-ownership table for all three paid classes (each under $390/mo, each with INDEX-01 re-author cost), the four numeric sufficiency bars, the real budget-vs-completeness coherence reconciliation, and a sign-off line."
    - "Any single free-tier sufficiency bar set to fail flips the verdict to the paid branch (encoded testably)."
    - "The recommendation is provisional-pending-Phase-3 with the subgraph-compatible paid alternative (Ormi Production $75/mo) pre-costed; a non-subgraph archive (GetBlock $39/mo) is noted as triggering an INDEX-01 sub-plan re-author."
  artifacts:
    - path: "research/data_sourcing_matrix.yaml"
      provides: "machine-checkable capability matrix + sufficiency-bar rows (fixed schema)"
      contains: "eth_getBlockReceipts"
    - path: "research/DATA_SOURCING.md"
      provides: "prose recommendation memo: verdict, cost-of-ownership table, four bars, coherence reconciliation, sign-off line"
      contains: "JUSTIFIED DEPARTURE"
  key_links:
    - from: "tests/test_data_sourcing_matrix.py"
      to: "research/data_sourcing_matrix.yaml"
      via: "fixed-schema conformance + provenance-completeness assertions"
      pattern: "source_url"
    - from: "tests/test_sufficiency_bars.py"
      to: "research/data_sourcing_matrix.yaml"
      via: "four-bar pass/fail logic + free-fail-flips-paid assertion"
      pattern: "sufficient"
---

<objective>
DATA-SOURCE-01 — the Phase-1 headline deliverable that GATES Phase 3 INDEX-01. Produce the machine-checkable capability matrix (`research/data_sourcing_matrix.yaml`, fixed schema) and the prose recommendation memo (`research/DATA_SOURCING.md`) that selects a PROVISIONAL data source via the four numeric sufficiency bars, prices all three paid classes under the $390/mo ceiling, and reconciles the free-vs-paid posture against abrigo-x402. All capability facts are live-probed in 01-RESEARCH.md — this plan transcribes them into the fixed-schema matrix with provenance and writes the verdict.

Supersedes (note prominently in DATA_SOURCING.md §Supersedes): 01-RESEARCH.md OVERTURNS three upstream premises —
1. Backfill is **36.3M blocks / ~42 days** (deploy block 283417317), NOT ~320M / ~270 days. The "direct-RPC backfill infeasible → Ormi mandatory" premise is FALSE — the public RPC is a full archive node; direct-RPC backfill is ~36,287 capped windows (feasible). Ormi is therefore PREFERRED (mapping surface + parity), not mandatory.
2. Free tier is amply sufficient and ALL paid fallbacks are under $390/mo. Provisional pick: Ormi free Developer ($0). Fallbacks: Ormi Production $75/mo (zero re-author), GetBlock Somnia archive RPC $39/mo (sub-plan re-author). Alchemy/QuickNode do NOT confirm chain 5031.
3. The deep-history retention worry collapses for the CHAIN (full archive at deploy block); it applies only to ORMI specifically (a deploy-and-observe probe in Phase 3, not a 320M-block-back query).

Purpose: land the provisional free-vs-paid verdict so INDEX-01 can be authored; satisfy ROADMAP Phase 1 SC#7(i)-(iv).
Output: the matrix YAML + the recommendation memo + two tests (schema conformance, sufficiency-bar logic).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md
@.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-CONTEXT.md
@.planning/scout/2026-05-29/event_count_addendum.md
@.planning/scout/2026-05-29/deployment_block.md
@.planning/scout/2026-05-29/rpc_capability_probe.md

<coherence_targets>
@../abrigo-x402/.planning/research/SUMMARY.md
@../abrigo-analytics/notes/somnia_cost_extraction.md
</coherence_targets>

<facts>
<!-- From 01-RESEARCH.md §Data-Sourcing Capability Matrix + §Standard Stack (paid fallbacks). Use EXACT values + provenance. -->
Fixed matrix schema (REQUIRED columns per row): source, capability, value, threshold, pass, source_url, utc_fetch_ts

MANDATORY capability rows (live-probed 2026-05-29T20:06Z–20:12Z unless vendor-doc):
  public-rpc | chain_id              | 0x13a7=5031                    | api.infra.mainnet.somnia.network | 2026-05-29T20:06Z
  public-rpc | eth_getLogs cap       | 1000 blocks                    | ″ | 2026-05-29T20:08Z
  public-rpc | eth_getBlockReceipts  | AVAILABLE                      | ″ | 2026-05-29T20:07Z
  public-rpc | archive depth         | FULL (correct at 283417317)    | ″ | 2026-05-29T20:09Z
  public-rpc | median block cadence  | ~100.7 ms/block                | ″ | 2026-05-29T20:09Z
  public-rpc | block.timestamp gran. | whole_second                   | ″ | 2026-05-29T20:07Z
  public-rpc | backfill_span         | 36286846 blocks (36287 windows)| derived | 2026-05-29T20:09Z
  proxy      | deployment_block      | 283417317                      | ″ | 2026-05-29T20:09Z
  blockscout | transactions_count anchor | 234999 (reference snapshot; re-query live at Phase-3 head) | explorer.somnia.network/api/v2 | scout 2026-05-25
  scout-addendum | event-structure provenance | events/tx 2.15(max11); topic0s 1:1:1; RequestCreated≈0.7×tx≈~165k; total≈~505k | .planning/scout/2026-05-29/event_count_addendum.md (endpoint api.infra.mainnet.somnia.network, head 319686151) | 2026-05-29
  ormi-free  | plan/cost             | Developer $0                   | ormilabs.com/docs/billing-and-pricing/pricing | 2026-05-29T20:11Z
  ormi-free  | entity cap            | 300000 (auto-upgrades to Production at cap) | ″ | 2026-05-29T20:11Z
  ormi-free  | subgraphs / sustained rps | 1 subgraph / 1 req-s        | ″ | 2026-05-29T20:11Z
  ormi-free  | deployment permission | DEPLOY allowed (credit card on file) | ″ | 2026-05-29T20:11Z
  ormi-free  | retention/history depth (DEEP-HISTORY probe) | UNVERIFIED by doc — deploy-and-observe at startBlock 283417317 in Phase 3 (42-day/36M span is small) | ormilabs.com/docs | 2026-05-29T20:10Z
  ormi-free  | backfill throttle (THROUGHPUT probe — distinct row/method) | 1 req/s sustained; one-shot backfill feasibility = Phase-3 deploy-probe | ″ | 2026-05-29T20:11Z
  ormi-free  | Somnia MAINNET deploy | docs reference mainnet+testnet; 0xGraph network-list showed only somnia-testnet — VERIFY at deploy | ″ | 2026-05-29T20:10Z
  coingecko  | OHLC ts convention    | candle-CLOSE                   | docs.coingecko.com/reference/coins-id-ohlc | 2026-05-29T20:10Z

PAID FALLBACK COST-OF-OWNERSHIP (all < $390/mo ceiling):
  Ormi Production       | $75/mo  | class (a) managed subgraph | re-author: ZERO (preserves mapping surface + SC#2 parity) | use if free 300k-entity cap exceeded OR 1 req/s too slow
  Ormi High Performance | $150/mo | class (a) managed subgraph | re-author: ZERO | only if 5 req/s (Production) insufficient — unlikely for M1
  GetBlock Starter      | $39/mo  | class (b) paid RPC/archive | re-author: INDEX-01 SUB-PLAN RE-AUTHOR (RPC-scan, not AssemblyScript) | cheapest paid; only if subgraph path fails entirely; eth_getBlockReceipts supported
  GetBlock Advanced/Pro | $159 / $399/mo | class (b) | sub-plan re-author | $399 Pro is AT/ABOVE $390 ceiling → forces re-scope conversation
  Self-hosted archive   | infra (VPS+storage) | class (c) | sub-plan re-author + ops burden | last resort; 42-day/36M span makes unnecessary
  Alchemy / QuickNode   | n/a     | class (b) | — | NEITHER publicly confirms chain 5031 → record "unavailable/unverified", do NOT assume support
  Protofire subgraph    | not retrieved | class (a) | ZERO | same subgraph-compatible class as Ormi; pricing row left for plan-phase if Ormi rejected

FOUR NUMERIC SUFFICIENCY BARS (ROADMAP SC#7(ii); any single free-tier fail ⇒ paid branch MANDATORY):
  bar_1_tx_coverage_and_structural_ratio: sampled distinct proxy-targeted tx_hash reconciles vs 234999 anchor (tolerance) AND per-topic counts track 2.15×/1:1:1/0.7× ratios (tolerance). PROVISIONAL pick: pass (sample-bounded).
  bar_2_timestamp_granularity: granularity ≤ native block cadence — whole_second acceptable BECAUSE (block_number, log_index) carries ordering. pass.
  bar_3_log_index_ordering: every tx-level multi-log block's log_index ordering preserved. pass (sample).
  bar_4_no_indexer_zero_rpc_positive: zero indexer=0 ∧ RPC>0 windows in sample. pass (sample).
  Verdict logic: free-tier SUFFICIENT iff ALL four pass. Any free fail ⇒ paid branch.

LEG-(b) TOLERANCE BAND (Claude's discretion — RESEARCH recommends + CONTEXT delegates): use a SAMPLE-CI BASIS, not a fixed ±X%. n=116 addendum sample: request-pair-per-tx 83/116≈0.716; Wilson 95% CI ≈ [0.627, 0.791]. Derive the leg-(b) ratio band from this CI; widen for the cross-epoch caveat (one 80k-block window, proxy 42 days old). State the basis in the matrix + memo. Leg-(b) is a NON-BLOCKING anomaly flag; legs (a)+(c) carry binding completeness (Phase 3).
</facts>

<deferred_items_to_pin>
<!-- The 3 review-deferred items from .planning/reviews/ROADMAP.md.review.2026-05-29.md to pin HERE -->
(a) leg-(b) structural-ratio tolerance band derivation basis → sample-CI from n=116 (above). PIN the basis + the computed band.
(b) "RequestCreated event" COLUMN definition → define the column semantics now (a RequestCreated row = the request-lifecycle event keyed by requestId); the topic0→event MAPPING is deferred to TOPIC-01/Phase 2 (keccak + shape match; the 3-topic/1120-byte 0xb623 is the leading candidate). Do NOT hard-code the scout's likely-inverted role labels.
(c) cross-epoch ratio-drift → note 42-day age = one impl epoch so far (low drift risk), but ratios are from ONE 80k-block window; flag for Phase-3 stratified (deploy/mid/head) re-measure per stratum.
</deferred_items_to_pin>
</context>

<tasks>

<task type="auto">
  <name>Task 1: research/data_sourcing_matrix.yaml — fixed-schema capability matrix + sufficiency bars</name>
  <read_first>
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Data-Sourcing Capability Matrix (the live-probed rows)
    - .planning/scout/2026-05-29/rpc_capability_probe.md + deployment_block.md (Plan 01 provenance sources)
    - The <facts> block (exact rows, values, provenance) + <deferred_items_to_pin>
  </read_first>
  <files>research/data_sourcing_matrix.yaml</files>
  <action>
    Write `research/data_sourcing_matrix.yaml` with the FIXED schema. Structure:
    1. `schema_version: v1` and a top-level `fixed_row_schema: [source, capability, value, threshold, pass, source_url, utc_fetch_ts]`.
    2. `capability_rows:` — a list, one mapping per MANDATORY row in <facts>. Every row MUST carry all 7 fixed-schema keys. For probe rows where there is no numeric threshold, set `threshold: n/a` and `pass: true|false|provisional`. Include: chain_id, eth_getLogs cap, eth_getBlockReceipts, archive depth, median cadence, block.timestamp granularity, backfill_span, deployment_block, transactions_count anchor (234999), the scout-addendum event-structure provenance row, all Ormi free-tier rows (cost, entity cap 300000, subgraphs/rps, deploy permission, deep-history retention [deploy-and-observe], backfill throttle [distinct row], mainnet-deploy verify), and the CoinGecko OHLC-convention row.
    3. `sufficiency_bars:` — a mapping of the four bars (bar_1..bar_4) each with `definition`, `value` (the provisional pass/fail), `pass: true`, and bar_1 carrying the leg-(b) `tolerance_band` sub-key with `basis: wilson_95ci_n116` and the computed `[0.627, 0.791]` interval + the cross-epoch widening note.
    4. `verdict:` — `selected_source: ormi-free-developer`, `selected_cost_usd_per_mo: 0`, `provisional: true`, `confirmed_in: phase-3`, `paid_fallback_subgraph_compatible: {vendor: ormi-production, cost_usd_per_mo: 75, reauthor: zero}`, `paid_fallback_nonsubgraph: {vendor: getblock-starter, cost_usd_per_mo: 39, reauthor: index01_subplan}`, `budget_ceiling_usd_per_mo: 390`, `all_paid_options_under_ceiling: true`, `sign_off_required: false` (free pick needs no sign-off per CONTEXT; only a forced paid crossing does).
    5. `deferred_items_pinned:` — the three pinned items (leg-b tolerance, RequestCreated column def, cross-epoch drift) per <deferred_items_to_pin>.
    Every paid-option entry references the cost-of-ownership in the memo. Do NOT include Alchemy/QuickNode as available — mark `unavailable_unverified`.
  </action>
  <verify>
    <automated>uv run python -c "import yaml; d=yaml.safe_load(open('research/data_sourcing_matrix.yaml')); rows=d['capability_rows']; assert all(all(k in r for k in ['source','capability','value','threshold','pass','source_url','utc_fetch_ts']) for r in rows), 'fixed-schema violation'; caps=[r['capability'] for r in rows]; assert any('getBlockReceipts' in c for c in caps); assert any('deployment_block' in str(r['value']) or '283417317' in str(r['value']) for r in rows); assert d['verdict']['selected_source']=='ormi-free-developer'; print('matrix-ok', len(rows), 'rows')"</automated>
  </verify>
  <acceptance_criteria>
    - `research/data_sourcing_matrix.yaml` loads as YAML; every `capability_rows` entry has all 7 fixed-schema keys (no row missing source_url or utc_fetch_ts).
    - Matrix contains a row whose capability mentions `eth_getBlockReceipts` and a row recording `283417317`.
    - `verdict.selected_source == 'ormi-free-developer'`, `verdict.budget_ceiling_usd_per_mo == 390`, `verdict.paid_fallback_subgraph_compatible.cost_usd_per_mo == 75`.
    - `sufficiency_bars` has bar_1..bar_4 each with `pass: true` and bar_1 carries the Wilson-CI tolerance band `[0.627, 0.791]`.
    - `deferred_items_pinned` present with the three items.
  </acceptance_criteria>
  <done>The machine-checkable capability matrix exists with full provenance, the four sufficiency bars encoded, the Ormi-free verdict, and the three deferred items pinned.</done>
</task>

<task type="auto">
  <name>Task 2: research/DATA_SOURCING.md — recommendation memo + coherence reconciliation + sign-off</name>
  <read_first>
    - research/data_sourcing_matrix.yaml (Task 1 — the memo narrates it)
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Coherence-Check Construction (the exact reconciliation language)
    - ../abrigo-x402/.planning/research/SUMMARY.md (budget-gate framing) + ../abrigo-analytics/notes/somnia_cost_extraction.md (stop-gap pricing, no-oracle)
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-CONTEXT.md §decisions (budget ceiling, justified-departure, sign-off posture)
  </read_first>
  <files>research/DATA_SOURCING.md</files>
  <action>
    Write `research/DATA_SOURCING.md` as a RECOMMENDATION MEMO the user signs off on (CONTEXT: structure as a memo with cheapest-sufficient flagged + cost-of-ownership table + sign-off line). Sections:
    1. `## Supersedes` — the three overturned premises (backfill 36.3M not ~320M; Ormi preferred not mandatory; retention worry collapses to Ormi-only). Cite 01-RESEARCH.md + the scout archive.
    2. `## Verdict (PROVISIONAL)` — recommend **Ormi free Developer ($0)** as INDEX-01's provisional host; provisional-pending-Phase-3-backfill-confirmation. State the one caveat: if Phase-1 entity-count modeling (Task: see RESEARCH Open Q #1) shows ~165k RequestCreated + responses-child rows exceed the 300k cap, the provisional pick becomes **Ormi Production $75/mo** (still subgraph-compatible, zero re-author, under ceiling) and is surfaced for sign-off BEFORE backfill; disable free-tier auto-upgrade at signup regardless (PITFALLS / RESEARCH Pitfall 5).
    3. `## Four Numeric Sufficiency Bars` — restate the four bars + their provisional pass status + that any single free fail ⇒ paid branch mandatory. Reference the matrix keys, not prose.
    4. `## Cost-of-Ownership Table` — all paid classes from <facts>: Ormi Production $75 (zero re-author), Ormi High-Perf $150 (zero), GetBlock Starter $39 (INDEX-01 sub-plan re-author), GetBlock Advanced/Pro $159/$399 ($399 forces re-scope), self-hosted (last resort), Alchemy/QuickNode (unavailable/unverified for chain 5031), Protofire (subgraph-compatible, price TBD if Ormi rejected). Each row: price, class, INDEX-01 re-author cost, when-to-use. State all are < $390/mo except GetBlock Pro $399.
    5. `## Coherence Reconciliation (SC#7(iv) — a REAL reconciliation, not a citation)` — use the exact framing from RESEARCH §Coherence-Check Construction + CONTEXT "real-or-nothing": x402's free-tier discipline is a paid-API BUDGET gate (90k-cap cost-ledger); this DATA-SOURCE-01 is a COMPLETENESS-sufficiency gate; x402's "settlement-infra modeled-not-paid" caveat has NO analogue (SOMI data is real-or-nothing — paying for a real feed is a **JUSTIFIED DEPARTURE**, a different category, not a violation); state explicitly that the free-vs-paid choice does NOT change any SHARED-SCHEMA-01 intersection assumption (intersection columns are source-agnostic). Cross-check abrigo-analytics no-oracle + stop-gap findings (both stand; FX-01 off-chain CoinGecko is consistent). Must NOT be a one-line "consistent ✓".
    6. `## Deferred Items Pinned` — leg-(b) tolerance (Wilson CI n=116), RequestCreated column definition (column semantics now, topic0 mapping → TOPIC-01), cross-epoch drift (Phase-3 stratified re-measure).
    7. `## Sign-Off` — a checkbox line: "[ ] User approves the PROVISIONAL Ormi-free-Developer selection (free → no spend → no sign-off strictly required per CONTEXT; this line records acknowledgement). Paid crossing (Ormi Production $75/mo if cap exceeded) requires explicit sign-off before provisioning."
  </action>
  <verify>
    <automated>grep -q "JUSTIFIED DEPARTURE" research/DATA_SOURCING.md && grep -q "Supersedes" research/DATA_SOURCING.md && grep -q "Ormi free Developer\|ormi-free-developer\|Ormi Free Developer" research/DATA_SOURCING.md && grep -q "390" research/DATA_SOURCING.md && grep -qi "sign-off\|sign off" research/DATA_SOURCING.md && grep -q "COMPLETENESS" research/DATA_SOURCING.md && grep -q "BUDGET" research/DATA_SOURCING.md && test $(wc -l < research/DATA_SOURCING.md) -gt 60</automated>
  </verify>
  <acceptance_criteria>
    - `research/DATA_SOURCING.md` contains `JUSTIFIED DEPARTURE`, `Supersedes`, the Ormi-free recommendation, `390`, a sign-off line, AND both `COMPLETENESS` and `BUDGET` (the real reconciliation, not a one-liner).
    - The cost-of-ownership table lists Ormi Production $75, GetBlock $39, and marks Alchemy/QuickNode unavailable/unverified.
    - File is > 60 lines (a real memo, not a stub).
    - The coherence section states the free-vs-paid choice does NOT change SHARED-SCHEMA-01 intersection assumptions.
  </acceptance_criteria>
  <done>The recommendation memo is written: provisional Ormi-free pick, full cost-of-ownership, the real budget-vs-completeness reconciliation, and a sign-off line. DATA-SOURCE-01 verdict lands → INDEX-01 unblocked.</done>
</task>

<task type="auto">
  <name>Task 3: Matrix schema-conformance + sufficiency-bar logic tests</name>
  <read_first>
    - research/data_sourcing_matrix.yaml (Task 1)
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Validation Architecture (test_data_sourcing_matrix + test_sufficiency_bars rows)
  </read_first>
  <files>tests/test_data_sourcing_matrix.py, tests/test_sufficiency_bars.py</files>
  <action>
    1. `tests/test_data_sourcing_matrix.py`:
       - `test_fixed_schema`: every `capability_rows` entry has exactly the 7 fixed-schema keys (source, capability, value, threshold, pass, source_url, utc_fetch_ts).
       - `test_provenance_complete`: every row's `source_url` and `utc_fetch_ts` are non-empty.
       - `test_mandatory_rows_present`: assert rows exist for eth_getLogs cap, eth_getBlockReceipts, deployment_block, transactions_count anchor, the scout-addendum provenance, and all Ormi free-tier rows (cost, entity cap, deploy permission, deep-history retention, backfill throttle).
       - `test_verdict`: `verdict.selected_source == 'ormi-free-developer'`, all paid options ≤ 390 except the explicitly-flagged GetBlock Pro $399.
       Use conftest `load_yaml` + `research_dir`.
    2. `tests/test_sufficiency_bars.py`:
       - `test_four_bars_have_passfail`: bar_1..bar_4 each have a boolean `pass`.
       - `test_free_sufficient_iff_all_pass`: implement a tiny pure function `free_tier_sufficient(bars) -> bool` (all four pass) in the test (or import from a small `probes/sufficiency.py` helper if you prefer) and assert: all-pass ⇒ True; flipping ANY one bar to fail ⇒ False (free fail flips to paid branch). This encodes the "any single bar fail ⇒ paid mandatory" rule.
       - `test_leg_b_tolerance_basis`: bar_1's tolerance band basis is `wilson_95ci_n116` and the interval is `[0.627, 0.791]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_data_sourcing_matrix.py tests/test_sufficiency_bars.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_data_sourcing_matrix.py tests/test_sufficiency_bars.py -x -q` exits 0.
    - `test_free_sufficient_iff_all_pass` proves flipping any single bar to fail returns False (paid branch).
    - `test_fixed_schema` enforces exactly the 7 fixed-schema keys per row.
  </acceptance_criteria>
  <done>The matrix is machine-validated against its fixed schema with full provenance, and the four-bar verdict logic is test-enforced (any single free-tier fail flips to paid).</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -q` green (adds matrix + sufficiency-bar tests).
- DATA_SOURCING.md is a real memo with the JUSTIFIED-DEPARTURE reconciliation + sign-off; matrix carries full provenance.
- Verdict: provisional Ormi-free, all paid options under $390 (except flagged GetBlock Pro), three deferred items pinned.
- Supersedes-note records the 36.3M-block / deploy-block-283417317 / Ormi-preferred-not-mandatory corrections.
</verification>

<success_criteria>
DATA-SOURCE-01 discharged: the provisional free-vs-paid verdict lands in `research/DATA_SOURCING.md` + `research/data_sourcing_matrix.yaml` with numeric sufficiency bars, full provenance, a real coherence reconciliation, and a sign-off line — Phase 3 INDEX-01 is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-03-SUMMARY.md`.
</output>

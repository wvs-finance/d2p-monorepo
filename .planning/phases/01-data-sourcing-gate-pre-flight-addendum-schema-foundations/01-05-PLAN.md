---
phase: 01-data-sourcing-gate-pre-flight-addendum-schema-foundations
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - schemas/abrigo_cost_panel_intersection_v1.md
  - schemas/abrigo_cost_panel_intersection_v1.json
  - schemas/abrigo_cost_panel_k_ai_extensions_v1.md
  - tests/test_shared_schema_json.py
  - tests/test_schema_consistency.py
autonomous: true
requirements: [SHARED-SCHEMA-01]
must_haves:
  truths:
    - "A consumer can load schemas/abrigo_cost_panel_intersection_v1.json as valid JSON-Schema (draft 2020-12) and map each intersection column to its polars dtype without inferring from data."
    - "The intersection schema is marked v1-K_AI-anchored with a documented breakage budget for when K_D (abrigo-x402) ships."
    - "The K_AI extension schema carries the sidecar columns joined on (chain_id, tx_hash); the intersection PK + the extension join key are consistent across the .md and .json artifacts and consistent with EVENT-01's dtypes."
  artifacts:
    - path: "schemas/abrigo_cost_panel_intersection_v1.json"
      provides: "machine-loadable JSON-Schema for the strict intersection columns"
      contains: "2020-12"
    - path: "schemas/abrigo_cost_panel_intersection_v1.md"
      provides: "prose intersection schema, v1-K_AI-anchored + breakage budget"
      contains: "v1-K_AI-anchored"
    - path: "schemas/abrigo_cost_panel_k_ai_extensions_v1.md"
      provides: "K_AI sidecar extension columns joined on (chain_id, tx_hash)"
      contains: "(chain_id, tx_hash)"
  key_links:
    - from: "tests/test_shared_schema_json.py"
      to: "schemas/abrigo_cost_panel_intersection_v1.json"
      via: "jsonschema meta-validation + polars dtype map load"
      pattern: "2020-12"
    - from: "tests/test_schema_consistency.py"
      to: "intersection .md/.json + k_ai_extensions .md + event_schema_v1.md"
      via: "PK + join-key + dtype consistency across artifacts"
      pattern: "chain_id"
---

<objective>
SHARED-SCHEMA-01 — commit the joint-analysis schema: `schemas/abrigo_cost_panel_intersection_v1.{md,json}` (strict intersection columns, machine-loadable, marked v1-K_AI-anchored with a documented breakage budget) + `schemas/abrigo_cost_panel_k_ai_extensions_v1.md` (K_AI sidecar). Joint-analysis consumers in `abrigo-analytics` join on the intersection PK. These artifacts are read by PANEL-01 (Phase 5), so they must precede it and stay consistent with EVENT-01's dtypes (Plan 04).

Supersedes (note in the intersection .md §Supersedes): per 01-RESEARCH.md §Coherence-Check Construction, the free-vs-paid data-source choice does NOT change any intersection assumption — the intersection columns are source-agnostic (a request's gross_cost_native / block_ts_utc are identical whether indexed by Ormi or RPC-scan). Record this explicitly so a later source swap does not trigger a schema-version bump.

Purpose: lock the shared joint-analysis contract before PANEL-01; keep K_AI-anchored with an explicit K_D breakage budget.
Output: intersection .md + .json, K_AI extension .md, two tests (JSON-Schema validity, cross-artifact consistency).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md
@schemas/event_schema_v1.md

<interfaces>
<!-- From 01-RESEARCH.md §Schema Artifacts (SHARED-SCHEMA-01). Columns + dtypes MUST be consistent with event_schema_v1.md (Plan 04). -->
INTERSECTION columns (abrigo_cost_panel_intersection_v1; mark v1-K_AI-anchored):
  request_id        (uint256 decimal string, pl.Utf8 ONLY — NEVER Int64, NEVER Decimal(38,0): keccak-derived uint256 = up to 78 digits > Decimal128's 38)   NOT NULL  [PK part]
  tx_hash           (Utf8 / Binary(32))                       NOT NULL  [PK part]
  block_number      (UInt64)                                  NOT NULL
  block_ts_utc      (Datetime us UTC — coarse secondary)      NOT NULL
  chain_id          (Int64)                                   NOT NULL
  gross_cost_native (wei: pl.Utf8 decimal string OR pl.Decimal(38,0) — SOMI wei ≈ 1e16–1e17 ≈ 17–18 digits, provably ≤ 38)      NOT NULL
  gross_cost_usd    (Float64)                                 nullable (FX may be stale)
  net_cost_usd      (Float64)                                 nullable
  fx_rate           (Float64)                                 nullable
  fx_source_ts_utc  (Datetime us UTC)                         nullable
  schema_version    (Utf8)                                    NOT NULL
Intersection PK: (chain_id, tx_hash, request_id)  — joint-analysis consumers join here.

K_AI EXTENSION columns (abrigo_cost_panel_k_ai_extensions_v1; sidecar, joined on (chain_id, tx_hash)):
  agent_class, implementation_address, subcommittee_size,
  per_agent_budget_native (wei: pl.Utf8 OR pl.Decimal(38,0), ≤ 38 digits), request_id_kai (pl.Utf8 ONLY — uint256 id, like request_id), agent_class_keccak, agent_class_string
Extension join key: (chain_id, tx_hash) → intersection.

JSON-Schema: draft 2020-12 ($schema, type:object, properties with type/format, required array = non-nullable columns).
Each JSON property maps the column to its polars dtype string so pl.scan_parquet(..., schema=...) reads explicitly (PITFALLS E1 — never infer; null-only early-partition implementation_address would infer Null-type and break later partitions).
DTYPE SCOPE RULE (B1 — consistent with EVENT-01): uint256 ID columns (request_id, request_id_kai) are pl.Utf8 ONLY — NEVER Int64 AND NEVER Decimal(38,0) (keccak-derived uint256 = up to 78 digits > Decimal128's 38-digit cap → BindingsError). Wei amounts (gross_cost_native, per_agent_budget_native) may be pl.Utf8 decimal-string OR pl.Decimal(38,0) (provably ≤ 38 digits), never Int64. In the JSON-Schema, the id columns' `x-polars-dtype` MUST be the Utf8/string dtype (never Decimal), wei columns may be either.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schemas/abrigo_cost_panel_intersection_v1.{md,json} — intersection schema + machine-loadable JSON-Schema</name>
  <read_first>
    - schemas/event_schema_v1.md (Plan 04 — dtypes must be consistent)
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Schema Artifacts (intersection columns + JSON-Schema pattern)
    - .planning/REQUIREMENTS.md SHARED-SCHEMA-01 (v1-K_AI-anchored + breakage budget)
    - The <interfaces> block (exact intersection columns + PK)
  </read_first>
  <files>schemas/abrigo_cost_panel_intersection_v1.md, schemas/abrigo_cost_panel_intersection_v1.json</files>
  <action>
    1. `schemas/abrigo_cost_panel_intersection_v1.md`:
       - `## Supersedes` — the source-agnostic note (free-vs-paid choice does NOT change intersection assumptions; a source swap does NOT bump the schema version).
       - `## Intersection columns` — the column table from <interfaces> (column | polars dtype | parquet | nullability | note), consistent with event_schema_v1.md. uint256 ID columns (request_id) as pl.Utf8 ONLY (never Int64, never Decimal(38,0) — uint256 = up to 78 digits > Decimal128's 38); wei amounts (gross_cost_native) as pl.Utf8 OR pl.Decimal(38,0) (≤ 38 digits). State the scope sentence verbatim.
       - `## Intersection PK` — `(chain_id, tx_hash, request_id)`; state joint-analysis consumers in abrigo-analytics join here.
       - `## v1-K_AI-anchored + breakage budget` — mark the schema `v1-K_AI-anchored`; document the breakage budget for when K_D (abrigo-x402) ships: which columns are stable contracts, what a K_D-driven change would version-bump, and that cross-version reads require an explicit version filter (PITFALLS E4).
    2. `schemas/abrigo_cost_panel_intersection_v1.json`: JSON-Schema draft 2020-12 — `$schema: "https://json-schema.org/draft/2020-12/schema"`, `type: object`, `title: abrigo_cost_panel_intersection_v1`, `properties` typing each intersection column (string for uint256-decimal/hashes, integer for block_number/chain_id, number for floats, string+format date-time for timestamps), a custom `x-polars-dtype` annotation per property mapping to the polars dtype string. For request_id the `x-polars-dtype` MUST be `Utf8` (NEVER `Decimal(38,0)` — uint256 id overflows Decimal128); for gross_cost_native it may be `Utf8` or `Decimal(38,0)`. `required` lists the NOT-NULL columns (request_id, tx_hash, block_number, block_ts_utc, chain_id, gross_cost_native, schema_version). Add `x-schema-anchor: "v1-K_AI-anchored"`.
  </action>
  <verify>
    <automated>uv run python -c "import json,jsonschema; s=json.load(open('schemas/abrigo_cost_panel_intersection_v1.json')); assert s['\$schema']=='https://json-schema.org/draft/2020-12/schema', s.get('\$schema'); jsonschema.Draft202012Validator.check_schema(s); req=set(s['required']); need={'request_id','tx_hash','block_number','block_ts_utc','chain_id','gross_cost_native','schema_version'}; assert need<=req, need-req; assert all('x-polars-dtype' in p for p in s['properties'].values()); print('intersection-json-ok')" && grep -q "v1-K_AI-anchored" schemas/abrigo_cost_panel_intersection_v1.md && grep -qi "breakage budget" schemas/abrigo_cost_panel_intersection_v1.md</automated>
  </verify>
  <acceptance_criteria>
    - `schemas/abrigo_cost_panel_intersection_v1.json` is a valid draft-2020-12 JSON-Schema (`check_schema` passes); `required` includes all 7 non-nullable columns; every property carries `x-polars-dtype`.
    - The `.md` contains `v1-K_AI-anchored`, `breakage budget`, the PK `(chain_id, tx_hash, request_id)`, and the source-agnostic Supersedes note.
    - The request_id id column's `x-polars-dtype` is `Utf8` (never Int64, never Decimal(38,0)); gross_cost_native is `Utf8` or `Decimal(38,0)` (≤ 38 digits). Documented verbatim.
  </acceptance_criteria>
  <done>The intersection schema is committed in both prose and machine-loadable JSON-Schema, v1-K_AI-anchored with a breakage budget.</done>
</task>

<task type="auto">
  <name>Task 2: schemas/abrigo_cost_panel_k_ai_extensions_v1.md — K_AI sidecar extension schema</name>
  <read_first>
    - schemas/abrigo_cost_panel_intersection_v1.md (Task 1 — join key consistency)
    - schemas/event_schema_v1.md (Plan 04 — agent_class_keccak/string + implementation_address dtypes)
    - The <interfaces> block (K_AI extension columns + join key)
  </read_first>
  <files>schemas/abrigo_cost_panel_k_ai_extensions_v1.md</files>
  <action>
    Write `schemas/abrigo_cost_panel_k_ai_extensions_v1.md`:
    - `## K_AI extension columns` — the table from <interfaces>: agent_class, implementation_address, subcommittee_size, request_id_kai (uint256 id — pl.Utf8 ONLY, never Int64, never Decimal(38,0)), per_agent_budget_native (wei — pl.Utf8 OR pl.Decimal(38,0), ≤ 38 digits, never Int64), agent_class_keccak, agent_class_string. Dtypes consistent with event_schema_v1.md.
    - `## Join key` — `(chain_id, tx_hash)` → intersection. State this is a SIDECAR (K_AI-specific columns not in the strict intersection), and that responses remain a separate child table (per EVENT-01 KPD-03 / Phase 5 KPD-PANEL-JOIN — responses are NOT exploded into the panel primary row surface).
    - `## Agent classes` — the three canonical classes + absolute SOMI/call prices: json-fetch 0.03, llm-inference 0.07, llm-parse-website 0.10.
    - Mark `v1-K_AI-anchored`.
  </action>
  <verify>
    <automated>grep -q "(chain_id, tx_hash)" schemas/abrigo_cost_panel_k_ai_extensions_v1.md && grep -q "agent_class_keccak" schemas/abrigo_cost_panel_k_ai_extensions_v1.md && grep -q "per_agent_budget_native" schemas/abrigo_cost_panel_k_ai_extensions_v1.md && grep -q "json-fetch" schemas/abrigo_cost_panel_k_ai_extensions_v1.md && grep -q "v1-K_AI-anchored" schemas/abrigo_cost_panel_k_ai_extensions_v1.md</automated>
  </verify>
  <acceptance_criteria>
    - `schemas/abrigo_cost_panel_k_ai_extensions_v1.md` contains the join key `(chain_id, tx_hash)`, all extension columns (incl. agent_class_keccak, per_agent_budget_native), the three agent classes with prices, and `v1-K_AI-anchored`.
    - per_agent_budget_native is documented as wei (pl.Utf8 OR pl.Decimal(38,0), ≤ 38 digits), never Int64; request_id_kai is pl.Utf8 ONLY (never Decimal(38,0) — uint256 id).
  </acceptance_criteria>
  <done>The K_AI sidecar extension schema is committed, joined on (chain_id, tx_hash), consistent with EVENT-01.</done>
</task>

<task type="auto">
  <name>Task 3: JSON-Schema validity test + cross-artifact consistency test</name>
  <read_first>
    - schemas/abrigo_cost_panel_intersection_v1.{md,json} + abrigo_cost_panel_k_ai_extensions_v1.md (Tasks 1-2)
    - schemas/event_schema_v1.md (Plan 04 — dtype source of truth)
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Validation Architecture (test_shared_schema_json + test_schema_consistency rows)
  </read_first>
  <files>tests/test_shared_schema_json.py, tests/test_schema_consistency.py</files>
  <action>
    1. `tests/test_shared_schema_json.py`:
       - `test_valid_draft_2020_12`: load the intersection JSON, assert `$schema` is draft 2020-12, run `jsonschema.Draft202012Validator.check_schema`.
       - `test_polars_dtype_map` (B1 — RUNTIME, not grep-only): every property carries `x-polars-dtype`; assert request_id's `x-polars-dtype` is `Utf8` (NOT Int64, NOT Decimal); assert gross_cost_native's is `Utf8` or `Decimal(38,0)` (not Int64). THEN a runtime polars round-trip proving the chosen id dtype actually holds a uint256: `import polars as pl; big = str(2**256 - 1); assert pl.Series([big], dtype=pl.Utf8).to_list()[0] == big`; AND `import pytest; with pytest.raises(Exception): pl.Series([2**256 - 1], dtype=pl.Decimal(38, 0))` (proves Decimal(38,0) cannot hold the id → the JSON-Schema choosing Utf8 for request_id is empirically correct); AND a wei-sized sanity check `pl.Series([10**17], dtype=pl.Decimal(38, 0))` constructs without error.
       - `test_required_nonnullable`: `required` contains the 7 NOT-NULL intersection columns.
    2. `tests/test_schema_consistency.py`:
       - `test_intersection_pk_consistent`: the PK `(chain_id, tx_hash, request_id)` appears in both the intersection .md and is reflected in the JSON `required`.
       - `test_extension_join_key`: `(chain_id, tx_hash)` is the documented K_AI extension join key in the extension .md.
       - `test_dtype_consistency_with_event_schema`: columns shared between event_schema_v1.md and the intersection (block_number, block_ts_utc, chain_id, tx_hash) carry the SAME dtype/nullability framing across both files (grep-level consistency: block_number UInt64 + NOT NULL, block_ts_utc coarse-secondary, uint256-not-Int64 in both).
    Consume the conftest fixtures `schemas_dir` (returns `Path`), `load_json` (returns a callable `fn(path)`), and `read_text` (returns a callable `fn(path)`) as TEST PARAMETERS (fixture-returning-callable contract, set in Plan 01 Task 1).
  </action>
  <verify>
    <automated>uv run pytest tests/test_shared_schema_json.py tests/test_schema_consistency.py -x -q && uv run pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_shared_schema_json.py tests/test_schema_consistency.py -x -q` exits 0.
    - `test_polars_dtype_map` proves request_id's x-polars-dtype is Utf8 (not Int64, not Decimal) and runs the polars round-trip: a 78-digit string survives Utf8, `pl.Series([2**256-1], dtype=pl.Decimal(38,0))` RAISES, and a wei-sized 10**17 constructs in Decimal(38,0).
    - `test_dtype_consistency_with_event_schema` proves the intersection schema is consistent with EVENT-01's dtypes.
    - Full suite `uv run pytest tests/ -q` is green (all Phase-1 plans' tests pass together).
  </acceptance_criteria>
  <done>The shared schema is machine-validated (draft 2020-12), and PK / join-key / dtype consistency across the intersection, extension, and EVENT-01 artifacts is test-enforced. Full Phase-1 suite green.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -q` green across the WHOLE Phase-1 suite (this is the last plan; the full suite must pass).
- Intersection JSON is valid draft-2020-12 with polars dtype map; v1-K_AI-anchored + breakage budget documented.
- K_AI extension joined on (chain_id, tx_hash); dtypes consistent with EVENT-01.
- Source-agnostic Supersedes note recorded (free-vs-paid swap does not bump the schema).
</verification>

<success_criteria>
SHARED-SCHEMA-01 discharged: the v1-K_AI-anchored intersection schema (prose + machine-loadable JSON-Schema) and the K_AI sidecar extension are committed, consistent with EVENT-01, with a documented K_D breakage budget — read-ready for PANEL-01 (Phase 5).
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-05-SUMMARY.md`.
</output>

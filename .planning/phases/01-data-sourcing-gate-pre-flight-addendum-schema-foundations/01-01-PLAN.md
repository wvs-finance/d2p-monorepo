---
phase: 01-data-sourcing-gate-pre-flight-addendum-schema-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/test_scout_archive.py
  - probes/somnia_rpc.py
  - probes/blockscout.py
  - .planning/scout/2026-05-29/rpc_capability_probe.md
  - .planning/scout/2026-05-29/deployment_block.md
  - .planning/scout/2026-05-29/event_shapes_onchain.md
  - .planning/scout/2026-05-29/PROVENANCE.sha256
  - .planning/scout/README.md
autonomous: true
requirements: [DATA-SOURCE-01]
must_haves:
  truths:
    - "A consumer can run `uv run pytest tests/ -q` on the greenfield repo and the suite executes (test infra exists)."
    - "A consumer can read the consolidated scout archive and find every load-bearing live-probe fact (deploy block 283417317, getBlockReceipts available, whole-second timestamp, archive depth, the three on-chain event shapes) with source_url + utc_fetch_ts."
    - "A consumer can verify each archived raw probe response against the sha256 manifest (provenance is tamper-evident)."
    - "Downstream Phase-1 tasks (DATA-SOURCE-01, pre-flight verdicts, EVENT-01) can cite the archive files instead of re-probing."
  artifacts:
    - path: "pyproject.toml"
      provides: "uv-managed Python 3.12 project + pytest/jsonschema/pyyaml/polars dev deps"
      contains: "pytest"
    - path: "tests/conftest.py"
      provides: "shared fixtures: schema-file loaders, yaml validator, scout-archive path helpers"
    - path: "probes/somnia_rpc.py"
      provides: "hardened reusable RPC probe (eth_getLogs window-scan, getBlockReceipts check, getStorageAt, getTransactionByHash) — seeded from scout /tmp tooling"
    - path: ".planning/scout/2026-05-29/rpc_capability_probe.md"
      provides: "recorded RPC capability facts with provenance (chain_id, head, getLogs cap, getBlockReceipts, archive depth, cadence, whole-second timestamp, baseFee, proxy/impl slots)"
      contains: "283417317"
    - path: ".planning/scout/2026-05-29/event_shapes_onchain.md"
      provides: "the three live on-chain event topic0 shapes (#topics + data bytes) — the inversion evidence for TOPIC-01"
      contains: "0xb62339927ed9948fd837358a55f5b9a824f7b047043faece66965593ed726889"
    - path: ".planning/scout/2026-05-29/PROVENANCE.sha256"
      provides: "sha256 of every raw probe response file in the archive"
  key_links:
    - from: "tests/test_scout_archive.py"
      to: ".planning/scout/2026-05-29/*.md"
      via: "file-presence + content-value assertions"
      pattern: "283417317"
    - from: ".planning/scout/2026-05-29/PROVENANCE.sha256"
      to: "raw probe response files"
      via: "sha256sum -c"
      pattern: "sha256"
---

<objective>
Stand up the greenfield test infrastructure (Wave 0) and consolidate the scout provenance archive (KPD-16 — ORDERED FIRST in Phase 1). Every downstream Phase-1 task assumes the scout artifacts are trustworthy and pinned; this plan makes that true. The live-probe facts already exist in 01-RESEARCH.md (re-probed 2026-05-29T20:06Z–20:12Z) — this plan hardens them into committed, sha256-stamped archive files plus reusable probe scripts, and builds the pytest harness every later task verifies against.

Supersedes: 01-RESEARCH.md corrects the upstream `.planning/scout/2026-05-25/` references — that directory was never committed (only `2026-05-29/event_count_addendum.md` exists). This plan consolidates the archive under `.planning/scout/2026-05-29/` and adds a `.planning/scout/README.md` index. The ROADMAP/PROJECT references to `2026-05-25/` paths are reconciled here: the canonical archive is `2026-05-29/`.

Purpose: KPD-16 provenance discipline before any verdict rests on the data; Wave 0 test infra so every later task has a runnable `<automated>` verify.
Output: pyproject.toml, tests/ scaffold, probes/ scripts, consolidated `.planning/scout/2026-05-29/` archive with sha256 manifest.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md
@.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-CONTEXT.md
@.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-VALIDATION.md
@.planning/scout/2026-05-29/event_count_addendum.md

<facts>
<!-- All live-probed 2026-05-29T20:06Z–20:12Z against https://api.infra.mainnet.somnia.network/ per 01-RESEARCH.md §Data-Sourcing Capability Matrix. Use these EXACT values; do not re-derive. -->
RPC endpoint:               https://api.infra.mainnet.somnia.network/  (second working endpoint: somnia.publicnode.com)
chain_id:                   0x13a7 = 5031
client_version:             somnia-16638fbbd16be8b-release
eth_getLogs cap:            1000 blocks (error "block range exceeds 1000" on 1001)
eth_getBlockReceipts:       AVAILABLE (returns populated receipt list)
archive depth:              FULL ARCHIVE — eth_getLogs AND eth_getStorageAt correct at deploy block 283417317
median block cadence:       ~100.7 ms/block over deploy→head span (refines scout ~72ms)
block.timestamp granularity: whole_second (consecutive blocks 1780085231 / 1780085231 / 1780085232)
baseFeePerGas:              6 gwei = 6000000000 wei (floor non-binding this window)
proxy:                      0x5E5205CF39E766118C01636bED000A54D93163E6
proxy deployment_block:     283417317 (creation tx 0x36596e1854e413681992166d5d55552d999f820eafdf6f8fe07afef1e66e8b0a, 2026-04-17T13:18:10Z)
proxy creator EOA:          0x320362C7...fdE88936
backfill_span:              36,286,846 blocks ≈ 36,287 getLogs windows (NOT ~320M)
proxy IMPLEMENTATION_SLOT:  ...9af59c5683...3edd  (confirms impl 0x9AF59C5683bb8686596B0D56e4F67655C6B73EdD)
proxy ADMIN_SLOT:           0x empty (UUPS-style)
proxy BEACON_SLOT:          0x empty
impl:                       0x9AF59C5683bb8686596B0D56e4F67655C6B73EdD (18507 bytes)
impl BEACON_SLOT (0xa3f0ad74e5423aebfd80d3ef4346578335a9a72aeaee59ff6cb3582b35133d50):  0x empty
impl IMPLEMENTATION_SLOT:   0x empty
impl EIP-2535 diamond storage (0xc8fcad8db84d3cc18b4c41d551ea0ee66dd599cde068d998e57d5e09332c131c): 0x empty
Blockscout tx anchor:       234999 transactions @ head 316099291 (reference snapshot 2026-05-25; re-query live at Phase-3 head)
Blockscout v2 REST:         explorer.somnia.network/api/v2  (rate-limits: 429 after ~2 rapid calls — space requests)

On-chain event shapes (live, against proxy logs):
  0xb62339927ed9948fd837358a55f5b9a824f7b047043faece66965593ed726889  | 3 topics | 1120 data bytes | 2 indexed + large dynamic payload  (matches RequestCreated shape)
  0x5c090ef48df2b4d8a01bd0639355d62c318b623aed749bdd12325f789e37a2cf  | 2 topics |   32 data bytes | 1 indexed + 1 word
  0x65db1ef5b3bcd84fe4fb8dbbe1cadc9fe6643bb261ab2e01d65c281c3d466af2  | 2 topics |   32 data bytes | 1 indexed + 1 word
NOTE: scout's role labels are likely INVERTED (TOPIC-01/Phase 2 resolves definitively). Record SHAPES, not roles.
</facts>

<reusable_assets>
Scout probe patterns to harden into probes/somnia_rpc.py + probes/blockscout.py:
- eth_getLogs window-scan (1000-block windows, address-filtered)
- eth_getTransactionReceipt / eth_getTransactionByHash structure decode
- eth_getStorageAt (slot reads for beacon/diamond/impl)
- eth_getBlockReceipts availability check
- Blockscout v2 paginated /addresses/{addr}/counters + /logs (sequential, spaced for 429)
Provenance-note format: source_url + utc_fetch_ts per fact (abrigo-analytics/notes/somnia_cost_extraction.md convention).
</reusable_assets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — uv project + pytest harness + shared fixtures</name>
  <read_first>
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Validation Architecture (framework, commands, Wave 0 Gaps)
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-VALIDATION.md (test infra table)
    - ../abrigo-analytics/pyproject.toml (if present — match the uv/polars convention)
  </read_first>
  <files>pyproject.toml, tests/conftest.py, tests/__init__.py</files>
  <action>
    Create a uv-managed Python 3.12 project (greenfield — no infra exists). Concrete steps:
    1. `pyproject.toml`: project name `abrigo-somnia`, `requires-python = ">=3.12"`, `[dependency-groups] dev = ["pytest>=8", "jsonschema>=4.20", "pyyaml>=6", "polars>=1.20"]` (match abrigo-analytics: polars >=1.20). Add `[tool.pytest.ini_options] testpaths = ["tests"]`, `addopts = "-q"`. Do NOT add watch-mode plugins (pytest-watch) — VALIDATION.md forbids watch-mode flags.
    2. `tests/conftest.py`: shared fixtures —
       - `scout_dir` fixture returning `Path(".planning/scout/2026-05-29")`.
       - `schemas_dir` fixture returning `Path("schemas")`.
       - `research_dir` fixture returning `Path("research")`.
       - `load_yaml(path)` helper (yaml.safe_load).
       - `load_json(path)` helper (json.load).
       - `read_text(path)` helper.
    3. `tests/__init__.py`: empty.
    4. Install: `uv sync` (or `uv add --dev pytest jsonschema pyyaml polars` if no lock). Verify the env builds.
    These fixtures are consumed by every later Phase-1 test (test_scout_archive, test_data_sourcing_matrix, test_event_schema, test_shared_schema_json, etc.).
  </action>
  <verify>
    <automated>uv run pytest tests/ -q --collect-only 2>&1 | tail -5; uv run python -c "import pytest, jsonschema, yaml, polars; print('deps-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` exists and contains `pytest` and `polars>=1.20`.
    - `uv run python -c "import pytest, jsonschema, yaml, polars"` prints `deps-ok` (exit 0).
    - `uv run pytest tests/ -q --collect-only` exits 0 (harness collects, even with zero tests yet).
    - `tests/conftest.py` contains `def scout_dir` and `def load_yaml` and `def load_json`.
    - `grep -c "pytest-watch\|--watch\|-f " pyproject.toml` returns 0 (no watch-mode).
  </acceptance_criteria>
  <done>Greenfield repo has a runnable uv+pytest env with shared fixtures; later tasks can write tests against it.</done>
</task>

<task type="auto">
  <name>Task 2: KPD-16 — harden probe scripts + consolidate scout RPC/event/deployment archive</name>
  <read_first>
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Data-Sourcing Capability Matrix + §On-Chain Event Shapes vs Interface + §Code Examples
    - .planning/scout/2026-05-29/event_count_addendum.md (existing archive file — the format/provenance template to match)
    - The <facts> block in this plan's <context> (the exact values to record)
  </read_first>
  <files>probes/somnia_rpc.py, probes/blockscout.py, .planning/scout/2026-05-29/rpc_capability_probe.md, .planning/scout/2026-05-29/deployment_block.md, .planning/scout/2026-05-29/event_shapes_onchain.md, .planning/scout/README.md</files>
  <action>
    Harden the scout's ad-hoc /tmp probe tooling into committed reusable scripts, then write the archive files recording the live-probe facts from the <facts> block (these were re-probed this session per RESEARCH.md — record them with provenance; do NOT block on re-running the network, but the scripts MUST be runnable to re-confirm).

    1. `probes/somnia_rpc.py`: reusable functions (stdlib `urllib` or `httpx` if added) — `chain_id()`, `head_block()`, `get_logs(addr, from_blk, to_blk)` (asserts ≤1000-block window), `get_block_receipts(blk)` (returns None if method-not-found), `get_storage_at(addr, slot, blk)`, `get_tx_by_hash(h)`, `get_block(blk)`. Default `RPC = "https://api.infra.mainnet.somnia.network/"`. Each function prints `source_url + utc_fetch_ts` when run as `__main__`.
    2. `probes/blockscout.py`: `tx_count(addr)` (GET `explorer.somnia.network/api/v2/addresses/{addr}/counters`) with a `time.sleep`-spaced retry-on-429 loop (Blockscout 429s after ~2 rapid calls).
    3. `.planning/scout/2026-05-29/rpc_capability_probe.md`: a provenance table (columns: capability, value, source_url, utc_fetch_ts) recording EVERY <facts> RPC row — chain_id 0x13a7=5031, client `somnia-16638fbbd16be8b-release`, eth_getLogs cap 1000, eth_getBlockReceipts AVAILABLE, archive depth FULL (correct at 283417317), median cadence ~100.7 ms/block, block.timestamp `whole_second` (cite the 1780085231/1780085231/1780085232 consecutive-block evidence), baseFee 6 gwei, proxy/impl IMPLEMENTATION_SLOT/ADMIN_SLOT/BEACON_SLOT/diamond reads (all the empty-slot reads belong to KPD-17 in Plan 02 — here record only the proxy IMPLEMENTATION_SLOT confirm + the cap/receipts/archive/cadence/timestamp facts).
    4. `.planning/scout/2026-05-29/deployment_block.md`: record deployment_block = **283417317**, creation tx `0x36596e1854e413681992166d5d55552d999f820eafdf6f8fe07afef1e66e8b0a` (2026-04-17T13:18:10Z), creator EOA `0x320362C7...fdE88936`, backfill_span = 36,286,846 blocks ≈ 36,287 getLogs windows. State explicitly: "INDEX-01 startBlock = 283417317 — RESOLVED (not TBD-via-creator-EOA). Supersedes ROADMAP/PROJECT '~320M blocks / ~270 days' — that conflated chain-age-since-TGE with proxy-age (proxy is ~42 days old)."
    5. `.planning/scout/2026-05-29/event_shapes_onchain.md`: a table of the three live on-chain event shapes (topic0, #topics, data bytes) from <facts>. State: "Roles NOT assigned here — scout role labels likely INVERTED; the 3-topic/1120-byte 0xb623… matches RequestCreated's ABI shape. TOPIC-01 (Phase 2) keccak-resolves definitively against pinned commit e15d4e9. There is NO ResponseReceived event in the main interface — flag for Phase 2."
    6. `.planning/scout/README.md`: index of the archive. State that `2026-05-29/` is the canonical archive (the upstream `2026-05-25/` referenced in ROADMAP/PROJECT was never committed; this README reconciles the path). List each file + one-line purpose.
  </action>
  <verify>
    <automated>test -f probes/somnia_rpc.py && test -f probes/blockscout.py && grep -q "283417317" .planning/scout/2026-05-29/deployment_block.md && grep -q "whole_second" .planning/scout/2026-05-29/rpc_capability_probe.md && grep -q "0xb62339927ed9948fd837358a55f5b9a824f7b047043faece66965593ed726889" .planning/scout/2026-05-29/event_shapes_onchain.md && uv run python -c "import ast; ast.parse(open('probes/somnia_rpc.py').read()); ast.parse(open('probes/blockscout.py').read()); print('probes-parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `probes/somnia_rpc.py` parses as valid Python and defines `get_block_receipts` and `get_storage_at`.
    - `.planning/scout/2026-05-29/deployment_block.md` contains `283417317` AND the string `Supersedes` (the ~320M correction note).
    - `.planning/scout/2026-05-29/rpc_capability_probe.md` contains `whole_second` AND `AVAILABLE` (getBlockReceipts) AND `1000` (getLogs cap).
    - `.planning/scout/2026-05-29/event_shapes_onchain.md` contains all three topic0 hashes and the string `INVERTED`.
    - `.planning/scout/README.md` contains `canonical`.
    - Every archive `.md` row carries `source_url` and a `utc_fetch_ts` (grep both strings present in each file).
  </acceptance_criteria>
  <done>The scout archive holds every load-bearing live-probe fact with provenance, the corrected deployment block, and the on-chain event shapes; reusable probe scripts can re-confirm.</done>
</task>

<task type="auto">
  <name>Task 3: KPD-16 — sha256 provenance manifest + scout-archive presence/value test</name>
  <read_first>
    - .planning/scout/2026-05-29/ (the files Task 2 + the existing event_count_addendum.md)
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Validation Architecture (test_scout_archive.py row)
  </read_first>
  <files>.planning/scout/2026-05-29/PROVENANCE.sha256, tests/test_scout_archive.py</files>
  <action>
    1. Generate `.planning/scout/2026-05-29/PROVENANCE.sha256`: run `sha256sum` over every `.md` file in `.planning/scout/2026-05-29/` (event_count_addendum.md, rpc_capability_probe.md, deployment_block.md, event_shapes_onchain.md) and write the standard `<hash>  <relative-path>` lines. This makes the archive tamper-evident (KPD-16 provenance discipline).
    2. `tests/test_scout_archive.py`: pytest tests asserting —
       - `test_archive_files_present`: all four `.md` files + PROVENANCE.sha256 exist.
       - `test_deployment_block_recorded`: deployment_block.md contains `283417317`.
       - `test_getblockreceipts_recorded`: rpc_capability_probe.md contains `AVAILABLE`.
       - `test_whole_second_timestamp`: rpc_capability_probe.md contains `whole_second`.
       - `test_event_shapes_present`: event_shapes_onchain.md contains all three topic0 hashes.
       - `test_provenance_manifest_valid`: parse PROVENANCE.sha256 and assert every listed file exists and its recomputed sha256 matches (use hashlib).
    Use the conftest `scout_dir` fixture.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scout_archive.py -x -q && sha256sum -c .planning/scout/2026-05-29/PROVENANCE.sha256</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_scout_archive.py -x -q` exits 0 with ≥6 tests passing.
    - `sha256sum -c .planning/scout/2026-05-29/PROVENANCE.sha256` reports `OK` for every line (exit 0).
    - `tests/test_scout_archive.py` contains `test_provenance_manifest_valid` and imports `hashlib`.
  </acceptance_criteria>
  <done>The scout archive is sha256-pinned and a test enforces presence + the load-bearing values + manifest integrity; KPD-16 (ordered first) is discharged.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -q` green (harness + scout-archive tests).
- `sha256sum -c .planning/scout/2026-05-29/PROVENANCE.sha256` OK.
- Archive records the corrected deployment block (283417317) and supersedes-note for the ~320M error.
- Probe scripts parse and are runnable to re-confirm any fact.
</verification>

<success_criteria>
KPD-16 discharged (scout archive consolidated, sha256-stamped, presence+value tested) and Wave 0 test infra exists so every downstream Phase-1 task has a runnable `<automated>` verify. INDEX-01 startBlock resolved to 283417317 in the archive.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-01-SUMMARY.md`.
</output>

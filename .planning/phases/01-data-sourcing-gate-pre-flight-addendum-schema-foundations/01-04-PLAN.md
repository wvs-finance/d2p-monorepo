---
phase: 01-data-sourcing-gate-pre-flight-addendum-schema-foundations
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - schemas/event_schema_v1.md
  - schemas/batch_manifest_v1.yaml
  - schemas/batch_manifest_v1.schema.json
  - tests/test_event_schema.py
  - tests/test_batch_manifest_schema.py
autonomous: true
requirements: [EVENT-01, DATA-SOURCE-01]
must_haves:
  truths:
    - "A consumer can read schemas/event_schema_v1.md and reproduce the exact polars/parquet column types + nullability without inferring from data."
    - "The DDL treats block_ts_utc, block_number, log_index, and gap-detection metadata as first-class non-nullable load-bearing columns; arrival ordering is on (block_number, log_index) with block_ts_utc as documented coarse secondary; dedup key is (chain_id, tx_hash, log_index)."
    - "uint256 fields (requestId, agentId, perAgentBudget, wei amounts) are typed as decimal-string / pl.Decimal — NEVER Int64/UInt64 (overflow is a HARD constraint)."
    - "The responses child table is specified with PK (chain_id, tx_hash, log_index, member_index) and FK (chain_id, tx_hash) → requests; agent_class_keccak + agent_class_string reserved (KPD-18)."
    - "A consumer can validate a batch manifest against schemas/batch_manifest_v1.schema.json (KPD-11a — schema only; write-time enforcement is Phase 5)."
  artifacts:
    - path: "schemas/event_schema_v1.md"
      provides: "EVENT-01 full DDL with arrival-timing fields first-class + responses child table + KPD-18 reservations"
      contains: "log_index"
    - path: "schemas/batch_manifest_v1.yaml"
      provides: "KPD-11a batch manifest schema artifact"
      contains: "schema_version_sha256"
    - path: "schemas/batch_manifest_v1.schema.json"
      provides: "JSON-Schema validator for the batch manifest"
      contains: "required"
  key_links:
    - from: "tests/test_event_schema.py"
      to: "schemas/event_schema_v1.md"
      via: "DDL-lint: arrival fields non-nullable, ordering + dedup keys documented, uint256 not Int64"
      pattern: "block_number.*log_index"
    - from: "tests/test_batch_manifest_schema.py"
      to: "schemas/batch_manifest_v1.yaml + schemas/batch_manifest_v1.schema.json"
      via: "jsonschema.validate(sample_manifest, schema)"
      pattern: "jsonschema"
---

<objective>
EVENT-01 — commit the `IAgentRequester` event-schema DDL (`schemas/event_schema_v1.md`) with arrival-timing fields as first-class non-nullable load-bearing columns ordered on `(block_number, log_index)`, the `responses` child table, the KPD-18 column reservations, and the uint256-overflow-safe dtypes — plus KPD-11a (`schemas/batch_manifest_v1.yaml` + a JSON-Schema validator; schema artifact only, write-time enforcement is Phase 5). This is the foundational schema every later phase reads; it must be locked before INDEX-01 manifest authoring.

Supersedes (note in event_schema_v1.md §Supersedes): per 01-RESEARCH.md —
1. KPD-18 reframed: the `main` interface has NO indexed-dynamic field; `agentId` is an indexed `uint256`, not `string indexed agentClass`. The keccak-trap (B1) does NOT apply as written. KEEP agent_class_keccak + agent_class_string as cheap insurance, but document the likely recovery mechanism is `agentId → class` mapping (a uint registry) or `bytes payload` decode, NOT keccak→string. Final per-(impl,topic0) enumeration is Phase 2.
2. There is NO `ResponseReceived` event in the interface (it has RequestFinalized + SubcommitteePaid). The `Response[]` data lives in the `Request.responses` struct — confirm in Phase 2/TOPIC-01 whether per-member response data is event-derivable or only state-readable BEFORE the responses child table is treated as event-fillable. Reserve the child table now; flag the population-path open question.
3. Arrival key is `(block_number, log_index)` because block.timestamp is whole_second (live-confirmed: 1780085231/1780085231/1780085232) — a timestamp sort scrambles intra-second order at ~100ms/block.

Purpose: lock the load-bearing schema (arrival-periodicity primacy) before any indexer code.
Output: event_schema_v1.md, batch_manifest_v1.yaml + .schema.json, two tests.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md
@.planning/scout/2026-05-29/event_shapes_onchain.md

<interfaces>
<!-- polars/parquet dtype table from 01-RESEARCH.md §Schema Artifacts. Executor uses these EXACT dtypes — no codebase exploration. -->
Logical type            | polars dtype                  | parquet physical        | nullability | note
chain_id                | pl.Int64                      | INT64                   | NOT NULL    | 5031 constant; wide for cross-leg join
block_number            | pl.UInt64                     | INT64                   | NOT NULL    | arrival-ordering PK part 1
log_index               | pl.UInt32                     | INT32                   | NOT NULL    | arrival-ordering PK part 2
tx_hash                 | pl.Utf8 (or Binary(32))       | BYTE_ARRAY              | NOT NULL    | dedup key part
block_ts_utc            | pl.Datetime("us","UTC")       | INT64 TIMESTAMP         | NOT NULL    | COARSE SECONDARY — whole-second source, never sort-primary
topic0                  | pl.Utf8 (or Binary(32))       | BYTE_ARRAY              | NOT NULL    | resolver join key
requestId / agentId     | pl.Utf8 (uint256 decimal str) | BYTE_ARRAY              | NOT NULL    | NEVER Int64 — uint256 overflow
perAgentBudget / gross_cost_native | pl.Utf8 (wei decimal) or pl.Decimal(38,0) | BYTE_ARRAY/FIXED | NOT NULL | wei exceeds Int64
gross_cost_usd / fx_rate| pl.Float64                    | DOUBLE                  | nullable    | FX may be stale
implementation_address  | pl.Utf8                       | BYTE_ARRAY              | NOT NULL    | NEVER null-type (PITFALLS E1)
agent_class_keccak      | pl.Utf8 (or Binary(32))       | BYTE_ARRAY              | nullable    | KPD-18 reservation
agent_class_string      | pl.Utf8                       | BYTE_ARRAY              | nullable    | KPD-18 reservation
gap_status (enum)       | pl.Enum(["observed","indexer_gap_censored","source_gap"]) | BYTE_ARRAY | NOT NULL | feeds STATS-01
schema_version          | pl.Utf8                       | BYTE_ARRAY              | NOT NULL    |

Arrival-ordering contract (EVENT-01 SC#8): sort key (block_number, log_index); block_ts_utc documented coarse secondary; dedup key (chain_id, tx_hash, log_index).
responses child table (KPD-03): PK (chain_id, tx_hash, log_index, member_index); FK (chain_id, tx_hash) → requests.
batch_manifest_v1.yaml required fields (KPD-11a): run_id, rpc_endpoint, subgraph_endpoint, indexer_commit_sha, from_block, to_block, observed_impl_address, fx_cache_sha256, topic0_map_sha256, impl_history_sha256, schema_version_sha256.
Three live on-chain topic0s (record as topic0 domain, roles UNASSIGNED — TOPIC-01 resolves):
  0xb62339927ed9948fd837358a55f5b9a824f7b047043faece66965593ed726889 (3-topic/1120-byte — likely RequestCreated)
  0x5c090ef48df2b4d8a01bd0639355d62c318b623aed749bdd12325f789e37a2cf (2-topic/32-byte)
  0x65db1ef5b3bcd84fe4fb8dbbe1cadc9fe6643bb261ab2e01d65c281c3d466af2 (2-topic/32-byte)
Agent classes (canonical absolute prices SOMI/call): json-fetch 0.03, llm-inference 0.07, llm-parse-website 0.10.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schemas/event_schema_v1.md — EVENT-01 full DDL (arrival-timing first-class, uint256-safe, responses child table)</name>
  <read_first>
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Schema Artifacts (dtype table + arrival contract + responses child table + critical uint256 pitfall)
    - .planning/scout/2026-05-29/event_shapes_onchain.md (the three topic0 shapes — record domain, not roles)
    - .planning/ROADMAP.md Phase 1 SC#8 (arrival-timing schema criterion) + REQUIREMENTS.md EVENT-01
    - The <interfaces> block above (the EXACT dtypes to use)
  </read_first>
  <files>schemas/event_schema_v1.md</files>
  <action>
    Write `schemas/event_schema_v1.md` — the full DDL. Sections:
    1. `## Supersedes` — the three corrections from <objective> (KPD-18 reframe: agentId uint256 not string indexed; no ResponseReceived event → responses population-path open question for Phase 2; arrival key (block_number, log_index) because whole_second timestamp).
    2. `## requests table` — a column table with EXACTLY the <interfaces> dtype rows: column | polars dtype | parquet physical | nullability | note. Every arrival-timing column (block_number, log_index, block_ts_utc) marked NOT NULL and "load-bearing (arrival-periodicity primacy)". Include topic0, requestId, agentId, perAgentBudget, implementation_address, agent_class_keccak, agent_class_string, gap_status, schema_version, chain_id, tx_hash, gross_cost_native, gross_cost_usd, fx_rate.
    3. `## uint256 overflow (HARD constraint)` — state requestId, agentId, perAgentBudget, and ALL wei amounts MUST be `pl.Utf8` (decimal string) or `pl.Decimal(38,0)`, NEVER `pl.Int64`/`pl.UInt64`. This is the single most common silent-corruption trap.
    4. `## Arrival-ordering contract` — sort key `(block_number, log_index)`; `block_ts_utc` is a DOCUMENTED coarse secondary (whole-second source — live evidence 1780085231/1780085231/1780085232; ~100ms/block means up to ~10 blocks × N logs share a block_ts_utc; a timestamp sort scrambles arrival order). Dedup key `(chain_id, tx_hash, log_index)`. State the M2 consumer note: the arrival-process estimation reads this inter-arrival series directly → a dropped/mis-ordered event corrupts the inter-arrival distribution.
    5. `## responses child table (KPD-03)` — PK `(chain_id, tx_hash, log_index, member_index)`, FK `(chain_id, tx_hash) → requests`. Caveat: confirm per-member response data is event-derivable (no ResponseReceived event in interface; may be state-read from Request.responses) BEFORE Phase 3 treats it as event-fillable — flag this open question.
    6. `## KPD-18 reservations` — agent_class_keccak + agent_class_string reserved for the three classes {json-fetch 0.03, llm-inference 0.07, llm-parse-website 0.10}; recovery mechanism is LIKELY agentId→class (uint registry) or bytes-payload decode, NOT keccak→string (no indexed string in interface). Per-(impl,topic0) enumeration is Phase 2.
    7. `## topic0 domain` — the three live topic0 hashes with their shapes; roles UNASSIGNED (TOPIC-01 keccak-resolves; the 3-topic/1120-byte 0xb623 is the leading RequestCreated candidate — do NOT hard-code).
    8. `## gap-detection metadata` — gap_status enum {observed, indexer_gap_censored, source_gap} NOT NULL; feeds STATS-01 Var(N)/E[N] censoring.
  </action>
  <verify>
    <automated>grep -q "block_number" schemas/event_schema_v1.md && grep -q "log_index" schemas/event_schema_v1.md && grep -q "(chain_id, tx_hash, log_index)" schemas/event_schema_v1.md && grep -qi "uint256" schemas/event_schema_v1.md && grep -q "member_index" schemas/event_schema_v1.md && grep -q "agent_class_keccak" schemas/event_schema_v1.md && grep -q "agent_class_string" schemas/event_schema_v1.md && grep -qi "coarse secondary" schemas/event_schema_v1.md && grep -q "0xb62339927ed9948fd837358a55f5b9a824f7b047043faece66965593ed726889" schemas/event_schema_v1.md</automated>
  </verify>
  <acceptance_criteria>
    - `schemas/event_schema_v1.md` contains `block_number`, `log_index`, the dedup key `(chain_id, tx_hash, log_index)`, `uint256` (overflow constraint), `member_index` (responses PK), `agent_class_keccak`, `agent_class_string`, and `coarse secondary` (block_ts_utc semantics).
    - The DDL marks block_number/log_index/block_ts_utc NOT NULL and states requestId/agentId/perAgentBudget are decimal-string / pl.Decimal, never Int64.
    - The three live topic0 hashes appear with roles UNASSIGNED (TOPIC-01 to resolve).
    - A `## Supersedes` section records the KPD-18 reframe + no-ResponseReceived note.
  </acceptance_criteria>
  <done>EVENT-01 DDL committed: arrival-timing first-class, uint256-safe, responses child table specified, KPD-18 columns reserved, topic0 roles deferred to Phase 2.</done>
</task>

<task type="auto">
  <name>Task 2: KPD-11a batch manifest schema artifact + JSON-Schema validator</name>
  <read_first>
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Schema Artifacts (batch_manifest_v1.yaml required fields)
    - .planning/ROADMAP.md Phase 5 KPD-11b (write-time enforcement — scope boundary: schema only HERE)
    - The <interfaces> block (batch_manifest required fields list)
  </read_first>
  <files>schemas/batch_manifest_v1.yaml, schemas/batch_manifest_v1.schema.json</files>
  <action>
    KPD-11a — schema artifact ONLY (write-time enforcement is Phase 5 / KPD-11b).
    1. `schemas/batch_manifest_v1.yaml`: a documented example/template manifest with all required fields populated with placeholder/example values + inline comments: run_id, rpc_endpoint (https://api.infra.mainnet.somnia.network/), subgraph_endpoint, indexer_commit_sha, from_block (283417317 example), to_block, observed_impl_address (0x9AF59C5683bb8686596B0D56e4F67655C6B73EdD), fx_cache_sha256, topic0_map_sha256, impl_history_sha256, schema_version_sha256. Add a header comment: "KPD-11a schema artifact; runtime write-time validation is Phase 5 / KPD-11b."
    2. `schemas/batch_manifest_v1.schema.json`: a JSON-Schema (draft 2020-12) — `$schema`, `type: object`, `properties` typing each required field (strings for hashes/endpoints/sha, integers for from_block/to_block), and a `required` array enumerating ALL the fields above. This validator is what KPD-11b will invoke at write time in Phase 5.
  </action>
  <verify>
    <automated>uv run python -c "import json,yaml,jsonschema; sch=json.load(open('schemas/batch_manifest_v1.schema.json')); man=yaml.safe_load(open('schemas/batch_manifest_v1.yaml')); jsonschema.validate(man, sch); req=set(sch['required']); need={'run_id','rpc_endpoint','subgraph_endpoint','indexer_commit_sha','from_block','to_block','observed_impl_address','fx_cache_sha256','topic0_map_sha256','impl_history_sha256','schema_version_sha256'}; assert need<=req, need-req; print('manifest-schema-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `schemas/batch_manifest_v1.yaml` loads as YAML and the example manifest validates against `schemas/batch_manifest_v1.schema.json` (jsonschema.validate passes).
    - The JSON-Schema `required` array includes all 11 fields (run_id … schema_version_sha256).
    - The YAML carries the "write-time enforcement is Phase 5 / KPD-11b" scope note.
  </acceptance_criteria>
  <done>KPD-11a batch manifest schema + validator committed; Phase 5 KPD-11b can invoke the validator at write time.</done>
</task>

<task type="auto">
  <name>Task 3: EVENT-01 DDL-lint test + batch-manifest schema test</name>
  <read_first>
    - schemas/event_schema_v1.md + schemas/batch_manifest_v1.yaml + schemas/batch_manifest_v1.schema.json (Tasks 1-2)
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Validation Architecture (test_event_schema + test_batch_manifest_schema rows)
  </read_first>
  <files>tests/test_event_schema.py, tests/test_batch_manifest_schema.py</files>
  <action>
    1. `tests/test_event_schema.py` (DDL-lint over the markdown):
       - `test_arrival_fields_nonnullable`: the DDL marks block_number, log_index, block_ts_utc as NOT NULL.
       - `test_ordering_and_dedup_keys`: contains the `(block_number, log_index)` ordering key AND the `(chain_id, tx_hash, log_index)` dedup key.
       - `test_uint256_not_int64`: asserts the doc states uint256 fields use decimal-string / pl.Decimal and explicitly NOT Int64 (grep for "never" + "Int64" near uint256, or a documented constraint marker).
       - `test_responses_child_table`: contains `member_index` and the FK `(chain_id, tx_hash)`.
       - `test_kpd18_reservations`: contains agent_class_keccak AND agent_class_string.
       - `test_block_ts_secondary`: contains "coarse secondary".
       Use conftest `schemas_dir` + `read_text`.
    2. `tests/test_batch_manifest_schema.py`:
       - `test_manifest_validates`: load the YAML manifest + JSON schema, `jsonschema.validate` passes.
       - `test_required_fields`: the schema `required` array contains all 11 fields.
       - `test_missing_field_rejected`: deleting `schema_version_sha256` from a copy of the manifest makes `jsonschema.validate` raise (proves the validator is enforcing).
  </action>
  <verify>
    <automated>uv run pytest tests/test_event_schema.py tests/test_batch_manifest_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_event_schema.py tests/test_batch_manifest_schema.py -x -q` exits 0.
    - `test_uint256_not_int64` and `test_ordering_and_dedup_keys` both pass (the load-bearing arrival-periodicity + overflow guarantees are enforced).
    - `test_missing_field_rejected` proves the manifest validator rejects an incomplete manifest.
  </acceptance_criteria>
  <done>EVENT-01 DDL invariants and the KPD-11a manifest validator are test-enforced.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -q` green (adds event-schema + batch-manifest tests).
- event_schema_v1.md is uint256-safe, arrival-timing first-class, ordered on (block_number, log_index), responses child table + KPD-18 columns specified.
- batch_manifest_v1.yaml validates against its JSON-Schema; an incomplete manifest is rejected.
- Supersedes-note records KPD-18 reframe + no-ResponseReceived population-path open question.
</verification>

<success_criteria>
EVENT-01 + KPD-11a discharged: the foundational event-schema DDL and batch-manifest schema artifact are committed and test-enforced, with arrival-periodicity primacy and uint256-overflow safety locked before any INDEX-01 authoring.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-04-SUMMARY.md`.
</output>

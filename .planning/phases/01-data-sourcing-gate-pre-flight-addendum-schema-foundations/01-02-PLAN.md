---
phase: 01-data-sourcing-gate-pre-flight-addendum-schema-foundations
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/scout/2026-05-29/beacon_diamond_probe.md
  - .planning/scout/2026-05-29/somnia_finality_semantics.md
  - .planning/scout/2026-05-29/coingecko_convention.md
  - .planning/scout/2026-05-29/PROVENANCE.sha256
  - adapters/fx/coingecko_config.yaml
  - tests/fixtures/fx_candle_convention.py
  - tests/fixtures/test_fx_candle_convention.py
  - tests/test_preflight_verdicts.py
autonomous: true
requirements: [DATA-SOURCE-01]
must_haves:
  truths:
    - "A consumer can read the beacon/diamond probe result and confirm both BEACON_SLOT and IMPLEMENTATION_SLOT on impl 0x9AF5…3EdD returned 0x00 (KPD-17 happy path — IMPL-01 tracks only the EIP-1967 proxy impl slot)."
    - "A consumer can read the finality-semantics verdict and find the MEDIUM-confidence KPD-09-docs branch-(a) conclusion (sub-second finality asserted, irreversibility not stated → safe_block_depth=1 provisional, empirical confirmation deferred to Phase 3)."
    - "A consumer can read coingecko_config.yaml::timestamp_convention == 'close' and the FX LOCF subtracts candle duration (KPD-19 — no look-ahead into inequality #1)."
    - "The candle-convention unit fixture is committed for Phase 4c to consume."
  artifacts:
    - path: ".planning/scout/2026-05-29/beacon_diamond_probe.md"
      provides: "KPD-17 beacon-slot + diamond-storage reads on impl with provenance"
      contains: "0x"
    - path: ".planning/scout/2026-05-29/somnia_finality_semantics.md"
      provides: "KPD-09-docs finality verdict + citation"
      contains: "safe_block_depth"
    - path: "adapters/fx/coingecko_config.yaml"
      provides: "KPD-19 timestamp_convention=close config stub for FX-01"
      contains: "timestamp_convention"
    - path: "tests/fixtures/fx_candle_convention.py"
      provides: "candle-convention fixture consumed by Phase 4c (FX-01)"
  key_links:
    - from: "tests/test_preflight_verdicts.py"
      to: ".planning/scout/2026-05-29/beacon_diamond_probe.md + somnia_finality_semantics.md + adapters/fx/coingecko_config.yaml"
      via: "presence + value assertions"
      pattern: "timestamp_convention.*close"
    - from: "tests/fixtures/test_fx_candle_convention.py"
      to: "tests/fixtures/fx_candle_convention.py"
      via: "pytest fixture import"
      pattern: "candle"
---

<objective>
Discharge the three pre-flight probe verdicts that gate downstream phases: KPD-17 (beacon/diamond — gates IMPL-01/Phase 3 data model), KPD-09-docs (finality semantics — gates Phase 3 safe_block_depth assumption), KPD-19 (CoinGecko candle convention — gates the Phase 4c FX adapter). All three are RESOLVED in 01-RESEARCH.md against primary sources this session; this plan records the verdicts with provenance and commits the FX config stub + candle-convention fixture.

Supersedes: KPD-18's original framing assumed a `string indexed agentClass` keccak trap. RESEARCH.md found NO indexed-dynamic field in the interface (`agentId` is `uint256`). KPD-18's schema-column reservation moves to Plan 04 (EVENT-01) with the corrected recovery framing; this plan does not touch KPD-18.

Purpose: lock the three pre-flight verdicts so Phases 2/3/4 can proceed on confirmed facts.
Output: three scout verdict files, the FX config stub, the candle-convention fixture + its test.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md
@.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-CONTEXT.md

<facts>
<!-- From 01-RESEARCH.md §Data-Sourcing Capability Matrix, §Code Examples, §Sources. Use EXACT values. -->
KPD-17 (beacon/diamond probe on impl 0x9AF59C5683bb8686596B0D56e4F67655C6B73EdD):
  BEACON_SLOT  = keccak256('eip1967.proxy.beacon')-1 = 0xa3f0ad74e5423aebfd80d3ef4346578335a9a72aeaee59ff6cb3582b35133d50  -> 0x (EMPTY)
  IMPLEMENTATION_SLOT on impl                                                                                          -> 0x (EMPTY, impl is not itself a proxy)
  EIP-2535 diamond storage slot = 0xc8fcad8db84d3cc18b4c41d551ea0ee66dd599cde068d998e57d5e09332c131c                  -> 0x (EMPTY)
  Proxy ADMIN_SLOT -> 0x empty (UUPS-style, not transparent admin)
  VERDICT: KPD-17 HAPPY PATH — plain EIP-1967, NOT a beacon proxy, NOT a diamond. IMPL-01 tracks ONLY the proxy EIP-1967 impl slot. (Roadmap SC#2a happy path; no PROJECT.md re-scope trigger fires.)

KPD-09-docs (eth_blockNumber finality semantics):
  Source: docs.somnia.network/llms-full.txt + MultiStream consensus page + GetBlock docs (fetched 2026-05-29).
  Finding: docs assert "sub-second finality" + deterministic PBFT merge, but NEVER explicitly state reorg/irreversibility.
  CONFIDENCE: MEDIUM.
  VERDICT: branch (a) PROVISIONAL — safe_block_depth = 1 provisionally accepted; Phase 3 KPD-09-empirical confirms via ≥1-hour rollback observation. (NOT branch (b): docs DO assert sub-second finality, so default to (a) provisional, not full deferral.)

KPD-19 (CoinGecko OHLC timestamp convention):
  Source: docs.coingecko.com/reference/coins-id-ohlc (fetched 2026-05-29T20:10Z).
  VERDICT: candle-CLOSE. FX LOCF MUST subtract candle duration (else up to 1 candle of look-ahead poisons inequality #1).
  SOMI: CoinGecko id `somnia`, ~$0.151, ATL $0.1466 May-2026, full history.
  Free-tier note: OHLC hourly is PAID-ONLY; use coins/{id}/market_chart/range for free hourly (1–90d auto-granularity).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KPD-17 beacon/diamond probe verdict + KPD-09-docs finality verdict</name>
  <read_first>
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Code Examples (beacon/diamond probe snippet) + §Sources (finality)
    - .planning/scout/2026-05-29/rpc_capability_probe.md (Plan 01 — the slot-read provenance source)
    - The <facts> block above (exact slot hashes + verdicts)
  </read_first>
  <files>.planning/scout/2026-05-29/beacon_diamond_probe.md, .planning/scout/2026-05-29/somnia_finality_semantics.md</files>
  <action>
    1. `.planning/scout/2026-05-29/beacon_diamond_probe.md` (KPD-17, PITFALLS A3): record the three impl slot reads from <facts> as a provenance table (slot, slot_hash, value, source_url, utc_fetch_ts). Each read = `0x` empty. State the VERDICT verbatim: "KPD-17 HAPPY PATH — impl 0x9AF59C5683bb8686596B0D56e4F67655C6B73EdD is plain EIP-1967, NOT a beacon proxy, NOT a diamond. IMPL-01 tracks ONLY the proxy EIP-1967 implementation slot. ROADMAP SC#2a happy path satisfied; no PROJECT.md re-scope trigger (SC#2b) fires." Include the BEACON_SLOT hash `0xa3f0ad74e5423aebfd80d3ef4346578335a9a72aeaee59ff6cb3582b35133d50` and the diamond slot `0xc8fcad8db84d3cc18b4c41d551ea0ee66dd599cde068d998e57d5e09332c131c` so the test can grep them.
    2. `.planning/scout/2026-05-29/somnia_finality_semantics.md` (KPD-09-docs): record the finality finding + citation from <facts>. State CONFIDENCE: MEDIUM and the verdict verbatim: "Branch (a) PROVISIONAL: docs assert sub-second finality + deterministic PBFT merge but do not state irreversibility → safe_block_depth = 1 provisionally accepted; Phase 3 KPD-09-empirical confirms via ≥1-hour rollback observation." Cite docs.somnia.network/llms-full.txt + the MultiStream consensus page with utc_fetch_ts 2026-05-29.
  </action>
  <verify>
    <automated>grep -q "HAPPY PATH" .planning/scout/2026-05-29/beacon_diamond_probe.md && grep -q "0xa3f0ad74e5423aebfd80d3ef4346578335a9a72aeaee59ff6cb3582b35133d50" .planning/scout/2026-05-29/beacon_diamond_probe.md && grep -q "safe_block_depth = 1" .planning/scout/2026-05-29/somnia_finality_semantics.md && grep -qi "medium" .planning/scout/2026-05-29/somnia_finality_semantics.md</automated>
  </verify>
  <acceptance_criteria>
    - `beacon_diamond_probe.md` contains `HAPPY PATH`, the BEACON_SLOT hash, the diamond slot hash, and three `0x` empty reads each with source_url + utc_fetch_ts.
    - `somnia_finality_semantics.md` contains `safe_block_depth = 1`, `MEDIUM`, `Branch (a)`, and a docs.somnia.network citation with utc_fetch_ts.
  </acceptance_criteria>
  <done>KPD-17 + KPD-09-docs verdicts recorded with provenance; IMPL-01 tracks only the proxy slot, safe_block_depth=1 provisional.</done>
</task>

<task type="auto">
  <name>Task 2: KPD-19 CoinGecko convention verdict + FX config stub</name>
  <read_first>
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Common Pitfalls (Pitfall 4) + §Don't Hand-Roll (FX rows) + §Sources (CoinGecko)
    - The <facts> block above (candle-CLOSE verdict, market_chart/range free-hourly note)
  </read_first>
  <files>.planning/scout/2026-05-29/coingecko_convention.md, adapters/fx/coingecko_config.yaml</files>
  <action>
    1. `.planning/scout/2026-05-29/coingecko_convention.md` (KPD-19, PITFALLS C1): record the verdict verbatim — "CoinGecko OHLC timestamp = candle-CLOSE (docs.coingecko.com/reference/coins-id-ohlc, fetched 2026-05-29T20:10Z). Consequence: FX LOCF MUST subtract the candle duration before the strict `t_price < t_block` join, else up to one candle-duration of look-ahead poisons inequality #1." Record the empirical-cross-check intent (Phase 4c validates against an alternative source for a named SOMI day per SC#3). Record SOMI id `somnia`, ~$0.151, and the free-tier note (OHLC hourly is paid-only; use `coins/{id}/market_chart/range` for free hourly 1–90d).
    2. `adapters/fx/coingecko_config.yaml`: config stub with keys —
       `coin_id: somnia`
       `timestamp_convention: close`   # KPD-19 — subtract candle duration on LOCF join
       `endpoint_ohlc: https://api.coingecko.com/api/v3/coins/somnia/ohlc`
       `endpoint_range: https://api.coingecko.com/api/v3/coins/somnia/market_chart/range`   # free hourly 1-90d
       `fx_stale_minutes: 90`
       `fx_very_stale_hours: 4`
       `fallback_chain: [coingecko, coinmarketcap, messari]`
       Add a comment block citing source_url + utc_fetch_ts (KPD-16 provenance discipline).
  </action>
  <verify>
    <automated>uv run python -c "import yaml; c=yaml.safe_load(open('adapters/fx/coingecko_config.yaml')); assert c['timestamp_convention']=='close', c; assert c['coin_id']=='somnia'; print('fx-config-ok')" && grep -qi "candle-CLOSE\|candle-close" .planning/scout/2026-05-29/coingecko_convention.md</automated>
  </verify>
  <acceptance_criteria>
    - `adapters/fx/coingecko_config.yaml` loads as YAML and `timestamp_convention == 'close'` and `coin_id == 'somnia'`.
    - `coingecko_convention.md` contains `candle-CLOSE` (case-insensitive) and `inequality #1` and a source_url + utc_fetch_ts.
  </acceptance_criteria>
  <done>KPD-19 verdict recorded; FX-01 has a provenance-stamped config stub asserting candle-CLOSE.</done>
</task>

<task type="auto">
  <name>Task 3: Candle-convention fixture (for Phase 4c) + pre-flight verdict tests</name>
  <read_first>
    - .planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-RESEARCH.md §Validation Architecture (test_fx_candle_convention + test rows) + §Wave 0 Gaps
    - adapters/fx/coingecko_config.yaml (Task 2 output)
    - .planning/ROADMAP.md Phase 4 SC#4 (the XX:00:30Z join expectation the fixture encodes)
    - .planning/scout/2026-05-29/PROVENANCE.sha256 (Plan 01 — the manifest this task EXTENDS with Plan-02's three new .md files)
  </read_first>
  <files>tests/fixtures/fx_candle_convention.py, tests/fixtures/test_fx_candle_convention.py, tests/test_preflight_verdicts.py, .planning/scout/2026-05-29/PROVENANCE.sha256</files>
  <action>
    1. `tests/fixtures/fx_candle_convention.py`: a standalone, framework-agnostic helper Phase 4c consumes. Provide:
       - `CANDLE_CONVENTION = "close"` constant.
       - `def joined_candle_close_ts(block_ts_utc, candle_duration_s=3600)`: given a block timestamp, returns the close-ts of the most-recent candle that CLOSED strictly before the block (i.e. the candle to LOCF-join under the CLOSE convention). For a block at `XX:00:30Z` with hourly candles, returns the candle that closed at `XX:00:00Z` (the one that just closed), NEVER the candle closing at `(XX+1):00:00Z` (not yet closed). This encodes ROADMAP Phase 4 SC#4.
       - A small in-memory example dataset (a few `(block_ts, expected_candle_close_ts)` pairs) for the test.
    2. `tests/fixtures/test_fx_candle_convention.py`: assert `joined_candle_close_ts` for a block at `XX:00:30Z` returns the just-closed candle, not the future one; assert strict `t_price < t_block`; assert `CANDLE_CONVENTION == "close"`.
    3. `tests/test_preflight_verdicts.py`: smoke tests over the three scout verdict files + the FX config — assert `beacon_diamond_probe.md` contains `HAPPY PATH`, `somnia_finality_semantics.md` contains `safe_block_depth = 1`, `coingecko_config.yaml` loads with `timestamp_convention == 'close'`. Consume the conftest `scout_dir` fixture as a test parameter (returns a `Path`).
    4. (N3 — close the provenance gap) Append Plan-02's three new scout `.md` files to the Plan-01 manifest so they are pinned too. Run `sha256sum .planning/scout/2026-05-29/beacon_diamond_probe.md .planning/scout/2026-05-29/somnia_finality_semantics.md .planning/scout/2026-05-29/coingecko_convention.md` and APPEND the resulting `<hash>  <relative-path>` lines to `.planning/scout/2026-05-29/PROVENANCE.sha256` (do NOT rewrite the existing four Plan-01 lines). After this, `sha256sum -c .planning/scout/2026-05-29/PROVENANCE.sha256` must report OK for all seven `.md` files, and Plan-01's `test_provenance_manifest_valid` set-equality assertion (manifest set == `.md` files present in `scout_dir`) must still hold with the dir now containing seven `.md` files.
  </action>
  <verify>
    <automated>uv run pytest tests/fixtures/test_fx_candle_convention.py tests/test_preflight_verdicts.py -x -q && sha256sum -c .planning/scout/2026-05-29/PROVENANCE.sha256 && uv run pytest tests/test_scout_archive.py::test_provenance_manifest_valid -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/fixtures/test_fx_candle_convention.py tests/test_preflight_verdicts.py -x -q` exits 0.
    - `tests/fixtures/fx_candle_convention.py` defines `CANDLE_CONVENTION = "close"` and `joined_candle_close_ts`.
    - The fixture test proves a block at `XX:00:30Z` joins the just-closed candle (no look-ahead).
    - `tests/test_preflight_verdicts.py` asserts all three verdict artifacts.
    - `PROVENANCE.sha256` now lists all SEVEN scout `.md` files (4 Plan-01 + 3 Plan-02) and `sha256sum -c` reports OK for every line; Plan-01's `test_provenance_manifest_valid` set-equality assertion still passes with the dir holding seven `.md` files.
  </acceptance_criteria>
  <done>The candle-convention fixture is committed for Phase 4c (ROADMAP Phase 4 SC#4), all three pre-flight verdicts are test-enforced, and Plan-02's three scout files are appended to PROVENANCE.sha256 (no unpinned archive file remains).</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -q` green (adds pre-flight + candle-convention tests).
- KPD-17 happy-path verdict, KPD-09-docs MEDIUM branch-(a) verdict, KPD-19 candle-CLOSE verdict all recorded with provenance.
- FX config stub asserts `timestamp_convention: close`; candle fixture committed for Phase 4c.
</verification>

<success_criteria>
KPD-17, KPD-09-docs, KPD-19 discharged with primary-source provenance; IMPL-01 confirmed to track only the proxy slot; safe_block_depth=1 provisional; FX-01 has a candle-CLOSE config + a reusable no-look-ahead fixture.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-sourcing-gate-pre-flight-addendum-schema-foundations/01-02-SUMMARY.md`.
</output>

---
phase: 02-research-lab-presence-and-iteration-catalog
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/IterationCatalogCard.tsx
  - components/IterationDetailHeader.tsx
  - components/EvidenceChain.tsx
  - components/BetaCIChart.tsx
  - components/ReplicationHash.tsx
  - components/DispositionMemo.tsx
  - components/PublicationCard.tsx
  - components/ContributorCard.tsx
  - components/NumberedStep.tsx
  - components/CheckmarkList.tsx
  - tests/unit/iteration-catalog-card.test.tsx
  - tests/unit/iteration-detail-header.test.tsx
  - tests/unit/evidence-chain.test.tsx
  - tests/unit/beta-ci-chart.test.tsx
  - tests/unit/replication-hash.test.tsx
  - tests/unit/disposition-memo.test.tsx
  - tests/unit/publication-card.test.tsx
  - tests/unit/contributor-card.test.tsx
  - tests/unit/numbered-step.test.tsx
  - tests/unit/checkmark-list.test.tsx
autonomous: true
agent: UI Designer
requirements: []
must_haves:
  truths:
    - "IterationCatalogCard renders identical min-h-[120px] for every status (epistemic equality)"
    - "IterationDetailHeader stacks StatusPill / version / H1 vertically at <sm; horizontal flex at sm+"
    - "EvidenceChain uses dl/dt/dd semantic HTML with font-mono for all numeric values"
    - "BetaCIChart inline SVG renders β point, CI whisker, zero reference line, sr-only data table, aria-label"
    - "ReplicationHash truncates display (first-8 + last-4) but exposes full hash in title + aria-label + copy"
    - "DispositionMemo uses text-text-primary with no collapse/accordion — same visual weight as PASS sections"
    - "PublicationCard supports optional numbered-order prefix in accent-mono font"
    - "ContributorCard is a row in divide-y list (no card-in-card nesting)"
    - "NumberedStep stacks number above title at <md; horizontal at md+ (panoptic pattern)"
    - "CheckmarkList uses CheckCircle2 lucide icon in text-status-pass color + body text-base"
    - "Every new component has a vitest unit test covering shape, status variants, and ARIA contract"
    - "ITER-07: StatusPill is reused (not duplicated) in IterationCatalogCard + IterationDetailHeader + DispositionMemo"
  artifacts:
    - path: components/IterationCatalogCard.tsx
      provides: "Equal-weight iteration card with StatusPill + title + β + date"
      contains: "min-h-[120px]"
    - path: components/IterationDetailHeader.tsx
      provides: "Header for /apps/abrigo/iterations/[slug]/v[n]"
      contains: "StatusPill"
    - path: components/EvidenceChain.tsx
      provides: "β / CI / p / N / replication block — dl/dt/dd"
      contains: "<dl"
    - path: components/BetaCIChart.tsx
      provides: "Inline SVG β + 95% CI horizontal range bar (≤40 lines)"
      contains: "viewBox"
    - path: components/ReplicationHash.tsx
      provides: "Client component: truncated hash display + copy-to-clipboard"
      contains: "navigator.clipboard"
    - path: components/DispositionMemo.tsx
      provides: "Full-weight failure/parked narrative section, no collapse"
      contains: "text-text-primary"
    - path: components/PublicationCard.tsx
      provides: "Research / decision-memo / write-up / talk card"
      contains: "Badge"
    - path: components/ContributorCard.tsx
      provides: "Team member row with avatar + role + GitHub + iteration ownership"
      contains: "ArrowUpRight"
    - path: components/NumberedStep.tsx
      provides: "Panoptic-derived numbered step for /about methodology"
      contains: "font-mono"
    - path: components/CheckmarkList.tsx
      provides: "Discipline commitments list with semantic PASS-green checkmarks"
      contains: "CheckCircle2"
  key_links:
    - from: components/IterationCatalogCard.tsx
      to: components/StatusPill.tsx
      via: "import StatusPill"
      pattern: "from ['\"]@?/?components/StatusPill"
    - from: components/IterationDetailHeader.tsx
      to: components/StatusPill.tsx
      via: "import StatusPill"
      pattern: "from ['\"]@?/?components/StatusPill"
    - from: components/DispositionMemo.tsx
      to: components/StatusPill.tsx
      via: "StatusPill reuse for FAIL/PARKED context"
      pattern: "StatusPill"
    - from: components/EvidenceChain.tsx
      to: components/BetaCIChart.tsx
      via: "BetaCIChart composed inside EvidenceChain"
      pattern: "BetaCIChart"
    - from: components/EvidenceChain.tsx
      to: components/ReplicationHash.tsx
      via: "ReplicationHash composed inside EvidenceChain replication row"
      pattern: "ReplicationHash"
---

<objective>
Build the 10 reusable Phase 2 components per the UI-SPEC §Component Inventory specifications. Each component is a self-contained, fully tested unit with strict adherence to the impeccable anti-pattern blocklist (no card-in-card, no side-accent borders, no icon-tile-above-heading, no gradient text, no bounce easing) and CROSS-09 (color + icon + text for all status indicators).

Purpose: Plans 02-03, 02-05, 02-06, 02-07 compose these primitives into pages. Centralizing component authoring in Wave 2 enables those plans to run in parallel without rediscovering visual contracts. ITER-07 (StatusPill encodes color + icon + text) is delivered by ensuring every status-bearing component in this plan reuses the existing `<StatusPill>` rather than reinventing status display.

Output: 10 components in `components/`, each with a sibling unit test in `tests/unit/`, all passing biome + tsc + impeccable + axe-core lint, and ready for page composition in Wave 3.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-CONTEXT.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-RESEARCH.md
@components/StatusPill.tsx
@components/IterationCountTile.tsx
@components/StructuredData.tsx

<interfaces>
<!-- Existing Phase 1 contracts to consume verbatim. Do not re-derive. -->

From components/StatusPill.tsx:
```typescript
export type IterationStatus = 'PASS' | 'FAIL' | 'PARKED' | 'IN_PROGRESS'
export interface StatusPillProps {
  status: IterationStatus
  label: string   // locale-resolved label from caller via getTranslations()
  className?: string
}
export function StatusPill(props: StatusPillProps): JSX.Element
```

From .velite (generated post-Plan 02-01):
```typescript
// .velite/iterations.ts (auto-generated by Velite)
export interface Iteration {
  slug: string
  version: number
  status: 'PASS' | 'FAIL' | 'PARKED' | 'IN_PROGRESS'
  title_es: string
  title_en: string
  beta?: number
  ci_lower?: number
  ci_upper?: number
  p_value?: number
  sample_size?: number
  replication_hash?: string
  notebook_url?: string
  dataset_ref?: string
  analysis_date: Date
  disposition_memo?: string  // required by refine() when status === 'FAIL'
  code: string  // compiled MDX
}

// .velite/research.ts (auto-generated)
export interface Research {
  slug: string
  title_es: string
  title_en: string
  authors: string[]
  date: Date
  type: 'paper' | 'decision-memo' | 'write-up' | 'talk'
  external_url?: string
  summary_es: string
  summary_en: string
  tags: string[]
  order?: number
}
```

Locale type from next-intl:
```typescript
import { useLocale } from 'next-intl'
// useLocale() returns 'es-CO' | 'en'
```

Icon imports from lucide-react:
```typescript
import { ClipboardCopy, CheckCircle2, ArrowUpRight, Check } from 'lucide-react'
```
</interfaces>

<phase_1_test_patterns>
<!-- Existing Phase 1 test patterns to mirror in new unit tests -->

From tests/unit/status-pill.test.tsx:
- File extension MUST be .tsx (JSX in .ts is rejected by tsc)
- @vitest-environment jsdom directive at top
- Uses @testing-library/react `render` + `screen`
- Asserts text + class + aria-label presence
</phase_1_test_patterns>
</context>

<tasks>

<task type="auto" tdd="true" agent="UI Designer">
  <name>Task 1: Build iteration-detail visualization primitives — BetaCIChart, ReplicationHash, EvidenceChain, DispositionMemo, IterationDetailHeader</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md (lines 297-470 — Component Inventory)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-RESEARCH.md (Pattern 4 — Inline SVG BetaCIChart math, Pattern 5 — details element, Pattern 6 — EvidenceChain dl/dt/dd)
    - /home/jmsbpp/apps/d2p/frontend/components/StatusPill.tsx (existing API to consume in IterationDetailHeader + DispositionMemo)
    - /home/jmsbpp/apps/d2p/frontend/components/IterationCountTile.tsx (existing Phase 1 token-using component for style reference)
    - /home/jmsbpp/apps/d2p/frontend/tests/unit/status-pill.test.tsx (test pattern — .tsx + jsdom env)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-CONTEXT.md ("Mobile responsive composition" block — IterationDetailHeader stacking at <sm)
  </read_first>
  <files>
    components/BetaCIChart.tsx,
    components/ReplicationHash.tsx,
    components/EvidenceChain.tsx,
    components/DispositionMemo.tsx,
    components/IterationDetailHeader.tsx,
    tests/unit/beta-ci-chart.test.tsx,
    tests/unit/replication-hash.test.tsx,
    tests/unit/evidence-chain.test.tsx,
    tests/unit/disposition-memo.test.tsx,
    tests/unit/iteration-detail-header.test.tsx
  </files>
  <behavior>
    BetaCIChart:
    - Test 1: renders an svg with role="img" and aria-label containing β value + CI bounds.
    - Test 2: includes sr-only table with β, CI lower, CI upper rows.
    - Test 3: when beta = 0.137, ci_lower = 0.092, ci_upper = 0.182, the rendered SVG contains a `<circle>` at the β position and `<line>` elements for CI whisker.
    - Test 4: zero-reference line is present (stroke-dasharray="3 3").
    - Test 5: viewBox is "0 0 300 60".

    ReplicationHash:
    - Test 6: truncates display to first-8 chars + ellipsis + last-4 chars when hash has length ≥ 16.
    - Test 7: full hash appears in title attribute and aria-label.
    - Test 8: copy button has aria-label "Copy replication hash" / "Copiar hash de replicación" (locale-aware via prop).
    - Test 9: click on copy button calls navigator.clipboard.writeText (mocked) with the full hash.

    EvidenceChain:
    - Test 10: renders <dl> with <dt>/<dd> pairs for each present field (beta / ci / p_value / sample_size / replication_hash).
    - Test 11: omits row entirely when field is null/undefined (does not render placeholder dashes for optional fields).
    - Test 12: BetaCIChart is composed inside the β row when beta + ci_lower + ci_upper are all present.
    - Test 13: heading element has id="evidence-heading" and section uses aria-labelledby.

    DispositionMemo:
    - Test 14: renders a <section> with H3 heading "Disposition" / "Disposición".
    - Test 15: prose content uses class `text-text-primary` (NOT text-text-muted — epistemic equality).
    - Test 16: contains NO <details> or <summary> element (no collapse).
    - Test 17: composes StatusPill with the iteration's status for screen-reader context.

    IterationDetailHeader:
    - Test 18: renders StatusPill + version + H1 + analysis date.
    - Test 19: H1 has the iteration title text and class containing 'text-3xl' or 'text-4xl' (display size).
    - Test 20: at viewport <640px (mobile), layout uses flex-col (sm:flex-row) — assert class contains 'flex-col sm:flex-row' for stacking.
  </behavior>
  <action>
    BetaCIChart.tsx (Server Component, no 'use client'):
    - Props: `{ beta: number; ciLower: number; ciUpper: number; label?: string }`.
    - Implementation per RESEARCH.md Pattern 4 verbatim: W=300, H=60, pad=20, axisMax = 1.5 * max(|ciLower|, |ciUpper|), axisMin = -axisMax.
    - SVG elements: zero reference `<line>` (dashed, currentColor 30% opacity), CI whisker `<line>`, two CI cap `<line>` elements (vertical, 10px height), β `<circle>` (r=5, fill="var(--color-accent-default)").
    - `<figure>` wraps SVG + sr-only `<table>` (β / CI lower / CI upper rows) + `<figcaption className="sr-only">`.
    - aria-label: `β = {beta.toFixed(4)}, 95% CI [{ciLower.toFixed(4)}, {ciUpper.toFixed(4)}]` (override via label prop).

    ReplicationHash.tsx (Client Component, 'use client'):
    - Props: `{ hash: string; copyLabel: string; copiedLabel: string }`.
    - Truncated display: hash.length ≥ 16 ? `${hash.slice(0,8)}…${hash.slice(-4)}` : hash.
    - useState for copied flag; setTimeout 1500ms to revert.
    - Copy button: ClipboardCopy icon, aria-label={copyLabel}, on success show transient tooltip with copiedLabel.
    - Wrapping `<span className="inline-flex items-center gap-2 font-mono text-xs text-text-secondary hover:border-accent-default/50 focus-within:ring-2 focus-within:ring-accent-default rounded">`.
    - Full hash also in `title` attribute and aria-label of the span.
    - On clipboard error: catch silently, do not crash UI.

    EvidenceChain.tsx (Server Component):
    - Props: `{ iteration: Iteration; t: (key: string) => string }` (t is the resolved next-intl translator — caller passes from RSC).
    - Render `<section aria-labelledby="evidence-heading">` with `<h2 id="evidence-heading">{t('iterations.detail.evidence.heading')}</h2>`.
    - `<dl className="divide-y divide-border-default">` containing one `<div className="flex flex-col sm:flex-row gap-1 sm:gap-4 py-3">` per present field:
      - β row: dt label, dd with formatted value (sign + 6 decimals via `iteration.beta > 0 ? '+' : ''`). If ci_lower + ci_upper also present, include BetaCIChart immediately after the dd value.
      - 95% CI row: range like `[{ciLower.toFixed(4)}, {ciUpper.toFixed(4)}]`.
      - p-value row: scientific notation via `iteration.p_value < 1e-4 ? iteration.p_value.toExponential(2) : iteration.p_value.toFixed(4)`.
      - Sample size row: `N = {sample_size}`.
      - Replication hash row: render ReplicationHash component, with t() lookups for labels.
    - Each row omitted if field is null/undefined. NO placeholder dashes.
    - All numeric values in `<dd className="font-mono text-sm text-text-primary">`.

    DispositionMemo.tsx (Server Component):
    - Props: `{ iteration: Iteration; locale: 'es-CO' | 'en'; t: (key: string) => string }`.
    - Render `<section aria-labelledby="disposition-heading">` with `<h2 id="disposition-heading" className="text-xl font-semibold text-text-primary mb-4">{t('iterations.detail.disposition.heading')}</h2>`.
    - Below H2: `<div className="flex items-center gap-3 mb-4">` containing StatusPill + small `<p className="text-sm text-text-secondary">{t('iterations.detail.disposition.fail_notice')}</p>` if status is FAIL.
    - Memo body: render `iteration.disposition_memo` (markdown string from frontmatter) inside `<div className="prose prose-sm max-w-3xl text-text-primary">`.
    - NO `<details>`, NO `<summary>`, NO `aria-expanded`. Memo is always fully visible.

    IterationDetailHeader.tsx (Server Component):
    - Props: `{ iteration: Iteration; locale: 'es-CO' | 'en'; t: (key: string) => string }`.
    - Outer `<header className="flex flex-col gap-3 pb-6 border-b border-border-default">`.
    - Top row: `<div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2 sm:gap-4">` containing StatusPill on left + version label (e.g., `<span className="text-sm text-text-muted font-mono">{t('iterations.detail.version_label')} v{iteration.version}</span>`) on right.
    - H1: `<h1 className="text-3xl sm:text-4xl font-semibold text-text-primary leading-tight">{locale === 'es-CO' ? iteration.title_es : iteration.title_en}</h1>`.
    - Date row: `<time className="text-xs text-text-muted" dateTime={iteration.analysis_date.toISOString()}>{formatDate(iteration.analysis_date, locale)}</time>` — use lib/format/date.ts.
    - Mobile stacking (CONTEXT.md spec): the flex-col → sm:flex-row pattern explicitly handles <sm by stacking title above status pill.

    Test files (all .tsx, @vitest-environment jsdom):
    - Use @testing-library/react for render/screen/queryAllByRole.
    - For ReplicationHash copy test: mock navigator.clipboard via `vi.stubGlobal('navigator', { clipboard: { writeText: vi.fn().mockResolvedValue(undefined) } })`.
    - Avoid `as any` — fully type the test fixtures using imported types from .velite.
  </action>
  <acceptance_criteria>
    - `test -f components/BetaCIChart.tsx components/ReplicationHash.tsx components/EvidenceChain.tsx components/DispositionMemo.tsx components/IterationDetailHeader.tsx` all succeed.
    - `grep -q 'navigator.clipboard' components/ReplicationHash.tsx`.
    - `grep -q "'use client'" components/ReplicationHash.tsx` (only this component is client).
    - `grep -L "'use client'" components/BetaCIChart.tsx components/EvidenceChain.tsx components/DispositionMemo.tsx components/IterationDetailHeader.tsx` — none of these contain 'use client'.
    - `grep -q '<dl' components/EvidenceChain.tsx && grep -q '<dt' components/EvidenceChain.tsx && grep -q '<dd' components/EvidenceChain.tsx`.
    - `grep -q 'viewBox=' components/BetaCIChart.tsx`.
    - `grep -q 'text-text-primary' components/DispositionMemo.tsx && ! grep -q '<details' components/DispositionMemo.tsx && ! grep -q 'aria-expanded' components/DispositionMemo.tsx`.
    - `grep -q 'flex-col sm:flex-row' components/IterationDetailHeader.tsx`.
    - `grep -q 'StatusPill' components/IterationDetailHeader.tsx && grep -q 'StatusPill' components/DispositionMemo.tsx`.
    - `pnpm vitest run tests/unit/beta-ci-chart.test.tsx tests/unit/replication-hash.test.tsx tests/unit/evidence-chain.test.tsx tests/unit/disposition-memo.test.tsx tests/unit/iteration-detail-header.test.tsx` exits 0.
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm biome check components/ tests/unit/` exits 0.
    - `npx --yes impeccable detect components/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f components/BetaCIChart.tsx && test -f components/ReplicationHash.tsx && test -f components/EvidenceChain.tsx && test -f components/DispositionMemo.tsx && test -f components/IterationDetailHeader.tsx && grep -q 'navigator.clipboard' components/ReplicationHash.tsx && grep -q "'use client'" components/ReplicationHash.tsx && ! grep -lq "'use client'" components/BetaCIChart.tsx && ! grep -lq "'use client'" components/EvidenceChain.tsx && ! grep -lq "'use client'" components/DispositionMemo.tsx && ! grep -lq "'use client'" components/IterationDetailHeader.tsx && grep -q '<dl' components/EvidenceChain.tsx && grep -q 'viewBox=' components/BetaCIChart.tsx && grep -q 'text-text-primary' components/DispositionMemo.tsx && ! grep -q '<details' components/DispositionMemo.tsx && grep -q 'flex-col sm:flex-row' components/IterationDetailHeader.tsx && grep -q 'StatusPill' components/IterationDetailHeader.tsx && grep -q 'StatusPill' components/DispositionMemo.tsx && pnpm vitest run tests/unit/beta-ci-chart.test.tsx tests/unit/replication-hash.test.tsx tests/unit/evidence-chain.test.tsx tests/unit/disposition-memo.test.tsx tests/unit/iteration-detail-header.test.tsx && pnpm tsc --noEmit && pnpm biome check components/ tests/unit/ && npx --yes impeccable detect components/</automated>
  </verify>
  <done>5 iteration-detail components + 5 unit tests exist; BetaCIChart renders inline SVG with sr-only table; ReplicationHash is the sole client component (copy-to-clipboard); EvidenceChain uses dl/dt/dd with omit-on-missing pattern; DispositionMemo has no collapse and uses text-text-primary; IterationDetailHeader stacks flex-col on mobile / sm:flex-row on tablet+; all unit tests + impeccable + biome + tsc green.</done>
</task>

<task type="auto" tdd="true" agent="UI Designer">
  <name>Task 2: Build catalog + lab-page components — IterationCatalogCard, PublicationCard, ContributorCard, NumberedStep, CheckmarkList</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md (lines 297-470 — Component Inventory, especially IterationCatalogCard min-h-[120px] and ContributorCard row-not-card)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-CONTEXT.md ("Mobile responsive composition" — NumberedStep stacks number above title at <md)
    - /home/jmsbpp/apps/d2p/frontend/components/StatusPill.tsx
    - /home/jmsbpp/apps/d2p/frontend/components/ui/button.tsx (for any CTA composition)
    - /home/jmsbpp/apps/d2p/frontend/lib/format/date.ts (locale-aware date formatting; reuse in catalog cards)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-RESEARCH.md (PITFALL #16 — no PASS-only filtering; PITFALL #5 — no icon-tile-above-heading)
  </read_first>
  <files>
    components/IterationCatalogCard.tsx,
    components/PublicationCard.tsx,
    components/ContributorCard.tsx,
    components/NumberedStep.tsx,
    components/CheckmarkList.tsx,
    components/ui/badge.tsx,
    tests/unit/iteration-catalog-card.test.tsx,
    tests/unit/publication-card.test.tsx,
    tests/unit/contributor-card.test.tsx,
    tests/unit/numbered-step.test.tsx,
    tests/unit/checkmark-list.test.tsx
  </files>
  <behavior>
    IterationCatalogCard:
    - Test 1: renders min-h-[120px] class on outer element.
    - Test 2: card is a single anchor `<a href="/apps/abrigo/iterations/{slug}/v{version}">` (entire card clickable).
    - Test 3: contains StatusPill with the iteration's status.
    - Test 4: contains H3 with title.
    - Test 5: when beta is present, renders β value in font-mono; when absent renders nothing (NOT em-dash or "N/A").
    - Test 6: card height for status=PASS equals card height for status=FAIL (same fixture data structure, same min-h-[120px] class — assert identical class strings).

    PublicationCard:
    - Test 7: renders H3 title.
    - Test 8: renders type Badge (e.g., "Memo" / "Working draft").
    - Test 9: when order prop is provided, renders order prefix in `font-mono text-accent-default text-xs` ("01", "02"…).
    - Test 10: when external_url present, renders "Read document" link with ArrowUpRight icon and target="_blank" rel="noopener noreferrer".
    - Test 11: contains 2-line clamp class on abstract (`line-clamp-2`).

    ContributorCard:
    - Test 12: renders as a flex row `<li>` (not a card).
    - Test 13: contains <img> with alt text "{name}'s avatar".
    - Test 14: GitHub link uses ArrowUpRight icon and points to https://github.com/{handle}.
    - Test 15: assert NO border-radius on the row container that would simulate a card; only `border-b border-border-default` divider treatment.

    NumberedStep:
    - Test 16: renders number in font-mono text-accent-default.
    - Test 17: layout is `flex-col md:flex-row` (stacks number above title at <md per CONTEXT.md).
    - Test 18: title is H3.
    - Test 19: body has max-w-2xl class (45-75 char reading length).

    CheckmarkList:
    - Test 20: renders <ul> (not <ol>, not <div>).
    - Test 21: each item has CheckCircle2 icon with text-status-pass color class.
    - Test 22: items are flex items-start gap-2 (icon top-aligned with text).
  </behavior>
  <action>
    IterationCatalogCard.tsx (Server Component):
    - Props: `{ iteration: Iteration; locale: 'es-CO' | 'en'; labels: { statusLabels: Record<IterationStatus, string> } }`.
    - Outer: `<a href={`/apps/abrigo/iterations/${iteration.slug}/v${iteration.version}`} className="group block rounded-lg border border-border-default bg-bg-surface min-h-[120px] p-4 transition-shadow motion-safe:hover:shadow-sm motion-safe:hover:border-accent-default/30 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-default">`.
    - Inside: `<div className="flex flex-col gap-2">` containing:
      - Row 1: `<div className="flex items-start gap-3"><StatusPill status={iteration.status} label={labels.statusLabels[iteration.status]} /><h3 className="text-xl font-semibold text-text-primary leading-tight flex-1">{locale === 'es-CO' ? iteration.title_es : iteration.title_en}</h3></div>`.
      - Row 2 (β key metric): `iteration.beta != null && <p className="text-sm font-mono text-text-secondary">β = {iteration.beta > 0 ? '+' : ''}{iteration.beta.toFixed(4)}</p>`.
      - Row 3 (metadata): `<div className="flex items-center gap-3 text-xs text-text-muted mt-auto"><time dateTime={iteration.analysis_date.toISOString()}>{formatDate(iteration.analysis_date, locale)}</time><span>{iteration.slug}</span></div>`.

    PublicationCard.tsx (Server Component):
    - Props: `{ research: Research; locale: 'es-CO' | 'en'; t: (key: string) => string }`.
    - Outer: `<article className="group rounded-lg border border-border-default bg-bg-surface p-6 transition-shadow motion-safe:hover:shadow-sm">`.
    - Optional order prefix at top: `{research.order != null && <span className="font-mono text-accent-default text-xs">{String(research.order).padStart(2, '0')}</span>}`.
    - H3 title: `<h3 className="text-xl font-semibold text-text-primary">{locale === 'es-CO' ? research.title_es : research.title_en}</h3>`.
    - Metadata row: date + Badge with type label.
    - Abstract: `<p className="mt-2 text-sm text-text-secondary line-clamp-2">{locale === 'es-CO' ? research.summary_es : research.summary_en}</p>`.
    - Read CTA: `{research.external_url && <a href={research.external_url} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-1 text-sm text-accent-default underline-offset-2 hover:underline mt-3">{t('research.cta.read_document')}<ArrowUpRight className="h-3 w-3" /></a>}`.

    Badge (components/ui/badge.tsx):
    - Create only if not present (shadcn standard). Use Tailwind variants: default, outline, secondary. For PublicationCard, variant="outline".

    ContributorCard.tsx (Server Component):
    - Props: `{ contributor: Contributor; t: (key: string) => string }` where Contributor type is `{ slug: string; name: string; role_es: string; role_en: string; github_handle: string; avatar_url?: string; focus_iteration_slug?: string }`.
    - Outer: `<li className="flex items-center gap-4 py-4 border-b border-border-default last:border-b-0">`.
    - Avatar: `<img src={avatar_url ?? `https://github.com/${github_handle}.png`} alt={`${name}'s avatar`} className="h-10 w-10 rounded-full bg-bg-surface" loading="lazy" />`.
    - Right side `<div className="flex-1 min-w-0">`: name (text-sm font-semibold text-text-primary), role (text-sm text-text-secondary).
    - Far right: GitHub link with ArrowUpRight + current iteration label.
    - NO outer card border-radius wrapping the row (only border-b — divider treatment per UI-SPEC).

    NumberedStep.tsx (Server Component):
    - Props: `{ number: string; title: string; body: string }`.
    - Outer: `<div className="flex flex-col md:flex-row gap-4 md:gap-8">`.
    - Number block: `<span className="font-mono text-2xl text-accent-default min-w-[3rem]">{number}</span>`.
    - Right block: `<div className="flex-1"><h3 className="text-xl font-semibold text-text-primary">{title}</h3><p className="mt-2 text-base text-text-secondary leading-relaxed max-w-2xl">{body}</p></div>`.

    CheckmarkList.tsx (Server Component):
    - Props: `{ items: string[] }`.
    - Render `<ul className="flex flex-col gap-3">` with `<li key={...} className="flex items-start gap-2"><CheckCircle2 className="h-4 w-4 text-status-pass mt-1 shrink-0" aria-hidden="true" /><span className="text-base text-text-primary">{item}</span></li>` per item.

    All unit tests:
    - .tsx files with @vitest-environment jsdom.
    - Use @testing-library/react.
    - For equal-height assertion: render two cards with same fixture but different status, assert their className includes 'min-h-[120px]' (string match).
  </action>
  <acceptance_criteria>
    - All 5 component files + 5 test files exist (or 6 if Badge needed).
    - `grep -q 'min-h-\[120px\]' components/IterationCatalogCard.tsx`.
    - `grep -q 'StatusPill' components/IterationCatalogCard.tsx`.
    - `grep -q 'flex-col md:flex-row' components/NumberedStep.tsx`.
    - `grep -q 'CheckCircle2' components/CheckmarkList.tsx && grep -q 'text-status-pass' components/CheckmarkList.tsx`.
    - `grep -q '<li' components/ContributorCard.tsx && ! grep -q 'rounded-lg' components/ContributorCard.tsx` (no card-radius — divider row pattern).
    - `grep -q 'line-clamp-2' components/PublicationCard.tsx`.
    - `pnpm vitest run tests/unit/iteration-catalog-card.test.tsx tests/unit/publication-card.test.tsx tests/unit/contributor-card.test.tsx tests/unit/numbered-step.test.tsx tests/unit/checkmark-list.test.tsx` exits 0.
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm biome check components/ tests/unit/` exits 0.
    - `npx --yes impeccable detect components/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f components/IterationCatalogCard.tsx && test -f components/PublicationCard.tsx && test -f components/ContributorCard.tsx && test -f components/NumberedStep.tsx && test -f components/CheckmarkList.tsx && grep -q 'min-h-\[120px\]' components/IterationCatalogCard.tsx && grep -q 'StatusPill' components/IterationCatalogCard.tsx && grep -q 'flex-col md:flex-row' components/NumberedStep.tsx && grep -q 'CheckCircle2' components/CheckmarkList.tsx && grep -q 'text-status-pass' components/CheckmarkList.tsx && grep -q '<li' components/ContributorCard.tsx && ! grep -q 'rounded-lg' components/ContributorCard.tsx && grep -q 'line-clamp-2' components/PublicationCard.tsx && pnpm vitest run tests/unit/iteration-catalog-card.test.tsx tests/unit/publication-card.test.tsx tests/unit/contributor-card.test.tsx tests/unit/numbered-step.test.tsx tests/unit/checkmark-list.test.tsx && pnpm tsc --noEmit && pnpm biome check components/ tests/unit/ && npx --yes impeccable detect components/</automated>
  </verify>
  <done>5 catalog/lab-page components + 5 unit tests exist; IterationCatalogCard uses min-h-[120px] + StatusPill; PublicationCard supports order prefix and Badge type; ContributorCard is a row-with-divider (not a card); NumberedStep stacks at <md per panoptic pattern; CheckmarkList uses semantic PASS-green icons; all tests + impeccable + biome + tsc green.</done>
</task>

</tasks>

<verification>
- 10 new components in `components/`
- 10 sibling unit tests in `tests/unit/`
- All tests pass (`pnpm vitest run tests/unit/`)
- TypeScript strict mode passes (`pnpm tsc --noEmit`)
- Biome lint passes (`pnpm biome check components/ tests/unit/`)
- Impeccable detector passes (`npx --yes impeccable detect components/`)
- StatusPill is consumed (imported) by IterationCatalogCard + IterationDetailHeader + DispositionMemo (ITER-07 reuse contract)
- ReplicationHash is the only Client Component; others are RSC
</verification>

<success_criteria>
Plans 02-03, 02-05, 02-06, 02-07 can import any of the 10 components without re-deriving visual contracts. Every component renders with WCAG AA contrast, color + icon + text (never color-alone) status encoding, and impeccable anti-pattern compliance. Unit tests cover shape, status variants, ARIA, and the epistemic-equality invariant (equal card dimensions across statuses).
</success_criteria>

<output>
After completion, create `.planning/phases/02-research-lab-presence-and-iteration-catalog/02-02-SUMMARY.md` documenting:
- Each component's exported props interface (verbatim)
- Whether component is RSC or Client (and why ReplicationHash is the only Client)
- StatusPill reuse map (which components import it)
- Test count per component
- Any deviations from UI-SPEC noted
</output>

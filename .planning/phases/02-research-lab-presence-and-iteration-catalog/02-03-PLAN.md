---
phase: 02-research-lab-presence-and-iteration-catalog
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - app/(lab)/page.tsx
  - app/(lab)/about/page.tsx
  - messages/es-CO/lab.json
  - messages/en/lab.json
  - messages/es-CO/about.json
  - messages/en/about.json
  - tests/e2e/homepage-content.spec.ts
  - tests/e2e/about-page.spec.ts
  - tests/unit/homepage-counts.test.ts
autonomous: true
agent: Frontend Developer
requirements: [LAB-01, LAB-05]
must_haves:
  truths:
    - "LAB-01: / renders DS2P Labs mission, What is d2-π explainer, Apps overview card linking to /apps/abrigo, headline iteration counts (Pass / Fail / Parked / In Progress) derived from Velite, and link to https://github.com/wvs-finance"
    - "LAB-05: /about renders 5 NumberedStep components (Spec → Data → Estimation → Tests → Disposition) and 4 CheckmarkList items (the 4 lab invariants) authored in both es-CO and en"
    - "Iteration counts on homepage are derived from .velite (build-time), NOT hardcoded — IterationCountTile receives counts via prop"
    - "Both pages render in es-CO and en with no untranslated keys (no 'lab.X.Y' literal strings leak)"
    - "Both pages are RSC (no 'use client' — pass through (lab) layout)"
    - "Footer reuse: link to wvs-finance GitHub org is present on /"
  artifacts:
    - path: app/(lab)/page.tsx
      provides: "Lab umbrella homepage with mission + Apps overview + iteration counts + GitHub link"
      contains: "IterationCountTile"
    - path: app/(lab)/about/page.tsx
      provides: "Methodology page — 5 NumberedSteps + 4 CheckmarkList items"
      contains: "NumberedStep"
    - path: messages/es-CO/lab.json
      provides: "Authored es-CO copy for homepage hero / mission / explainer / counts heading"
    - path: messages/en/lab.json
      provides: "Authored en parallel copy"
    - path: messages/es-CO/about.json
      provides: "Authored es-CO copy for methodology page (5 steps + 4 commitments)"
    - path: messages/en/about.json
      provides: "Authored en parallel copy"
    - path: tests/e2e/homepage-content.spec.ts
      provides: "Filled Playwright spec covering LAB-01 truths"
    - path: tests/e2e/about-page.spec.ts
      provides: "Filled Playwright spec covering LAB-05 truths"
  key_links:
    - from: app/(lab)/page.tsx
      to: ".velite (iterations collection)"
      via: "build-time count query"
      pattern: "from ['\"]@?/?\\.velite"
    - from: app/(lab)/page.tsx
      to: components/IterationCountTile.tsx
      via: "import IterationCountTile"
      pattern: "IterationCountTile"
    - from: app/(lab)/about/page.tsx
      to: components/NumberedStep.tsx
      via: "import NumberedStep"
      pattern: "NumberedStep"
    - from: app/(lab)/about/page.tsx
      to: components/CheckmarkList.tsx
      via: "import CheckmarkList"
      pattern: "CheckmarkList"
---

<objective>
Deliver LAB-01 (lab umbrella homepage at `/`) and LAB-05 (methodology page at `/about`) with real authored content in both es-CO and en, consuming Phase 2 components from Plan 02-02 and Velite-derived iteration counts from Plan 02-01's extended pipeline.

Purpose: Two umbrella pages, both pure RSC under `(lab)` route group (no wallet imports — architecture test enforced). Both pages convert from Phase 1 placeholders to real Phase 2 content. The author's voice register (anti-fishing, no marketing prose) is non-negotiable — copy must mirror the abrigo-analytics README tone.

Output: 2 working pages with authored es-CO + en copy, 2 filled e2e specs, 1 filled unit test for homepage counts, all green under biome + tsc + impeccable.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-CONTEXT.md
@app/(lab)/page.tsx
@app/(lab)/layout.tsx
@components/IterationCountTile.tsx
@components/NumberedStep.tsx
@components/CheckmarkList.tsx
@messages/es-CO/lab.json
@messages/en/lab.json
@messages/es-CO/about.json
@messages/en/about.json

<interfaces>
<!-- Plan 02-01 + 02-02 contracts -->

From Plan 02-01: `.velite/iterations.ts` is generated; import as:
```typescript
import { iterations } from '@/.velite'
// iterations: Iteration[]
```

From Plan 02-02:
```typescript
import { NumberedStep } from '@/components/NumberedStep'
import { CheckmarkList } from '@/components/CheckmarkList'
```

From Phase 1:
```typescript
import { IterationCountTile } from '@/components/IterationCountTile'
// Props: { status: IterationStatus; count: number; label: string }
```

From next-intl RSC API:
```typescript
import { getTranslations, getLocale } from 'next-intl/server'
const t = await getTranslations()
const locale = await getLocale()
```
</interfaces>

<authored_copy>
<!-- Copy values verbatim from UI-SPEC §Copywriting Contract. Both locales authored side-by-side. -->

LAB-01 Homepage (existing keys in messages/{locale}/lab.json from Phase 1 — extend, don't replace):

```
lab.hero.h1 — "DS2P Labs"
lab.hero.subline — es-CO: "Instrumentos de cobertura para exposición macroeconómica" / en: "Hedging instruments for macro-risk exposure"
lab.mission.body — es-CO: "Diseñamos y desplegamos instrumentos de cobertura convexa en ecosistemas EVM, validados por econometría estructural antes de su lanzamiento. El primer instrumento: Abrigo, para trabajadores colombianos expuestos al riesgo COP/USD." / en: "We design and deploy permissionless convex-hedge instruments on EVM ecosystems, validated by structural econometrics before deployment. First instrument: Abrigo, for Colombian workers exposed to COP/USD macro risk."
lab.what_is_d2pi.heading — es-CO: "¿Qué es d2-π?" / en: "What is d2-π?"
lab.what_is_d2pi.body — es-CO: "d2-π (DS2P Labs) es un laboratorio de investigación que diseña instrumentos financieros convexos. La letra π denota el payoff; el operador ∂² captura la convexidad — la curvatura del payoff respecto al subyacente. Cada instrumento que el lab incuba debe primero pasar por evidencia estructural antes de su despliegue." / en: "d2-π (DS2P Labs) is a research lab that designs convex financial instruments. π denotes the payoff; the ∂² operator captures convexity — the curvature of payoff with respect to the underlying. Every instrument the lab incubates must clear structural evidence before deployment."
lab.apps.heading — es-CO: "Instrumentos activos" / en: "Active instruments"
lab.apps.abrigo.name — "Abrigo"
lab.apps.abrigo.description — es-CO: "Coberturas convexas para trabajadores colombianos — gamma ∂²Π" / en: "Convex hedges for Colombian wage-earners — ∂²Π gamma"
lab.apps.abrigo.cta — es-CO: "Ver Abrigo" / en: "Explore Abrigo"
lab.counts.heading — es-CO: "Estado del catálogo" / en: "Catalog status"
lab.counts.empty — es-CO: "Aún no hay iteraciones registradas." / en: "No iterations recorded yet."
lab.github.label — es-CO: "wvs-finance en GitHub" / en: "wvs-finance on GitHub"
```

LAB-05 About / Methodology (new in messages/{locale}/about.json — extend stubs from Plan 02-01):

```
about.h1 — es-CO: "Metodología" / en: "Methodology"
about.subheading — es-CO: "La disciplina anti-fishing del laboratorio: especificaciones pre-comprometidas, flujo de trabajo estructural, y honestidad epistémica." / en: "The lab's anti-fishing discipline: pre-committed specs, structural workflow, and epistemic honesty."
about.intro.p1 — es-CO: "Investigamos antes de desplegar. Cada instrumento de cobertura que el lab pone en producción debe haber sido validado por econometría estructural — no por análisis post-hoc, no por selección de outcomes favorables, no por narrativa." / en: "We research before we deploy. Every hedging instrument the lab ships must clear structural-econometrics validation — not post-hoc analysis, not favorable-outcome selection, not narrative."
about.intro.p2 — es-CO: "Esta página documenta la disciplina que aplicamos a cada iteración. Las cinco etapas y los cuatro compromisos están vivos: se aplican igualmente al éxito y al fracaso." / en: "This page documents the discipline we apply to every iteration. The five stages and four commitments are live: they apply equally to success and failure."

about.steps.01.number — "01"
about.steps.01.title — es-CO: "Especificación" / en: "Spec"
about.steps.01.body — es-CO: "Pre-comprometemos la hipótesis, el universo de datos, los identificadores instrumentales, y los umbrales de prueba ANTES de mirar resultados. La especificación se publica como documento citable." / en: "We pre-commit the hypothesis, data universe, instrumental identifiers, and test thresholds BEFORE looking at results. The spec is published as a citable document."

about.steps.02.number — "02"
about.steps.02.title — es-CO: "Datos" / en: "Data"
about.steps.02.body — es-CO: "Extraemos los paneles macro y micro del registro fijado en la especificación. Cualquier ajuste posterior — limpieza, imputación, recorte — se documenta en un bloque de cita de decisión antes de aplicarse." / en: "We pull the macro and micro panels from the registry fixed in the spec. Any later adjustment — cleaning, imputation, trimming — is documented in a decision-citation block before being applied."

about.steps.03.number — "03"
about.steps.03.title — es-CO: "Estimación" / en: "Estimation"
about.steps.03.body — es-CO: "OLS estructural con errores estándar HAC. Las pruebas de robustez (sensibilidad a la ventana, al sub-panel, a la transformación) se ejecutan automáticamente — no son opcionales." / en: "Structural OLS with HAC standard errors. Robustness checks (window, sub-panel, transform sensitivity) run automatically — they are not optional."

about.steps.04.number — "04"
about.steps.04.title — es-CO: "Pruebas" / en: "Tests"
about.steps.04.body — es-CO: "Cada celda de código pasa por un trío de verificación: la celda corre, los outputs son los esperados, y un revisor lo confirma. Sin el trío, la celda no se considera resuelta." / en: "Every code cell passes a trio checkpoint: the cell runs, the outputs are what we expected, and a reviewer confirms. Without the trio, the cell is not considered resolved."

about.steps.05.number — "05"
about.steps.05.title — es-CO: "Disposición" / en: "Disposition"
about.steps.05.body — es-CO: "La iteración cierra con un memo de disposición: aprobado, rechazado, o pausado. El memo explica el resultado — y, en caso de rechazo, qué se aprendió. El rechazo tiene el mismo peso narrativo que la aprobación." / en: "The iteration closes with a disposition memo: pass, fail, or parked. The memo explains the outcome — and, on rejection, what was learned. Rejection carries equal narrative weight as approval."

about.commitments.intro — es-CO: "Compromisos del laboratorio en cada iteración:" / en: "Lab commitments on every iteration:"
about.commitments.item_1 — es-CO: "El bloque de cita de decisión precede cada prueba" / en: "Decision-citation block precedes every test"
about.commitments.item_2 — es-CO: "Trío de verificación después de cada celda de código" / en: "Trio checkpoint after every code cell"
about.commitments.item_3 — es-CO: "DETENER si hay conflicto especificación–datos" / en: "HALT on spec–data conflict"
about.commitments.item_4 — es-CO: "El rechazo tiene el mismo peso narrativo que la aprobación" / en: "Rejection carries equal narrative weight as approval"

about.reference.heading — es-CO: "Referencia metodológica" / en: "Methodology reference"
about.reference.link_label — es-CO: "Leer la metodología de abrigo-analytics" / en: "Read abrigo-analytics methodology"
```
</authored_copy>
</context>

<tasks>

<task type="auto" tdd="true" agent="Frontend Developer">
  <name>Task 1: Build lab homepage (/) with Velite-derived counts + Apps overview + GitHub link</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/frontend/app/(lab)/page.tsx (current Phase 1 placeholder — replace contents, preserve any layout-level concerns)
    - /home/jmsbpp/apps/d2p/frontend/app/(lab)/layout.tsx (verify no wallet provider — RSC purity)
    - /home/jmsbpp/apps/d2p/frontend/components/IterationCountTile.tsx (existing component API)
    - /home/jmsbpp/apps/d2p/frontend/messages/es-CO/lab.json + messages/en/lab.json (Phase 1 keys to extend)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md (lines 485-495 — Homepage section order; lines 199-209 — homepage copy)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-RESEARCH.md (Pattern 2 — Velite count query)
    - /home/jmsbpp/apps/d2p/frontend/tests/e2e/homepage-content.spec.ts (Wave 0 stub from Plan 02-01 — fill with real assertions)
  </read_first>
  <files>
    app/(lab)/page.tsx,
    messages/es-CO/lab.json,
    messages/en/lab.json,
    tests/e2e/homepage-content.spec.ts,
    tests/unit/homepage-counts.test.ts
  </files>
  <behavior>
    Unit (homepage-counts.test.ts):
    - Test 1: countsByStatus({iterations: [{status:'PASS'},{status:'FAIL'},{status:'PASS'}]}) returns {PASS:2, FAIL:1, PARKED:0, IN_PROGRESS:0, total:3}.
    - Test 2: countsByStatus({iterations: []}) returns {PASS:0, FAIL:0, PARKED:0, IN_PROGRESS:0, total:0}.

    E2E (homepage-content.spec.ts) — covers LAB-01:
    - Test 3: GET / returns 200.
    - Test 4: page contains text "DS2P Labs" (H1).
    - Test 5: page contains the mission body sentence "convex-hedge instruments" (en) or "cobertura convexa" (es-CO via cookie).
    - Test 6: page contains "What is d2-π" or "¿Qué es d2-π?" heading.
    - Test 7: page renders an Apps overview card with link href="/apps/abrigo".
    - Test 8: page renders 4 IterationCountTile elements (one per status) — count by `[data-testid="iteration-count-tile"]` or by aria-label containing each status label.
    - Test 9: page contains an anchor with href="https://github.com/wvs-finance".
    - Test 10: page is RSC (no client bundle for homepage route). Verified via `expect(await page.evaluate(() => typeof window === 'object')).toBe(true)` (trivially true) PLUS structural assertion that `app/(lab)/page.tsx` has no 'use client' directive.
  </behavior>
  <action>
    1. Extract a tiny pure helper `countsByStatus(iterations)` into the page file (or lib/iterations/counts.ts if it grows). Returns `{ PASS, FAIL, PARKED, IN_PROGRESS, total }`. Unit-test it in homepage-counts.test.ts.

    2. Rewrite app/(lab)/page.tsx as an async RSC. Pseudo-skeleton:

    ```typescript
    // app/(lab)/page.tsx — NO 'use client'
    import { getTranslations, getLocale } from 'next-intl/server'
    import { iterations } from '@/.velite'
    import { IterationCountTile } from '@/components/IterationCountTile'
    import { ArrowUpRight } from 'lucide-react'

    export default async function HomePage() {
      const t = await getTranslations()
      const locale = await getLocale()
      const counts = countsByStatus(iterations)
      const isEmpty = counts.total === 0

      return (
        <main className="max-w-[1200px] mx-auto px-4 sm:px-6 lg:px-8">
          {/* Hero */}
          <section className="py-20 lg:py-[120px] text-center">
            {/* ∂²Π SVG logo placeholder — use a simple <svg> or text mark; full lockup is Phase polish */}
            <h1 className="text-4xl font-semibold text-text-primary leading-tight">{t('lab.hero.h1')}</h1>
            <p className="mt-4 text-base text-text-secondary">{t('lab.hero.subline')}</p>
            <p className="mt-6 max-w-2xl mx-auto text-base text-text-primary leading-relaxed">{t('lab.mission.body')}</p>
          </section>

          {/* What is d2-π */}
          <section className="py-12">
            <h2 id="what-is-d2pi" className="text-3xl font-semibold text-text-primary">{t('lab.what_is_d2pi.heading')}</h2>
            <p className="mt-4 max-w-2xl text-base text-text-secondary leading-relaxed">{t('lab.what_is_d2pi.body')}</p>
          </section>

          {/* Apps overview — single Abrigo card (not a grid) */}
          <section className="py-12">
            <h2 className="text-3xl font-semibold text-text-primary mb-6">{t('lab.apps.heading')}</h2>
            <a href="/apps/abrigo" className="group block max-w-2xl rounded-lg border border-border-default bg-bg-surface p-6 motion-safe:hover:shadow-sm motion-safe:hover:border-accent-default/30 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-default">
              <h3 className="text-xl font-semibold text-text-primary">{t('lab.apps.abrigo.name')}</h3>
              <p className="mt-2 text-base text-text-secondary">{t('lab.apps.abrigo.description')}</p>
              <span className="mt-4 inline-flex items-center gap-1 text-sm text-accent-default underline-offset-2 group-hover:underline">{t('lab.apps.abrigo.cta')} <ArrowUpRight className="h-3 w-3" /></span>
            </a>
          </section>

          {/* Iteration count rollup — divide-y list per epistemic equality (NOT a 4-card grid) */}
          <section className="py-12">
            <h2 className="text-3xl font-semibold text-text-primary mb-6">{t('lab.counts.heading')}</h2>
            {isEmpty ? (
              <p className="text-base text-text-secondary">{t('lab.counts.empty')}</p>
            ) : (
              <ul className="divide-y divide-border-default max-w-2xl">
                {(['PASS','FAIL','PARKED','IN_PROGRESS'] as const).map(status => (
                  <li key={status} className="py-4">
                    <IterationCountTile status={status} count={counts[status]} label={t(`iterations.status.${status.toLowerCase()}.label`)} />
                  </li>
                ))}
              </ul>
            )}
          </section>

          {/* GitHub link */}
          <section className="py-12">
            <a href="https://github.com/wvs-finance" target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-1 text-sm text-accent-default underline-offset-2 hover:underline">
              {t('lab.github.label')} <ArrowUpRight className="h-3 w-3" />
            </a>
          </section>
        </main>
      )
    }

    export function countsByStatus(iterations: { status: 'PASS'|'FAIL'|'PARKED'|'IN_PROGRESS' }[]) {
      const acc = { PASS:0, FAIL:0, PARKED:0, IN_PROGRESS:0, total:0 }
      for (const it of iterations) { acc[it.status]++; acc.total++ }
      return acc
    }
    ```

    3. Extend messages/{es-CO,en}/lab.json with the keys listed in `<authored_copy>` above. PRESERVE all existing Phase 1 keys (nav, common). The new keys are nested under `lab.hero / lab.mission / lab.what_is_d2pi / lab.apps / lab.counts / lab.github` — do not flatten.

    4. Fill tests/e2e/homepage-content.spec.ts with real Playwright assertions per the behavior list above. Use `page.goto('/')`. For es-CO test, set NEXT_LOCALE cookie before navigation. Use `data-testid="iteration-count-tile"` on the IterationCountTile root (add the attribute if it doesn't already exist in the component — minor non-breaking update to components/IterationCountTile.tsx is acceptable).

    5. Fill tests/unit/homepage-counts.test.ts with the two countsByStatus assertions. Use @vitest-environment node (no React).

    6. Do NOT add a `'use client'` directive to the page file. Verify with `! grep -q "'use client'" app/(lab)/page.tsx`.

    7. PITFALL #16 enforcement: do NOT filter iterations by status before passing to count. Show ALL statuses, including 0-count.
  </action>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/homepage-counts.test.ts` exits 0.
    - `! grep -q "'use client'" app/\(lab\)/page.tsx` (RSC purity).
    - `grep -q 'github.com/wvs-finance' app/\(lab\)/page.tsx`.
    - `grep -q 'apps/abrigo' app/\(lab\)/page.tsx`.
    - `grep -q 'iterations' app/\(lab\)/page.tsx` (import from .velite).
    - All required keys exist in both locale files: `node -e "const f=require('./messages/es-CO/lab.json'); ['hero','mission','what_is_d2pi','apps','counts','github'].forEach(k=>{if(!f.lab[k])throw new Error('missing '+k)})"` (and same for en).
    - i18n-coverage test (Plan 02-01) still passes: `pnpm vitest run tests/unit/i18n-coverage.test.ts`.
    - `pnpm playwright test tests/e2e/homepage-content.spec.ts` exits 0 against Vercel preview OR local `pnpm dev`.
    - `pnpm tsc --noEmit && pnpm biome check app/\(lab\)/page.tsx messages/ tests/` all exit 0.
    - `npx --yes impeccable detect app/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "'use client'" app/\(lab\)/page.tsx && grep -q 'github.com/wvs-finance' app/\(lab\)/page.tsx && grep -q 'apps/abrigo' app/\(lab\)/page.tsx && grep -q '\.velite' app/\(lab\)/page.tsx && pnpm vitest run tests/unit/homepage-counts.test.ts tests/unit/i18n-coverage.test.ts && node -e "const f=require('./messages/es-CO/lab.json'); ['hero','mission','what_is_d2pi','apps','counts','github'].forEach(k=>{if(!f.lab[k])throw new Error('es-CO missing '+k)})" && node -e "const f=require('./messages/en/lab.json'); ['hero','mission','what_is_d2pi','apps','counts','github'].forEach(k=>{if(!f.lab[k])throw new Error('en missing '+k)})" && pnpm tsc --noEmit && pnpm biome check app/\(lab\)/page.tsx messages/es-CO/lab.json messages/en/lab.json tests/e2e/homepage-content.spec.ts tests/unit/homepage-counts.test.ts && npx --yes impeccable detect app/ && pnpm playwright test tests/e2e/homepage-content.spec.ts</automated>
  </verify>
  <done>LAB-01 delivered: / renders mission + What is d2-π + Apps overview Abrigo card + Velite-derived iteration counts (all 4 statuses, including 0 counts) + wvs-finance GitHub link, in both locales, as pure RSC; homepage-content.spec.ts and homepage-counts.test.ts both green.</done>
</task>

<task type="auto" tdd="true" agent="Frontend Developer">
  <name>Task 2: Build /about methodology page with 5 NumberedSteps + 4 CheckmarkList items</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/frontend/components/NumberedStep.tsx (from Plan 02-02)
    - /home/jmsbpp/apps/d2p/frontend/components/CheckmarkList.tsx (from Plan 02-02)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md (lines 528-541 — About page section order; lines 260-275 — about page copy)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-CONTEXT.md ("About page content composition" block)
    - /home/jmsbpp/apps/d2p/frontend/messages/es-CO/about.json (stub from Plan 02-01 — fill with full content)
    - /home/jmsbpp/apps/d2p/frontend/messages/en/about.json (stub from Plan 02-01)
    - /home/jmsbpp/apps/d2p/frontend/tests/e2e/about-page.spec.ts (Wave 0 stub from Plan 02-01)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/README.md (author's voice register — read FIRST for tone calibration)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/CLAUDE.md (anti-fishing discipline canonical description)
  </read_first>
  <files>
    app/(lab)/about/page.tsx,
    messages/es-CO/about.json,
    messages/en/about.json,
    tests/e2e/about-page.spec.ts
  </files>
  <behavior>
    E2E (about-page.spec.ts) — covers LAB-05:
    - Test 1: GET /about returns 200.
    - Test 2: page H1 contains "Methodology" (en) or "Metodología" (es-CO).
    - Test 3: page contains exactly 5 elements matching the NumberedStep pattern — `[data-testid="numbered-step"]` count is 5.
    - Test 4: each NumberedStep has number 01..05 in font-mono accent color.
    - Test 5: page contains exactly 4 CheckmarkList items — `ul[data-testid="commitments-list"] > li` count is 4.
    - Test 6: each commitment item contains a CheckCircle2 icon (SVG with lucide attribution or aria-label).
    - Test 7: locale switching: with es-CO cookie, H1 reads "Metodología".
    - Test 8: page contains text "Decision-citation block precedes every test" (en) or its es-CO equivalent.
    - Test 9: no marketing slop: page does NOT contain banned phrases — "Empower", "cutting-edge", "unlock your potential", "leverage our platform" (case-insensitive grep).
  </behavior>
  <action>
    1. Create app/(lab)/about/page.tsx as async RSC. Skeleton:

    ```typescript
    // app/(lab)/about/page.tsx — RSC, no 'use client'
    import { getTranslations } from 'next-intl/server'
    import { NumberedStep } from '@/components/NumberedStep'
    import { CheckmarkList } from '@/components/CheckmarkList'
    import { ArrowUpRight } from 'lucide-react'

    export default async function AboutPage() {
      const t = await getTranslations()
      const stepKeys = ['01','02','03','04','05'] as const

      return (
        <main className="max-w-[1200px] mx-auto px-4 sm:px-6 lg:px-8">
          <section className="py-12 lg:py-20">
            <h1 className="text-4xl font-semibold text-text-primary">{t('about.h1')}</h1>
            <p className="mt-4 max-w-2xl text-base text-text-secondary leading-relaxed">{t('about.subheading')}</p>
          </section>

          <section className="py-12 max-w-3xl">
            <p className="text-base text-text-primary leading-relaxed">{t('about.intro.p1')}</p>
            <p className="mt-4 text-base text-text-primary leading-relaxed">{t('about.intro.p2')}</p>
          </section>

          <section className="py-12">
            <h2 className="text-3xl font-semibold text-text-primary mb-12 sr-only">Pipeline</h2>
            <div className="flex flex-col gap-16">
              {stepKeys.map(k => (
                <div key={k} data-testid="numbered-step">
                  <NumberedStep
                    number={t(`about.steps.${k}.number`)}
                    title={t(`about.steps.${k}.title`)}
                    body={t(`about.steps.${k}.body`)}
                  />
                </div>
              ))}
            </div>
          </section>

          <section className="py-12 max-w-3xl">
            <p className="text-base text-text-primary">{t('about.commitments.intro')}</p>
            <div className="mt-4" data-testid="commitments-wrapper">
              <CheckmarkList items={[
                t('about.commitments.item_1'),
                t('about.commitments.item_2'),
                t('about.commitments.item_3'),
                t('about.commitments.item_4'),
              ]} />
            </div>
          </section>

          <section className="py-12">
            <h2 className="text-xl font-semibold text-text-primary">{t('about.reference.heading')}</h2>
            <a href="https://github.com/wvs-finance/abrigo-analytics#readme" target="_blank" rel="noopener noreferrer" className="mt-3 inline-flex items-center gap-1 text-sm text-accent-default underline-offset-2 hover:underline">{t('about.reference.link_label')} <ArrowUpRight className="h-3 w-3" /></a>
          </section>
        </main>
      )
    }
    ```

    NOTE: If CheckmarkList component does not natively render with a `data-testid` attribute, add `data-testid="commitments-list"` to its <ul> root via prop pass-through or wrap it. Acceptable: wrap CheckmarkList in `<div data-testid="commitments-wrapper"><CheckmarkList ... /></div>` AND update e2e selector to `[data-testid="commitments-wrapper"] ul > li`. Update behavior test accordingly.

    2. Fill messages/{es-CO,en}/about.json with the FULL `<authored_copy>` LAB-05 block above. NO machine translation. Both locales authored side-by-side. Preserve any stub-level keys created in Plan 02-01.

    3. Fill tests/e2e/about-page.spec.ts with the 9 assertions per behavior list. Use Playwright `page.locator('[data-testid="numbered-step"]').count()` and equivalent for commitments. For banned-phrase check, use `await page.content()` then regex.

    4. Anti-marketing-slop enforcement: while authoring, re-read /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/README.md to internalize the register. Phrases to AVOID even unintentionally: "empower", "cutting-edge", "leverage", "unlock", "transform your", "innovate", "next-generation", "best-in-class". The copy must read like an economics-journal abstract, not a SaaS landing page.

    5. No `'use client'` on the page. Pure RSC.

    6. PITFALL #5 (impeccable): no icon tile stacked above heading. NumberedStep's pattern (number-left, title-right at md+; number-above-title at <md) is panoptic-derived and explicitly allowed.
  </action>
  <acceptance_criteria>
    - `! grep -q "'use client'" app/\(lab\)/about/page.tsx`.
    - `grep -c 'NumberedStep' app/\(lab\)/about/page.tsx` returns ≥ 2 (import + usage).
    - `grep -q 'CheckmarkList' app/\(lab\)/about/page.tsx`.
    - About JSON files contain all 5 steps + 4 commitments + intro + reference keys (both locales).
    - `! grep -iE "empower|cutting-edge|unlock|leverage our|best-in-class|next-generation|transform your" messages/es-CO/about.json messages/en/about.json app/\(lab\)/about/page.tsx` (no marketing slop).
    - `pnpm playwright test tests/e2e/about-page.spec.ts` exits 0.
    - `pnpm vitest run tests/unit/i18n-coverage.test.ts` (Plan 02-01) still green (es-CO / en parity for about namespace).
    - `pnpm tsc --noEmit && pnpm biome check app/\(lab\)/about/ messages/ tests/e2e/about-page.spec.ts` all exit 0.
    - `npx --yes impeccable detect app/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f app/\(lab\)/about/page.tsx && ! grep -q "'use client'" app/\(lab\)/about/page.tsx && grep -q 'NumberedStep' app/\(lab\)/about/page.tsx && grep -q 'CheckmarkList' app/\(lab\)/about/page.tsx && node -e "const f=require('./messages/es-CO/about.json'); ['h1','subheading','intro','steps','commitments','reference'].forEach(k=>{if(!f.about[k])throw new Error('es-CO missing '+k)}); ['01','02','03','04','05'].forEach(s=>{if(!f.about.steps[s])throw new Error('es-CO step '+s)})" && node -e "const f=require('./messages/en/about.json'); ['h1','subheading','intro','steps','commitments','reference'].forEach(k=>{if(!f.about[k])throw new Error('en missing '+k)}); ['01','02','03','04','05'].forEach(s=>{if(!f.about.steps[s])throw new Error('en step '+s)})" && ! grep -irE "empower|cutting-edge|unlock|leverage our|best-in-class|next-generation|transform your" messages/es-CO/about.json messages/en/about.json app/\(lab\)/about/page.tsx && pnpm vitest run tests/unit/i18n-coverage.test.ts && pnpm tsc --noEmit && pnpm biome check app/\(lab\)/about/page.tsx messages/es-CO/about.json messages/en/about.json tests/e2e/about-page.spec.ts && npx --yes impeccable detect app/ && pnpm playwright test tests/e2e/about-page.spec.ts</automated>
  </verify>
  <done>LAB-05 delivered: /about renders 5 NumberedSteps + 4 CheckmarkList items + intro prose + reference link, both locales, RSC; about-page.spec.ts asserts step count, commitment count, locale switching, and no marketing slop.</done>
</task>

</tasks>

<verification>
- LAB-01: / homepage renders mission + Apps overview + Velite counts + GitHub link in both locales
- LAB-05: /about renders 5 NumberedSteps + 4 CheckmarkList items in both locales
- Both pages are RSC (no 'use client')
- All e2e specs green
- i18n parity preserved (i18n-coverage test still passes)
- impeccable + biome + tsc all green
- No marketing-slop phrases in copy
</verification>

<success_criteria>
A first-time visitor at https://www.d2pfinance.xyz/ reads the lab mission, "What is d2-π" explainer, sees iteration headline counts (Pass / Fail / Parked / In Progress derived from Velite at build time), finds the link to wvs-finance GitHub, and can navigate to /about to read the 5-step methodology + 4 lab commitments — all in es-CO or en via the language switcher.
</success_criteria>

<output>
After completion, create `.planning/phases/02-research-lab-presence-and-iteration-catalog/02-03-SUMMARY.md` documenting:
- Final iteration-count values surfaced on homepage (read from .velite at SUMMARY time)
- Authored copy diff (full text of new keys committed to lab.json + about.json)
- Banned-phrase audit result (must be 0 hits)
- e2e + i18n test results
</output>

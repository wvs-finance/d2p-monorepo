---
phase: 08-scenario1-agent-cornerstone
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
optional: true
files_modified:
  - package.json
  - components/defi/cornerstone/MonitorPanel.tsx
  - lib/apps/abrigo/cornerstone/history.ts
  - components/defi/cornerstone/RunHistory.tsx
  - tests/unit/cornerstone-history.test.ts
  - tests/unit/cornerstone-monitor.test.tsx
  - vitest.setup.ts
  - tsconfig.json
autonomous: true
requirements: [MOD4-MONITOR, MOD4-HISTORY]
user_setup: []
must_haves:
  truths:
    - "MonitorPanel renders mark/premiumAccrued/marginHealthBps/pnl each with an adjacent ilustrativo label, under fork-verified + mock sub-label, no $ as realized PnL, no <details>"
    - "RunHistory persists past runs in IndexedDB via lazy openDB inside function bodies, client-only, guarded by typeof indexedDB"
    - "history.ts is never imported by an RSC; idb enters package.json ONLY in this plan"
  artifacts:
    - path: "components/defi/cornerstone/MonitorPanel.tsx"
      provides: "mock PerformanceUpdated read panel (basic)"
      contains: "fork-verified"
    - path: "lib/apps/abrigo/cornerstone/history.ts"
      provides: "lazy-openDB idb run-history persistence, client-only, indexedDB guard"
      contains: "openDB"
  key_links:
    - from: "components/defi/cornerstone/RunHistory.tsx"
      to: "lib/apps/abrigo/cornerstone/history.ts"
      via: "client island calls lazy openDB in function bodies"
      pattern: "openDB"
---

<objective>
**OPTIONAL — IF-TIME ONLY (NOT required for Phase 8 completion).** Per spec §7 and 08-CONTEXT decision 3: build this ONLY after the committed MVP (08-00/01/02) demos cleanly and all its tests are green, with time to spare before the June 11 deadline. Adds MonitorPanel (basic mock PerformanceUpdated read) + RunHistory (idb persistence). This is the ONLY plan that adds the `idb` dependency — keeping IndexedDB and its jsdom/fake-indexeddb test setup off the critical path.

Purpose: Nice-to-have demo depth. If time is tight, SKIP entirely; the phase is complete without it.
Output: idb dep + 2 components + history lib + 2 unit tests + fake-indexeddb test setup.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-scenario1-agent-cornerstone/08-UI-SPEC.md
@docs/superpowers/specs/2026-06-06-module4-scenario1-cornerstone-design.md

<interfaces>
From lib/apps/abrigo/cornerstone/events.ts (08-00): the PerformanceUpdated event { positionId:bigint; mark:bigint; premiumAccrued:bigint; marginHealthBps:bigint; pnl:bigint } + its fromMockEvent view.
From components/defi/cornerstone/HedgeDecisionCardV2.tsx (08-01): reuse the fork-verified + FlaskConical mock sub-label idiom + adjacent-ilustrativo-label pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (IF-TIME): RED — history + monitor tests with fake-indexeddb</name>
  <read_first>
    - 08-UI-SPEC.md §If-Time (MonitorPanel fields + ilustrativo labels + no $/no <details>; RunHistory lazy openDB + client-only + fake-indexeddb)
    - docs/superpowers/specs §8 (idb discipline: lazy openDB in function bodies, "use client" only import, typeof indexedDB guard, history.ts never imported by an RSC)
    - lib/apps/abrigo/cornerstone/events.ts (PerformanceUpdated view)
    - components/defi/cornerstone/HedgeDecisionCardV2.tsx (mock-label idiom to reuse)
    - vitest config / any existing vitest.setup.ts
  </read_first>
  <behavior>
    cornerstone-history.test.ts (import 'fake-indexeddb/auto' in setup): saveRun(record) then listRuns() returns the persisted record; openDB is called lazily (not at module top-level — assert no DB connection before the first call); typeof indexedDB undefined path returns [] gracefully.
    cornerstone-monitor.test.tsx: MonitorPanel renders mark/premiumAccrued/marginHealthBps/pnl, each with an adjacent ilustrativo label; under fork-verified + FlaskConical mock sub-label; no green; no <details>; no '$' as realized PnL; no executed/realized.
  </behavior>
  <action>
    Add `idb` + `fake-indexeddb` (dev) via pnpm add. Wire fake-indexeddb into vitest.setup.ts (or a scoped setup) for these tests only. Create the 2 RED tests importing not-yet-created modules; add both to tsconfig exclude. Commit RED: `test(08-03): if-time RunHistory + MonitorPanel failing-first`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone-history.test.ts tests/unit/cornerstone-monitor.test.tsx 2>&1 | grep -qiE "fail|error|cannot find" && echo RED-OK</automated>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>Both tests FAIL; idb + fake-indexeddb installed; tsc passes (stubs excluded).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (IF-TIME): GREEN — history.ts (lazy openDB), RunHistory + MonitorPanel; un-exclude</name>
  <read_first>
    - The 2 RED tests from Task 1
    - docs/superpowers/specs §8 (idb discipline)
    - components/defi/cornerstone/HedgeDecisionCardV2.tsx (mock idiom)
    - app/(defi)/apps/abrigo/cornerstone/page.tsx (where RunHistory/MonitorPanel mount — below PromptBox; both client islands)
  </read_first>
  <action>
    history.ts: lazy `openDB` INSIDE function bodies (saveRun/listRuns), guarded by `typeof indexedDB === 'undefined'` → return []/no-op. NEVER imported by an RSC (only from "use client" RunHistory). MonitorPanel.tsx + RunHistory.tsx ('use client'): reuse the fork-verified + FlaskConical mock sub-label + adjacent-ilustrativo-label idiom; no <details>; no green; no $ as realized PnL. Mount both below the PromptBox in page.tsx (RunHistory list; MonitorPanel under the mint when present). Remove the 2 test paths from tsconfig exclude in the same atomic commit. Commit GREEN: `feat(08-03): if-time MonitorPanel + RunHistory (idb, lazy openDB)`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone-history.test.ts tests/unit/cornerstone-monitor.test.tsx</automated>
    <automated>pnpm tsc --noEmit && pnpm biome check components/defi/cornerstone lib/apps/abrigo/cornerstone/history.ts</automated>
    <automated>! grep -rqn "history" app/\(defi\)/apps/abrigo/cornerstone/page.tsx | grep -iE "import.*history" && echo RSC-NO-HISTORY-IMPORT || echo "verify history.ts only imported by use-client RunHistory"</automated>
    <automated>! grep -riE "status-pass|emerald|<details|executed|realized" components/defi/cornerstone/MonitorPanel.tsx components/defi/cornerstone/RunHistory.tsx && echo NO-BANNED</automated>
  </verify>
  <done>Both suites GREEN; tsc + biome pass; history.ts uses lazy openDB + indexedDB guard and is imported only by the client RunHistory; no banned strings/green in the new components.</done>
</task>

</tasks>

<verification>
- idb + fake-indexeddb added ONLY in this plan (kept off the committed-MVP critical path).
- history.ts lazy openDB + indexedDB guard + client-only; never imported by an RSC.
- MonitorPanel honest: ilustrativo labels, fork-verified + mock sub-label, no $/no <details>/no green.
</verification>

<success_criteria>
If-time scope only (MOD4-MONITOR, MOD4-HISTORY). The phase is COMPLETE without this plan; it adds demo depth when time permits. Honesty invariants preserved; idb isolated here.
</success_criteria>

<output>
After completion, create `.planning/phases/08-scenario1-agent-cornerstone/08-03-SUMMARY.md`.
If the route surface is built, run the Evidence Collector live-verify of the history/monitor sections per CLAUDE.md; otherwise note the skip.
</output>

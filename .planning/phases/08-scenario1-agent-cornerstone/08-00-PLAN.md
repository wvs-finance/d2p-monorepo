---
phase: 08-scenario1-agent-cornerstone
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - lib/apps/abrigo/cornerstone/events.ts
  - lib/apps/abrigo/cornerstone/presets.ts
  - lib/apps/abrigo/cornerstone/workflow-store.ts
  - lib/apps/abrigo/cornerstone/workflow-engine.ts
  - lib/apps/abrigo/cornerstone/mock-fixtures.ts
  - tests/unit/cornerstone-store.test.ts
  - tests/unit/cornerstone-engine.test.ts
  - tests/unit/cornerstone-events.test.ts
  - tests/unit/cornerstone-presets.test.ts
  - tsconfig.json
autonomous: true
requirements: [MOD4-FLOW]
must_haves:
  truths:
    - "The workflow run is driven by a dedicated store that owns its reducer and returns a stable RunState ref per emit"
    - "getServerSnapshot returns idle so the first paint is the idle prompt (no streamed content at SSR)"
    - "The mock WorkflowEngine emits the 4 provisional WorkflowEvents in A1→A2→mint order behind a fromMockEvent adapter"
    - "Each preset id maps deterministically to one real recordedDecisionId (4083729 / 4083997)"
    - "Signed int256 (pnl/mark/premiumAccrued/margins, may be NEGATIVE) and int24 (strike/width) round-trip through fromMockEvent with sign + decimal preserved, never coerced/lost"
    - "createWorkflowStore() returns a FRESH per-call instance; only the IDLE constant is module-level (no exported singleton — server-leak guard)"
  artifacts:
    - path: "lib/apps/abrigo/cornerstone/workflow-store.ts"
      provides: "useSyncExternalStore-shaped store owning the reduce(RunState,event); subscribe/getSnapshot/getServerSnapshot/emit; factory returns fresh instance per call"
      contains: "getServerSnapshot"
    - path: "lib/apps/abrigo/cornerstone/events.ts"
      provides: "PROVISIONAL WorkflowEvent union (signed int256/int24 fields) + HedgeLegParamsView type + fromMockEvent adapter"
      contains: "fromMockEvent"
    - path: "lib/apps/abrigo/cornerstone/workflow-engine.ts"
      provides: "deterministic timed mock producer emitting WorkflowEvent[] in order"
    - path: "lib/apps/abrigo/cornerstone/presets.ts"
      provides: "preset id → recordedDecisionId mapping + nearest-preset resolver"
      contains: "4083729"
  key_links:
    - from: "lib/apps/abrigo/cornerstone/workflow-engine.ts"
      to: "lib/apps/abrigo/cornerstone/workflow-store.ts"
      via: "engine emit(event) → store.emit(fromMockEvent(event)) → reduce"
      pattern: "fromMockEvent"
    - from: "lib/apps/abrigo/cornerstone/presets.ts"
      to: "lib/apps/abrigo/somnia/reader.ts"
      via: "recordedDecisionId is a real snapshot decisionId consumed by getDecisionTraceById"
      pattern: "4083729|4083997"
---

<objective>
Freeze the Phase-8 data layer: the PROVISIONAL WorkflowEvent contract + adapter, the preset→recordedDecisionId map, the dedicated useSyncExternalStore-shaped workflow-store that OWNS the reducer, and the deterministic timed mock WorkflowEngine — all TDD failing-first. No React components, no route, no UI. This is the seam every downstream wave builds on; a Phase-15 backend reshape swaps only the producer + adapter.

Purpose: One source of truth for run state (store owns reduce; NO separate useReducer), stable-ref invariant (the Phase-7 hydration lesson), idle getServerSnapshot, an isolated provisional event contract, and a per-request store factory (no server singleton leak).
Output: 5 lib files + 4 unit test files; tsconfig adjusted so RED stubs do not deadlock the pre-commit tsc gate.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scenario1-agent-cornerstone/08-CONTEXT.md
@.planning/phases/08-scenario1-agent-cornerstone/08-UI-SPEC.md
@docs/superpowers/specs/2026-06-06-module4-scenario1-cornerstone-design.md

<interfaces>
<!-- The store MUST mirror the liveness.ts useSyncExternalStore CONTRACT (pattern only — do NOT reuse LivenessSource, it is a single-value store, wrong shape). -->

From lib/apps/abrigo/somnia/liveness.ts (PATTERN to mirror — NOT to import):
```ts
export type LivenessSource<T> = {
  getSnapshot: () => T
  getServerSnapshot: () => T          // stable seed so SSR == first client paint (React #418)
  subscribe: (onStoreChange: () => void) => () => void
}
// snapshotSource: getServerSnapshot() === getSnapshot() === seed (SAME reference, no new object)
```

From lib/apps/abrigo/somnia/reader.ts (the REAL decision source — recordedDecisionId is a snapshot decisionId):
```ts
export function getDecisionTraceById(id: string): DecisionTraceView | null
// snapshot decisionIds: "4083729" (ADD_LONG_GAMMA, sizeBps 6800, consensus 500, macroValue 568)
//                       "4083997" (REDUCE, sizeBps 568, consensus 900, macroValue 568)
```

<!-- THE REAL EVENT CONTRACT — verbatim from abrigo-somnia/docs/UI-AGENT-HANDOFF.md §5 (intended schema, finalized Phases 12–14). -->
<!-- CRITICAL: int256 fields are SIGNED — pnl/mark/premiumAccrued/marginToken0/marginToken1 can be NEGATIVE. -->
<!-- int24 strike/width are SIGNED ticks. thesis + poolRepresentativenessRationale are FREE-TEXT strings (the executor ships a hardcoded rationale string). -->
```solidity
event StrategistDecided(uint256 indexed requestId, string thesis, HedgeLegParams spec);                 // Agent 1 — thesis is free-text
event ExecutorDecided  (uint256 indexed requestId, string poolRepresentativenessRationale,              // Agent 2 — free-text rationale
                        TokenId positionId, int24 strike, int24 width, bool isLong);                     // strike/width are SIGNED int24
event PositionMinted   (TokenId indexed positionId, address indexed owner, int256 marginToken0, int256 marginToken1); // SIGNED margins
event PerformanceUpdated(TokenId indexed positionId, int256 mark, int256 premiumAccrued, uint256 marginHealthBps, int256 pnl); // mark/premium/pnl SIGNED
```

From abrigo-somnia contracts/src/types/{HedgeLegParams,PayoffTerms}.sol (verbatim source for the VIEW type — DO NOT import Solidity; hand-author the view):
```solidity
struct HedgeLegParams { PoolId underlyingMarket; uint256 strikeWAD; uint256 size; IMacroThesis economicTheory; uint32 chainId; bool isLong; PayoffTerms payoffTerms; }
struct PayoffTerms { uint88 vol; uint32 horizonBlocks; int24 tickSpacing; uint8 asset; uint8 riskPartner; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing-first tests for store, engine, events/adapter, presets</name>
  <read_first>
    - lib/apps/abrigo/somnia/liveness.ts (the useSyncExternalStore contract to mirror — stable-ref + getServerSnapshot=seed)
    - lib/apps/abrigo/somnia/snapshot.json (the two real decisions: 4083729 / 4083997 + their sizeBps/consensus/macroValue)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/docs/UI-AGENT-HANDOFF.md §5 (the REAL event contract — int256 SIGNED margins/mark/premium/pnl, int24 SIGNED strike/width, free-text thesis + poolRepresentativenessRationale)
    - docs/superpowers/specs/2026-06-06-module4-scenario1-cornerstone-design.md §2 (provisional event contract), §4 (store architecture), §9 (testing list)
    - 08-UI-SPEC.md Q2 (preset chip table → recordedDecisionId) + Q4 (pacing table)
    - tsconfig.json (the exclude array — RED stubs importing not-yet-created modules go here, mirroring the structured-data.test.tsx precedent)
    - tests/unit/somnia-decision-trace-reader.test.ts (a concrete existing somnia LIB unit test — copy its vitest idiom + @vitest-environment node; no cornerstone-adjacent test exists yet)
  </read_first>
  <behavior>
    cornerstone-events.test.ts:
    - WorkflowEvent union has exactly the 4 variants StrategistDecided / ExecutorDecided / PositionMinted / PerformanceUpdated with the REAL §5 fields. SIGNED int256 fields are typed bigint and may be NEGATIVE: PositionMinted.marginToken0/marginToken1, PerformanceUpdated.mark/premiumAccrued/pnl. uint256: requestId, marginHealthBps, positionId. SIGNED int24: ExecutorDecided.strike/width. FREE-TEXT strings: StrategistDecided.thesis, ExecutorDecided.poolRepresentativenessRationale.
    - SIGNED-EDGE AC (BLOCKER RC-B1): construct a PerformanceUpdated with a NEGATIVE int256 pnl (e.g. -1234567890123456789n) and an ExecutorDecided with a negative int24 tick (e.g. strike -887272). fromMockEvent must round-trip BOTH: the sign is preserved (the view's pnl reads as a negative value, NOT abs/coerced) and the int24 decimal/scale is preserved (the strike formats to the demo `4.100` form WITHOUT losing the sign on a negative tick). Assert no field is coerced to unsigned, NaN, or string-stripped of its '-'.
    - fromMockEvent(StrategistDecided) → a view object carrying recordedDecisionId (string) AND the free-text thesis exposed verbatim (no truncation).
    - fromMockEvent(ExecutorDecided) → a view carrying the HedgeLegParamsView with bigint fields formatted/preserved at the edge (BigInt burn class — assert strikeWAD survives as the demo display 4.100 and size/vol stay bigint or formatted-string, never lost; the free-text poolRepresentativenessRationale is exposed verbatim).
    cornerstone-store.test.ts:
    - createWorkflowStore().getServerSnapshot() === RunState 'idle' (status 'idle').
    - PER-REQUEST AC (FD-M1): two createWorkflowStore() calls return INDEPENDENT instances — emit on store A does NOT change store B's snapshot (assert B.getSnapshot().status stays 'idle' after A advances to 'a1'). No module-level mutable store instance is exported (grep-asserted in Task 2 verify; only the IDLE constant is module-level).
    - emit(StrategistDecided-view) transitions idle → a1; emit(ExecutorDecided-view) → a2_decision; a 'confirm' action → minting; emit(PositionMinted-view) → done.
    - getSnapshot() returns a NEW object reference after each emit, but the SAME reference across repeated getSnapshot() calls with no intervening emit (stable-ref invariant — assert Object.is true with no emit, false after emit).
    - subscribe(listener): listener fires once per emit; the returned cleanup stops further notifications.
    cornerstone-engine.test.ts (use fake timers):
    - runEngine(presetId, emit) emits StrategistDecided FIRST, then ExecutorDecided, in that order; mint is NOT auto-emitted before a confirm signal (user-gated); after confirm, PositionMinted is emitted.
    - the StrategistDecided event's recordedDecisionId equals the preset's mapped real id.
    cornerstone-presets.test.ts:
    - PRESETS has exactly 2 entries: 'infl-surprise-add' → '4083729', 'infl-cooling-reduce' → '4083997'.
    - resolveNearestPreset(freeText) returns one of the 2 preset ids for arbitrary input (never null/undefined) — the §0.6 "maps to nearest, never live inference" rule.
    - PRESET-MAPPING AC (RC-M4): pin the demo-critical mapping with 2–3 fixture inputs → expected preset id: a rate-hike / inflation-surprise text (e.g. "la inflación sorprendió al alza, sube tasas") → 'infl-surprise-add' (→ 4083729); a cooling / moderation text (e.g. "la inflación se modera, enfriamiento") → 'infl-cooling-reduce' (→ 4083997). Assert the resolved id (not just non-null).
  </behavior>
  <action>
    Create the 4 test files under tests/unit/. They import from lib/apps/abrigo/cornerstone/{events,workflow-store,workflow-engine,presets} which DO NOT EXIST yet → they fail to compile/run (RED). Use the project vitest idiom; engine timing tests use vi.useFakeTimers().
    Then add ALL 4 test paths to tsconfig.json `exclude` (mirror the existing tests/unit/structured-data.test.tsx precedent) so `tsc --noEmit` in the pre-commit hook passes while the imported modules are absent. They run under vitest (esbuild), not tsc. Document in the SUMMARY that these exclusions are removed in Task 2's atomic commit once the modules exist.
    Commit RED: `test(08-00): failing-first cornerstone store/engine/events/presets`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone-store.test.ts tests/unit/cornerstone-engine.test.ts tests/unit/cornerstone-events.test.ts tests/unit/cornerstone-presets.test.ts 2>&1 | grep -qiE "fail|error|cannot find" && echo RED-OK</automated>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>All 4 test files exist and FAIL (modules absent); tsc --noEmit passes because the 4 RED stubs are excluded; both verify commands succeed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement events+adapter, presets, store, engine; remove RED exclusions</name>
  <read_first>
    - The 4 RED test files from Task 1 (the contracts they assert)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/docs/UI-AGENT-HANDOFF.md §5 (the REAL signed int256/int24 + free-text event contract)
    - lib/apps/abrigo/somnia/liveness.ts (stable-ref + getServerSnapshot pattern — mirror exactly)
    - lib/apps/abrigo/somnia/reader.ts getDecisionTraceById (recordedDecisionId is a snapshot decisionId; the route key is the snapshot `decisionId` join key = the size-leg requestId, NOT a surrogate — presets must emit the real ids)
    - lib/apps/abrigo/somnia/snapshot.json (4083729: ADD_LONG_GAMMA/6800/consensus500/macro568; 4083997: REDUCE/568/consensus900/macro568)
    - 08-UI-SPEC.md Q2 (chip→id table), Q4 (delay table: idle→a1 600ms, a1→a2 1800ms, confirm gate user, minting→done 1200ms)
    - abrigo-somnia contracts/src/types/{HedgeLegParams,PayoffTerms}.sol (verbatim field source for HedgeLegParamsView)
  </read_first>
  <action>
    events.ts (PROVISIONAL — top-of-file comment: "PROVISIONAL — backend Phases 12–15 reshape expected; isolated behind fromMockEvent. NOT zero-rework. REAL contract (UI-AGENT-HANDOFF §5): margins/mark/premiumAccrued/pnl are SIGNED int256 (may be NEGATIVE); strike/width are SIGNED int24; thesis + poolRepresentativenessRationale are FREE-TEXT, human-authored strings — NEVER render as live agent/LLM reasoning."):
    - export type WorkflowEvent = the 4-variant discriminated union from the §5 real contract. SIGNED int256 fields (bigint, may be negative): PositionMinted.marginToken0/marginToken1, PerformanceUpdated.mark/premiumAccrued/pnl. uint256 (bigint, non-negative): requestId, positionId, marginHealthBps. SIGNED int24 (number, may be negative): ExecutorDecided.strike/width. Free-text strings: StrategistDecided.thesis, ExecutorDecided.poolRepresentativenessRationale.
    - export type HedgeLegParamsView — hand-authored DISPLAY view (NOT the raw ABI struct): { marketLabel: string; strikeWAD: string (demo "4.100"); size: bigint; isLong: boolean; schoolLabel: string (from economicTheory address(0) → a human school label, NEVER raw 0x000…0); rationale: string (verbatim free-text, human-authored — surfaced under an explicit human-authored label by 08-01); payoff: { volToWidth: string; horizonBlocks: number; tickSpacing: number; asset: string }; maxLoss: string ("= prima"); upside: string ("ilimitado"); marginDelta: { token0: bigint; token1: bigint } }.
    - export function fromMockEvent(e: WorkflowEvent) → a serializable view model. Format/preserve at THIS edge (the documented burn class): SIGNED int256 → preserve the sign (negative stays negative — no abs, no unsigned coercion); strike/width int24 → format to the demo decimal form while preserving sign; no raw bigint leaks to JSX without a formatter. Expose thesis/rationale verbatim as free-text fields.
    presets.ts:
    - export const PRESETS = [{ id:'infl-surprise-add', recordedDecisionId:'4083729' }, { id:'infl-cooling-reduce', recordedDecisionId:'4083997' }] as const. Comment: "recordedDecisionId is the snapshot decisionId join key (= the size-leg requestId), not a surrogate."
    - export function resolveNearestPreset(text: string): preset id — deterministic keyword heuristic (cooling/modera/enfría/reduce → infl-cooling-reduce; hike/sorpresa/sube/alza → infl-surprise-add; default → infl-surprise-add); NEVER returns null. Comment: "maps to nearest preset; never live inference (§0.6)".
    workflow-store.ts (the heart — OWNS the reducer; NO separate useReducer anywhere; FACTORY only — no exported singleton):
    - export const IDLE: RunState (module-level, frozen) — the ONLY module-level state.
    - export type RunState = { status:'idle'|'a1'|'a2_decision'|'minting'|'done'; a1?:{recordedDecisionId}; a2?:HedgeLegParamsView; mint?:{...} } (immutable).
    - export function createWorkflowStore(): { subscribe; getSnapshot; getServerSnapshot; emit } — returns a FRESH instance per call (closure-local state; two calls are independent — no shared module-level mutable instance, server-leak guard). getServerSnapshot() returns the module-stable IDLE constant (same ref). reduce(state,event) returns a NEW immutable RunState per emit; getSnapshot returns the cached ref (stable until next emit — Object.is true across no-emit reads). emit notifies subscribers once.
    engine.ts → workflow-engine.ts:
    - export async function runWorkflow(presetId, emit, { confirm }) — deterministic timed producer using the Q4 delays; emits StrategistDecided (recordedDecisionId = PRESETS lookup) then ExecutorDecided; awaits the confirm signal (user-gated, no auto-delay) before PositionMinted; tunable for ~10–12s. Accept an injected timer/clock so tests use fake timers.
    Then REMOVE the 4 test paths from tsconfig.json `exclude` (modules now exist). Land events+presets+store+engine + the tsconfig un-exclude in ONE atomic commit (pre-commit tsc gate blocks any mid-state — the 05.1-01 lesson).
    Commit GREEN: `feat(08-00): cornerstone events+adapter, presets, workflow-store, mock engine`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone-store.test.ts tests/unit/cornerstone-engine.test.ts tests/unit/cornerstone-events.test.ts tests/unit/cornerstone-presets.test.ts</automated>
    <automated>pnpm tsc --noEmit</automated>
    <automated>grep -q "fromMockEvent" lib/apps/abrigo/cornerstone/events.ts && grep -q "4083729" lib/apps/abrigo/cornerstone/presets.ts && grep -q "getServerSnapshot" lib/apps/abrigo/cornerstone/workflow-store.ts && echo WIRED</automated>
    <automated>! grep -rqE "0x0{20,}" lib/apps/abrigo/cornerstone/ && echo NO-RAW-ZERO-ADDR</automated>
    <automated>! grep -qE "^export const [a-zA-Z]+ = createWorkflowStore\(\)" lib/apps/abrigo/cornerstone/workflow-store.ts && echo NO-SINGLETON-EXPORT</automated>
  </verify>
  <done>All 4 unit suites GREEN; tsc passes with RED stubs un-excluded; events/presets/store wired; signed int256/int24 round-trip with sign preserved; createWorkflowStore is a per-call factory (no exported singleton); no raw 0x000…0 in the cornerstone lib; store getServerSnapshot=idle and getSnapshot is stable-ref per emit.</done>
</task>

</tasks>

<verification>
- Store owns reduce; one source of truth (grep: no `useReducer` in lib/apps/abrigo/cornerstone/); createWorkflowStore is a per-call factory, no exported singleton.
- getServerSnapshot returns idle (unit-asserted).
- 4 provisional events behind fromMockEvent (unit-asserted; PROVISIONAL comment present, documents signed int256/int24 + free-text).
- Signed int256/int24 round-trip with sign + decimal preserved (unit-asserted).
- presets map to the two REAL recorded decision ids; preset keyword mapping pinned by fixture ACs.
- BigInt formatted at the events edge; no raw 0x000…0 anywhere.
</verification>

<success_criteria>
Maps to ROADMAP success criterion 4: a dedicated workflow-store (useSyncExternalStore-shaped, owns the reducer, stable RunState ref per emit, getServerSnapshot=idle, per-call factory) + the mock WorkflowEngine producer emitting provisional WorkflowEvents (signed int256/int24, free-text thesis/rationale) behind fromMockEvent. (honker-node/AI-SDK/idb absent — verified by no new package.json deps in this plan.)
</success_criteria>

<output>
After completion, create `.planning/phases/08-scenario1-agent-cornerstone/08-00-SUMMARY.md`.
Pure-lib plan (no rendered route) → skip the Evidence Collector live-verify per CLAUDE.md "When to skip"; note this in the SUMMARY.
</output>
</content>
</invoke>

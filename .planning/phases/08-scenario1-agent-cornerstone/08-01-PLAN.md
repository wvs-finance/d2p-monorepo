---
phase: 08-scenario1-agent-cornerstone
plan: 01
type: tdd
wave: 1
depends_on: ["08-00"]
files_modified:
  - components/defi/cornerstone/HedgeDecisionCardV2.tsx
  - components/defi/cornerstone/MintCard.tsx
  - messages/es-CO/somnia.json
  - messages/en/somnia.json
  - docs/copy-review.md
  - tests/unit/cornerstone-decision-card.test.tsx
  - tests/unit/cornerstone-mint-card.test.tsx
  - tsconfig.json
autonomous: true
requirements: [MOD4-A2, MOD4-MINT]
must_haves:
  truths:
    - "Agent-2 decision card renders all ~10 HedgeLegParamsView fields at full visual weight under the fork-verified tier + a 'mock · no en vivo' sub-label, never green, never <details>"
    - "Every mock numeric carries an adjacent sibling mock/ilustrativo label; no $ presented as realized PnL; raw 0x000…0 never shown"
    - "Any free-text thesis/rationale renders under an EXPLICIT 'explicación (autoría humana)' label — never presented as live agent/LLM reasoning"
    - "The mock MintCard renders TokenId, mock margin BalanceDelta, leg fields under the same mock label with no <details>"
    - "A Confirm button is present at the card foot with ≥44px hit target and the simulated-action gate caption"
    - "strike renders as the demo 4.100 (formatted, not the raw WAD integer); no raw bigint reaches a JSX string; no new (4th) type size is introduced"
  artifacts:
    - path: "components/defi/cornerstone/HedgeDecisionCardV2.tsx"
      provides: "Agent-2 mock decision card reusing HedgeDecisionCard DataRow/badge/CARD_CLASS idiom + Confirm button + human-authored rationale label"
      contains: "fork-verified"
    - path: "components/defi/cornerstone/MintCard.tsx"
      provides: "mock mint card (TokenId, BalanceDelta, leg fields) under fork-verified + mock sub-label"
      contains: "FlaskConical"
    - path: "messages/es-CO/somnia.json"
      provides: "somnia.cornerstone.* es-CO-first copy"
      contains: "cornerstone"
  key_links:
    - from: "components/defi/cornerstone/HedgeDecisionCardV2.tsx"
      to: "components/defi/ProvenanceBadge.tsx"
      via: "ProvenancePill tier='fork-verified' + an INLINE neutral FlaskConical sub-label pill (shell string inlined verbatim — ProvenanceBadge exports no shell consts)"
      pattern: "fork-verified"
    - from: "components/defi/cornerstone/HedgeDecisionCardV2.tsx"
      to: "lib/apps/abrigo/cornerstone/events.ts"
      via: "renders the HedgeLegParamsView view model from fromMockEvent"
      pattern: "HedgeLegParamsView"
---

<objective>
Build the two MOCK card components — the judged differentiator HedgeDecisionCardV2 (Agent-2) + MintCard — as pure client-renderable presentational components, TDD failing-first, with the full honesty contract (fork-verified neutral tier + FlaskConical "mock · no en vivo" sub-label, adjacent ilustrativo labels on every numeric, free-text rationale under an explicit human-authored label, no <details>, no green, no raw 0x000…0, no real $). Author the es-CO-first somnia.cornerstone.* copy. No route wiring yet (Wave 2).

Purpose: Isolate the riskiest component (per spec §7) with its own plan and full test budget; honesty greps live as unit assertions before the e2e wave. The free-text thesis/rationale is the HIGHEST fabrication risk (the executor ships a hardcoded rationale string) — it must NEVER read as live agent/LLM reasoning.
Output: 2 components + extended somnia.json (both locales) + copy-review row + 2 unit tests.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scenario1-agent-cornerstone/08-UI-SPEC.md
@docs/superpowers/specs/2026-06-06-module4-scenario1-cornerstone-design.md

<interfaces>
From lib/apps/abrigo/cornerstone/events.ts (08-00 output — the view the card renders):
```ts
export type HedgeLegParamsView = {
  marketLabel: string; strikeWAD: string /* formatted "4.100", NOT raw WAD */; size: bigint; isLong: boolean;
  schoolLabel: string /* human school, never 0x000…0 */;
  rationale: string /* free-text, human-authored — render under the explicit human-authored label */;
  payoff: { volToWidth: string; horizonBlocks: number; tickSpacing: number; asset: string };
  maxLoss: string; upside: string; marginDelta: { token0: bigint; token1: bigint };
}
```

From components/defi/somnia/HedgeDecisionCard.tsx (REUSE the idiom — do NOT re-derive):
```ts
const CARD_CLASS = 'rounded-lg border border-border-default bg-bg-surface p-4 space-y-4'  // identical weight invariant — module-LOCAL, inline if not exported
// DataRow pattern: <div className="flex items-start justify-between gap-4 py-2"><dt class=text-text-muted>{label}</dt><dd class="font-mono text-text-primary">{value}</dd></div>
```

From components/defi/ProvenanceBadge.tsx:
```ts
export function ProvenancePill({ tier, label, ariaLabel, subState }): ...
// tier='fork-verified' → NEUTRAL token ('text-text-muted ring-border-default bg-bg-surface'), ShieldCheck icon, NEVER green.
// NOTE: ProvenanceBadge exports NO pill-shell consts. SimuladoBadge uses AMBER status-parked — do NOT reuse it for the mock sub-label.
// The "mock · no en vivo" sub-label is a SEPARATE neutral pill you build INLINE (recipe below) with lucide FlaskConical (LOCKED — not TriangleAlert, not amber).
```

<!-- INLINE MOCK SUB-LABEL RECIPE (FD-B2) — PILL_SHELL/NEUTRAL_CLASS are module-LOCAL to LivenessPill, NOT importable. Inline the verbatim shell string below. -->
```tsx
// neutral mock sub-label pill — inline verbatim, color+icon+text (CROSS-09), never green/amber:
<span className="inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-sm font-normal ring-1 ring-inset text-text-muted ring-border-default bg-bg-surface" aria-label={strings.mockSubLabelAria}>
  <FlaskConical className="h-3.5 w-3.5 shrink-0" aria-hidden="true" />
  <span>{strings.mockSubLabel}</span>
</span>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing-first card tests + somnia.cornerstone.* copy stubs</name>
  <read_first>
    - components/defi/somnia/HedgeDecisionCard.tsx (CARD_CLASS, DataRow idiom, ActionBadge neutral shell, ProvenancePill usage)
    - components/defi/ProvenanceBadge.tsx (fork-verified neutral config + the LOCKED FlaskConical icon import — do NOT reuse the amber SimuladoBadge styling, and note no shell consts are exported)
    - components/defi/somnia/LivenessPill.tsx (PILL_SHELL/NEUTRAL_CLASS are module-LOCAL here — confirm they are NOT exported; the inline recipe above duplicates the verbatim shell string)
    - 08-UI-SPEC.md §Color (honesty color invariants), Q3 (confirm-card treatment + awaiting-confirm + ≥44px + gate caption), §Copywriting (es-CO/en table + banned-strings greps), §Typography (the existing type scale — assert NO 4th size is added)
    - messages/es-CO/somnia.json + messages/en/somnia.json (nested-object format; where to add the cornerstone block)
    - lib/apps/abrigo/cornerstone/events.ts (HedgeLegParamsView fields the card iterates, incl. the free-text rationale)
    - tests/unit/provenance-fork-verified.test.tsx (a concrete existing component test that renders a fork-verified somnia surface — copy its .tsx render idiom + @vitest-environment jsdom)
  </read_first>
  <behavior>
    cornerstone-decision-card.test.tsx (render HedgeDecisionCardV2 with a mock HedgeLegParamsView + threaded strings):
    - renders a ProvenancePill with tier fork-verified AND a sub-label pill containing the FlaskConical icon + the "mock · no en vivo" text.
    - the card root has NO green/emerald/status-pass class (assert className string excludes 'status-pass' and 'green' and 'emerald').
    - contains ZERO <details>/<summary> (querySelector('details') is null).
    - all ~10 fields render (market, strike 4.100, size, isLong, school label, vol→width, horizon, tickSpacing, asset, max-loss, upside, margin) — assert key labels present.
    - STRIKE-FORMAT AC (RC-M6): the strike renders as the demo `4.100` (NOT the raw WAD integer); assert no raw bigint string (e.g. no /\d{17,}/ WAD-length integer) reaches the rendered text — the value passed through a formatter at the edge.
    - HUMAN-AUTHORED RATIONALE AC (BLOCKER RC-B2): the free-text rationale renders under an EXPLICIT label whose text === strings.humanAuthoredLabel ("explicación (autoría humana)" es-CO / "human-authored explanation" en); assert the rationale text node is a sibling/child of that labelled region AND the textContent contains NEITHER "agente"/"agent" NOR "LLM"/"IA"/"AI" adjacent to it as a reasoning attribution (it must NOT read as live agent/LLM reasoning).
    - TYPE-SIZE AC (RC-M6/FD-M5): the ilustrativo sibling label uses the EXISTING small label size (the 14px label or mono role already in the scale) — assert no new (4th) font-size class is introduced (className uses only the established label/value/heading size tokens).
    - EVERY mock numeric has an adjacent sibling label containing 'mock' or 'ilustrativo'/'illustrative' (assert each numeric value node has a sibling with that text — not merely "no $").
    - schoolLabel text is a human school string; the DOM contains NO raw 0x000…0 (assert no /0x0{20,}/ in textContent).
    - a Confirm button is rendered (role button), min-height class encodes ≥44px, preceded by the gate caption "Acción simulada — no se ejecuta en ninguna cadena." (es-CO).
    - no banned strings: textContent has no executed/realized/ejecutad/realizad and no '$' adjacent to a value.
    cornerstone-mint-card.test.tsx:
    - renders TokenId, mock margin BalanceDelta (token0/token1), leg fields under fork-verified + FlaskConical mock sub-label; ZERO <details>; no green; no raw 0x000…0; no executed/realized; each numeric has an adjacent mock/ilustrativo label.
  </behavior>
  <action>
    Create the 2 .tsx test files importing the not-yet-created components → RED. Add both test paths to tsconfig.json `exclude` (structured-data.test.tsx precedent).
    Add the somnia.cornerstone.* block to BOTH messages/es-CO/somnia.json and messages/en/somnia.json (es-CO authored FIRST, en second — NO machine translation). Keys (verbatim copy from 08-UI-SPEC §Copywriting + Q2/Q3): cornerstone.pageTitle, cornerstone.confirmCta ("Confirmar (simulado)" / "Confirm (simulated)"), cornerstone.confirmGateCaption ("Acción simulada — no se ejecuta en ninguna cadena." / "Simulated action — nothing executes on-chain."), cornerstone.idleHeading, cornerstone.idleBody, cornerstone.agent1Label ("Agente 1 · decisión registrada · verificada por consenso" / "Agent 1 · recorded run · consensus-verified"), cornerstone.mockSubLabel ("mock · no en vivo" / "mock · not live"), cornerstone.mockSubLabelAria, cornerstone.humanAuthoredLabel ("explicación (autoría humana)" / "human-authored explanation"), cornerstone.mockUnit ("ilustrativo" / "illustrative"), cornerstone.maxLossLabel ("= prima (máx. pérdida)"), cornerstone.upsideLabel ("ilimitado (ilustrativo)"), cornerstone.errorState, the two preset chip labels + captions (Q2 table), cornerstone.forkVerifiedLabel + ariaLabel, cornerstone.replayingMock ("reproduciendo · mock" / "replaying · mock") + aria, and the card field labels (market/strike/size/direction/school/vol-width/horizon/tickSpacing/asset/margin/tokenId). Append a docs/copy-review.md row (es-CO authored by developer; native sign-off pending — non-blocking, mirror prior phases). i18n/request.ts is INTENTIONALLY UNTOUCHED (somnia.json already wired — note this in the SUMMARY).
    Commit RED: `test(08-01): failing-first HedgeDecisionCardV2 + MintCard + cornerstone copy`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone-decision-card.test.tsx tests/unit/cornerstone-mint-card.test.tsx 2>&1 | grep -qiE "fail|error|cannot find" && echo RED-OK</automated>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('messages/es-CO/somnia.json')).somnia.cornerstone; if(!c||!c.humanAuthoredLabel) throw new Error('missing humanAuthoredLabel'); JSON.parse(require('fs').readFileSync('messages/en/somnia.json')).somnia.cornerstone.humanAuthoredLabel; console.log('COPY-OK')"</automated>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>Both card tests exist and FAIL; somnia.cornerstone.* (incl. humanAuthoredLabel) present in both locales; copy-review row added; tsc passes (RED stubs excluded).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement HedgeDecisionCardV2 + MintCard; remove RED exclusions</name>
  <read_first>
    - The 2 RED test files from Task 1
    - components/defi/somnia/HedgeDecisionCard.tsx (CARD_CLASS + DataRow + ActionBadge — INLINE the CARD_CLASS/DataRow idiom; they are module-local, not exported)
    - components/defi/ProvenanceBadge.tsx (ProvenancePill fork-verified + the LOCKED FlaskConical import)
    - 08-UI-SPEC.md Q3 (full field weight, adjacent ilustrativo labels, max-loss/upside copy, Confirm ≥44px + gate caption + focus-on-enter), §Color (accent ONLY on the Confirm fill; pills neutral), §Typography (existing type scale — no 4th size)
    - lib/apps/abrigo/cornerstone/events.ts (HedgeLegParamsView, incl. free-text rationale)
    - messages/{es-CO,en}/somnia.json (the cornerstone keys from Task 1, incl. humanAuthoredLabel)
  </read_first>
  <action>
    HedgeDecisionCardV2.tsx ('use client' — it owns the Confirm button + onConfirm callback + focus management hook for Wave 2; pure presentational otherwise, all copy threaded via a CardV2Strings interface, NO getTranslations inside — Phase-6/7 string-threading pattern):
    - INLINE the CARD_CLASS string verbatim ('rounded-lg border border-border-default bg-bg-surface p-4 space-y-4') + the DataRow idiom (they are module-local to HedgeDecisionCard, NOT exported). No card-nested-in-card (impeccable).
    - Header: ProvenancePill tier='fork-verified' (neutral) + a SECOND neutral pill = the mock sub-label built INLINE per the recipe: the verbatim shell string `inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-sm font-normal ring-1 ring-inset` + `text-text-muted ring-border-default bg-bg-surface` + lucide FlaskConical + strings.mockSubLabel + aria-label (color+icon+text, never green/amber — do NOT import PILL_SHELL/NEUTRAL_CLASS; they are not exported).
    - Render every HedgeLegParamsView field as a full-weight DataRow; each numeric value gets an adjacent SIBLING <span> with the ilustrativo/mock unit using the EXISTING label type size (no new 4th font-size). strikeWAD renders the formatted `4.100` (events.ts already formatted it — never the raw WAD integer; no raw bigint to JSX). max-loss → "= prima (máx. pérdida)"; upside → "ilimitado (ilustrativo)". schoolLabel renders the human string (never 0x000…0).
    - Free-text rationale: render under an EXPLICIT labelled region whose label === strings.humanAuthoredLabel ("explicación (autoría humana)"). Never attribute it to a live agent/LLM. NO <details> anywhere. NO bare $.
    - Foot: a one-line gate caption (strings.confirmGateCaption) + a primary Confirm button — the ONLY accent fill in the card (bg accent), min-h-[44px], text = strings.confirmCta. Expose an `onConfirm` prop + a `confirmRef` (forwarded) so Wave 2 can move focus on entering a2_decision.
    MintCard.tsx ('use client' or RSC-safe presentational — no interactivity needed): TokenId, margin BalanceDelta (token0/token1), leg fields; fork-verified + INLINE FlaskConical mock sub-label (same verbatim recipe); each numeric + adjacent ilustrativo sibling (existing type size); no <details>; no green; no raw 0x000…0; no executed/realized.
    Remove the 2 test paths from tsconfig.json `exclude`. Land both components + tsconfig un-exclude in ONE atomic commit (tsc gate blocks mid-state).
    Commit GREEN: `feat(08-01): HedgeDecisionCardV2 + MintCard (fork-verified+mock, full weight, no details)`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone-decision-card.test.tsx tests/unit/cornerstone-mint-card.test.tsx</automated>
    <automated>pnpm tsc --noEmit</automated>
    <automated>! grep -riE "status-pass|emerald|text-green|bg-green" components/defi/cornerstone/ && echo NO-GREEN</automated>
    <automated>! grep -riE "<details|executed|realized|ejecutad|realizad" components/defi/cornerstone/ && echo NO-BANNED</automated>
    <automated>grep -q "FlaskConical" components/defi/cornerstone/HedgeDecisionCardV2.tsx && grep -q "FlaskConical" components/defi/cornerstone/MintCard.tsx && echo MOCK-ICON-OK</automated>
    <automated>! grep -rqE "PILL_SHELL|NEUTRAL_CLASS" components/defi/cornerstone/ && echo NO-NONEXPORTED-IMPORT</automated>
    <automated>pnpm biome check components/defi/cornerstone/</automated>
  </verify>
  <done>Both card suites GREEN; tsc + biome pass; no green token, no <details>, no banned strings; FlaskConical mock sub-label inlined (no PILL_SHELL/NEUTRAL_CLASS import); free-text rationale under the human-authored label; strike formatted 4.100 (no raw bigint), no 4th type size; Confirm button + gate caption present with confirmRef exposed for Wave 2 focus.</done>
</task>

</tasks>

<verification>
- fork-verified tier renders neutral only (grep: no green/emerald/status-pass in components/defi/cornerstone/).
- FlaskConical (LOCKED) is the mock sub-label icon on both cards, built INLINE (PILL_SHELL/NEUTRAL_CLASS not imported — they are not exported).
- Free-text rationale/thesis renders under the explicit "explicación (autoría humana)" label, never as live agent/LLM reasoning (unit-asserted).
- strike renders formatted 4.100, no raw bigint to JSX, no 4th type size (unit-asserted).
- Zero <details> in HedgeDecisionCardV2/MintCard.
- Every mock numeric has an adjacent ilustrativo/mock sibling label (unit-asserted).
- No raw 0x000…0; no executed/realized/ejecutad/realizad; no real $.
- es-CO authored first; en second; copy-review row added; i18n/request.ts untouched.
</verification>

<success_criteria>
Maps to ROADMAP success criterion 2 (Agent-2 mock card from HedgeLegParamsView under fork-verified + "mock · no en vivo", full weight, adjacent labels, free-text rationale under a human-authored label, Confirm gate) and criterion 3 (mock MintCard with TokenId/BalanceDelta/leg fields, no executed/realized, no raw 0x000…0).
</success_criteria>

<output>
After completion, create `.planning/phases/08-scenario1-agent-cornerstone/08-01-SUMMARY.md`.
Components are not yet mounted on a route → Evidence Collector live-verify deferred to 08-02 (where the cornerstone route renders them); note this in the SUMMARY per CLAUDE.md.
</output>
</content>
</invoke>

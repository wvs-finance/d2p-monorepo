---
phase: 10-backend-on-chain-single-use-guard-no-mint-provisioning-variant
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts
  - packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol
  - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md
autonomous: true
requirements: [EXEC-01, PROV-04]
must_haves:
  truths:
    - "The frontend BuildBearDeployment type accepts mintTxHash: null and a snapshotId field without tsc error"
    - "A keyless Foundry guard test exists that asserts the 2nd mint reverts \"fork used\" — and it currently FAILS (RED), proving the guard is not yet present"
    - "The guard test file name contains no 'fork' substring, so it rides the secret-free CI lane"
    - "An operator-fillable 10-SPIKE-EVIDENCE.md scaffold exists with the four required live-fork sections (a)-(d)"
  artifacts:
    - path: "packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts"
      provides: "BuildBearDeployment type with mintTxHash: string | null, mintedStrike: number | null, snapshotId?: string"
      contains: "snapshotId"
    - path: "packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol"
      provides: "EXEC-01 RED guard test (MockPool + executor; 1st mint succeeds, 2nd reverts \"fork used\" via resolveFromMandate AND resolveAndMint)"
      contains: "fork used"
    - path: ".planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md"
      provides: "Operator-manual live-fork transcript scaffold (sections a-d)"
      contains: "## (a)"
  key_links:
    - from: "MacroHedgeExecutor.guard.t.sol"
      to: "MacroHedgeExecutor._resolveAndMintAtStrike"
      via: "resolveFromMandate / resolveAndMint call into the shared sink"
      pattern: "resolveFromMandate|resolveAndMint"
    - from: "artifact-loader.ts BuildBearDeployment"
      to: "--no-mint artifact (Plan 10-02)"
      via: "mintTxHash null + snapshotId fields the shell will write"
      pattern: "snapshotId"
---

<objective>
Wave 0: establish the test-first and type-migration foundation for Phase 10 before any production code changes.

Three deliverables, all CI-checkable or operator-scaffolded:
1. **artifact-loader.ts type migration** (PROV-04 enabler, shared with Phase 11): widen `mintTxHash`/`mintedStrike` to nullable and add `snapshotId?: string` so the `--no-mint` artifact (written in Plan 10-02) is not rejected by the validator or `tsc`.
2. **EXEC-01 guard test, RED-first** (EXEC-01): a keyless Foundry unit test asserting the 2nd mint reverts `"fork used"`. It MUST fail now (the guard does not exist yet) — Plan 10-02 turns it GREEN. Named WITHOUT any `fork` substring so it runs in the secret-free CI lane.
3. **10-SPIKE-EVIDENCE.md scaffold** (operator-manual gate): the committed transcript file with sections (a)-(d) the operator fills from the live BuildBear run in Plan 10-03.

Purpose: lock the cross-layer contract (`"fork used"` string + nullable artifact shape) and the red-before-green discipline before touching the contract or the provisioning script.
Output: one modified TS type file, one new RED Foundry test, one new evidence scaffold.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-CONTEXT.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-RESEARCH.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-VALIDATION.md

<interfaces>
<!-- Current BuildBearDeployment type the executor reads. Extracted from codebase — use directly. -->

From packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts (current):
```typescript
export type BuildBearDeployment = {
  chainId: number
  executor: string
  pool: string
  riskManagement: string
  rpcUrl: string
  mintTxHash: string        // <- widen to string | null
  mintedStrike: number      // <- widen to number | null
  capturedAt: string
  source: string
  factory?: string
  riskEngine?: string
  // <- add snapshotId?: string
}
```
The `validateDeployment` `required` array is `['chainId','executor','pool','rpcUrl','capturedAt']` — it does NOT list `mintTxHash`/`mintedStrike`, so null values already pass the runtime check. Only the TS type needs widening.

From packages/backend/contracts/test/instrument/MacroHedgeExecutor.onResult.t.sol (the mock/subclass pattern to follow):
```solidity
import {MockPlatform} from "../mocks/MockPlatform.sol";
import {PanopticPoolV2} from "@contracts/PanopticPool.sol";
import {RiskManagement} from "../../src/RiskManagement.sol";
// probe = new MacroHedgeExecutorDecodeProbe(address(platform), PanopticPoolV2(address(0)), RiskManagement(address(0)), 0)
```
Available mocks: packages/backend/contracts/test/mocks/{MockPlatform,MockRegimeOracle,MockSurpriseOracle,MockMacroOracle,MockCcop}.sol

The shared sink under test (MacroHedgeExecutor.sol):
- `_resolveAndMintAtStrike` (line 357) is reached by all three entrypoints: `resolveFromMandate` (~line 279), `resolveAndMint` (~line 168), `_onResult` (~line 195).
- `pool` is an `immutable` set in the 9-arg ctor. The guard (added in Plan 10-02) will be `require(pool.numberOfLegs(address(this)) == 0, "fork used")`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate artifact-loader.ts BuildBearDeployment type to nullable mintTxHash + snapshotId</name>
  <read_first>
    - packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts (the file being modified — read fully)
  </read_first>
  <files>packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts</files>
  <action>
    In the `BuildBearDeployment` type (currently lines 20-33):
    - Change `mintTxHash: string` to `mintTxHash: string | null` with comment `// null on --no-mint artifact (Phase 10)`.
    - Change `mintedStrike: number` to `mintedStrike: number | null` with the same note.
    - Add a new optional field after `riskEngine?: string`:
      `snapshotId?: string // NEW (Phase 10) — evm_snapshot id, hex e.g. "0x1"; absent on legacy artifacts`
    Do NOT touch `validateDeployment`'s `required` array — it already omits mintTxHash/mintedStrike, so null values pass the runtime check (research Pattern 5 confirms this is safe, purely additive). Do NOT change `isExpired` or the static import.
    This lands BEFORE Plan 10-02 writes the --no-mint artifact, and unblocks Phase 11's buildbear-reset route reading snapshotId (Wave-0 shared migration per the v3.0 dependency graph).
  </action>
  <acceptance_criteria>
    - `grep -n "mintTxHash: string | null" packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts` matches
    - `grep -n "snapshotId" packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts` matches
    - `pnpm --filter d2p-frontend exec tsc --noEmit` exits 0 (the committed artifact still type-checks; no consumer breaks)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/d2p-monorepo && pnpm --filter d2p-frontend exec tsc --noEmit</automated>
  </verify>
  <done>BuildBearDeployment accepts `mintTxHash: null`, `mintedStrike: null`, and an optional `snapshotId`; tsc green; validateDeployment untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write EXEC-01 guard test (RED — must fail before Plan 10-02 adds the guard)</name>
  <read_first>
    - packages/backend/contracts/src/MacroHedgeExecutor.sol (lines 357-423 — the shared sink + entrypoint signatures resolveFromMandate / resolveAndMint / _onResult; the `pool` immutable + 9-arg ctor)
    - packages/backend/contracts/test/instrument/MacroHedgeExecutor.onResult.t.sol (the MockPlatform + PanopticPoolV2(address(0)) + RiskManagement(address(0)) subclass/mock pattern to mirror; `_demoLegParams()`)
    - packages/backend/contracts/test/mocks/MockPlatform.sol (constructor + interface surface)
    - .github/workflows/ci.yml (lines 49-50 — `forge test --no-match-path 'test/**/*[Ff]ork*'`; the test file MUST NOT match `*[Ff]ork*`)
  </read_first>
  <files>packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol</files>
  <behavior>
    - Test 1 (`test_WhenNumberOfLegsIsZeroFirstMintSucceeds`): with `pool.numberOfLegs(executor) == 0`, the first mint does NOT revert `"fork used"`.
    - Test 2 (`test_WhenNumberOfLegsIsNonZeroSecondMintRevertsForkedUsed`): after the first mint sets legs > 0, a second `resolveFromMandate` reverts with `"fork used"` (`vm.expectRevert(bytes("fork used"))`).
    - Test 3 (`test_WhenCalledViaResolveAndMintSecondCallRevertsForkedUsed`): the second mint via the OTHER entrypoint `resolveAndMint` also reverts `"fork used"`, proving shared-sink coverage.
  </behavior>
  <action>
    Create a NEW keyless Foundry unit test at `test/unit/MacroHedgeExecutor.guard.t.sol` (create the `test/unit/` dir). The filename MUST contain NO `fork` substring (Pitfall 5 — else excluded from the secret-free CI lane, violating OPS-06).

    Strategy (research Pattern 2): a mock pool that increments a per-address `legs` counter on `dispatch` and returns it from `numberOfLegs(address)`. Point a `MacroHedgeExecutor` at this mock pool so the real `_resolveAndMintAtStrike` runs to the (future) guard `require`. First mint: legs 0 → passes; mock increments. Second mint: legs > 0 → must revert `"fork used"`.

    MEDIUM RISK to resolve here (research Open Question 2 + 10-VALIDATION Wave-0 open risk): the executor ctor takes a `PanopticPoolV2`-typed `pool`; a bare counter may not be castable. Resolve by EITHER (preferred) `contract MockPoolForGuard is PanopticPoolV2` overriding only `numberOfLegs(address)` + `dispatch(...)`, OR `vm.mockCall`/`vm.etch` on `numberOfLegs(address(executor))` returning 0 then 1 across the two calls. Pick whichever compiles cleanly against the real `PanopticPoolV2` signature; document the choice in a header comment. If neither is feasible within budget, escalate — do NOT fabricate a passing test.

    Use the demo mandate shape from onResult.t.sol `_demoLegParams()` / s.sol `_demoMandate` (economicTheory sentinel `address(uint160(0x6))`, `chainId = uint32(block.chainid)`, `isLong: true`) so the chainId self-check passes and execution reaches the guard site.

    This test MUST be RED right now (the guard is added in Plan 10-02). Confirm RED: the second call does NOT yet revert `"fork used"`. Record the RED outcome in the commit message.
  </action>
  <acceptance_criteria>
    - File exists at `test/unit/MacroHedgeExecutor.guard.t.sol`; the path contains no `fork` substring (`echo the-path | grep -i fork` returns nothing)
    - `grep -c 'fork used' packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol` >= 1
    - `grep -E 'test_WhenNumberOfLegsIsNonZero|test_WhenCalledViaResolveAndMint' packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol` matches both names
    - The test file COMPILES (`forge build` succeeds)
    - RED proof: `forge test ... --match-path 'test/unit/MacroHedgeExecutor.guard.t.sol'` shows `test_WhenNumberOfLegsIsNonZeroSecondMintRevertsForkedUsed` FAILING (guard absent)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/d2p-monorepo/packages/backend/contracts && forge build 2>&1 | tail -5 && forge test --no-match-path 'test/**/*[Ff]ork*' --match-path 'test/unit/MacroHedgeExecutor.guard.t.sol' 2>&1 | tail -20</automated>
  </verify>
  <done>Guard test compiles, is fork-free-named, asserts `"fork used"` on the 2nd mint via BOTH `resolveFromMandate` and `resolveAndMint`, and currently FAILS (RED) because the guard is not yet inserted.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold 10-SPIKE-EVIDENCE.md (operator-manual transcript gate)</name>
  <read_first>
    - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-CONTEXT.md (the four required sections a-d; the poisoned executor `0xa95Ffdf…` to never reuse)
    - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-VALIDATION.md (Manual-Only Verifications table — the exact cast commands per section)
  </read_first>
  <files>.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md</files>
  <action>
    Create the committed evidence scaffold the operator fills in Plan 10-03 from the LIVE BuildBear run. It is the phase's completion gate (transcript-based, NOT CI). Include a header stating: this file is operator-manual, NOT CI-runnable, and must NEVER be claimed on-rhythm/green (OPS-06). State loudly that the spike runs against a FRESHLY-PROVISIONED `--no-mint` throwaway stack — never the poisoned committed executor `0xa95Ffdf51f71fE9C8861Fdbb1cAA664FA78A5FE3` (Pitfall 4).

    Four sections, each with a status checkbox `[ ]`, a fenced ```` ```text ```` block placeholder for the pasted transcript, and the exact command to run:
    - `## (a) Pre-guard 2nd-mint baseline (EXEC-01 / PROV-01)` — provision a throwaway `--no-mint` stack; `cast call <pool> "numberOfLegs(address)(uint256)" <executor>` returns `0`; then `cast send <executor> resolveFromMandate(...)` twice; record the 2nd outcome (the unguarded baseline) + the fresh executor address (must NOT equal `0xa95Ffdf…`).
    - `## (b) evm_snapshot -> evm_revert round-trip (PROV-02 / PROV-03)` — `cast rpc evm_snapshot`; mint; `cast rpc evm_revert <id>`; then assert `numberOfLegs == 0`, `cast balance <SIGNER_EOA>` equals the funded amount, and a fresh `resolveFromMandate` succeeds.
    - `## (c) viem server-side signing dry-run (Phase 11 dependency)` — privateKeyToAccount(DEMO_SIGNER_PK) + createWalletClient against the artifact chain config; dry-run `resolveFromMandate` succeeds.
    - `## (d) On-fork post-guard "fork used" revert (EXEC-01)` — after redeploying the GUARDED executor (post Plan 10-02), `cast send resolveFromMandate` twice; the 2nd reverts `"fork used"`.

    Add a final `## Sign-off` checklist: all four sections recorded + fresh executor address ≠ poisoned address + `git diff` shows the frontend artifact changed.
  </action>
  <acceptance_criteria>
    - `grep -E '^## \(a\)|^## \(b\)|^## \(c\)|^## \(d\)' .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md` matches all four sections
    - `grep -i '0xa95Ffdf' .planning/phases/.../10-SPIKE-EVIDENCE.md` matches (the poisoned address is named as the thing to avoid)
    - `grep -iE 'operator-manual|never.*on-rhythm|not CI' .../10-SPIKE-EVIDENCE.md` matches (OPS-06 honesty note present)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/d2p-monorepo && grep -cE '^## \((a|b|c|d)\)' .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md</automated>
  </verify>
  <done>Evidence scaffold exists with sections (a)-(d), the poisoned-address warning, and the OPS-06 not-CI honesty header — ready for the operator to fill in Plan 10-03.</done>
</task>

</tasks>

<verification>
- `pnpm --filter d2p-frontend exec tsc --noEmit` green (type migration safe).
- `forge build` succeeds and the guard test compiles.
- `forge test --no-match-path 'test/**/*[Ff]ork*' --match-path 'test/unit/MacroHedgeExecutor.guard.t.sol'` shows the 2nd-mint test FAILING (RED) — this is the expected, desired state for Wave 0.
- The guard test path has no `fork` substring (rides the secret-free CI lane).
- 10-SPIKE-EVIDENCE.md scaffold has all four sections.
</verification>

<success_criteria>
- BuildBearDeployment type accepts `mintTxHash: null` + `snapshotId?` (PROV-04/Phase-11 enabler); tsc green.
- EXEC-01 guard test exists, compiles, is fork-free-named, asserts `"fork used"` via both entrypoints, and is RED.
- 10-SPIKE-EVIDENCE.md scaffold committed with sections (a)-(d) + OPS-06 honesty header + poisoned-address warning.
- No production contract or shell changes in this wave (red-before-green discipline preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-01-SUMMARY.md`
</output>

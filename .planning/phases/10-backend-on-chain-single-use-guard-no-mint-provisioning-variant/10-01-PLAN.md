---
phase: 10-backend-on-chain-single-use-guard-no-mint-provisioning-variant
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts
  - packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol
  - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md
autonomous: true
requirements: [EXEC-01, PROV-04]
must_haves:
  truths:
    - "The frontend BuildBearDeployment type accepts mintTxHash: null and a snapshotId field without tsc error, and validateDeployment is exported for downstream round-trip tests"
    - "A keyless Foundry guard test exists that asserts the 2nd resolveAndMint call reverts \"fork used\" — and it currently FAILS (RED) because call 2 does NOT revert with \"fork used\" (the guard is absent), NOT because of an unrelated oracle/dispatch revert"
    - "The guard test constructs the executor against a placeholder pool address via vm.mockCall (numberOfLegs returns 0 then 1) — NOT a PanopticPoolV2 subclass (which cannot compile: numberOfLegs/dispatch are non-virtual)"
    - "The guard test file name contains no 'fork' substring, so it rides the secret-free CI lane"
    - "An operator-fillable 10-SPIKE-EVIDENCE.md scaffold exists with the four required live-fork sections (a)-(d)"
  artifacts:
    - path: "packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts"
      provides: "BuildBearDeployment type with mintTxHash: string | null, mintedStrike: number | null, snapshotId?: string; validateDeployment EXPORTED"
      contains: "export function validateDeployment"
    - path: "packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol"
      provides: "EXEC-01 RED guard test (real-oracle executor + vm.mockCall numberOfLegs 0→1; call 1 via resolveAndMint does not revert \"fork used\", call 2 must revert \"fork used\")"
      contains: "fork used"
    - path: ".planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md"
      provides: "Operator-manual live-fork transcript scaffold (sections a-d)"
      contains: "## (a)"
  key_links:
    - from: "MacroHedgeExecutor.guard.t.sol"
      to: "MacroHedgeExecutor._resolveAndMintAtStrike"
      via: "resolveAndMint call into the shared sink (does NOT touch the regime oracle, unlike resolveFromMandate)"
      pattern: "resolveAndMint"
    - from: "MacroHedgeExecutor.guard.t.sol"
      to: "pool.numberOfLegs(address(executor))"
      via: "vm.mockCall on the PanopticPoolV2 placeholder pool returning 0 then 1"
      pattern: "vm.mockCall"
    - from: "artifact-loader.ts BuildBearDeployment + exported validateDeployment"
      to: "--no-mint artifact (Plan 10-02) + null-fixture round-trip vitest test (Plan 10-02 Task 3)"
      via: "mintTxHash null + snapshotId fields the shell will write, validated via the exported validateDeployment"
      pattern: "snapshotId"
---

<objective>
Wave 0: establish the test-first and type-migration foundation for Phase 10 before any production code changes.

Three deliverables, all CI-checkable or operator-scaffolded:
1. **artifact-loader.ts type migration + validateDeployment export** (PROV-04 enabler, shared with Phase 11): widen `mintTxHash`/`mintedStrike` to nullable, add `snapshotId?: string`, AND change `function validateDeployment` → `export function validateDeployment` so the `--no-mint` artifact (written in Plan 10-02) is not rejected by the validator or `tsc`, and the Plan 10-02 null-fixture round-trip vitest test can import the real validator.
2. **EXEC-01 guard test, RED-first** (EXEC-01): a keyless Foundry unit test asserting the 2nd `resolveAndMint` reverts `"fork used"`. It MUST fail now (the guard does not exist yet) — Plan 10-02 turns it GREEN. Named WITHOUT any `fork` substring so it runs in the secret-free CI lane.
3. **10-SPIKE-EVIDENCE.md scaffold** (operator-manual gate): the committed transcript file with sections (a)-(d) the operator fills from the live BuildBear run in Plan 10-03.

Purpose: lock the cross-layer contract (`"fork used"` string + nullable artifact shape + exported validator) and the red-before-green discipline before touching the contract or the provisioning script.
Output: one modified TS type file (two deliberate changes: nullable/snapshotId widening + validateDeployment export; the static JSON import stays), one new RED Foundry test, one new evidence scaffold.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-CONTEXT.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-RESEARCH.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-VALIDATION.md

<interfaces>
<!-- Current BuildBearDeployment type the executor reads. Extracted from codebase — use directly. -->

From packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts (current):
```typescript
export type BuildBearDeployment = {
  chainId: number
  executor: string
  pool: string
  riskManagement: string
  rpcUrl: string
  mintTxHash: string        // <- widen to string | null
  mintedStrike: number      // <- widen to number | null
  capturedAt: string
  source: string
  factory?: string
  riskEngine?: string
  // <- add snapshotId?: string
}

// CURRENTLY module-private — Plan 10-02 Task 3's null-fixture round-trip test needs it.
// Change `function validateDeployment` → `export function validateDeployment`:
function validateDeployment(raw: unknown): BuildBearDeployment { ... }
```
The `validateDeployment` `required` array is `['chainId','executor','pool','rpcUrl','capturedAt']` — it does NOT list `mintTxHash`/`mintedStrike`, so null values already pass the runtime check. Only the TS type needs widening; the function body is unchanged — just add the `export` keyword.

From packages/backend/contracts/test/instrument/MacroHedgeExecutor.onResult.t.sol (the executor ctor wiring to follow — but NOTE the probe OVERRIDES and SKIPS the sink; the guard test must NOT subclass-and-skip, it must hit the REAL sink):
```solidity
import {MockPlatform} from "../mocks/MockPlatform.sol";
import {PanopticPoolV2} from "@contracts/PanopticPool.sol";
import {RiskManagement} from "../../src/RiskManagement.sol";
import {MockRegimeOracle} from "../mocks/MockRegimeOracle.sol";
import {MockSurpriseOracle} from "../mocks/MockSurpriseOracle.sol";
// The full ctor is (platform, pool, riskManager, vegoid, beta1Tranquil, beta1Stress,
//   targetDev, baseVol, regimeOracle, surpriseOracle) — see the probe ctor at lines 156-170.
```
Available mocks: packages/backend/contracts/test/mocks/{MockPlatform,MockRegimeOracle,MockSurpriseOracle,MockMacroOracle,MockCcop}.sol

The shared sink under test (MacroHedgeExecutor.sol), VERIFIED line numbers:
- `_resolveAndMintAtStrike` (line 357) is reached by all three entrypoints: `resolveAndMint` (line 156, computes strike from strikeWAD, NO oracle read), `resolveFromMandate` (line 209, PKE arm calls `regimeOracle.latestRegime()` at line 223 BEFORE the sink), `_onResult` (line 180, internal).
- The (future) guard inserts at the blank line 366, immediately AFTER `require(... block.chainid, "No crosschain allowed yet")` (line 365) and BEFORE the first `pool.dispatch` (line 401). The second dispatch is at line 415.
- CRITICAL: the guard at line 366 is ABOVE the `if (legParams.isLong && positionSize > 0)` dispatch gate (line 386). So `pool.numberOfLegs(address(this))` is dereferenced UNCONDITIONALLY on EVERY sink call once the guard exists — for both the success and the revert call. Pre-guard, with `positionSize == 0` the dispatch block is skipped and the pool is never touched.
- `pool` is an `immutable PanopticPoolV2` set in the 9-arg ctor. The guard (added in Plan 10-02) will be `require(pool.numberOfLegs(address(this)) == 0, "fork used")`.
- `PanopticPoolV2` is a CONCRETE contract; `numberOfLegs` (line 2155) and `dispatch` (line 666) are NON-virtual — a mock subclass cannot override them. Use `vm.mockCall` against a placeholder pool address (see Task 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate artifact-loader.ts BuildBearDeployment type to nullable mintTxHash + snapshotId, and export validateDeployment</name>
  <read_first>
    - packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts (the file being modified — read fully)
  </read_first>
  <files>packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts</files>
  <action>
    TWO deliberate changes to `artifact-loader.ts` (the static `import rawDeployment from './buildbear-deployments.json'` STAYS — do NOT touch it; do NOT touch `isExpired`):

    (1) In the `BuildBearDeployment` type (currently lines 20-33):
    - Change `mintTxHash: string` to `mintTxHash: string | null` with comment `// null on --no-mint artifact (Phase 10)`.
    - Change `mintedStrike: number` to `mintedStrike: number | null` with the same note.
    - Add a new optional field after `riskEngine?: string`:
      `snapshotId?: string // NEW (Phase 10) — evm_snapshot id, hex e.g. "0x1"; absent on legacy artifacts`

    (2) Change `function validateDeployment` (currently line 39) to `export function validateDeployment` — add ONLY the `export` keyword; the function BODY is unchanged. This makes the validator importable by Plan 10-02 Task 3's null-fixture round-trip vitest test (`import { validateDeployment } from '@/lib/apps/abrigo/cornerstone/artifact-loader'`). Do NOT change the `required` array — it already omits mintTxHash/mintedStrike, so null values pass the runtime check (research Pattern 5 confirms this is safe, purely additive).

    These two changes (nullable widening + the export) land BEFORE Plan 10-02 writes the --no-mint artifact and before its round-trip test imports the validator, and unblock Phase 11's buildbear-reset route reading snapshotId (Wave-0 shared migration per the v3.0 dependency graph).
  </action>
  <acceptance_criteria>
    - `grep -n "mintTxHash: string | null" packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts` matches
    - `grep -n "snapshotId" packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts` matches
    - `grep -nE '^export function validateDeployment' packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts` matches (the validator is now exported for the Plan 10-02 round-trip test)
    - The static JSON import is unchanged: `grep -n "import rawDeployment from './buildbear-deployments.json'" packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts` still matches
    - `pnpm --filter d2p-frontend exec tsc --noEmit` exits 0 (the committed artifact still type-checks; no consumer breaks)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/d2p-monorepo && grep -nE '^export function validateDeployment' packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts && pnpm --filter d2p-frontend exec tsc --noEmit</automated>
  </verify>
  <done>BuildBearDeployment accepts `mintTxHash: null`, `mintedStrike: null`, and an optional `snapshotId`; `validateDeployment` is exported (body unchanged); the static JSON import is untouched; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write EXEC-01 guard test (RED — must fail for the RIGHT reason before Plan 10-02 adds the guard)</name>
  <read_first>
    - packages/backend/contracts/src/MacroHedgeExecutor.sol (lines 127-148 the full ctor; lines 156-169 resolveAndMint — the RED carrier, NO oracle read; lines 357-422 the shared sink — confirm the guard site at 366, the dispatch gate at 386, dispatch at 401/415)
    - packages/backend/contracts/test/instrument/MacroHedgeExecutor.onResult.t.sol (the ctor WIRING to mirror at lines 156-170 — but note the probe OVERRIDES the sink to skip dispatch; the guard test must NOT do that, it must hit the real sink; use `_demoLegParams()` at lines 50-62 as the leg shape)
    - packages/backend/contracts/test/mocks/MockRegimeOracle.sol (real oracle mock: `set(Regime)` + `latestRegime()`)
    - packages/backend/contracts/test/mocks/MockSurpriseOracle.sol (real oracle mock: `set(...)` + `latestSurprise()`)
    - packages/backend/contracts/test/mocks/MockPlatform.sol (constructor `MockPlatform(uint256 floor)`)
    - .github/workflows/ci.yml (lines 49-50 — `forge test --no-match-path 'test/**/*[Ff]ork*'`; the test file MUST NOT match `*[Ff]ork*`)
  </read_first>
  <files>packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol</files>
  <behavior>
    - Test 1 (`test_WhenNumberOfLegsIsZeroFirstResolveAndMintDoesNotRevertForkUsed`): with `numberOfLegs` mocked to 0, the first `resolveAndMint` call does NOT revert `"fork used"`. (It does not claim a real "mint succeeds" — with `positionSize == 0` the dispatch block is skipped and the call simply completes.)
    - Test 2 (`test_WhenNumberOfLegsIsNonZeroSecondResolveAndMintRevertsForkUsed`): with `numberOfLegs` mocked to 1, a second `resolveAndMint` is wrapped in `vm.expectRevert(bytes("fork used"))`. RIGHT NOW (no guard) this call does NOT revert with `"fork used"`, so the test FAILS — that is the desired RED.
    - Test 3 (`test_WhenCalledViaResolveFromMandateGuardSiteCoversThatEntrypoint`) [OPTIONAL coverage note]: assert the sink is shared so the guard (Plan 10-02) also covers `resolveFromMandate`. If wiring `resolveFromMandate` (which reads the regime oracle) is non-trivial within budget, document the shared-sink argument in a comment instead — the primary RED carrier is `resolveAndMint`.
  </behavior>
  <action>
    Create a NEW keyless Foundry unit test at `test/unit/MacroHedgeExecutor.guard.t.sol` (create the `test/unit/` dir). The filename MUST contain NO `fork` substring (Pitfall 5 — else excluded from the secret-free CI lane, violating OPS-06).

    STRATEGY — `vm.mockCall` is the SOLE approach (research Open Question 2 is now RESOLVED in favor of vm.mockCall; the `MockPoolForGuard is PanopticPoolV2` subclass option is DELETED — it cannot compile: both `numberOfLegs` (PanopticPool.sol:2155) and `dispatch` (PanopticPool.sol:666) are NON-virtual on the concrete `PanopticPoolV2`, and a standalone mock is not castable to the `PanopticPoolV2`-typed immutable ctor param). Use a non-zero placeholder pool address.

    Use `resolveAndMint` as the PRIMARY RED carrier — it does NOT call the regime oracle (unlike `resolveFromMandate`, which reverts at `regimeOracle.latestRegime()` ~line 223 before ever reaching the sink). The executor must still be constructed with REAL oracle mocks so any path is safe.

    Provide this concrete `setUp()` (do NOT just "follow the onResult pattern" — that pattern SKIPS the sink):
    ```solidity
    // Valid hex placeholder pool (any non-zero address; NO non-hex chars).
    address constant POOL = address(0x000000000000000000000000000000000000C0FE);
    MacroHedgeExecutor executor;

    function setUp() public {
        MockPlatform platform = new MockPlatform(0.01 ether);
        MockRegimeOracle regime = new MockRegimeOracle();
        MockSurpriseOracle surprise = new MockSurpriseOracle();
        regime.set(IRegimeOracle.Regime.Tranquil);   // live, non-stale (resolveAndMint doesn't read it, but keep safe)
        executor = new MacroHedgeExecutor(
            address(platform),
            PanopticPoolV2(POOL),
            RiskManagement(address(0)),
            0,            // vegoid
            0.10e18, 0.35e18, 0.15e18, 14_400,
            IRegimeOracle(address(regime)),
            ISurpriseOracle(address(surprise))
        );
        vm.deal(address(this), 1000 ether);
    }
    ```

    Build the leg via `_demoLegParams()` (copy the helper from onResult.t.sol: `isLong: true`, `chainId: uint32(block.chainid)`, `size: 100`, `strikeWAD: 4.1e18`, the WCOP/USDC pool id). Call `resolveAndMint(legParams, 0, 0)` — use `positionSize == 0` so the `if (legParams.isLong && positionSize > 0)` dispatch gate (line 386) is SKIPPED and `pool.dispatch` is NEVER reached. This makes the call complete cleanly once the guard passes, with no real Panoptic stack needed.

    Mock `numberOfLegs` for BOTH calls (the guard at line 366 dereferences it UNCONDITIONALLY once it exists — above the dispatch gate, so it must be live even on the positionSize==0 path):
    ```solidity
    // Call 1 — legs == 0 → guard (once present) passes:
    vm.mockCall(POOL, abi.encodeWithSelector(PanopticPoolV2.numberOfLegs.selector, address(executor)), abi.encode(uint256(0)));
    executor.resolveAndMint(_demoLegParams(), 0, 0); // MUST NOT revert "fork used"

    // Call 2 — legs == 1 → guard (once present) reverts "fork used":
    vm.mockCall(POOL, abi.encodeWithSelector(PanopticPoolV2.numberOfLegs.selector, address(executor)), abi.encode(uint256(1)));
    vm.expectRevert(bytes("fork used"));
    executor.resolveAndMint(_demoLegParams(), 0, 0);
    ```
    If you instead choose a non-zero `positionSize` (NOT recommended) and the call reaches `pool.dispatch`, you MUST also `vm.mockCall(POOL, abi.encodeWithSelector(PanopticPoolV2.dispatch.selector), "")` so call 1 completes without a real Panoptic stack. Prefer `positionSize == 0` to avoid this entirely.

    Test names/behaviors must NOT claim a real "mint succeeds". Assert precisely: call 1 does NOT revert with `"fork used"`; call 2 (legs mocked to 1) reverts with exactly `"fork used"`.

    This test MUST be RED right now (the guard is added in Plan 10-02). The RED must be for the RIGHT reason: call 2's `vm.expectRevert(bytes("fork used"))` FAILS because the call does NOT revert / reverts with different data (the guard is absent) — NOT because of an unrelated oracle or dispatch revert. Record the RED outcome and confirm the failure message is "call did not revert as expected" (or a non-"fork used" revert), in the commit message.
  </action>
  <acceptance_criteria>
    - File exists at `test/unit/MacroHedgeExecutor.guard.t.sol`; the path contains no `fork` substring (`echo the-path | grep -i fork` returns nothing)
    - `grep -c 'fork used' packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol` >= 1
    - `grep -E 'vm.mockCall' packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol` matches (vm.mockCall is the strategy; NO `is PanopticPoolV2` subclass)
    - `grep -c 'is PanopticPoolV2' packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol` == 0 (the non-compilable subclass approach is absent)
    - The placeholder pool literal is VALID hex (no `P`/`O`/`L` chars): `grep -E '0x[0-9a-fA-F]+' packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol` matches the POOL constant AND `grep -E '0x[0-9a-fA-FxX]*[P-Zp-z]' packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol` returns nothing for the POOL literal (no invalid hex digits)
    - `grep -E 'test_WhenNumberOfLegsIsNonZeroSecondResolveAndMintRevertsForkUsed' packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol` matches
    - The test file COMPILES (`forge build` succeeds — proves the placeholder hex literal is valid)
    - RED proof (for the RIGHT reason): `forge test ... --match-path 'test/unit/MacroHedgeExecutor.guard.t.sol'` shows `test_WhenNumberOfLegsIsNonZeroSecondResolveAndMintRevertsForkUsed` FAILING with a `vm.expectRevert(bytes("fork used"))` mismatch (the call did NOT revert / reverted with different data — guard absent), NOT an oracle/dispatch/Reentrancy revert. The failure line must reference the expected `fork used` bytes.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/d2p-monorepo/packages/backend/contracts && forge build 2>&1 | tail -5 && forge test --no-match-path 'test/**/*[Ff]ork*' --match-path 'test/unit/MacroHedgeExecutor.guard.t.sol' 2>&1 | tail -25</automated>
  </verify>
  <done>Guard test compiles (valid hex placeholder pool `0x...C0FE`), is fork-free-named, uses `vm.mockCall` (no PanopticPoolV2 subclass) to drive `numberOfLegs` 0→1, exercises the REAL sink via `resolveAndMint` with `positionSize == 0`, and currently FAILS (RED) because call 2 does NOT revert `"fork used"` (guard absent) — not because of an unrelated revert.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold 10-SPIKE-EVIDENCE.md (operator-manual transcript gate)</name>
  <read_first>
    - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-CONTEXT.md (the four required sections a-d; the poisoned executor `0xa95Ffdf…` to never reuse)
    - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-VALIDATION.md (Manual-Only Verifications table — the exact cast commands per section)
  </read_first>
  <files>.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md</files>
  <action>
    Create the committed evidence scaffold the operator fills in Plan 10-03 from the LIVE BuildBear run. It is the phase's completion gate (transcript-based, NOT CI). Include a header stating: this file is operator-manual, NOT CI-runnable, and must NEVER be claimed on-rhythm/green (OPS-06). State loudly that the spike runs against a FRESHLY-PROVISIONED `--no-mint` throwaway stack — never the poisoned committed executor `0xa95Ffdf51f71fE9C8861Fdbb1cAA664FA78A5FE3` (Pitfall 4).

    At the very top, add a FORK-LIVENESS PRE-FLIGHT line: `cast chain-id --rpc-url $RPC` must respond before each section (guards against the 3-day-TTL dead-node failure — if it errors, the sandbox is dead and must be re-provisioned).

    Four sections, each with a status checkbox `[ ]`, a fenced ```` ```text ```` block placeholder for the pasted transcript, and the exact command to run:
    - `## (a) Pre-guard 2nd-mint baseline (EXEC-01 / PROV-01)` — provision a throwaway `--no-mint` stack; `cast call <pool> "numberOfLegs(address)(uint256)" <executor>` returns `0`; then `cast send <executor> resolveFromMandate(...)` twice; record the 2nd outcome (the unguarded baseline) + the fresh executor address (must NOT equal `0xa95Ffdf…`).
    - `## (b) evm_snapshot -> evm_revert round-trip (PROV-02 / PROV-03)` — `cast rpc evm_snapshot`; mint; `cast rpc evm_revert <id>`; then assert `numberOfLegs == 0`, `cast balance <SIGNER_EOA>` equals the funded amount, and a fresh `resolveFromMandate` succeeds.
    - `## (c) viem server-side signing dry-run (Phase 11 dependency)` — privateKeyToAccount(DEMO_SIGNER_PK) + createWalletClient against the artifact chain config; dry-run `resolveFromMandate` succeeds. (Reproduced via the type-checked `packages/frontend/scripts/spike-viem-sign.ts` committed in Plan 10-02.)
    - `## (d) On-fork post-guard "fork used" revert (EXEC-01)` — after redeploying the GUARDED executor (post Plan 10-02), `cast send resolveFromMandate` twice; the 2nd reverts `"fork used"`.

    Add a final `## Sign-off` checklist: all four sections recorded + fresh executor address ≠ poisoned address + `git diff` shows the frontend artifact changed.
  </action>
  <acceptance_criteria>
    - `grep -E '^## \(a\)|^## \(b\)|^## \(c\)|^## \(d\)' .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md` matches all four sections
    - `grep -i 'cast chain-id' .../10-SPIKE-EVIDENCE.md` matches (fork-liveness pre-flight present)
    - `grep -i '0xa95Ffdf' .planning/phases/.../10-SPIKE-EVIDENCE.md` matches (the poisoned address is named as the thing to avoid)
    - `grep -iE 'operator-manual|never.*on-rhythm|not CI' .../10-SPIKE-EVIDENCE.md` matches (OPS-06 honesty note present)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/d2p-monorepo && grep -cE '^## \((a|b|c|d)\)' .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md</automated>
  </verify>
  <done>Evidence scaffold exists with sections (a)-(d), the fork-liveness pre-flight, the poisoned-address warning, and the OPS-06 not-CI honesty header — ready for the operator to fill in Plan 10-03.</done>
</task>

</tasks>

<verification>
- `pnpm --filter d2p-frontend exec tsc --noEmit` green (type migration safe); `validateDeployment` is exported.
- `forge build` succeeds and the guard test compiles (vm.mockCall strategy, no PanopticPoolV2 subclass, VALID hex placeholder pool).
- `forge test --no-match-path 'test/**/*[Ff]ork*' --match-path 'test/unit/MacroHedgeExecutor.guard.t.sol'` shows the 2nd-`resolveAndMint` test FAILING (RED) because call 2 does NOT revert `"fork used"` (guard absent) — NOT an unrelated revert. This is the expected, desired state for Wave 0.
- The guard test path has no `fork` substring (rides the secret-free CI lane).
- 10-SPIKE-EVIDENCE.md scaffold has all four sections + the fork-liveness pre-flight.
</verification>

<success_criteria>
- BuildBearDeployment type accepts `mintTxHash: null` + `snapshotId?` (PROV-04/Phase-11 enabler); `validateDeployment` exported for the Plan 10-02 round-trip test; static JSON import untouched; tsc green.
- EXEC-01 guard test exists, compiles (valid hex placeholder pool), is fork-free-named, uses `vm.mockCall` (numberOfLegs 0→1) against a placeholder pool with the REAL sink via `resolveAndMint` (positionSize 0), and is RED for the right reason (call 2 fails the `"fork used"` expectRevert because the guard is absent).
- 10-SPIKE-EVIDENCE.md scaffold committed with sections (a)-(d) + fork-liveness pre-flight + OPS-06 honesty header + poisoned-address warning.
- No production contract or shell changes in this wave (red-before-green discipline preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-01-SUMMARY.md`
</output>
</content>

---
phase: 10-backend-on-chain-single-use-guard-no-mint-provisioning-variant
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - packages/backend/contracts/src/MacroHedgeExecutor.sol
  - packages/backend/contracts/script/ProvisionBuildBearDemo.s.sol
  - packages/backend/contracts/script/provision-buildbear-demo.sh
  - packages/frontend/scripts/spike-viem-sign.ts
  - packages/frontend/tests/unit/cornerstone/artifact-null-roundtrip.test.ts
autonomous: true
requirements: [EXEC-01, PROV-01, PROV-04]
must_haves:
  truths:
    - "The shared sink _resolveAndMintAtStrike reverts \"fork used\" when the executor already holds a position — covering all three entrypoints"
    - "The EXEC-01 guard test (from Plan 10-01) is now GREEN, and the GREEN is non-vacuous (removing the guard line makes it RED again — proven by a mutation check)"
    - "The provisioning script skips the final mint (and its require) when SKIP_MINT=true, deploying a fresh executor with numberOfLegs == 0"
    - "The provision shell exposes a --no-mint flag that exports SKIP_MINT=true, funds a dedicated signer inside an evm_snapshot, gates the resolveFromMandate receipt-parse behind !NO_MINT, and writes the artifact directly to the frontend path with mintTxHash: null and a snapshotId; the MONO_ROOT anchor is runtime-asserted, a null-mintTxHash fixture round-trips through the exported validateDeployment via vitest, and the DEMO_SIGNER_PK-unset branch fails clearly"
    - "A type-checked frontend-resident spike-viem-sign.ts exists so the §(c) viem server-sign dry-run (Plan 10-03) is reproducible and CI-type-checked"
  artifacts:
    - path: "packages/backend/contracts/src/MacroHedgeExecutor.sol"
      provides: "EXEC-01 on-chain single-use guard in _resolveAndMintAtStrike, before any pool.dispatch"
      contains: "fork used"
    - path: "packages/backend/contracts/script/ProvisionBuildBearDemo.s.sol"
      provides: "SKIP_MINT gate (vm.envOr) around the final mint + its require(r.legs>0)"
      contains: "SKIP_MINT"
    - path: "packages/backend/contracts/script/provision-buildbear-demo.sh"
      provides: "--no-mint flag, DEMO_SIGNER_PK funding before evm_snapshot, !NO_MINT-gated receipt parse, MONO_ROOT runtime assertion, direct frontend artifact write with mintTxHash null + snapshotId"
      contains: "no-mint"
    - path: "packages/frontend/scripts/spike-viem-sign.ts"
      provides: "Type-checked viem privateKeyToAccount + createWalletClient dry-run of resolveFromMandate against the artifact chain config (frontend-resident so viem + artifact-loader resolve)"
      contains: "privateKeyToAccount"
    - path: "packages/frontend/tests/unit/cornerstone/artifact-null-roundtrip.test.ts"
      provides: "vitest unit test asserting validateDeployment accepts a mintTxHash:null + snapshotId fixture"
      contains: "validateDeployment"
  key_links:
    - from: "_resolveAndMintAtStrike guard"
      to: "pool.numberOfLegs(address(this))"
      via: "require before the first pool.dispatch (line ~401)"
      pattern: "numberOfLegs\\(address\\(this\\)\\) == 0"
    - from: "provision-buildbear-demo.sh --no-mint"
      to: "packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json"
      via: "MONO_ROOT stable-anchor path (runtime-asserted) + mkdir -p + jq --argjson mintTxHash null"
      pattern: "argjson mintTxHash null"
    - from: "provision-buildbear-demo.sh signer funding"
      to: "cast rpc evm_snapshot"
      via: "hardhat_setBalance SIGNER_EOA BEFORE evm_snapshot so revert restores gas"
      pattern: "hardhat_setBalance.*SIGNER_EOA"
    - from: "packages/frontend/scripts/spike-viem-sign.ts"
      to: "artifact-loader.ts BuildBearDeployment (rpcUrl/chainId)"
      via: "privateKeyToAccount(DEMO_SIGNER_PK) + createWalletClient against the artifact chain config"
      pattern: "createWalletClient"
    - from: "artifact-null-roundtrip.test.ts"
      to: "exported validateDeployment (Plan 10-01)"
      via: "import { validateDeployment } from '@/lib/apps/abrigo/cornerstone/artifact-loader'"
      pattern: "validateDeployment"
---

<objective>
Wave 1: turn the RED guard test GREEN (non-vacuously) and build the `--no-mint` provisioning variant + the reproducible viem dry-run spike script.

Five deliverables:
1. **EXEC-01 guard insertion** (EXEC-01): the three-line `require(pool.numberOfLegs(address(this)) == 0, "fork used")` in the shared sink `_resolveAndMintAtStrike`, before any `pool.dispatch`. This turns the Plan 10-01 guard test GREEN (and a mutation check proves the GREEN is non-vacuous) and covers all three entrypoints.
2. **SKIP_MINT gate** (PROV-01): gate the final mint + its `require(r.legs>0)` in `ProvisionBuildBearDemo.s.sol` behind `vm.envOr("SKIP_MINT", false)`, so a `--no-mint` run deploys a fresh executor with `numberOfLegs == 0`.
3. **Shell `--no-mint` variant** (PROV-01/PROV-04): a `--no-mint` flag that exports `SKIP_MINT=true`, derives + funds the dedicated `DEMO_SIGNER_PK` address via `hardhat_setBalance` BEFORE capturing an `evm_snapshot`, gates the `resolveFromMandate` receipt-parse block behind `!NO_MINT`, strips the snapshot-id quotes, runtime-asserts the `MONO_ROOT` anchor, and writes `buildbear-deployments.json` directly to the frontend path with `mintTxHash: null` (JSON null) and a `snapshotId` field.
4. **viem dry-run spike script** (Phase 11 dependency / §(c) reproducibility): a type-checked `packages/frontend/scripts/spike-viem-sign.ts` (frontend-resident so `viem` + `artifact-loader` resolve and the frontend `tsc` globs it) so the operator's §(c) dry-run is reproducible and CI-type-checked, not improvised at run time.
5. **null-fixture round-trip vitest test** (PROV-04 validation): a vitest unit test importing the now-exported `validateDeployment` and asserting a `mintTxHash:null` + `snapshotId` fixture passes — replacing the un-installed `tsx` invocation.

NOTE: the actual live `--no-mint` RUN against a hosted BuildBear sandbox (and the snapshot round-trip proof) is Plan 10-03's operator-manual task — it is NOT executed here. This plan delivers and statically verifies the CODE: guard test GREEN + mutation-proven, shell hardened (MONO_ROOT assertion + receipt-parse gate + unset-signer branch), the null-fixture round-trips through the exported validator via vitest, the spike script type-checks, and a dry-rendered artifact passes the `jq` check.
Output: one modified contract, one modified Solidity script, one modified shell script, one new type-checked TS spike script (frontend-resident), one new vitest test.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-CONTEXT.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-RESEARCH.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-VALIDATION.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-01-PLAN.md

<interfaces>
<!-- Exact edit sites extracted from the codebase. Use directly — no exploration needed. -->

MacroHedgeExecutor.sol — `_resolveAndMintAtStrike` (the guard chokepoint), VERIFIED:
```solidity
// line 363-365 (existing) — guard inserts at the blank line 366, AFTER 365, BEFORE line 367:
function _resolveAndMintAtStrike(...) internal virtual returns (TokenId positionId) {
    require(legParams.size <= 127, "optionRatio overflow");                       // line 364
    require(uint256(legParams.chainId) == block.chainid, "No crosschain allowed yet"); // line 365
    // <- INSERT GUARD HERE (line 366 is currently blank)
    int24 tickSpacing_ = legParams.payoffTerms.tickSpacing;                       // line 367
    ...
    if (legParams.isLong && positionSize > 0) {                                   // line 386 — dispatch gate
        ...
        pool.dispatch(ml, fl, sl, lim, true, 0);   // first dispatch at line 401 — guard MUST precede this
        ...
        pool.dispatch(ml, fl, sl, lim, true, 0);   // second dispatch at line 415
    }
```
`pool` is the immutable PanopticPoolV2 set in the ctor. `numberOfLegs` is a view that reverts `Reentrancy()` if called during dispatch — hence the guard must be before line 401 (Pitfall 1). The guard at line 366 is ABOVE the dispatch gate at 386, so it is dereferenced on EVERY sink call.

HedgeMandate tuple (packages/backend/contracts/src/types/HedgeMandate.sol) — the EXACT 5-field struct the viem ABI fragment must mirror for the §(c) dry-run, field order/types VERIFIED:
```solidity
struct HedgeMandate {
    IMacroThesis economicTheory;   // address  (resolvable handle)
    PoolId       underlyingMarket; // bytes32  (PoolId is a bytes32 user-defined value type)
    uint256      targetNotional;   // uint256  (whole-USD notional)
    uint32       chainId;          // uint32   (137 Polygon)
    bool         isLong;           // bool
}
```
viem ABI tuple for `resolveFromMandate(HedgeMandate, uint256, uint128)` is therefore: components in order `[address economicTheory, bytes32 underlyingMarket, uint256 targetNotional, uint32 chainId, bool isLong]`, then a `uint256` (legIndex/0) and a `uint128` (quoteMargin/1e6).

ProvisionBuildBearDemo.s.sol — current run()/_provision() (lines 125-195):
```solidity
function run() external {
    vm.startBroadcast();
    ProvisionResult memory r = _provision();   // <- _provision() currently does deploy+deposit+MINT
    vm.stopBroadcast();
    console2.log(...);                          // FACTORY_ADDRESS=, ..., NUMBER_OF_LEGS=
    require(r.legs > 0, "mint failed: executor owns no leg");   // line 138 — gate this
}
// _provision(): (1) core deploy, (2) pool, (3) executor, (4) deposit-on-behalf (lines 164-171),
// (5) mint via exec.resolveFromMandate(mandate,0,1e6) + strike + legs (lines 173-194).
```

provision-buildbear-demo.sh — current anchors:
- line 58: `EOA="$(cast wallet address --private-key "$BUILDBEAR_DEPLOYER_PK")"`
- line 65: `cast rpc hardhat_setBalance "$EOA" 0xd3c21bcecceda1000000 --rpc-url "$RPC" ... || true`  (deployer funding — same RPC method, new target for signer)
- line 91-93: `forge script ... --broadcast --slow --private-key "$BUILDBEAR_DEPLOYER_PK"`
- lines ~105-111: parse MINT_TX_HASH from run-latest.json (resolveFromMandate tx) — MUST be wrapped in `if [[ "$NO_MINT" != true ]]; then … fi` (FIX E: the --no-mint path has no resolveFromMandate tx to parse)
- line 114-115: `mkdir -p script/out; ART="script/out/buildbear-deployments.json"`  (REDIRECT to frontend)
- line 117-131: `jq -n ... --arg mintTxHash "$MINT_TX_HASH" ...`  (switch to --argjson null on --no-mint)

artifact-loader.ts (migrated by Plan 10-01) — the validator the null-fixture round-trip must pass:
- `validateDeployment(d)` is now EXPORTED (Plan 10-01 Task 1). Its required array: `['chainId','executor','pool','rpcUrl','capturedAt']` (mintTxHash NOT required → null passes).
- The `--no-mint` fixture `{chainId, executor, pool, riskManagement, rpcUrl, mintTxHash:null, mintedStrike:null, capturedAt, snapshotId, source}` MUST pass `validateDeployment` without throwing.

Frontend vitest convention (VERIFIED): tests live under `packages/frontend/tests/unit/cornerstone/*.test.ts`; vitest `include: ['tests/**/*.{test,spec}.{ts,tsx}']`, `@` alias → package root. `tsx` is NOT installed; `vitest` ^4.1.6 IS. The frontend `tsconfig.json` globs `**/*.ts` relative to the package, so `packages/frontend/scripts/spike-viem-sign.ts` IS type-checked by `pnpm --filter d2p-frontend exec tsc --noEmit` (a sibling-package file under `packages/backend` would NOT be).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert EXEC-01 single-use guard in _resolveAndMintAtStrike (turn Plan 10-01 test GREEN, non-vacuously)</name>
  <read_first>
    - packages/backend/contracts/src/MacroHedgeExecutor.sol (lines 357-422 — the shared sink; confirm line 365 is the chainId require and line 401 is the first pool.dispatch)
    - packages/backend/contracts/test/unit/MacroHedgeExecutor.guard.t.sol (the RED test from Plan 10-01 this insertion must satisfy)
    - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-RESEARCH.md (Pattern 1 + Pitfall 1 — exact insertion + why before dispatch)
  </read_first>
  <files>packages/backend/contracts/src/MacroHedgeExecutor.sol</files>
  <behavior>
    - With `pool.numberOfLegs(address(this)) == 0`, `_resolveAndMintAtStrike` proceeds normally (first call passes the guard).
    - With `pool.numberOfLegs(address(this)) != 0`, `_resolveAndMintAtStrike` reverts `"fork used"` BEFORE any `pool.dispatch` — for all three entrypoints (resolveFromMandate, resolveAndMint, _onResult), since they share this sink.
  </behavior>
  <action>
    Insert exactly these lines into `_resolveAndMintAtStrike` immediately AFTER the existing `require(uint256(legParams.chainId) == block.chainid, "No crosschain allowed yet");` (line 365) and BEFORE `int24 tickSpacing_ = ...` (line 367 — line 366 is currently blank):

    ```solidity
    // EXEC-01: single-use guard. Reverts on ANY resolve attempt once this executor already
    // holds legs (numberOfLegs != 0) — it is NOT a "before mint" check; it fires on every
    // entry to the shared sink. Must precede pool.dispatch (the numberOfLegs view reverts
    // Reentrancy() if called while the pool's reentrancy guard is active during dispatch).
    require(pool.numberOfLegs(address(this)) == 0, "fork used");
    ```

    CRITICAL constraints (locked decisions + Pitfall 1):
    - Use the EXACT string `"fork used"` — it is a cross-layer contract the frontend string-matches (HONEST-01, Phase 12). Do NOT use a custom error. Do NOT change the wording/casing.
    - NatSpec wording (FIX 6): describe the semantics as "reverts on any resolve attempt once the executor holds legs" — NOT "before mint". The guard is reached on every sink entry (it is above the dispatch gate), so any second resolve (regardless of which entrypoint) reverts once legs > 0.
    - Place it before the first `pool.dispatch` (line ~401). Do NOT place it between the two dispatches (line 401 and 415) — that would let the first dispatch mutate state and could trip `Reentrancy()`.
    - Do NOT add redundant guards on the other entrypoints — the sink placement already covers all three (CONTEXT discretion: only add elsewhere if the sink is bypassable, which it is not).

    This turns the Plan 10-01 guard test GREEN.
  </action>
  <acceptance_criteria>
    - `grep -n 'require(pool.numberOfLegs(address(this)) == 0, "fork used")' packages/backend/contracts/src/MacroHedgeExecutor.sol` matches, and its line number is GREATER than the `"No crosschain allowed yet"` line and LESS than the first `pool.dispatch` line
    - The guard NatSpec describes "reverts on any resolve attempt once the executor holds legs" (FIX 6 — NOT "before mint"): `grep -i 'any resolve attempt\|once.*holds legs' packages/backend/contracts/src/MacroHedgeExecutor.sol` matches near the guard
    - GREEN proof: `forge test --no-match-path 'test/**/*[Ff]ork*' --match-path 'test/unit/MacroHedgeExecutor.guard.t.sol'` — all guard tests PASS
    - NON-VACUOUS GREEN (mutation check): temporarily comment out / delete ONLY the inserted `require(... "fork used")` line, re-run `forge test --match-path 'test/unit/MacroHedgeExecutor.guard.t.sol'`, and CONFIRM `test_WhenNumberOfLegsIsNonZeroSecondResolveAndMintRevertsForkUsed` goes RED again (the `vm.expectRevert(bytes("fork used"))` is unmet because call 2 no longer reverts). Restore the guard line and re-confirm GREEN. Document both the RED-on-removal and GREEN-on-restore outcomes (command + result) in the SUMMARY. This proves the GREEN is driven by the guard, not by some incidental revert.
    - Full secret-free suite still green: `forge test --no-match-path 'test/**/*[Ff]ork*'` (no regression in the existing onResult/Representativeness/etc. unit suites)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/d2p-monorepo/packages/backend/contracts && forge test --no-match-path 'test/**/*[Ff]ork*' --match-path 'test/unit/MacroHedgeExecutor.guard.t.sol' 2>&1 | tail -15 && echo "--- full secret-free suite ---" && forge test --no-match-path 'test/**/*[Ff]ork*' 2>&1 | tail -8</automated>
  </verify>
  <done>The `"fork used"` guard sits between the chainId require and the first dispatch with NatSpec stating it reverts on any resolve attempt once the executor holds legs; all Plan-10-01 guard tests are GREEN; the mutation check proves the GREEN is non-vacuous (removing the guard makes call-2 RED again); the full secret-free suite is unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: Add SKIP_MINT gate to ProvisionBuildBearDemo.s.sol (PROV-01)</name>
  <read_first>
    - packages/backend/contracts/script/ProvisionBuildBearDemo.s.sol (lines 125-195 — run() + _provision(), incl. the deposit block 164-171, the mint block 173-184, the require at 138)
    - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-RESEARCH.md (Pattern 3 — extract-mint-to-run() approach; Open Question 3 — _provision refactor scope)
  </read_first>
  <files>packages/backend/contracts/script/ProvisionBuildBearDemo.s.sol</files>
  <action>
    Gate the final mint behind `vm.envOr("SKIP_MINT", false)` (research Pattern 3, preferred extraction approach):
    - Extract the mint step (currently lines 173-184: the `HedgeMandate` build, `exec.resolveFromMandate(mandate,0,1e6)`, `strike`, `quoteMargin`) OUT of `_provision()` and INTO `run()`, wrapped in `if (!vm.envOr("SKIP_MINT", false)) { ... }`. `_provision()` becomes deploy + deposit only, returning a `ProvisionResult` with `strike: 0` and `legs: 0` defaults on the skip path.
    - In `run()`, after `_provision()`: if `!skipMint`, perform the mint, then set `r.strike` and `r.legs = PanopticPoolV2(r.pool).numberOfLegs(r.executor)`.
    - Guard the `require(r.legs > 0, "mint failed: executor owns no leg")` (line 138) so it ONLY runs when `!skipMint` — on the `--no-mint` path legs is legitimately 0 and the require must NOT fire.
    - Keep all console2.log read-back lines (FACTORY_ADDRESS=, ..., NUMBER_OF_LEGS=) so the shell `grab()` still parses; on skip, MINTED_STRIKE and NUMBER_OF_LEGS log 0 (correct — no strike/legs yet).

    Either extraction-to-run() OR an inline `if (!vm.envOr(...))` inside `_provision()` compiles; extraction is cleaner (research). Pick one; keep `ProvisionResult` field set consistent so the shell parse does not break.
  </action>
  <acceptance_criteria>
    - `grep -n 'vm.envOr("SKIP_MINT"' packages/backend/contracts/script/ProvisionBuildBearDemo.s.sol` matches
    - The `require(r.legs > 0` is inside a `!skipMint` branch (not unconditional)
    - `forge build` succeeds (the script compiles)
    - Static gate proof (no broadcast): `forge build` clean; the SKIP_MINT branch is syntactically present (grep above)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/d2p-monorepo/packages/backend/contracts && forge build 2>&1 | tail -5 && grep -n 'SKIP_MINT' script/ProvisionBuildBearDemo.s.sol</automated>
  </verify>
  <done>The final mint + its require are gated behind `vm.envOr("SKIP_MINT", false)`; the script compiles; `--no-mint` will deploy a fresh executor with `numberOfLegs == 0`.</done>
</task>

<task type="auto">
  <name>Task 3: Add --no-mint flag, signer-funding-in-snapshot, !NO_MINT-gated receipt parse, MONO_ROOT-asserted frontend artifact write to provision-buildbear-demo.sh + null-fixture round-trip vitest (PROV-01/PROV-04)</name>
  <read_first>
    - packages/backend/contracts/script/provision-buildbear-demo.sh (full — esp. lines 58, 65, 91-93, 105-131)
    - packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts (the migrated nullable type + EXPORTED validateDeployment from Plan 10-01 — the artifact must match it AND pass validateDeployment)
    - packages/frontend/tests/unit/cornerstone/artifact-loader.test.ts (the existing vitest convention to mirror: `import { ... } from '@/lib/apps/abrigo/cornerstone/artifact-loader'`, `describe`/`it`/`expect` from 'vitest')
    - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-RESEARCH.md (Pattern 4 — arg parsing, signer funding order, snapshot quote-strip, stable-anchor path, jq --argjson null; Pitfalls 2/3)
  </read_first>
  <files>packages/backend/contracts/script/provision-buildbear-demo.sh, packages/frontend/tests/unit/cornerstone/artifact-null-roundtrip.test.ts</files>
  <action>
    Add a `--no-mint` variant. Keep bash minimal (the flag just sets the env var); the Solidity gate does the real work.

    1. **Arg parse** (near top, after shebang/setup): `NO_MINT=false; for arg in "$@"; do case "$arg" in --no-mint) NO_MINT=true ;; esac; done`.
    2. **Export the gate** before the `forge script` broadcast (line 91): `[[ "$NO_MINT" == true ]] && export SKIP_MINT=true`. (Forge reads `vm.envOr("SKIP_MINT", false)`.)
    3. **Gate the resolveFromMandate receipt-parse block (FIX E)** — wrap the EXISTING `MINT_TX_HASH` receipt-parse block (the `run-latest.json` existence check + the `jq` grep for the resolveFromMandate tx, ~shell lines 105-111) in `if [[ "$NO_MINT" != true ]]; then … fi` so the `--no-mint` path NEVER touches the resolveFromMandate receipt logic (on `--no-mint` there is no mint tx and `MINT_TX_HASH` stays unset → the artifact write uses `--argjson mintTxHash null`).
    4. **Dedicated signer funding INSIDE the snapshot** — only on `--no-mint`, AFTER the broadcast completes (deploy + deposit done) and BEFORE the snapshot:
       - Require `DEMO_SIGNER_PK` is set (env-sourced from gitignored contracts/.env; distinct from `BUILDBEAR_DEPLOYER_PK`). If unset on the `--no-mint` path, fail with a clear message and a non-zero exit — never fall back to the deployer key. Use exactly:
         ```bash
         if [[ "$NO_MINT" == true && -z "${DEMO_SIGNER_PK:-}" ]]; then
           echo "FAIL: --no-mint requires DEMO_SIGNER_PK (gitignored contracts/.env); refusing to fall back to BUILDBEAR_DEPLOYER_PK" >&2
           exit 1
         fi
         ```
       - `SIGNER_EOA="$(cast wallet address --private-key "$DEMO_SIGNER_PK")"; echo "DEMO_SIGNER_EOA=$SIGNER_EOA"` (print address ONLY, never the key).
       - `cast rpc hardhat_setBalance "$SIGNER_EOA" 0xd3c21bcecceda1000000 --rpc-url "$RPC" >/dev/null 2>&1 || true` (same method as line 65; NOT anvil_setBalance — Pitfall on BuildBear). This MUST run before evm_snapshot (Pitfall 2: snapshot-before-funding loses signer gas on revert).
    5. **Capture snapshot** (only on `--no-mint`, after signer funding, before any mint — there is no mint on this path):
       `SNAPSHOT_ID="$(cast rpc evm_snapshot --rpc-url "$RPC" | tr -d '"')"` then validate `[[ "$SNAPSHOT_ID" =~ ^0x[0-9a-fA-F]+$ ]] || { echo "FAIL: evm_snapshot returned: $SNAPSHOT_ID"; exit 1; }` (Pitfall 3: strip the quotes from cast rpc).
    6. **MONO_ROOT stable-anchor + RUNTIME ASSERTION** — replace the `script/out` write (lines 114-131) so on `--no-mint` it writes to the frontend path from a stable anchor, and prove the anchor resolved correctly:
       ```bash
       MONO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../.." && pwd)"
       echo "MONO_ROOT=$MONO_ROOT"
       # Hard anchor assertion: the resolved root MUST contain the monorepo's pnpm-workspace + both packages.
       [[ -f "$MONO_ROOT/pnpm-workspace.yaml" && -d "$MONO_ROOT/packages/frontend" && -d "$MONO_ROOT/packages/backend" ]] \
         || { echo "FAIL: MONO_ROOT anchor wrong: $MONO_ROOT (expected pnpm-workspace.yaml + packages/{frontend,backend})" >&2; exit 1; }
       FRONTEND_ART="$MONO_ROOT/packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json"
       mkdir -p "$(dirname "$FRONTEND_ART")"
       ```
    7. **Direct frontend artifact write** — on `--no-mint`, build the JSON with `jq -n ... --argjson mintTxHash null --argjson mintedStrike null --arg snapshotId "$SNAPSHOT_ID" ... > "$FRONTEND_ART"` (use `--argjson` for the nulls — `--arg ""` would write an empty STRING the validator rejects). Add `snapshotId:$snapshotId` to the jq object and set `source:"abrigo-somnia --no-mint provision-buildbear-demo.sh"`. On the default (mint) path, preserve the existing string-mintTxHash behavior writing to the same frontend path (so the artifact never drifts — eliminate the old `script/out` manual-copy step).
    8. **Summary block**: on `--no-mint`, echo `SNAPSHOT_ID`, `DEMO_SIGNER_EOA`, and `ARTIFACT=$FRONTEND_ART`. Loudly note the executor is fresh (`numberOfLegs == 0`, `mintTxHash: null`) — this overwrites/retires the poisoned committed artifact.

    Do NOT print `DEMO_SIGNER_PK` or `BUILDBEAR_DEPLOYER_PK` anywhere. Do NOT use `vm.rpc` for the snapshot — it would target the in-process node, not the hosted BuildBear sandbox (research anti-pattern). The live RUN is Plan 10-03; here only the script logic + the static checks below.

    9. **null-fixture round-trip vitest test (FIX B — replaces the un-installed `tsx` invocation)** — create `packages/frontend/tests/unit/cornerstone/artifact-null-roundtrip.test.ts` (the repo's frontend vitest dir, mirroring `artifact-loader.test.ts`). It MUST:
       - `import { validateDeployment } from '@/lib/apps/abrigo/cornerstone/artifact-loader'` (the symbol Plan 10-01 Task 1 now exports — no conditional "export it if missing" hedge needed; it IS exported).
       - `import { describe, expect, it } from 'vitest'`.
       - Build a fixture `{ chainId: 31337, executor: '0x0000000000000000000000000000000000000001', pool: '0x0000000000000000000000000000000000000002', riskManagement: '0x0000000000000000000000000000000000000003', rpcUrl: 'https://example.invalid', mintTxHash: null, mintedStrike: null, capturedAt: '2026-06-08T00:00:00.000Z', snapshotId: '0x1', source: 'test' }`.
       - Assert `validateDeployment(fixture)` does NOT throw and returns an object whose `mintTxHash` is `null` and whose `snapshotId` is `'0x1'` (e.g. `expect(() => validateDeployment(fixture)).not.toThrow()` and `expect(validateDeployment(fixture).mintTxHash).toBeNull()`).
       This proves the `--no-mint` artifact shape round-trips through the REAL exported validator, using the installed `vitest` (NOT `tsx`/`ts-node`, which are not installed).
  </action>
  <acceptance_criteria>
    - `bash -n packages/backend/contracts/script/provision-buildbear-demo.sh` exits 0 (syntax valid)
    - `grep -E '\-\-no-mint' packages/backend/contracts/script/provision-buildbear-demo.sh` matches the arg-parse case
    - `grep -E 'SKIP_MINT=true' packages/backend/contracts/script/provision-buildbear-demo.sh` matches
    - **(FIX E) receipt-parse gated behind !NO_MINT**: `grep -E 'if \[\[ "\$NO_MINT" != true \]\]' packages/backend/contracts/script/provision-buildbear-demo.sh` matches, AND the `MINT_TX_HASH` / `run-latest.json` parse lines fall inside that guard (manual review confirms the `fi` closes after the resolveFromMandate parse block)
    - `grep -E 'hardhat_setBalance "\$SIGNER_EOA"' packages/backend/contracts/script/provision-buildbear-demo.sh` matches AND appears on a line BEFORE the `evm_snapshot` line (funding precedes snapshot — Pitfall 2)
    - `grep -E 'evm_snapshot .*tr -d' packages/backend/contracts/script/provision-buildbear-demo.sh` matches (quote-strip — Pitfall 3)
    - `grep -E 'argjson mintTxHash null' packages/backend/contracts/script/provision-buildbear-demo.sh` matches
    - `grep -E 'packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json' packages/backend/contracts/script/provision-buildbear-demo.sh` matches (direct frontend write)
    - **(a) MONO_ROOT runtime anchor proof** — run the anchor block in isolation and assert it equals the real monorepo root:
      `MONO_ROOT="$(cd "$(dirname packages/backend/contracts/script/provision-buildbear-demo.sh)/../../../.." && pwd)"; echo "$MONO_ROOT"; test "$MONO_ROOT" = "/home/jmsbpp/apps/d2p/d2p-monorepo"` exits 0, AND the in-script guard (`pnpm-workspace.yaml` + `packages/{frontend,backend}` existence) is present (`grep -E 'pnpm-workspace.yaml.*packages/frontend|MONO_ROOT anchor wrong'` matches).
    - **(b) null-mintTxHash fixture round-trips through the EXPORTED validateDeployment via vitest** — the test file exists and passes:
      `pnpm --filter d2p-frontend exec vitest run tests/unit/cornerstone/artifact-null-roundtrip.test.ts` exits 0, the test imports the real `validateDeployment` from `@/lib/apps/abrigo/cornerstone/artifact-loader`, feeds a `mintTxHash:null` + `snapshotId` fixture, and asserts it does not throw (`grep -E "from '@/lib/apps/abrigo/cornerstone/artifact-loader'" packages/frontend/tests/unit/cornerstone/artifact-null-roundtrip.test.ts` matches; `grep -E 'validateDeployment' packages/frontend/tests/unit/cornerstone/artifact-null-roundtrip.test.ts` matches; `grep -E 'mintTxHash: null|mintTxHash:null' packages/frontend/tests/unit/cornerstone/artifact-null-roundtrip.test.ts` matches). NO `tsx`/`ts-node` invocation anywhere.
    - **(c) DEMO_SIGNER_PK-unset branch fails clearly** — the guard is present (`grep -E 'requires DEMO_SIGNER_PK|refusing to fall back'` matches) AND a dry invocation with the var unset exits non-zero with the message:
      `( unset DEMO_SIGNER_PK; NO_MINT=true; if [[ "$NO_MINT" == true && -z "${DEMO_SIGNER_PK:-}" ]]; then echo "FAIL: --no-mint requires DEMO_SIGNER_PK ..." >&2; exit 1; fi ); test $? -ne 0` — or invoke the script with `--no-mint` and `DEMO_SIGNER_PK` unset against a non-reachable RPC and confirm it exits before any cast call with the clear message.
    - `grep -c 'echo .*DEMO_SIGNER_PK\|echo .*BUILDBEAR_DEPLOYER_PK' ...` — no line echoes the KEY (only `cast wallet address --private-key` usage); manual review confirms no `echo "$DEMO_SIGNER_PK"`
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/d2p-monorepo && bash -n packages/backend/contracts/script/provision-buildbear-demo.sh && grep -nE '\-\-no-mint|SKIP_MINT=true|if \[\[ "\$NO_MINT" != true \]\]|hardhat_setBalance "\$SIGNER_EOA"|evm_snapshot|argjson mintTxHash null|packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json|MONO_ROOT anchor wrong|requires DEMO_SIGNER_PK' packages/backend/contracts/script/provision-buildbear-demo.sh && M="$(cd "$(dirname packages/backend/contracts/script/provision-buildbear-demo.sh)/../../../.." && pwd)" && echo "MONO_ROOT=$M" && test "$M" = "/home/jmsbpp/apps/d2p/d2p-monorepo" && echo MONO_ROOT_OK && pnpm --filter d2p-frontend exec vitest run tests/unit/cornerstone/artifact-null-roundtrip.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>The shell has a `--no-mint` flag exporting SKIP_MINT, gates the resolveFromMandate receipt-parse behind `!NO_MINT`, funds the dedicated signer before snapshot, strips the snapshot-id quotes, runtime-asserts the MONO_ROOT anchor, writes the artifact directly to the frontend path with `mintTxHash: null` + `snapshotId`; a vitest test feeds a null-mintTxHash fixture through the exported validateDeployment and passes; the DEMO_SIGNER_PK-unset branch fails clearly; `bash -n` clean; no key is ever printed. The live RUN is deferred to Plan 10-03.</done>
</task>

<task type="auto">
  <name>Task 4: Commit type-checked packages/frontend/scripts/spike-viem-sign.ts (reproducible §(c) viem dry-run, Phase 11 dependency)</name>
  <read_first>
    - packages/backend/contracts/src/types/HedgeMandate.sol (the EXACT 5-field mandate tuple `(address economicTheory, bytes32 underlyingMarket, uint256 targetNotional, uint32 chainId, bool isLong)` — pin the viem ABI fragment to this, do not guess)
    - packages/frontend/lib/apps/abrigo/cornerstone/artifact-loader.ts (BuildBearDeployment shape — rpcUrl, chainId, executor — the script reads the same artifact)
    - packages/frontend/lib/apps/abrigo/cornerstone/buildbear.ts (chain/client factory pattern — mirror its rpcUrl/chainId wiring)
    - packages/frontend/tests/unit/cornerstone/artifact-loader.test.ts (the `@/...` import convention the frontend tsconfig resolves)
    - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-RESEARCH.md (Standard Stack — viem privateKeyToAccount + createWalletClient; §(c) is a Phase 11 dependency)
  </read_first>
  <files>packages/frontend/scripts/spike-viem-sign.ts</files>
  <action>
    Create a minimal, TYPE-CHECKED viem dry-run script at `packages/frontend/scripts/spike-viem-sign.ts` (FIX D — frontend-resident so `viem` ^2.48 + the `@/lib/...artifact-loader` import resolve and the frontend `tsc`/`**/*.ts` glob type-checks it; a `packages/backend/contracts/script/*.ts` file is NOT globbed by the frontend tsconfig and viem is not installed there). The operator runs this for §(c) in Plan 10-03 — so the dry-run is reproducible and CI-type-checked, not improvised. This replaces the reviewer-flagged un-reproducible §(c).

    The script must:
    - Read `DEMO_SIGNER_PK` from `process.env` (NEVER hard-code; NEVER log it). If unset, exit non-zero with a clear message.
    - Load the artifact from `@/lib/apps/abrigo/cornerstone/buildbear-deployments.json` (rpcUrl, chainId, executor) — reuse the `BuildBearDeployment` type / loader from `@/lib/apps/abrigo/cornerstone/artifact-loader` so the field shapes are type-enforced.
    - Build a viem account with `privateKeyToAccount(process.env.DEMO_SIGNER_PK as \`0x${string}\`)` and a `createWalletClient({ account, chain: <from artifact chainId/rpcUrl>, transport: http(rpcUrl) })`.
    - DRY-RUN `resolveFromMandate` against the executor — i.e., a `publicClient.simulateContract` / `walletClient.prepareTransactionRequest`-style call that PREPARES/SIMULATES the tx WITHOUT broadcasting (the operator confirms it does not revert). Print only the signer ADDRESS and a `DRY_RUN_OK` / simulated gas — never the key, never broadcast.
    - Use a CONCRETE inline ABI fragment for `resolveFromMandate(HedgeMandate, uint256, uint128)` (FIX F): the first argument is the 5-field tuple in EXACT order/types from `HedgeMandate.sol` — `components: [{name:'economicTheory',type:'address'},{name:'underlyingMarket',type:'bytes32'},{name:'targetNotional',type:'uint256'},{name:'chainId',type:'uint32'},{name:'isLong',type:'bool'}]` (a `tuple`), then `{type:'uint256'}` (legIndex) and `{type:'uint128'}` (quoteMargin). Do NOT guess the tuple — it is pinned to the source struct.
    - Be self-contained for `tsc --noEmit` type-checking.

    Add a top-of-file comment: "Reproducible spike for 10-SPIKE-EVIDENCE.md §(c). Operator runs this live in Plan 10-03; here it is committed type-checked only. Reads DEMO_SIGNER_PK from env; never logs the key; never broadcasts (simulate/prepare only)."

    Acceptance is `tsc`/type-check green (the frontend `pnpm --filter d2p-frontend exec tsc --noEmit` globs `packages/frontend/scripts/*.ts`) — NOT a live run (the live §(c) run is Plan 10-03). This makes §(c) reproducible and CI-type-checked. (Per FIX 5 we commit the type-checked script rather than downscoping §(c) to non-gating.)
  </action>
  <acceptance_criteria>
    - File exists at `packages/frontend/scripts/spike-viem-sign.ts`
    - `grep -E 'privateKeyToAccount|createWalletClient' packages/frontend/scripts/spike-viem-sign.ts` matches both
    - `grep -E 'simulateContract|prepareTransactionRequest' packages/frontend/scripts/spike-viem-sign.ts` matches (dry-run, not broadcast)
    - **(FIX F) the HedgeMandate tuple is pinned**: `grep -E "name: ?'economicTheory'|economicTheory" packages/frontend/scripts/spike-viem-sign.ts` matches AND the ABI fragment lists `underlyingMarket` (bytes32), `targetNotional` (uint256), `chainId` (uint32), `isLong` (bool) in that order (`grep -E 'underlyingMarket|targetNotional|isLong'` matches all three)
    - No hard-coded key and no key logging: `grep -E '0x[0-9a-fA-F]{64}' packages/frontend/scripts/spike-viem-sign.ts` returns nothing; `grep -E 'console.*DEMO_SIGNER_PK' ...` returns nothing
    - Type-check green: `pnpm --filter d2p-frontend exec tsc --noEmit` exits 0 (the frontend tsconfig `**/*.ts` glob covers `scripts/spike-viem-sign.ts`).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/d2p-monorepo && test -f packages/frontend/scripts/spike-viem-sign.ts && grep -E 'privateKeyToAccount|createWalletClient|simulateContract|prepareTransactionRequest' packages/frontend/scripts/spike-viem-sign.ts && grep -E 'underlyingMarket|targetNotional|isLong' packages/frontend/scripts/spike-viem-sign.ts && ! grep -E '0x[0-9a-fA-F]{64}' packages/frontend/scripts/spike-viem-sign.ts && pnpm --filter d2p-frontend exec tsc --noEmit</automated>
  </verify>
  <done>A type-checked `packages/frontend/scripts/spike-viem-sign.ts` exists that reads DEMO_SIGNER_PK from env, builds a viem wallet client against the artifact chain config, and SIMULATES (never broadcasts) `resolveFromMandate` with the source-pinned 5-field HedgeMandate tuple ABI; it type-checks green under the frontend tsconfig; no key is hard-coded or logged. §(c) is now reproducible and CI-type-checked.</done>
</task>

</tasks>

<verification>
- `forge test --no-match-path 'test/**/*[Ff]ork*' --match-path 'test/unit/MacroHedgeExecutor.guard.t.sol'` — all guard tests GREEN, and the mutation check proves the GREEN is non-vacuous (guard removal → call-2 RED).
- `forge test --no-match-path 'test/**/*[Ff]ork*'` — full secret-free suite green (no regression).
- `forge build` — `ProvisionBuildBearDemo.s.sol` with the SKIP_MINT gate compiles.
- `bash -n provision-buildbear-demo.sh` — `--no-mint` variant syntactically valid; the resolveFromMandate receipt-parse is gated behind `!NO_MINT`; MONO_ROOT runtime anchor equals the real root; the DEMO_SIGNER_PK-unset branch fails clearly; all greps pass.
- `pnpm --filter d2p-frontend exec vitest run tests/unit/cornerstone/artifact-null-roundtrip.test.ts` — the null-mintTxHash fixture passes the real exported validateDeployment (no `tsx` needed).
- `pnpm --filter d2p-frontend exec tsc --noEmit` — `packages/frontend/scripts/spike-viem-sign.ts` type-checks green (§(c) reproducible) with the source-pinned HedgeMandate tuple.
- The `"fork used"` string and guard placement (before first dispatch) are exact; the guard NatSpec says "reverts on any resolve attempt once the executor holds legs".
</verification>

<success_criteria>
- EXEC-01 guard inserted before the first `pool.dispatch` with the corrected "any resolve attempt once the executor holds legs" NatSpec; Plan-10-01 guard test GREEN and mutation-proven non-vacuous; full secret-free suite unbroken.
- `SKIP_MINT=true` deploys a fresh executor with `numberOfLegs == 0` (mint + require gated).
- `--no-mint` shell: receipt-parse gated behind `!NO_MINT`, signer funded before snapshot, snapshot-id quote-stripped, MONO_ROOT runtime-asserted, artifact written directly to the frontend path with `mintTxHash: null` + `snapshotId`, unset-signer branch fails clearly, no key leaked.
- The null-mintTxHash fixture round-trips through the exported `validateDeployment` via vitest (`tsx` removed).
- `packages/frontend/scripts/spike-viem-sign.ts` committed and type-checked with the source-pinned 5-field HedgeMandate tuple — §(c) is reproducible and CI-type-checked.
- All work CI-checkable statically (forge + bash -n + vitest validator + tsc). The live fork RUN is Plan 10-03.
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-02-SUMMARY.md`
</output>
</content>

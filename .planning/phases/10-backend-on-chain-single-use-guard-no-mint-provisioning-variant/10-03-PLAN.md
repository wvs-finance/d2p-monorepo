---
phase: 10-backend-on-chain-single-use-guard-no-mint-provisioning-variant
plan: 03
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md
  - packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json
autonomous: false
requirements: [EXEC-01, PROV-01, PROV-02, PROV-03, PROV-04]
must_haves:
  truths:
    - "A freshly-provisioned --no-mint stack on a live BuildBear sandbox shows pool.numberOfLegs(executor) == 0 and a fresh executor address (never the poisoned 0xa95Ffdf…)"
    - "The dedicated DEMO_SIGNER_PK address is funded inside the captured snapshot, and evm_revert restores its gas balance"
    - "The evm_snapshot -> evm_revert round-trip restores legs/collateral/signer-gas so a fresh resolveFromMandate succeeds after revert"
    - "The redeployed GUARDED executor reverts \"fork used\" on the 2nd on-fork resolveFromMandate (cast send)"
    - "buildbear-deployments.json at the frontend path reflects the fresh stack with mintTxHash: null and a snapshotId; the poisoned artifact is retired"
  artifacts:
    - path: ".planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md"
      provides: "Operator-recorded live-fork transcripts for all four sections (a)-(d) + sign-off"
      contains: "fork used"
    - path: "packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json"
      provides: "Fresh --no-mint artifact (mintTxHash: null, snapshotId present, fresh executor)"
      contains: "snapshotId"
  key_links:
    - from: "operator live --no-mint run"
      to: "10-SPIKE-EVIDENCE.md sections (a)-(d)"
      via: "pasted cast call / cast send / cast rpc transcripts"
      pattern: "numberOfLegs"
    - from: "--no-mint run"
      to: "buildbear-deployments.json (frontend path)"
      via: "direct write retiring the poisoned executor 0xa95Ffdf…"
      pattern: "mintTxHash"
---

<objective>
Wave 2 (OPERATOR-MANUAL, NOT CI): run the live BuildBear-fork empirical spike + the actual `--no-mint` provisioning, and record the transcripts that GATE the phase.

This task is `autonomous: false`. It cannot be CI-verified: it requires a live, secret-gated BuildBear sandbox with a 3-day TTL (OPS-06 — explicitly excluded from CI, never claimed on-rhythm/green). Its acceptance is the committed `10-SPIKE-EVIDENCE.md` transcript (all four sections filled) plus the refreshed frontend artifact — NOT a green pipeline.

Closes the operator-manual halves of the requirements the CI lane cannot reach: PROV-01 (fresh `numberOfLegs == 0`), PROV-02 (signer funded inside snapshot, restored by revert), PROV-03 (snapshot round-trip), the EXEC-01 on-fork `"fork used"` proof, and the PROV-04 live artifact write that retires the poisoned committed artifact.

Purpose: prove on a real fork what the unit tests and static checks cannot — the snapshot lifecycle, the dedicated-signer funding-in-snapshot, and the redeployed guarded executor's 2nd-mint revert.
Output: a fully-filled `10-SPIKE-EVIDENCE.md` and a refreshed `buildbear-deployments.json`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-CONTEXT.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-RESEARCH.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-VALIDATION.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md
@.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-02-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run the live --no-mint spike + provisioning, record 10-SPIKE-EVIDENCE.md sections (a)-(d)</name>
  <files>.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md, packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json</files>
  <read_first>
    - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md (the scaffold from Plan 10-01 — fill its four sections)
    - .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-VALIDATION.md (Manual-Only Verifications table — exact cast commands)
    - packages/backend/contracts/script/provision-buildbear-demo.sh (the --no-mint variant delivered in Plan 10-02)
  </read_first>
  <what-built>
    Plan 10-01 + 10-02 delivered and CI-verified: the EXEC-01 guard (`"fork used"`) in `_resolveAndMintAtStrike`, the guard unit test GREEN, the `SKIP_MINT` gate in `ProvisionBuildBearDemo.s.sol`, and the `--no-mint` shell variant (signer-funding-in-snapshot, snapshot-id capture, direct frontend artifact write). The only thing the harness CANNOT do is execute it against a live, secret-gated BuildBear sandbox.
  </what-built>
  <action>
    OPERATOR-MANUAL (no autonomous execution). Perform the live run and paste every transcript into 10-SPIKE-EVIDENCE.md per <how-to-verify>. The harness must NOT attempt to run this against a live fork; it pauses here for the operator and resumes on the signal. Never claim this CI-green (OPS-06).
  </action>
  <how-to-verify>
    PRE-FLIGHT (operator, local shell — NOT CI):
    1. Create a FRESH throwaway BuildBear sandbox in the dashboard (Polygon fork, re-chained to 31337). Set `BUILDBEAR_RPC_URL` (server-only secret). Confirm `BUILDBEAR_DEPLOYER_PK` and the DEDICATED `DEMO_SIGNER_PK` (distinct key) are in gitignored `packages/backend/contracts/.env`.
    2. Confirm the fresh executor address it deploys is NOT the poisoned `0xa95Ffdf51f71fE9C8861Fdbb1cAA664FA78A5FE3` (Pitfall 4 — never reuse the poisoned stack).

    SPIKE + PROVISION (record every transcript into 10-SPIKE-EVIDENCE.md):
    - §(a) Pre-guard 2nd-mint baseline (run BEFORE redeploying the guarded build, or on a separate throwaway): provision a `--no-mint` stack; `cast call <pool> "numberOfLegs(address)(uint256)" <executor> --rpc-url $RPC` → expect `0`; then `cast send <executor> resolveFromMandate(...)` twice; paste the 2nd outcome (the unguarded baseline) + the fresh executor address.
    - §(b) Snapshot round-trip: `cast rpc evm_snapshot` (note one-use); mint once; `cast rpc evm_revert <snapshotId>`; then assert `cast call ... numberOfLegs ...` == `0`, `cast balance <SIGNER_EOA>` == the funded amount, and a fresh `cast send ... resolveFromMandate` SUCCEEDS after the revert. Paste all.
    - §(c) viem server-sign dry-run: a small Node script using `privateKeyToAccount(DEMO_SIGNER_PK)` + `createWalletClient` against the artifact's `rpcUrl`/chainId dry-runs `resolveFromMandate` successfully. Paste the output (Phase 11 dependency).
    - §(d) On-fork post-guard revert: provision/redeploy the GUARDED executor via `./provision-buildbear-demo.sh --no-mint`; `cast send <executor> resolveFromMandate(...)` once (succeeds), then again (must revert `"fork used"`). Paste the revert.

    ARTIFACT: confirm `./provision-buildbear-demo.sh --no-mint` wrote `packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json` with the fresh executor, `mintTxHash: null`, and a `snapshotId`. Run `jq '.mintTxHash, .snapshotId, .executor' <artifact>` and confirm: `null`, a hex string, and an address ≠ `0xa95Ffdf…`. Confirm `git diff` shows the frontend artifact changed (poisoned version retired).

    NEVER paste `DEMO_SIGNER_PK` or `BUILDBEAR_DEPLOYER_PK` into the evidence file — addresses and tx hashes only.
  </how-to-verify>
  <acceptance_criteria>
    Verified at the Plan 10-03 SUMMARY / verify-work gate (transcript-based, not CI):
    - `grep -c 'fork used' .planning/phases/.../10-SPIKE-EVIDENCE.md` >= 1 (§(d) revert recorded)
    - All four sections (a)-(d) have their `[ ]` boxes checked `[x]` and a non-empty transcript block
    - `jq '.mintTxHash' packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json` returns `null`
    - `jq -r '.snapshotId' packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json` matches `^0x[0-9a-fA-F]+$`
    - `jq -r '.executor' packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json` does NOT equal `0xa95Ffdf51f71fE9C8861Fdbb1cAA664FA78A5FE3`
    - `pnpm --filter d2p-frontend exec tsc --noEmit` still green with the new artifact (nullable type from Plan 10-01 accepts it)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/d2p-monorepo && grep -c 'fork used' .planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-SPIKE-EVIDENCE.md && jq -r '.mintTxHash, .snapshotId, .executor' packages/frontend/lib/apps/abrigo/cornerstone/buildbear-deployments.json && pnpm --filter d2p-frontend exec tsc --noEmit</automated>
  </verify>
  <done>All four sections (a)-(d) of 10-SPIKE-EVIDENCE.md are filled with live transcripts (incl. the §(d) `"fork used"` revert); the frontend artifact shows `mintTxHash: null` + a `snapshotId` + a fresh executor ≠ poisoned; tsc green. The phase completion gate is satisfied.</done>
  <resume-signal>Type "spike recorded" once all four sections of 10-SPIKE-EVIDENCE.md are filled and the frontend artifact is refreshed — or describe what blocked the live run (sandbox/TTL/secret).</resume-signal>
</task>

</tasks>

<verification>
- 10-SPIKE-EVIDENCE.md: all four sections (a)-(d) filled with live transcripts; §(d) shows the `"fork used"` revert; §(b) shows the snapshot round-trip restoring legs/collateral/signer-gas.
- buildbear-deployments.json (frontend path): `mintTxHash: null`, `snapshotId` hex, executor ≠ poisoned address.
- `jq` greps + `tsc --noEmit` confirm the artifact is well-formed and type-valid.
- This task is NEVER claimed CI-green (OPS-06) — its proof is the committed transcript.
</verification>

<success_criteria>
- PROV-01/02/03 + EXEC-01-on-fork proven via the recorded live transcripts (operator-manual).
- PROV-04 live artifact written directly to the frontend path with `mintTxHash: null` + `snapshotId`; poisoned committed artifact retired.
- The phase completion gate (`10-SPIKE-EVIDENCE.md` with all four sections) is satisfied for `/gsd:verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-on-chain-single-use-guard-no-mint-provisioning-variant/10-03-SUMMARY.md`
</output>

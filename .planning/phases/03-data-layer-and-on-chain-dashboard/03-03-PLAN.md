---
phase: 03-data-layer-and-on-chain-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/api/status/route.ts
  - app/(dashboard)/status/page.tsx
  - app/(dashboard)/layout.tsx
  - tests/api/status.test.ts
  - tests/e2e/status-page.spec.ts
autonomous: true
requirements: [DASH-08]
# DESCOPED per 03-CONTEXT.md (2026-05-13): the HuggingFace dataset-version line is DROPPED
# from /status (DASH-02/05/06 econometrics descoped). /status reports RPC health + build hash
# + freshness + per-app rollup ONLY. Plan-checker: not missing coverage.

must_haves:
  truths:
    - "A developer or agent hitting /api/status gets JSON with status, build (commit sha), timestamp, a 5-element chains array (per-chain RPC health), and an apps rollup (abrigo: pre-launch)"
    - "A human at /status sees a per-chain RPC health row for all 5 chains, each with a StatusPill (color + icon + text, CROSS-09), plus the build hash and a freshness timestamp"
    - "When one chain's RPC probe fails, /status and /api/status still respond - the failing chain shows 'degraded' while healthy chains show 'healthy' (no all-or-nothing failure)"
    - "The /status page is umbrella-scoped (under (dashboard), NOT under (apps)) and does not use nuqs"
    - "No HuggingFace dataset-version line appears (econometrics descoped)"
  artifacts:
    - path: "app/api/status/route.ts"
      provides: "DASH-08 JSON surface for agents/CI; runtime=nodejs, force-dynamic, build hash, per-chain health"
      contains: "export const runtime = 'nodejs'"
    - path: "app/(dashboard)/status/page.tsx"
      provides: "DASH-08 human-readable RSC status page with per-chain StatusPills"
      min_lines: 25
    - path: "app/(dashboard)/layout.tsx"
      provides: "stale TanStack-QueryClientProvider comment corrected to reflect actual /status use"
      contains: "status"
  key_links:
    - from: "app/api/status/route.ts"
      to: "lib/status/health.ts"
      via: "import checkAllChains"
      pattern: "checkAllChains"
    - from: "app/api/status/route.ts"
      to: "process.env.VERCEL_GIT_COMMIT_SHA"
      via: "build hash field"
      pattern: "VERCEL_GIT_COMMIT_SHA"
    - from: "app/(dashboard)/status/page.tsx"
      to: "lib/status/health.ts"
      via: "checkAllChains() server-side"
      pattern: "checkAllChains"
    - from: "app/(dashboard)/status/page.tsx"
      to: "components/StatusPill.tsx"
      via: "per-chain health pill (color+icon+text)"
      pattern: "StatusPill"
---

<objective>
Build the umbrella-scoped /status surface (DASH-08): a JSON route at /api/status for agents and
CI health checks, and a human-readable RSC page at /status that shows per-chain RPC health,
build hash, freshness timestamp, and a per-app rollup. Correct the stale (dashboard) layout
comment that still mentions a Phase-3 TanStack QueryClientProvider that is no longer needed.

Purpose: Give operators and agents a single honest health surface that degrades per-chain - one
RPC down must not blank the page. This unblocks the Phase 4 agent layer (which can wrap
/api/status) and demonstrates multi-chain readiness for the hackathon.

Output: app/api/status/route.ts, app/(dashboard)/status/page.tsx, a one-line comment fix on
app/(dashboard)/layout.tsx, the filled status unit test, and the filled status e2e spec.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-data-layer-and-on-chain-dashboard/03-CONTEXT.md
@.planning/phases/03-data-layer-and-on-chain-dashboard/03-RESEARCH.md
@.planning/phases/03-data-layer-and-on-chain-dashboard/03-VALIDATION.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md
@.planning/phases/03-data-layer-and-on-chain-dashboard/03-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 03-01 (already built). Use directly - no exploration. -->

From lib/status/health.ts:
```typescript
export interface ChainHealth { chainId: number; name: string; status: 'healthy'|'degraded'; blockNumber?: string; latencyMs?: number; error?: string }
export async function checkChainHealth(chainId: SupportedChainId): Promise<ChainHealth>
export async function checkAllChains(): Promise<ChainHealth[]>  // length 5, Promise.allSettled isolation
```

From app/api/health/route.ts (the build-hash + force-dynamic pattern to mirror):
```typescript
export const dynamic = 'force-dynamic'
export function GET() { return NextResponse.json({ status:'ok', build: process.env.VERCEL_GIT_COMMIT_SHA ?? 'local', runtime:'node', timestamp: new Date().toISOString() }) }
```

From app/(dashboard)/layout.tsx (CURRENT - the comment to correct; pure RSC pass-through, NO nuqs, NO wallet):
```typescript
// Dashboard route group layout - pure RSC shell.
// In Phase 3, this will wrap a client-side TanStack QueryClientProvider.  <-- STALE; correct it
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <div className="min-h-screen">{children}</div>
}
```

From components/StatusPill.tsx (color+icon+text; pass an already-translated label):
```typescript
export function StatusPill({ status, label }: { status: 'PASS'|'FAIL'|'PARKED'|'IN_PROGRESS'; label: string })
// Map RPC health -> pill: healthy -> PASS (CheckCircle2 green); degraded -> FAIL (XCircle red).
```

i18n: getTranslations('dashboard') -> the nested `status` object in messages/{locale}/dashboard.json
(status.page_title, status.page_subtitle, status.rpc_health_heading, status.build_hash_label,
status.freshness_label, status.app_rollup_heading, status.health_healthy, status.health_degraded,
status.app_prelaunch_label).

The /status page is umbrella-scoped under (dashboard) - it does NOT inherit the (apps) NuqsAdapter and MUST NOT use nuqs (RESEARCH Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JSON status route /api/status (DASH-08, agent/CI surface)</name>
  <files>app/api/status/route.ts, tests/api/status.test.ts</files>
  <read_first>
    - app/api/health/route.ts (force-dynamic, VERCEL_GIT_COMMIT_SHA build hash, Response.json shape)
    - lib/status/health.ts (checkAllChains signature + ChainHealth shape - blockNumber is already a string)
    - tests/api/health.test.ts (route-handler unit-test pattern: import GET, await, assert)
    - tests/api/status.test.ts (the test.fixme/it.todo stub from 03-01 to fill)
    - 03-RESEARCH.md Architecture Patterns Pattern 6 (dual surface, allSettled, bigint note), Common Pitfalls P1/P2/P7
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const runtime = 'nodejs'" app/api/status/route.ts` and `grep -q "export const dynamic = 'force-dynamic'" app/api/status/route.ts`
    - `grep -q "checkAllChains" app/api/status/route.ts` and `grep -q "VERCEL_GIT_COMMIT_SHA" app/api/status/route.ts`
    - GET returns body with fields: status, build, timestamp, chains (length 5), apps (with abrigo)
    - No HuggingFace/dataset-version field anywhere in the route (grep -i "huggingface\|parquet\|dataset" returns nothing)
    - tests/api/status.test.ts has no `it.todo`/`test.fixme`; it imports GET and asserts 200 + the field set + chains length 5; it mocks one failing probe and asserts the response still resolves (no throw, status reflects degraded)
    - `pnpm biome check` exits 0; `pnpm tsc --noEmit` exits 0; `pnpm vitest run tests/api/status.test.ts` passes
  </acceptance_criteria>
  <action>
    Create app/api/status/route.ts:
    - `export const dynamic = 'force-dynamic'`; `export const runtime = 'nodejs'` (viem -> nodejs, Pitfall 1).
    - import { checkAllChains } from '@/lib/status/health'.
    - `export async function GET()`: `const chains = await checkAllChains()` (already isolates per-chain failure via allSettled, blockNumber already string - Pitfall 2 handled in the lib). Return:
      ```typescript
      return Response.json({
        status: chains.every(c => c.status === 'healthy') ? 'ok' : 'degraded',
        build: process.env.VERCEL_GIT_COMMIT_SHA ?? 'local', // server-only, Pitfall 7
        timestamp: new Date().toISOString(),                  // freshness
        chains,
        apps: { abrigo: { status: 'pre-launch', instrumentsDeployed: 0 } }, // per-app rollup
      })
      ```
      No HuggingFace dataset-version line (descoped).

    Fill tests/api/status.test.ts (convert stub -> real):
    - import { GET } from '@/app/api/status/route'; describe/it (vitest).
    - it('returns 200 with status/build/timestamp/chains[5]/apps'): await GET(); expect 200; body.chains length 5; body.apps.abrigo present; expect(() => JSON.stringify(body)).not.toThrow().
    - it('isolates a single failing RPC probe - response still resolves'): vi.mock or vi.spyOn the aggregator's publicClients (imported by health.ts) so one chain's getBlockNumber rejects; await GET(); expect 200; expect the mocked chain entry status 'degraded'; overall body.status 'degraded'; the call did not throw.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/frontend && pnpm biome check app/api/status/route.ts tests/api/status.test.ts && NODE_OPTIONS=--max-old-space-size=4096 pnpm tsc --noEmit && grep -q "runtime = 'nodejs'" app/api/status/route.ts && grep -q "checkAllChains" app/api/status/route.ts && ! grep -iq "huggingface\|parquet" app/api/status/route.ts && pnpm vitest run tests/api/status.test.ts</automated>
  </verify>
  <done>
    /api/status exists, passes biome + tsc, runtime nodejs + force-dynamic, calls checkAllChains, returns the 5-field body with no HuggingFace line. The unit test is real (no todo) and green, including the single-failing-probe isolation case.
  </done>
</task>

<task type="auto">
  <name>Task 2: /status RSC page + correct stale (dashboard) layout comment (DASH-08 human surface)</name>
  <files>app/(dashboard)/status/page.tsx, app/(dashboard)/layout.tsx, tests/e2e/status-page.spec.ts</files>
  <read_first>
    - app/(dashboard)/layout.tsx (the stale comment to correct - keep it a pure RSC pass-through; no nuqs, no TanStack)
    - lib/status/health.ts (checkAllChains for the page's server-side read)
    - components/StatusPill.tsx (color+icon+text; healthy->PASS, degraded->FAIL)
    - app/(apps)/apps/abrigo/page.tsx (page style: max-w container, getLocale/getTranslations, section headings, tokens)
    - 02-UI-SPEC.md Page-Level Layout Contracts (container, divide-y rows over grids), Color (accent reserved list), Accessibility Contract (heading hierarchy, landmarks), Impeccable Anti-Pattern Checklist
    - tests/e2e/status-page.spec.ts (the fixme stub from 03-01 to fill); tests/e2e/homepage-content.spec.ts (Playwright + NEXT_LOCALE cookie convention)
    - 03-RESEARCH.md Open Question 2 ((dashboard) repurposing -> /status here)
  </read_first>
  <acceptance_criteria>
    - File app/(dashboard)/status/page.tsx exists; first non-comment line is NOT `'use client'` (RSC)
    - `grep -q "checkAllChains" "app/(dashboard)/status/page.tsx"` and `grep -q "StatusPill" "app/(dashboard)/status/page.tsx"`
    - No `from 'nuqs'`, no `from 'wagmi'`, no wallet hooks in the status page or layout (umbrella-scoped, no client URL state)
    - app/(dashboard)/layout.tsx no longer claims "TanStack QueryClientProvider"; the comment references the actual /status use; layout remains a pass-through
    - The page renders all 5 chains, each wrapped in a StatusPill (healthy->PASS, degraded->FAIL), plus build hash and freshness timestamp; no HuggingFace line
    - tests/e2e/status-page.spec.ts has no `test.fixme`; it asserts /status returns 200, shows 5 per-chain rows with status pills (assert pill icon/aria-label, color+icon+text), shows build hash + freshness; and that a mocked-degraded chain shows degraded while others stay healthy (or, if network mocking is impractical e2e, assert all-5-present + at-least-one-healthy and record the degradation case as covered by the unit test)
    - `pnpm biome check` exits 0; `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <action>
    ONE comment fix + ONE page + fill ONE e2e spec.

    Correct app/(dashboard)/layout.tsx comment: replace the "In Phase 3, this will wrap a client-side TanStack QueryClientProvider" line with: "Phase 3 hosts the umbrella /status page here. Server-side reads use viem directly (lib/status/health.ts); no client QueryClientProvider is needed - this stays a pure RSC pass-through." Keep the function body unchanged (`<div className="min-h-screen">{children}</div>`).

    Create app/(dashboard)/status/page.tsx (RSC):
    - NO 'use client'. `export const runtime = 'nodejs'`; `export const dynamic = 'force-dynamic'` (live probes).
    - `const chains = await checkAllChains(); const t = await getTranslations('dashboard'); const locale = await getLocale()`.
    - `<main className="max-w-[1200px] mx-auto px-4 sm:px-6 lg:px-8 py-12 space-y-8">`:
      - `<h1>` t('status.page_title'); `<p>` t('status.page_subtitle').
      - Section `status.rpc_health_heading`: a `divide-y divide-border-default` list (NOT a grid - equal weight per chain). Each row: chain name + `<StatusPill status={c.status === 'healthy' ? 'PASS' : 'FAIL'} label={c.status === 'healthy' ? t('status.health_healthy') : t('status.health_degraded')} />` + (when present) `text-xs font-mono text-text-muted` block number + latencyMs. CROSS-09: the pill carries color + icon + text; do not rely on color alone.
      - A line for build hash: t('status.build_hash_label') + `process.env.VERCEL_GIT_COMMIT_SHA ?? 'local'` in font-mono.
      - A line for freshness: t('status.freshness_label') + a locale-aware timestamp (reuse lib/format if present; else new Date().toISOString()).
      - Section `status.app_rollup_heading`: one row for Abrigo with a StatusPill (IN_PROGRESS, label t('status.app_prelaunch_label')) and "0 instruments deployed".
      - NO HuggingFace dataset-version line (descoped).
    - `generateMetadata` title "Estado / Status - DS2P Labs".
    - Tokens only: no accent fill on rows, no card-in-card, no left-colored borders.

    Fill tests/e2e/status-page.spec.ts (convert fixme -> real): goto('/status'); expect 200; expect 5 per-chain rows (locator count); for each, assert a status pill is present (assert the `<output>` StatusPill node and its icon - color+icon+text, CROSS-09); assert build hash label + freshness label visible. For the degradation case, prefer asserting "all 5 present and the page rendered without error even if a chain is degraded"; the strict single-probe-failure isolation is owned by the unit test in Task 1.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/frontend && pnpm biome check "app/(dashboard)/layout.tsx" "app/(dashboard)/status/page.tsx" tests/e2e/status-page.spec.ts && NODE_OPTIONS=--max-old-space-size=4096 pnpm tsc --noEmit && grep -q "checkAllChains" "app/(dashboard)/status/page.tsx" && grep -q "StatusPill" "app/(dashboard)/status/page.tsx" && ! grep -q "from 'nuqs'" "app/(dashboard)/status/page.tsx" && ! grep -q "TanStack" "app/(dashboard)/layout.tsx" && ! grep -rq "test.fixme" tests/e2e/status-page.spec.ts && pnpm build && (pnpm start -p 3040 &) && sleep 6 && PLAYWRIGHT_TEST_BASE_URL=http://localhost:3040 pnpm playwright test tests/e2e/status-page.spec.ts; pkill -f "next start" || true</automated>
  </verify>
  <done>
    /status RSC page renders 5 per-chain health rows with StatusPills (color+icon+text), build hash, and freshness; (dashboard) layout comment corrected (no TanStack claim); no nuqs/wallet imports; no HuggingFace line. e2e spec real and green against the production build.
  </done>
</task>

</tasks>

<verification>
- `pnpm build && pnpm start` serves /status and /api/status - confirms route-segment config honored under the production (webpack) build (Pitfall 5).
- `pnpm test:quick` green; status unit test + status e2e green.
- `curl -s http://localhost:3040/api/status` returns JSON with a 5-element chains array, build, timestamp, apps.abrigo - and no dataset-version field.
- Per-chain degradation: the page/route still respond when one probe fails (covered by the unit test isolation case).
</verification>

<success_criteria>
- DASH-08: /api/status (agents/CI) and /status (humans) both report per-chain RPC health for all 5 chains, build hash, freshness, and the Abrigo pre-launch rollup.
- Graceful per-chain degradation: one RPC down shows that chain 'degraded' while others stay 'healthy' - no all-or-nothing blanking.
- /status is umbrella-scoped (under (dashboard)), uses no nuqs, and the stale layout comment is corrected.
- No HuggingFace dataset-version line (econometrics descoped).
</success_criteria>

<live_verification>
MANDATORY per CLAUDE.md - this plan produces rendered route surfaces (/status, /api/status). After the Task 2 commit lands, launch the Evidence Collector:

```
Agent(
  subagent_type="Evidence Collector",
  description="Live verify task 03-03",
  prompt="""
    Working dir: /home/jmsbpp/apps/d2p/frontend
    Target: https://www.d2pfinance.xyz (fall back to local `pnpm build && pnpm start` on port 3040 if the route 4xx/5xx)
    Use the mcp__plugin_playwright_playwright__* tools.

    Task just completed: 03-03 - umbrella /status surface (JSON route + RSC page, per-chain RPC health)
    Routes affected: /status, /api/status
    Plan claims to verify:
      - /status shows a per-chain RPC health row for ALL 5 chains (celo, ethereum, base, arbitrum, optimism)
      - Each chain row has a StatusPill encoding color + ICON + TEXT (CROSS-09) - read the icon node's aria-label, do not accept color-only
      - The build hash and a freshness timestamp are present on the page
      - A per-app rollup row for Abrigo shows a pre-launch pill and '0 instruments deployed'
      - NO HuggingFace / dataset-version line is present (econometrics descoped)
      - /api/status returns 200 JSON with status, build, timestamp, chains[5], apps.abrigo - no serialization error
      - The page responds even if a chain is degraded (no all-or-nothing blank)
      - Tokens: muted ochre accent only, IBM Plex, no card-in-card, no left-colored borders

    For each affected route: browser_navigate, browser_snapshot (assert each claim against DOM),
    browser_console_messages (surface runtime errors), browser_take_screenshot to /tmp/d2p-verify/03-03-{slug}.png,
    browser_evaluate to read each StatusPill icon aria-label and confirm all 5 chains render. Default to skepticism;
    screenshots required.

    Output: append a section to .planning/phases/03-data-layer-and-on-chain-dashboard/03-LIVE-VERIFICATION.md
    with verdicts per claim (PASS/PARTIAL/FAIL/UNREACHABLE) and screenshot paths. Return short text summary.
  """
)
```

A task is NOT complete until every claim is PASS or has an explicit waiver recorded in 03-LIVE-VERIFICATION.md.
</live_verification>

<output>
After completion, create `.planning/phases/03-data-layer-and-on-chain-dashboard/03-03-SUMMARY.md`.
</output>

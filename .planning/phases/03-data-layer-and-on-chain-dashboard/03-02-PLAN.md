---
phase: 03-data-layer-and-on-chain-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/api/dashboard/route.ts
  - app/(apps)/apps/abrigo/dashboard/page.tsx
  - app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx
  - app/(apps)/apps/abrigo/dashboard/DashboardContent.tsx
  - app/(apps)/apps/abrigo/page.tsx
  - messages/es-CO/dashboard.json
  - messages/en/dashboard.json
  - playwright.config.ts
  - tests/api/dashboard.test.ts
  - tests/e2e/api-dashboard.spec.ts
  - tests/e2e/dashboard-page.spec.ts
  - tests/e2e/dashboard-chain-selector.spec.ts
  - tests/e2e/dashboard-no-js.spec.ts
autonomous: true
requirements: [DASH-01, DASH-03, DASH-04, DASH-07]
# DESCOPED per 03-CONTEXT.md (2026-05-13): DASH-02/05/06 (HuggingFace econometrics route,
# visx beta/CI charts, chart a11y) are NOT in this phase. The dashboard renders on-chain
# protocol state ONLY. No charts, no econometrics. Plan-checker: not missing coverage.

must_haves:
  truths:
    - "A visitor at /apps/abrigo/dashboard with no wallet and no JS sees labelled metric tiles (pool balance, settlement events, LP positions, last block synced) per chain on first paint"
    - "Because ABRIGO_INSTRUMENTS is empty, every tile value renders as the dashed placeholder '-' and a 'Live once contracts deploy' banner is present - NO fabricated numbers (anti-fishing)"
    - "Switching chains in the selector updates the URL to ?chain=<slug>; pasting that URL into a new tab restores the same selected chain"
    - "GET /api/dashboard?app=abrigo returns 200 with a version:1 envelope + chains array (5 entries) and no bigint values; one degraded RPC does not 500 the response; ?app=unknown returns 404"
    - "The dashboard page is a Server Component (no 'use client' at page top); only ChainSelector is the hydrated client island, and it reuses the SHARED dashboardSearchParams.chain parser (no inline parseAsStringEnum)"
    - "pnpm build exits 0 with the new routes added AND the 3 existing force-static routes (llms.txt, mcp.json, openapi.yaml) still appear as ○ (Static) — no cacheComponents regression"
    - "The /apps/abrigo overview page 'Live dashboard' teaser now links to /apps/abrigo/dashboard"
  artifacts:
    - path: "app/api/dashboard/route.ts"
      provides: "DASH-01 BFF aggregation route: runtime=nodejs, force-dynamic, calls aggregateAllChains() directly (NO 'use cache'), returns the version:1 DashboardResponse envelope"
      contains: "export const runtime = 'nodejs'"
    - path: "app/(apps)/apps/abrigo/dashboard/page.tsx"
      provides: "DASH-03/07 RSC dashboard page reading nuqs chain param + aggregator directly"
      min_lines: 25
    - path: "app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx"
      provides: "DASH-04 client island; only hydrated component; reuses dashboardSearchParams.chain"
      contains: "'use client'"
    - path: "app/(apps)/apps/abrigo/dashboard/DashboardContent.tsx"
      provides: "the single render path: skeleton when empty, live tiles when populated"
      min_lines: 30
  key_links:
    - from: "app/api/dashboard/route.ts"
      to: "lib/dashboard/aggregator.ts"
      via: "import aggregateAllChains and call it directly in GET (no 'use cache' helper)"
      pattern: "aggregateAllChains"
    - from: "app/api/dashboard/route.ts"
      to: "lib/dashboard/contract.ts"
      via: "import DashboardResponse for the typed envelope"
      pattern: "DashboardResponse"
    - from: "app/(apps)/apps/abrigo/dashboard/page.tsx"
      to: "lib/dashboard/search-params.ts"
      via: "loadDashboardParams(searchParams)"
      pattern: "loadDashboardParams"
    - from: "app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx"
      to: "lib/dashboard/search-params.ts"
      via: "useQueryState('chain', dashboardSearchParams.chain)"
      pattern: "dashboardSearchParams.chain"
    - from: "app/(apps)/apps/abrigo/page.tsx"
      to: "/apps/abrigo/dashboard"
      via: "Link in the Live dashboard teaser section"
      pattern: "/apps/abrigo/dashboard"
---

<objective>
Build the Abrigo on-chain dashboard vertical slice: the BFF aggregation route (DASH-01), the
RSC dashboard page (DASH-03), the nuqs chain selector (DASH-04), the no-JS read-first first
paint (DASH-07), and wire the existing overview-page teaser to the new route.

Purpose: Give visitors and hackathon judges a real, honest dashboard that shows WHAT will be
displayed per chain without fabricating any numbers - the empty instrument registry drives a
labelled skeleton through the exact same render path that will later show live data.

NOTE — no caching this phase (B1): the route calls aggregateAllChains() directly (NO 'use cache'
helper) and next.config.ts has NO cacheComponents. Caching is deferred to the contracts-deploy
phase per 03-CONTEXT deferred items — there is nothing to cache while the registry is empty, and
cacheComponents would break the existing force-static/force-dynamic routes. force-dynamic +
runtime=nodejs stay on both the route and the page.

Output: app/api/dashboard/route.ts, the dashboard page + DashboardContent + ChainSelector
under (apps), a one-line link wire on the overview page, two added dashboard.json keys, the
playwright webServer config, and the 4 filled e2e specs + the filled route-handler unit test.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-data-layer-and-on-chain-dashboard/03-CONTEXT.md
@.planning/phases/03-data-layer-and-on-chain-dashboard/03-RESEARCH.md
@.planning/phases/03-data-layer-and-on-chain-dashboard/03-VALIDATION.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md
@.planning/phases/03-data-layer-and-on-chain-dashboard/03-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 03-01 (already built). Use directly - no exploration. -->

From lib/dashboard/aggregator.ts:
```typescript
export interface InstrumentState { id; name; nameEn; address: string; poolBalance: string|null; settlementCount: string|null; lpPositionCount: string|null }
export interface ChainAggregationResult { chainId: number; chainName: string; status: 'healthy'|'degraded'|'empty'; instruments: InstrumentState[]; lastBlockSynced: string|null; error?: string; fetchedAt: string }
export async function aggregateAllChains(): Promise<ChainAggregationResult[]>  // length 5; empty path short-circuits before any viem client
```

From lib/dashboard/contract.ts (shared envelopes — import these; do NOT redefine inline):
```typescript
export interface DashboardResponse { version: 1; app: string; status: 'ok'|'degraded'; chains: ChainAggregationResult[]; fetchedAt: string }
export interface StatusResponse { version: 1; status: 'ok'|'degraded'; build: string; timestamp: string; chains: ChainHealth[]; apps: Record<string, { status: string; instrumentsDeployed: number }> }
```

From lib/dashboard/search-params.ts (the SINGLE chain parser — ChainSelector MUST reuse dashboardSearchParams.chain, never re-declare parseAsStringEnum):
```typescript
export const CHAIN_SLUGS = ['celo','ethereum','base','arbitrum','optimism'] as const
export type ChainSlug = (typeof CHAIN_SLUGS)[number]
export const dashboardSearchParams  // { chain: parseAsStringEnum(...).withDefault('celo') }
export const loadDashboardParams     // createLoader - call: await loadDashboardParams(searchParams)
export const CHAIN_SLUG_TO_ID: Record<ChainSlug, SupportedChainId>  // derived via satisfies
```

From lib/apps/abrigo/instruments.ts:
```typescript
export const ABRIGO_INSTRUMENTS: AbrigoInstrument[]  // empty at launch
```

From components/StatusPill.tsx (reuse for empty/coming-soon affordances; pass already-translated label):
```typescript
export function StatusPill({ status, label, className }: { status: 'PASS'|'FAIL'|'PARKED'|'IN_PROGRESS'; label: string; className?: string })
```

i18n: getTranslations('dashboard') resolves keys from messages/{locale}/dashboard.json
(page_title, page_subtitle, live_banner, chain_selector_label, tile_pool_balance,
tile_settlement_events, tile_lp_positions, tile_last_block, empty_value). This plan ADDS two
keys (open_dashboard, open_dashboard_aria) for the overview teaser link — the i18n-coverage
recursive parity test from 03-01 will assert es-CO/en parity automatically.

NuqsAdapter is already in app/(apps)/layout.tsx - the dashboard inherits it (RESEARCH Pitfall 6). Do NOT add another adapter.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BFF aggregation route /api/dashboard (DASH-01) — direct call, no 'use cache'</name>
  <files>app/api/dashboard/route.ts</files>
  <read_first>
    - app/api/health/route.ts (the force-dynamic + build-hash route-handler style to mirror)
    - lib/dashboard/aggregator.ts (aggregateAllChains signature + ChainAggregationResult shape)
    - lib/dashboard/contract.ts (the DashboardResponse envelope to return)
    - 03-RESEARCH.md Architecture Patterns Pattern 6 (Response.json shape, allSettled, bigint note)
    - 03-RESEARCH.md Common Pitfalls P1 (runtime=nodejs), P2 (bigint), P5 (Turbopack force-dynamic; verified by the pnpm build gate in Task 2)
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const runtime = 'nodejs'" app/api/dashboard/route.ts`
    - `grep -q "export const dynamic = 'force-dynamic'" app/api/dashboard/route.ts`
    - `grep -q "aggregateAllChains" app/api/dashboard/route.ts`
    - The route does NOT contain `'use cache'` and does NOT import `cacheLife` (caching deferred — B1): `! grep -q "use cache" app/api/dashboard/route.ts` and `! grep -q "cacheLife" app/api/dashboard/route.ts`
    - The route imports and returns the shared `DashboardResponse` envelope with `version: 1`: `grep -q "version: 1" app/api/dashboard/route.ts`
    - GET with `?app=unknown` returns 404 (RC m4)
    - `pnpm biome check app/api/dashboard/route.ts` exits 0; `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <action>
    Create app/api/dashboard/route.ts:
    - `export const dynamic = 'force-dynamic'` (live reads, never prerendered - Pitfall 5).
    - `export const runtime = 'nodejs'` (viem breaks on edge - Pitfall 1; must be explicit).
    - import { aggregateAllChains } from '@/lib/dashboard/aggregator'; import type { DashboardResponse } from '@/lib/dashboard/contract'.
    - `export async function GET(request: Request)`: parse `const { searchParams } = new URL(request.url)`; `const app = searchParams.get('app') ?? 'abrigo'`. Today only 'abrigo' is supported — `if (app !== 'abrigo') return Response.json({ error: 'unknown app' }, { status: 404 })` (RC m4). Otherwise:
      ```typescript
      const chains = await aggregateAllChains()
      const body: DashboardResponse = {
        version: 1,
        app,
        status: chains.every((c) => c.status !== 'degraded') ? 'ok' : 'degraded',
        chains,
        fetchedAt: new Date().toISOString(),
      }
      return Response.json(body)
      ```
    - NO `'use cache'` helper, NO `cacheLife` import. Call aggregateAllChains() DIRECTLY in the handler. Add a one-line comment: "Caching deferred to the contracts-deploy phase (03-CONTEXT deferred items) — nothing to cache while the registry is empty; cacheComponents stays off to keep the existing force-static routes working (B1)."
    - The aggregator already string-converts all bigint (lib/chains/serialize.ts), so Response.json is safe — add a one-line comment "all numeric fields are pre-stringified by the aggregator (Pitfall 2)".
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/frontend && pnpm biome check app/api/dashboard/route.ts && NODE_OPTIONS=--max-old-space-size=4096 pnpm tsc --noEmit && grep -q "runtime = 'nodejs'" app/api/dashboard/route.ts && grep -q "aggregateAllChains" app/api/dashboard/route.ts && grep -q "version: 1" app/api/dashboard/route.ts && ! grep -q "use cache" app/api/dashboard/route.ts && ! grep -q "cacheLife" app/api/dashboard/route.ts</automated>
  </verify>
  <done>
    Route file exists, passes biome + tsc. runtime='nodejs', dynamic='force-dynamic', aggregateAllChains called directly (no 'use cache', no cacheLife), returns the version:1 DashboardResponse envelope, and ?app=unknown returns 404.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page + DashboardContent + ChainSelector (DASH-03/04/07) + production build gate</name>
  <files>app/(apps)/apps/abrigo/dashboard/page.tsx, app/(apps)/apps/abrigo/dashboard/DashboardContent.tsx, app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx</files>
  <read_first>
    - app/(apps)/layout.tsx (NuqsAdapter already wraps - do not add another)
    - app/(apps)/apps/abrigo/page.tsx (sibling page style: max-w container, getLocale/getTranslations, section headings, token classes)
    - lib/dashboard/aggregator.ts + lib/dashboard/search-params.ts (the data + param contracts)
    - 02-UI-SPEC.md Page-Level Layout Contracts (max-w-[1200px] container, divide-y over grids for epistemic equality), Color (accent reserved list - do NOT use accent for tiles), Impeccable Anti-Pattern Enforcement Checklist
    - 03-RESEARCH.md Architecture Patterns Pattern 5 (RSC reads param via loadDashboardParams; ChainSelector is the only client island)
    - 03-RESEARCH.md Common Pitfalls P6 (NuqsAdapter scope), P8 (single render path - no separate coming-soon hardcode)
    - components/StatusPill.tsx
  </read_first>
  <acceptance_criteria>
    - File app/(apps)/apps/abrigo/dashboard/page.tsx exists; its first non-comment line is NOT `'use client'` (page is RSC)
    - `grep -q "loadDashboardParams" "app/(apps)/apps/abrigo/dashboard/page.tsx"`
    - `grep -q "aggregateAllChains" "app/(apps)/apps/abrigo/dashboard/page.tsx"`
    - `grep -q "export const runtime = 'nodejs'" "app/(apps)/apps/abrigo/dashboard/page.tsx"`
    - `grep -q "'use client'" "app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx"` AND `grep -q "useQueryState" "app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx"`
    - ChainSelector reuses the SHARED parser (M-nuqs): `grep -q "dashboardSearchParams.chain" "app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx"` AND `! grep -q "parseAsStringEnum" "app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx"`
    - No `from 'wagmi'` and no `useAccount`/`useConnect` in any of the three files (no wallet on the dashboard)
    - DashboardContent.tsx renders the live_banner string and the empty_value placeholder; grep shows both label props referenced
    - `pnpm biome check` exits 0 on all three files; `pnpm tsc --noEmit` exits 0
    - PRODUCTION BUILD GATE (B1, Reality Checker): `pnpm build` exits 0; the route table still lists `/llms.txt`, `/.well-known/mcp.json`, `/.well-known/openapi.yaml` as ○ (Static) and does NOT error on `export const dynamic` for any existing route
  </acceptance_criteria>
  <action>
    Create THREE files. The page is a Server Component; ChainSelector is the ONLY 'use client'. Then run the production build gate.

    app/(apps)/apps/abrigo/dashboard/page.tsx (RSC - DASH-03/07):
    - NO 'use client'. `export const runtime = 'nodejs'` and `export const dynamic = 'force-dynamic'` (DASH-07 read-first; viem on this path -> nodejs).
    - Signature: `export default async function DashboardPage({ searchParams }: { searchParams: Promise<Record<string,string|string[]|undefined>> })`.
    - `const { chain } = await loadDashboardParams(searchParams)` (DASH-04 server read).
    - `const data = await aggregateAllChains()` (direct lib call - the page does NOT fetch its own /api route; same data, RSC-rendered, no client fetch -> satisfies no-JS first paint DASH-07).
    - Add a comment (BA M2): "The page calls aggregateAllChains() directly for the no-JS first paint; /api/dashboard serves agents/CI. Phase-4 TODO: once caching is real, both must read the same cached aggregation so the page and the route never diverge."
    - `const t = await getTranslations('dashboard'); const locale = await getLocale()`.
    - Render `<main className="max-w-[1200px] mx-auto px-4 sm:px-6 lg:px-8 py-12 space-y-8">` with: an `<h1>` (t('page_title')), a `<p>` subtitle (t('page_subtitle')), the `<ChainSelector label={t('chain_selector_label')} />` island, and `<DashboardContent data={data} selectedChain={chain} labels={{ poolBalance: t('tile_pool_balance'), settlementEvents: t('tile_settlement_events'), lpPositions: t('tile_lp_positions'), lastBlock: t('tile_last_block'), liveBanner: t('live_banner'), emptyValue: t('empty_value') }} />`.
    - Add `generateMetadata` returning title "Panel - Abrigo / DS2P Labs" (mirror existing pages' metadata style).

    app/(apps)/apps/abrigo/dashboard/DashboardContent.tsx (RSC presentational - the single render path, Pitfall 8):
    - Props: `{ data: ChainAggregationResult[]; selectedChain: ChainSlug; labels: { poolBalance: string; settlementEvents: string; lpPositions: string; lastBlock: string; liveBanner: string; emptyValue: string } }`. import type from '@/lib/dashboard/aggregator' and '@/lib/dashboard/search-params'.
    - Render the "Live once contracts deploy" banner (labels.liveBanner) when any chain has status 'empty' OR no chain has any instruments. Today the registry is empty so the banner always shows - this is the honest pre-launch state. Style: a bordered notice `border border-border-default rounded-lg bg-bg-surface p-4 text-text-secondary` with an inline lucide icon (e.g. Info) - icon+text, not color-only.
    - Resolve the selected chain via CHAIN_SLUG_TO_ID[selectedChain]; render that chain's 4 metric tiles prominently. Other chains may render in a secondary list below at EQUAL visual weight (no chain is bolder/larger than another - anti-fishing). Claude's discretion on exact layout within tokens.
    - 4 tiles per chain: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`, each tile `border border-border-default rounded-lg p-4` (NO card-in-card, NO accent fill, NO left-colored border). Tile = label (`text-sm text-text-muted`) over value (`text-2xl font-mono text-text-primary`).
    - Value rule (anti-fishing, CRITICAL): when the instrument field is a non-null number string, render it; otherwise render labels.emptyValue ("-"). With the empty registry every tile shows "-". NEVER fabricate a number; do NOT render "0" for a missing instrument (a real future "0" balance is distinct from "-" no-data). lastBlock tile uses chain.lastBlockSynced (string|null) -> "-" when null.

    app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx (DASH-04 - ONLY client island; SINGLE parser, M-nuqs):
    - `'use client'`. import { useQueryState } from 'nuqs'; import { CHAIN_SLUGS, dashboardSearchParams } from '@/lib/dashboard/search-params'.
    - Props: `{ label: string }`.
    - `const [chain, setChain] = useQueryState('chain', dashboardSearchParams.chain)` — REUSE the shared parser (M-nuqs). Do NOT re-declare `parseAsStringEnum(...)` inline; one parser, one default, no server/client URL-contract drift.
    - Render a labelled native `<select>` (keyboard-accessible by default) with the 5 CHAIN_SLUGS options; onChange calls `setChain(e.target.value as ChainSlug)`. Associate the visible label via `<label htmlFor>` using the `label` prop; `focus-visible:ring-2 focus-visible:ring-accent-default`, `min-h-[44px]`.
    - No wallet hooks, no wagmi import.

    Confirm the path resolves to /apps/abrigo/dashboard (file at app/(apps)/apps/abrigo/dashboard/page.tsx).

    PRODUCTION BUILD GATE (B1): after the three files typecheck, run `pnpm build`. It MUST exit 0. Inspect the printed route table: `/llms.txt`, `/.well-known/mcp.json`, `/.well-known/openapi.yaml` must still be marked ○ (Static) and `/api/health` ƒ (Dynamic); no "export const dynamic is not allowed" / cacheComponents error may appear. This is the gate that proves the foundation (no cacheComponents) did not break the existing force-static/force-dynamic routes — the single change Reality Checker flagged as most likely to break prod.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/frontend && pnpm biome check "app/(apps)/apps/abrigo/dashboard/page.tsx" "app/(apps)/apps/abrigo/dashboard/DashboardContent.tsx" "app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx" && NODE_OPTIONS=--max-old-space-size=4096 pnpm tsc --noEmit && grep -q "useQueryState" "app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx" && grep -q "dashboardSearchParams.chain" "app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx" && ! grep -q "parseAsStringEnum" "app/(apps)/apps/abrigo/dashboard/ChainSelector.tsx" && grep -q "loadDashboardParams" "app/(apps)/apps/abrigo/dashboard/page.tsx" && ! grep -rq "from 'wagmi'" "app/(apps)/apps/abrigo/dashboard/" && pnpm build 2>&1 | tee /tmp/d2p-03-02-build.log && grep -Eq "llms\.txt.*(○|Static)" /tmp/d2p-03-02-build.log</automated>
  </verify>
  <done>
    Three files exist and pass biome + tsc. Page is RSC (no 'use client' at top), reads nuqs param server-side and calls aggregateAllChains; ChainSelector is the only client island, reuses dashboardSearchParams.chain (no inline parser); no wallet imports anywhere; DashboardContent shows "-" for every tile and the live banner with the empty registry. `pnpm build` exits 0 and the 3 existing force-static routes remain Static in the route table (no cacheComponents regression).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire overview teaser + add 2 i18n keys + fill the route-handler unit test + fill the 4 dashboard e2e specs (deterministic webServer)</name>
  <files>app/(apps)/apps/abrigo/page.tsx, messages/es-CO/dashboard.json, messages/en/dashboard.json, playwright.config.ts, tests/api/dashboard.test.ts, tests/e2e/api-dashboard.spec.ts, tests/e2e/dashboard-page.spec.ts, tests/e2e/dashboard-chain-selector.spec.ts, tests/e2e/dashboard-no-js.spec.ts</files>
  <read_first>
    - app/(apps)/apps/abrigo/page.tsx (the "Live dashboard" teaser section - currently a non-link with StatusPill)
    - messages/es-CO/dashboard.json, messages/en/dashboard.json (add the two teaser-link keys; the i18n-coverage recursive parity test from 03-01 enforces es-CO/en parity)
    - playwright.config.ts (current config — add a `webServer` block; check whether one already exists)
    - tests/api/dashboard.test.ts (the it.todo stub from 03-01 to fill — route-handler unit test; follow tests/api/health.test.ts: import GET, await, assert)
    - tests/api/health.test.ts (route-handler unit-test pattern)
    - tests/e2e/homepage-content.spec.ts (Playwright goto + locator + NEXT_LOCALE cookie pattern)
    - tests/e2e/api-dashboard.spec.ts, dashboard-page.spec.ts, dashboard-chain-selector.spec.ts, dashboard-no-js.spec.ts (the test.fixme stubs from 03-01 to convert to real tests)
    - 03-VALIDATION.md Per-Task Verification Map (the exact assertions each spec owns)
  </read_first>
  <acceptance_criteria>
    - `grep -q "/apps/abrigo/dashboard" "app/(apps)/apps/abrigo/page.tsx"` (teaser now links to the route)
    - The two new i18n keys (open_dashboard, open_dashboard_aria) exist in BOTH dashboard.json files; the i18n-coverage suite stays green (recursive parity)
    - playwright.config.ts has a `webServer` block (command + url + reuseExistingServer + timeout) — NOT a `sleep`/`pkill` shell pattern (M-webserver)
    - tests/api/dashboard.test.ts has no `it.todo`; it imports GET and asserts 200 + version:1 + chains length 5 + JSON.stringify(body) does not throw; `?app=unknown` → 404; a mocked degraded chain still returns 200 (not 500)
    - None of the 4 e2e specs contains `test.fixme(` any longer (all converted to real `test(`)
    - api-dashboard.spec.ts asserts GET /api/dashboard?app=abrigo returns 200, body.chains length 5, no bigint, and ?app=unknown returns 404
    - dashboard-page.spec.ts asserts /apps/abrigo/dashboard renders the live banner text and at least 4 tile labels, with tile values equal to the dashed placeholder (and NO numeric digit)
    - dashboard-chain-selector.spec.ts asserts choosing a chain updates the URL ?chain= and reload restores it
    - dashboard-no-js.spec.ts uses `test.use({ javaScriptEnabled: false })` and asserts the banner + tiles are present on first paint
    - `pnpm biome check` exits 0 on all touched files; the e2e suite passes against the playwright `webServer` (production build), and `pnpm vitest run tests/api/dashboard.test.ts` is green
  </acceptance_criteria>
  <action>
    ONE page edit + 2 i18n keys + playwright webServer + fill 1 unit test + 4 e2e conversions.

    Edit app/(apps)/apps/abrigo/page.tsx: in the "Live dashboard / Panel en vivo" section, change the StatusPill-only teaser into a real link to `/apps/abrigo/dashboard`. Use a Next `<Link href="/apps/abrigo/dashboard">` wrapping the existing copy (or add a CTA below it). Keep the IN_PROGRESS StatusPill (the dashboard is honestly pre-launch). Use the new i18n keys for the link text (es-CO "Abrir el panel" / en "Open the dashboard") via getTranslations('dashboard') — t('open_dashboard') and an aria-label t('open_dashboard_aria').

    Add the two keys to BOTH messages/es-CO/dashboard.json and messages/en/dashboard.json under the `dashboard` object: `open_dashboard` ("Abrir el panel" / "Open the dashboard") and `open_dashboard_aria` ("Abrir el panel en vivo de Abrigo" / "Open the live Abrigo dashboard"). Author es-CO first. The 03-01 i18n-coverage recursive parity test will fail if these are not mirrored — keep them symmetric.

    GROUND TRUTH: playwright.config.ts ALREADY has a `webServer` (lines ~6-13) but it runs `pnpm dev` (Turbopack) on port 3000 and is `undefined` when `process.env.CI`. That dev-vs-prod gap is EXACTLY the Pitfall 5 / Phase-2 burn class (route-segment config honored in dev, 500s in the production webpack build). REPLACE the dev command with the production build so the DASH routes are exercised under `pnpm build && pnpm start` (M-webserver), and make it run in CI too:
    ```typescript
    // Production build is the ground truth for force-dynamic route-segment config (Pitfall 5).
    const webServer = {
      command: 'pnpm build && pnpm start -p 3040',
      url: 'http://localhost:3040',
      reuseExistingServer: !process.env.CI,
      timeout: 180_000,
    }
    ```
    Update the `BASE_URL` default (or `use.baseURL`) to `http://localhost:3040` so specs resolve relative paths against this server, and remove the `process.env.CI ? undefined :` guard so the webServer also runs in CI. Keep exactly ONE `webServer` block. Readiness is awaited via `webServer.url` — no `sleep`, no `pkill -f "next start"`.

    Fill tests/api/dashboard.test.ts (convert it.todo -> real, M4 reconcile; `// @vitest-environment node` already at top from 03-01):
    - `import { GET } from '@/app/api/dashboard/route'`. it('returns 200 with version:1, chains[5], no bigint'): `const res = await GET(new Request('http://localhost/api/dashboard?app=abrigo'))`; expect res.status 200; `const body = await res.json()`; expect body.version 1; expect body.chains length 5; expect(() => JSON.stringify(body)).not.toThrow().
    - it('returns 404 for an unknown app'): `const res = await GET(new Request('http://localhost/api/dashboard?app=unknown'))`; expect res.status 404.
    - it('a degraded chain does not 500'): mock `@/lib/chains/clients` so one chain rejects; await GET(...); expect res.status 200; expect body.status 'degraded'.

    Fill the 4 e2e specs (convert test.fixme -> real test). Follow homepage-content.spec.ts conventions; set NEXT_LOCALE cookie when asserting English copy. Rely on the playwright webServer baseURL — no inline server spawning.

    tests/e2e/api-dashboard.spec.ts (DASH-01): `const res = await request.get('/api/dashboard?app=abrigo')`; expect res.status() 200; `const body = await res.json()`; expect body.chains length 5; expect(() => JSON.stringify(body)).not.toThrow(); expect each chain to have a `status` field. Also `const r404 = await request.get('/api/dashboard?app=unknown')`; expect r404.status() 404 (RC m4).

    tests/e2e/dashboard-page.spec.ts (DASH-03): goto('/apps/abrigo/dashboard'); expect the live_banner text visible; expect the 4 tile labels (pool balance / settlement events / LP positions / last block) visible; expect at least one tile value to equal the dashed placeholder "-" (anti-fishing: no fabricated number). Assert no tile shows a numeric digit while the registry is empty.

    tests/e2e/dashboard-chain-selector.spec.ts (DASH-04): goto('/apps/abrigo/dashboard'); select 'base' in the chain selector; expect page.url() to contain '?chain=base'; reload; expect the selector still shows 'base' (URL state restored).

    tests/e2e/dashboard-no-js.spec.ts (DASH-07): `test.use({ javaScriptEnabled: false })`; goto('/apps/abrigo/dashboard'); expect the live_banner and the 4 tile labels present in the DOM on first paint (meaningful content with no hydration). The chain selector being non-interactive without JS is acceptable; the tiles + banner must still render.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/frontend && pnpm biome check "app/(apps)/apps/abrigo/page.tsx" messages/es-CO/dashboard.json messages/en/dashboard.json playwright.config.ts tests/api/dashboard.test.ts tests/e2e/api-dashboard.spec.ts tests/e2e/dashboard-page.spec.ts tests/e2e/dashboard-chain-selector.spec.ts tests/e2e/dashboard-no-js.spec.ts && grep -q "/apps/abrigo/dashboard" "app/(apps)/apps/abrigo/page.tsx" && grep -q "webServer" playwright.config.ts && ! grep -rq "test.fixme" tests/e2e/api-dashboard.spec.ts tests/e2e/dashboard-page.spec.ts tests/e2e/dashboard-chain-selector.spec.ts tests/e2e/dashboard-no-js.spec.ts && pnpm vitest run tests/unit/i18n-coverage.test.ts tests/api/dashboard.test.ts && pnpm playwright test tests/e2e/api-dashboard.spec.ts tests/e2e/dashboard-page.spec.ts tests/e2e/dashboard-chain-selector.spec.ts tests/e2e/dashboard-no-js.spec.ts</automated>
  </verify>
  <done>
    Overview teaser links to /apps/abrigo/dashboard with localized text; the two new i18n keys mirror across locales (parity test green); playwright.config.ts has a deterministic webServer block (no sleep/pkill); the route-handler unit test is real (200 + version:1 + 5 chains + 404 + degraded-not-500) and green; all 4 e2e specs are real (no fixme) and pass against the playwright-managed production server. The dashboard renders banner + 4 dashed tiles with no fabricated numbers; ?chain= round-trips; no-JS first paint shows the tiles.
  </done>
</task>

</tasks>

<verification>
- The playwright `webServer` (production build via `pnpm build && pnpm start`) serves /apps/abrigo/dashboard and /api/dashboard - confirms Turbopack/webpack route-segment config is honored (Pitfall 5, the Phase 2 burn class). The `pnpm build` gate in Task 2 separately proves the existing force-static routes survive.
- `pnpm test:quick` green; the route-handler unit test + 4 e2e specs green against the production server.
- `curl -s http://localhost:3040/api/dashboard?app=abrigo` returns a version:1 envelope with a 5-element chains array and no serialization error; `?app=unknown` returns 404.
- Anti-fishing: every tile shows "-" (no digit) while the registry is empty; live banner present.
</verification>

<success_criteria>
- DASH-01: /api/dashboard?app=abrigo returns a typed version:1 envelope (5 chains, no bigint), degrades per-chain not all-or-nothing, 404s unknown apps; called directly (no 'use cache').
- DASH-03: /apps/abrigo/dashboard shows the 4 labelled metric tiles per chain.
- DASH-04: chain selector writes ?chain=<slug> via the shared parser and the URL restores state on reload/paste.
- DASH-07: meaningful first paint with javaScriptEnabled:false - skeleton tiles + banner, no wallet gate.
- The production build passes with the existing force-static routes intact (B1 build gate), and the overview teaser is now a live link into the dashboard.
</success_criteria>

<live_verification>
MANDATORY per CLAUDE.md - this plan produces rendered route surfaces (/apps/abrigo/dashboard, /api/dashboard, and the edited /apps/abrigo). After the Task 3 commit lands, launch the Evidence Collector:

```
Agent(
  subagent_type="Evidence Collector",
  description="Live verify task 03-02",
  prompt="""
    Working dir: /home/jmsbpp/apps/d2p/frontend
    Target: https://www.d2pfinance.xyz (fall back to local `pnpm build && pnpm start` on port 3040 if the route 4xx/5xx)
    Use the mcp__plugin_playwright_playwright__* tools.

    Task just completed: 03-02 - Abrigo on-chain dashboard slice (BFF route + RSC page + chain selector + no-JS first paint)
    Routes affected: /apps/abrigo/dashboard, /api/dashboard?app=abrigo, /apps/abrigo (teaser link)
    Plan claims to verify:
      - /apps/abrigo/dashboard renders 4 labelled metric tiles (pool balance, settlement events, LP positions, last block synced)
      - Every tile value is the dashed placeholder '-' (NO fabricated numbers) while the instrument registry is empty
      - A 'Live once contracts deploy' banner is present (icon + text, not color-only)
      - Selecting a chain updates the URL to ?chain=<slug>; pasted URL restores the selected chain
      - /api/dashboard?app=abrigo returns 200 JSON (version:1 envelope) with a 5-element chains array, no runtime serialization error; ?app=unknown returns 404
      - The /apps/abrigo overview 'Live dashboard' teaser links to /apps/abrigo/dashboard
      - Tokens: muted ochre accent only, IBM Plex, no card-in-card, no left-colored borders

    For each affected route: browser_navigate, browser_snapshot (assert each claim against DOM),
    browser_console_messages (surface runtime errors), browser_take_screenshot to /tmp/d2p-verify/03-02-{slug}.png,
    browser_evaluate for the anti-fishing invariant (assert no tile textContent matches /\\d/ while empty) and
    for the ?chain= URL round-trip. Default to skepticism; screenshots required.

    Output: append a section to .planning/phases/03-data-layer-and-on-chain-dashboard/03-LIVE-VERIFICATION.md
    with verdicts per claim (PASS/PARTIAL/FAIL/UNREACHABLE) and screenshot paths. Return short text summary.
  """
)
```

A task is NOT complete until every claim is PASS or has an explicit waiver recorded in 03-LIVE-VERIFICATION.md.
</live_verification>

<output>
After completion, create `.planning/phases/03-data-layer-and-on-chain-dashboard/03-02-SUMMARY.md`.
</output>

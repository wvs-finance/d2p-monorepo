---
phase: 01-foundation-and-scaffold
plan: 08
type: execute
wave: 4
depends_on: ['01-04', '01-07']
files_modified:
  - .github/workflows/ci.yml
  - lighthouserc.cjs
  - tests/unit/anti-patterns.test.ts
  - tests/unit/fixtures/anti-patterns.html
  - tests/a11y/homepage.spec.ts
  - package.json
  - docs/impeccable-flag.md
  - docs/a11y-audit.md
  - docs/copy-review.md
autonomous: true
requirements:
  - FOUND-07
  - FOUND-08
  - FOUND-09
  - CROSS-01
  - CROSS-03
  - CROSS-04

must_haves:
  truths:
    - "The impeccable CI step relies on the CLI's non-zero exit code only — no `<VERIFIED_FLAG>` placeholder ships in the workflow. Task 1 verifies the flag situation via `npx impeccable detect --help` and records findings in `docs/impeccable-flag.md`; if a strengthening flag exists, Task 3 may add it as a second hardening step, but the base workflow committed must work with zero substitution."
    - "The CI workflow contains seven parallel jobs: `lint`, `typecheck`, `test-unit`, `test-e2e`, `a11y`, `lighthouse`, `impeccable`. Lighthouse/e2e/a11y run on Vercel `deployment_status` events. Lint/typecheck/test-unit/impeccable run on push and pull_request."
    - "Lighthouse CI enforces LCP ≤ 2500ms and TBT ≤ 200ms on the Moto G Power 3G profile (412×823, DPR 2.625, 1.6Mbps throttling, 4× CPU slowdown). Build fails on regression."
    - "axe-core scan via `@axe-core/playwright` runs against the deployed preview URL with tags `wcag2a wcag2aa wcag21a wcag21aa wcag22aa`. Zero violations required."
    - "A planted-pattern test (`tests/unit/anti-patterns.test.ts`) confirms impeccable WOULD detect each major anti-pattern (Inter font, purple gradient, eyebrow chip, italic serif h1, nested cards, dark glow) — so the CI job is proven to fire on regressions, not just pass because no violations exist."
    - "`docs/a11y-audit.md` exists with the manual screen-reader audit checklist (CROSS-01 manual half)."
    - "`docs/copy-review.md` exists with the AI-slop copy review checklist (CROSS-10 manual half)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions workflow with 7 parallel jobs and proper deployment_status gating; impeccable step uses exit-code-only enforcement"
    - path: "lighthouserc.cjs"
      provides: "LHCI config with Moto G Power 3G + LCP/TBT budgets"
      contains: "largest-contentful-paint"
    - path: "tests/unit/anti-patterns.test.ts"
      provides: "Planted-pattern Vitest test that shells out to `impeccable detect` and asserts non-zero exit + violation categories in stdout"
    - path: "tests/unit/fixtures/anti-patterns.html"
      provides: "Static HTML fixtures intentionally violating impeccable rules"
    - path: "docs/impeccable-flag.md"
      provides: "Live-verified record of impeccable CLI flag situation (or exit-code-only fallback)"
    - path: "docs/a11y-audit.md"
      provides: "Manual screen-reader audit checklist for top-5-template review"
    - path: "docs/copy-review.md"
      provides: "AI-slop copy review checklist"
  key_links:
    - from: ".github/workflows/ci.yml impeccable job"
      to: "npx impeccable detect (non-zero exit code on violations)"
      via: "shell command relying on exit code; no flag substitution required"
      pattern: "impeccable detect"
    - from: ".github/workflows/ci.yml lighthouse job"
      to: "lighthouserc.cjs"
      via: "lhci autorun reads config"
      pattern: "lhci autorun"
    - from: "tests/a11y/homepage.spec.ts"
      to: "@axe-core/playwright AxeBuilder"
      via: "import + withTags"
      pattern: "wcag22aa"
---

<objective>
Land the CI quality matrix: GitHub Actions workflow with seven parallel jobs covering lint, typecheck, unit tests, e2e tests, accessibility scan, Lighthouse performance budget, and impeccable anti-pattern detection. The impeccable CI step is committed using exit-code-only enforcement so the workflow YAML ships without any `<VERIFIED_FLAG>` placeholder. Task 1 still verifies the live CLI flag situation via `npx impeccable detect --help` and records findings in `docs/impeccable-flag.md`; if a strengthening flag exists, Task 3 MAY add an optional hardening step, but the base step relies on the binary's non-zero exit code. Plant adversarial test fixtures that violate each anti-pattern so the impeccable CI job is proven to detect, not just pass by coincidence.

Purpose: FOUND-07, FOUND-08, FOUND-09, CROSS-01, CROSS-03, CROSS-04 are ALL CI gates. The whole point of Phase 1 is that no downstream phase can ship a regression past the gates established here. The gates must be required for merge from PR #1 — no "warn-only" warm-up.

Output: CI workflow with all 7 jobs (impeccable uses exit-code-only); Lighthouse config; impeccable-proof planted-pattern fixtures; manual audit checklists for the two non-automatable cross-cutting requirements (CROSS-01 manual screen-reader, CROSS-10 copy review).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-and-scaffold/01-CONTEXT.md
@.planning/phases/01-foundation-and-scaffold/01-RESEARCH.md
@.planning/phases/01-foundation-and-scaffold/01-VALIDATION.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-and-scaffold/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-scaffold/01-04-SUMMARY.md
@.planning/phases/01-foundation-and-scaffold/01-07-SUMMARY.md

<interfaces>
<!-- impeccable CLI enforcement strategy:
     The base CI step relies ONLY on the binary's non-zero exit code (well-behaved CLIs
     exit non-zero when they detect violations). No flag substitution is required for
     the workflow to ship working. Task 1 documents what flags (if any) exist for
     potential hardening in a follow-up. -->

<!-- Lighthouse Moto G Power emulation values (from RESEARCH.md Pattern 10) -->
- formFactor: mobile
- screenEmulation: { width: 412, height: 823, deviceScaleFactor: 2.625, mobile: true }
- throttling: { rttMs: 150, throughputKbps: 1638.4, cpuSlowdownMultiplier: 4 }
- Budgets: LCP ≤ 2500ms (error), TBT ≤ 200ms (error), CLS ≤ 0.1 (warn)

<!-- axe-core tags for WCAG 2.2 AA (Pattern 11 in RESEARCH.md) -->
.withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa'])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify impeccable CLI flag situation + build planted-pattern fixtures + finalize anti-pattern test</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-RESEARCH.md (Open Question 1: impeccable CLI exact flag for CI failure; Common Pitfall 4: impeccable CLI Flag Name Uncertainty)
    - /home/jmsbpp/apps/d2p/frontend/.planning/research/PITFALLS.md (Pitfalls 1-15: every impeccable anti-pattern detector — Inter, purple gradient, nested cards, side-tab accent, icon tile, bounce easing, gray on color, pure black, hero metric, gradient text, identical card grids, slop copy, italic serif hero, eyebrow chip, dark glow)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-VALIDATION.md (FOUND-07, CROSS-04 rows)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-CONTEXT.md (CI architecture; impeccable detect)
    - tests/unit/anti-patterns.test.ts (Wave 0 stub file from Plan 01 — Vitest test that shells out to impeccable)
  </read_first>
  <action>
    1. **Investigate the impeccable CLI** before doing anything else. Run:
       ```bash
       npx --yes impeccable detect --help 2>&1 | tee /tmp/impeccable-help.txt
       ```
       Inspect the output. Look for one of: `--fail-on-error`, `--fail-on-issues`, `--ci`, `--format json`, or any flag that affects exit code on detected violations.

       Record findings in `docs/impeccable-flag.md` using this template:
       ```markdown
       # impeccable CLI flag situation (Phase 1 verification)

       **Verified:** <DATE>
       **CLI version:** `<output of `npx impeccable --version`>`

       ## Help output summary
       <Relevant flags from --help, OR "no flags affect exit-code behavior">

       ## Strategy decision
       <One of:
         - "Base CI step uses exit-code-only (CLI exits non-zero on violations). No flag needed."
         - "Base CI step uses exit-code-only. Optional hardening flag `--<X>` available; if added, see Task 3."
         - "CLI cannot be resolved on npm registry. Switching to <alternative package or removing impeccable CI gate>."
       >

       ## Sanity check
       Ran against the planted fixtures at `tests/unit/fixtures/anti-patterns.html`:
       - Exit code on fixtures with violations: <non-zero or zero>
       - Exit code on clean source (app/): <expected 0>
       ```

       If `npx --yes impeccable` cannot resolve (package not on npm registry / different name), document the resolution: search via `npm search impeccable detect` or check if installation requires a scoped name. If no viable form exists, escalate via SUMMARY — but commit `docs/impeccable-flag.md` describing the situation.

    2. Plant adversarial fixtures at `tests/unit/fixtures/anti-patterns.html` (kept under `tests/unit/` because the consumer is a Vitest test — there is no `tests/visual/` directory in this project) — a static HTML file deliberately triggering each detectable rule:
       ```html
       <!DOCTYPE html>
       <html>
       <head>
         <style>
           body { font-family: 'Inter', sans-serif; background: #000000; }
           .hero { background: linear-gradient(135deg, #7c3aed, #2563eb); }
           h1 { font-family: 'Fraunces'; font-style: italic; font-size: 64px;
                background: linear-gradient(90deg, #f43f5e, #8b5cf6);
                -webkit-background-clip: text; }
           .eyebrow { border-radius: 9999px; letter-spacing: 0.15em;
                      text-transform: uppercase; font-size: 11px; }
           .card { border: 1px solid #ddd; border-radius: 8px; padding: 16px;
                   box-shadow: 0 0 80px rgba(124, 58, 237, 0.4); }
           .card .card { /* nested card — anti-pattern */ }
           .accent-side { border-left: 4px solid #7c3aed; border-radius: 8px; }
           .gray-on-color { background: #e0e7ff; color: #6b7280; }
         </style>
       </head>
       <body>
         <span class="eyebrow">RESEARCH · FINANCE</span>
         <h1>Empower your financial future</h1>
         <div class="hero"></div>
         <div class="card"><div class="card">Nested</div></div>
         <div class="accent-side"></div>
         <p class="gray-on-color">Hard to read</p>
       </body>
       </html>
       ```
       This fixture is read by `tests/unit/anti-patterns.test.ts` (Vitest) — NOT by the impeccable CI job. The CI job runs against `app/`, not against the fixtures directory. The project has NO `tests/visual/` directory; fixtures live alongside their consumer test under `tests/unit/fixtures/`.

    3. Update `tests/unit/anti-patterns.test.ts` (the Wave 0 stub from Plan 01) to invoke impeccable against the fixture file and assert violations are reported. The fixture lives under `tests/unit/fixtures/` (created in step 2 above — there is no `tests/visual/` directory in this project; fixtures co-locate with their consumer test). Pseudocode:
       ```typescript
       import { execSync } from 'node:child_process'
       import { test, expect } from 'vitest'

       test('impeccable detects each planted anti-pattern in fixtures', () => {
         let stdout = ''
         let exitCode = 0
         try {
           stdout = execSync('npx impeccable detect tests/unit/fixtures/anti-patterns.html', { encoding: 'utf8' })
         } catch (err: any) {
           stdout = (err.stdout ?? '') + (err.stderr ?? '')
           exitCode = err.status ?? 1
         }
         // Primary signal: non-zero exit (the strategy our CI workflow relies on).
         expect(exitCode).not.toBe(0)
         // Secondary signal: each planted category is named in the report.
         expect(stdout).toMatch(/inter|overused-font/i)
         expect(stdout).toMatch(/purple|gradient|ai-color-palette/i)
         expect(stdout).toMatch(/eyebrow|icon-tile-stacked-above-heading|eyebrow-chip/i)
         expect(stdout).toMatch(/italic-serif|oversized-italic-serif/i)
         expect(stdout).toMatch(/nested-cards|wrapping-everything-in-cards/i)
         expect(stdout).toMatch(/gray-text-on-colored-background/i)
         expect(stdout).toMatch(/pure-black-background/i)
         expect(stdout).toMatch(/dark-glow|box-shadow.*blur/i)
       })
       ```

    4. Run the test: `pnpm vitest run tests/unit/anti-patterns.test.ts` — must pass (i.e., impeccable DID detect the planted anti-patterns AND exited non-zero). Note: `tests/unit/` is included in Vitest's `include` glob and is NOT excluded by Plan 01's vitest.config (which excludes `tests/e2e/**`, `tests/a11y/**`, `node_modules`). Playwright's `testIgnore` excludes `**/unit/**` so it will never try to collect this file.

    5. The `app/` directory (homepage from Plan 04) should pass impeccable cleanly. Run `npx impeccable detect app/` and confirm zero violations / exit code 0. If violations exist, this is a Plan 04 regression — STOP and report back, do not silently rewrite the homepage.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/anti-patterns.test.ts && npx impeccable detect app/ && test -f tests/unit/fixtures/anti-patterns.html && grep -q "Inter" tests/unit/fixtures/anti-patterns.html && grep -q "#7c3aed" tests/unit/fixtures/anti-patterns.html && grep -q "border-left" tests/unit/fixtures/anti-patterns.html && ! test -d tests/visual && test -f docs/impeccable-flag.md && grep -q "exit-code" docs/impeccable-flag.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/impeccable-flag.md` documents the live-verified CLI flag situation (or fallback strategy) and notes that the CI step relies on exit-code-only.
    - `tests/unit/fixtures/anti-patterns.html` contains intentional violations for at least 8 distinct impeccable rules. No `tests/visual/` directory exists in the project.
    - `tests/unit/anti-patterns.test.ts` (Vitest, not Playwright) invokes impeccable against the fixture and asserts BOTH non-zero exit code AND that violations are reported by category.
    - `npx impeccable detect app/` returns 0 violations on the real homepage.
    - Maps to 01-VALIDATION.md FOUND-07, CROSS-04 rows.
  </acceptance_criteria>
  <done>Impeccable exit-code strategy verified; planted fixtures prove the detector fires; real homepage passes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author lighthouserc.cjs + axe-core homepage test</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-RESEARCH.md (Pattern 10: Lighthouse CI Config for Moto G Power 3G; Pattern 11: axe-core/playwright Integration)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-CONTEXT.md (Lighthouse profile: Moto G Power 3G; LCP < 2.5s; TBT < 200ms)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-VALIDATION.md (FOUND-08, FOUND-09, CROSS-01, CROSS-03 rows)
    - /home/jmsbpp/apps/d2p/frontend/.planning/research/PITFALLS.md (Pitfall 30: LCP > 2.5s on 3G Android)
    - tests/a11y/homepage.spec.ts (stub from Plan 01)
    - lighthouserc.cjs (stub from Plan 01 — may need refinement)
  </read_first>
  <action>
    1. `@lhci/cli` and `@axe-core/playwright` were installed by Plan 01. Verify:
       ```bash
       jq -e '.devDependencies["@lhci/cli"]' package.json
       jq -e '.devDependencies["@axe-core/playwright"]' package.json
       ```
       If missing, HALT and report Plan 01 regression.

    2. Refine `lighthouserc.cjs` with Moto G Power 3G profile and budgets — use the exact values from RESEARCH.md Pattern 10:
       ```javascript
       module.exports = {
         ci: {
           collect: {
             numberOfRuns: 3,
             url: [process.env.LHCI_COLLECT__URL ?? 'http://localhost:3000'],
             settings: {
               formFactor: 'mobile',
               screenEmulation: {
                 mobile: true,
                 width: 412,
                 height: 823,
                 deviceScaleFactor: 2.625,
                 disabled: false,
               },
               throttling: {
                 rttMs: 150,
                 throughputKbps: 1638.4,
                 cpuSlowdownMultiplier: 4,
                 requestLatencyMs: 562.5,
                 downloadThroughputKbps: 1474.6,
                 uploadThroughputKbps: 675,
               },
               throttlingMethod: 'simulate',
             },
           },
           assert: {
             assertions: {
               'largest-contentful-paint': ['error', { maxNumericValue: 2500 }],
               'total-blocking-time':      ['error', { maxNumericValue: 200 }],
               'cumulative-layout-shift':  ['warn',  { maxNumericValue: 0.1 }],
               'first-contentful-paint':   ['warn',  { maxNumericValue: 2000 }],
               'categories:accessibility': ['warn',  { minScore: 0.9 }],
               'categories:performance':   ['warn',  { minScore: 0.75 }],
             },
           },
           upload: { target: 'temporary-public-storage' },
         },
       }
       ```

    3. Update `tests/a11y/homepage.spec.ts` (the Plan 01 stub) to scan the real homepage with WCAG 2.2 AA tags:
       ```typescript
       import { test, expect } from '@playwright/test'
       import AxeBuilder from '@axe-core/playwright'

       test('homepage has no WCAG 2.2 AA violations', async ({ page }) => {
         await page.goto('/')
         const results = await new AxeBuilder({ page })
           .withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa'])
           .analyze()
         expect(results.violations).toEqual([])
       })

       test('homepage in en locale has no WCAG 2.2 AA violations', async ({ page, context }) => {
         await context.addCookies([{ name: 'NEXT_LOCALE', value: 'en', url: 'http://localhost:3000' }])
         await page.goto('/')
         const results = await new AxeBuilder({ page })
           .withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa'])
           .analyze()
         expect(results.violations).toEqual([])
       })
       ```
       Both locales must pass since they may render different text widths affecting overflow.

    4. Add npm scripts to `package.json`:
       ```json
       {
         "scripts": {
           "test:a11y":        "playwright test tests/a11y/",
           "test:lighthouse":  "lhci autorun",
           "test:impeccable":  "impeccable detect app/"
         }
       }
       ```

    5. Run locally (against the dev server):
       - `pnpm dev &` then `pnpm test:a11y` — should pass.
       - `pnpm dev &` then `LHCI_COLLECT__URL=http://localhost:3000 pnpm test:lighthouse` — LCP should fit under 2500ms.
       If LCP fails locally, this is a Plan 02/04 regression — investigate the homepage before tweaking the budget.
  </action>
  <verify>
    <automated>pnpm test:a11y && test -f lighthouserc.cjs && grep -q "largest-contentful-paint" lighthouserc.cjs && grep -q "2500" lighthouserc.cjs && grep -q "wcag22aa" tests/a11y/homepage.spec.ts && grep -qE "test:lighthouse|test:a11y|test:impeccable" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `lighthouserc.cjs` exists with Moto G Power 3G emulation and LCP/TBT error budgets.
    - `tests/a11y/homepage.spec.ts` scans both locales with the five WCAG tags.
    - `pnpm test:a11y` passes against the dev server.
    - `pnpm test:lighthouse` against local dev server produces a passing run (or run fails informatively if homepage is too heavy).
    - Maps to 01-VALIDATION.md FOUND-08, FOUND-09, CROSS-01, CROSS-03 rows.
  </acceptance_criteria>
  <done>Lighthouse config + axe-core test green against the real homepage; budgets enforced locally before CI.</done>
</task>

<task type="auto">
  <name>Task 3: Write .github/workflows/ci.yml with 7 parallel jobs + manual audit checklists (impeccable uses exit-code-only)</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-RESEARCH.md (Pattern 12: GitHub Actions CI Architecture)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-CONTEXT.md (CI architecture decisions; all jobs required for merge)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-VALIDATION.md (CROSS-01 manual audit; CROSS-10 manual copy review)
    - docs/impeccable-flag.md (just created in Task 1)
    - lighthouserc.cjs (just created in Task 2)
  </read_first>
  <action>
    1. Author `.github/workflows/ci.yml` with seven jobs. The first four run on push/PR; the last three run on `deployment_status` (Vercel preview ready). The impeccable job uses **exit-code-only enforcement** — no `<VERIFIED_FLAG>` placeholder substitution required to ship:

       ```yaml
       name: CI

       on:
         push:
           branches: [main]
         pull_request:
         deployment_status:

       jobs:
         lint:
           if: github.event_name != 'deployment_status'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm biome check .

         typecheck:
           if: github.event_name != 'deployment_status'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm velite build  # Required so iterations types resolve
             - run: pnpm tsc --noEmit

         test-unit:
           if: github.event_name != 'deployment_status'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm velite build
             - run: pnpm vitest run

         impeccable:
           if: github.event_name != 'deployment_status'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - name: Run impeccable detector
               run: npx --yes impeccable detect app/
               # Non-zero exit code from impeccable fails this step.
               # No --fail-on-error flag needed; the binary exits non-zero on any detected anti-pattern.
               # See docs/impeccable-flag.md for the live-verified CLI flag situation. If a strengthening
               # flag is discovered post-merge, a follow-up PR may add an additional hardening step
               # below this one — but the base step above ships working without substitution.

         test-e2e:
           if: github.event_name == 'deployment_status' && github.event.deployment_status.state == 'success'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm playwright install --with-deps chromium
             - run: pnpm playwright test --project=chromium
               env:
                 PLAYWRIGHT_TEST_BASE_URL: ${{ github.event.deployment_status.target_url }}

         a11y:
           if: github.event_name == 'deployment_status' && github.event.deployment_status.state == 'success'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm playwright install --with-deps chromium
             - run: pnpm playwright test tests/a11y/
               env:
                 PLAYWRIGHT_TEST_BASE_URL: ${{ github.event.deployment_status.target_url }}

         lighthouse:
           if: github.event_name == 'deployment_status' && github.event.deployment_status.state == 'success'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm dlx @lhci/cli@^0.15 autorun
               env:
                 LHCI_BUILD_CONTEXT__CURRENT_HASH: ${{ github.sha }}
                 LHCI_COLLECT__URL: ${{ github.event.deployment_status.target_url }}
       ```

       **Critical:** The committed YAML contains NO `<VERIFIED_FLAG>` placeholder anywhere. The impeccable step is `npx --yes impeccable detect app/` exactly — relying on the binary's exit code.

       **Optional hardening:** If `docs/impeccable-flag.md` (from Task 1) documented that an additional flag like `--fail-on-error` strengthens enforcement, the executor MAY add a second step under the impeccable job below the base step, e.g.:
       ```yaml
             - name: Hardening — impeccable strict flag (optional)
               if: false  # set to true after manual confirmation that the flag exists
               run: npx --yes impeccable detect app/ --fail-on-error
       ```
       This optional step must default to `if: false` so it does NOT execute on first merge. Leaving it out entirely is also acceptable.

    2. Author `docs/a11y-audit.md` with the manual screen-reader audit checklist (CROSS-01 manual half):
       ```markdown
       # Manual Accessibility Audit — Top 5 Templates

       This checklist supplements the automated axe-core CI gate. Manual screen-reader audits
       are required for the top 5 page templates by traffic / agent importance.

       ## Phase 1 — Stub Homepage (`/`)

       Run with VoiceOver (macOS) or NVDA (Windows).

       - [ ] Heading order is logical (h1 then h2; no skipped levels).
       - [ ] Language switcher region is announced ("Language navigation" or similar).
       - [ ] LanguageSwitcher buttons announce active state via aria-current="true".
       - [ ] StatusPill text labels (PASS / FAIL / etc.) are announced — NOT just icons.
       - [ ] Tab order matches visual order: header → main → footer.
       - [ ] Focus ring visible on every interactive element.
       - [ ] Page renders correctly when font size scaled to 200%.

       Record findings inline (date, reviewer, result).

       ## Phase 2 — Iteration Catalog (`/iterations`)
       *To be filled by Phase 2 reviewer.*

       ## Phase 2 — Iteration Detail (`/iterations/<slug>/v<n>`)
       *To be filled by Phase 2 reviewer.*

       ## Phase 3 — Dashboard (`/dashboard`)
       *To be filled by Phase 3 reviewer.*

       ## Phase 5 — Instrument Detail (`/instruments/<id>/<chain>`)
       *To be filled by Phase 5 reviewer.*
       ```

    3. Author `docs/copy-review.md` with the AI-slop copy review checklist (CROSS-10 manual half):
       ```markdown
       # Copy Review — Author Quality, Not AI Slop

       Before any new copy ships, run this manual check. Banned phrasing is also
       caught by impeccable's copy detector, but human review remains required
       for tonal judgment.

       ## Banned phrases (zero tolerance)
       - "empower [audience]"
       - "revolutionize"
       - "transform your X"
       - "seamless"
       - "unlock"
       - "next-gen", "cutting-edge"
       - "leverage your"
       - "unleash"
       - "best-in-class"
       - "[N]% increase in [vanity metric]" without source citation

       ## Tone check
       - [ ] First-person plural ("We measured...") for lab voice.
       - [ ] Specific claims with citations (β, p-value, sample size, replication hash).
       - [ ] Failures rendered with same weight as successes — never buried.
       - [ ] No marketing superlatives without measurable backing.
       - [ ] Both es-CO and en copy reviewed by a native speaker.

       ## Phase 1 review (stub homepage)
       - [ ] `messages/es-CO/lab.json` — reviewed by: ___ on: ___
       - [ ] `messages/en/lab.json`    — reviewed by: ___ on: ___
       ```

    4. Validate YAML: `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'))"` — must parse. CRITICAL: also confirm that the committed YAML contains NO `<VERIFIED_FLAG>` placeholder text:
       ```bash
       ! grep -q "<VERIFIED_FLAG>" .github/workflows/ci.yml
       ```

    5. After committing, push to a feature branch and open a PR — observe the workflow run. Confirm:
       - lint, typecheck, test-unit, impeccable jobs run on the PR.
       - test-e2e, a11y, lighthouse jobs run when Vercel preview deploys (deployment_status: success).
       - All jobs are required for merge (configure in GitHub repo branch protection settings; user manual setup).
  </action>
  <verify>
    <automated>node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'))" && test -f .github/workflows/ci.yml && [ "$(grep -c 'runs-on: ubuntu-latest' .github/workflows/ci.yml)" -ge 7 ] && grep -q "biome check" .github/workflows/ci.yml && grep -q "tsc --noEmit" .github/workflows/ci.yml && grep -q "playwright test" .github/workflows/ci.yml && grep -q "lhci autorun" .github/workflows/ci.yml && grep -q "impeccable detect" .github/workflows/ci.yml && grep -q "deployment_status" .github/workflows/ci.yml && ! grep -q "<VERIFIED_FLAG>" .github/workflows/ci.yml && test -f docs/a11y-audit.md && test -f docs/copy-review.md</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists with at least 7 distinct jobs (lint, typecheck, test-unit, impeccable, test-e2e, a11y, lighthouse).
    - YAML parses without errors.
    - The committed YAML contains NO `<VERIFIED_FLAG>` placeholder text anywhere.
    - The impeccable step is `npx --yes impeccable detect app/` with a comment explaining exit-code-only enforcement.
    - `test-e2e`, `a11y`, `lighthouse` are conditioned on `deployment_status` event and `state == 'success'`.
    - `docs/a11y-audit.md` exists with top-5-templates checklist (Phase 1 row filled).
    - `docs/copy-review.md` exists with banned-phrases list and Phase 1 sign-off slots.
    - Maps to 01-VALIDATION.md FOUND-07, FOUND-08, FOUND-09, CROSS-01, CROSS-03, CROSS-04 rows.
  </acceptance_criteria>
  <done>CI workflow committed with 7 parallel jobs (impeccable uses exit-code-only — no placeholder); manual audit checklists in place; YAML valid.</done>
</task>

</tasks>

<verification>
- `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'))"` exits 0.
- `! grep -q "<VERIFIED_FLAG>" .github/workflows/ci.yml` — no placeholder ships.
- `pnpm vitest run tests/unit/anti-patterns.test.ts` exits 0 (impeccable detects planted anti-patterns AND exits non-zero).
- `pnpm test:a11y` exits 0 against local dev server.
- `pnpm test:lighthouse` exits 0 (or produces a passing run) against local dev server.
- `npx impeccable detect app/` exits 0 against the real homepage.
- After Plan 08 is merged: open a PR and confirm all 7 CI jobs run + are required.
</verification>

<success_criteria>
- All FOUND-07 acceptance criteria met: impeccable runs in CI on every PR via exit-code enforcement (no placeholder substitution).
- All FOUND-08 acceptance criteria met: Lighthouse CI enforces LCP < 2.5s on Moto G Power 3G.
- All FOUND-09 acceptance criteria met: axe-core scans every PR preview with WCAG 2.2 AA tags.
- All CROSS-01 acceptance criteria met: axe-core CI + manual screen-reader audit checklist.
- All CROSS-03 acceptance criteria met: LCP budget enforced (same as FOUND-08).
- All CROSS-04 acceptance criteria met: impeccable detect with planted-pattern test proving the detector fires AND exits non-zero.
- Manual audit checklists exist for CROSS-01 manual half and CROSS-10 manual half (sign-off pending native speaker / copy author).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-scaffold/01-08-SUMMARY.md` documenting:
- The live-verified impeccable CLI flag situation (full content of docs/impeccable-flag.md).
- Confirmation that the committed CI YAML ships with zero placeholders (impeccable uses exit-code-only).
- The full CI job matrix and which jobs trigger on which events.
- Lighthouse Moto G Power emulation values + budget thresholds.
- Manual checklists' Phase 1 sign-off status (es-CO native speaker review, copy author review).
- Branch protection setup required by user (each job marked required for merge in repo settings).
- Any open issues blocking CROSS-01 manual or CROSS-10 manual sign-off.
</output>
</content>
</invoke>
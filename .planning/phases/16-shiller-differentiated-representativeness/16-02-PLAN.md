---
phase: 16-shiller-differentiated-representativeness
plan: 02
type: tdd
wave: 2
depends_on: ["16-01"]
files_modified:
  - contracts/src/MacroHedgeExecutor.sol
  - contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol
autonomous: true
requirements: [SHILLER-01]
must_haves:
  truths:
    - "resolveFromMandate branches on mandate.economicTheory: 0x5 -> SHILLER arm, else -> the verbatim PKE body"
    - "a SHILLER mandate mints a strike DIFFERENT from the PKE 360360 for the same input"
    - "ExecutorDecided carries a per-school TEMPLATE rationale (SHILLER=Shiller/UNVALIDATED, PKE=post-Keynesian), both nonErgodicDisclosed=true"
    - "the PKE mandate STILL mints exact 360360 with its unchanged rationale (regression anchor)"
    - "the downside (s<0, K_lo) strike question is resolved concretely (fork-mint check OR depreciation-only-v1)"
  artifacts:
    - path: "contracts/src/MacroHedgeExecutor.sol"
      provides: "the school-branched resolveFromMandate + SHILLER TEMPLATE rationale constant"
      contains: "SHILLER_RATIONALE"
    - path: "contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol"
      provides: "test_branch_shillerDiffersFromPke + test_executorDecided_perSchoolHonesty"
      contains: "test_branch_shillerDiffersFromPke"
  key_links:
    - from: "contracts/src/MacroHedgeExecutor.sol"
      to: "surpriseOracle.latestSurprise"
      via: "the SHILLER arm reads the surprise oracle with the staleness fail-safe"
      pattern: "surpriseOracle\\.latestSurprise"
    - from: "contracts/src/MacroHedgeExecutor.sol"
      to: "RepresentativenessLib.shillerOptionRatio"
      via: "the SHILLER arm derives convex size/sign-strike/width from the lib"
      pattern: "shillerOptionRatio|shillerStrikeTick|shillerWidth"
    - from: "contracts/src/MacroHedgeExecutor.sol"
      to: "_resolveAndMintAtStrike"
      via: "the SHILLER arm mints through the SHARED unchanged Fix-C sink"
      pattern: "_resolveAndMintAtStrike"
---

<objective>
Branch `resolveFromMandate` on `mandate.economicTheory`: the SHILLER sentinel (0x5) takes a new arm that reads the surprise oracle (staleness fail-safe -> s=0), derives the convex size + sign-driven further-OTM strike + |s|-scaled width from the Wave-1 lib fns, and emits a SHILLER-specific TEMPLATE rationale; every other sentinel (incl. PKE 0x6) falls through to the EXISTING body moved VERBATIM into the else. Prove on the Polygon fork that a SHILLER mandate mints a strike different from PKE's 360360, that the per-school honesty surfaces on ExecutorDecided, and that the PKE mandate STILL mints exactly 360360 (the regression anchor).

Purpose: this is the load-bearing differentiation — the school CHANGES the geometry, not just the label. PKE must stay byte-identical (Phase-14 fork-proven).
Output: the branched resolveFromMandate + SHILLER rationale constant on MacroHedgeExecutor.sol, and the fork differentiation/honesty/regression tests in DemoMacroHedgeExecutor.fork.t.sol.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shiller-differentiated-representativeness/16-RESEARCH.md
@.planning/phases/16-shiller-differentiated-representativeness/16-VALIDATION.md
@.planning/phases/16-shiller-differentiated-representativeness/16-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The branch point + sink + sentinel + event + fork harness. Use directly. -->

resolveFromMandate (MacroHedgeExecutor.sol:197-259) currently ALWAYS runs the PKE regime path and mints via `_resolveAndMintAtStrike(legParams, legIndex, positionSize, 0, strikeTick)` (the SHARED sink, :274, UNCHANGED). The PKE K_hi = 360360 (`structuralStrikeTick((3900*115)/100, 60)`).

The sentinel branch key (IMacroThesis.sol): `address(mandate.economicTheory) == address(uint160(0x5))` is SHILLER; `0x6` is POST_KEYNESIAN. The sentinel is NEVER called — only address-compared. Default/else = PKE (a malformed sentinel fails toward the proven path).

ExecutorDecided (MacroHedgeExecutor.sol:94) — 8 params, shape UNCHANGED:
`(uint256 requestId, uint8 regimeZt, uint256 inflationAdjustmentWad, int24 strikeTick, int24 regimeWidth, bool parametricHedged, bool nonErgodicDisclosed, string rationale)`. topic0 = `keccak256("ExecutorDecided(uint256,uint8,uint256,int24,int24,bool,bool,string)")` (decode pattern at DemoMacroHedgeExecutor.fork.t.sol:368-369,483-484). The per-school difference is ONLY the rationale string + the derived tick fields.

PKE existing rationale (TEMPLATE_RATIONALE, MacroHedgeExecutor.sol:85) — keep verbatim.

Wave-1 SHILLER lib fns now available on RepresentativenessLib: `shillerSurprise`, `shillerOptionRatio`, `shillerStrikeTick`, `shillerWidth`, `shillerStale`, `SHILLER_MAX_STALENESS`. `surpriseOracle` immutable now wired (Wave-1 ctor migration). The fork harness `_demoMandate` (DemoMacroHedgeExecutor.fork.t.sol:411-419) uses the 0x6 PKE sentinel; `_init_world` THEN `_deployExecutor` order (:447-448); the existing PKE mint-at-360360 test is `test_resolveFromMandate_mintsThroughExecutor` (:446).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — extend the fork tree + failing branch/honesty tests (committed FIRST)</name>
  <read_first>contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol (:368-369 topic0, :411-419 mandate, :446-492 mint+honesty), contracts/test/fork/DemoMacroHedgeExecutor.fork.tree (if present)</read_first>
  <files>contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol</files>
  <behavior>
    - test_branch_shillerDiffersFromPke (onlyForked): build a SHILLER mandate (clone `_demoMandate` but `economicTheory = IMacroThesis(address(uint160(0x5)))`); seed `MockSurpriseOracle.set(actual,consensus,sigma)` for s=+2sigma (actual=consensus+2*sigma); `resolveFromMandate` mints; decode the SHILLER ExecutorDecided strikeTick and assert it == 361200 (s=+2 upside) AND != 360360 (the PKE anchor); `numberOfLegs(executor) > 0`.
    - test_executorDecided_perSchoolHonesty (onlyForked): run the SHILLER mandate AND the PKE mandate; decode both ExecutorDecided rationales; assert SHILLER rationale `_contains("Shiller")` AND `_contains("UNVALIDATED")`; assert PKE rationale `_contains("post-Keynesian")` (its existing string); assert BOTH `nonErgodicDisclosed == true`.
    - (regression, ALREADY exists, must stay green) test_resolveFromMandate_mintsThroughExecutor: the PKE mandate STILL mints EXACT 360360, rationale unchanged.
    - downside resolution (RESOLVE open-Q3): add test_branch_shillerDownsideFork (onlyForked) — seed s=-2sigma, `resolveFromMandate`, assert the K_lo mint either (A) SUCCEEDS with strikeTick==356100 and numberOfLegs>0 (downside fork-safe), OR if it reverts InputListFail, switch the impl to the depreciation-only-v1 fallback (s<0 -> minimal stance s=0 -> 360360) and assert THAT. Pick (A) first; document the chosen path in the SUMMARY.
  </behavior>
  <action>
Add the failing tests above to `DemoMacroHedgeExecutor.fork.t.sol` (reuse `_init_world`+`_deployExecutor`, the topic0 decode helper at :368-369, and the `_contains` string helper used at :466-492). Add the corresponding leaves to the fork `.tree` BTT spec (when SHILLER mandate / it mints a different strike; when per-school / it surfaces distinct honesty; when downside surprise / it [mints K_lo | falls to minimal stance]). These MUST fail before the Wave-2 branch impl exists.
COMMIT as a SEPARATE RED commit BEFORE the impl: `test(16-02): RED resolveFromMandate school-branch fork tests`. Ancestry-verify after GREEN.
  </action>
  <verify>
    <automated>cd contracts && bulloak check test/fork/DemoMacroHedgeExecutor.fork.tree 2>&1 | tail -2 && grep -q 'test_branch_shillerDiffersFromPke' test/fork/DemoMacroHedgeExecutor.fork.t.sol && grep -q 'test_executorDecided_perSchoolHonesty' test/fork/DemoMacroHedgeExecutor.fork.t.sol && grep -q 'test_branch_shillerDownsideFork' test/fork/DemoMacroHedgeExecutor.fork.t.sol</automated>
  </verify>
  <done>The new branch/honesty/downside fork tests are committed as a separate RED commit, present in the file + tree; they fail before the impl (no SHILLER arm yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — branch resolveFromMandate (SHILLER arm + verbatim PKE else) + per-school rationale</name>
  <read_first>contracts/src/MacroHedgeExecutor.sol (:197-259 resolveFromMandate, :274 sink, :85 TEMPLATE_RATIONALE, :94 event), .planning/phases/16-shiller-differentiated-representativeness/16-RESEARCH.md (§"The branch architecture")</read_first>
  <files>contracts/src/MacroHedgeExecutor.sol</files>
  <action>
Add a SHILLER TEMPLATE rationale constant (the REQUIRED honesty wording from 16-RESEARCH §Honesty):
```solidity
string private constant SHILLER_RATIONALE =
    "TEMPLATE: Shiller surprise-driven convex (s=(actual-consensus)/sigma); consensus/sigma are placeholders; the CPI-surprise->FX-move linkage is an UNVALIDATED empirical assumption, NOT a proven transfer function. Single-leg approximation of the Carr-Madan digital strip; non-ergodic tail disclosed, NOT covered.";
```
Wrap the body of `resolveFromMandate` in an `if (address(mandate.economicTheory) == address(uint160(0x5))) { SHILLER arm } else { PKE arm }`. The PKE arm = the EXISTING body (lines 201-258) moved VERBATIM into the else — byte-identical, NO edits inside it (the regression anchor).

The SHILLER arm:
1. `(int256 actual, int256 consensus, uint256 sigma, uint64 observedAt) = surpriseOracle.latestSurprise();`
2. staleness fail-safe: `int256 s = RepresentativenessLib.shillerStale(observedAt, block.timestamp) ? int256(0) : RepresentativenessLib.shillerSurprise(actual, consensus, sigma);` (sigma==0 only reachable when fresh, and shillerSurprise requires sigma>0 — when stale we never call it, so s=0 minimal stance is safe).
3. `uint256 size = RepresentativenessLib.shillerOptionRatio(s);`
4. `int24 strikeTick = RepresentativenessLib.shillerStrikeTick(s, TICK_SPACING);`
5. width: `int24 width = RepresentativenessLib.shillerWidth(s, baseVol, HORIZON_BLOCKS, TICK_SPACING);` and the same `vol` into PayoffTerms.vol (use the |s|-scaled vol the lib computes — expose it or recompute `baseVol*(1+|s|/WAD)`).
6. build `HedgeLegParams` (strikeWAD=0, the int24 path — same as PKE :227-243; economicTheory = mandate.economicTheory; isLong/chainId/underlyingMarket pass-through).
7. emit `ExecutorDecided(0, uint8(0)/*no regime on the SHILLER path; document as N/A*/, uint256(s<0?uint256(-s):uint256(s))/*surface |s| in the inflationAdjustment slot for the UI*/, strikeTick, width, true, RepresentativenessLib.nonErgodicDisclosed(), SHILLER_RATIONALE);`
8. mint through the SHARED `_resolveAndMintAtStrike(legParams, legIndex, positionSize, 0, strikeTick)` — UNCHANGED sink.

Downside (open-Q3): keep the s<0 K_lo path live first (test (A)). If the fork RED proves InputListFail on the K_lo leg-upper, add `if (s < 0) s = 0;` at the top of the arm (depreciation-only-v1 minimal stance) and document downside as a deferred stretch in the SUMMARY. Pick exactly one and make the downside test green.

COMMIT as GREEN: `feat(16-02): branch resolveFromMandate on economic school (SHILLER arm + verbatim PKE else)`. Ancestry-verify the RED commit per-file.
  </action>
  <verify>
    <automated>cd contracts && forge build 2>&1 | tail -3 && grep -q 'SHILLER_RATIONALE' src/MacroHedgeExecutor.sol && grep -q 'address(uint160(0x5))' src/MacroHedgeExecutor.sol && grep -q 'surpriseOracle.latestSurprise' src/MacroHedgeExecutor.sol && grep -q 'UNVALIDATED' src/MacroHedgeExecutor.sol</automated>
  </verify>
  <done>resolveFromMandate branches on the 0x5 sentinel; the SHILLER arm reads surpriseOracle + derives convex/sign/width via the lib + emits SHILLER_RATIONALE (Shiller/UNVALIDATED); the PKE else is byte-identical; build exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Prove on the Polygon fork — differentiation + honesty + PKE regression anchor</name>
  <read_first>contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol, contracts/.env.example (ALCHEMY_API_KEY), .planning/phases/16-shiller-differentiated-representativeness/16-RESEARCH.md (§Validation Architecture)</read_first>
  <files>contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol</files>
  <action>
Run the fork suite (needs `ALCHEMY_API_KEY` in gitignored `contracts/.env`; `onlyForked`-gated). Confirm the four claims:
1. `test_branch_shillerDiffersFromPke` green — SHILLER strike 361200 != PKE 360360, numberOfLegs>0.
2. `test_executorDecided_perSchoolHonesty` green — SHILLER rationale contains "Shiller"+"UNVALIDATED", PKE contains "post-Keynesian", both nonErgodic.
3. `test_branch_shillerDownsideFork` green per the chosen path (A K_lo mint OR depreciation-only-v1) — document which in the SUMMARY (open-Q3 resolved with the fork evidence).
4. `test_resolveFromMandate_mintsThroughExecutor` STILL green — PKE mints EXACT 360360, rationale unchanged (the regression anchor). Also re-run `test/instrument/Representativeness.t.sol` (17/17) and `MacroHedgeExecutor.onResult.t.sol` (4/4).
If any SHILLER fork test reverts unexpectedly, fix the arm (this is the integration-debug task), keeping the PKE else untouched. Record the exact tick + the downside decision in the SUMMARY.
  </action>
  <verify>
    <automated>cd contracts && forge test --match-test 'test_branch_shillerDiffersFromPke|test_executorDecided_perSchoolHonesty|test_branch_shillerDownsideFork|test_resolveFromMandate_mintsThroughExecutor' -vvv 2>&1 | tail -25</automated>
  </verify>
  <done>All four fork claims green (or the documented depreciation-only-v1 fallback for downside); PKE 360360 unchanged; Representativeness 17/17 + onResult 4/4 un-regressed.</done>
</task>

</tasks>

<verification>
- `cd contracts && forge test --match-test 'test_branch_shillerDiffersFromPke|test_executorDecided_perSchoolHonesty|test_branch_shillerDownsideFork' -vvv` — all green.
- `cd contracts && forge test --match-test test_resolveFromMandate_mintsThroughExecutor -vvv` — STILL 360360 (regression anchor).
- `cd contracts && forge test --match-path 'test/instrument/Representativeness.t.sol'` — 17/17.
- `cd contracts && forge test --match-path 'test/instrument/MacroHedgeExecutor.onResult.t.sol'` — 4/4.
- `cd contracts && forge build` — exit 0.
- Iron Law: the RED fork-test commit is an ancestor of the GREEN branch commit (per-file).
</verification>

<success_criteria>
- resolveFromMandate branches on the economic school; SHILLER (0x5) gets the surprise-driven convex/sign/width geometry; the PKE else is byte-identical.
- A SHILLER mandate mints a DIFFERENT strike than PKE's 360360 for the same input, fork-proven.
- ExecutorDecided carries the per-school TEMPLATE rationale (SHILLER=Shiller/UNVALIDATED, PKE=post-Keynesian), both nonErgodicDisclosed=true.
- The PKE regression anchor (360360 + unchanged rationale + 17/17 + 4/4) is un-regressed.
- The downside (K_lo) question is resolved concretely with fork evidence (open-Q3).
</success_criteria>

<output>
After completion, create `.planning/phases/16-shiller-differentiated-representativeness/16-02-SUMMARY.md`
</output>

---
phase: 16-shiller-differentiated-representativeness
plan: 03
type: tdd
wave: 3
depends_on: ["16-02"]
files_modified:
  - contracts/test/instrument/MacroWorkflow.tree
  - contracts/test/instrument/MacroWorkflow.fork.t.sol
autonomous: true
requirements: [SHILLER-02]
must_haves:
  truths:
    - "the suite joins Agent-1 (MacroHedgeStrategist + MockPlatform, in-VM) -> HedgeMandate -> Agent-2 (resolveFromMandate, Polygon fork) -> mint"
    - "4 Colombian macro-risk scenarios (CPI upside, CPI downside, fiscal-slippage tail, carry-unwind) run under BOTH schools (SHILLER 0x5 and PKE 0x6)"
    - "for the same scenario, SHILLER vs PKE produce DIFFERENT strike/width/size (the load-bearing differentiation)"
    - "both schools surface their per-school TEMPLATE honesty on ExecutorDecided and both MINT (numberOfLegs>0)"
  artifacts:
    - path: "contracts/test/instrument/MacroWorkflow.tree"
      provides: "the SHILLER-02 whole-workflow BTT spec"
      contains: "MacroWorkflow"
    - path: "contracts/test/instrument/MacroWorkflow.fork.t.sol"
      provides: "the 4-scenario x 2-school whole-workflow suite + the same-input-different-geometry assertion"
      contains: "test_workflow_sameInputDifferentGeometry"
  key_links:
    - from: "contracts/test/instrument/MacroWorkflow.fork.t.sol"
      to: "MacroHedgeStrategist.getMandate"
      via: "Agent-1 assembles the mandate (mocked inferString/inferNumber callbacks), the test bridges via getMandate"
      pattern: "getMandate"
    - from: "contracts/test/instrument/MacroWorkflow.fork.t.sol"
      to: "MacroHedgeExecutor.resolveFromMandate"
      via: "the assembled mandate drives the Agent-2 fork mint"
      pattern: "resolveFromMandate"
---

<objective>
Build the SHILLER-02 whole-workflow integration suite (`MacroWorkflow`): a single fork test contract that runs Agent-1 (`MacroHedgeStrategist` + `MockPlatform`, in-VM — no fork needed for the LLM legs) to assemble a `HedgeMandate`, bridges it via `getMandate`, then drives Agent-2 (`MacroHedgeExecutor.resolveFromMandate`) on the Polygon fork to mint. It runs 4 Colombian macro-risk scenarios under BOTH schools and asserts the load-bearing claim: for the SAME macro-risk input, SHILLER vs PKE produce DIFFERENT strike/width/size, with per-school TEMPLATE honesty on `ExecutorDecided` and a successful mint for both.

Purpose: this is the agent-interaction-layer proof (the layer just below the UI) that the school selection actually branches the geometry end-to-end — the differentiation surfaced by `/gsd:verify-work 15`.
Output: MacroWorkflow.tree + MacroWorkflow.fork.t.sol.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shiller-differentiated-representativeness/16-RESEARCH.md
@.planning/phases/16-shiller-differentiated-representativeness/16-VALIDATION.md
@.planning/phases/16-shiller-differentiated-representativeness/16-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The Agent-1 in-VM driving pattern + the Agent-2 fork harness. Use directly. -->

Agent-1 (contracts/src/instrument/MacroHedgeStrategist.sol):
- ctor `new MacroHedgeStrategist(address(platform), address(oracle))`.
- `requestSchoolDecision(string userIntent, bytes32 dataKey, int256 consensus) returns (bytes32 decisionId)` -> fires `inferString` over the registry `schoolLabels()` (SHILLER_MACRO_RISK / POST_KEYNESIAN).
- `requestNotionalDecision(bytes32 decisionId)` -> fires `inferNumber` (bounded [MIN_NOTIONAL, MAX_NOTIONAL]).
- callbacks delivered via `MockPlatform.fulfill(...)` (mirror MacroHedgeStrategist.t.sol: `_fireSchool`/`fulfill` deliver a school string; a second `fulfill` delivers the notional). After both, `StrategistDecided(decisionId, school, mandate)` emits.
- `getMandate(bytes32 decisionId) returns (HedgeMandate)` — the bridge. The mandate's `economicTheory` is the resolved 0x5/0x6 sentinel (from the school string the test feeds the inferString callback).
- The MockPlatform-driven harness is fork-FREE; instantiate it in the SAME VM as the fork test (Foundry runs both; the fork only affects the Panoptic-touching Agent-2 calls).

Agent-2 (contracts/src/MacroHedgeExecutor.sol, Wave-2 branched): `resolveFromMandate(HedgeMandate, legIndex, positionSize)` mints; reads `surpriseOracle` on the SHILLER arm. Deploy via the proven `_init_world` THEN `_deployExecutor` order (DemoMacroHedgeExecutor.fork.t.sol:447-448); decode `ExecutorDecided` via topic0 `keccak256("ExecutorDecided(uint256,uint8,uint256,int24,int24,bool,bool,string)")` (helper at :368-369). Seed `MockSurpriseOracle.set(actual,consensus,sigma)` per scenario for the SHILLER arm; the PKE arm reads `MockRegimeOracle` (set the matched regime).

Scenario matrix (TEMPLATE inputs, 16-RESEARCH §"Scenario matrix"):
| Scenario | SHILLER surprise | PKE regime | SHILLER strike |
|----------|------------------|------------|----------------|
| CPI upside | s=+2sigma | Stress | 361200 |
| CPI downside | s=-2sigma | Tranquil | 356100 (or minimal-stance per 16-02 downside decision) |
| fiscal-slippage tail | s=+3.5sigma | Stress | 361620, ratio~90 |
| carry-unwind | s=+3sigma | Stress | 361620, ratio~62 |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — MacroWorkflow.tree + failing whole-workflow suite (committed FIRST)</name>
  <read_first>contracts/test/instrument/MacroHedgeStrategist.t.sol (the MockPlatform.fulfill school/notional callback pattern), contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol (_init_world/_deployExecutor + topic0 decode), contracts/test/mocks/MockPlatform.sol</read_first>
  <files>contracts/test/instrument/MacroWorkflow.tree, contracts/test/instrument/MacroWorkflow.fork.t.sol</files>
  <behavior>
    - A helper `_runWorkflow(string userIntent, string school, int256 sActualOffsetSigmas, Regime pkeRegime) returns (int24 strike, int24 width, uint256 size, string rationale, bool nonErgodic)`: drives Agent-1 (requestSchoolDecision -> fulfill the school string -> requestNotionalDecision -> fulfill the notional) to assemble the mandate, reads it via getMandate, seeds the surprise/regime mocks for the scenario, calls Agent-2 resolveFromMandate on the fork, decodes ExecutorDecided.
    - test_workflow_sameInputDifferentGeometry (onlyForked): for each of the 4 scenarios, run `_runWorkflow` ONCE as SHILLER and ONCE as PKE (all other inputs equal); assert strike_shiller != strike_pke (e.g. CPI-upside: 361200 != 360360); assert size_shiller != size_pke OR width_shiller != width_pke (at least one geometry axis differs per scenario); assert BOTH minted (numberOfLegs>0 after each). ANTI-TAUTOLOGY (per the gate MAJOR — the differentiation must NOT be trivially satisfiable; PKE size is regime-invariant and SHILLER strike is ≠360360 by construction, so a stubbed school-keyed-constant would pass the above): (a) INTRA-SCHOOL monotonicity — within SHILLER, a larger |s| MOVES the geometry: assert SIZE STRICTLY increases between two upside scenarios (e.g. s=+2σ ratio 22 < s=+3.5σ ratio 90) — use SIZE, not strike, for the +3σ/+3.5σ pair since both floor to strike tick 361620; (b) FLIP-ONLY-THE-SENTINEL — seed the surprise + regime oracles so the geometry would otherwise coincide, then flip ONLY the mandate's `economicTheory` sentinel (0x5↔0x6, identical everything else); assert the geometry STILL differs — proving the SCHOOL branch (not merely the different oracle) drives it.
    - test_workflow_perSchoolHonesty (onlyForked): for one scenario, assert the SHILLER rationale _contains("Shiller")+"UNVALIDATED", the PKE rationale _contains("post-Keynesian"), both nonErgodic==true.
    - test_workflow_fourScenariosBothSchools (onlyForked): all 4 scenarios x 2 schools mint successfully (8 mints), each SHILLER strike on the sign-correct side of 360360 (upside scenarios > 360360; CPI-downside per the 16-02 downside decision).
  </behavior>
  <action>
Write `MacroWorkflow.tree` as a bulloak 0.9.2 BTT spec (when/it) covering: when same scenario across schools / it derives different geometry; when per-school / it surfaces distinct honesty; when 4 scenarios x 2 schools / each mints. Write `MacroWorkflow.fork.t.sol` with the helper + the 3 tests, mirroring the MockPlatform.fulfill Agent-1 driving (MacroHedgeStrategist.t.sol) joined to the fork Agent-2 harness (DemoMacroHedgeExecutor.fork.t.sol). Use the TEMPLATE scenario inputs from the matrix above. These MUST fail before the file is complete / against an un-wired harness.
COMMIT as a SEPARATE RED commit FIRST: `test(16-03): RED MacroWorkflow whole-workflow suite (tree + failing)`. Ancestry-verify after GREEN.
  </action>
  <verify>
    <automated>cd contracts && bulloak check test/instrument/MacroWorkflow.tree && grep -q 'test_workflow_sameInputDifferentGeometry' test/instrument/MacroWorkflow.fork.t.sol && grep -q 'test_workflow_fourScenariosBothSchools' test/instrument/MacroWorkflow.fork.t.sol && grep -q 'resolveFromMandate' test/instrument/MacroWorkflow.fork.t.sol && grep -q 'getMandate' test/instrument/MacroWorkflow.fork.t.sol</automated>
  </verify>
  <done>MacroWorkflow.tree passes bulloak check; the 3-test suite is committed RED and references the Agent-1 getMandate bridge + Agent-2 resolveFromMandate.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — run the whole-workflow suite on the Polygon fork, prove same-input-different-geometry</name>
  <read_first>contracts/test/instrument/MacroWorkflow.fork.t.sol, contracts/.env.example (ALCHEMY_API_KEY)</read_first>
  <files>contracts/test/instrument/MacroWorkflow.fork.t.sol</files>
  <action>
Run the `MacroWorkflow` suite on the Polygon fork (needs `ALCHEMY_API_KEY` in gitignored `contracts/.env`). Debug the Agent-1-in-VM -> getMandate -> Agent-2-fork join until green (this is the integration task — the harness wiring is the work, no new production code). Confirm:
1. `test_workflow_sameInputDifferentGeometry` green — each scenario's SHILLER strike/width/size differs from PKE's; both mint.
2. `test_workflow_perSchoolHonesty` green — per-school TEMPLATE rationale + both nonErgodic.
3. `test_workflow_fourScenariosBothSchools` green — 8 mints, SHILLER strikes on the sign-correct side.
Then run the FULL suite to confirm no regression: PKE `test_resolveFromMandate_mintsThroughExecutor` 360360, Representativeness 17/17, ShillerRepresentativeness 5/5, onResult 4/4. Record the per-scenario decoded (strike, width, size) pairs in the SUMMARY as the differentiation evidence.
  </action>
  <verify>
    <automated>cd contracts && forge test --match-path 'test/instrument/MacroWorkflow*' -vvv 2>&1 | tail -25 && forge test --match-test 'test_resolveFromMandate_mintsThroughExecutor' 2>&1 | tail -5</automated>
  </verify>
  <done>The MacroWorkflow suite is green on the Polygon fork (same-input-different-geometry + per-school honesty + 8 mints); the PKE 360360 anchor + the Wave-1/Phase-14 unit suites stay green.</done>
</task>

</tasks>

<verification>
- `cd contracts && bulloak check test/instrument/MacroWorkflow.tree` passes.
- `cd contracts && forge test --match-path 'test/instrument/MacroWorkflow*' -vvv` — all green (the 4-scenario x 2-school matrix + same-input-different-geometry).
- `cd contracts && forge test --match-test test_resolveFromMandate_mintsThroughExecutor` — PKE 360360 anchor still green.
- `cd contracts && forge test --match-path 'test/instrument/Representativeness.t.sol' && forge test --match-path 'test/instrument/ShillerRepresentativeness*'` — both unit suites green.
- Iron Law: the RED MacroWorkflow commit is an ancestor of the GREEN commit (per-file).
</verification>

<success_criteria>
- A whole-workflow suite joins Agent-1 (in-VM mock platform) -> HedgeMandate -> Agent-2 (Polygon fork mint) across 4 Colombian macro-risk scenarios x 2 schools.
- For the SAME scenario input, SHILLER vs PKE produce DIFFERENT strike/width/size (fork-proven differentiation — the load-bearing assertion).
- Per-school TEMPLATE honesty surfaces on ExecutorDecided; both schools mint (numberOfLegs>0).
- No regression: PKE 360360 + Representativeness 17/17 + ShillerRepresentativeness 5/5 + onResult 4/4.
</success_criteria>

<output>
After completion, create `.planning/phases/16-shiller-differentiated-representativeness/16-03-SUMMARY.md`
</output>

---
phase: 16-shiller-differentiated-representativeness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - contracts/src/interfaces/ISurpriseOracle.sol
  - contracts/test/mocks/MockSurpriseOracle.sol
  - contracts/src/libraries/Representativeness.sol
  - contracts/src/MacroHedgeExecutor.sol
  - contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol
  - contracts/test/fork/MacroHedgeExecutor.fork.t.sol
  - contracts/test/instrument/MacroHedgeExecutor.onResult.t.sol
  - contracts/test/instrument/ShillerRepresentativeness.tree
  - contracts/test/instrument/ShillerRepresentativeness.t.sol
autonomous: true
requirements: [SHILLER-01]
must_haves:
  truths:
    - "shillerSurprise(actual,consensus,sigma) returns (actual-consensus)/sigma in WAD, sign preserved"
    - "shillerOptionRatio is convex-monotone in |s|, clamped [1,127], exact integer outputs"
    - "shillerStrikeTick is sign-driven (s>0 -> tick>360360, s<0 -> tick<360360), all x60-aligned"
    - "shillerWidth grows with |s| and is even-snapped via volToWidth"
    - "stale/unset surprise oracle -> s=0 minimal stance"
    - "the executor compiles with the 10th ISurpriseOracle ctor arg across all 5 migrated sites"
  artifacts:
    - path: "contracts/src/interfaces/ISurpriseOracle.sol"
      provides: "latestSurprise() staleness contract mirroring IRegimeOracle"
      contains: "function latestSurprise"
    - path: "contracts/test/mocks/MockSurpriseOracle.sol"
      provides: "settable surprise mock mirroring MockRegimeOracle"
      contains: "function set"
    - path: "contracts/src/libraries/Representativeness.sol"
      provides: "shillerSurprise/shillerOptionRatio/shillerStrikeTick/shillerWidth + TEMPLATE constants"
      contains: "function shillerSurprise"
    - path: "contracts/test/instrument/ShillerRepresentativeness.t.sol"
      provides: "the SHILLER-01 pure-lib unit suite"
      contains: "test_shillerOptionRatio_convexMonotone"
  key_links:
    - from: "contracts/src/libraries/Representativeness.sol"
      to: "VolToWidthLib.volToWidth"
      via: "shillerWidth feeds |s|-scaled vol into the even-snapping width fn"
      pattern: "VolToWidthLib\\.volToWidth"
    - from: "contracts/src/libraries/Representativeness.sol"
      to: "structuralStrikeTick"
      via: "shillerStrikeTick reuses the Fix-C exact decimal-gap snap"
      pattern: "structuralStrikeTick"
    - from: "contracts/src/MacroHedgeExecutor.sol"
      to: "ISurpriseOracle"
      via: "immutable surpriseOracle wired in the 10-arg ctor"
      pattern: "ISurpriseOracle"
---

<objective>
Build the SHILLER-01 substrate: a new `ISurpriseOracle`/`MockSurpriseOracle` (mirroring the proven `IRegimeOracle`/`MockRegimeOracle` staleness contract), the four pure SHILLER lib fns on `RepresentativenessLib` (surprise, convex size, sign-driven strike, |s|-scaled width) with TEMPLATE-labeled constants, and the atomic 10-arg ctor migration that wires the surprise oracle into the executor across all 5 ctor sites.

Purpose: this is the pure, fork-free half of SHILLER-01. It establishes the contracts and arithmetic the Wave-2 branch consumes, keeps the suite compiling, and proves the convex/sign/width/staleness math with sub-second unit tests — WITHOUT touching the PKE path (additive only).
Output: ISurpriseOracle.sol, MockSurpriseOracle.sol, the SHILLER fns + constants on Representativeness.sol, the migrated 10-arg ctor + 5 call sites, and the ShillerRepresentativeness BTT spec + unit suite.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shiller-differentiated-representativeness/16-CONTEXT.md
@.planning/phases/16-shiller-differentiated-representativeness/16-RESEARCH.md
@.planning/phases/16-shiller-differentiated-representativeness/16-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- The exact patterns to mirror/extend. Use these directly — no codebase exploration needed. -->

The staleness contract to mirror (contracts/src/interfaces/IRegimeOracle.sol):
```solidity
interface IRegimeOracle {
    enum Regime { Unknown, Tranquil, Stress }
    function latestRegime() external view returns (Regime regime, uint64 observedAt);
}
```

The mock to mirror (contracts/test/mocks/MockRegimeOracle.sol): `set(...)` seeds a fresh `observedAt=block.timestamp` (or 1 if 0); `setStaleAt(..., ts)` pins an explicit `observedAt` (0=unset, old=stale).

The PKE lib primitives to REUSE verbatim (contracts/src/libraries/Representativeness.sol):
```solidity
uint256 internal constant WAD = 1e18;
uint256 internal constant DECIMAL_GAP = 1e12;
uint64 internal constant MAX_STALENESS = 1 hours; // PKE window — do NOT change
function structuralStrikeTick(uint256 humanRateCopPerUsd, int24 tickSpacing) internal pure returns (int24);
function feasibleOptionRatio(uint256 targetNotional) internal pure returns (uint256); // [1,127] clamp discipline
function nonErgodicDisclosed() internal pure returns (bool) { return true; }
// VolToWidthLib.volToWidth(uint88 vol, uint32 horizonBlocks, int24 tickSpacing) -> int24 (auto even-snap)
```
CANONICAL_COP_USD anchor (MacroHedgeExecutor.sol:81) = 3900; PKE K_hi = 4485 -> tick 360360.

The current ctor (MacroHedgeExecutor.sol:117-136), 9 args ending in `IRegimeOracle _regimeOracle`. The 5 ctor sites to migrate (grep-verified):
1. contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol:379 (`exec = new MacroHedgeExecutor(`)
2. contracts/test/fork/MacroHedgeExecutor.fork.t.sol:143 (`executor = new MacroHedgeExecutor(`)
3. contracts/test/fork/MacroHedgeExecutor.fork.t.sol:242 (`underfundedExecutor = new MacroHedgeExecutor(`)
4. contracts/test/fork/MacroHedgeExecutor.fork.t.sol:297 (`underfundedExecutor = new MacroHedgeExecutor(`)
5. contracts/test/instrument/MacroHedgeExecutor.onResult.t.sol:157 (`MacroHedgeExecutor(` — the DecodeProbe ctor chain-up; the probe ctor itself is at :38)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ISurpriseOracle + MockSurpriseOracle (mirror IRegimeOracle)</name>
  <read_first>contracts/src/interfaces/IRegimeOracle.sol, contracts/test/mocks/MockRegimeOracle.sol</read_first>
  <files>contracts/src/interfaces/ISurpriseOracle.sol, contracts/test/mocks/MockSurpriseOracle.sol</files>
  <action>
Create `ISurpriseOracle.sol` mirroring the `IRegimeOracle` staleness contract exactly:
```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;
/// @notice CPI-surprise inputs for the Shiller branch. signed-WAD actual & consensus, WAD sigma.
///         observedAt==0 => never set (the unset fail-safe). Mirrors IRegimeOracle's staleness contract.
interface ISurpriseOracle {
    function latestSurprise() external view
        returns (int256 actualWad, int256 consensusWad, uint256 sigmaWad, uint64 observedAt);
}
```
Create `MockSurpriseOracle.sol` mirroring `MockRegimeOracle`: storage `int256 _actual; int256 _consensus; uint256 _sigma; uint64 _observedAt;`. Add `set(int256 actual, int256 consensus, uint256 sigma)` -> stores the three values and seeds `_observedAt = uint64(block.timestamp == 0 ? 1 : block.timestamp)` (fresh/live). Add `setStaleAt(int256 actual, int256 consensus, uint256 sigma, uint64 ts)` -> stores values at the EXPLICIT `ts` (0 = unset, old = beyond staleness). `latestSurprise()` returns the four. An unseeded mock reads (0,0,0,0).
  </action>
  <verify>
    <automated>cd contracts && forge build 2>&1 | tail -3 && grep -q 'function latestSurprise' src/interfaces/ISurpriseOracle.sol && grep -q 'function set' test/mocks/MockSurpriseOracle.sol && grep -q 'function setStaleAt' test/mocks/MockSurpriseOracle.sol</automated>
  </verify>
  <done>ISurpriseOracle declares latestSurprise (4-tuple incl observedAt); MockSurpriseOracle has set + setStaleAt mirroring MockRegimeOracle; build exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — ShillerRepresentativeness.tree + failing unit suite (Iron Law, committed FIRST)</name>
  <read_first>contracts/test/instrument/Representativeness.t.sol, contracts/test/instrument/Representativeness.tree, .planning/phases/16-shiller-differentiated-representativeness/16-RESEARCH.md (§"The Shiller -> tick translation")</read_first>
  <files>contracts/test/instrument/ShillerRepresentativeness.tree, contracts/test/instrument/ShillerRepresentativeness.t.sol</files>
  <behavior>
    - test_shillerSurprise_wadMath: shillerSurprise(actualWad, consensusWad, sigmaWad) == (actual-consensus)*WAD/sigma; e.g. actual=4e18, consensus=3e18, sigma=1e18 -> s=+1e18; actual=2e18,consensus=3e18,sigma=1e18 -> s=-1e18; assert sign preserved and the WAD scale (s in units of sigma).
    - test_shillerOptionRatio_convexMonotone: EXACT integer outputs (mutation-resistant) at SCALE=10,k=0.5: s=1e18->2, s=1.5e18->10, s=2e18->22, s=2.5e18->40, s=3e18->62, s=3.5e18->90, s=4e18->122; s<=0.5e18->1 (below threshold floors to 1); never exceeds 127; monotone non-decreasing across the array. Assert with `assertEq` on each exact value.
    - test_shillerStrike_signDrivenAligned: shillerStrikeTick(+1e18,60)==360780; (+2e18,60)==361200; (+3e18,60)==361620; (-1e18,60)==356760; (-2e18,60)==356100; assert every result % 60 == 0; assert s>0 results > 360360 and s<0 results < 360360 (the PKE anchor).
    - test_shillerWidth_evenMonotone: shillerWidth(s, baseVol=14400, horizon=100, tickSpacing=60) returns an EVEN width (`width % 2 == 0`); width(2e18) >= width(1e18) >= width(0) (non-decreasing); AND at least one STRICT step `assertGt(width(2e18), width(0))` (per the gate MINOR — proves the widening is LIVE, not silently clamped flat at the 4095 even-width ceiling); width(0) == gbmBaselineWidth(14400,100,60) (s=0 floor == baseVol).
    - test_shillerStaleness_minimalStance: the staleness fail-safe maps stale/unset -> s=0 -> ratio 1 + base-15%-OTM strike (tick 360360 K_hi) + baseVol width. Exercise BOTH staleness sub-leaves (per the gate MINOR): (i) UNSET — `setStaleAt(...,0)` (observedAt==0) -> stale; (ii) AGED — `setStaleAt(..., ts)` with `ts < nowTs - SHILLER_MAX_STALENESS` (35 days) -> stale; both map to s=0 minimal stance. (Test against `shillerStale` / the lib's stale check returning s=0.)
  </behavior>
  <action>
Write `ShillerRepresentativeness.tree` as a bulloak 0.9.2 BTT spec (when/it form) covering the 5 leaves above. Then write `ShillerRepresentativeness.t.sol` with the 5 failing tests (the impl does not exist yet — they MUST fail to compile/run). Mirror `Representativeness.t.sol` structure (pure-lib, no fork, `import {RepresentativenessLib}`). Use the EXACT integer expectations from 16-RESEARCH §"The Shiller -> tick translation" (do NOT recompute loosely — assert the exact ticks 360780/361200/361620/356760/356100 and the exact ratios 2/10/22/40/62/90/122).
COMMIT this tree+failing-test as a SEPARATE commit BEFORE any impl (evm-TDD Iron Law): `test(16-01): RED ShillerRepresentativeness unit suite (tree + failing)`. Verify per-file ancestry after the GREEN commit lands (`git merge-base --is-ancestor <RED-commit> HEAD`).
  </action>
  <verify>
    <automated>cd contracts && bulloak check test/instrument/ShillerRepresentativeness.tree && (forge test --match-path 'test/instrument/ShillerRepresentativeness*' 2>&1 | grep -qiE 'fail|error|cannot find|undeclared' && echo "RED confirmed (fails before impl)")</automated>
  </verify>
  <done>The .tree passes `bulloak check`; the 5-test suite is committed as a separate RED commit and fails to compile/run (impl absent).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — SHILLER lib fns + TEMPLATE constants + 10-arg ctor migration</name>
  <read_first>contracts/src/libraries/Representativeness.sol, contracts/src/MacroHedgeExecutor.sol (ctor :117-136), contracts/src/libraries/VolToWidth.sol</read_first>
  <files>contracts/src/libraries/Representativeness.sol, contracts/src/MacroHedgeExecutor.sol, contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol, contracts/test/fork/MacroHedgeExecutor.fork.t.sol, contracts/test/instrument/MacroHedgeExecutor.onResult.t.sol</files>
  <action>
Add to `RepresentativenessLib` (ADDITIVE — do NOT touch any PKE fn). New TEMPLATE constants:
```solidity
uint256 internal constant SHILLER_K = 0.5e18;          // TEMPLATE: surprise must exceed 0.5 sigma
uint256 internal constant SHILLER_RATIO_SCALE = 10;    // TEMPLATE: convex size scale
uint256 internal constant SHILLER_OTM_SIGMA_BPS = 500; // TEMPLATE: +5% OTM per whole sigma
uint256 internal constant SHILLER_BASE_OTM_BPS = 1500; // 15% OTM base (== PKE K_hi)
uint256 internal constant SHILLER_CANONICAL_COP_USD = 3900; // mirror the executor anchor
uint64  internal constant SHILLER_MAX_STALENESS = 35 days;  // TEMPLATE: MONTHLY CPI cadence (NOT PKE's 1h) — resolves open-Q2; document the monthly rationale in NatSpec
```
Four pure fns (the exact bodies from 16-RESEARCH §"Shiller -> tick translation"):
- `shillerSurprise(int256 actualWad, int256 consensusWad, uint256 sigmaWad) returns (int256)` — `require(sigmaWad>0)`, `((actual-consensus)*int256(WAD))/int256(sigmaWad)`.
- `shillerOptionRatio(int256 sWad) returns (uint256)` — absS = |s|; if absS<=SHILLER_K return 1; excess=absS-SHILLER_K; raw=(SHILLER_RATIO_SCALE*excess*excess)/(WAD*WAD); clamp [1,127]. (UNIT CARE: divide by WAD*WAD — Pitfall 9.)
- `shillerStrikeTick(int256 sWad, int24 tickSpacing) returns (int24)` — absSigmas=|s|/WAD; pctBps=SHILLER_BASE_OTM_BPS+SHILLER_OTM_SIGMA_BPS*absSigmas; rate = s>=0 ? CANONICAL*(10000+pctBps)/10000 : CANONICAL*(10000-pctBps)/10000; return structuralStrikeTick(rate, tickSpacing). REUSE structuralStrikeTick — do NOT hand-roll.
- `shillerWidth(int256 sWad, uint256 baseVol, uint32 horizonBlocks, int24 tickSpacing) returns (int24)` — absSigmas=|s|/WAD; vol=baseVol*(1+absSigmas); return VolToWidthLib.volToWidth(uint88(vol), horizonBlocks, tickSpacing). REUSE volToWidth (auto even-snap) — do NOT hand-roll a snap.
Add a staleness helper for the minimal-stance test: `shillerStale(uint64 observedAt, uint256 nowTs) returns (bool)` = `observedAt==0 || nowTs-observedAt > SHILLER_MAX_STALENESS`. (The Wave-2 arm calls this; the unit test asserts stale -> caller uses s=0.)

NOTE the CPI->FX magnitude is STRUCTURE-ONLY (open-Q1): tests assert monotonicity/sign/alignment/exact-template-output, NEVER a real FX magnitude. Keep every constant TEMPLATE-labeled in NatSpec.

Then the ATOMIC ctor migration (ONE compiling commit, the Phase-14 precedent): add `ISurpriseOracle _surpriseOracle` as the 10th ctor arg of `MacroHedgeExecutor` (append AFTER `_regimeOracle`), add the `ISurpriseOracle public immutable surpriseOracle;` field + `surpriseOracle = _surpriseOracle;` assignment, import ISurpriseOracle. Migrate all 5 enumerated sites to pass a 10th arg: the fork sites (1-4) that will run SHILLER mandates in later waves pass `new MockSurpriseOracle()`; the onResult DecodeProbe (site 5) never runs the mandate path so `ISurpriseOracle(address(0))` is safe there. PKE arm untouched — surpriseOracle is read ONLY in the (future) SHILLER arm.

COMMIT as GREEN: `feat(16-01): SHILLER lib fns + TEMPLATE constants + 10-arg ctor migration`. Then verify the RED commit is an ancestor (per-file Iron Law).
  </action>
  <verify>
    <automated>cd contracts && forge build 2>&1 | tail -3 && forge test --match-path 'test/instrument/ShillerRepresentativeness*' -vvv 2>&1 | tail -15 && grep -q 'function shillerSurprise' src/libraries/Representativeness.sol && grep -q 'SHILLER_MAX_STALENESS = 35 days' src/libraries/Representativeness.sol && grep -c 'new MockSurpriseOracle\|ISurpriseOracle(address(0))' test/fork/DemoMacroHedgeExecutor.fork.t.sol test/fork/MacroHedgeExecutor.fork.t.sol test/instrument/MacroHedgeExecutor.onResult.t.sol</automated>
  </verify>
  <done>The 5 SHILLER unit tests pass GREEN; build exit 0; all 4 SHILLER fns + the 6 TEMPLATE constants present; the 35-day SHILLER staleness window set (open-Q2 resolved); the 10-arg ctor compiles across all 5 migrated sites; PKE fns byte-unchanged.</done>
</task>

</tasks>

<verification>
- `cd contracts && bulloak check test/instrument/ShillerRepresentativeness.tree` passes.
- `cd contracts && forge test --match-path 'test/instrument/ShillerRepresentativeness*' -vvv` — 5/5 green.
- `cd contracts && forge test --match-path 'test/instrument/Representativeness.t.sol'` — PKE 17/17 STILL green (no-regression; SHILLER fns additive).
- `cd contracts && forge build` — exit 0 (the 10-arg ctor compiles across all 5 sites).
- `git merge-base --is-ancestor <RED-commit> HEAD` per the two new test files (Iron Law).
</verification>

<success_criteria>
- ISurpriseOracle + MockSurpriseOracle exist mirroring the regime-oracle staleness contract.
- The 4 SHILLER lib fns produce the EXACT template outputs (ratios 2/10/22/40/62/90/122; ticks 360780/361200/361620/356760/356100; even widths).
- The SHILLER staleness window is 35 days (monthly CPI, TEMPLATE), distinct from PKE's 1h (open-Q2 resolved); CPI->FX asserted STRUCTURE-only (open-Q1).
- The 10-arg ctor migration is one compiling commit across the 5 enumerated sites; PKE path byte-identical.
- evm-TDD: the RED tree+failing-test committed before the GREEN impl, ancestry-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/16-shiller-differentiated-representativeness/16-01-SUMMARY.md`
</output>

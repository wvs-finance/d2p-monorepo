---
phase: 08-longgammawrapper-cash-flow
plan: 06
type: execute
wave: 6
depends_on: ["08-05"]
files_modified:
  - contracts/test/instrument/LongGammaWrapper.settleLong.tree
  - contracts/test/instrument/LongGammaWrapper.forceExercise.tree
  - contracts/test/instrument/LongGammaWrapper.liquidation.tree
  - contracts/test/instrument/LongGammaWrapper.settleLong.t.sol
  - contracts/test/instrument/LongGammaWrapper.forceExercise.t.sol
  - contracts/test/instrument/LongGammaWrapper.liquidation.t.sol
autonomous: true
requirements: [WRAP-03]
must_haves:
  truths:
    - "settleLongPremium (dispatchFrom toLen==finalLen, solvent) erodes the wrapper while it stays Open (numberOfLegs unchanged); syncResidual emits ResidualEroded; residual recomputed from surviving"
    - "forceExercise (dispatchFrom finalLen==toLen-1, solvent, out-of-range long) burns the leg -> numberOfLegs->0 -> Closed; claimResidual pays max(surviving-cost,0); ResidualEroded fires"
    - "liquidation (dispatchFrom finalLen==0, insolvent at all 4 ticks) burns all -> Closed; residual floors at 0 if surviving wiped; the wrapper never pays more than it HELD pre-claim; insolvency is driven by a CONCRETE first-class mechanism (vm.store collateral-shrink applied to BOTH ct0 AND ct1, slot derived via forge inspect storage-layout NOT slot 0 which is _internalSupply, preferred; or a multi-block bounded-swap loop under tickDeltaLiquidation) — a single large swap trips StaleOracle (L1388) and is rejected; vm-assist is permitted (highest-risk fork task)"
    - "in every involuntary branch the wrapper share-burn never exceeds holdings (no NotEnoughTokens swallowed) and the residual is read live from convertToAssets(balanceOf(wrapper))"
  artifacts:
    - path: "contracts/test/instrument/LongGammaWrapper.settleLong.tree"
      provides: "BTT spec: settle erodes while Open, committed BEFORE its t.sol"
      contains: "settleLong"
    - path: "contracts/test/instrument/LongGammaWrapper.forceExercise.tree"
      provides: "BTT spec: forceExercise closes + residual from surviving"
      contains: "forceExercise"
    - path: "contracts/test/instrument/LongGammaWrapper.liquidation.tree"
      provides: "BTT spec: liquidation closes + residual floors at zero"
      contains: "liquidation"
    - path: "contracts/test/instrument/LongGammaWrapper.liquidation.t.sol"
      provides: "fork test driving insolvency then dispatchFrom finalLen==0"
      contains: "test_liquidation_residualFloorZero"
  key_links:
    - from: "contracts/test/instrument/LongGammaWrapper.settleLong.t.sol"
      to: "panoptic-borrowed PanopticPool.dispatchFrom (via IPanopticData)"
      via: "pool.dispatchFrom(callerList, wrapper, [tokenId], [tokenId], usePremiaLR) — toLen==finalLen"
      pattern: "dispatchFrom"
    - from: "contracts/test/instrument/LongGammaWrapper.liquidation.t.sol"
      to: "panoptic-borrowed PanopticPool.dispatchFrom (via IPanopticData)"
      via: "pool.dispatchFrom(liqList, wrapper, [tokenId], new TokenId[](0), usePremiaLR) — finalLen==0, insolvent"
      pattern: "dispatchFrom"
---

<objective>
Prove WRAP-03's three involuntary-close branches, all routed through `dispatchFrom` (PanopticPool has NO standalone forceExercise/settle/liquidate functions — Pattern 6). Three trees (committed before their tests) + three fork tests. The wrapper's CODE does not execute during a third-party `dispatchFrom` (it is the `account`, not the caller), so each test drives the branch on the pool, then calls the wrapper's `syncResidual()`/`claimResidual()` to reconcile and assert the surviving-collateral residual + `ResidualEroded`. settle is a mid-life EROSION (stays Open); forceExercise + liquidation are terminal CLOSES.

Purpose: WRAP-03 requires each involuntary branch with a committed `.tree` and a fork test where `residual = max(surviving - realizedCosts, 0)`, the wrapper never pays more than it holds (structurally enforced — settleBurn reverts NotEnoughTokens when shares < debit), and `ResidualEroded` fires on involuntary debit. The settle-stays-Open distinction is load-bearing (CONTEXT N1/B1; `_settlePremium` never calls `_burnOptions`).
This plan is TEST-ONLY: it consumes the involuntary-close detection (`_reconcile` Open->Closed promotion) that Plan 05 authored in `LongGammaWrapper.sol`; it modifies NO source contract.
Output: settleLong/forceExercise/liquidation trees (committed first) + their three `.t.sol` green on the fork.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-longgammawrapper-cash-flow/08-CONTEXT.md
@.planning/phases/08-longgammawrapper-cash-flow/08-RESEARCH.md

<interfaces>
<!-- Use directly. -->

IPanopticData.dispatchFrom(TokenId[] positionIdListFrom, address account, TokenId[] positionIdListTo, TokenId[] positionIdListToFinal, LeftRightUnsigned usePremiaAsCollateral) external payable.
DISAMBIGUATION (PanopticPool.sol L1360-1476, verified):
  - settleLongPremium: account SOLVENT at all 4 ticks AND toLen==finalLen (same list) -> _settlePremium (L1671). NO _burnOptions => numberOfLegs UNCHANGED => wrapper stays Open. Caller = any seller of the chunk.
  - forceExercise: account SOLVENT AND toLen==finalLen+1 (final one shorter) AND the long is out-of-range exercisable -> _forceExercise (L1598, _burnOptions L1611) => numberOfLegs->0 => Closed. Caller = any exercisor.
  - liquidation: account INSOLVENT at all 4 ticks AND finalLen==0 -> _liquidate (L1482) burns all => Closed. Caller = any liquidator.
usePremiaAsCollateral here is LeftRightUnsigned (rightSlot=callee flag, leftSlot=caller flag) — NOT a bool. Pass LeftRightUnsigned.wrap(0) for the demo unless a branch needs the flag.

LongGammaWrapper (Plans 03/04/05): deposit/recordStreamia/close/syncResidual/claimResidual all implemented. After an involuntary debit the test calls wrapper.syncResidual() (emits ResidualEroded if surviving dropped) and, once numberOfLegs==0, wrapper.claimResidual().
LongGammaWrapperBase (Plan 02): deployedPool/ct0/ct1/token0/token1/poolKey/swapHelper/seller/sellerShortId/_longTokenId/_oneLegArgs/_closeSellerShort/LONG_SIZE/SELLER_SIZE/chunkStrike/getOracleTicks-via-IPanopticData.

STORAGE LAYOUT (M2, source-verified — load-bearing for the Task-3 vm.store):
  CollateralTracker inherits ERC20Minimal (tokens/ERC20Minimal.sol): `uint256 internal _internalSupply;` is the FIRST state var (storage SLOT 0), and `mapping(address => uint256) public balanceOf;` is the SECOND (storage SLOT 1). A naive `vm.store` to slot 0 corrupts total-supply accounting, NOT the wrapper's balance. The `balanceOf[wrapper]` slot is `keccak256(abi.encode(address(wrapper), uint256(BAL_SLOT)))` where BAL_SLOT is the storage index of the `balanceOf` mapping — which the executor MUST DERIVE via `forge inspect CollateralTracker storage-layout` (or probe-and-verify by reading back `IERC20(address(ct)).balanceOf(wrapper)` after the store) rather than hard-coding. It is NOT slot 0. Convert assets shrink to a SHARE target via `ct.convertToShares(...)` before writing (balanceOf holds SHARES, and `convertToAssets(balanceOf)` is what `assetsAndInterest` reads).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: settleLong.tree + forceExercise.tree + liquidation.tree — BTT specs (Iron Law, trees FIRST)</name>
  <files>contracts/test/instrument/LongGammaWrapper.settleLong.tree, contracts/test/instrument/LongGammaWrapper.forceExercise.tree, contracts/test/instrument/LongGammaWrapper.liquidation.tree</files>
  <read_first>
    - contracts/test/instrument/LongGammaWrapper.close.tree (Plan 05 — parseable style to mirror)
    - contracts/test/instrument/PanopticDataSeam.fork.tree (counter-evidence: bulloak 0.9.2 parses prose `it`/`when` labels and `::` fine — there is NO no-dot/slash rule)
    - 08-CONTEXT.md section domain "Close vs erosion" (settle stays Open; forceExercise single-leg closes; liquidation closes) + section decisions "Events" (ResidualEroded on involuntary debit; cause coarse label)
    - 08-RESEARCH.md Pattern 6 (the three dispatchFrom signatures + disambiguation table) + Pitfall 5 (solvency gating) + Open Question 2 (driving insolvency)
    - 08-VALIDATION.md WRAP-03 forceExercise/settleLong/liquidation rows (the match-test names + Nyquist signals)
  </read_first>
  <action>
    Create THREE trees. Commit all three as ONE commit BEFORE Task 2/3. Suggested message: `test(08-06): add settleLong + forceExercise + liquidation trees (WRAP-03 involuntary, tree-before-impl)`.

    `LongGammaWrapper.settleLong.tree`, root `LongGammaWrapperSettleLong`:
    ```
    LongGammaWrapperSettleLong
    └── when a seller settles the wrappers long premium via dispatchFrom with equal to and final lists
        ├── it erodes the wrapper collateral while the position stays Open
        ├── it leaves numberOfLegs of the wrapper unchanged and greater than zero
        └── it emits ResidualEroded from syncResidual when surviving dropped
    ```

    `LongGammaWrapper.forceExercise.tree`, root `LongGammaWrapperForceExercise`:
    ```
    LongGammaWrapperForceExercise
    └── when an exercisor force exercises the out of range long via dispatchFrom with a shorter final list
        ├── it burns the long leg and drives numberOfLegs of the wrapper to zero
        ├── it transitions the wrapper to Closed so claimResidual can run
        ├── it pays a residual equal to max of surviving minus cost and zero
        └── it emits ResidualEroded for the involuntary debit and never pays more than holdings
    ```

    `LongGammaWrapper.liquidation.tree`, root `LongGammaWrapperLiquidation`:
    ```
    LongGammaWrapperLiquidation
    └── when the wrapper is insolvent at all four ticks and a liquidator calls dispatchFrom with an empty final list
        ├── it burns all positions and drives numberOfLegs of the wrapper to zero
        ├── it floors the residual at zero when surviving collateral is wiped
        └── it never lets the wrapper pay more than it holds
    ```
    Each leaf a single `it ...` line.
  </action>
  <verify>
    <automated>cd contracts && for t in settleLong forceExercise liquidation; do bulloak scaffold test/instrument/LongGammaWrapper.$t.tree >/dev/null 2>&1 && echo "$t=PARSE_OK" || echo "$t=PARSE_FAIL"; done</automated>
  </verify>
  <acceptance_criteria>
    - REAL PARSE GATE on all THREE trees (NOT `$?` — `bulloak check` exits 0 even on a genuine parse error): `for t in settleLong forceExercise liquidation; do bulloak scaffold test/instrument/LongGammaWrapper.$t.tree >/dev/null 2>&1 && echo "$t=PARSE_OK" || echo "$t=PARSE_FAIL"; done` prints `<t>=PARSE_OK` for all three (scaffold exits 0 ONLY when the tree parses). Equivalent per tree: `bulloak check <tree> 2>&1 | grep -qi "error occurred while parsing" && echo PARSE_FAIL || echo PARSE_OK`. The absent companion `.t.sol` files do NOT affect this parse gate.
    - `grep -c "stays Open\|numberOfLegs of the wrapper unchanged" contracts/test/instrument/LongGammaWrapper.settleLong.tree` returns `2`.
    - `grep -c "numberOfLegs of the wrapper to zero" contracts/test/instrument/LongGammaWrapper.forceExercise.tree contracts/test/instrument/LongGammaWrapper.liquidation.tree` returns `2`.
    - All three trees committed as a standalone commit BEFORE any involuntary `.t.sol` commit.
  </acceptance_criteria>
  <done>The three involuntary trees exist, bulloak-parseable, encode the settle-stays-Open vs forceExercise/liquidation-close distinction + the residual-floor + never-pays-more-than-holdings invariants, committed before their tests.</done>
</task>

<task type="auto">
  <name>Task 2: settleLong.t.sol + forceExercise.t.sol — the two SOLVENT dispatchFrom branches (test-only; consumes the Plan-05 involuntary-close detection)</name>
  <files>contracts/test/instrument/LongGammaWrapper.settleLong.t.sol, contracts/test/instrument/LongGammaWrapper.forceExercise.t.sol</files>
  <read_first>
    - contracts/test/instrument/LongGammaWrapper.settleLong.tree + forceExercise.tree (Task 1 — full-stem co-location)
    - contracts/test/instrument/LongGammaWrapper.claimResidual.t.sol (Plan 05 — the open+swap+close+claim flow to reuse; _closeSellerShort before claim)
    - contracts/src/instrument/LongGammaWrapper.sol (Plan 05 — `_reconcile()` ALREADY contains the Open->Closed involuntary-close promotion `if (state == State.Open && pool.numberOfLegs(address(this)) == 0) state = State.Closed;`; this plan is TEST-ONLY and consumes it, it does NOT edit the src)
    - contracts/test/instrument/LongGammaWrapperBase.sol (Plan 02 — seller/sellerShortId/_longTokenId/_oneLegArgs/swapHelper/poolKey)
    - 08-RESEARCH.md Pattern 6 (settle: toLen==finalLen, solvent; forceExercise: finalLen==toLen-1, solvent, out-of-range long) + Code Examples + Pitfall 5
    - panoptic-borrowed/PanopticPool.sol L1671 (_settlePremium — NO _burnOptions, stays Open), L1598-1664 (_forceExercise — _burnOptions L1611, refund), L1433-1434 (`tokenId.countLongs()==0 || tokenId.validateIsExercisable()==0` => `NoLegsExercisable` — the out-of-range precondition) ; 08-VALIDATION settleLong/forceExercise rows
  </read_first>
  <action>
    Create both `.t.sol` (SPDX MIT, pragma `^0.8.24`), each `is LongGammaWrapperBase`, full-stem co-located, bulloak-branch-fn delegation to a shared proof. Import the base, `LongGammaWrapper`, `IPanopticData`, `IERC20`, `TokenId`, `LeftRightUnsigned`.

    `settleLong.t.sol` shared proof `test_settleLong_residualFromSurviving()`:
    ```
    // open + swap to accrue long premium (reuse the Plan 05 open+swap block)
    LongGammaWrapper wrapper = ...deploy + deposit(user, 1e12, 1e12, _longTokenId(), LONG_SIZE, limits)...;
    ...swapHelper.swapExactIn(poolKey, true, -1e9); swapHelper.swapExactIn(poolKey, false, -1e9);...
    uint256 survBefore = ct0.convertToAssets(IERC20(address(ct0)).balanceOf(address(wrapper)));
    uint256 legsBefore = IPanopticData(deployedPool).numberOfLegs(address(wrapper));

    // SETTLE: a seller settles the wrapper's long premium — dispatchFrom with toList == finalList (equal)
    TokenId[] memory wrap1 = new TokenId[](1); wrap1[0] = _longTokenId();
    TokenId[] memory callerList = new TokenId[](1); callerList[0] = sellerShortId;
    vm.prank(seller);
    IPanopticData(deployedPool).dispatchFrom(callerList, address(wrapper), wrap1, wrap1, LeftRightUnsigned.wrap(0));

    // settle is mid-life EROSION: position STAYS Open (numberOfLegs unchanged), surviving may drop
    assertEq(IPanopticData(deployedPool).numberOfLegs(address(wrapper)), legsBefore, "settle does not close");
    assertGt(IPanopticData(deployedPool).numberOfLegs(address(wrapper)), 0, "still Open");

    // syncResidual reconciles + emits ResidualEroded if surviving dropped
    vm.recordLogs();
    wrapper.syncResidual();
    // assert ResidualEroded present IF survAfter < survBefore (capture survAfter; if equal, assert no false erosion)
    uint256 survAfter = ct0.convertToAssets(IERC20(address(ct0)).balanceOf(address(wrapper)));
    if (survAfter < survBefore) { /* assert ResidualEroded in logs */ }
    assertEq(uint8(wrapper.state()), uint8(1), "wrapper still Open after settle");   // State.Open
    ```
    Note: settle requires the seller's `dispatchFrom` with the SAME to/final list (hashes equal, L1430). If the demo's available long premium is zero (cap), settle is a no-op erosion — assert the STATE invariant (stays Open, numberOfLegs unchanged) unconditionally, and the ResidualEroded only when surviving actually dropped (the load-bearing claim is settle-stays-Open, NOT a forced erosion magnitude).

    KNOWN COVERAGE GAP — `ResidualEroded`-on-settle may NOT fire (m1, MUST be flagged in the test as a NOTE):
    the `ResidualEroded` assertion is (correctly) CONDITIONAL on `survAfter < survBefore` because the demo's
    available long premium may settle to ZERO (Pitfall 3 — owed vs available cap). That conditionality is honest,
    but it means the `syncResidual -> ResidualEroded` erosion-observability SEAM (which Phase 9 consumes as its
    erosion signal) MAY NEVER ACTUALLY FIRE in this suite. Add a `// NOTE(m1): coverage gap — the settle test
    proves settle-stays-Open UNCONDITIONALLY, but the ResidualEroded emission FROM settle is only exercised when
    the demo's long premium settles NON-ZERO; if survAfter==survBefore the event is not observed here, so this
    is NOT proven event coverage for settle.` comment so it is not mistaken for proven coverage. PREFER, if
    feasible within the fee-seeding already planned, to OVER-SEED the swaps (more/larger fee-generating swaps
    before the settle, and/or a larger seller short so more premium is available to settle) so the settle erosion
    is non-zero and `ResidualEroded` GENUINELY FIRES — but the UNCONDITIONAL settle-stays-Open state assertion
    stays the load-bearing one; the ResidualEroded check stays conditional even when over-seeding makes it likely.
    (forceExercise + liquidation DO fire ResidualEroded reliably — they wipe legs and surviving drops — so the
    seam IS covered there; the gap is specifically settle's non-terminal erosion.)

    `forceExercise.t.sol` shared proof `test_forceExercise_residualFromSurviving()`:
    ```
    // open the long, then MOVE PRICE via swaps so the long chunk is OUT OF RANGE exercisable (Pitfall 5)
    ...open wrapper...
    // push the price away from the chunk so the long becomes out-of-range; confirm via getCurrentTick/getOracleTicks
    swapHelper.swapExactIn(poolKey, /*direction that moves price past chunkStrike*/ true, -int256(BIG));
    // (prototype the swap size/direction until getCurrentTick crosses chunkStrike + the chunk is out of range)

    // PRECONDITION (m2): forceExercise's dispatchFrom branch requires `tokenId.validateIsExercisable() != 0`
    // (PanopticPool.sol L1433-1434) or it reverts `NoLegsExercisable`. Assert the long is OUT-OF-RANGE
    // exercisable BEFORE the dispatchFrom so a NoLegsExercisable revert is DIAGNOSED, not silently retried:
    int24 curTick = IPanopticData(deployedPool).getCurrentTick();
    assertTrue(curTick > chunkStrike + tickSpacing || curTick < chunkStrike - tickSpacing,
        "long must be out-of-range (getCurrentTick crossed chunkStrike +/- width) before forceExercise");

    uint256 survBefore = ct0.convertToAssets(IERC20(address(ct0)).balanceOf(address(wrapper)));
    // FORCE EXERCISE: dispatchFrom with finalList one element SHORTER (the exercised position removed)
    TokenId[] memory wrap1 = new TokenId[](1); wrap1[0] = _longTokenId();
    TokenId[] memory emptyFinal = new TokenId[](0);
    TokenId[] memory exList = new TokenId[](1); exList[0] = sellerShortId;   // exercisor caller list
    vm.prank(makeAddr("exercisor"));
    IPanopticData(deployedPool).dispatchFrom(exList, address(wrapper), wrap1, emptyFinal, LeftRightUnsigned.wrap(0));

    // forceExercise CLOSES the single-leg long
    assertEq(IPanopticData(deployedPool).numberOfLegs(address(wrapper)), 0, "forceExercise closed the long");
    // the wrapper's code did NOT run during the third-party dispatchFrom, so `state` is still Open. The
    // involuntary-close detection ALREADY lives in Plan-05's `_reconcile()`, so a permissionless
    // `syncResidual()` promotes Open->Closed; then claimResidual can run.
    _closeSellerShort();             // free the pool-wide redeem cap before the claim (B4)
    wrapper.syncResidual();          // promotes Open->Closed via the Plan-05 _reconcile detection
    assertEq(uint8(wrapper.state()), uint8(2), "Closed after involuntary forceExercise");  // State.Closed
    uint256 u0pre = IERC20(token0).balanceOf(wrapper.user());
    wrapper.claimResidual();         // caller-agnostic; payout goes to the stored user
    assertLe(IERC20(token0).balanceOf(wrapper.user()) - u0pre, survBefore + 1, "residual <= surviving (never overpays)");
    // ResidualEroded fires on the involuntary debit (assert present in the syncResidual/claim logs).
    ```
    DESIGN NOTE (the enabler is ALREADY in src — this plan is TEST-ONLY): the involuntary-close gap — `claimResidual()` gates on `state == Closed`, but an involuntary close (forceExercise/liquidation) drops `numberOfLegs` to 0 WITHOUT running wrapper code, so `state` would stay `Open` — is resolved in Plan 05, whose `_reconcile()` already promotes `state` Open->Closed when it observes `numberOfLegs(wrapper)==0`. This plan does NOT edit `LongGammaWrapper.sol`; it merely CONSUMES that promotion: after the third-party `dispatchFrom`, the test calls `_closeSellerShort(); wrapper.syncResidual();` (which promotes Open->Closed via the Plan-05 detection) then `wrapper.claimResidual();`, asserting `residual <= survBefore` paid to user + `ResidualEroded` fired. PRECONDITION (no-regression): confirm the Plan-05 src promotion line is present before relying on it — if absent, this is a Plan-05 gap to fix there, NOT a src edit in this plan.
  </action>
  <verify>
    <automated>cd contracts && forge test --match-path "test/instrument/LongGammaWrapper.settleLong.t.sol" --fork-url "$BASE_RPC_URL" 2>&1 | tail -10; forge test --match-path "test/instrument/LongGammaWrapper.forceExercise.t.sol" --fork-url "$BASE_RPC_URL" 2>&1 | tail -10; forge test --match-path "test/instrument/LongGammaWrapper.claimResidual.t.sol" --fork-url "$BASE_RPC_URL" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd contracts && forge test --match-test test_settleLong_residualFromSurviving --fork-url "$BASE_RPC_URL"` exits 0 (settle leaves numberOfLegs unchanged + wrapper stays Open — the load-bearing settle-stays-Open invariant).
    - `cd contracts && forge test --match-test test_forceExercise_residualFromSurviving --fork-url "$BASE_RPC_URL"` exits 0 (numberOfLegs->0; after syncResidual promotes to Closed, claimResidual pays surviving-derived residual + ResidualEroded fires).
    - m1 COVERAGE-GAP NOTE present: `grep -c "NOTE(m1): coverage gap" contracts/test/instrument/LongGammaWrapper.settleLong.t.sol` returns `1` (the conditional ResidualEroded-on-settle is flagged, not mistaken for proven event coverage).
    - `cd contracts && forge test --match-path "test/instrument/LongGammaWrapper.claimResidual.t.sol" --fork-url "$BASE_RPC_URL"` STILL green (the `_reconcile` close-detection addition did not regress the voluntary path).
    - PRECONDITION (consumed from Plan 05, NOT authored here): `grep -n "if (state == State.Open && pool.numberOfLegs(address(this)) == 0) state = State.Closed;" contracts/src/instrument/LongGammaWrapper.sol` matches — the involuntary-close detection landed in 08-05; this plan does not modify the src.
    - `grep -c "panoptic-borrowed" contracts/test/instrument/LongGammaWrapper.settleLong.t.sol contracts/test/instrument/LongGammaWrapper.forceExercise.t.sol` returns `0` for both (and NO src edit: this plan touches no `.sol` source).
  </acceptance_criteria>
  <done>settleLong + forceExercise tests pass (TEST-ONLY, no src edit): settle erodes while the wrapper stays Open (numberOfLegs unchanged) with the conditional ResidualEroded-on-settle coverage gap flagged in a NOTE; forceExercise closes (numberOfLegs->0), syncResidual promotes Open->Closed via the Plan-05 _reconcile detection (consumed here, authored in 08-05), claimResidual pays a surviving-derived residual; the Plan-05 voluntary claim test is not regressed.</done>
</task>

<task type="auto">
  <name>Task 3: liquidation.t.sol — drive insolvency then dispatchFrom with empty final list</name>
  <files>contracts/test/instrument/LongGammaWrapper.liquidation.t.sol</files>
  <read_first>
    - contracts/test/instrument/LongGammaWrapper.liquidation.tree (Task 1 — full-stem co-location)
    - contracts/test/instrument/LongGammaWrapper.forceExercise.t.sol (Task 2 — the dispatchFrom + syncResidual->Closed + claimResidual pattern + the price-driving swap approach to reuse)
    - contracts/panoptic-borrowed/tokens/ERC20Minimal.sol (storage layout — `_internalSupply` is slot 0, `balanceOf` is slot 1; the vm.store slot-derivation hazard below)
    - 08-RESEARCH.md Pattern 6 liquidation row (insolvent at all 4 ticks + finalLen==0 -> _liquidate) + Open Question 2 (driving insolvency: large adverse swap and/or sizeable position-to-collateral ratio) + Pitfall 5
    - 08-CONTEXT.md section Discretion "Fork-state manipulation to DRIVE each involuntary branch (insolvent-at-all-ticks for liquidation)" (highest-risk fork task; budget iterations + sizeable-position-vs-collateral ratio) + 08-VALIDATION Manual-Only (documented vm-assisted setup permitted)
    - panoptic-borrowed/PanopticPool.sol L1482 (_liquidate burns all + liquidator bonus), L1453-1461 (insolvent at all ticks + finalLen==0), L1384-1392 (StaleOracle: `|currentTick - twapTick| > tickDeltaLiquidation` reverts BEFORE liquidation — why a single large swap fails), L1388 (the StaleOracle revert line)
  </read_first>
  <action>
    Create `contracts/test/instrument/LongGammaWrapper.liquidation.t.sol` (SPDX MIT, pragma `^0.8.24`), `contract LongGammaWrapperLiquidation is LongGammaWrapperBase`, full-stem co-located, bulloak-branch-fn delegation.

    Shared proof `test_liquidation_residualFloorZero()`:
    ```
    // open the wrapper with a LARGE long size RELATIVE TO a thin deposit so an adverse move flips solvency.
    // (override the default by depositing the MINIMUM collateral that still mints, then a near-SELLER_SIZE long.)
    LongGammaWrapper wrapper = ...deploy...;
    // deposit a deliberately thin collateral so the position-to-collateral ratio is high (OQ2):
    wrapper.deposit(user, THIN0, THIN1, _longTokenId(), LONG_SIZE, limits);   // tune THIN* down until solvency is fragile

    // DRIVE INSOLVENCY — CONCRETE FIRST-CLASS MECHANISM (liquidation is the highest-risk fork task; per
    // 08-VALIDATION Manual-Only it is PERMITTED to be vm-assisted). A single large adverse swap does NOT work:
    // it trips `StaleOracle` (PanopticPool.sol L1388: `|currentTick - twapTick| > tickDeltaLiquidation`) BEFORE
    // liquidation is reachable, and the median/twap ticks LAG a single-block swap. Use ONE of these two paths:
    //
    // PREFERRED — `vm.store` collateral shrink (no price move, no StaleOracle bound) — applied to BOTH ct0 AND ct1:
    //   The CollateralTracker is `ERC20Minimal`; the wrapper's share balance lives in `balanceOf[wrapper]`.
    //   ───────────────────────────────────────────────────────────────────────────────────────────────
    //   ⚠ SLOT HAZARD (M2): in `ERC20Minimal` slot 0 is `_internalSupply` and `balanceOf` is slot 1 — a naive
    //   `vm.store` to slot 0 corrupts TOTAL-SUPPLY accounting, NOT the wrapper's balance, and silently breaks
    //   convertToAssets. You MUST DERIVE the `balanceOf` mapping's storage index FIRST, do NOT hard-code 0/1:
    //     `forge inspect CollateralTracker storage-layout`  (read the `balanceOf` slot from the JSON), OR
    //     probe slots 0..N writing a sentinel and reading back `IERC20(address(ct)).balanceOf(wrapper)` until
    //     the read reflects the write (probe-and-verify). Call the derived index BAL_SLOT.
    //   ───────────────────────────────────────────────────────────────────────────────────────────────
    //   uint256 BAL_SLOT = /* derived via forge inspect storage-layout (NOT slot 0 = _internalSupply) */;
    //   uint256 tinyShares0 = ct0.convertToShares(1);   // shrink to a near-zero SHARE balance (balanceOf holds shares)
    //   uint256 tinyShares1 = ct1.convertToShares(1);
    //   bytes32 slot0 = keccak256(abi.encode(address(wrapper), BAL_SLOT));
    //   vm.store(address(ct0), slot0, bytes32(tinyShares0));   // shrink ct0 holdings
    //   vm.store(address(ct1), slot0, bytes32(tinyShares1));   // shrink ct1 holdings — BOTH, else a one-sided
    //                                                          // shrink may not push solvent==0 at all 4 ticks
    //   // verify the writes landed (probe-and-verify, also guards the slot derivation):
    //   assertLe(IERC20(address(ct0)).balanceOf(address(wrapper)), tinyShares0, "ct0 balance shrunk via vm.store");
    //   assertLe(IERC20(address(ct1)).balanceOf(address(wrapper)), tinyShares1, "ct1 balance shrunk via vm.store");
    //   // now `_checkSolvencyAtTicks` -> isAccountSolvent -> assetsAndInterest -> convertToAssets(balanceOf)
    //   // returns ~0 at ALL 4 ticks WITHOUT moving price past the StaleOracle bound (no tick moved at all).
    //   // Record this in a `// NOTE` (08-VALIDATION Manual-Only).
    //
    // ALTERNATIVE — multi-block bounded swaps across the oracle window:
    //   `vm.roll`+`vm.warp` across >= the oracle window (10 blocks) with BOUNDED per-block swaps so currentTick,
    //   twapTick, medianTick, latestTick ALL cross into insolvency while each step stays under `tickDeltaLiquidation`
    //   (avoids StaleOracle). Confirm via getCurrentTick()/getOracleTicks() between rolls.
    deal(token0, address(swapHelper), type(uint104).max); deal(token1, address(swapHelper), type(uint104).max);
    // (if using the ALTERNATIVE path, loop bounded swaps with vm.roll/vm.warp here instead of one LARGE swap)

    // LIQUIDATE: dispatchFrom with finalList length 0 (and the account insolvent at all ticks)
    TokenId[] memory wrap1 = new TokenId[](1); wrap1[0] = _longTokenId();
    TokenId[] memory emptyFinal = new TokenId[](0);
    TokenId[] memory liqList = new TokenId[](1); liqList[0] = sellerShortId;
    vm.prank(makeAddr("liquidator"));
    IPanopticData(deployedPool).dispatchFrom(liqList, address(wrapper), wrap1, emptyFinal, LeftRightUnsigned.wrap(0));

    // liquidation CLOSES all positions
    assertEq(IPanopticData(deployedPool).numberOfLegs(address(wrapper)), 0, "liquidation closed all legs");

    // reconcile (promotes Open->Closed via the involuntary-close detection from Task 2) + claim
    _closeSellerShort();
    wrapper.syncResidual();
    assertEq(uint8(wrapper.state()), uint8(2), "Closed after involuntary liquidation");   // State.Closed
    // snapshot surviving BEFORE the claim (the correct never-overpay reference — reading post-claim is wrong
    // and `paid <= surv + paid` is a tautology). Mirror the corrected form in 08-05's claimResidual test.
    uint256 survBeforeClaim0 = ct0.convertToAssets(IERC20(address(ct0)).balanceOf(address(wrapper)));
    uint256 survBeforeClaim1 = ct1.convertToAssets(IERC20(address(ct1)).balanceOf(address(wrapper)));
    uint256 u0pre = IERC20(token0).balanceOf(user);
    uint256 u1pre = IERC20(token1).balanceOf(user);
    wrapper.claimResidual();
    uint256 u0post = IERC20(token0).balanceOf(user);
    uint256 u1post = IERC20(token1).balanceOf(user);

    // residual FLOORS at zero when surviving is wiped — and the wrapper NEVER pays more than it HELD (pre-claim).
    assertLe(u0post - u0pre, survBeforeClaim0 + 1, "paid0 <= surviving the wrapper held pre-claim (never overpays)");
    assertLe(u1post - u1pre, survBeforeClaim1 + 1, "paid1 <= surviving the wrapper held pre-claim (never overpays)");
    // the load-bearing assertions: numberOfLegs==0, state==Closed, claimResidual did NOT revert, and
    // residual0 == max(surviving0 - cost0, 0) (>= 0 always; may be 0 under severe loss).
    ```
    The PRIMARY invariants to assert (not the exact wei, which depends on the liquidation bonus): (1) `numberOfLegs(wrapper)==0` after the liquidating `dispatchFrom`; (2) `wrapper.claimResidual()` does NOT revert; (3) the paid residual is `>= 0` and `<= the surviving the wrapper held` (never overpays — structurally enforced by settleBurn's NotEnoughTokens, but asserted here too); (4) `AccountLiquidated` (pool event) and/or `ResidualEroded` (wrapper) observed. Budget swap-size iterations (this is the highest-risk fork task per CONTEXT/OQ2). If insolvency cannot be reached organically after reasonable iteration, use the documented vm-assisted setup (08-VALIDATION Manual-Only) and record it in a `// NOTE`.
  </action>
  <verify>
    <automated>cd contracts && forge test --match-path "test/instrument/LongGammaWrapper.liquidation.t.sol" --fork-url "$BASE_RPC_URL" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd contracts && forge test --match-test test_liquidation_residualFloorZero --fork-url "$BASE_RPC_URL"` exits 0 (numberOfLegs->0 after the liquidating dispatchFrom; claimResidual does not revert; residual floors at >=0 and the wrapper never overpays).
    - `grep -n "dispatchFrom" contracts/test/instrument/LongGammaWrapper.liquidation.t.sol` matches with `emptyFinal` (length-0 final list — the liquidation disambiguator).
    - BOTH-SIDED vm.store (M2): if the vm.store path is used, `grep -c "vm.store(address(ct" contracts/test/instrument/LongGammaWrapper.liquidation.t.sol` returns `2` (ct0 AND ct1 — a one-sided shrink may not push solvent==0 at all 4 ticks). The slot is DERIVED (not hard-coded slot 0): `grep -Eqi "forge inspect|storage-layout|probe|balanceOf\(address\(wrapper\)\), tinyShares" contracts/test/instrument/LongGammaWrapper.liquidation.t.sol && echo SLOT_DERIVED || echo SLOT_HARDCODED` prints `SLOT_DERIVED`, and the `_internalSupply`-slot-0 hazard is acknowledged in a comment: `grep -ci "_internalSupply\|slot 0\|BAL_SLOT" contracts/test/instrument/LongGammaWrapper.liquidation.t.sol` returns `>=1`.
    - `grep -c "panoptic-borrowed" contracts/test/instrument/LongGammaWrapper.liquidation.t.sol` returns `0` (seam grep-guard).
    - `cd contracts && bulloak check test/instrument/LongGammaWrapper.liquidation.tree` co-location holds (the full-stem `.t.sol` now exists).
    - INSOLVENCY MECHANISM is concrete and first-class (NOT an "if organic fails" afterthought): the test uses EITHER the `vm.store` collateral-shrink (preferred — shrinks the wrapper's CollateralTracker share balance on BOTH ct0 AND ct1, slot DERIVED via `forge inspect storage-layout` or probe-and-verify, NOT slot 0 which is `_internalSupply`, so `_checkSolvencyAtTicks`==0 at all 4 ticks without tripping StaleOracle) OR the multi-block `vm.roll`/`vm.warp` bounded-swap loop (each step under `tickDeltaLiquidation`). A SINGLE large adverse swap is explicitly rejected (it trips `StaleOracle` L1388 before liquidation is reachable). The `dispatchFrom`(finalLen==0) liquidation acceptance ALLOWS this vm-assist path (08-VALIDATION Manual-Only) — a `// NOTE` documents whichever path was used; vm-assist is permitted because liquidation is the highest-risk fork task.
  </acceptance_criteria>
  <done>liquidation.t.sol passes: the wrapper is driven insolvent (organic adverse swaps or documented vm-assisted vm.store shrinking BOTH ct0 AND ct1 at the DERIVED balanceOf slot — never the slot-0 _internalSupply), a liquidator's empty-final-list dispatchFrom closes all legs (numberOfLegs->0), syncResidual promotes to Closed, claimResidual pays a floored (>=0) residual without reverting, and the wrapper never pays more than it held.</done>
</task>

</tasks>

<verification>
- `cd contracts && forge test --match-path "test/instrument/LongGammaWrapper.settleLong.t.sol"` / `forceExercise.t.sol` / `liquidation.t.sol` all green on the Base fork.
- The Plan-05 voluntary close/claim tests STILL green after the `_reconcile` involuntary-close-detection addition.
- All three involuntary trees committed as their own commit BEFORE their tests (Iron Law).
- settle stays Open (numberOfLegs unchanged); forceExercise + liquidation close (numberOfLegs->0); residual floors at >=0; the wrapper never overpays; seam grep-guards hold.
- m1: the conditional ResidualEroded-on-settle coverage gap is flagged in a NOTE in settleLong.t.sol (not mistaken for proven event coverage); the seam IS covered by forceExercise/liquidation which fire ResidualEroded reliably.
- M2: the liquidation vm.store path shrinks BOTH ct0 AND ct1 at the DERIVED balanceOf slot (forge inspect storage-layout / probe-and-verify), never the slot-0 `_internalSupply`.
</verification>

<success_criteria>
WRAP-03's three involuntary branches are proven via `dispatchFrom`: settleLongPremium erodes the wrapper while it stays Open (numberOfLegs unchanged — the load-bearing settle-stays-Open distinction, with the conditional ResidualEroded-on-settle coverage gap flagged); forceExercise and liquidation close it (numberOfLegs->0), the wrapper's `syncResidual` promotes Open->Closed (involuntary-close detection), and `claimResidual` pays `max(surviving - cost, 0)` (floored at 0 under severe loss) without ever overpaying. `ResidualEroded` fires on involuntary debit (reliably on forceExercise/liquidation; conditionally on settle). Liquidation insolvency is driven by a concrete first-class mechanism — `vm.store` shrinking BOTH ct0 AND ct1 at the derived (not slot-0) balanceOf slot, or a multi-block bounded-swap loop — never a single large swap (StaleOracle).
</success_criteria>

<output>
After completion, create `.planning/phases/08-longgammawrapper-cash-flow/08-06-SUMMARY.md`.
</output>

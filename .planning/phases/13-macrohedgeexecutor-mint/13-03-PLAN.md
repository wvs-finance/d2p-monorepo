---
phase: 13-macrohedgeexecutor-mint
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - contracts/src/OperationalCostManagement.sol
  - contracts/test/instrument/OperationalCostManagement.tree
  - contracts/test/instrument/OperationalCostManagement.t.sol
autonomous: true
requirements: [EXEC-03]
must_haves:
  truths:
    - "OperationalCostManagement accrues a cumulative agent-call cost + data cost per decisionId via accrueAgentCost(bytes32 decisionId, uint8 leg, uint256 somi) (the cummCost the user is charged for across Agent 1 = leg 0 + Agent 2 = leg 1)"
    - "CONSERVATION invariant: the global accumulator cummCostSomi equals Σ over decisions of costOf[decision].agentCostSomi (and likewise cummDataCost == Σ costOf[d].dataCost)"
    - "CONSERVATION is mutation-proven NON-VACUOUS by a DISTINCT mutation from idempotency: mutate 'add to cummCostSomi WITHOUT costOf[d]' → the conservation invariant FAILS (the conservation invariant is BLIND to symmetric double-counting, so it can ONLY be the non-vacuity proof for the asymmetric global-vs-line drift, NOT for idempotency)"
    - "IDEMPOTENCY: re-accruing the same (decisionId, leg) does NOT double-count — a SEPARATE unit leaf asserts cummCostSomi is UNCHANGED after the 2nd accrue of the same (d, leg)"
    - "IDEMPOTENCY is mutation-proven NON-VACUOUS by its OWN mutation: drop the (decisionId, leg) guard so a 2nd accrue re-adds → BOTH cummCostSomi AND costOf[d].agentCostSomi double, so conservation STILL HOLDS but the SEPARATE idempotency unit leaf FAILS (this is why idempotency cannot be proven by the conservation invariant)"
    - "Accrual records the BUDGETED forwarded SOMI, not a realized executionCost (structurally unavailable on Somnia) — documented in the units column"
  artifacts:
    - path: "contracts/src/OperationalCostManagement.sol"
      provides: "cummCost accumulator keyed by decisionId with per-leg agent+data entry points and a conservation read"
      contains: "cummCostSomi"
      min_lines: 30
    - path: "contracts/test/instrument/OperationalCostManagement.t.sol"
      provides: "Conservation invariant (cummCost == Σ lines) + idempotent no-double-count, fuzz-driven, mutation-proven non-vacuous"
  key_links:
    - from: "contracts/src/OperationalCostManagement.sol"
      to: "cummCostSomi accumulator"
      via: "accrueAgentCost(decisionId, leg, somi) adds to BOTH costOf[decisionId].agentCostSomi and cummCostSomi under the (decisionId, leg) idempotency guard — conservation needs both sides moved together"
      pattern: "cummCostSomi \\+="
    - from: "contracts/src/OperationalCostManagement.sol"
      to: "agentLegAccrued[decisionId][leg] guard"
      via: "accrueAgentCost gates the (decisionId, leg) line so a re-accrue is a no-op/revert — the idempotency proof targets THIS, not the conservation invariant"
      pattern: "agentLegAccrued\\[decisionId\\]\\[leg\\]"
---

<objective>
Design and build the minimal demo-scoped `OperationalCostManagement` ledger (EXEC-03): a `cummCost` accumulator keyed by `decisionId` (Agent 1's `bytes32(requestId)` join key) that accrues the budgeted agent-call SOMI + the data cost across both agents, under a no-double-count conservation invariant. This is the `cummCost` the user is ultimately charged for (DRAFT.md:64-65).

Purpose: Give the cornerstone a conserved cost ledger. It is an independent unit/fuzz contract — NO fork, NO dependency on the executor's mint or the PoolId constant — so it runs in Wave 1 in parallel with Plan 01 and ahead of the executor (Plan 02 wires it as a settable field, not a ctor requirement, so this plan does not block Plan 02).
Output: `contracts/src/OperationalCostManagement.sol` (promoted from the empty stub), a co-located BTT `.tree`+test proving conservation + idempotency, mutation-proven non-vacuous.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-macrohedgeexecutor-mint/13-RESEARCH.md
@.planning/phases/13-macrohedgeexecutor-mint/13-VALIDATION.md

<interfaces>
<!-- The recommended demo-scoped shape (RESEARCH §7). Discretionary — the conservation + idempotency are the contract. -->

From contracts/src/OperationalCostManagement.sol (current — empty stub to promote):
```solidity
// note: SOMI measured, this needs to have entry/enxit points to pay Somnia
contract OperationalCostManagement {}
```

From 13-RESEARCH.md §7 (the recommended minimal shape — RESEARCH STARTING POINT, REFINED below):
```solidity
contract OperationalCostManagement {
    struct CostLine { uint256 agentCostSomi; uint256 dataCost; bool minted; }
    mapping(bytes32 => CostLine) public costOf;   // keyed by decisionId (Agent-1's bytes32(requestId))
    uint256 public cummCostSomi;                  // global running total (the cummCost)
    uint256 public cummDataCost;
    event AgentCostAccrued(bytes32 indexed decisionId, uint256 somi, uint256 cummSomi);
    event DataCostAccrued(bytes32 indexed decisionId, uint256 dataCost, uint256 cummData);
    function accrueAgentCost(bytes32 decisionId, uint256 somi) external; // 2-arg STARTING POINT — REFINED below to add `leg`
    function accrueDataCost(bytes32 decisionId, uint256 dataCost) external;
    function totalCost(bytes32 decisionId) external view returns (uint256 somi, uint256 data);
}
```

> **CANONICAL SIGNATURE (the §7 quote above is the research starting point — REFINED here):** `accrueAgentCost` takes a `leg` param so per-`(decisionId, leg)` idempotency is enforceable (Agent 1 = leg 0, Agent 2 = leg 1 each accrue exactly once under one `decisionId`). Use this 3-arg form EVERYWHERE in this plan (must_haves, key_links, behavior, action, acceptance, success_criteria):
> ```solidity
> function accrueAgentCost(bytes32 decisionId, uint8 leg, uint256 somi) external;
> ```

Idempotency precedent — from STATE.md (Phase 11 Strategist) + RESEARCH §7:
"guard with the same idempotency the Strategist uses (`actionSet`/`sizeSet`-style flags) so a re-delivered callback or a re-sent request does not double-accrue."
The conservation pattern mirrors Phase-9's planned `invariant_dataCostConserved` (ROADMAP.md:110) AND the proven Phase-8 mutation discipline (STATE.md:53): a fixed cost charged once must NOT be re-added N times; the invariant must be mutation-proven non-vacuous.

From contracts/test/instrument/MacroHedgeStrategist.tree (the bulloak-0.9.2 `when/it` keyword form to mirror — bare leaf labels do NOT parse).
From contracts/test/instrument/LongGammaWrapper.invariants.t.sol (the proven Handler + named-invariant + mutation-proof pattern to mirror; the additive `[invariant]` floor runs=16/depth=16 already in foundry.toml).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — BTT .tree + FAILING conservation/idempotency tests (committed FIRST, Iron Law)</name>
  <files>contracts/test/instrument/OperationalCostManagement.tree, contracts/test/instrument/OperationalCostManagement.t.sol</files>
  <read_first>
    - contracts/test/instrument/MacroHedgeStrategist.tree (the `when/it` keyword form; bare leaves throw `unexpected token` in bulloak 0.9.2 — the 08-01 precedent)
    - 13-RESEARCH.md §7 (the cost components, the entry/exit points per agent call, the conservation invariant `cummCost == Σ per-decision lines`, the idempotency guard, the budgeted-not-realized boundary)
    - contracts/test/instrument/LongGammaWrapper.invariants.t.sol (the Handler-drives-two-ledgers + named `invariant_*` + mutation-proof pattern; the handler stores an INDEPENDENT mirror sum, asserts call-counts > 0 for non-vacuity)
    - contracts/foundry.toml (the additive `[invariant]` floor runs=16/depth=16, fail_on_revert=false — reuse; this is a PURE in-memory contract so it will NOT 429)
  </read_first>
  <action>
    1. Create `contracts/test/instrument/OperationalCostManagement.tree` (CO-LOCATED, bulloak 0.9.2 same-dir + full-stem). Root contract `OperationalCostManagementTest`. `when/it` keyword form. The canonical agent entry point is the 3-arg `accrueAgentCost(bytes32 decisionId, uint8 leg, uint256 somi)`. Cover exactly:
       - when accrueAgentCost is called on a fresh decision and leg → it should increase costOf agentCostSomi by the amount and increase cummCostSomi by the amount and emit AgentCostAccrued
       - when accrueAgentCost is called a second time for the same decision and leg → it should leave BOTH cummCostSomi AND costOf agentCostSomi unchanged after the second call (the IDEMPOTENCY leaf — this is the leaf the idempotency-mutation must break; it does NOT go through the conservation invariant)
       - when accrueDataCost is called on a fresh decision → it should increase costOf dataCost and cummDataCost and emit DataCostAccrued
       - when accrueDataCost is called a second time for the same decision → it should leave BOTH cummDataCost AND costOf dataCost unchanged after the second call
       - when totalCost is read for a decision → it should return the accrued agent SOMI and data cost for that decision
       - when many decisions and legs have been accrued → it should hold cummCostSomi equal to the sum over decisions of costOf agentCostSomi and cummDataCost equal to the sum of the data lines (the CONSERVATION invariant — this is what the conservation-mutation must break)
    2. Write `contracts/test/instrument/OperationalCostManagement.t.sol` as the FAILING test FIRST (the empty stub has none of the surface, so it will not compile — the RED state the Iron Law requires). Include:
       - the per-leaf unit tests above — and CRITICALLY a SEPARATE idempotency unit leaf `test_accrueSameLegTwice_cummCostSomiUnchanged`: accrue `(d, leg)` once, snapshot `cummCostSomi`, accrue the SAME `(d, leg)` a second time, assert `cummCostSomi` is UNCHANGED (and `costOf[d].agentCostSomi` unchanged). This leaf — NOT the conservation invariant — is the idempotency assertion.
       - a named `invariant_costConserved` asserting `cummCostSomi == handlerMirrorAgentSum` AND `cummDataCost == handlerMirrorDataSum`, and a bounded `OperationalCostHandler` (`act_accrueAgent(seedDecision, seedLeg, somi)` / `act_accrueData(seedDecision, data)`, each `bound()`ing the decision to a small id set and the leg to {0,1}, maintaining an INDEPENDENT mirror sum). Assert the handler's call-counts > 0 so the invariant is non-vacuous (the Phase-8 discipline).
       - NOTE (the M3 distinction, must be reflected in the test design): the conservation invariant is BLIND to symmetric double-counting — if the per-leg guard is dropped, a 2nd accrue re-adds to BOTH `cummCostSomi` AND `costOf[d].agentCostSomi`, so `invariant_costConserved` STILL PASSES. Therefore the idempotency proof MUST live in the separate `test_accrueSameLegTwice_cummCostSomiUnchanged` leaf, and the conservation proof MUST live in `invariant_costConserved`. They are two distinct assertions broken by two distinct mutations (see Task 2).
    Commit the `.tree` + the FAILING `.t.sol` in a SEPARATE commit BEFORE the impl (evm-tdd Iron Law). Commit: `test(13): OperationalCostManagement.tree + failing conservation/idempotency suite (RED)`.
  </action>
  <acceptance_criteria>
    - `cd contracts && bulloak check test/instrument/OperationalCostManagement.tree` — must NOT error on parse (no "unexpected token"; it WILL report the missing/incomplete `.t.sol` until impl).
    - `cd contracts && forge test --match-path "test/instrument/OperationalCostManagement.t.sol" 2>&1 | grep -Eq "FAIL|Compiler|error\\["` — RED (the surface does not exist yet).
    - `git log --oneline -1 -- contracts/test/instrument/OperationalCostManagement.tree` predates the `OperationalCostManagement.sol` impl commit (Iron-Law ancestry).
  </acceptance_criteria>
  <verify>
    <automated>cd contracts && bulloak check test/instrument/OperationalCostManagement.tree 2>&1 | grep -vq "unexpected token"</automated>
  </verify>
  <done>The `.tree` parses cleanly and the FAILING conservation/idempotency suite + the named `invariant_costConserved` + the bounded handler are committed BEFORE the impl; the suite is RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — promote OperationalCostManagement (cummCost accumulator + per-leg idempotency + conservation), mutation-proven</name>
  <files>contracts/src/OperationalCostManagement.sol</files>
  <read_first>
    - contracts/src/OperationalCostManagement.sol (the empty stub to promote; bump pragma `^0.8.0` → `^0.8.24`, SPDX MIT)
    - 13-RESEARCH.md §7 (the recommended shape + the conservation invariant + the idempotency guard named exactly: "accrued once per (decisionId, leg)") + RESEARCH Open Q3 (budgeted-not-realized SOMI; document the units/source column)
    - 13-VALIDATION.md Per-Task Verification Map EXEC-03 (the conservation invariant green + non-vacuous-by-mutation) + Proof-semantics ("EXEC-03 budgeted, not realized")
    - contracts/test/instrument/LongGammaWrapper.invariants.t.sol (the mutation-proof method: zero one accumulator → the unchanged assertion fails → restore)
  </read_first>
  <behavior>
    - `accrueAgentCost(d, leg, somi)` on a fresh `(d, leg)` → `costOf[d].agentCostSomi += somi` AND `cummCostSomi += somi`; emits `AgentCostAccrued(d, somi, cummCostSomi)`.
    - `accrueAgentCost(d, leg, somi)` a SECOND time for the SAME `(d, leg)` → idempotent (revert OR no-op); `cummCostSomi` UNCHANGED (the no-double-count guard). Leg 0 (Agent 1) and leg 1 (Agent 2) each accrue exactly once.
    - `accrueDataCost(d, data)` → `costOf[d].dataCost += data`, `cummDataCost += data`, idempotent per the data line; emits `DataCostAccrued`.
    - CONSERVATION (the EXEC-03 gate): after N decisions × M legs, `cummCostSomi == Σ_d costOf[d].agentCostSomi` AND `cummDataCost == Σ_d costOf[d].dataCost`.
    - `totalCost(d)` returns `(agentCostSomi, dataCost)` for the decision — the EXIT read the UI uses to show the user the `cummCost`.
    - NON-VACUITY — TWO DISTINCT mutations against TWO DISTINCT assertions (do NOT conflate; the conservation invariant CANNOT prove idempotency):
      - (1) CONSERVATION non-vacuity: mutate `accrueAgentCost` to add to `cummCostSomi` WITHOUT touching `costOf[d].agentCostSomi` (asymmetric drift) → `invariant_costConserved` MUST FAIL.
      - (2) IDEMPOTENCY non-vacuity: mutate `accrueAgentCost` to DROP the `(decisionId, leg)` guard so a 2nd accrue re-adds → BOTH `cummCostSomi` AND `costOf[d].agentCostSomi` double, so `invariant_costConserved` STILL PASSES (blind to symmetric double-counting); the SEPARATE `test_accrueSameLegTwice_cummCostSomiUnchanged` leaf MUST FAIL instead.
  </behavior>
  <action>
    Promote the empty stub to the §7 shape (SPDX MIT, pragma `^0.8.24`):
    1. `struct CostLine { uint256 agentCostSomi; uint256 dataCost; }`; `mapping(bytes32 => CostLine) public costOf;`; `uint256 public cummCostSomi;`; `uint256 public cummDataCost;`.
    2. A per-leg accrual guard: `mapping(bytes32 => mapping(uint8 => bool)) public agentLegAccrued;` (each `(decisionId, leg)` agent line accrues once) and `mapping(bytes32 => bool) public dataAccrued;` (the single data line). (Adding the `leg` param to the §7 signature is the discretionary refinement that makes "accrued once per (decisionId, leg)" enforceable — RESEARCH §7 names exactly this guard.)
    3. `function accrueAgentCost(bytes32 decisionId, uint8 leg, uint256 somi) external`: `require(!agentLegAccrued[decisionId][leg], "double-count")` (or no-op return), set the flag, `costOf[decisionId].agentCostSomi += somi`, `cummCostSomi += somi`, emit `AgentCostAccrued`.
    4. `function accrueDataCost(bytes32 decisionId, uint256 dataCost) external`: idempotent via `dataAccrued`; `costOf[decisionId].dataCost += dataCost`, `cummDataCost += dataCost`, emit `DataCostAccrued`.
    5. `function totalCost(bytes32 decisionId) external view returns (uint256 somi, uint256 data)`.
    6. Demo-scoped: no access control needed for the unit proof (open entry points; the executor will be the caller in the cornerstone — wiring is Plan 02's optional/settable concern, NOT this plan's). Keep it a standalone, fork-free unit.
    7. **NatSpec boundary (state explicitly):** the ledger ACCRUES and CONSERVES the BUDGETED forwarded SOMI (the over-funded `msg.value` per agent call — realized `executionCost` is structurally unavailable on Somnia, memory `project-realized-executioncost-structurally-absent`). It does NO real SOMI transfer / settlement — the K_AI escrow / cross-chain payment is the deferred XCHAIN-01 path (the mint is on Polygon, the agent calls on Somnia). Every cost line carries a units/source column (budgeted SOMI vs data cost) in comments.
    Run the Task-1 suite GREEN. Then run BOTH non-vacuity mutations (they are distinct — see <behavior>), one at a time, restoring between:
      - MUTATION (1) — CONSERVATION: temporarily make `accrueAgentCost` add to `cummCostSomi` WITHOUT `costOf[decisionId].agentCostSomi` → confirm `invariant_costConserved` FAILS → restore.
      - MUTATION (2) — IDEMPOTENCY: temporarily DROP the `agentLegAccrued[decisionId][leg]` guard so a 2nd accrue re-adds → confirm `invariant_costConserved` STILL PASSES (it is blind to symmetric double-count) AND `test_accrueSameLegTwice_cummCostSomiUnchanged` FAILS → restore.
      Record BOTH mutations + which assertion each broke in the SUMMARY (the Phase-8/9 discipline). The point is explicit: the conservation invariant is NOT the idempotency proof.
    Commit the impl AFTER Task 1's RED commit (Iron-Law ancestry). Commit: `feat(13): OperationalCostManagement cummCost ledger — conservation + per-leg idempotency`.
  </action>
  <acceptance_criteria>
    - `cd contracts && forge build` exits 0.
    - `cd contracts && bulloak check test/instrument/OperationalCostManagement.tree` exits 0 (the `.t.sol` satisfies the tree).
    - `cd contracts && forge test --match-path "test/instrument/OperationalCostManagement.t.sol" -vv` — all unit leaves + the `invariant_costConserved` PASS. [13-VALIDATION EXEC-03 command]
    - `cd contracts && grep -q "cummCostSomi" src/OperationalCostManagement.sol` (the accumulator exists).
    - BOTH mutations recorded in the SUMMARY (distinct, two assertions): (1) cummCostSomi-without-costOf FAILS `invariant_costConserved`; (2) dropping the `(decisionId, leg)` guard leaves `invariant_costConserved` GREEN but FAILS `test_accrueSameLegTwice_cummCostSomiUnchanged`. Each restored to green (the non-vacuity proof that conservation ≠ idempotency).
  </acceptance_criteria>
  <verify>
    <automated>cd contracts && forge build && bulloak check test/instrument/OperationalCostManagement.tree && forge test --match-path "test/instrument/OperationalCostManagement.t.sol" -vv</automated>
  </verify>
  <done>`OperationalCostManagement` accrues `cummCost` (budgeted agent SOMI + data cost) keyed by `decisionId` with per-leg idempotency; `invariant_costConserved` is GREEN and mutation-proven non-vacuous; the budgeted-not-realized + no-settlement boundaries are documented in NatSpec; `bulloak check` exit 0; Iron-Law ancestry preserved.</done>
</task>

</tasks>

<verification>
- `cd contracts && bulloak check test/instrument/OperationalCostManagement.tree` — exit 0 (co-located, full-stem).
- `cd contracts && forge build` — green.
- `cd contracts && forge test --match-path "test/instrument/OperationalCostManagement.t.sol" -vv` — all unit tests + the conservation invariant PASS.
- Mutation (TWO distinct, two assertions): (1) add to `cummCostSomi` without `costOf` → `invariant_costConserved` FAILS; (2) drop the `(decisionId, leg)` guard → `invariant_costConserved` STILL PASSES (blind to symmetric double-count) but `test_accrueSameLegTwice_cummCostSomiUnchanged` FAILS. Restore between each. Record BOTH in SUMMARY.
- Iron-Law ancestry: the `.tree` + FAILING `.t.sol` commit predates the impl commit.
</verification>

<success_criteria>
- `OperationalCostManagement` accrues `cummCost` (budgeted agent SOMI + data cost) keyed by `decisionId` with per-leg idempotency. *(EXEC-03)*
- `invariant_costConserved`: `cummCostSomi == Σ costOf[d].agentCostSomi` AND `cummDataCost == Σ costOf[d].dataCost`, fuzz-proven at the `[invariant]` floor, mutation-proven non-vacuous by the (1) cummCostSomi-without-costOf mutation. *(EXEC-03)*
- Re-accruing the same `(decisionId, leg)` does NOT double-count — proven by the SEPARATE `test_accrueSameLegTwice_cummCostSomiUnchanged` leaf (NOT the conservation invariant, which is blind to symmetric double-counting), mutation-proven non-vacuous by the (2) drop-the-guard mutation. *(EXEC-03)*
- The budgeted-not-realized SOMI boundary + the no-settlement (XCHAIN-01 deferred) boundary are documented in NatSpec.
- `bulloak check` exit 0; `forge build` green; Iron-Law ancestry preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/13-macrohedgeexecutor-mint/13-03-SUMMARY.md`
</output>

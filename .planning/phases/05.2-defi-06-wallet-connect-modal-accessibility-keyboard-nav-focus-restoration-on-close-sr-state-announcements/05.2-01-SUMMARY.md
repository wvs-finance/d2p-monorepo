---
phase: 05.2-defi-06-wallet-connect-modal-accessibility
plan: 01
subsystem: ui
tags: [wagmi, rainbowkit, accessibility, a11y, wcag, aria-live, focus-management, playwright, e2e]

# Dependency graph
requires:
  - phase: 05.1
    provides: WalletPanel (Wave 2 readOnly + WalletStatusPill useMounted guard)
provides:
  - scoped <output> live region replacing panel-wide aria-atomic in WalletPanel
  - connect-success focus recovery to <output> node (WCAG 2.4.3)
  - permanent dev/e2e-guarded audit route /a11y-wallet-check
  - wagmi mock connector for DISCONNECTED→CONNECTED e2e transitions
  - durable Playwright e2e: wallet-connect-modal.spec.ts
affects: [05.2-accessibility-auditor-sign-off, DEFI-06-complete]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "<output> element for SR live regions (implicit role=status aria-live=polite, biome-compliant)"
    - "tabIndex={-1} + useEffect focus() for programmatic focus recovery on mount unmounts"
    - "NEXT_PUBLIC_E2E env var to unlock dev/audit routes in production builds"
    - "Nested isolated WagmiProvider + RainbowKitProvider for audit route mock connector"

key-files:
  created:
    - components/defi/WalletPanel.tsx (modified — scoped output node + useEffect focus)
    - tests/unit/wallet-panel-connect-a11y.test.tsx
    - lib/wallet/mock-connector.ts
    - app/(defi)/a11y-wallet-check/page.tsx
    - app/(defi)/a11y-wallet-check/AuditShell.tsx
    - tests/e2e/wallet-connect-modal.spec.ts
  modified:
    - tests/unit/wallet-read-only.test.tsx
    - tests/e2e/wallet-states.spec.ts
    - playwright.config.ts

key-decisions:
  - "Use <output> element (not <p role=status>) — biome useSemanticElements enforces it; semantically superior (implicit role+aria-live)"
  - "Audit route named a11y-wallet-check without _ prefix — Next.js App Router treats _folders as private/non-routed (deviation from plan)"
  - "Nested isolated WagmiProvider in AuditShell.tsx overrides layout provider context for mock connector"
  - "connect-success focus: useEffect keyed on walletState !== DISCONNECTED|CONNECTING, focuses tabIndex={-1} <output> node"
  - "NEXT_PUBLIC_E2E controls audit route guard; read directly from process.env (not validated in env schema — it's a bypass flag)"

requirements-completed: []  # DEFI-06 pending Accessibility Auditor sign-off (Task 3 checkpoint)

# Metrics
duration: 13min
completed: 2026-06-02
---

# Phase 05.2 Plan 01: DEFI-06 Wallet A11y — Tasks 1+2 Summary

**Scoped `<output>` live region replacing panel-wide aria-atomic + permanent /a11y-wallet-check audit route with mock connector and durable Playwright e2e regression guard**

## Performance

- **Duration:** 13 min
- **Started:** 2026-06-02T17:10:20Z
- **Completed:** 2026-06-02T17:23:16Z
- **Tasks:** 2 of 4 (stopped at Task 3 checkpoint — Accessibility Auditor gate)
- **Files modified:** 8

## Accomplishments

- Removed the panel-wide `aria-live="polite" aria-atomic="true"` from `WalletPanel`'s outer div (DEFI-06 BLOCKER 1 — caused full panel re-read on every state transition)
- Added a scoped `<output>` element (implicit `role=status aria-live=polite`) with only the state label text, tabIndex={-1} for programmatic focus
- Added `useEffect` to move focus to the `<output>` node when `walletState` leaves DISCONNECTED/CONNECTING — prevents focus loss to `<body>` on connect-success (DEFI-06 BLOCKER 2)
- Created `lib/wallet/mock-connector.ts` using wagmi's `mock()` for real DISCONNECTED→CONNECTED e2e transitions
- Created `app/(defi)/a11y-wallet-check/` — permanent dev/e2e-guarded route with isolated mock-wired WagmiProvider + RainbowKitProvider
- Added `NEXT_PUBLIC_E2E=true` to playwright.config.ts webServer env to unlock the audit route in the production build
- Created durable `tests/e2e/wallet-connect-modal.spec.ts` covering keyboard operability, focus-return on all close paths (Escape/X/backdrop), connect-success focus to `<output>`, and announcement text changes

## Task Commits

1. **Task 1: scoped live region + connect-success focus + component test** - `dd8dcff` (feat)
2. **Task 2: guarded audit route + mock connector + durable e2e** - `2960ee8` (feat)

## Files Created/Modified

- `components/defi/WalletPanel.tsx` — removed panel-wide aria-live/aria-atomic; added `<output>` sr-only node + useEffect focus recovery
- `tests/unit/wallet-panel-connect-a11y.test.tsx` — NEW: asserts output node present, label text, no outer aria-atomic, text updates on re-render
- `tests/unit/wallet-read-only.test.tsx` — updated getAllByText for dual label nodes (output + pill span)
- `tests/e2e/wallet-states.spec.ts` — updated assertions: `output.sr-only` toBeAttached() instead of `[aria-live]` toBeVisible()
- `lib/wallet/mock-connector.ts` — NEW: wagmi mock() wired to deterministic test address
- `app/(defi)/a11y-wallet-check/page.tsx` — NEW: guarded page (notFound in prod without NEXT_PUBLIC_E2E)
- `app/(defi)/a11y-wallet-check/AuditShell.tsx` — NEW: isolated mock-wired WagmiProvider + RainbowKitProvider + non-readOnly WalletPanel
- `tests/e2e/wallet-connect-modal.spec.ts` — NEW: durable regression guard for DEFI-06's three WCAG criteria
- `playwright.config.ts` — added `env: { NEXT_PUBLIC_E2E: 'true' }` to webServer config

## Decisions Made

- Used `<output>` instead of `<p role="status">` — biome's `useSemanticElements` rule enforces the semantic HTML element; `<output>` has implicit `role=status + aria-live=polite`, which is spec-compliant and more appropriate
- Renamed audit route folder from `_a11y-wallet-check` to `a11y-wallet-check` — Next.js App Router treats folders starting with `_` as private/non-routed (this is a known Next.js convention)
- `NEXT_PUBLIC_E2E` is read directly from `process.env` (not added to the validated env schema) — it's a bypass flag, not an application env var; adding it to the schema would require it in `.env.local` for all devs

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] wallet-read-only.test.tsx: getByText fails with multiple matches**
- **Found during:** Task 1 (running vitest after WalletPanel change)
- **Issue:** `screen.getByText('Solo lectura')` threw "Found multiple elements" because the new `<output>` sr-only node AND the visible pill `<span>` both contain the label text
- **Fix:** Changed to `screen.getAllByText(...)` + filter by `tagName === 'SPAN'` to target the visible pill specifically
- **Files modified:** tests/unit/wallet-read-only.test.tsx
- **Verification:** All 4 tests in the file pass
- **Committed in:** dd8dcff (Task 1 commit)

**2. [Rule 1 - Bug] wallet-states.spec.ts: `[aria-live="polite"]` assertions need update**
- **Found during:** Task 1 (analyzing existing e2e assertions)
- **Issue:** Existing e2e asserted `[aria-live="polite"]` `.toBeVisible()` — but the new `<output>` node is `sr-only` (not visible) and `<output>` doesn't carry `aria-live` as a DOM attribute (it's implicit)
- **Fix:** Changed to `output.sr-only` `.toBeAttached()` + added assertion that `[aria-atomic]` has count 0
- **Files modified:** tests/e2e/wallet-states.spec.ts
- **Committed in:** dd8dcff (Task 1 commit)

**3. [Rule 3 - Blocking] Audit route folder renamed from _a11y-wallet-check to a11y-wallet-check**
- **Found during:** Task 2 (build verification)
- **Issue:** Next.js App Router private folder convention: `_` prefix excludes folders from routing. The `_a11y-wallet-check` folder was built but the route wasn't registered (not in `next build` route output)
- **Fix:** Renamed folder; updated AUDIT_ROUTE constant in e2e spec from `/_a11y-wallet-check` to `/a11y-wallet-check`
- **Files modified:** app/(defi)/a11y-wallet-check/ (folder rename), tests/e2e/wallet-connect-modal.spec.ts
- **Committed in:** 2960ee8 (Task 2 commit)

---

**Total deviations:** 3 auto-fixed (1 Rule 1, 1 Rule 1, 1 Rule 3)
**Impact on plan:** All auto-fixes necessary for correctness. No scope creep.

## Issues Encountered

- biome `useSemanticElements` rule rejected `<p role="status">` — used `<output>` instead (superior semantics, resolves the lint error cleanly)
- biome format/organize-imports errors on new files — resolved with `pnpm biome format --write`

## Checkpoint: Task 3 — Accessibility Auditor Gate

**Status:** STOPPED — awaiting Accessibility Auditor sign-off.

**What to audit:** `/a11y-wallet-check` (local server with NEXT_PUBLIC_E2E=true)

**Three DEFI-06 criteria to verify:**
1. **Keyboard operability (WCAG 2.1.1/2.1.2):** Tab reaches ConnectButton trigger, Enter opens modal, Tab cycles inside, Escape/X/backdrop close returns focus to trigger
2. **Focus restoration on connect-success (WCAG 2.4.3):** After connecting via Mock Connector, `document.activeElement` is the `<output>` node (not `<body>`)
3. **SR announcement (WCAG 4.1.3):** Real screen-reader picks up the `<output>` text change across DISCONNECTED→CONNECTING→CONNECTED transition; only the scoped label is read (not the full panel)

**Verification target:** `05.2-LIVE-VERIFICATION.md` — per-criterion verdicts (✓/⚠/✗) + SR transcript.

## Next Phase Readiness

- After Accessibility Auditor records ✓ for all three criteria and the user types "approved", continue to Task 4: mark DEFI-06 `[x]` in REQUIREMENTS.md and finalize SUMMARY.md

---
*Phase: 05.2-defi-06-wallet-connect-modal-accessibility*
*Partial (Tasks 1+2 of 4 — stopped at Task 3 checkpoint)*
*Completed: 2026-06-02*

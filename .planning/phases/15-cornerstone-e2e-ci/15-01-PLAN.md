---
phase: 15-cornerstone-e2e-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol
  - contracts/script/ProvisionBuildBearDemo.s.sol
  - contracts/script/provision-buildbear-demo.sh
  - contracts/.env.example
autonomous: false
requirements: [E2E-01]
user_setup:
  - service: buildbear
    why: "The provisioning script targets a BuildBear Sandbox (hosted Polygon fork the Vercel UI can reach); the sandbox RPC URL cannot be obtained by Claude"
    env_vars:
      - name: BUILDBEAR_RPC_URL
        source: "BuildBear Dashboard → create a Sandbox forking Polygon (latest) → copy the single Sandbox RPC URL (carries standard JSON-RPC AND the buildbear_* cheat methods; there is NO separate admin RPC). Free 'Explorer' tier sandboxes live 3 DAYS — create within 72h of June 11. Bearer secret → gitignored contracts/.env ONLY, never committed."
      - name: BUILDBEAR_DEPLOYER_PK
        source: "The well-known foundry test account 0 private key (the `test test test … junk` mnemonic, addr 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266 — BuildBear pre-funds its gas). The documented exception to the no-key rule, but STILL env-sourced from gitignored contracts/.env, never hardcoded in a committed file."

must_haves:
  truths:
    - "The already-green resolveFromMandate mint at strike 360360 stays green (regression anchor, in-VM via ALCHEMY_API_KEY — unchanged)"
    - "A standalone in-VM fork test asserts the BASIC live read: quoteMargin returns a BalanceDelta without reverting after the mint, and numberOfLegs(executor) > 0"
    - "A Wave-0 SPIKE proves the INLINED Panoptic-core deploy lands against the hosted BuildBear sandbox IN ISOLATION (metadata resolves, UNIV4_POOL_MANAGER set + PoolManager has code on the Polygon fork, factory/SFPM deploy succeeds) — the single biggest unknown is de-risked before the full provisioning is built"
    - "A provisioning forge script INLINES the DeployProtocol body (no nested-broadcast run() call), threads the READ-BACK deployed addresses (not the hardcoded constants) into deployNewPool + the 9-arg executor ctor, and mints via resolveFromMandate under ONE vm.startBroadcast"
    - "A provisioning bash runner funds the DEPLOYER EOA (the broadcast sender) with wCOP + USDC via buildbear_ERC20Faucet on the SAME single sandbox RPC BEFORE a SINGLE forge --broadcast; the EOA approves + deposit-on-behalf (receiver=executor) inside that one broadcast — no deal(), no whale, no prank, no multi-broadcast split"
    - "The runner reads the live chainId via cast chain-id (31337 — BuildBear re-chains the Polygon fork) and parses broadcast/.../<chainId>/run-latest.json, selecting the resolveFromMandate tx hash, writing it to a frontend-consumable deployments artifact + printing it (BuildBear-explorer-verifiable)"
  artifacts:
    - path: "contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol"
      provides: "An explicit quoteMargin BASIC-read assertion leaf alongside the existing mandate-mint tests"
      contains: "quoteMargin"
    - path: "contracts/script/ProvisionBuildBearDemo.s.sol"
      provides: "The INLINED-deploy + read-back forge script (factory/RiskEngine/SFPM as locals threaded into deployNewPool + ctor; single broadcast; mints)"
      contains: "console2.log"
    - path: "contracts/script/provision-buildbear-demo.sh"
      provides: "The BuildBear sandbox provisioning runner (EOA fund-before-broadcast via buildbear_ERC20Faucet + single forge-script broadcast + chainId-aware run-latest.json parse + deployments-artifact write + mint-tx-hash print)"
      contains: "buildbear_ERC20Faucet"
  key_links:
    - from: "contracts/script/provision-buildbear-demo.sh"
      to: "BuildBear sandbox RPC (EOA funding BEFORE broadcast)"
      via: "cast rpc buildbear_ERC20Faucet on the DEPLOYER EOA against $BUILDBEAR_RPC_URL (the single RPC carries the cheats)"
      pattern: "buildbear_ERC20Faucet"
    - from: "contracts/script/ProvisionBuildBearDemo.s.sol"
      to: "the read-back deployed factory/RiskEngine addresses threaded into deployNewPool + ctor"
      via: "locals from the inlined new PanopticFactoryV4/new RiskEngine, console2.log'd AND consumed (not the hardcoded constant)"
      pattern: "console2\\.log"
    - from: "contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol"
      to: "MacroHedgeExecutor.quoteMargin"
      via: "post-mint margin read"
      pattern: "exec\\.quoteMargin"
---

<objective>
Package the SHIPPED, fork-proven contract mint leg into the judges' demo artifact, now via a HOSTED fork the Vercel UI can reach. Deliverables: (1) the explicit BASIC live-read assertion E2E-01 names (`quoteMargin` returns a `BalanceDelta` without reverting after the mint), proven in-VM (unchanged from the green Foundry fork suite); (2) a **Wave-0 cold-deploy SPIKE** that de-risks the single biggest unknown (the inlined Panoptic-core deploy landing against a HOSTED BuildBear sandbox — never run there before); (3) a **BuildBear Sandbox provisioning flow** (a forge script that INLINES the DeployProtocol body + threads read-back addresses + a bash runner that funds the deployer EOA via `buildbear_ERC20Faucet` BEFORE a single broadcast) that stands up a hosted Polygon-fork (BuildBear forks Polygon at LATEST; chainId 31337), mints via `resolveFromMandate`, writes a frontend-consumable deployments artifact + prints the mint tx hash; (4) the recorded Scenario-1 video (human-verify checkpoint).

Purpose: the CLAUDE.md three-step gate returned NEEDS WORK on the prior local-anvil node (B1 funding, B2 core-deploy) AND on the FIRST hosted-fork draft (broadcast semantics: `deal()`/`prank` are forge-test-only; `new DeployProtocol().run()` self-broadcasts → nested-broadcast hard error + returns nothing). Tenderly's free tier was then found unusable (no API / no public-RPC on free); BuildBear free was research #1 and works (forks Polygon + `buildbear_ERC20Faucet` on the public RPC + a bundled explorer). This revision commits to ONE broadcast-correct sequence: **fund the deployer EOA via `buildbear_ERC20Faucet` on the single sandbox RPC BEFORE the broadcast**, then a SINGLE `forge script --broadcast` does the inlined core deploy + pool + executor + deposit-on-behalf (`receiver=executor`) + mint. B1 resolved by `buildbear_ERC20Faucet` on the EOA; B2 resolved by INLINING the deploy body and reading back the live addresses (sidesteps #8493 + the nested-broadcast + the no-return). NO new contracts, NO bridge, NO monitoring agent, NO `deal()`, NO `vm.prank`, NO multi-broadcast split.
Output: an augmented in-VM fork test file (a new `quoteMargin` leaf), a new `ProvisionBuildBearDemo.s.sol` forge script (inlined deploy + read-back), a new `provision-buildbear-demo.sh` runner (fund-before-broadcast + chainId-aware parse), an updated `.env.example` documenting the BuildBear RPC + the deployer key, a frontend-consumable deployments artifact, and the recorded demo video (the human-verify checkpoint).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cornerstone-e2e-ci/15-RESEARCH.md
@.planning/phases/15-cornerstone-e2e-ci/15-RESEARCH-scaffold-fork.md
@.planning/phases/15-cornerstone-e2e-ci/15-RESEARCH-fork-env-landscape.md
@.planning/phases/15-cornerstone-e2e-ci/15-VALIDATION.md

# The proven mint + the deploy/fund recipe the provisioning must reproduce on the sandbox — the source of truth.
@contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol
# The production core deploy whose BODY the provisioning forge script INLINES (NOT calls — run() self-broadcasts + returns nothing).
@contracts/lib/panoptic-v2-core/script/DeployProtocol.s.sol

<interfaces>
<!-- The executor entrypoints + the deploy recipe the script and the new assertion call. Extracted from the codebase — use these directly, no exploration needed. -->

From contracts/src/MacroHedgeExecutor.sol (SHIPPED, fork-proven):
```solidity
// 9-arg ctor (the exact order/values _deployExecutorWith already uses):
constructor(
    address platform,          // MockPlatform(0.01 ether) — the mint path never touches it
    PanopticPoolV2 _pool,      // the per-run pool from panopticFactory.deployNewPool
    RiskManagement _riskManager,
    uint8 _vegoid,             // IRiskEngine(<deployed riskEngine LOCAL>).vegoid()
    uint256 _beta1TranquilWad, // 0.10e18 TEMPLATE
    uint256 _beta1StressWad,   // 0.35e18 TEMPLATE
    uint256 _targetDevaluationWad, // 0.15e18 TEMPLATE
    uint256 _baseVol,          // 14_400 — TICK-SPACE
    IRegimeOracle _regimeOracle
);
// the mandate front-end (mints at the EXACT structural K_hi tick 360360):
function resolveFromMandate(HedgeMandate calldata mandate, uint256 legIndex, uint128 positionSize)
    external returns (TokenId positionId);
// the BASIC post-mint read (forwards to RiskManagement.quoteCollateralRequirements):
function quoteMargin(TokenId id, int24 strike) external view returns (BalanceDelta);
// chainId guard inside the sink (MacroHedgeExecutor.sol:282):
//   require(uint256(legParams.chainId) == block.chainid, "No crosschain allowed yet");
// legParams.chainId is sourced from mandate.chainId (MacroHedgeExecutor.sol:232), and _demoMandate()
// sets chainId: uint32(block.chainid) (DemoMacroHedgeExecutor.fork.t.sol:416). => the guard
// SELF-SATISFIES on ANY chainId. BuildBear re-chains the Polygon fork to 31337; the guard reads
// block.chainid=31337 and the mandate carries 31337, so it passes. Do NOT reconfigure the sandbox
// chainId for the guard. The ONLY chainId dependency in the whole flow is the broadcast-receipt
// subdir name (run-latest.json path = .../31337/run-latest.json).
```

CollateralTracker.deposit IS standard-4626 deposit-on-behalf (verified CollateralTracker.sol:557-592):
```solidity
function deposit(uint256 assets, address receiver) external payable returns (uint256 shares);
// pulls `assets` from msg.sender (the EOA), mints shares to `receiver` (the executor).
// => the deployer EOA can `ct0.deposit(amount, receiver=executor)` / `ct1.deposit(...)` and the
//    executor ends up the 4626 share owner exactly as the in-VM prank+deposit does — NO prank needed.
```

The in-VM _init_world + _deployExecutorWith recipe the provisioning INLINES, MINUS the cheatcodes
(DemoMacroHedgeExecutor.fork.t.sol:271-300, 376-401):
- in-VM: `deal(token, addr, amt)` then `vm.startPrank(exec)` then `ct.deposit(amt, exec)`.
- ON THE SANDBOX (broadcast): the EOA is pre-funded by the runner's buildbear_ERC20Faucet BEFORE the broadcast;
  inside the single broadcast the EOA `IERC20(token).approve(ct, max)` then `ct.deposit(amt, receiver=exec)`
  (deposit-on-behalf). NO deal(), NO prank.
- The pool/RiskManagement/PanopticQuery are deployed exactly as `_init_world` BUT against the READ-BACK
  factory/riskEngine locals — NOT FACTORY_V4_ADDR / RISK_ENGINE_ADDR (those are the in-VM snapshot
  constants; a fresh sandbox deploy produces different addresses).
- The mandate path mints at strike 360360 (Fix C, fork-proven) via `_demoMandate()`.

DeployProtocol.s.sol — INLINE its body, do NOT call run() (verified DeployProtocol.s.sol):
- `run()` opens its OWN `vm.startBroadcast()` (:37) / `vm.stopBroadcast()` (:146) → calling it from inside
  ProvisionBuildBearDemo's broadcast is a NESTED-BROADCAST hard error. And run() returns NOTHING (:117 the
  `new PanopticFactoryV4(...)` is discarded) → no address handle.
- It reads `./metadata/out/MetadataPackage.json` (:39) via `vm.readFile` (CWD-relative — the spike must
  confirm it resolves under the forge run CWD = contracts/).
- It needs `UNIV4_POOL_MANAGER` (:34) + `UNIV3_FACTORY` (:35) from the OS env (export them in the shell
  BEFORE the forge run — real OS env, NOT vm.setEnv).
- INLINE the body up to and INCLUDING `factory = new PanopticFactoryV4(...)` and `riskEngine = new RiskEngine(...)`
  into local variables; `console2.log` each; thread `factory`/`riskEngine` into `deployNewPool` + the ctor.
- Polygon constants from the fork test: POOL_MANAGER 0x67366782805870060151383F4BbFF9daB53e5cD6, USDC
  0x3c499c542cEF5E3811e1192ce70d8cC03d5c3359 (6dec), WCOP 0x8a1D45e102e886510e891d2Ec656a708991e2D76 (18dec),
  UNIV3_FACTORY 0x1F98431c8aD98523631AE4a59f267346ea31F984. (These addresses are block-invariant on the Polygon
  fork — BuildBear forks Polygon at LATEST, the cold deploy only needs these to have code, NOT the 86.9M pin.)

Existing Somnia deployments.json shape (the artifact pattern to mirror — frontend/lib/apps/abrigo/somnia/deployments.json):
```json
{ "chainId": 50312, "macroOracle": "0x...", "platform": "0x...", "rpcUrl": "https://...", "capturedAt": "ISO8601", "source": "..." }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the explicit quoteMargin BASIC-read assertion leaf to the in-VM demo fork file</name>
  <files>contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol</files>
  <read_first>
    - contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol (the whole file — reuse `_init_world`, `_deployExecutor`, `_demoMandate`, `EXPECTED_KHI_STRIKE`, the `onlyForked` modifier; the margin read at `:351` shows the BalanceDelta read shape)
    - contracts/src/MacroHedgeExecutor.sol (the `quoteMargin(TokenId,int24) -> BalanceDelta` signature)
  </read_first>
  <behavior>
    - The new test mints via `resolveFromMandate` (reusing `_demoMandate()` + `_deployExecutor()` after `_init_world()`), then reads `exec.quoteMargin(positionId, positionId.strike(0))` and asserts the call returns WITHOUT reverting and `numberOfLegs(executor) > 0` — the literal E2E-01 "basic live read (mark/margin), NOT a monitoring agent" claim, as a standalone named leaf.
    - It does NOT assert a specific margin magnitude or any "PnL"/"live value" — a read-back of the just-minted position only (honesty: the e2e grep bans a bare `$value`).
    - The existing four mandate-path tests (`test_resolveFromMandate_mintsThroughExecutor`, `test_executorDecided_surfacesHonestyFlag`, `test_resolveFromMandate_llmIndependentGeometry`, `test_resolveAndMint_sizeOver127_reverts`) MUST stay green — pure addition, zero edits to them.
    - This leaf runs IN-VM via ALCHEMY_API_KEY (the existing Foundry fork harness) — it is INDEPENDENT of the BuildBear provisioning (Tasks 3/4). It is the E2E-01 contract-read proof and the evm-TDD tdd=true leaf; per the existing exception this demo file has NO `.tree`.
  </behavior>
  <action>
    Add ONE new `external onlyForked` test function to `DemoMacroHedgeExecutorForkTest`, named EXACTLY `test_quoteMargin_basicReadAfterMint` (the VALIDATION `--match-test` string this row owns). Body:
    1. `_init_world();` then `(MacroHedgeExecutor exec,) = _deployExecutor();` (FIRST/THEN ordering, matching the sibling tests).
    2. `HedgeMandate memory mandate = _demoMandate();`
    3. `TokenId positionId = exec.resolveFromMandate(mandate, 0, 1e6);`
    4. `int24 strike = positionId.strike(0);` — the EXACT minted strike (360360); the quoteMargin arg is `positionId.strike(0)`, NOT a literal tick (honesty: the read targets the just-minted leg's own strike).
    5. `BalanceDelta marginDelta = exec.quoteMargin(positionId, strike);` — the call returning at all is the proof it does not revert (a reverting read aborts the test). Reference `marginDelta` in a `console2.log` of its two int128 components (via `BalanceDeltaLibrary.amount0/amount1`, or cast through `int256`) so the variable is not an unused-var warning; DO NOT assert a magnitude.
    6. `assertGt(WCOP_USDC_PANOPTIC_POOL.numberOfLegs(address(exec)), 0, "basic read: executor still owns the minted leg");`
    Do NOT add a `.tree` for this file (it has none today; the CI bulloak loop only checks EXISTING trees — see Plan 02). Keep the addition inside the existing contract so no new bulloak root is introduced. If `BalanceDelta` component access needs an import, add `import {BalanceDeltaLibrary} from "v4-core/types/BalanceDelta.sol";` (BalanceDelta is already imported at the top).
  </action>
  <verify>
    <automated>cd contracts && ALCHEMY_API_KEY="${ALCHEMY_API_KEY}" forge test --match-test test_quoteMargin_basicReadAfterMint --retries 2 --delay 3 -vv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'function test_quoteMargin_basicReadAfterMint' contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol` (the named leaf exists)
    - `grep -q 'exec\.quoteMargin' contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol` (it calls the executor's quoteMargin, not riskManagement directly)
    - `grep -q 'positionId\.strike(' contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol` AND the quoteMargin arg is `positionId.strike(0)` (the minted 360360, NOT a literal tick)
    - `! grep -q 'assertEq.*marginDelta\|assertEq.*amount0\|assertEq.*amount1' contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol` (no magnitude assertion on the margin — it is a non-reverting basic read, not a PnL claim)
    - `cd contracts && forge build` exits 0
    - With `ALCHEMY_API_KEY` set: `forge test --match-test test_quoteMargin_basicReadAfterMint` reports `1 passed`; the four existing mandate tests still pass under `forge test --match-test 'test_resolveFromMandate_mintsThroughExecutor|test_executorDecided_surfacesHonestyFlag|test_resolveAndMint_sizeOver127_reverts|test_resolveFromMandate_llmIndependentGeometry'`
  </acceptance_criteria>
  <done>A standalone `test_quoteMargin_basicReadAfterMint` leaf proves the post-mint `quoteMargin` BASIC read returns a `BalanceDelta` without reverting (arg = `positionId.strike(0)`, not a literal) and the executor still owns the leg; the existing mandate suite is un-regressed; `forge build` exit 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Wave-0 SPIKE — prove the INLINED Panoptic-core deploy lands against the hosted BuildBear sandbox in isolation</name>
  <files>contracts/script/ProvisionBuildBearDemo.s.sol</files>
  <read_first>
    - contracts/lib/panoptic-v2-core/script/DeployProtocol.s.sol (the body to inline; the `vm.readFile("./metadata/out/MetadataPackage.json")` at :39; the `vm.envAddress("UNIV4_POOL_MANAGER")` at :34; the `new SemiFungiblePositionManagerV4`/`new RiskEngine`/`new PanopticFactoryV4` at :92/:107/:117)
    - contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol:188-256 (the Polygon constants POOL_MANAGER/UNIV3_FACTORY and the in-VM env setup the spike mirrors)
  </read_first>
  <what-built>
    A FIRST, ISOLATED cut of `ProvisionBuildBearDemo.s.sol` containing ONLY the inlined Panoptic-core deploy (SFPM + RiskEngine + PanopticFactoryV4 + the metadata-pointer loop from DeployProtocol's body) under a single `vm.startBroadcast()`, `console2.log`-ing the factory + riskEngine addresses and `require`-ing their `.code.length > 0`. This is the de-risking spike for the single biggest unknown: the in-VM cold path produced the committed allocs snapshot, so it works in-VM — but it has NEVER run against a HOSTED sandbox. Tasks 3–4 are GATED on this spike passing.
  </what-built>
  <how-to-verify>
    1. Create a BuildBear Sandbox forking Polygon (latest); put `BUILDBEAR_RPC_URL` (the single sandbox RPC — carries JSON-RPC AND the `buildbear_*` cheats) and `BUILDBEAR_DEPLOYER_PK` (the well-known foundry account-0 key) into gitignored `contracts/.env`. (BuildBear free sandboxes live 3 DAYS — create within 72h of the demo.)
    2. The deployer EOA is the foundry test-junk account 0 = `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`; BuildBear pre-funds its native gas. If gas is somehow short, top up via `cast rpc buildbear_nativeFaucet "<EOA>" --rpc-url "$BUILDBEAR_RPC_URL"` (native top-up on the SAME RPC).
    3. Export `UNIV4_POOL_MANAGER=0x67366782805870060151383F4BbFF9daB53e5cD6` and `UNIV3_FACTORY=0x1F98431c8aD98523631AE4a59f267346ea31F984` and `GUARDIAN_ADMIN`/`TREASURER` (the deployer EOA is fine for both) in the shell.
    4. From `contracts/`, run the spike: `forge script script/ProvisionBuildBearDemo.s.sol --sig 'spike()' --rpc-url "$BUILDBEAR_RPC_URL" --broadcast --slow --private-key "$BUILDBEAR_DEPLOYER_PK"`.
    5. Confirm: (a) `./metadata/out/MetadataPackage.json` resolved (no `vm.readFile` revert), (b) the PoolManager `0x6736…5cD6` has code on the sandbox (`cast code 0x6736… --rpc-url $BUILDBEAR_RPC_URL | head -c 12` is non-`0x`), (c) the run logs `FACTORY_ADDRESS=0x…` + `RISK_ENGINE_ADDRESS=0x…` with non-zero code, (d) `cast chain-id --rpc-url $BUILDBEAR_RPC_URL` returns `31337`.
  </how-to-verify>
  <action>
    Create `contracts/script/ProvisionBuildBearDemo.s.sol` (`is Script`) with a `spike()` external entrypoint that INLINES the DeployProtocol body (NOT `new DeployProtocol().run()`): copy the metadata-pointer loop + `new SemiFungiblePositionManagerV4(uniPoolManager, 21*10**20, 21*10**20, 10000)` + `new PanopticGuardian(...)` + `new BuilderFactory(...)` + `RiskEngine riskEngine = new RiskEngine(10_000_000, 10_000_000, address(panopticGuardian), address(builderFactory))` + `CollateralTrackerV2` + `PanopticFactoryV4 factory = new PanopticFactoryV4(sfpm, uniPoolManager, address(new PanopticPoolV2(...)), collateralTracker, props, indices, pointers)` — all under ONE `vm.startBroadcast()/vm.stopBroadcast()`. Read `UNIV4_POOL_MANAGER`/`UNIV3_FACTORY` via `vm.envAddress` (the runner exports them as real OS env). `console2.log("FACTORY_ADDRESS=", address(factory))` and `console2.log("RISK_ENGINE_ADDRESS=", address(riskEngine))`; `require(address(factory).code.length > 0, "core deploy failed")` and the same for riskEngine. Header comment: this is the Wave-0 cold-deploy spike — it proves the inlined deploy lands on a hosted BuildBear sandbox (the in-VM cold path made the snapshot but has never run against a hosted node); BuildBear forks Polygon at LATEST and re-chains it to 31337 — the cold deploy only needs the block-invariant wCOP/USDC + v4 PoolManager addresses to have code, NOT the 86.9M pin. Do NOT add the pool/executor/mint yet — Task 3 extends THIS file's `run()` entrypoint.
  </action>
  <acceptance_criteria>
    - `grep -q 'function spike' contracts/script/ProvisionBuildBearDemo.s.sol` (the isolated spike entrypoint exists)
    - `! grep -q 'new DeployProtocol' contracts/script/ProvisionBuildBearDemo.s.sol` (the body is INLINED, run() is NOT called — no nested broadcast)
    - `grep -q 'new PanopticFactoryV4' contracts/script/ProvisionBuildBearDemo.s.sol` AND `grep -q 'new RiskEngine' contracts/script/ProvisionBuildBearDemo.s.sol` (the deploy is inlined as locals)
    - `grep -q 'FACTORY_ADDRESS' contracts/script/ProvisionBuildBearDemo.s.sol` AND `grep -q 'RISK_ENGINE_ADDRESS' contracts/script/ProvisionBuildBearDemo.s.sol` (read-back addresses logged)
    - `cd contracts && forge build` exits 0
    - (HUMAN, with the sandbox up) the spike run logs a non-zero `FACTORY_ADDRESS` + `RISK_ENGINE_ADDRESS`, `./metadata/out/MetadataPackage.json` resolved, `cast code 0x6736…5cD6` is non-`0x` on the sandbox, and `cast chain-id` returns `31337`
  </acceptance_criteria>
  <verify>
    <automated>cd contracts && forge build 2>&1 | tail -5 && grep -q 'function spike' script/ProvisionBuildBearDemo.s.sol && ! grep -q 'new DeployProtocol' script/ProvisionBuildBearDemo.s.sol && echo SPIKE_INLINED_OK</automated>
  </verify>
  <done>`ProvisionBuildBearDemo.s.sol` exists with an isolated `spike()` that INLINES the DeployProtocol core deploy (no `new DeployProtocol().run()`), compiles, and — verified by the human against a live sandbox — lands the factory + riskEngine with non-zero code, the metadata file resolves, the Polygon PoolManager has code on the fork, and `cast chain-id` is 31337. Tasks 3–4 are unblocked.</done>
  <resume-signal>Type "spike passed" (note the FACTORY_ADDRESS / RISK_ENGINE_ADDRESS), or describe what failed (metadata not found, PoolManager has no code, deploy revert, nested-broadcast error).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Extend ProvisionBuildBearDemo.s.sol with the full run() — read-back addresses threaded into pool + executor + deposit-on-behalf + mint</name>
  <files>contracts/script/ProvisionBuildBearDemo.s.sol, contracts/.env.example</files>
  <read_first>
    - contracts/script/ProvisionBuildBearDemo.s.sol (Task 2 output — the inlined `spike()` deploy body to reuse in `run()`)
    - contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol:271-300, 376-419 (the `_init_world` pool/RiskManagement deploy, the 9-arg `_deployExecutorWith` ctor order/values, `_demoMandate`)
    - contracts/lib/panoptic-v2-core/contracts/CollateralTracker.sol:557-592 (deposit-on-behalf — msg.sender pays, receiver gets shares)
    - contracts/.env.example (the key-name documentation convention — add the BuildBear RPC + the deployer key, values empty)
  </read_first>
  <action>
    Add a `run()` external entrypoint to `ProvisionBuildBearDemo.s.sol` that BROADCASTS the FULL provisioning against a live node (the BuildBear sandbox, reached via `--rpc-url $BUILDBEAR_RPC_URL --broadcast` from the runner in Task 4). ONE `vm.startBroadcast()/vm.stopBroadcast()` wraps EVERYTHING (the deployer EOA is pre-funded by the runner BEFORE the broadcast, so the broadcast never needs to interleave a cheat call). Structure inside the single broadcast:
    1. INLINE the core deploy (reuse the `spike()` body): `PanopticFactoryV4 factory = new PanopticFactoryV4(...)`, `RiskEngine riskEngine = new RiskEngine(...)` — as LOCALS. Do NOT call `new DeployProtocol().run()`. Do NOT read `FACTORY_V4_ADDR`/`RISK_ENGINE_ADDR` constants.
    2. Thread the LOCALS: `PanopticQuery panopticQuery = new PanopticQuery();` then `PanopticPoolV2 pool = factory.deployNewPool(wcopUsdcKey, IRiskEngine(address(riskEngine)), 0);` (the read-back `factory`/`riskEngine`, NOT the constants; wcopUsdcKey: currency0=USDC, currency1=WCOP, fee=3000, tickSpacing=60, hooks=address(0)). Then `RiskManagement riskManagement = new RiskManagement(panopticQuery, pool);`.
    3. Deploy the executor with the EXACT 9-arg ctor values from `_deployExecutorWith`, threading the locals: `new MacroHedgeExecutor(address(new MockPlatform(0.01 ether)), pool, riskManagement, IRiskEngine(address(riskEngine)).vegoid(), 0.10e18, 0.35e18, 0.15e18, 14_400, <MockRegimeOracle set to Stress>)`. (`riskEngine` LOCAL drives `vegoid()`, not the constant.)
    4. Deposit-on-behalf (NO deal(), NO prank — the EOA is the broadcast sender and is pre-funded): `IERC20Partial(POLYGON_USDC).approve(address(ct0), max); IERC20Partial(POLYGON_WCOP).approve(address(ct1), max); ct0.deposit(DEFAULT_FUND_USD, address(exec)); ct1.deposit(DEFAULT_FUND_COP, address(exec));` where ct0/ct1 are `pool.collateralToken0()/collateralToken1()`. `receiver=exec` ⇒ executor is the 4626 owner exactly as in-VM.
    5. `TokenId positionId = exec.resolveFromMandate(_demoMandate(), 0, 1e6);` then read `int24 strike = positionId.strike(0);` + `pool.numberOfLegs(address(exec))` + `exec.quoteMargin(positionId, strike)`.
    6. `console2.log` a clearly-delimited block of the READ-BACK locals + results: `EXECUTOR_ADDRESS=`, `POOL_ADDRESS=`, `RISK_MANAGEMENT_ADDRESS=`, `FACTORY_ADDRESS=`, `RISK_ENGINE_ADDRESS=`, `MINTED_STRIKE=`, `NUMBER_OF_LEGS=` — the values the runner greps into the deployments artifact (the mint tx hash itself comes from the broadcast receipt, captured by the runner in Task 4).
    Header comment (extend the spike's): the chainId guard SELF-SATISFIES (`_demoMandate` sets `chainId=uint32(block.chainid)` = 31337 on the sandbox) — do NOT reconfigure the sandbox chainId; B1 funding is the runner's buildbear_ERC20Faucet on the EOA BEFORE the broadcast, B2 is this inlined read-back deploy; NO deal()/prank/multi-broadcast.
    Then edit `contracts/.env.example`: append the BuildBear key names + the deployer key with empty values + a comment ("BuildBear Sandbox — hosted Polygon fork (chainId 31337) for the UI-visible demo mint; the single sandbox RPC carries the buildbear_* cheats; bearer secret, server-side ONLY, gitignored, 3-day free TTL"): `BUILDBEAR_RPC_URL=`, `BUILDBEAR_DEPLOYER_PK=`, and keep/ensure `UNIV4_POOL_MANAGER=`, `UNIV3_FACTORY=` documented (the inlined deploy reads them).
  </action>
  <verify>
    <automated>cd contracts && forge build 2>&1 | tail -5 && grep -c 'console2.log' script/ProvisionBuildBearDemo.s.sol && grep -q 'BUILDBEAR_DEPLOYER_PK' .env.example && echo ENV_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd contracts && forge build` exits 0 (the script compiles)
    - `! grep -q 'new DeployProtocol' contracts/script/ProvisionBuildBearDemo.s.sol` (inlined, no nested-broadcast run() call)
    - `! grep -q 'FACTORY_V4_ADDR\|RISK_ENGINE_ADDR' contracts/script/ProvisionBuildBearDemo.s.sol` (the read-back LOCALS are threaded into deployNewPool + the ctor — the hardcoded constants are NEVER consumed)
    - `grep -q 'deployNewPool' contracts/script/ProvisionBuildBearDemo.s.sol` AND `grep -q 'vegoid' contracts/script/ProvisionBuildBearDemo.s.sol` (the pool deploy + the ctor both consume the read-back factory/riskEngine locals)
    - `grep -q 'EXECUTOR_ADDRESS' contracts/script/ProvisionBuildBearDemo.s.sol` AND `grep -q 'POOL_ADDRESS' contracts/script/ProvisionBuildBearDemo.s.sol` AND `grep -q 'MINTED_STRIKE' contracts/script/ProvisionBuildBearDemo.s.sol` (it logs the read-back addresses + result)
    - `grep -q 'resolveFromMandate' contracts/script/ProvisionBuildBearDemo.s.sol` (it drives the proven mint path)
    - `! grep -q 'deal(' contracts/script/ProvisionBuildBearDemo.s.sol` AND `! grep -q 'startPrank\|vm.prank' contracts/script/ProvisionBuildBearDemo.s.sol` (NO forge-only cheatcodes — funding is the runner's buildbear_ERC20Faucet, deposit is on-behalf under the single broadcast)
    - `! grep -qE '0x[a-fA-F0-9]{64}' contracts/script/ProvisionBuildBearDemo.s.sol` (no committed private key)
    - `grep -q 'BUILDBEAR_RPC_URL' contracts/.env.example` AND `grep -q 'BUILDBEAR_DEPLOYER_PK' contracts/.env.example` (BuildBear RPC + deployer key names documented, values empty)
  </acceptance_criteria>
  <done>`ProvisionBuildBearDemo.s.sol` `run()` compiles, INLINES the core deploy, threads the READ-BACK factory/riskEngine LOCALS into `deployNewPool` + the 9-arg ctor (never the hardcoded constants), deposit-on-behalf (`receiver=exec`, no deal/prank), mints via `resolveFromMandate` under ONE broadcast, and logs the read-back addresses + result; `.env.example` documents the BuildBear RPC + deployer key (empty values).</done>
</task>

<task type="auto">
  <name>Task 4: Author provision-buildbear-demo.sh — fund the EOA BEFORE a single broadcast + chainId-aware run-latest.json parse + deployments artifact + mint-tx print</name>
  <files>contracts/script/provision-buildbear-demo.sh</files>
  <read_first>
    - contracts/script/macro-hedge-strategist-e2e.sh (the repo's existing cast/bash runner conventions — env from gitignored contracts/.env, set -euo pipefail style, slash/host handling)
    - contracts/script/ProvisionBuildBearDemo.s.sol (Task 3 output — the `run()` this runner broadcasts; the console2 EXECUTOR_ADDRESS/POOL_ADDRESS/MINTED_STRIKE lines it greps)
    - .planning/phases/15-cornerstone-e2e-ci/15-RESEARCH-fork-env-landscape.md § "FREE-tier Polygon-fork options" (the BuildBear cheat-method name buildbear_ERC20Faucet + buildbear_nativeFaucet, on the single sandbox RPC)
    - /home/jmsbpp/apps/d2p/frontend/lib/apps/abrigo/somnia/deployments.json (the artifact shape to mirror: chainId, addresses, rpcUrl, capturedAt, source)
  </read_first>
  <action>
    Create `contracts/script/provision-buildbear-demo.sh` (chmod +x), `#!/usr/bin/env bash` + `set -euo pipefail`. It provisions the hosted demo against a BuildBear Sandbox (Polygon fork at LATEST, chainId 31337) with ONE forge broadcast, funding the deployer EOA BEFORE it. Concretely:
    1. Source `contracts/.env` if present; hard-fail with a clear message listing the missing var if either of `BUILDBEAR_RPC_URL`, `BUILDBEAR_DEPLOYER_PK` is unset (secrets stay in gitignored `.env`, NEVER hardcoded — the well-known account-0 key is the documented exception but is STILL env-sourced). Comment that the sandbox is created in the BuildBear dashboard; this runner TARGETS an existing sandbox via its single RPC (which carries the `buildbear_*` cheats — there is NO separate admin RPC).
    2. Define the Polygon token constants (USDC 0x3c499c542cEF5E3811e1192ce70d8cC03d5c3359, WCOP 0x8a1D45e102e886510e891d2Ec656a708991e2D76) and the fund amounts (USDC 10_000e6, WCOP 10_000e18). Export `UNIV4_POOL_MANAGER=0x67366782805870060151383F4BbFF9daB53e5cD6`, `UNIV3_FACTORY=0x1F98431c8aD98523631AE4a59f267346ea31F984`, `GUARDIAN_ADMIN`/`TREASURER` (= the deployer EOA addr) as REAL OS env BEFORE the forge run (the inlined deploy reads them via vm.envAddress).
    3. Derive the broadcasting EOA address from `BUILDBEAR_DEPLOYER_PK` via `cast wallet address --private-key "$BUILDBEAR_DEPLOYER_PK"` (NEVER a literal key/addr). **BEFORE the broadcast**, fund the EOA on the sandbox via the SAME single RPC: `cast rpc buildbear_ERC20Faucet "$USDC" "$EOA" 0x... --rpc-url "$BUILDBEAR_RPC_URL"` + the same for `$WCOP` (the wCOP/USDC the EOA deposits-on-behalf to the executor). This is the B1 resolution — arbitrary ERC20, no whale, no deal(). BuildBear pre-funds the test-junk EOA's native gas; add a `cast rpc buildbear_nativeFaucet "$EOA" --rpc-url "$BUILDBEAR_RPC_URL"` top-up ONLY if a dry gas check shows it short. Because the EOA is funded HERE on the same RPC, there is no public-vs-admin RPC split and the single broadcast never interleaves a cheat call. NOTE: confirm the exact `buildbear_ERC20Faucet` parameter order against the sandbox on first run (the live-verify caveat) — `(token, recipient, amount)` per the docs.
    4. Run the SINGLE forge broadcast: `forge script script/ProvisionBuildBearDemo.s.sol --rpc-url "$BUILDBEAR_RPC_URL" --broadcast --slow --private-key "$BUILDBEAR_DEPLOYER_PK"` (the inlined deploy + pool + executor + deposit-on-behalf + mint, all in `run()`). Capture stdout to a temp file. PARSE the read-back addresses (executor/pool/factory/riskManagement/riskEngine, MINTED_STRIKE, NUMBER_OF_LEGS) from the console2 `*=` lines.
    5. Read the live chainId: `CHAIN_ID=$(cast chain-id --rpc-url "$BUILDBEAR_RPC_URL")` (= 31337 on the sandbox). Parse the mint tx hash from the chainId-aware broadcast receipt `broadcast/ProvisionBuildBearDemo.s.sol/$CHAIN_ID/run-latest.json` with `jq` — SELECT the tx whose function is `resolveFromMandate` (NOT `transactions[0]`): e.g. `jq -r '.transactions[] | select(.function != null and (.function|test("resolveFromMandate"))) | .hash' "$RECEIPT"`. That is `MINT_TX_HASH`.
    6. Write a frontend-consumable deployments artifact `contracts/script/out/buildbear-deployments.json` (mkdir -p) mirroring the Somnia shape: `{ "chainId": <CHAIN_ID>, "executor": "0x...", "pool": "0x...", "riskManagement": "0x...", "factory": "0x...", "rpcUrl": "<BUILDBEAR_RPC_URL>", "mintTxHash": "0x...", "mintedStrike": 360360, "capturedAt": "<ISO8601>", "source": "abrigo-somnia 15-01 provision-buildbear-demo.sh" }`. The single sandbox RPC goes in the artifact — it is the browser-reachable endpoint.
    7. Print a clearly-delimited final block: `EXECUTOR_ADDRESS=...`, `POOL_ADDRESS=...`, `RPC_URL=<sandbox>`, `MINT_TX_HASH=...` (verifiable in the BuildBear explorer — BuildBear auto-provisions a per-sandbox block explorer), `MINTED_STRIKE=...`, `NUMBER_OF_LEGS=...`.
    Header comment naming: the B1/B2 resolution (EOA buildbear_ERC20Faucet BEFORE a single broadcast + inlined read-back deploy), the foundry#8493 sidestep (NO --load-state — a fresh deploy on the persistent sandbox), the sandbox RPC is a bearer secret server-side only, the 3-day free TTL (create within 72h), the BuildBear-free RPC rate cap (do NOT loop this in CI — it is a Wave-0/checkpoint step), and the browser→BuildBear CORS caveat (verify with one eth_chainId fetch; if blocked, the frontend proxies the sandbox RPC through a Next.js route — the Somnia keeper-proxy pattern; sibling-repo's job, see Plan 03).
  </action>
  <verify>
    <automated>cd contracts && bash -n script/provision-buildbear-demo.sh && echo SYNTAX_OK && grep -c 'buildbear_ERC20Faucet' script/provision-buildbear-demo.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x contracts/script/provision-buildbear-demo.sh` (executable)
    - `grep -q 'buildbear_ERC20Faucet' contracts/script/provision-buildbear-demo.sh` (B1 resolved — arbitrary ERC20 funding on the EOA, no whale/deal)
    - `grep -q 'cast wallet address' contracts/script/provision-buildbear-demo.sh` (the EOA addr is derived from the key, never a literal)
    - `! grep -q 'load-state' contracts/script/provision-buildbear-demo.sh` (foundry#8493 sidestepped — no --load-state)
    - `! grep -qi 'tenderly' contracts/script/provision-buildbear-demo.sh` (no stale Tenderly references)
    - `grep -q 'ProvisionBuildBearDemo.s.sol' contracts/script/provision-buildbear-demo.sh` AND `grep -q -- '--broadcast' contracts/script/provision-buildbear-demo.sh` AND `[ "$(grep -c -- '--broadcast' contracts/script/provision-buildbear-demo.sh)" = 1 ]` (a SINGLE broadcast, not a multi-broadcast split)
    - `grep -q 'cast chain-id' contracts/script/provision-buildbear-demo.sh` AND `grep -q 'resolveFromMandate' contracts/script/provision-buildbear-demo.sh` (chainId-aware run-latest.json path + selects the resolveFromMandate tx, not transactions[0])
    - `grep -q 'BUILDBEAR_RPC_URL' contracts/script/provision-buildbear-demo.sh` AND `[ "$(grep -c 'BUILDBEAR_RPC_URL' contracts/script/provision-buildbear-demo.sh)" -ge 1 ]` (reaches the single sandbox RPC; the cheats + the broadcast share it — no admin/public split)
    - `! grep -qE '0x[a-fA-F0-9]{64}' contracts/script/provision-buildbear-demo.sh` (NO 64-hex private key literal committed — keys read from gitignored .env)
    - `grep -q 'buildbear-deployments.json' contracts/script/provision-buildbear-demo.sh` AND `grep -qE 'MINT_TX_HASH|mintTxHash' contracts/script/provision-buildbear-demo.sh` (writes the deployments artifact incl. read-back addresses + RPC, prints/records the mint tx hash)
    - `bash -n contracts/script/provision-buildbear-demo.sh` exits 0 (valid bash)
  </acceptance_criteria>
  <done>`provision-buildbear-demo.sh` is an executable, syntax-valid runner that funds the deployer EOA (wCOP + USDC via buildbear_ERC20Faucet on the single sandbox RPC, native via buildbear_nativeFaucet if needed) BEFORE a SINGLE `forge script --broadcast` of `run()` (deposit-on-behalf + mint, B1+B2; never --load-state), parses the chainId-aware (31337) run-latest.json selecting the `resolveFromMandate` tx for `MINT_TX_HASH`, writes `buildbear-deployments.json` with the read-back addresses + sandbox RPC + mint tx hash, and prints the mint tx hash; secrets come from gitignored `.env`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human provisions the BuildBear sandbox + records the Scenario-1 video (judges' artifact)</name>
  <files>contracts/script/provision-buildbear-demo.sh</files>
  <read_first>
    - contracts/script/provision-buildbear-demo.sh (Task 4 output)
    - .planning/phases/15-cornerstone-e2e-ci/15-VALIDATION.md § Manual-Only Verifications (the MVP deliverable vs the sibling-repo-gated real-UI flow)
    - .planning/phases/15-cornerstone-e2e-ci/15-RESEARCH.md § "Phase-15 Demo-Env DECISION (revised post-gate)" (the no-bridge / Agent-1-live-on-Somnia framing)
  </read_first>
  <what-built>
    A BuildBear-sandbox provisioning runner (`provision-buildbear-demo.sh` + `ProvisionBuildBearDemo.s.sol`) that funds the deployer EOA via `buildbear_ERC20Faucet` on the single sandbox RPC BEFORE a single forge broadcast that inlines the Panoptic-core deploy, threads the read-back addresses into the pool + the 9-arg executor, deposit-on-behalf funds the executor, mints the real wCOP/USDC position at strike 360360 through `resolveFromMandate`, writes `buildbear-deployments.json`, and prints an explorer-verifiable `MINT_TX_HASH` — plus the green in-VM `quoteMargin` basic-read assertion. The abrigo-somnia MVP deliverable is THIS provisioning + the artifact + the printed mint tx hash + the refreshed handoff doc (Plan 03). The browser prompt→mint UI flow is a SIBLING-REPO deliverable (the d2p-frontend mint panel reading the position/decision off the live sandbox) — NOT guaranteed by June 11.
  </what-built>
  <how-to-verify>
    1. (Prereq: Task 2 spike PASSED — the inlined core deploy already proven to land on the sandbox.) Ensure the BuildBear Sandbox (Polygon fork, latest) is up and `BUILDBEAR_RPC_URL`, `BUILDBEAR_DEPLOYER_PK` are in gitignored `contracts/.env`. (Free sandboxes live 3 days — create within 72h of demo day.)
    2. From `contracts/`, run `bash script/provision-buildbear-demo.sh`. Confirm it prints `EXECUTOR_ADDRESS`, `POOL_ADDRESS`, `RPC_URL=<sandbox>`, `MINT_TX_HASH=0x...`, `MINTED_STRIKE=360360`, and `NUMBER_OF_LEGS` > 0 with no revert, and that `contracts/script/out/buildbear-deployments.json` was written.
    3. Open the per-sandbox BuildBear block explorer and confirm the `MINT_TX_HASH` transaction exists and succeeded (the automated 'real mint happened' proof — this is the MVP deliverable's core artifact; the explorer gives the judges a clickable tx link).
    4. (MVP deliverable artifact) Record a screen capture of the script run + the BuildBear explorer showing the `MINT_TX_HASH` succeeded, narrated over the existing cornerstone UI to tell the Scenario-1 story (prompt → Agent-1 recorded-live-on-Somnia trace → Agent-2 decision card 8-param `ExecutorDecided` honesty flag `nonErgodicDisclosed=true` + TEMPLATE caveat → the real mint tx on the BuildBear Polygon fork → the basic margin read). On-screen, the honesty label MUST appear: "Agent 1 runs live on Somnia testnet; Agent 2 mints on a hosted Polygon fork (BuildBear) — the mandate is carried by this app, there is NO cross-chain bridge in this demo (deferred)."
    5. STRETCH (sibling-repo-gated, NOT required for the MVP): if the d2p-frontend mint panel (Plan 03 handoff) is wired to read the position/decision off the live sandbox by demo day, record the real browser prompt→mint flow instead. This depends on sibling-repo wiring not guaranteed by June 11.
    6. FALLBACK if the hosted env fails on demo day: a recorded `make test-demo` run (the in-VM mint is already green).
  </how-to-verify>
  <action>
    The contract leg is already automated (Task 1 in-VM + Tasks 3/4 against the sandbox, Task 2 spike de-risked the deploy). This checkpoint is the human-only steps Claude cannot perform: (1) ensure the BuildBear Sandbox is up + keys in gitignored `.env`, (2) run `bash contracts/script/provision-buildbear-demo.sh`, confirm `MINT_TX_HASH` + `MINTED_STRIKE=360360` + `NUMBER_OF_LEGS>0` and the explorer tx, then (3) record the MVP-deliverable artifact — the script-run + explorer-tx capture narrated over the cornerstone UI with the on-screen no-bridge honesty label, saved into the gitignored `.hackathon/` war-room. Everything Claude CAN automate (funding, deploy, mint, reads, artifact, scripts) is done before this gate.
  </action>
  <acceptance_criteria>
    - The script run prints `MINT_TX_HASH=0x...`, `MINTED_STRIKE=360360`, and `NUMBER_OF_LEGS` > 0; `buildbear-deployments.json` exists with the read-back addresses + sandbox RPC; the tx is visible + succeeded in the BuildBear explorer (the MVP deliverable).
    - A screen recording exists capturing the Scenario-1 narrative (script run + explorer tx, narrated over the cornerstone UI) with the on-screen no-bridge honesty label (Agent-1-live-on-Somnia / Agent-2-on-hosted-Polygon-fork). The real-browser-prompt→mint flow is an explicitly-labeled STRETCH gated on sibling-repo wiring, NOT a success condition for this plan.
  </acceptance_criteria>
  <verify>
    <automated>cd contracts && bash -n script/provision-buildbear-demo.sh && echo CHECKPOINT_SCRIPT_SYNTAX_OK</automated>
  </verify>
  <done>The BuildBear sandbox provisions and the script prints MINT_TX_HASH + MINTED_STRIKE=360360 + NUMBER_OF_LEGS>0 (tx verifiable + succeeded in the BuildBear explorer); `buildbear-deployments.json` is written; the MVP-deliverable screen recording (script-run + explorer-tx narrated over the cornerstone UI, no-bridge honesty label) is saved. The real-browser UI E2E is a labeled sibling-repo-gated STRETCH, not required here.</done>
  <resume-signal>Type "approved" (note where the video + the mint tx hash are), or describe what failed (sandbox, EOA funding, deploy revert, mint revert, run-latest.json parse, CORS).</resume-signal>
</task>

</tasks>

<verification>
- `cd contracts && forge build` exits 0.
- With `ALCHEMY_API_KEY`: `forge test --match-test test_quoteMargin_basicReadAfterMint --retries 2` passes; the existing mandate suite (`make test-demo`) stays green (demo fork 6/6 → now 7/7 with the new leaf).
- `ProvisionBuildBearDemo.s.sol` compiles; the `spike()` + `run()` INLINE the core deploy (no `new DeployProtocol`), thread the read-back locals into `deployNewPool` + the ctor (never `FACTORY_V4_ADDR`/`RISK_ENGINE_ADDR`), use NO `deal()`/`prank`.
- `bash -n contracts/script/provision-buildbear-demo.sh` exits 0; the runner funds the EOA via `buildbear_ERC20Faucet` on the single sandbox RPC BEFORE a SINGLE `--broadcast`; parses the chainId-aware (31337) run-latest.json selecting the `resolveFromMandate` tx; contains NO `load-state`; no 64-hex key literal; no stale `tenderly` reference.
- `.env.example` documents `BUILDBEAR_RPC_URL` + `BUILDBEAR_DEPLOYER_PK` (empty values).
- The Wave-0 spike (Task 2) lands the core deploy on the sandbox; then the BuildBear sandbox provisions, prints a mint tx hash, writes `buildbear-deployments.json`, and the demo video is captured (Task 5 checkpoint).
</verification>

<success_criteria>
- E2E-01 BASIC-read row covered by a standalone in-VM `test_quoteMargin_basicReadAfterMint` leaf (non-reverting margin read at `positionId.strike(0)`, no PnL claim).
- E2E-01 demo-run row covered by `ProvisionBuildBearDemo.s.sol` (inlined deploy + read-back addresses threaded into pool + ctor + deposit-on-behalf + mint, ONE broadcast) + `provision-buildbear-demo.sh` (EOA-funded-before-broadcast via buildbear_ERC20Faucet, chainId-aware mint-tx parse, deployments artifact) + the recorded video.
- B1 (funding) resolved via `buildbear_ERC20Faucet` on the deployer EOA BEFORE the broadcast; B2 (core deploy) resolved via INLINED deploy + read-back locals (no nested broadcast, no --load-state, no hardcoded FACTORY_V4_ADDR consumption). The Wave-0 spike de-risks the hosted cold deploy.
- The mandate-mint + honesty-flag regression anchors stay green; Agent-1 stays LIVE on real Somnia testnet (never forked); no bridge.
- E2E-01's real-browser UI flow is honestly scoped as a SIBLING-REPO-gated stretch; the MVP deliverable = the provisioning + mint tx hash + artifact + handoff (Plan 03), with the script-run-narrated-over-the-UI video as the guaranteed artifact.
- No monitoring agent, no public Panoptic deploy to a real chain (the sandbox is a hosted fork); secrets only in gitignored `.env`.
</success_criteria>

<output>
After completion, create `.planning/phases/15-cornerstone-e2e-ci/15-01-SUMMARY.md`
</output>

---
phase: 15-cornerstone-e2e-ci
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/contracts-ci.yml
autonomous: true
requirements: [E2E-02]
user_setup:
  - service: github-actions
    why: "The polygon-fork CI job needs the ALCHEMY_API_KEY repo secret to actually run green on a runner (it skips gracefully without it)"
    env_vars:
      - name: ALCHEMY_API_KEY
        source: "GitHub repo → Settings → Secrets and variables → Actions → New repository secret (the Alchemy key used by the polygon fork tests)"

must_haves:
  truths:
    - "contracts-ci.yml gains a `polygon` job that runs the Polygon-fork tests gated on the ALCHEMY_API_KEY secret (the var the tests actually read), with an RPC cache + retries, skipping gracefully when the secret is unset"
    - "The keyless build-and-spec job (forge build + the per-file bulloak loop over every EXISTING .tree + the fork-excluded keyless suite) stays green and unchanged"
    - "somnia-e2e stays workflow_dispatch-only (never auto-spends STT); no live e2e is wired into push/PR"
    - "No invented --shard flag — the 429 defense is the RPC cache keyed on the pinned block + --retries, documented honestly"
  artifacts:
    - path: ".github/workflows/contracts-ci.yml"
      provides: "The polygon-fork CI job mirroring the existing base `fork` job"
      contains: "foundry-rpc-polygon-86900000-v1"
  key_links:
    - from: ".github/workflows/contracts-ci.yml (polygon job)"
      to: "the Polygon-fork test (DemoMacroHedgeExecutor.fork.t.sol + MacroHedgeExecutor.fork.t.sol)"
      via: "forge test --match-path 'test/**/*fork*' gated on ALCHEMY_API_KEY"
      pattern: "ALCHEMY_API_KEY"
---

<objective>
Extend `contracts-ci.yml` with a `polygon` fork job that gates the repo on the Polygon-fork mint suite — mirroring the proven base `fork` job's cache+retry+secret-skip posture, but keyed on `ALCHEMY_API_KEY` (the variable the Polygon fork tests actually read, NOT a new `POLYGON_RPC_URL`) and on a `foundry-rpc-polygon-86900000-v1` cache. The keyless build/bulloak job and the `workflow_dispatch`-only `somnia-e2e` stay exactly as they are.

Purpose: E2E-02 — "`contracts-ci.yml` gates the repo: `forge build` + per-file `bulloak check` + the fork tests (a `polygon` fork endpoint via an Actions secret + Foundry RPC cache keyed on the pinned block, sharded to dodge the 429); the Somnia-testnet + Polygon-mint live e2e stays a manual `workflow_dispatch`." The "sharded" wording is satisfied by the RPC cache + retries (Foundry has NO `--shard` flag — the existing job already documents this).
Output: the modified workflow with one new `polygon` job.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-cornerstone-e2e-ci/15-RESEARCH.md
@.planning/phases/15-cornerstone-e2e-ci/15-VALIDATION.md

# The workflow to extend — the existing `fork` job is the exact template.
@.github/workflows/contracts-ci.yml

<interfaces>
<!-- The existing base `fork` job to mirror (contracts-ci.yml:84-114). The polygon job is a near-copy with the secret name + cache key + match-path swapped. -->

The base `fork` job (the template):
- `env: BASE_RPC_URL: ${{ secrets.BASE_RPC_URL }}` (secret on that job only)
- `actions/cache@v4` path `~/.foundry/cache/rpc`, key `foundry-rpc-base-46700000-v1`, restore-keys `foundry-rpc-base-`
- run step: skip-with-`::warning::` + `exit 0` when the secret is empty; else `forge test --match-path 'test/**/*fork*' --retries 2 --delay 3 -vvv`
- comment notes: "Foundry has no --shard flag. The pinned-block RPC cache + --retries absorb the Alchemy cold-cache 429."

The Polygon fork tests read `ALCHEMY_API_KEY` (NOT BASE_RPC_URL):
- `DemoMacroHedgeExecutor.fork.t.sol:221`: `try vm.envString("ALCHEMY_API_KEY") ... vm.createSelectFork(vm.rpcUrl("polygon"), 86_900_000)`
- `foundry.toml:26`: `polygon = "https://polygon-mainnet.g.alchemy.com/v2/${ALCHEMY_API_KEY}"`
- pinned fork block: `86_900_000` → cache key `foundry-rpc-polygon-86900000-v1`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the `polygon` fork job to contracts-ci.yml</name>
  <files>.github/workflows/contracts-ci.yml</files>
  <read_first>
    - .github/workflows/contracts-ci.yml (the whole file — the `fork` job at :84-114 is the template; the `build-and-spec` and `somnia-e2e` jobs MUST stay byte-unchanged)
    - contracts/foundry.toml:24-26 (the `polygon` rpc endpoint reads `${ALCHEMY_API_KEY}`)
  </read_first>
  <action>
    Add a new job `polygon:` to `.github/workflows/contracts-ci.yml`, placed AFTER the `fork` job and BEFORE `somnia-e2e`. It is a near-copy of the base `fork` job with exactly these substitutions:
    1. `env: ALCHEMY_API_KEY: ${{ secrets.ALCHEMY_API_KEY }}` (NOT `BASE_RPC_URL` — the Polygon fork tests read `ALCHEMY_API_KEY` via `vm.envString`; gating on `BASE_RPC_URL` would silently skip every Polygon test).
    2. The cache step: `actions/cache@v4`, `path: ~/.foundry/cache/rpc`, `key: foundry-rpc-polygon-86900000-v1`, `restore-keys: |\n  foundry-rpc-polygon-` and a step name naming the pinned block 86900000.
    3. The run step: same shape as the base job — if `ALCHEMY_API_KEY` is empty, emit two `::warning::` lines (one stating the Polygon-fork tests are skipped, one telling the maintainer to add the `ALCHEMY_API_KEY` secret under Settings → Secrets and variables → Actions) and `exit 0`; otherwise `forge test --match-path 'test/**/*fork*' --retries 2 --delay 3 -vvv`.
    4. Keep `runs-on: ubuntu-latest`, `defaults.run.working-directory: contracts`, the `actions/checkout@v4` + `foundry-rs/foundry-toolchain@v1` + `bash script/ci-install-deps.sh` steps identical to the base job.
    5. Add a job header comment: "Secret-bearing (ALCHEMY_API_KEY → the polygon rpc_endpoint), RPC-cached on pinned block 86900000, retried. Skips gracefully (warning, not failure) when ALCHEMY_API_KEY is not configured. Foundry has NO --shard flag — the RPC cache + --retries are the 429 defense (NOT a real shard)."
    DO NOT touch `build-and-spec` (the keyless `forge build` + the `for t in test/fork/*.tree test/instrument/*.tree; do bulloak check "$t"; done` loop + the fork-excluded keyless suite) and DO NOT touch `somnia-e2e` (stays `if: github.event_name == 'workflow_dispatch'`). DO NOT add a new `.tree` requirement — the bulloak loop already checks the existing `test/fork/MacroHedgeExecutor.fork.tree` per-file (E2E-02's "per-file bulloak check over every existing tree" is the current behavior; the demo file deliberately has no tree). DO NOT add `POLYGON_RPC_URL` anywhere.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia && python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/contracts-ci.yml')); j=d['jobs']; assert 'polygon' in j, 'no polygon job'; assert j['somnia-e2e']['if']=='github.event_name == \'workflow_dispatch\'', 'somnia-e2e if changed'; print('JOBS_OK', list(j.keys()))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'foundry-rpc-polygon-86900000-v1' .github/workflows/contracts-ci.yml` (the pinned-block polygon cache key)
    - `grep -q 'ALCHEMY_API_KEY' .github/workflows/contracts-ci.yml` AND `grep -q 'secrets.ALCHEMY_API_KEY' .github/workflows/contracts-ci.yml` (gated on the var the tests read)
    - `! grep -q 'POLYGON_RPC_URL' .github/workflows/contracts-ci.yml` (NOT a new env var)
    - `! grep -q -- '--shard' .github/workflows/contracts-ci.yml` (no invented shard flag)
    - `grep -q 'workflow_dispatch' .github/workflows/contracts-ci.yml` AND the `somnia-e2e` job still carries `if: github.event_name == 'workflow_dispatch'` (live STT e2e stays manual)
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/contracts-ci.yml'))"` parses with no error AND the `jobs` map contains `build-and-spec`, `fork`, `polygon`, `somnia-e2e`
  </acceptance_criteria>
  <done>A `polygon` job exists, gated on `secrets.ALCHEMY_API_KEY`, cached on `foundry-rpc-polygon-86900000-v1`, retried, skipping gracefully when unset; the keyless job and `somnia-e2e` are unchanged; the YAML parses; no `--shard`, no `POLYGON_RPC_URL`.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the keyless gate still passes locally + document the secret-set manual step</name>
  <files>.github/workflows/contracts-ci.yml</files>
  <read_first>
    - .github/workflows/contracts-ci.yml (the just-edited file — confirm the keyless `build-and-spec` job's steps are byte-identical to before)
    - .planning/phases/15-cornerstone-e2e-ci/15-VALIDATION.md § Manual-Only Verifications (the "polygon-fork CI job green on a real runner needs the ALCHEMY_API_KEY secret set" row)
  </read_first>
  <action>
    No workflow edit in this task — it is the keyless-gate regression + the documentation of the human secret-set step. Run the two keyless gates the `build-and-spec` job runs, locally, to prove the edit did not break the secret-free path:
    1. `cd contracts && forge build` (exit 0).
    2. `cd contracts && for t in test/fork/*.tree test/instrument/*.tree; do bulloak check "$t"; done` (every existing tree checks clean — this is the literal "per-file bulloak check" the job runs).
    3. `cd contracts && forge test --no-match-path 'test/**/*fork*'` (the keyless suite exits 0 with no RPC secret).
    Then record, in the 15-02-SUMMARY, the one human-only follow-up (it is in `user_setup`): the maintainer must add the `ALCHEMY_API_KEY` repository secret (Settings → Secrets and variables → Actions) for the `polygon` job to actually run green on a runner; until then it skips with a warning (by design). This is NOT a code task — it is a documented handoff so the gate is honest about what it needs.
  </action>
  <verify>
    <automated>cd contracts && forge build && for t in test/fork/*.tree test/instrument/*.tree; do bulloak check "$t" || exit 1; done && forge test --no-match-path 'test/**/*fork*' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd contracts && forge build` exits 0
    - `cd contracts && for t in test/fork/*.tree test/instrument/*.tree; do bulloak check "$t" || exit 1; done` exits 0 (every existing tree, including `MacroHedgeExecutor.fork.tree`, checks per-file)
    - `cd contracts && forge test --no-match-path 'test/**/*fork*'` exits 0 (keyless suite green without any RPC secret — the build-and-spec contract is preserved)
    - The 15-02-SUMMARY records the `ALCHEMY_API_KEY`-secret manual step from `user_setup`
  </acceptance_criteria>
  <done>The keyless `build-and-spec` contract is proven intact locally (forge build + per-file bulloak + the fork-excluded suite all exit 0); the human `ALCHEMY_API_KEY`-secret step is documented for the polygon job to run green on a runner.</done>
</task>

</tasks>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/contracts-ci.yml'))"` parses; `jobs` = {build-and-spec, fork, polygon, somnia-e2e}.
- The polygon job is gated on `secrets.ALCHEMY_API_KEY`, cached on `foundry-rpc-polygon-86900000-v1`, retried; skips gracefully when unset; no `--shard`, no `POLYGON_RPC_URL`.
- Keyless gates green locally: `forge build`, the per-file bulloak loop, `forge test --no-match-path 'test/**/*fork*'`.
- `somnia-e2e` unchanged (`workflow_dispatch`-only).
</verification>

<success_criteria>
- E2E-02 polygon-fork row covered by the new `polygon` job.
- E2E-02 `forge build` + per-file bulloak rows covered (the keyless job runs them; proven un-regressed).
- Live e2e stays manual `workflow_dispatch`.
</success_criteria>

<output>
After completion, create `.planning/phases/15-cornerstone-e2e-ci/15-02-SUMMARY.md`
</output>

---
phase: 09-cornerstone-live-tx-integration
plan: 04
type: execute
wave: 2
depends_on: ["09-02", "09-03"]
files_modified:
  - components/defi/cornerstone/ModeBanner.tsx
  - components/defi/cornerstone/LiveTxStateRow.tsx
  - components/defi/cornerstone/OnChainEvidencePanel.tsx
  - components/defi/cornerstone/ExecutorRationalePanel.tsx
  - components/defi/cornerstone/AgentCostPlaceholder.tsx
  - components/defi/cornerstone/FreshnessGate.tsx
  - app/(defi)/apps/abrigo/cornerstone/page.tsx
  - messages/es/somnia.json
  - messages/en/somnia.json
  - docs/copy-review.md
  - tests/e2e/cornerstone.spec.ts
  - tests/e2e/cornerstone-modes.spec.ts
autonomous: true
requirements: [MOD5-MODES, MOD5-SURFACE, MOD5-FALLBACK]
must_haves:
  truths:
    - "the page supports live | replay | mock modes; replay (the GUARANTEED in-phase artifact) is guarded by the 09-01 T0 smoke test and never broken; mock/replay never show a fake tx hash; the live on-chain RUN stays ⊘ DEFERRED (verified mocked here, real run in 09-05)"
    - "the live write path uses wagmi useWriteContract({chainId:31337}) + useSwitchChain to 0x7a69 (31337) BEFORE the write (v5 fix-4); the eth_chainId mount probe decides direct-vs-proxy (09-01 RPC proxy)"
    - "the mode banner is always visible and announces the mode; live shows BOTH the Somnia explorer tx (Agent-1) and the BuildBear explorer tx (Agent-2); narrative never implies prompt-varying geometry (PKE strike CONSTANT 360360, only targetNotional varies)"
    - "the verbatim §0.2 no-bridge disclosure (es-CO + en) is present on every live-path view, not collapsed"
    - "the live→replay degradation announces via aria-live (mock /api/abrigo/agent1 returns ok:false → banner switches to replay + aria-live fires + NO tx-hash element); never a silent substitution"
    - "the 6 UI-SPEC surfaces render; nonErgodicDisclosed + (TEMPLATE) rationale stay full-weight on the card (D1); geometry fields live in the expandable panel; cost panel is static 'not deployed for this demo'"
    - "e2e honesty greps pass: no executed/realized/ejecutad/realizad, no $ PnL, no raw 0x000…0, decision card not under <details>, fork-verified pill never green, status pills color+icon+text"
  artifacts:
    - path: "components/defi/cornerstone/ModeBanner.tsx"
      provides: "Surface 1 — always-visible live/replay/mock banner + verbatim disclosure + dual explorer links in live + aria-live degradation announce"
      min_lines: 30
    - path: "components/defi/cornerstone/OnChainEvidencePanel.tsx"
      provides: "Surface 3 — real tx hash + strike + TokenId, fork-verified pill"
      min_lines: 30
    - path: "components/defi/cornerstone/ExecutorRationalePanel.tsx"
      provides: "Surface 4 — expandable geometry fields"
      min_lines: 30
    - path: "components/defi/cornerstone/AgentCostPlaceholder.tsx"
      provides: "Surface 5 — static not-deployed placeholder"
      min_lines: 15
    - path: "components/defi/cornerstone/FreshnessGate.tsx"
      provides: "Surface 6 — gate / connect / switch-chain affordances"
      min_lines: 30
    - path: "tests/e2e/cornerstone-modes.spec.ts"
      provides: "T8 mode-switch + honesty-grep + switch-chain-before-write + degradation e2e (live mocked via RPC proxy / replay / mock)"
      min_lines: 30
  key_links:
    - from: "app/(defi)/apps/abrigo/cornerstone/page.tsx"
      to: "components/defi/cornerstone/ModeBanner.tsx + FreshnessGate.tsx"
      via: "mode-driven banner above PromptBox + gate at confirm area"
      pattern: "ModeBanner|FreshnessGate"
    - from: "app/(defi)/apps/abrigo/cornerstone/page.tsx"
      to: "useSwitchChain(31337) + useWriteContract({chainId:31337})"
      via: "switch to the fork BEFORE the resolveFromMandate write (v5 fix-4)"
      pattern: "useSwitchChain|useWriteContract.*31337|chainId.*31337"
    - from: "app/(defi)/apps/abrigo/cornerstone/page.tsx"
      to: "runWorkflowLive | runWorkflow + replay snapshot"
      via: "mode selects producer; live calls /api/abrigo/agent1 then runWorkflowLive; ok:false → degrade to replay (aria-live)"
      pattern: "runWorkflowLive|runWorkflow"
---

<objective>
T6 + T8 (governing plan §4): wire the `live | replay | mock` mode switch and build the six UI-SPEC surfaces with honest labels and es-CO/en copy, then gate everything with mode-switch + honesty-grep + switch-chain-before-write + degradation e2e. The live write path uses wagmi `useSwitchChain` to 0x7a69 (31337) BEFORE `useWriteContract({chainId:31337})` (v5 fix-4); the mount-time eth_chainId probe decides direct-vs-proxy (09-01 RPC proxy). Live mode shows BOTH explorer txs (Somnia Agent-1 + BuildBear Agent-2); replay is the guaranteed artifact guarded by the T0 smoke test; mock/replay never show a fake hash. The narrative never implies prompt-varying geometry (PKE strike is CONSTANT 360360; only targetNotional varies).

Purpose: the user-visible payoff — a judge sees the guaranteed replay (or honest mock, or the mocked live two-chain run) under fork-verified provenance, with every §0 honesty invariant enforced in the DOM and the live→replay degradation announced (never silent). The real live on-chain RUN is ⊘ DEFERRED (09-05).
Output: 6 components, the wired page (mode switch + live producer + switch-chain-before-write + aria-live degradation), somnia.json copy (es+en), copy-review row, extended + new cornerstone e2e.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/cornerstone-live-twochain-PLAN.md
@docs/superpowers/specs/2026-06-07-module5-cornerstone-live-tx-design.md
@.planning/phases/09-cornerstone-live-tx-integration/09-UI-SPEC.md
@.planning/phases/09-cornerstone-live-tx-integration/09-02-SUMMARY.md
@.planning/phases/09-cornerstone-live-tx-integration/09-03-SUMMARY.md

<interfaces>
<!-- Produced by Waves 0/1 — consume directly. -->
From lib/apps/abrigo/cornerstone/mode.ts: CornerstoneMode ('live'|'replay'|'mock'), DEFAULT_MODE ('replay'), parseMode
From lib/apps/abrigo/cornerstone/events.ts: ExecutorDecidedView (8 fields), fromChainEvent, fromMockEvent, formatWadToPercent
From lib/apps/abrigo/cornerstone/workflow-engine.ts: runWorkflowLive, runWorkflow, buildLiveMandate (PKE-pinned + chainId override)
From lib/apps/abrigo/cornerstone/artifact-loader.ts: deployment, isExpired
From lib/apps/abrigo/cornerstone/buildbear.ts: BuildBearChainId (31337), createBuildBearPublicClient
From lib/wagmi/config.ts: wagmiConfig (6 chains incl. 31337)
JSON-RPC CORS proxy (09-01): POST /api/cornerstone/rpc — fallback target when the mount-time eth_chainId direct probe fails.
Live Agent-1 producer: POST /api/abrigo/agent1 (09-02) → { ok, somniaSchoolTx, somniaNotionalTx, school, mandate(serialized) } | { ok:false, reason, leg, spentSchoolStt }

EXISTING components to reuse unchanged (UI-SPEC "Reuse Without Change"): RunTranscript, MintCard, PromptBox, DecisionPipelineTrace, ProvenanceBadge, LivenessPill, SystemPromptDisclosure, PipelineStage.
HedgeDecisionCardV2 (09-03): already carries the D1 nonErgodicDisclosed pill + (TEMPLATE) rationale + extended CardV2Strings keys — add the copy strings here.
Existing route: app/(defi)/apps/abrigo/cornerstone/page.tsx (Phase 8 RSC shell + RunTranscript client island). Existing e2e: tests/e2e/cornerstone.spec.ts (prod webServer).
i18n: messages/es/somnia.json + messages/en/somnia.json — EXTEND with somnia.cornerstone.live.* (do not replace existing keys).
lucide icons available: Loader2, Clock, CheckCircle2, XCircle, ChevronDown, ChevronUp, Copy, ExternalLink, Radio, FlaskConical, CircleDashed, GitFork, Circle.
v5 reframe: the live on-chain RUN is ⊘ DEFERRED (external Somnia validator-callback outage). This plan wires + e2e-verifies the live path MOCKED (MSW /api/abrigo/agent1 + the RPC proxy); the real run is 09-05.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 6 surfaces + es-CO/en copy (incl. CardV2 strings + dual-explorer live labels)</name>
  <read_first>
    - 09-UI-SPEC.md §New Surfaces (Surfaces 1–6 full visual/interaction contracts) + Color/Typography/Spacing tables + Copywriting Contract (verbatim copy + banned terms + regimeZt map + boolean pills)
    - docs/cornerstone-live-twochain-PLAN.md T6 (live = "en vivo · on-chain" with BOTH Somnia + BuildBear explorer tx links; narrative must NOT imply prompt-varying geometry — PKE strike CONSTANT 360360, only targetNotional varies; replay/mock never a fake tx hash)
    - spec §0.2 verbatim disclosure + §5 D1 split + §5.3 cost copy + v5 fix-2 (PKE strike constant; the narrative copy must say the strike is fixed, only notional varies)
    - components/defi/cornerstone/HedgeDecisionCardV2.tsx (09-03 — CardV2Strings keys to fill) + MintCard.tsx (DataRow pattern) + RunTranscript.tsx (append-only aria-live)
    - CLAUDE.md locked tokens (muted-ochre single accent; IBM Plex Sans/Mono); messages/es/somnia.json existing keys
  </read_first>
  <action>
    Create the six components per UI-SPEC, composing existing shadcn Button/Badge/Separator + lucide + the DataRow pattern. All copy via next-intl `somnia.cornerstone.live.*` (no literals in JSX except icon names):
    1. `ModeBanner.tsx` — Surface 1, mode-aware (live | replay | mock). Live: Radio + "en vivo · on-chain" + fork-verified pill + 2px accent left border + ALWAYS both verbatim disclosure lines (es-CO then en) + (when present) BOTH explorer links: the Somnia tx (Agent-1) and the BuildBear tx (Agent-2) — only render an explorer anchor if the artifact/route supplies a real URL, else mono hash, NEVER a fake link. Replay: "modo repetición · recibos reales" (real captured receipts). Mock: FlaskConical + "modo demostración (sin cadena)". `role="status" aria-live="polite"` on the mode-label so a live→replay degradation is ANNOUNCED (the page flips the mode prop; the aria-live region speaks the new label). 16px H / 12px V padding (documented exception). Narrative copy must NOT imply prompt-varying geometry (strike fixed 360360; only targetNotional varies).
    2. `LiveTxStateRow.tsx` — Surface 2. One row per state (submitting/pending/confirmed/reverted/error), color+icon+text pill; tx hash Plex Mono truncated 10+6 + full title + 44px Copy button; NO fake explorer link; no hash on revert/error.
    3. `OnChainEvidencePanel.tsx` — Surface 3, after confirmed. "Evidencia en cadena" + fork-verified pill (GitFork, neutral). DataRows: Tx hash, Strike (formatScaledPercent + raw tick "5.68% (tick 360360)"), TokenId (mono + copy). Back-ref disclosure line (12px muted).
    4. `ExecutorRationalePanel.tsx` — Surface 4, expandable (aria-expanded/aria-controls; Chevron accent; 44px trigger). Body = GEOMETRY fields ONLY: regimeZt (label map), inflationAdjustment %, strikeTick (mono), regimeWidth (mono), parametricHedged (boolean pill). nonErgodicDisclosed + rationale are NOT here (on the card — D1). requestId NOT surfaced.
    5. `AgentCostPlaceholder.tsx` — Surface 5, static, dashed border, CircleDashed, muted heading + verbatim §5.3 body (es-CO+en, `OperationalCostManagement` mono). NO numbers/call/address.
    6. `FreshnessGate.tsx` — Surface 6, confirm-area gate states (checking / live-CTA / mock-CTA / connect-wallet / switch-chain / rpc-unreachable) per UI-SPEC; advisory in aria-live; disabled buttons keep aria-disabled + visible reason. The switch-chain state surfaces the "switch to fork 31337" CTA (wired to useSwitchChain in Task 2).
    COPY: add all `somnia.cornerstone.live.*` keys to messages/es/somnia.json (es-CO first) + messages/en/somnia.json from the UI-SPEC Copywriting Contract VERBATIM (the two §0.2 disclosure lines, §5.3 cost lines, regimeZt map, boolean pills, gate copy, the CardV2 D1 keys from 09-03, the replay/mock mode labels, the degradation announce string, the constant-strike narrative). Append a copy-review.md row for the namespace (native sign-off pending, non-blocking).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm run test:impeccable && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - 6 new component files exist under components/defi/cornerstone/
    - ModeBanner renders both disclosure lines verbatim (grep "NO hay puente" in messages/es/somnia.json and "NO cross-chain bridge" in messages/en/somnia.json) and supports live/replay/mock labels (grep all three mode labels); has `aria-live` on the mode-label (grep)
    - ExecutorRationalePanel does NOT render rationale/nonErgodicDisclosed (those are on the card) — grep absent
    - AgentCostPlaceholder contains no `totalCost` and no numeric value; grep `OperationalCostManagement` present
    - all six surfaces use only `--accent-default` ochre as accent; pills include icon + text (CROSS-09)
    - copy-review.md has a row for somnia.cornerstone.live.*; es/en somnia.json have matching key sets for the new namespace
    - no banned terms (executed/realized/ejecutad/realizad) in the new copy (grep messages); the narrative copy states the strike is constant 360360 (only targetNotional varies)
  </acceptance_criteria>
  <done>Six honest surfaces + es-CO-first copy; mode banner supports live/replay/mock with dual-explorer links in live, an aria-live degradation announce, verbatim no-bridge disclosure, and constant-strike narrative; cost panel static.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the live|replay|mock mode switch + switch-chain-before-write into the page + mode/honesty/degradation e2e</name>
  <read_first>
    - app/(defi)/apps/abrigo/cornerstone/page.tsx (FULL — Phase-8 RSC shell + RunTranscript + PromptBox + workflow-store wiring; force-dynamic; runtime nodejs)
    - docs/cornerstone-live-twochain-PLAN.md T6 + §3 architecture (browser wallet on fork 31337 ONLY via useSwitchChain + useWriteContract({chainId:31337}); Somnia leg via the server route; the mount-time eth_chainId probe → direct-vs-proxy) + §8 (replay guaranteed; live latency-prone) + v5 fix-4 (switch to 0x7a69 BEFORE the write)
    - spec §4a (mount-time probe / mode selection BEFORE Confirm) + §6 acceptance + §0 honesty invariants + v5 reframe (live RUN ⊘ DEFERRED — verify mocked)
    - lib/apps/abrigo/cornerstone/mode.ts (parseMode/DEFAULT_MODE) + freshness probe (numberOfLegs/isExpired/eth_chainId via createBuildBearPublicClient OR the /api/cornerstone/rpc proxy) + runWorkflowLive/runWorkflow + wagmi useSwitchChain/useWriteContract
    - tests/e2e/cornerstone.spec.ts + tests/e2e/cornerstone-replay-smoke.spec.ts (09-01) — extend; prod webServer
  </read_first>
  <action>
    Wire the page (client island where wallet/probe live; keep the RSC shell pre-rendering as Phase 8):
    1. Resolve mode via `parseMode` (URL param / control), DEFAULT_MODE = replay. Mount `ModeBanner` above PromptBox driven by the resolved mode; mount `FreshnessGate` at the confirm area. On mount, run ONE eth_chainId probe against the direct fork RPC; on CORS/network failure, route subsequent reads through `POST /api/cornerstone/rpc` (09-01 proxy) — this decision happens BEFORE Confirm.
    2. Mode `live`: on Confirm, first POST `/api/abrigo/agent1` (with the shared-secret header) to run Agent-1; on `ok:true`: BEFORE writing, call wagmi `useSwitchChain` to switch the wallet to 31337 (0x7a69) — only then `runWorkflowLive` with `useWriteContract({chainId:31337})` feeding the serialized mandate; append `LiveTxStateRow`s; on confirmed render `OnChainEvidencePanel` + `ExecutorRationalePanel`; show BOTH explorer txs in the banner. If the Agent-1 route returns `ok:false` (live strategist callback not landing / oracle stale / DecisionFailed / timeout / partial mandate) → degrade to `replay` honestly (the guaranteed artifact), the banner flips the mode prop so the aria-live region ANNOUNCES the switch (never silent), and NO tx-hash element renders. Mode `replay`: render the recorded snapshot end-to-end (the T0 path — must keep the 09-01 smoke green). Mode `mock`: run `runWorkflow` under the demo-mode banner with NO tx hash row. `AgentCostPlaceholder` always rendered in the evidence section.
    3. Mode is ALWAYS visible; live→replay degradation announces via aria-live; never a silent substitution.
    Extend tests/e2e/cornerstone.spec.ts + add tests/e2e/cornerstone-modes.spec.ts (run against the prod build; MSW-mock the /api/abrigo/agent1 route + the /api/cornerstone/rpc proxy for the live-state sequence):
    - banner present in all modes; verbatim disclosure (es-CO+en) present on live; replay renders the snapshot (delegates to / mirrors the T0 anchor incl. 360360); mock shows demo label + NO tx hash / block link.
    - SWITCH-CHAIN-BEFORE-WRITE (v5 fix-4): with /api/abrigo/agent1 mocked ok:true, assert the live path triggers `wallet_switchEthereumChain` / useSwitchChain to 0x7a69 (31337) BEFORE the resolveFromMandate write fires (assert ordering via the mocked provider call sequence). Also grep the page source: `grep -qE 'useWriteContract.*31337|chainId.*31337' app/(defi)/apps/abrigo/cornerstone/page.tsx` and that `useSwitchChain` is imported/used.
    - DEGRADATION (minor C): with /api/abrigo/agent1 mocked to return `{ok:false}`, assert the banner switches to the replay label, the aria-live region fires the announce, and NO tx-hash element is present.
    - honesty greps: rendered DOM contains none of executed/realized/ejecutad/realizad; no `$` realized-PnL; no raw 0x000…0; HedgeDecisionCardV2 not inside `<details>`; fork-verified pill never uses the status-pass (green) class; every status pill has an icon node + text; live shows both a Somnia + a BuildBear tx affordance (mocked).
  </action>
  <verify>
    <automated>pnpm build && pnpm exec playwright test tests/e2e/cornerstone.spec.ts tests/e2e/cornerstone-modes.spec.ts tests/e2e/cornerstone-replay-smoke.spec.ts && grep -qE 'useWriteContract.*31337|chainId.*31337' "app/(defi)/apps/abrigo/cornerstone/page.tsx" && grep -q "useSwitchChain" "app/(defi)/apps/abrigo/cornerstone/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports + mounts ModeBanner and FreshnessGate (grep both) and resolves mode via parseMode (grep)
    - page.tsx imports useSwitchChain and uses useWriteContract({chainId:31337}) (grep `useSwitchChain` + `useWriteContract.*31337|chainId.*31337`)
    - the mount-time eth_chainId probe decides direct-vs-/api/cornerstone/rpc proxy before Confirm (grep the probe + proxy fallback)
    - live path POSTs /api/abrigo/agent1, switches to 31337 BEFORE the write, then calls runWorkflowLive; mock path calls runWorkflow; replay renders the snapshot (grep all three producers/paths)
    - e2e asserts the switch-to-0x7a69 happens BEFORE the resolveFromMandate write (mocked provider ordering)
    - e2e asserts the ok:false degradation: banner flips to replay + aria-live fires + NO tx-hash element
    - e2e asserts banner present in live/replay/mock; the es-CO + en §0.2 disclosure strings present on the live path; mock path has NO tx-hash element / block link
    - e2e honesty greps pass: no executed/realized/ejecutad/realizad, no `$` PnL, no 0x000…0, no `<details>` wrapping the decision card, fork-verified pill not green
    - the 09-01 replay smoke spec STILL passes (replay not broken — regression guard)
    - `pnpm build` succeeds (Velite prebuild); e2e green locally
  </acceptance_criteria>
  <done>The page drives live|replay|mock with an always-visible mode, switches to the fork 31337 BEFORE the write, degrades to replay honestly with an aria-live announce when Agent-1 isn't available, and passes the mode-switch + switch-chain-before-write + degradation + honesty e2e on the production build without breaking replay.</done>
</task>

</tasks>

<verification>
- `pnpm build && pnpm exec playwright test` green locally (cornerstone + modes + replay-smoke).
- `pnpm run test:impeccable` + token tests green; `pnpm tsc --noEmit` passes; `pnpm vitest run` green (no Wave-0/1 regressions).
- grep: page uses useSwitchChain + useWriteContract({chainId:31337}); copy-review.md row present for somnia.cornerstone.live.*; replay smoke (T0) still green.
</verification>

<success_criteria>
A judge at /apps/abrigo/cornerstone sees an always-visible mode banner (live|replay|mock) + verbatim no-bridge disclosure; live switches the wallet to the fork 31337 BEFORE the mint write and shows BOTH the Somnia (Agent-1) and BuildBear (Agent-2) explorer txs with the D1 honesty flag + TEMPLATE rationale full-weight on the card and geometry in the expandable panel; the narrative states the strike is constant 360360 (only targetNotional varies); the cost panel is a static not-deployed placeholder; replay (the guaranteed artifact) and mock render with no fake tx hash; live→replay degrades with an aria-live announce; and all honesty invariants pass in e2e on the prod build. The real live on-chain RUN is ⊘ DEFERRED (09-05).
</success_criteria>

<output>
After completion, create `.planning/phases/09-cornerstone-live-tx-integration/09-04-SUMMARY.md`.
The full live-DOM Evidence Collector gate runs in 09-05 (with the operator runbook + provisioned sandbox). This plan's e2e covers the mocked live sequence (incl. switch-chain-before-write + ok:false degradation) + replay + mock honesty greps on the prod build.
</output>
</content>

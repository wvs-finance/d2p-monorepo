---
phase: 09-cornerstone-live-tx-integration
plan: 03
type: tdd
wave: 1
depends_on: ["09-01"]
files_modified:
  - lib/apps/abrigo/cornerstone/events.ts
  - lib/apps/abrigo/cornerstone/workflow-engine.ts
  - components/defi/cornerstone/HedgeDecisionCardV2.tsx
  - tests/unit/cornerstone/from-chain-event.test.ts
  - tests/unit/cornerstone/mandate-override.test.ts
  - tests/unit/cornerstone/producer-ordering.test.ts
  - tsconfig.json
autonomous: true
requirements: [MOD5-ABI, MOD5-LIVE, MOD5-FALLBACK]
must_haves:
  truths:
    - "the live two-chain mint producer is BUILT + unit-proven (mocked viem); the live on-chain RUN is ⊘ DEFERRED (external Somnia recovery) — not a Phase-9 gate. replay (09-01 T0) remains the guaranteed artifact and stays green"
    - "fromChainEvent builds WorkflowEventView directly from decoded logs (decodeEventLog strict:false), surfacing all 8 ExecutorDecided fields + PositionMintedView.positionId from the log"
    - "StrategistDecidedView.recordedDecisionId is set ONCE inside fromChainEvent to the bytes32 decisionId.toString() (live has no outside-enrich wrapper)"
    - "HedgeLegParamsView gains nonErgodicDisclosed + parametricHedged (D1) and HedgeDecisionCardV2 renders them at full weight, never inside <details>"
    - "the MINT pins economicTheory = 0x…06 (PKE) per mode rule (v5 fix-2): live mint = PKE-pinned (preserving the extractStrike===360360 anchor + the 'tick 360360' copy), the school LABEL still renders from the StrategistDecided event string; a live SHILLER (0x5) mandate must NOT route a different/reverting strike"
    - "runWorkflowLive overrides mandate.chainId to 31337 (the connected fork block.chainid) BEFORE resolveFromMandate(mandate, 0n, 1_000_000n) (D4) and reads quoteMargin only after a confirmed PositionMinted using extractStrike(positionId)"
    - "runWorkflow (mock) and the workflow-store seam are unchanged; idempotent DecisionFailed / partial-mandate handling"
  artifacts:
    - path: "lib/apps/abrigo/cornerstone/events.ts"
      provides: "reshaped view types + fromChainEvent real-ABI adapter (recordedDecisionId set once)"
      contains: "fromChainEvent"
    - path: "lib/apps/abrigo/cornerstone/workflow-engine.ts"
      provides: "runWorkflowLive + buildLiveMandate (PKE-pinned economicTheory + chainId override) alongside runWorkflow (mock)"
      exports: ["runWorkflowLive", "buildLiveMandate"]
    - path: "components/defi/cornerstone/HedgeDecisionCardV2.tsx"
      provides: "D1 Davidson split — nonErgodicDisclosed pill + (TEMPLATE) rationale full weight"
      contains: "nonErgodicDisclosed"
  key_links:
    - from: "lib/apps/abrigo/cornerstone/workflow-engine.ts"
      to: "MacroHedgeExecutor.resolveFromMandate"
      via: "writeContract({chainId:31337}) after buildLiveMandate override (chainId 31337 + economicTheory PKE 0x6 pinned)"
      pattern: "resolveFromMandate"
    - from: "lib/apps/abrigo/cornerstone/events.ts fromChainEvent"
      to: "StrategistDecidedView.recordedDecisionId"
      via: "decisionId.toString() set once in the adapter"
      pattern: "recordedDecisionId"
    - from: "components/defi/cornerstone/HedgeDecisionCardV2.tsx"
      to: "ExecutorDecidedView.nonErgodicDisclosed + rationale"
      via: "full-weight rows, never <details>"
      pattern: "nonErgodicDisclosed|TEMPLATE"
---

<objective>
T4 + T5 + T5b (governing plan §4 + §2 frontend view-type changes): BUILD the browser fork-mint producer and reshape the view types. `fromChainEvent` decodes real receipt logs into `WorkflowEventView` (8-field ExecutorDecided, PositionMinted positionId, recordedDecisionId set once), `runWorkflowLive` re-hydrates the serialized mandate, PINS `economicTheory = 0x…06` (PKE) on the mint + OVERRIDES chainId→31337 (D4), submits `resolveFromMandate(mandate,0,1e6)`, decodes the receipt, and reads `quoteMargin` post-mint; and `HedgeLegParamsView`/`HedgeDecisionCardV2` gain the D1 honesty fields (nonErgodicDisclosed + TEMPLATE rationale at full weight).

Purpose: this is Agent-2 — the live fork mint behind the unchanged workflow-store seam, plus the view-contract reshape the live data needs. The mint pins PKE (0x6) so the strike anchor (360360) + the evidence-panel "tick 360360" copy hold even if a live mandate carried a SHILLER (0x5) economicTheory (v5 fix-2); the human school LABEL still renders from the StrategistDecided event string. Agent-2 stays fork-verified (never green); the mock + replay paths are preserved untouched. The live on-chain RUN is ⊘ DEFERRED (09-05) — this plan BUILDS + unit-proves it.
Output: reshaped events.ts + fromChainEvent, runWorkflowLive + buildLiveMandate (PKE-pinned), the D1 card edit + 3 unit test files.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/cornerstone-live-twochain-PLAN.md
@docs/superpowers/specs/2026-06-07-module5-cornerstone-live-tx-design.md
@.planning/phases/09-cornerstone-live-tx-integration/09-RESEARCH.md
@.planning/phases/09-cornerstone-live-tx-integration/09-UI-SPEC.md
@.planning/phases/09-cornerstone-live-tx-integration/09-01-SUMMARY.md

<interfaces>
<!-- Produced by 09-01 (Wave 0) — use directly. -->
From lib/contracts/generated.ts: macroHedgeExecutorAbi (8-field ExecutorDecided, PositionMinted, resolveFromMandate, quoteMargin)
From lib/apps/abrigo/cornerstone/artifact-loader.ts: deployment ({chainId:31337, executor, pool, rpcUrl, mintTxHash, mintedStrike, capturedAt}), isExpired
From lib/apps/abrigo/cornerstone/buildbear.ts: BuildBearChainId (31337), createBuildBearChain, createBuildBearPublicClient
From lib/apps/abrigo/cornerstone/balance-delta.ts: decodeBalanceDelta(raw): {amount0,amount1}
From lib/apps/abrigo/cornerstone/token-id.ts: extractStrike(positionId): number (offset 76, sdk-cross-checked)

From lib/apps/abrigo/cornerstone/events.ts (EXISTING — extend ADDITIVELY, keep fromMockEvent):
  type WorkflowEventView = StrategistDecidedView | ExecutorDecidedView | PositionMintedView | PerformanceUpdatedView
  export function fromMockEvent(e): WorkflowEventView   // KEEP for mock fallback
  StrategistDecidedView { kind; recordedDecisionId:string; thesis; spec{...} }
  HedgeLegParamsView { ...; rationale; marginDelta{token0,token1} }  // GAIN nonErgodicDisclosed + parametricHedged
  ExecutorDecidedView { kind; hedgeLegParams: HedgeLegParamsView }
  PositionMintedView { kind; positionId:string; marginToken0; marginToken1 }
  NOTE: in the MOCK path recordedDecisionId is enriched OUTSIDE the adapter (workflow-engine.ts ~line 90-93).
        In the LIVE path there is NO outside wrapper → set recordedDecisionId ONCE inside fromChainEvent.

From lib/apps/abrigo/cornerstone/workflow-engine.ts (EXISTING): export async function runWorkflow(presetId, emit, {confirm})  // mock — KEEP unchanged
From lib/apps/abrigo/cornerstone/workflow-store.ts (UNCHANGED seam): createWorkflowStore(), emit, RunState

HedgeMandate (serialized from 09-02 route): { economicTheory:'0x..', underlyingMarket:'0x..', targetNotional:'<decimal>', chainId:137, isLong } → re-hydrate to viem tuple (bigint targetNotional), OVERRIDE chainId→31337, PIN economicTheory→0x06 (PKE).
Demo constants (PINNED): legIndex = 0n, positionSize = 1_000_000n. economicTheory MINT VALUE = 0x…06 (PKE — POST_KEYNESIAN), the only sentinel reproducing strike 360360 (spec §2/§4a M3; v5 fix-2). A live SHILLER (0x5) mandate routes a different/possibly-reverting strike → DO NOT pass it to resolveFromMandate; pin PKE. The human school LABEL is independent — render it from the StrategistDecided event STRING.

ExecutorDecided event (spec §2): (uint256 requestId[sentinel 0, not surfaced], uint8 regimeZt, uint256 inflationAdjustmentWad, int24 strikeTick, int24 regimeWidth, bool parametricHedged, bool nonErgodicDisclosed, string rationale[TEMPLATE]).
PositionMinted(address owner, TokenId positionId, uint128 positionSize) — positionId from the indexed topic. quoteMargin returns BalanceDelta (decode with decodeBalanceDelta after extractStrike).
HedgeDecisionCardV2.tsx: exports CardV2Strings + HedgeDecisionCardV2; MUST NEVER use <details> (CROSS-09/LAB-05).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reshape view types + fromChainEvent (recordedDecisionId once, 8-field ExecutorDecided) — RED then GREEN</name>
  <read_first>
    - lib/apps/abrigo/cornerstone/events.ts (FULL — the union, view types, fromMockEvent, formatters)
    - docs/cornerstone-live-twochain-PLAN.md §2 "Frontend reality — view-type changes REQUIRED" (recordedDecisionId from log decisionId.toString() set ONCE; HedgeLegParamsView gains nonErgodicDisclosed/parametricHedged; PositionMintedView.positionId from the log) + T5
    - spec §2 reconciliation rules (8-field ExecutorDecided; PositionMinted 2 indexed topics; strict:false tolerates RepresentativenessAssessed; requestId sentinel 0 not surfaced; WAD→percent; schoolLabel from event string)
    - 09-RESEARCH.md Pattern 6 + "ExecutorDecidedView — reconciled fields" + "WAD → percent formatter"
    - lib/contracts/generated.ts macroHedgeExecutorAbi
  </read_first>
  <action>
    RED: write `tests/unit/cornerstone/from-chain-event.test.ts` using viem encodeEventLog(macroHedgeExecutorAbi) for fixtures:
    - `fromChainEvent(executorDecidedLog)` → ExecutorDecidedView with regimeZt:number, inflationAdjustment:string (WAD→percent), strikeTick:number (negative case e.g. -120), regimeWidth:number, parametricHedged:boolean, nonErgodicDisclosed:true, rationale:'TEMPLATE: ...', and hedgeLegParams carrying nonErgodicDisclosed + parametricHedged.
    - `formatWadToPercent(56800000000000000n) === '5.68%'`.
    - `fromChainEvent(positionMintedLog)` → PositionMintedView with positionId from the indexed topic (string).
    - `fromChainEvent` for a StrategistDecided-shaped input: recordedDecisionId === the bytes32 decisionId `.toString()` (set ONCE, not enriched outside).
    - an unrecognized topic (RepresentativenessAssessed) → fromChainEvent returns null (not a throw).
    Run, confirm fail.
    GREEN, edit events.ts ADDITIVELY (keep fromMockEvent + existing views):
    1. EXTEND `HedgeLegParamsView` with `nonErgodicDisclosed: boolean` + `parametricHedged: boolean`.
    2. EXTEND `ExecutorDecidedView` with regimeZt, inflationAdjustment(string), strikeTick(signed), regimeWidth(signed), parametricHedged, nonErgodicDisclosed, rationale (keep hedgeLegParams).
    3. Add `export function formatWadToPercent(wad: bigint): string` = `(Number(wad / 10n**14n)/100).toFixed(2) + '%'`.
    4. Fix `schoolLabelFromAddress`: source the human school label from the StrategistDecided event STRING when available; 0x6→"POST_KEYNESIAN", 0x5→"SHILLER_MACRO_RISK"; for economicTheory address(0)/unknown → em-dash (never raw hex).
    5. Add `export function fromChainEvent(input): WorkflowEventView | null` using `decodeEventLog({abi:macroHedgeExecutorAbi, strict:false})` in try/catch (catch → null). Map ExecutorDecided → ExecutorDecidedView (int24 as number from viem; WAD via formatWadToPercent; margins filled LATER by quoteMargin in the producer). Map PositionMinted → PositionMintedView (positionId from indexed topic, string). For StrategistDecided view construction, set `recordedDecisionId = decisionId.toString()` ONCE inside the adapter. Do NOT surface requestId (sentinel 0).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone/from-chain-event.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - events.ts exports `fromChainEvent` + `formatWadToPercent`; `fromMockEvent` still exported (grep all three)
    - HedgeLegParamsView has nonErgodicDisclosed + parametricHedged; ExecutorDecidedView has all 8 fields (grep nonErgodicDisclosed + rationale)
    - negative strikeTick decodes signed; `formatWadToPercent(56800000000000000n)==='5.68%'`
    - recordedDecisionId is set inside fromChainEvent from decisionId.toString() (grep `recordedDecisionId` + `.toString()` in fromChainEvent)
    - decodeEventLog called with `strict: false`; a RepresentativenessAssessed-shaped log returns null, not a throw
    - schoolLabelFromAddress: 0x6→POST_KEYNESIAN, 0x5→SHILLER_MACRO_RISK, address(0)→em-dash, never raw hex
  </acceptance_criteria>
  <done>View types reshaped; fromChainEvent decodes the 8-field ExecutorDecided + PositionMinted (strict:false) and sets recordedDecisionId once — downstream view types stable, mock path intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runWorkflowLive + buildLiveMandate (PKE-pinned economicTheory + D4 override, post-mint quoteMargin, idempotent failure) — RED then GREEN</name>
  <read_first>
    - lib/apps/abrigo/cornerstone/workflow-engine.ts (FULL — runWorkflow signature, RunWorkflowOptions, emit pattern, the outside recordedDecisionId enrich at ~line 90)
    - lib/apps/abrigo/cornerstone/workflow-store.ts (the seam — DO NOT change it)
    - docs/cornerstone-live-twochain-PLAN.md T4 + T5 + §2 D4 (override mandate.chainId=31337 before resolveFromMandate; executor :365 reverts "No crosschain allowed yet"; legIndex 0, positionSize 1e6) + §2/§4a M3 + v5 fix-2 (PIN economicTheory=0x06 PKE on the mint; a live SHILLER 0x5 breaks the 360360 anchor) + §3 (re-hydrate serialized mandate to viem tuple; bigint targetNotional)
    - spec §4a states (submitting→pending→confirmed→reverted/error; quoteMargin AFTER confirmed PositionMinted using extractStrike(positionId), NOT artifact mintedStrike) + 09-RESEARCH Pitfall 2/6
    - events.ts fromChainEvent (Task 1); token-id extractStrike + balance-delta decodeBalanceDelta (09-01)
  </read_first>
  <action>
    RED: write `tests/unit/cornerstone/mandate-override.test.ts` and `tests/unit/cornerstone/producer-ordering.test.ts`.
    mandate-override.test.ts: `buildLiveMandate(serializedMandate, 31337)` — given serialized chainId 137 AND a SHILLER economicTheory 0x05, returns a re-hydrated viem tuple with `chainId === 31337`, `economicTheory === 0x…06` (PKE — PINNED, NOT carried from the SHILLER mandate), `targetNotional` as bigint (carried from the mandate), underlyingMarket as hex, isLong unchanged. A producer test (spy/mock the writeContract fn) asserts `args[0].chainId === 31337`, `args[0].economicTheory === 0x…06`, `args[1] === 0n`, `args[2] === 1_000_000n` (D4 + PKE-pin regression guard).
    producer-ordering.test.ts: with a mocked publicClient/writeContract, assert emit ordering — StrategistDecided → submitting → pending → (decode logs) → confirmed; and that on a `receipt.status==='reverted'` path NO quoteMargin call happens and a reverted state is emitted (idempotent, no minted claim). Assert quoteMargin is read only after a PositionMinted log is decoded. Assert that when the upstream Agent-1 result is `ok:false` (partial/failed), runWorkflowLive does NOT attempt the mint and surfaces the failure.
    Run, confirm fail.
    GREEN, edit workflow-engine.ts ADDITIVELY (keep runWorkflow):
    1. `export const MINT_ECONOMIC_THEORY = '0x0000000000000000000000000000000000000006' as const` (PKE — POST_KEYNESIAN; the only sentinel reproducing strike 360360; v5 fix-2). `export function buildLiveMandate(serialized, connectedChainId)` — re-hydrate to the viem HedgeMandate tuple (bigint targetNotional from the decimal string), set `chainId = connectedChainId` (the connected fork block.chainid, 31337), and PIN `economicTheory = MINT_ECONOMIC_THEORY` (do NOT carry a live SHILLER through to the mint — that would route a different/reverting strike). Carry underlyingMarket/targetNotional/isLong from the live mandate. MUST run before any write.
    2. `export async function runWorkflowLive(opts)` taking the store-emit fn, a writeContract fn (wagmi `useWriteContract({chainId:31337})` from the caller), the buildbear publicClient, and the live StrategistDecided view (from the 09-02 route result). Sequence:
       a. If the upstream Agent-1 result is ok:false (oracle stale, DecisionFailed, timeout/silent no-show, partial mandate) → do NOT mint; surface the failure (idempotent). Else emit StrategistDecided (recordedDecisionId already set by fromChainEvent / route mapping; the school LABEL from the event string).
       b. await Confirm (caller-gated).
       c. `mandate = buildLiveMandate(serialized, 31337)`; emit `submitting`; `hash = writeContract({ address: deployment.executor, abi: macroHedgeExecutorAbi, functionName:'resolveFromMandate', args:[mandate, 0n, 1_000_000n], chainId:31337 })`.
       d. emit `pending` (real hash, NO fake explorer link); `receipt = await publicClient.waitForTransactionReceipt({hash})`.
       e. if `receipt.status==='reverted'` → emit reverted, STOP (no minted claim — idempotent). else decode logs via fromChainEvent → ExecutorDecided + PositionMinted; emit them.
       f. `strike = extractStrike(positionId)`; `raw = await publicClient.readContract(quoteMargin, [positionId, strike])`; `{amount0,amount1}=decodeBalanceDelta(raw)`; attach to margins; emit confirmed.
       Wrap in try/catch → emit `error` on reject/RPC failure. NEVER read quoteMargin before a confirmed PositionMinted.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone/mandate-override.test.ts tests/unit/cornerstone/producer-ordering.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `buildLiveMandate(serialized{chainId:137,economicTheory:0x05}, 31337)` → `chainId === 31337` AND `economicTheory === 0x…06` (PKE pinned, NOT the SHILLER input); targetNotional is bigint carried from the mandate
    - the producer passes the OVERRIDDEN + PKE-pinned mandate to resolveFromMandate (spy asserts `args[0].chainId===31337` AND `args[0].economicTheory===0x…06`) — D4 + v5-fix-2 regression guard
    - MINT_ECONOMIC_THEORY pinned to 0x…06 in workflow-engine.ts (grep the literal); the school LABEL is sourced from the event string, NOT from this mint sentinel
    - resolveFromMandate args are `[mandate, 0n, 1_000_000n]` (grep + test)
    - quoteMargin read only after a PositionMinted log is decoded; reverted path emits reverted with no quoteMargin call (test asserts); ok:false upstream → no mint attempted
    - strike for quoteMargin comes from `extractStrike(positionId)`, NOT `deployment.mintedStrike` (grep extractStrike used)
    - runWorkflow (mock) still exported unchanged; workflow-store.ts NOT modified (git diff shows no change)
  </acceptance_criteria>
  <done>runWorkflowLive submits the real resolveFromMandate with the D4 chainId override AND the PKE-pinned economicTheory (preserving the 360360 anchor), decodes the receipt, reads quoteMargin post-mint, and handles reverted/error/upstream-failure idempotently — behind the unchanged store seam with the mock path intact.</done>
</task>

<task type="auto">
  <name>Task 3: T5b — D1 Davidson split on HedgeDecisionCardV2 (nonErgodicDisclosed + TEMPLATE rationale, full weight)</name>
  <read_first>
    - docs/cornerstone-live-twochain-PLAN.md T5b (render nonErgodicDisclosed + TEMPLATE rationale on HedgeDecisionCardV2; NOT DecisionPipelineTrace) + spec §5.2 D1 (Davidson honesty split, full weight, never collapsed)
    - 09-UI-SPEC.md Surface 4 boolean-pill spec + Color/Typography tables + Copywriting Contract (boolean pill sí/yes, the (TEMPLATE) marker) + Honesty Invariants
    - components/defi/cornerstone/HedgeDecisionCardV2.tsx (FULL — CardV2Strings interface, props, existing DataRows; it must NEVER use <details>)
    - CLAUDE.md locked tokens (muted-ochre single accent; IBM Plex Sans/Mono) + CROSS-09 (pills color+icon+text)
  </read_first>
  <action>
    Edit HedgeDecisionCardV2.tsx to add two FULL-WEIGHT rows (NOT inside <details>), reading from ExecutorDecidedView.hedgeLegParams / ExecutorDecidedView:
    1. A `nonErgodicDisclosed` honesty-flag boolean pill (color+icon+text — true: status-pass bg + CheckCircle2 + "sí/yes"; false: surface bg + Circle + "no"). CROSS-09 compliant.
    2. The `rationale` string explicitly prefixed/marked "(TEMPLATE)" (sourced from the event string), full body weight.
    Extend `CardV2Strings` with the new label keys (nonErgodicDisclosed label, the TEMPLATE marker, boolean sí/no) — actual copy strings are added to messages in 09-04; here add the interface keys + wire them. Card stays full weight — assert no `<details>` introduced. The geometry fields (regimeZt/strikeTick/regimeWidth/parametricHedged) do NOT go on the card (they live in the 09-04 expandable panel).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm run test:impeccable && grep -L "<details" components/defi/cornerstone/HedgeDecisionCardV2.tsx</automated>
  </verify>
  <acceptance_criteria>
    - HedgeDecisionCardV2.tsx references `nonErgodicDisclosed` and renders a "(TEMPLATE)" marker (grep both)
    - HedgeDecisionCardV2.tsx contains NO `<details>` (grep `<details` returns empty in that file)
    - the boolean pill includes an icon node + text (CROSS-09 — grep lucide icon import used for the pill)
    - CardV2Strings extended with the new label keys (grep the new keys present in the interface)
    - geometry fields (regimeZt/strikeTick/regimeWidth) are NOT added to the card (those belong to the 09-04 panel — grep absent)
    - `pnpm run test:impeccable` green (no anti-fishing / token violation)
  </acceptance_criteria>
  <done>The D1 split is applied: the honesty flag + TEMPLATE rationale render at full weight on the decision card, never collapsed; geometry stays off the card for the 09-04 panel.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/cornerstone/` green (Wave-0 + the 3 new files); `pnpm tsc --noEmit` + `pnpm run test:impeccable` pass.
- git diff: workflow-store.ts unchanged; fromMockEvent + runWorkflow retained; replay smoke (09-01) still green.
- grep: mandate override before write; economicTheory PKE 0x…06 pinned on the mint; D4 args [mandate,0n,1_000_000n]; HedgeDecisionCardV2 has no <details>.
</verification>

<success_criteria>
View types reshaped + fromChainEvent (recordedDecisionId once, 8-field ExecutorDecided, strict:false); runWorkflowLive with the D4 chainId override AND the PKE-pinned economicTheory (360360 anchor preserved; school label from the event string), post-mint quoteMargin, idempotent revert/error/upstream-failure; D1 honesty flag + TEMPLATE rationale full-weight on the card — all behind the unchanged store seam, mock + replay paths preserved. The live on-chain RUN is ⊘ DEFERRED (09-05); this plan BUILDS + unit-proves it.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cornerstone-live-tx-integration/09-03-SUMMARY.md`.
The HedgeDecisionCardV2 edit is rendered surface but is wired into a route only in 09-04 — the live-DOM Evidence Collector gate runs in 09-05 against the wired page. Note so per CLAUDE.md (verify the card render via the 09-04/05 wiring, not in isolation here).
</output>
</content>

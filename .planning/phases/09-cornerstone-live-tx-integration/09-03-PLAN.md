---
phase: 09-cornerstone-live-tx-integration
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - components/defi/cornerstone/ModeBanner.tsx
  - components/defi/cornerstone/LiveTxStateRow.tsx
  - components/defi/cornerstone/OnChainEvidencePanel.tsx
  - components/defi/cornerstone/ExecutorRationalePanel.tsx
  - components/defi/cornerstone/AgentCostPlaceholder.tsx
  - components/defi/cornerstone/FreshnessGate.tsx
  - components/defi/cornerstone/HedgeDecisionCardV2.tsx
  - app/(defi)/apps/abrigo/cornerstone/page.tsx
  - messages/es/somnia.json
  - messages/en/somnia.json
  - docs/copy-review.md
  - tests/e2e/cornerstone.spec.ts
autonomous: false
requirements: [MOD5-SURFACE, MOD5-FALLBACK, MOD5-LIVE]
user_setup:
  - service: buildbear
    why: "Live in-demo mint needs a freshly provisioned fork (numberOfLegs(executor)==0) per spec §4b runbook"
    dashboard_config:
      - task: "Run abrigo-somnia provision-buildbear-demo.sh (fresh-executor / --no-mint variant) morning-of, then mirror the new buildbear-deployments.json into lib/apps/abrigo/cornerstone/ and commit"
        location: "abrigo-somnia/contracts/script (backend owner); FE owner re-mirrors"
must_haves:
  truths:
    - "Mode banner is always visible above the fold and announces live vs demo mode; never a silent substitution"
    - "The verbatim §0.2 no-bridge disclosure (es-CO + en) is present on every live-path view, not collapsed"
    - "nonErgodicDisclosed + the (TEMPLATE)-marked rationale render ON HedgeDecisionCardV2 at full weight, never inside <details> (D1)"
    - "After a confirmed mint the On-Chain Evidence Panel shows the real tx hash + strike + TokenId under a neutral fork-verified pill"
    - "The other ExecutorDecided geometry fields live in the expandable evidence panel; the cost panel is static 'not deployed for this demo'"
    - "Mock fallback renders under 'modo demostración (sin cadena)' with NO tx hash / block link"
  artifacts:
    - path: "components/defi/cornerstone/ModeBanner.tsx"
      provides: "Surface 1 — always-visible live/demo banner + verbatim disclosure"
      min_lines: 30
    - path: "components/defi/cornerstone/OnChainEvidencePanel.tsx"
      provides: "Surface 3 — real tx hash + strike + TokenId, fork-verified pill"
      min_lines: 30
    - path: "components/defi/cornerstone/ExecutorRationalePanel.tsx"
      provides: "Surface 4 — expandable geometry fields"
      min_lines: 30
    - path: "components/defi/cornerstone/AgentCostPlaceholder.tsx"
      provides: "Surface 5 — static not-deployed placeholder"
      min_lines: 15
    - path: "components/defi/cornerstone/FreshnessGate.tsx"
      provides: "Surface 6 — gate states + switch-chain/connect affordances"
      min_lines: 30
  key_links:
    - from: "app/(defi)/apps/abrigo/cornerstone/page.tsx"
      to: "components/defi/cornerstone/ModeBanner.tsx"
      via: "banner mounted above PromptBox driven by freshness probe"
      pattern: "ModeBanner"
    - from: "components/defi/cornerstone/HedgeDecisionCardV2.tsx"
      to: "ExecutorDecidedView.nonErgodicDisclosed + rationale"
      via: "full-weight rows, never <details>"
      pattern: "nonErgodicDisclosed|TEMPLATE"
---

<objective>
Wave-2 surfaces + wire + honesty gate: build the six new presentational surfaces (mode banner with the verbatim no-bridge disclosure, live-tx state rows, on-chain evidence panel, expandable executor-rationale panel, static cost placeholder, freshness-gate confirm affordances), perform the D1 Davidson split on HedgeDecisionCardV2 (honesty flag + TEMPLATE rationale at full weight), wire the freshness probe into the cornerstone page to select live-vs-fallback, author es-CO-first/en copy, and gate the whole phase behind e2e honesty greps + the Evidence Collector live-DOM check.

Purpose: this is the user-visible payoff — a judge sees the live mint evidence (or the honest demo-mode fallback) under fork-verified provenance, with every §0 honesty invariant enforced in the DOM.
Output: 6 components, the D1 card edit, the wired page, somnia.json copy (es+en), copy-review row, extended cornerstone e2e.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-06-07-module5-cornerstone-live-tx-design.md
@.planning/phases/09-cornerstone-live-tx-integration/09-UI-SPEC.md
@.planning/phases/09-cornerstone-live-tx-integration/09-02-SUMMARY.md

<interfaces>
<!-- Produced by Wave 0/1 — consume directly. -->
From lib/apps/abrigo/cornerstone/events.ts:
  ExecutorDecidedView (8 fields incl. nonErgodicDisclosed, rationale, regimeZt, inflationAdjustment, strikeTick, regimeWidth, parametricHedged), fromChainLog, fromMockEvent
From lib/apps/abrigo/cornerstone/freshness-gate.ts: evaluateFreshness, probeFreshness
From lib/apps/abrigo/cornerstone/workflow-engine.ts: runWorkflowLive, runWorkflow, buildLiveMandate
From lib/apps/abrigo/cornerstone/artifact-loader.ts: deployment, isExpired
From lib/wagmi/config.ts: wagmiConfig (now 6 chains incl. 31337), BuildBearChainId

EXISTING components to reuse unchanged (per UI-SPEC "Reuse Without Change"):
  RunTranscript, MintCard, PromptBox, DecisionPipelineTrace, ProvenanceBadge, LivenessPill, SystemPromptDisclosure, PipelineStage
HedgeDecisionCardV2 (components/defi/cornerstone/HedgeDecisionCardV2.tsx): exports CardV2Strings + HedgeDecisionCardV2({...}); EDIT to add the D1 honesty-flag + TEMPLATE-rationale rows.

Existing cornerstone route: app/(defi)/apps/abrigo/cornerstone/page.tsx (Phase 8). Existing e2e: tests/e2e/cornerstone.spec.ts.
i18n: messages/es/somnia.json + messages/en/somnia.json — EXTEND with somnia.cornerstone.live.* (do not replace existing keys).
lucide icons available: Loader2, Clock, CheckCircle2, XCircle, ChevronDown, ChevronUp, Copy, ExternalLink, Radio, FlaskConical, CircleDashed, GitFork, Circle.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 6 new surfaces + D1 Davidson split on HedgeDecisionCardV2 + copy</name>
  <read_first>
    - 09-UI-SPEC.md §New Surfaces (Surfaces 1–6 full visual/interaction contracts) + Color/Typography/Spacing tables + Copywriting Contract (verbatim copy + banned terms + regimeZt map)
    - spec §5 (the D1 split: nonErgodicDisclosed + "(TEMPLATE)" rationale ON the card at full weight; geometry fields in the expandable panel) + §0.2 verbatim disclosure + §5.3 cost copy
    - components/defi/cornerstone/HedgeDecisionCardV2.tsx (FULL — CardV2Strings interface, props, existing rows; it must NEVER use <details>)
    - components/defi/cornerstone/MintCard.tsx (DataRow dt/dd pattern to reuse) + RunTranscript.tsx (append-only aria-live container)
    - CLAUDE.md locked tokens (muted-ochre oklch(0.6 0.08 70) single accent; IBM Plex Sans/Mono); CROSS-09 (pills color+icon+text); messages/es/somnia.json existing keys
  </read_first>
  <action>
    Create the six components per UI-SPEC, composing existing shadcn Button/Badge/Separator + lucide icons + the existing DataRow pattern. All copy via next-intl `somnia.cornerstone.live.*` keys (no literals in JSX except the icon names):
    1. `ModeBanner.tsx` — Surface 1. Always visible above PromptBox. Live: Radio icon + "en vivo · fork de Polygon (BuildBear)" + fork-verified pill + 2px accent left border. Demo: FlaskConical + "modo demostración (sin cadena)" + 2px border-default. ALWAYS renders both verbatim disclosure lines (es-CO then en) — never collapsed/tooltip. `role="status" aria-live="polite"` on the mode-label span; 16px H / 12px V padding (documented banner exception).
    2. `LiveTxStateRow.tsx` — Surface 2. One row per state (submitting/pending/confirmed/reverted/error) with the spec icon + color+icon+text pill. Tx hash in Plex Mono truncated 10+6 with full `title` + Copy button (44px target, aria-label). NO fake explorer link — only render an ExternalLink anchor if the artifact carries a real explorer URL, else mono hash only. No hash on revert/error.
    3. `OnChainEvidencePanel.tsx` — Surface 3. Rendered after confirmed. Heading "Evidencia en cadena" + fork-verified pill (GitFork, neutral). DataRows: Tx hash, Strike (`formatScaledPercent` + raw tick e.g. "5.68% (tick 360360)"), TokenId (mono + copy). Back-ref disclosure line (12px muted).
    4. `ExecutorRationalePanel.tsx` — Surface 4. Expandable (aria-expanded/aria-controls; ChevronDown/Up accent; 44px trigger). Expanded body = the GEOMETRY fields ONLY: regimeZt (via regimeZt label map), inflationAdjustment %, strikeTick (mono), regimeWidth (mono), parametricHedged (boolean pill). nonErgodicDisclosed + rationale are NOT here (they live on the card — D1). requestId NOT surfaced.
    5. `AgentCostPlaceholder.tsx` — Surface 5. Static, dashed border, CircleDashed icon, muted heading + verbatim §5.3 body (es-CO+en, `OperationalCostManagement` in mono inline). NO numbers, NO call, NO address, `aria-label` per spec.
    6. `FreshnessGate.tsx` — Surface 6. Confirm-button-area gate states (checking / live-CTA / demo-CTA / connect-wallet / switch-chain / rpc-unreachable) per the UI-SPEC table; advisory copy in an `aria-live="polite"` region; disabled buttons keep `aria-disabled` + visible reason.
    D1 EDIT on HedgeDecisionCardV2.tsx: add two FULL-WEIGHT rows (NOT in <details>): a `nonErgodicDisclosed` honesty-flag pill (color+icon+text — true/false boolean pill) and the `rationale` string explicitly prefixed/marked "(TEMPLATE)" sourced from the event string. Extend `CardV2Strings` with the new labels. Card stays full weight; assert no `<details>` introduced.
    COPY: add all `somnia.cornerstone.live.*` keys to messages/es/somnia.json (es-CO authored first) and messages/en/somnia.json from the UI-SPEC Copywriting Contract table verbatim (incl. the two §0.2 disclosure lines, §5.3 cost lines, regimeZt map, boolean pills, gate copy). Append a copy-review.md row for the new namespace (native sign-off pending, non-blocking).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm run test:impeccable && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - 6 new component files exist under components/defi/cornerstone/
    - ModeBanner renders both disclosure lines verbatim (grep "NO hay puente" in messages/es/somnia.json and "NO cross-chain bridge" in messages/en/somnia.json)
    - HedgeDecisionCardV2.tsx references `nonErgodicDisclosed` and renders a "(TEMPLATE)" marker (grep both) and contains NO `<details>` (grep `<details` returns empty in that file)
    - ExecutorRationalePanel does NOT render rationale/nonErgodicDisclosed (those are on the card) — grep absent
    - AgentCostPlaceholder contains no `totalCost` and no numeric token; grep `OperationalCostManagement` present
    - all six surfaces use only `--accent-default` ochre as accent (no second accent); pills include icon + text (CROSS-09)
    - copy-review.md has a row for somnia.cornerstone.live.*
    - es/en somnia.json have matching key sets for the new namespace
  </acceptance_criteria>
  <done>Six honest surfaces built; D1 split applied (honesty flag + TEMPLATE rationale at full weight on the card, geometry in the panel); cost panel static; copy authored es-CO-first.</done>
</task>

<task type="auto">
  <name>Task 2: Wire freshness probe into the cornerstone page (live-vs-fallback) + honesty e2e greps</name>
  <read_first>
    - app/(defi)/apps/abrigo/cornerstone/page.tsx (FULL — the Phase-8 RSC shell + RunTranscript + PromptBox + workflow-store wiring)
    - spec §4a (mount-time probe decides live-vs-fallback BEFORE Confirm; states; fallback under demo label) + §6 acceptance (every bullet) + §0 honesty invariants
    - 09-UI-SPEC.md §Honesty Invariants Checklist + Surface 1/6 placement
    - tests/e2e/cornerstone.spec.ts (existing Phase-8 specs to extend; production webServer in playwright.config.ts — e2e runs prod build)
    - freshness-gate.ts (evaluateFreshness/probeFreshness), workflow-engine.ts (runWorkflowLive/runWorkflow), buildbear public client
  </read_first>
  <action>
    Wire the page (client island where wallet/probe live; keep the RSC shell pre-rendering DecisionPipelineTrace as in Phase 8):
    1. On mount, run `probeFreshness(createBuildBearPublicClient(deployment.rpcUrl), deployment, Date.now())` (through the /api/cornerstone/rpc proxy transport) → `evaluateFreshness` → set mode. Mount `ModeBanner` above PromptBox driven by this mode; mount `FreshnessGate` at the confirm area.
    2. Mode `live` → Confirm runs `runWorkflowLive` (wagmi `useWriteContract({chainId:31337})` + `useSwitchChain` to 31337 first) emitting into the existing store; append `LiveTxStateRow`s to RunTranscript; on confirmed render `OnChainEvidencePanel` + `ExecutorRationalePanel`. Mode `fallback` → Confirm runs the existing `runWorkflow` mock under the demo-mode banner with NO tx hash row. `AgentCostPlaceholder` always rendered in the evidence section.
    3. The mode is ALWAYS visible; switching live→demo on a freshness fail announces via the banner's aria-live. Never a silent substitution.
    Extend tests/e2e/cornerstone.spec.ts with honesty greps (run against prod build, MSW-mock the RPC proxy for the live-state sequence): banner always present; verbatim disclosure (es-CO + en) present on live path; demo-mode path shows demo label and NO tx hash / block link; rendered DOM contains none of "executed"/"realized"/"ejecutad"/"realizad"; no `$`-prefixed realized-PnL; no raw `0x000…0`; HedgeDecisionCardV2 not inside `<details>`; fork-verified pill never uses the `--status-pass` (green) class; every status pill has an icon node + text.
  </action>
  <verify>
    <automated>pnpm build && pnpm exec playwright test tests/e2e/cornerstone.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports + mounts ModeBanner and FreshnessGate (grep both)
    - live path calls runWorkflowLive; fallback path calls runWorkflow (grep both)
    - e2e asserts banner present in BOTH modes
    - e2e asserts the es-CO and en §0.2 disclosure strings present on the live path
    - e2e asserts demo-mode path has NO tx-hash element / no block link
    - e2e honesty greps pass: no executed/realized/ejecutad/realizad, no `$` PnL, no `0x000…0`, no `<details>` wrapping the decision card, fork-verified pill not green
    - `pnpm build` succeeds (Velite prebuild runs); e2e green locally
  </acceptance_criteria>
  <done>The cornerstone page probes freshness on mount, selects live-vs-fallback honestly with an always-visible mode, and passes the e2e honesty-invariant suite on the production build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Evidence Collector live-DOM gate + provision checkpoint</name>
  <what-built>
    The wired cornerstone live-tx surface: mode banner + verbatim disclosure, live-tx state rows, on-chain evidence panel (real hash/strike/TokenId), expandable executor-rationale panel, static cost placeholder, freshness-gate confirm affordances, and the D1 card split — selecting live-vs-fallback from a mount-time freshness probe.
  </what-built>
  <action>This is a verification checkpoint, not an implementation task. The build work is done in Tasks 1-2; here the team provisions a fresh sandbox and runs the Evidence Collector live-DOM gate per CLAUDE.md to confirm every spec §6 honesty/acceptance claim against the real rendered route.</action>
  <how-to-verify>
    1. Backend owner: run the abrigo-somnia fresh-executor / `--no-mint` provisioning variant (spec §4b runbook) so `numberOfLegs(executor)==0` at demo time; FE owner re-mirrors the new buildbear-deployments.json into lib/apps/abrigo/cornerstone/ and commits. (If the fresh-executor variant is unavailable, the live path honestly degrades to demo-mode — verify the fallback DOM instead.)
    2. Launch the Evidence Collector agent per CLAUDE.md live-verification mode against `/apps/abrigo/cornerstone` (prod URL https://www.d2pfinance.xyz, fall back to local `pnpm start` port 3040):
       - Assert the mode banner + verbatim §0.2 disclosure (es-CO + en) in the DOM.
       - In live mode (fresh sandbox + connected wallet on 31337): Confirm → assert real tx hash, decoded ExecutorDecided rows, PositionMinted, On-Chain Evidence Panel with strike+TokenId, and quoteMargin-derived margins.
       - Assert HedgeDecisionCardV2 shows the nonErgodicDisclosed pill + the "(TEMPLATE)" rationale at full weight, NOT inside <details>.
       - Assert the cost panel reads "not deployed for this demo" with no numbers.
       - Run the honesty greps on the live DOM (no executed/realized, no `$` PnL, no `0x000…0`, fork-verified pill not green).
       - Screenshots to /tmp/d2p-verify/; append verdicts to .planning/phases/09-cornerstone-live-tx-integration/09-LIVE-VERIFICATION.md.
  </how-to-verify>
  <resume-signal>Type "approved" when every claim is ✓ (or has an explicit waiver recorded in 09-LIVE-VERIFICATION.md), else describe the ✗ to fix.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build && pnpm exec playwright test` green locally (the Phase-8 CI lesson).
- `pnpm run test:impeccable` + token tests green; `pnpm tsc --noEmit` passes.
- Evidence Collector 09-LIVE-VERIFICATION.md: every spec §6 acceptance bullet ✓ or waived.
- copy-review.md row present for somnia.cornerstone.live.*.
</verification>

<success_criteria>
A judge at /apps/abrigo/cornerstone sees an always-visible mode banner + verbatim no-bridge disclosure; on a fresh sandbox + wallet, Confirm submits a real resolveFromMandate and the UI renders the real tx hash + decoded ExecutorDecided + PositionMinted + quoteMargin under a neutral fork-verified pill; the D1 honesty flag + TEMPLATE rationale are full-weight on the card; the cost panel is a static not-deployed placeholder; and on a used/expired/unreachable sandbox the mock fallback runs under "modo demostración (sin cadena)" with no fake tx hash — all honesty invariants verified in the live DOM.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cornerstone-live-tx-integration/09-03-SUMMARY.md` and `.planning/phases/09-cornerstone-live-tx-integration/09-LIVE-VERIFICATION.md` (Evidence Collector verdicts).
</output>

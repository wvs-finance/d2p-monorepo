---
phase: 09-cornerstone-live-tx-integration
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - wagmi.config.ts
  - lib/contracts/generated.ts
  - lib/apps/abrigo/cornerstone/buildbear-deployments.json
  - lib/apps/abrigo/cornerstone/artifact-loader.ts
  - lib/apps/abrigo/cornerstone/buildbear.ts
  - lib/apps/abrigo/cornerstone/balance-delta.ts
  - lib/apps/abrigo/cornerstone/token-id.ts
  - tests/unit/cornerstone/balance-delta.test.ts
  - tests/unit/cornerstone/token-id.test.ts
  - tests/unit/cornerstone/artifact-loader.test.ts
  - tsconfig.json
autonomous: true
requirements: [MOD5-ABI, MOD5-CHAIN]
must_haves:
  truths:
    - "ABIs for MacroHedgeExecutor, MacroHedgeStrategist, IPanopticData are wagmi-generated from contracts/out/, not hand-typed"
    - "decodeBalanceDelta sign-extends the low word so a negative amount1 decodes correctly"
    - "extractStrike(positionId) returns 360360 for the recorded mint TokenId"
    - "isExpired returns false for a fresh capturedAt and true after capturedAt + 3 days"
    - "buildbear chain id 31337 and rpcUrl come only from the mirrored artifact, never a hardcoded endpoint"
  artifacts:
    - path: "lib/contracts/generated.ts"
      provides: "wagmi-generated ABIs incl. macroHedgeExecutorAbi, iPanopticDataAbi"
      contains: "macroHedgeExecutorAbi"
    - path: "lib/apps/abrigo/cornerstone/buildbear-deployments.json"
      provides: "mirrored fork artifact (addresses, rpcUrl, capturedAt)"
      contains: "rpcUrl"
    - path: "lib/apps/abrigo/cornerstone/balance-delta.ts"
      provides: "decodeBalanceDelta sign-extending decoder"
      exports: ["decodeBalanceDelta"]
    - path: "lib/apps/abrigo/cornerstone/artifact-loader.ts"
      provides: "typed loader + isExpired(nowMs)"
      exports: ["deployment", "isExpired"]
    - path: "lib/apps/abrigo/cornerstone/buildbear.ts"
      provides: "createBuildBearChain + public client from artifact rpcUrl"
      exports: ["createBuildBearChain", "createBuildBearPublicClient", "BuildBearChainId"]
    - path: "lib/apps/abrigo/cornerstone/token-id.ts"
      provides: "extractStrike(positionId) Panoptic TokenId leg-0 strike"
      exports: ["extractStrike"]
  key_links:
    - from: "lib/apps/abrigo/cornerstone/buildbear.ts"
      to: "lib/apps/abrigo/cornerstone/artifact-loader.ts"
      via: "rpcUrl sourced from deployment artifact"
      pattern: "deployment\\.rpcUrl|createBuildBearChain\\(.*rpcUrl"
---

<objective>
Wave-0 data layer: generate the real ABIs via wagmi codegen, mirror the BuildBear fork artifact, and write the pure decoders (BalanceDelta sign-extension, Panoptic TokenId strike extraction) and the artifact loader (isExpired) — all TDD, failing-first. These are the load-bearing edges where the BigInt/int24 burn class lives.

Purpose: every downstream live-producer task depends on correct ABIs + decoders + a typed artifact. Establishing them first, test-covered, prevents the Phase-2/6 burn class (silent sign / Turbopack JSON bundling failures).
Output: lib/contracts/generated.ts (wagmi), mirrored buildbear-deployments.json, balance-delta.ts, token-id.ts, artifact-loader.ts, buildbear.ts + 3 unit test files.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-06-07-module5-cornerstone-live-tx-design.md
@.planning/phases/09-cornerstone-live-tx-integration/09-RESEARCH.md
@.planning/phases/09-cornerstone-live-tx-integration/09-VALIDATION.md

<interfaces>
<!-- Existing project patterns the executor reuses directly. -->

From lib/apps/abrigo/somnia/chain.ts (the isolation/defineChain template):
```typescript
export const SomniaChainId = 50312 as const
export const somniaTestnet = defineChain({ /* id, name, nativeCurrency, rpcUrls */ })
export const somniaClient = createPublicClient({ chain: somniaTestnet, transport: http(...) })
```

From wagmi.config.ts (Phase-1 placeholder to repoint):
```typescript
export default defineConfig({
  out: 'lib/contracts/generated.ts',
  plugins: [ foundry({ project: '../abrigo', artifacts: 'out/' }), react() ],
})
```

Source artifact (mirror FROM here): /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/script/out/buildbear-deployments.json
Compiled ABIs live in: /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/out/{MacroHedgeExecutor.sol,MacroHedgeStrategist.sol,IPanopticData.sol}
Panoptic TokenId encoding source: contracts/lib/panoptic-sdk/src/generated.ts (or v4-core TokenId.sol) — strike(0) bit offset.
package.json already defines `"contracts:gen": "wagmi generate"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repoint wagmi codegen at abrigo-somnia contracts/out and generate ABIs</name>
  <read_first>
    - wagmi.config.ts (the Phase-1 placeholder pointing at ../abrigo, out/)
    - package.json scripts (`contracts:gen` = `wagmi generate` already present)
    - spec §2 D3 (line ~100) — ABIs via wagmi codegen from contracts/out/; spec §6 D3 acceptance
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/out/ (confirm MacroHedgeExecutor.sol/, MacroHedgeStrategist.sol/, IPanopticData.sol/ exist)
  </read_first>
  <action>
    Repoint wagmi.config.ts foundry plugin: set `project: '../abrigo/abrigo-somnia/contracts'`, `artifacts: 'out/'`, and add `include: ['MacroHedgeExecutor.json', 'MacroHedgeStrategist.json', 'IPanopticData.json']` so only the three needed ABIs are emitted (keeps generated.ts small). Keep `out: 'lib/contracts/generated.ts'` and the `react()` plugin.
    Run `pnpm contracts:gen`. Commit the generated `lib/contracts/generated.ts`. Verify it exports `macroHedgeExecutorAbi`, `macroHedgeStrategistAbi`, and `iPanopticDataAbi` (wagmi camelCases `<ContractName>Abi`).
    Confirm `iPanopticDataAbi` includes `numberOfLegs(address) returns (uint256)` and `macroHedgeExecutorAbi` includes the 8-field `ExecutorDecided` event, `PositionMinted`, `resolveFromMandate`, `quoteMargin`. If `pnpm contracts:gen` is NOT part of default CI/prebuild, leave it that way (do not wire into prebuild).
  </action>
  <verify>
    <automated>pnpm contracts:gen && grep -q "macroHedgeExecutorAbi" lib/contracts/generated.ts && grep -q "iPanopticDataAbi" lib/contracts/generated.ts && grep -q "numberOfLegs" lib/contracts/generated.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/contracts/generated.ts` exists and exports `macroHedgeExecutorAbi`, `macroHedgeStrategistAbi`, `iPanopticDataAbi`
    - grep `numberOfLegs` present in generated.ts
    - grep `ExecutorDecided` present in generated.ts
    - wagmi.config.ts `project` points at the abrigo-somnia contracts dir (grep `abrigo-somnia/contracts` in wagmi.config.ts)
    - `pnpm contracts:gen` NOT added to `prebuild` script in package.json
  </acceptance_criteria>
  <done>ABIs are wagmi-generated (not hand-typed); the three contract ABIs export with the needed members.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror artifact + artifact-loader (isExpired) + buildbear chain — RED then GREEN</name>
  <read_first>
    - lib/apps/abrigo/somnia/chain.ts (verbatim template for defineChain + createPublicClient)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/script/out/buildbear-deployments.json (source to copy)
    - 09-RESEARCH.md Pattern 1 + Pattern 7 (artifact loader, isExpired, Turbopack-safe static import)
    - spec §0.3 (no hardcoded fork addresses; 3-day TTL; capturedAt) + §3 (D2 reads may use a dedicated viem public client)
    - MEMORY note (STATE.md): static `import x from './x.json'` not dynamic require — Turbopack bundling lesson
  </read_first>
  <action>
    RED: write `tests/unit/cornerstone/artifact-loader.test.ts` asserting: `deployment.chainId === 31337`; `deployment.executor`, `deployment.pool`, `deployment.rpcUrl`, `deployment.capturedAt` are present non-empty; `isExpired(capturedAtMs + 2*86400e3) === false`; `isExpired(capturedAtMs + 3*86400e3 + 1) === true`. Run, confirm fail (module missing).
    GREEN:
    1. Copy `/home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/script/out/buildbear-deployments.json` to `lib/apps/abrigo/cornerstone/buildbear-deployments.json` (committed). Do NOT edit values.
    2. Create `lib/apps/abrigo/cornerstone/artifact-loader.ts`: `import rawDeployment from './buildbear-deployments.json'` (static relative import — Turbopack-safe). Export typed `BuildBearDeployment` (chainId, executor, pool, riskManagement, rpcUrl, mintTxHash, mintedStrike, capturedAt, source). Validate required fields at module load (throw if any of executor/pool/rpcUrl/capturedAt missing). Export `deployment: BuildBearDeployment` and `isExpired(nowMs: number): boolean` using `TTL_MS = 3*24*60*60*1000`.
    3. Create `lib/apps/abrigo/cornerstone/buildbear.ts` (mirror somnia/chain.ts): `export const BuildBearChainId = 31337 as const`; `export function createBuildBearChain(rpcUrl: string)` calling `defineChain({ id: 31337, name: 'BuildBear Polygon Fork', nativeCurrency: { name:'MATIC', symbol:'MATIC', decimals:18 }, rpcUrls: { default: { http: [rpcUrl] } } })`; `export function createBuildBearPublicClient(rpcUrl: string)` = `createPublicClient({ chain: createBuildBearChain(rpcUrl), transport: http(rpcUrl) })`. The rpcUrl MUST be passed in (from `deployment.rpcUrl`), never a literal.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone/artifact-loader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/apps/abrigo/cornerstone/buildbear-deployments.json` present, byte-identical to source (chainId 31337, rpcUrl present)
    - artifact-loader.ts exports `deployment` and `isExpired`; uses static `import ... from './buildbear-deployments.json'` (grep, NOT `require(`)
    - buildbear.ts contains NO hardcoded `rpc.buildbear.io` literal — grep `rpc.buildbear.io` in buildbear.ts returns empty
    - buildbear.ts exports `BuildBearChainId`, `createBuildBearChain`, `createBuildBearPublicClient`
    - isExpired boundary tests pass (false at +2d, true at +3d+1ms)
  </acceptance_criteria>
  <done>Artifact mirrored + typed loader with isExpired; isolated chain factory sourcing rpcUrl from the artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BalanceDelta decoder + Panoptic TokenId strike extractor — RED then GREEN</name>
  <read_first>
    - 09-RESEARCH.md Pattern 4 (decodeBalanceDelta, the asIntN low-word sign-extension) + "TokenId.strike(0) extraction" code example + Open Question 1
    - spec §2 M1 (BalanceDelta: amount0 = asIntN(128, v>>128n); amount1 = asIntN(128, v & ((1n<<128n)-1n))) + §6 acceptance (negative BalanceDelta + negative int24)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/lib/panoptic-sdk/src/generated.ts (or contracts/lib/v4-core .../TokenId.sol) — leg-0 strike bit offset
    - source mintTxHash 0xfce415... / mintedStrike 360360 in the mirrored artifact (the assertion anchor)
  </read_first>
  <action>
    RED: write `tests/unit/cornerstone/balance-delta.test.ts` and `tests/unit/cornerstone/token-id.test.ts`.
    balance-delta.test.ts asserts: helper `toBalanceDelta(a0,a1) = (a0 << 128n) | BigInt.asUintN(128, a1)`; `decodeBalanceDelta(toBalanceDelta(100n, -50n))` === `{ amount0: 100n, amount1: -50n }`; also `amount1 = -1n` and `amount1 = -(1n<<127n)` (min int128) round-trip; positive case `{100n, 50n}`.
    token-id.test.ts asserts: `extractStrike(<recorded positionId>) === 360360`. First read the panoptic-sdk generated.ts to PIN the exact leg-0 strike bit offset and the recorded positionId for the mintTxHash; encode the recorded positionId as a const in the test with a comment citing the source. Run both — confirm fail.
    GREEN:
    1. `lib/apps/abrigo/cornerstone/balance-delta.ts`: `export function decodeBalanceDelta(rawInt256: bigint): { amount0: bigint; amount1: bigint }` — `amount0 = BigInt.asIntN(128, rawInt256 >> 128n)`, `amount1 = BigInt.asIntN(128, rawInt256 & ((1n<<128n)-1n))`. (Use asIntN on BOTH words for safety even though `>>` is already arithmetic.)
    2. `lib/apps/abrigo/cornerstone/token-id.ts`: `export function extractStrike(positionId: bigint): number` — extract the leg-0 int24 strike at the pinned bit offset, sign-extend via `Number(BigInt.asIntN(24, (positionId >> OFFSETn) & ((1n<<24n)-1n)))`. Document the OFFSET const with the panoptic-sdk source line.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone/balance-delta.test.ts tests/unit/cornerstone/token-id.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `decodeBalanceDelta(toBalanceDelta(100n,-50n))` returns `{amount0:100n, amount1:-50n}` (negative-amount1 fixture passes)
    - min-int128 case `-(1n<<127n)` round-trips
    - `extractStrike(recordedPositionId) === 360360` passes
    - balance-delta.ts uses `BigInt.asIntN(128, ...)` for the low word (grep `asIntN(128`); NOT a bare `& mask` returned directly
    - token-id.ts pins the bit offset with a source-citing comment
    - if a Wave-0 RED stub imports a not-yet-created module, exclude it from tsconfig per the project's documented stub pattern, then re-include once GREEN
  </acceptance_criteria>
  <done>Both sign-correct decoders implemented and unit-tested incl. the negative fixtures and the 360360 strike anchor.</done>
</task>

</tasks>

<verification>
- `pnpm contracts:gen` regenerates ABIs with the three contracts.
- `pnpm vitest run tests/unit/cornerstone/` green (3 test files).
- `pnpm tsc --noEmit` passes (no leaked stubs).
- grep: no hardcoded `rpc.buildbear.io` in buildbear.ts; no `require(` in artifact-loader.ts.
</verification>

<success_criteria>
ABIs wagmi-generated; artifact mirrored + isExpired honored; BalanceDelta and TokenId decoders sign-correct and test-covered (incl. negative amount1 and strike===360360); buildbear chain factory sources rpcUrl from the artifact only.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cornerstone-live-tx-integration/09-01-SUMMARY.md`.
This plan produces no rendered route — skip Evidence Collector; note so in the SUMMARY (pure data-layer/config task per CLAUDE.md "When to skip").
</output>

---
phase: 9
slug: cornerstone-live-tx-integration
status: approved
shadcn_initialized: true
preset: "new-york / neutral / cssVariables / lucide"
created: 2026-06-07
reviewed_at: 2026-06-07
review: "gsd-ui-checker APPROVED 6/6 (2 non-blocking FLAGs: focal-point declaration, 12px banner exception); all honesty-contract checks PASS"
focal_point: "Persistent anchor = the always-visible Mode Banner (above the fold). After a confirmed mint, the first content focal point is the On-Chain Evidence Panel heading (real tx hash + strike + TokenId); the expandable ExecutorDecided rationale and the static cost placeholder are secondary appendices below it."
spacing_exception: "12px = the Mode Banner vertical padding only (compact persistent element); it is a multiple of 4 but intentionally NOT added to the 4/8/16/24/32/48/64 scale — do not normalize to 8/16; treat as a one-off documented in the banner component."
backend_handoff_v3: "Reconciled to abrigo-somnia/docs/UI-AGENT-HANDOFF.md (2026-06-07). D1 (Davidson honesty split): nonErgodicDisclosed (honesty flag) + the '(TEMPLATE)'-marked rationale render ON the Agent-2 decision card at FULL weight (color+icon+text rows, never <details>); the OTHER ExecutorDecided fields (regimeZt, inflationAdjustment, strikeTick, regimeWidth, parametricHedged) stay in the expandable post-mint evidence panel. This refines Surface 4 — the rationale moves onto the card, the panel keeps the geometry fields. See spec v3 §5.2."
---

# Phase 9 — UI Design Contract
# cornerstone-live-tx-integration (Module 5)

> Visual and interaction contract for Phase 9 frontend extension.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> SCOPE: This phase EXTENDS the existing Phase-8 cornerstone surface.
> It does NOT redesign any existing component. All design decisions below
> describe ONLY the six new or changed surfaces listed in §New Surfaces.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui | components.json detected |
| Preset | new-york · neutral · cssVariables | components.json |
| Component library | Radix UI (via shadcn new-york) | components.json |
| Icon library | lucide-react | components.json |
| Font family | IBM Plex Sans (body/UI) + IBM Plex Mono (hashes, code) | CLAUDE.md locked |

---

## Existing Surfaces — Reuse Without Change

The following Phase-8 components are UNCHANGED. Executor must NOT modify their visual contract:

| Component | Path |
|-----------|------|
| RunTranscript | `components/defi/cornerstone/RunTranscript.tsx` |
| HedgeDecisionCardV2 | `components/defi/cornerstone/HedgeDecisionCardV2.tsx` |
| MintCard | `components/defi/cornerstone/MintCard.tsx` |
| PromptBox | `components/defi/cornerstone/PromptBox.tsx` |
| DecisionPipelineTrace | `components/defi/somnia/DecisionPipelineTrace.tsx` |
| ProvenanceBadge | `components/defi/somnia/` (phase-7 pattern) |
| LivenessPill | `components/defi/somnia/LivenessPill.tsx` |
| SystemPromptDisclosure | `components/defi/somnia/SystemPromptDisclosure.tsx` |
| PipelineStage | `components/defi/somnia/PipelineStage.tsx` |

---

## Spacing Scale

Standard 8-point scale — unchanged from the project baseline.

| Token | Value | Usage in Phase 9 new surfaces |
|-------|-------|-------------------------------|
| xs | 4px | Icon-to-label gap inside status pills |
| sm | 8px | DataRow cell padding, pill padding |
| md | 16px | Panel inner padding, banner padding |
| lg | 24px | Between panels in the evidence section |
| xl | 32px | Between the mode banner and the transcript |
| 2xl | 48px | Section breaks |
| 3xl | 64px | Page-level (inherited from Phase 8 shell) |

Exceptions:
- Touch targets for copy-hash button: minimum 44px × 44px (WCAG 2.5.5).
- Expandable rationale panel trigger: minimum 44px tall (keyboard-activatable).

---

## Typography

Inherited from the project-wide token set. All new surfaces use exactly these 4 roles:

| Role | Size | Weight | Line Height | Font | Usage |
|------|------|--------|-------------|------|-------|
| Body | 14px | 400 (regular) | 1.5 | IBM Plex Sans | Mode banner disclosure text, panel body copy, rationale verbatim |
| Label | 12px | 600 (semibold) | 1.4 | IBM Plex Sans | DataRow dt labels, pill text, provenance label |
| Heading | 16px | 600 (semibold) | 1.2 | IBM Plex Sans | Panel section headings, mode banner primary line |
| Mono | 13px | 400 (regular) | 1.5 | IBM Plex Mono | Tx hash, TokenId string, strikeTick raw value |

No additional sizes or weights. Two weights maximum (400 + 600).

---

## Color

Project-locked token set. Phase 9 new surfaces follow the 60/30/10 split exactly.

| Role | Token | OKLCH | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `--bg-canvas` | oklch(0.97 0.005 80) | Page background behind new panels |
| Secondary (30%) | `--bg-surface` | oklch(0.94 0.008 75) | Mode banner background, evidence panel background, cost-placeholder panel |
| Accent (10%) | `--accent-default` | oklch(0.6 0.08 70) | Copy-hash button hover ring, expandable trigger chevron, DataRow value emphasis |
| Destructive | `--destructive` | oklch(0.52 0.19 25) | Reverted/error tx state text |

### Accent reserved for (Phase 9 new surfaces only):
1. Copy-hash button focus ring on the tx-hash mono field
2. Expandable rationale panel trigger chevron icon (open state)
3. Mode banner left border stripe (live mode only — 2px)

### Provenance color rule (binding from spec §0):
- `fork-verified` tier = NEUTRAL — rendered with `--text-secondary` + `--border-default`, NEVER with `--status-pass` (green) or `--accent-default` as a fill.
- The fork-verified pill background: `--bg-surface`. Pill border: `--border-default`. Pill text: `--text-secondary`. Pill icon: lucide `GitFork` at `--text-secondary`.

### Status pill color assignments for new states:

| State | Background | Border | Text | Icon |
|-------|------------|--------|------|------|
| submitting | `--status-in-progress / 0.12` | `--status-in-progress` | `--status-in-progress` | lucide `Loader2` (spin) |
| pending | `--status-in-progress / 0.12` | `--status-in-progress` | `--status-in-progress` | lucide `Clock` |
| confirmed | `--status-pass / 0.12` | `--status-pass` | `--status-pass` | lucide `CheckCircle2` |
| reverted / error | `--status-fail / 0.12` | `--status-fail` | `--status-fail` | lucide `XCircle` |
| demo mode (fallback) | `--bg-surface` | `--border-default` | `--text-muted` | lucide `FlaskConical` |
| live mode (banner) | `--accent-subtle` | `--accent-default` | `--text-primary` | lucide `Radio` |

### Anti-fishing enforcement (CROSS-09 + LAB-05, NON-NEGOTIABLE):
- Every status pill encodes color + icon + text. Never color alone.
- HedgeDecisionCardV2 is never wrapped in `<details>` or collapsed. Full-weight always.
- FAIL and PASS iteration states rendered at identical visual weight (inherited — do not break).
- No `executed`/`realized`/`ejecutad`/`realizad` in any rendered DOM text.
- No `$`-prefixed value presented as realized PnL.
- `address(0)` or unknown school address renders as em-dash — never raw hex.

---

## New Surfaces — Visual + Interaction Contracts

### Surface 1: Mode Banner

**Location:** Top of the cornerstone page, always visible, above the PromptBox.
**Purpose:** Declares live vs. demo mode at all times. §0.7 — never a silent substitution.

**Structure:**
```
[Radio icon] en vivo · fork de Polygon (BuildBear)     [fork-verified pill]
             [§0.2 verbatim disclosure]
```
OR in fallback:
```
[FlaskConical icon] modo demostración (sin cadena)      [fork-verified pill absent]
```

**Visual spec:**
- Container: `--bg-surface` fill, 2px left border in `--accent-default` (live mode only); `--border-default` all sides.
- Padding: 16px horizontal, 12px vertical (between xs and sm — exceptional because banner is a compact persistent element).
- Primary line: 16px / semibold / `--text-primary`.
- Disclosure text (§0.2 verbatim): 14px / regular / `--text-muted`. Rendered as two lines (es-CO first, en second), NOT as a tooltip or collapsed affordance — always visible.
- Live mode left border: 2px solid `--accent-default`.
- Demo mode left border: 2px solid `--border-default`.
- No dismiss button. The banner is permanent for the session.

**Verbatim disclosure copy (§0.2 — NON-NEGOTIABLE, displayed as-is):**

es-CO: "El Agente 1 corre en vivo sobre la testnet de Somnia; el Agente 2 acuña sobre un fork de Polygon alojado (BuildBear) — el mandato lo transporta esta app entre ellos; en esta demo NO hay puente entre cadenas."

en: "Agent 1 runs live on Somnia testnet; Agent 2 mints on a hosted Polygon fork (BuildBear) — the hedge mandate is carried between them by this app; there is NO cross-chain bridge in this demo."

**Accessibility:**
- `role="status"` with `aria-live="polite"` on the mode-label span so a mode change (live → demo on freshness fail) is announced.
- Disclosure text has no aria-live (static, not updated).

---

### Surface 2: Live-tx State Sequence in the Transcript

**Location:** Appended to RunTranscript after the user clicks Confirm (live path only).
**Purpose:** Shows real blockchain progress — submitting → pending → confirmed → reverted/error.

**Append-only rule:** Each state is a NEW row appended below the previous one. No overwriting/replacing. The transcript is a chronological log.

**aria-live:** The RunTranscript container already has `aria-live="polite"`. Each new row appended triggers announcement. No additional live region needed on individual rows.

**State rows:**

| State | Icon | Text (es-CO first) | Text (en) |
|-------|------|--------------------|-----------|
| submitting | lucide `Loader2` (animated spin) | "Enviando transacción — confirma en tu billetera..." | "Submitting transaction — confirm in your wallet..." |
| pending | lucide `Clock` | "Pendiente — hash: `{txHash}`" | "Pending — hash: `{txHash}`" |
| confirmed | lucide `CheckCircle2` | "Confirmado — bloque {blockNumber}" | "Confirmed — block {blockNumber}" |
| reverted | lucide `XCircle` | "Transacción revertida. Continuar en modo demostración." | "Transaction reverted. Continuing in demo mode." |
| error | lucide `XCircle` | "Error: {shortReason}. Continuar en modo demostración." | "Error: {shortReason}. Continuing in demo mode." |

**Tx hash display (pending + confirmed rows):**
- Hash rendered in IBM Plex Mono, 13px, `--text-primary`.
- Truncated to first 10 + last 6 chars with `…` in the middle for display. Full hash accessible via `title` attribute and via copy button.
- Copy button: lucide `Copy` icon at 16×16, 44px touch target, `aria-label="Copiar hash de transacción"` / `"Copy transaction hash"`.
- NO fake explorer link. If BuildBear exposes a real explorer URL in the artifact, a link with `target="_blank" rel="noopener noreferrer"` and lucide `ExternalLink` icon is shown. If absent, no link. Never construct a fake link to Polygonscan or any mainnet explorer.
- On revert/error: no hash shown (tx never confirmed).

**Demo mode restriction:** When fallback mock is running, NO tx-hash row appears. The mock transcript uses the Phase-8 existing `replayingMock` pill copy. The live-tx state rows are only rendered on the live path.

---

### Surface 3: On-Chain Evidence Panel

**Location:** Rendered below the MintCard, after confirmed state, inside the transcript section.
**Purpose:** Shows real on-chain mint evidence under `fork-verified` provenance.

**Visual spec:**
- Container: `--bg-surface` fill, 1px `--border-default` border, `--radius` (0.5rem) rounded corners.
- Padding: 16px all sides.
- Section heading: "Evidencia en cadena" (es-CO) / "On-chain evidence" (en) — 16px semibold.
- Fork-verified provenance pill positioned top-right of the panel header area (inline-flex, right-aligned).

**Fields (DataRow pattern — same dt/dd structure as existing MintCard DataRows):**

| Field | Label (es-CO) | Label (en) | Format |
|-------|--------------|------------|--------|
| Tx hash | "Hash de transacción" | "Transaction hash" | IBM Plex Mono 13px, truncated + copy button |
| Strike | "Strike (tick)" | "Strike (tick)" | `formatScaledPercent` output + raw tick in parentheses: e.g. "5.68% (tick 360360)" |
| TokenId | "ID de posición" | "Position ID" | IBM Plex Mono 13px, full string, copy button |

**Provenance pill:**
- Label: "verificado en fork · Agente 2" (es-CO) / "fork-verified · Agent 2" (en)
- Tier: fork-verified = NEUTRAL (never green). Colors per the provenance color rule above.
- Icon: lucide `GitFork`.

**§0.2 disclosure note:**
- A single line below the panel fields: the verbatim disclosure (same as banner) is NOT repeated here. Instead, a short back-reference: "Sobre un fork de Polygon (BuildBear) — sin puente." / "On a hosted Polygon fork (BuildBear) — no bridge." — 12px label weight, `--text-muted`.

**Empty/loading state:** Panel is not rendered until confirmed state. No skeleton loader (the transcript state rows communicate progress).

---

### Surface 4: Expandable ExecutorDecided Rationale Panel

**Location:** Below the On-Chain Evidence Panel, visible only after confirmed state.
**Purpose:** Post-mint evidence panel — shows ExecutorDecided rationale fields decoded from on-chain logs.

**CRITICAL rule (spec §5.2 + CROSS-09):**
- This panel MAY use a disclosure/expand pattern (it is NON-DISPOSITIONAL reasoning evidence, not a decision).
- The HedgeDecisionCardV2 (the decision card itself) MUST remain full-weight, never collapsed.
- These are separate concerns. The executor panel is an evidence appendix, not the decision.

**Collapsed state (default):**
```
[ChevronDown] Razonamiento del ejecutor (ExecutorDecided)    [fork-verified pill]
```
**Expanded state:**
```
[ChevronUp] Razonamiento del ejecutor (ExecutorDecided)      [fork-verified pill]
  ─────────────────────────────────────────────────
  Régimen Zt           | {regimeLabel}
  Ajuste inflación     | {inflationAdjustment}%
  Strike (tick)        | {strikeTick}
  Ancho de régimen     | {regimeWidth}
  Cobertura paramétrica| [pill: color+icon+text]
  Divulgación no ergódica | [pill: color+icon+text]
  ─────────────────────────────────────────────────
  Fundamento (verbatim):
  "{rationale}"
```

**Trigger element:**
- Full-width button, 44px minimum height, `--bg-surface` background, `--border-default` bottom border (collapsed) / none (expanded).
- Trigger text: "Razonamiento del ejecutor (ExecutorDecided)" / "Executor reasoning (ExecutorDecided)" — 16px semibold.
- ChevronDown/ChevronUp icon: lucide, `--accent-default` color, transitions 200ms ease.
- `aria-expanded` toggles true/false. Controlled expand panel uses `id` + `aria-controls`.

**Field specs:**

| Field | Label (es-CO) | Label (en) | Format |
|-------|--------------|------------|--------|
| regimeZt | "Régimen Zt" | "Regime Zt" | Numeric label from a regimeLabel map (0→"Contracción", 1→"Expansión", etc.) |
| inflationAdjustment | "Ajuste inflación" | "Inflation adjustment" | WAD→percent via `formatScaledPercent`, e.g. "1.2%" |
| strikeTick | "Strike (tick)" | "Strike (tick)" | SIGNED int24 as string, IBM Plex Mono 13px |
| regimeWidth | "Ancho de régimen" | "Regime width" | SIGNED int24 as string, IBM Plex Mono 13px |
| parametricHedged | "Cobertura paramétrica" | "Parametric hedged" | Boolean pill (see below) |
| nonErgodicDisclosed | "Divulgación no ergódica" | "Non-ergodic disclosed" | Boolean pill (see below) |
| rationale | "Fundamento" | "Rationale" | Verbatim string, 14px body, blockquote-style, `--text-secondary` |

**Boolean pills (parametricHedged, nonErgodicDisclosed):**
- true: pill background `--status-pass / 0.12`, border `--status-pass`, text `--status-pass`, icon `lucide CheckCircle2`, label "sí" / "yes".
- false: pill background `--bg-surface`, border `--border-default`, text `--text-muted`, icon `lucide Circle`, label "no" / "no".
- Always color + icon + text (CROSS-09).

**requestId:** NOT surfaced (sentinel 0 on direct path — spec §2 minor).

**Container:**
- `--bg-surface` fill, 1px `--border-default` border, `--radius` rounded corners.
- Expanded panel padding: 16px.
- `rationale` field: 14px / regular / `--text-secondary`, left border 2px `--border-default` for visual blockquote treatment, padding-left 12px.

---

### Surface 5: Static Agent Cost Placeholder Panel

**Location:** Below the ExecutorDecided rationale panel (always visible in the evidence section, not gated on confirmed state).
**Purpose:** Honest capability disclosure — cost accounting exists on-chain but is not deployed for this demo. NO numbers, NO on-chain read, NO fake values.

**Visual spec:**
- Container: `--bg-surface` fill, 1px dashed `--border-default` border (dashed signals "not active"), `--radius` rounded corners.
- Padding: 16px all sides.
- Icon: lucide `CircleDashed` at 20×20 `--text-muted`.
- Heading: "Contabilidad de costos del agente" / "Agent cost accounting" — 16px semibold `--text-muted` (muted signals inactive).

**Verbatim body copy (es-CO first, en second — NON-NEGOTIABLE per spec §5.3):**

es-CO: "Implementada on-chain (`OperationalCostManagement`) — no desplegada para esta demo."
en: "Implemented on-chain — not deployed for this demo."

- 14px / regular / `--text-muted`.
- The contract name `OperationalCostManagement` is rendered in IBM Plex Mono 13px inline.
- NO numeric values. NO `totalCost` call. NO address. NO skeleton/loading state (it is permanently static).

**Accessibility:** `aria-label="Contabilidad de costos del agente — no disponible en esta demo"` on the panel container. No interactive elements inside.

---

### Surface 6: Freshness Gate / Connect-Wallet / Switch-Chain Affordances

**Location:** On the Confirm button area (existing PromptBox / confirm flow), shown BEFORE the user can submit a live transaction.
**Purpose:** Gate live-submit behind all four preconditions (spec §4a).

**Gate states and their affordances:**

| Gate state | Confirm button label | Supplementary copy |
|------------|---------------------|-------------------|
| Checking freshness (mount probe) | "Verificando estado del fork..." (disabled, `Loader2` spin prefix) | none |
| All gates pass — live available | "Confirmar y acuñar en fork" / "Confirm and mint on fork" | Mode banner shows live mode |
| Expired sandbox | "Confirmar (modo demostración)" | Mode banner shows demo mode |
| Sandbox used (numberOfLegs > 0) | "Confirmar (modo demostración)" | Mode banner: "fork usado — ejecutando en modo demostración" |
| No wallet connected | "Conectar billetera" (redirects to RainbowKit modal, existing pattern) | advisory: "Se necesita una billetera con gas en fork para acuñar en vivo." |
| Wrong chain (not 31337) | "Cambiar a fork de Polygon" | `switchChain(31337)` on click |
| RPC unreachable | "Confirmar (modo demostración)" | Mode banner shows demo mode |

**Visual spec for Confirm button (live available state):**
- Same dimensions and position as the Phase-8 mock Confirm button.
- Background: `--accent-default`. Text: `--bg-elevated` (near-white on dark ochre). 16px semibold.
- Hover: `--accent-hover`.
- This replaces the Phase-8 mock button label only when live path is confirmed available. When fallback, the existing mock Confirm button copy and style is retained as-is.

**Switch-chain advisory (wrong chain):**
- 14px / regular / `--text-muted`.
- "Cambia a la red del fork de Polygon (chainId 31337) para acuñar en vivo." / "Switch to the Polygon fork network (chainId 31337) to mint live."
- The "Cambiar a fork de Polygon" button uses existing `switchChain` RainbowKit pattern.

**No wallet advisory:**
- "Se necesita una billetera con gas en fork para acuñar en vivo." / "A wallet with fork gas is needed to mint live."
- 14px / regular / `--text-muted`. Below the gate button.

**Accessibility:**
- All advisory copy has `role="status"` or is in an `aria-live="polite"` region so gate-state changes are announced.
- Disabled buttons retain `aria-disabled="true"` and visible label explaining why (not just visually grayed).

---

## Copywriting Contract

All copy is es-CO first, en second. No machine translation. Native sign-off logged in `docs/copy-review.md`.

### New i18n namespace: `somnia.cornerstone.live.*`

(Extends the existing `somnia.cornerstone.*` namespace from Phase 8 — do not replace existing keys.)

| Element | es-CO | en |
|---------|-------|----|
| Mode banner — live label | "en vivo · fork de Polygon (BuildBear)" | "live · Polygon fork (BuildBear)" |
| Mode banner — demo label | "modo demostración (sin cadena)" | "demo mode (no chain)" |
| Mode banner — §0.2 disclosure es-CO | "El Agente 1 corre en vivo sobre la testnet de Somnia; el Agente 2 acuña sobre un fork de Polygon alojado (BuildBear) — el mandato lo transporta esta app entre ellos; en esta demo NO hay puente entre cadenas." | _(es-CO only for this line)_ |
| Mode banner — §0.2 disclosure en | _(en only)_ | "Agent 1 runs live on Somnia testnet; Agent 2 mints on a hosted Polygon fork (BuildBear) — the hedge mandate is carried between them by this app; there is NO cross-chain bridge in this demo." |
| Tx state: submitting | "Enviando transacción — confirma en tu billetera..." | "Submitting transaction — confirm in your wallet..." |
| Tx state: pending | "Pendiente — hash:" | "Pending — hash:" |
| Tx state: confirmed | "Confirmado — bloque" | "Confirmed — block" |
| Tx state: reverted | "Transacción revertida. Continuar en modo demostración." | "Transaction reverted. Continuing in demo mode." |
| Tx state: error | "Error al enviar la transacción. Continuar en modo demostración." | "Error submitting transaction. Continuing in demo mode." |
| Evidence panel heading | "Evidencia en cadena" | "On-chain evidence" |
| Evidence panel provenance back-ref | "Sobre un fork de Polygon (BuildBear) — sin puente." | "On a hosted Polygon fork (BuildBear) — no bridge." |
| Copy hash aria-label | "Copiar hash de transacción" | "Copy transaction hash" |
| Copy tokenId aria-label | "Copiar ID de posición" | "Copy position ID" |
| Rationale panel heading | "Razonamiento del ejecutor (ExecutorDecided)" | "Executor reasoning (ExecutorDecided)" |
| Rationale field: rationale | "Fundamento" | "Rationale" |
| Cost panel heading | "Contabilidad de costos del agente" | "Agent cost accounting" |
| Cost panel body es-CO | "Implementada on-chain (`OperationalCostManagement`) — no desplegada para esta demo." | _(en below)_ |
| Cost panel body en | _(es-CO above)_ | "Implemented on-chain — not deployed for this demo." |
| Gate: checking | "Verificando estado del fork..." | "Checking fork state..." |
| Gate: live CTA | "Confirmar y acuñar en fork" | "Confirm and mint on fork" |
| Gate: demo CTA | "Confirmar (modo demostración)" | "Confirm (demo mode)" |
| Gate: connect wallet | "Conectar billetera" | "Connect wallet" |
| Gate: switch chain | "Cambiar a fork de Polygon" | "Switch to Polygon fork" |
| Gate: no wallet advisory | "Se necesita una billetera con gas en fork para acuñar en vivo." | "A wallet with fork gas is needed to mint live." |
| Gate: switch chain advisory | "Cambia a la red del fork de Polygon (chainId 31337) para acuñar en vivo." | "Switch to the Polygon fork network (chainId 31337) to mint live." |
| Boolean pill: true | "sí" | "yes" |
| Boolean pill: false | "no" | "no" |
| regimeZt label map | 0 → "Contracción", 1 → "Expansión", 2 → "Neutro", _unknown_ → "—" | 0 → "Contraction", 1 → "Expansion", 2 → "Neutral", _unknown_ → "—" |

### Banned in Phase 9 copy (all pre-existing project rules, plus §0 additions):
- "ejecutad" / "ejecutar" / "executed" / "realized" / "realizad" — anywhere in rendered DOM
- "$" adjacent to a value presented as realized PnL
- "consensus-verified" — this phase (Agente 2 / ExecutorDecided) is `fork-verified`
- "bridge" used positively — only in the negative "NO hay puente" / "NO cross-chain bridge"
- Raw `0x000…0` for address(0) — render as em-dash
- "empower", "revolutionize", "seamless", "unlock", "cutting-edge", "leverage your", "unleash", "best-in-class", "next-gen"

### Destructive actions:
None in this phase. The live mint is irreversible on the fork (no mainnet funds at risk), but the fork is a BuildBear sandbox. No destructive confirmation dialog is required. The gate affordance (freshness check + wallet confirmation) is the safety mechanism.

### Empty states:
- Evidence panel: not shown until confirmed (no empty state copy needed — transcript state rows communicate progress).
- Cost panel: always shows the static placeholder (no empty/filled variation).
- Rationale panel: not rendered if ExecutorDecided event absent from receipt logs (edge case); panel is omitted silently (no error copy needed — only rendered when data is present).

### Error states:
| Error | Copy (es-CO) | Copy (en) |
|-------|-------------|-----------|
| Tx reverted | "Transacción revertida. Continuar en modo demostración." | "Transaction reverted. Continuing in demo mode." |
| RPC unreachable on probe | (Mode banner silently switches to demo mode — no error toast) | same |
| Freshness gate fail | (Mode banner switches to demo — advisory via gate copy) | same |
| User wallet rejection | "Transacción cancelada. Continuar en modo demostración." | "Transaction cancelled. Continuing in demo mode." |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Existing: Button, Badge, Separator (Phase 8 baseline). No new blocks needed for Phase 9 — all new surfaces are composed from existing primitives + lucide icons. | not required |
| Third-party | none | not applicable |

No new shadcn blocks are introduced in Phase 9. All new UI is composed from:
- Existing shadcn Button, Badge, Separator
- lucide-react icons (Loader2, Clock, CheckCircle2, XCircle, ChevronDown, ChevronUp, Copy, ExternalLink, Radio, FlaskConical, CircleDashed, GitFork)
- Custom DataRow pattern (existing Phase 8 component)
- Native HTML `<button>` with appropriate ARIA where shadcn Button is overkill

---

## Honesty Invariants Checklist (§0 — binding, checked by executor before each task)

- [ ] §0.1: No `StrategistDecided` live-log subscription to Somnia. Recorded trace only.
- [ ] §0.2: Verbatim es-CO + en disclosure visible on every live-path view (not collapsed, not in tooltip).
- [ ] §0.3: All fork addresses/RPC from `buildbear-deployments.json` artifact. Zero hardcoded addresses.
- [ ] §0.4: No "bridge" language used positively. No cross-chain interop claim.
- [ ] §0.5: No "executed"/"realized"/"ejecutad"/"realizad" in DOM. No `$` realized PnL. Decision card not under `<details>`.
- [ ] §0.6: No `_onResult` simulation or claim.
- [ ] §0.7: Fallback path always labelled "modo demostración (sin cadena)" / "demo mode (no chain)". Never silent substitution.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

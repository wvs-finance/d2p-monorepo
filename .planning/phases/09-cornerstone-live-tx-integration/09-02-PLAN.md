---
phase: 09-cornerstone-live-tx-integration
plan: 02
type: tdd
wave: 1
depends_on: ["09-01"]
files_modified:
  - lib/env.ts
  - lib/apps/abrigo/somnia/agent1-inputs.ts
  - lib/apps/abrigo/cornerstone/agent1-route-logic.ts
  - app/api/abrigo/agent1/route.ts
  - tests/unit/cornerstone/agent1-route-logic.test.ts
  - .env.example
  - tsconfig.json
autonomous: true
requirements: [MOD5-AGENT1LIVE, MOD5-LIVE]
must_haves:
  truths:
    - "POST /api/abrigo/agent1 runs server-side only (runtime nodejs); SOMNIA_OPERATOR_PK is in the t3 server schema, never NEXT_PUBLIC_, never imported by a client component"
    - "the route implements the two-leg sequence: oracle-freshness pre-check → requestSchoolDecision → poll decisionState.schoolSet → requestNotionalDecision → parse StrategistDecided(decisionId, school, mandate)"
    - "DecisionFailed is terminal at BOTH legs, correlated by the per-leg requestId captured from HedgeDecisionRequested logs; per-leg timeouts never hang"
    - "the route returns the two Somnia tx hashes + school + the SERIALIZED mandate (hex/decimal strings — no raw bigint/bytes32 across JSON), or a typed failure"
    - "a shared-secret header gates the route; the request builds against the KNOWN two-leg ABI from 09-01 codegen"
  artifacts:
    - path: "app/api/abrigo/agent1/route.ts"
      provides: "T2 hardened Somnia server route (runtime nodejs, operator key, shared secret)"
      exports: ["POST", "runtime"]
    - path: "lib/apps/abrigo/cornerstone/agent1-route-logic.ts"
      provides: "pure two-leg orchestration + serialization helpers (testable without network)"
      exports: ["serializeMandate", "parseStrategistDecided", "correlateDecisionFailed", "AgentDecisionResult"]
    - path: "lib/apps/abrigo/somnia/agent1-inputs.ts"
      provides: "pinned dataKey / consensus / userIntent inputs (O-1) documented as open input"
      exports: ["AGENT1_INPUTS"]
    - path: "lib/env.ts"
      provides: "SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET added to the t3 server schema"
      contains: "SOMNIA_OPERATOR_PK"
  key_links:
    - from: "app/api/abrigo/agent1/route.ts"
      to: "lib/contracts/generated.ts macroHedgeStrategistAbi"
      via: "requestSchoolDecision/requestNotionalDecision/decisionState/StrategistDecided against the codegen ABI"
      pattern: "requestSchoolDecision|StrategistDecided|decisionState"
    - from: "lib/apps/abrigo/cornerstone/agent1-route-logic.ts"
      to: "HedgeDecisionRequested per-leg requestId"
      via: "correlate DecisionFailed terminal handling by captured leg requestId"
      pattern: "HedgeDecisionRequested|DecisionFailed|requestId"
---

<objective>
T2 + T3 (governing plan §4 + §2 Agent-1): build the hardened Somnia server route that runs Agent-1 LIVE every iteration. It pre-checks oracle freshness, fires `requestSchoolDecision`, polls `decisionState.schoolSet`, fires `requestNotionalDecision`, parses the `StrategistDecided` join log, and returns the two tx hashes + serialized mandate — with terminal `DecisionFailed` handling at both legs correlated by per-leg requestId, per-leg timeouts, server-only operator key, and shared-secret auth.

Purpose: Agent-1 is genuinely live now (real STT tx, consensus-verified) — this route is the producer for the live Somnia leg. Somnia is server-side ONLY; the browser never holds the operator key and never talks to Somnia directly. The mandate it returns is re-hydrated + minted on the fork in 09-03.
Output: the route + its pure testable orchestration/serialization logic + pinned inputs + env schema additions.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/cornerstone-live-twochain-PLAN.md
@docs/superpowers/specs/2026-06-07-module5-cornerstone-live-tx-design.md
@.planning/phases/09-cornerstone-live-tx-integration/09-RESEARCH.md
@.planning/phases/09-cornerstone-live-tx-integration/09-01-SUMMARY.md

<interfaces>
<!-- Produced by 09-01 (Wave 0) — use directly. -->
From lib/contracts/generated.ts: macroHedgeStrategistAbi (requestSchoolDecision, requestNotionalDecision, decisionState, StrategistDecided, HedgeDecisionRequested, DecisionFailed)

Verified Agent-1 contract surface (governing plan §2, confirmed against MacroHedgeStrategist.sol source):
  requestSchoolDecision(string userIntent, bytes32 dataKey, int256 consensus) payable returns (bytes32 decisionId)
    - reverts UnknownKey if MacroOracle.latest(dataKey).deliveredAt == 0 (oracle must be fresh first — T3)
    - decisionId is emitted on the receipt as HedgeDecisionRequested(uint256 indexed requestId, bytes32 indexed decisionId, uint8 leg=School)
      → READ decisionId FROM THE LOG, not a return value (EOA tx can't return Solidity returns)
  decisionState(bytes32 decisionId) view returns DecisionState { bool schoolSet; bool notionalSet; uint64 decidedAt; string schoolLabel; }  (FOUR fields)
  requestNotionalDecision(bytes32 decisionId) payable returns (uint256 requestId)
    - reverts UnknownDecision unless schoolSet; capture notional requestId from HedgeDecisionRequested(..., leg=Notional) log
  StrategistDecided(bytes32 indexed decisionId, string school, HedgeMandate mandate)  ← parse school + full mandate from THIS log (single source)
  DecisionFailed(uint256 indexed requestId, ResponseStatus status)  ← TERMINAL per leg, correlate via the captured per-leg requestId
  HedgeMandate = { address economicTheory; bytes32 underlyingMarket; uint256 targetNotional; uint32 chainId; bool isLong }  (chainId = 137 from Agent-1)

Existing patterns:
  lib/apps/abrigo/somnia/chain.ts — somniaTestnet (50312) defineChain + somniaClient public client (read seam)
  lib/env.ts — t3 createEnv; server: {} block (add SOMNIA_OPERATOR_PK, AGENT1_ROUTE_SECRET here, NEVER client/NEXT_PUBLIC_)
  app/api/health/route.ts — Next 16 Route Handler shape (export async function POST/GET)
  abrigo-somnia Phase-11/12 scripts — source of the pinned dataKey/consensus/userIntent (O-1)

CROSS-REPO PRECONDITION (non-blocking for BUILD): the two-leg StrategistDecided strategist is implemented + 19/19 offline-proven but NOT yet deployed on Somnia (backend request filed: abrigo-somnia/docs/FRONTEND-REQUEST-2026-06-07-strategist-live-deploy.md). BUILD this route against the KNOWN two-leg ABI from 09-01 codegen + unit-test the pure logic with mocked viem. The LIVE Evidence-Collector run is GATED on the deploy landing (09-05) — until then the demo uses replay (09-01 T0).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure two-leg orchestration logic + mandate serialization (RED then GREEN)</name>
  <read_first>
    - docs/cornerstone-live-twochain-PLAN.md §2 Agent-1 (read-vs-log policy per leg) + §3 mandate serialization contract (hex/decimal strings, no raw bigint/bytes32 across JSON)
    - spec v4 governance "Mandate serialization (server→browser)" bullet
    - lib/contracts/generated.ts macroHedgeStrategistAbi (the event shapes to decode)
    - 09-RESEARCH.md Pattern 3 (decodeEventLog strict:false) — applies to parsing Somnia receipt logs too
  </read_first>
  <action>
    RED: write `tests/unit/cornerstone/agent1-route-logic.test.ts` using viem `encodeEventLog`(macroHedgeStrategistAbi) to build synthetic Somnia receipt logs:
    - `parseStrategistDecided(logs)` → `{ school: string, mandate: {economicTheory, underlyingMarket, targetNotional(bigint), chainId(137), isLong} }` from a synthetic StrategistDecided log; returns null if absent.
    - `serializeMandate(mandate)` → `{ economicTheory:'0x..', underlyingMarket:'0x..', targetNotional:'<decimal string>', chainId:137, isLong:true }` — NO bigint, NO non-hex bytes32 (assert `typeof targetNotional === 'string'`, JSON.stringify round-trips without throwing on bigint).
    - `correlateDecisionFailed(logs, schoolRequestId)` → returns `{ failed:true, leg:'school' }` when a DecisionFailed log carries the school requestId; `{failed:false}` otherwise (terminal correlation).
    - capturing decisionId + per-leg requestId from a synthetic HedgeDecisionRequested(requestId, decisionId, leg) log (helper `readDecisionRequested(logs, leg)`).
    Run, confirm fail.
    GREEN, create `lib/apps/abrigo/cornerstone/agent1-route-logic.ts`:
    - `export type AgentDecisionResult = { ok:true; somniaSchoolTx:string; somniaNotionalTx:string; school:string; mandate:SerializedMandate } | { ok:false; reason:string; leg?:'school'|'notional' }`.
    - `export function serializeMandate(m): SerializedMandate` — bigint→decimal string, addresses/bytes32 as hex; chainId stays 137 (the override to 31337 happens in the BROWSER per 09-03/D4, NOT here).
    - `export function parseStrategistDecided(logs)` using `decodeEventLog({abi:macroHedgeStrategistAbi, strict:false})` in try/catch per log; return school + mandate from the StrategistDecided log.
    - `export function readDecisionRequested(logs, leg)` → `{ requestId, decisionId }` from the HedgeDecisionRequested log for that leg.
    - `export function correlateDecisionFailed(logs, requestId)` → terminal failure detection.
    Pure functions only — no network here (the route wires them in Task 2).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone/agent1-route-logic.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - serializeMandate returns targetNotional as a string; `JSON.stringify(serializeMandate(m))` does not throw (no raw bigint); chainId is 137 (NOT overridden here)
    - parseStrategistDecided returns {school, mandate} from a synthetic StrategistDecided log and null when absent
    - correlateDecisionFailed returns failed:true with the correct leg when the DecisionFailed requestId matches the captured per-leg requestId
    - readDecisionRequested extracts decisionId + requestId from the HedgeDecisionRequested log for each leg
    - decodeEventLog called with `strict: false` (grep)
  </acceptance_criteria>
  <done>The two-leg parsing + serialization + DecisionFailed-correlation logic is pure, ABI-driven, and unit-tested without a live chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hardened Somnia server route + env + pinned inputs (RED then GREEN)</name>
  <read_first>
    - docs/cornerstone-live-twochain-PLAN.md T2 + T3 + §3 architecture (a)-(e) (oracle freshness → school → poll → notional → StrategistDecided; per-leg timeouts; terminal DecisionFailed)
    - spec §0 honesty (Agent-1 live decision is genuinely real; render its real Somnia explorer tx) + spec v4 governance (operator STT key server-side, shared-secret, never imported by a client component)
    - lib/env.ts (FULL — t3 server block; add SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET there, add to runtimeEnv map)
    - app/api/health/route.ts (Route Handler shape) + lib/apps/abrigo/somnia/chain.ts (somniaClient read seam)
    - abrigo-somnia Phase-11/12 scripts for the pinned dataKey/consensus/userIntent (O-1) — treat exact values as a documented open input
    - agent1-route-logic.ts (from Task 1) — wire its pure helpers
  </read_first>
  <action>
    1. `lib/env.ts`: add to the `server` block `SOMNIA_OPERATOR_PK: z.string().min(1).optional()` and `AGENT1_ROUTE_SECRET: z.string().min(1).optional()` (optional so the build/CI passes without them; the route 503s at runtime when absent). Add both to the `runtimeEnv` map. NEVER NEXT_PUBLIC_.
    2. `lib/apps/abrigo/somnia/agent1-inputs.ts`: `export const AGENT1_INPUTS = { dataKey: '0x...', consensus: <int256>, userIntent: '...' } as const` — pin from abrigo-somnia Phase-11/12 scripts; add a comment marking these as documented open input O-1 (governing plan §6) and citing the source script. If the exact values can't be confirmed from source at execution time, use the best-known values with a TODO(O-1) comment — the route still BUILDS (live-verify is gated on the backend deploy anyway).
    3. `app/api/abrigo/agent1/route.ts`:
       - `export const runtime = 'nodejs'`.
       - `export async function POST(req)`: validate the shared-secret header (`x-agent1-secret === env.AGENT1_ROUTE_SECRET`); 401 on mismatch. If `SOMNIA_OPERATOR_PK`/`AGENT1_ROUTE_SECRET` unset → 503 typed `{ ok:false, reason:'route not configured' }`.
       - Build an operator wallet client from `SOMNIA_OPERATOR_PK` + somniaTestnet (viem `privateKeyToAccount` + `createWalletClient`). Read seam = somniaClient.
       - (a) oracle freshness: read `MacroOracle.latest(dataKey).deliveredAt` (via the IMacroOracleLatest/codegen ABI); if 0 → return typed `{ ok:false, reason:'oracle stale' }` (do NOT attempt the school request — it would revert UnknownKey).
       - (b) `requestSchoolDecision(userIntent, dataKey, consensus)`; waitForTransactionReceipt; `readDecisionRequested(logs,'school')` → decisionId + school requestId.
       - (c) poll `decisionState(decisionId).schoolSet` up to a per-leg timeout (bounded loop, e.g. ~N polls × interval); each poll also scans for a DecisionFailed correlated to the school requestId → terminal `{ ok:false, reason:'school decision failed', leg:'school' }`; on timeout → `{ ok:false, reason:'school timeout', leg:'school' }`. NEVER hang.
       - (d) `requestNotionalDecision(decisionId)`; waitForTransactionReceipt; capture notional requestId.
       - (e) poll for the StrategistDecided log (or decisionState.notionalSet) up to the per-leg timeout; correlate notional DecisionFailed (terminal); on success `parseStrategistDecided` → school + mandate.
       - Return `{ ok:true, somniaSchoolTx, somniaNotionalTx, school, mandate: serializeMandate(mandate) }`. Wrap in try/catch → typed `{ ok:false, reason }`.
       - This file MUST NOT be imported by any client component (server-only; key never reaches the browser).
    4. `.env.example`: add `SOMNIA_OPERATOR_PK=` and `AGENT1_ROUTE_SECRET=` with comments (server-only; funded test account >50 STT).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm build && grep -q "runtime = 'nodejs'" app/api/abrigo/agent1/route.ts && grep -q "SOMNIA_OPERATOR_PK" lib/env.ts</automated>
  </verify>
  <acceptance_criteria>
    - route.ts has `export const runtime = 'nodejs'`; validates `x-agent1-secret`; 503 when env unset
    - SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET are in lib/env.ts `server` block + runtimeEnv map; NEITHER appears in the `client` block (grep both absent from client)
    - route implements oracle-freshness pre-check (grep `deliveredAt`), requestSchoolDecision, poll decisionState, requestNotionalDecision, parseStrategistDecided (grep each)
    - DecisionFailed correlated per leg (grep `correlateDecisionFailed`); bounded poll loop (no `while(true)` without a timeout break)
    - route returns serializeMandate output (grep `serializeMandate`); mandate.chainId NOT overridden here (override is browser-side per D4/09-03)
    - no client component imports app/api/abrigo/agent1/route.ts (grep imports across components/ returns empty)
    - .env.example documents SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET; `pnpm build` succeeds without them set (optional schema)
  </acceptance_criteria>
  <done>The live Agent-1 Somnia route is server-only, shared-secret-gated, implements the full two-leg sequence with terminal per-leg DecisionFailed handling + timeouts, and returns the serialized mandate built against the KNOWN two-leg ABI.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/cornerstone/agent1-route-logic.test.ts` green; `pnpm tsc --noEmit` + `pnpm build` pass.
- grep: SOMNIA_OPERATOR_PK only in env server block + .env.example (never NEXT_PUBLIC_); route runtime nodejs; route never imported by a client component.
- decodeEventLog strict:false; serializeMandate emits strings (no raw bigint/bytes32 across JSON).
</verification>

<success_criteria>
A server-only POST /api/abrigo/agent1 runs Agent-1 live on Somnia (oracle-fresh → school → poll → notional → StrategistDecided), handles DecisionFailed terminally per leg with timeouts, and returns the two tx hashes + serialized mandate — built against the codegen two-leg ABI, unit-tested with mocked logs, with the operator key never leaving the server. Live-verify is gated on the backend deploy (09-05).
</success_criteria>

<output>
After completion, create `.planning/phases/09-cornerstone-live-tx-integration/09-02-SUMMARY.md`.
No rendered route in this plan (a server API + pure logic). Verify with `curl`/`browser_network_request` against /api/abrigo/agent1 (expect 401 without the secret, 503 when unconfigured) rather than browser_snapshot — note so per CLAUDE.md "Hooks/MCP tools/background workflows" skip clause. The LIVE two-leg run is gated on the cross-repo strategist deploy (09-05).
</output>

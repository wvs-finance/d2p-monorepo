---
phase: 09-cornerstone-live-tx-integration
plan: 02
type: tdd
wave: 1
depends_on: ["09-01"]
files_modified:
  - lib/env.ts
  - lib/apps/abrigo/somnia/agent1-inputs.ts
  - lib/apps/abrigo/cornerstone/agent1-route-logic.ts
  - app/api/abrigo/agent1/route.ts
  - tests/unit/cornerstone/agent1-route-logic.test.ts
  - .env.example
  - tsconfig.json
autonomous: true
requirements: [MOD5-AGENT1LIVE, MOD5-LIVE]
must_haves:
  truths:
    - "the live two-chain Agent-1 path is BUILT + wired against the LIVE two-leg strategist 0xf0570CcB1271FFaFf4caCA628F3632257f177b1D (Somnia 50312) + the 09-01 codegen ABI, and unit-proven with mocked viem; the live on-chain RUN is ⊘ DEFERRED (external Somnia validator-callback outage — NOT a Phase-9 gate)"
    - "POST /api/abrigo/agent1 runs server-side only (runtime nodejs); SOMNIA_OPERATOR_PK is in the t3 server schema, never NEXT_PUBLIC_, never imported by a client component"
    - "the route implements the two-leg sequence: oracle-freshness pre-check (latest(dataKey).deliveredAt != 0) → requestSchoolDecision → poll decisionState.schoolSet → requestNotionalDecision → parse StrategistDecided(decisionId, school, mandate)"
    - "DecisionFailed is terminal at BOTH legs, correlated by the per-leg requestId captured from HedgeDecisionRequested logs; a silent validator no-show hits a bounded per-leg timeout (pinned, e.g. 120s) → terminal {ok:false} that surfaces the already-spent school STT; the route NEVER hangs"
    - "schoolSet && !notionalSet partial-mandate is recovered/handled (not treated as success); the route returns the two Somnia tx hashes + school + the SERIALIZED mandate (hex/decimal strings — no raw bigint/bytes32 across JSON), or a typed failure"
    - "the route is rate-limited (it auto-spends operator STT per call) and MUST be unconfigured (503) on any non-operator deploy; a shared-secret header gates it; the request builds against the KNOWN two-leg ABI from 09-01 codegen"
    - "O-1 inputs (dataKey/consensus/userIntent) are PINNED from the live on-chain datum + the v1 trace (decisions 4083729/4083997, MacroOracle 0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f), not a best-known TODO"
  artifacts:
    - path: "app/api/abrigo/agent1/route.ts"
      provides: "T2 hardened Somnia server route (runtime nodejs, operator key, shared secret, rate-limited)"
      exports: ["POST", "runtime"]
    - path: "lib/apps/abrigo/cornerstone/agent1-route-logic.ts"
      provides: "pure two-leg orchestration + serialization + DecisionFailed/timeout/partial-mandate helpers (testable without network)"
      exports: ["serializeMandate", "parseStrategistDecided", "correlateDecisionFailed", "AgentDecisionResult"]
    - path: "lib/apps/abrigo/somnia/agent1-inputs.ts"
      provides: "PINNED dataKey / consensus / userIntent (O-1) from the live datum + v1 trace"
      exports: ["AGENT1_INPUTS"]
    - path: "lib/env.ts"
      provides: "SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET added to the t3 server schema"
      contains: "SOMNIA_OPERATOR_PK"
  key_links:
    - from: "app/api/abrigo/agent1/route.ts"
      to: "lib/contracts/generated.ts macroHedgeStrategistAbi (live addr 0xf0570C…7b1D)"
      via: "requestSchoolDecision/requestNotionalDecision/decisionState/StrategistDecided against the codegen ABI"
      pattern: "requestSchoolDecision|StrategistDecided|decisionState"
    - from: "app/api/abrigo/agent1/route.ts"
      to: "MacroOracle.latest(dataKey).deliveredAt"
      via: "oracle-freshness pre-check before the school request (avoids UnknownKey revert)"
      pattern: "deliveredAt"
    - from: "lib/apps/abrigo/cornerstone/agent1-route-logic.ts"
      to: "HedgeDecisionRequested per-leg requestId"
      via: "correlate DecisionFailed + timeout terminal handling by captured leg requestId"
      pattern: "HedgeDecisionRequested|DecisionFailed|requestId"
---

<objective>
T2 + T3 (governing plan §4 + §2 Agent-1): BUILD + wire the hardened Somnia server route that runs Agent-1 LIVE every iteration against the LIVE two-leg strategist `0xf0570CcB1271FFaFf4caCA628F3632257f177b1D`. It pre-checks oracle freshness (latest(dataKey).deliveredAt != 0), fires `requestSchoolDecision`, polls `decisionState.schoolSet`, fires `requestNotionalDecision`, parses the `StrategistDecided` join log, and returns the two tx hashes + serialized mandate — with terminal `DecisionFailed` AND bounded per-leg timeout handling (the silent validator no-show is the EXPECTED live state), partial-mandate recovery, server-only operator key, shared-secret auth, and rate-limiting.

Purpose: Agent-1 is genuinely live now (real STT tx, consensus-verified) but the on-chain RUN is gated ⊘ on the external Somnia validator-callback outage. This plan BUILDS + unit-proves the producer for the live Somnia leg so it auto-works on Somnia recovery with no code change. Somnia is server-side ONLY; the browser never holds the operator key and never talks to Somnia directly. The mandate it returns is re-hydrated + minted on the fork in 09-03. The LIVE RUN verdict is ⊘ DEFERRED (09-05), NOT a Phase-9 completion gate.
Output: the route + its pure testable orchestration/serialization/timeout logic + PINNED inputs + env schema additions.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/cornerstone-live-twochain-PLAN.md
@docs/superpowers/specs/2026-06-07-module5-cornerstone-live-tx-design.md
@.planning/phases/09-cornerstone-live-tx-integration/09-RESEARCH.md
@.planning/phases/09-cornerstone-live-tx-integration/09-01-SUMMARY.md

<interfaces>
<!-- Produced by 09-01 (Wave 0) — use directly. -->
From lib/contracts/generated.ts: macroHedgeStrategistAbi (requestSchoolDecision, requestNotionalDecision, decisionState, StrategistDecided, HedgeDecisionRequested, DecisionFailed) + the MacroOracle latest(bytes32) read (or the documented inline ABI tuple from 09-01).
LIVE strategist address: 0xf0570CcB1271FFaFf4caCA628F3632257f177b1D (Somnia 50312). MacroOracle: 0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f.

Verified Agent-1 contract surface (governing plan §2, confirmed against MacroHedgeStrategist.sol source — EVM layer validated unchanged by all 3 reviewers):
  requestSchoolDecision(string userIntent, bytes32 dataKey, int256 consensus) payable returns (bytes32 decisionId)
    - reverts UnknownKey if MacroOracle.latest(dataKey).deliveredAt == 0 (oracle must be fresh first — T3). The live datum IS already delivered (deliveredAt≠0, scaledValue=584) so the read won't revert.
    - decisionId is emitted on the receipt as HedgeDecisionRequested(uint256 indexed requestId, bytes32 indexed decisionId, uint8 leg=School)
      → READ decisionId FROM THE LOG, not a return value (EOA tx can't return Solidity returns)
  decisionState(bytes32 decisionId) view returns DecisionState { bool schoolSet; bool notionalSet; uint64 decidedAt; string schoolLabel; }  (FOUR fields)
  requestNotionalDecision(bytes32 decisionId) payable returns (uint256 requestId)
    - reverts UnknownDecision unless schoolSet; capture notional requestId from HedgeDecisionRequested(..., leg=Notional) log
  StrategistDecided(bytes32 indexed decisionId, string school, HedgeMandate mandate)  ← parse school + full mandate from THIS log (single source)
  DecisionFailed(uint256 indexed requestId, ResponseStatus status)  ← TERMINAL per leg, correlate via the captured per-leg requestId
  HedgeMandate = { address economicTheory; bytes32 underlyingMarket; uint256 targetNotional; uint32 chainId; bool isLong }  (chainId = 137 from Agent-1)

Existing patterns:
  lib/apps/abrigo/somnia/chain.ts — somniaTestnet (50312) defineChain + somniaClient public client (read seam)
  lib/env.ts — t3 createEnv; server: {} block (add SOMNIA_OPERATOR_PK, AGENT1_ROUTE_SECRET here, NEVER client/NEXT_PUBLIC_)
  app/api/health/route.ts — Next 16 Route Handler shape (export async function POST/GET)

O-1 PINNED INPUTS (v5 fix-5 — concrete, NOT a TODO): the dataKey/consensus/userIntent come from the LIVE on-chain datum + the v1 trace — decisions 4083729 / 4083997, MacroOracle 0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f, the delivered datum (deliveredAt≠0, scaledValue=584). Read these off-chain from the v1 trace / the abrigo-somnia Phase-11/12 scripts and pin the exact bytes32 dataKey + int256 consensus + the userIntent string with a source-citing comment. Because the datum is already delivered, the latest(dataKey) read will NOT revert UnknownKey.

REFRAME (v5): the live two-leg StrategistDecided strategist IS deployed live (0xf0570C…7b1D), but validator inference callbacks silently no-show (school-leg mines status 0x1, deterministic decisionId, decisionState stays (false,false,0,"") through 480s; ~0.9 STT spent for zero decisions). BUILD + unit-prove this route against the KNOWN two-leg ABI now; the silent-no-show is the EXPECTED live state → the route MUST hit a bounded per-leg timeout, terminate gracefully, surface the already-spent school STT, and NEVER hang. The LIVE on-chain RUN is ⊘ DEFERRED (09-05), gated on external Somnia recovery — not a Phase-9 gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure two-leg orchestration logic + serialization + DecisionFailed/timeout/partial-mandate (RED then GREEN)</name>
  <read_first>
    - docs/cornerstone-live-twochain-PLAN.md §2 Agent-1 (read-vs-log policy per leg) + §3 mandate serialization contract (hex/decimal strings, no raw bigint/bytes32 across JSON)
    - spec v5 reframe (silent no-show is the expected live state → bounded timeout terminal; surface spent school STT; schoolSet && !notionalSet partial-mandate) + governance "Mandate serialization (server→browser)" bullet
    - lib/contracts/generated.ts macroHedgeStrategistAbi (the event shapes to decode)
    - 09-RESEARCH.md Pattern 3 (decodeEventLog strict:false) — applies to parsing Somnia receipt logs too
  </read_first>
  <action>
    RED: write `tests/unit/cornerstone/agent1-route-logic.test.ts` using viem `encodeEventLog`(macroHedgeStrategistAbi) to build synthetic Somnia receipt logs:
    - `parseStrategistDecided(logs)` → `{ school: string, mandate: {economicTheory, underlyingMarket, targetNotional(bigint), chainId(137), isLong} }` from a synthetic StrategistDecided log; returns null if absent.
    - `serializeMandate(mandate)` → `{ economicTheory:'0x..', underlyingMarket:'0x..', targetNotional:'<decimal string>', chainId:137, isLong:true }` — NO bigint, NO non-hex bytes32 (assert `typeof targetNotional === 'string'`, JSON.stringify round-trips without throwing on bigint).
    - `correlateDecisionFailed(logs, schoolRequestId)` → returns `{ failed:true, leg:'school' }` when a DecisionFailed log carries the school requestId; `{failed:false}` otherwise (terminal correlation).
    - capturing decisionId + per-leg requestId from a synthetic HedgeDecisionRequested(requestId, decisionId, leg) log (helper `readDecisionRequested(logs, leg)`).
    - SILENT NO-SHOW (the expected live state): a pure `evaluateLegOutcome({ schoolSet, notionalSet, failedLeg, elapsedMs, timeoutMs })` helper → returns `{ terminal:true, ok:false, reason:'school timeout', leg:'school', spentSchoolStt:true }` when elapsedMs >= timeoutMs and nothing landed; `{ terminal:true, ok:false, reason:'school decision failed', leg }` on a correlated DecisionFailed; `{ terminal:false }` while still polling within the timeout; `{ ok:false, reason:'partial mandate', leg:'notional' }` for `schoolSet && !notionalSet` past timeout (NOT success). PIN `LEG_TIMEOUT_MS = 120_000` as an exported const.
    Run, confirm fail.
    GREEN, create `lib/apps/abrigo/cornerstone/agent1-route-logic.ts`:
    - `export type AgentDecisionResult = { ok:true; somniaSchoolTx:string; somniaNotionalTx:string; school:string; mandate:SerializedMandate } | { ok:false; reason:string; leg?:'school'|'notional'; spentSchoolStt?:boolean }`.
    - `export const LEG_TIMEOUT_MS = 120_000` (pinned per-leg timeout — the silent no-show terminates here).
    - `export function serializeMandate(m): SerializedMandate` — bigint→decimal string, addresses/bytes32 as hex; chainId stays 137 (the override to 31337 happens in the BROWSER per 09-03/D4, NOT here).
    - `export function parseStrategistDecided(logs)` using `decodeEventLog({abi:macroHedgeStrategistAbi, strict:false})` in try/catch per log; return school + mandate from the StrategistDecided log.
    - `export function readDecisionRequested(logs, leg)` → `{ requestId, decisionId }` from the HedgeDecisionRequested log for that leg.
    - `export function correlateDecisionFailed(logs, requestId)` → terminal failure detection.
    - `export function evaluateLegOutcome(state)` → the timeout/partial-mandate/DecisionFailed terminal decision (pure; the route's poll loop calls it each tick).
    Pure functions only — no network here (the route wires them in Task 2).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone/agent1-route-logic.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - serializeMandate returns targetNotional as a string; `JSON.stringify(serializeMandate(m))` does not throw (no raw bigint); chainId is 137 (NOT overridden here)
    - parseStrategistDecided returns {school, mandate} from a synthetic StrategistDecided log and null when absent
    - correlateDecisionFailed returns failed:true with the correct leg when the DecisionFailed requestId matches the captured per-leg requestId
    - readDecisionRequested extracts decisionId + requestId from the HedgeDecisionRequested log for each leg
    - evaluateLegOutcome: silent no-show past LEG_TIMEOUT_MS → terminal {ok:false, reason:'school timeout', spentSchoolStt:true}; correlated DecisionFailed → terminal; schoolSet&&!notionalSet past timeout → {ok:false, reason:'partial mandate', leg:'notional'}; still-polling → terminal:false (tests assert all four)
    - LEG_TIMEOUT_MS pinned (grep `120_000` / `LEG_TIMEOUT_MS`)
    - decodeEventLog called with `strict: false` (grep)
  </acceptance_criteria>
  <done>The two-leg parsing + serialization + DecisionFailed-correlation + bounded-timeout/partial-mandate logic is pure, ABI-driven, and unit-tested (incl. the expected silent-no-show terminal) without a live chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hardened + rate-limited Somnia server route + env + PINNED O-1 inputs (RED then GREEN)</name>
  <read_first>
    - docs/cornerstone-live-twochain-PLAN.md T2 + T3 + §3 architecture (a)-(e) (oracle freshness → school → poll → notional → StrategistDecided; per-leg timeouts; terminal DecisionFailed)
    - spec §0 honesty (Agent-1 live decision is genuinely real) + v5 reframe (live addr 0xf0570C…7b1D; silent no-show terminates via timeout; surface spent STT) + governance (operator STT key server-side, shared-secret, never imported by a client component) + v5 fix-6 (rate-limit; 503 on non-operator deploy)
    - lib/env.ts (FULL — t3 server block; add SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET there, add to runtimeEnv map)
    - app/api/health/route.ts (Route Handler shape) + lib/apps/abrigo/somnia/chain.ts (somniaClient read seam)
    - the v1 trace / abrigo-somnia Phase-11/12 scripts for the PINNED dataKey/consensus/userIntent (O-1: decisions 4083729/4083997, MacroOracle 0xAcA75144…, delivered datum scaledValue=584)
    - agent1-route-logic.ts (from Task 1) — wire its pure helpers incl. evaluateLegOutcome + LEG_TIMEOUT_MS
  </read_first>
  <action>
    1. `lib/env.ts`: add to the `server` block `SOMNIA_OPERATOR_PK: z.string().min(1).optional()` and `AGENT1_ROUTE_SECRET: z.string().min(1).optional()` (optional so build/CI passes without them; the route 503s at runtime when absent). Add both to the `runtimeEnv` map. NEVER NEXT_PUBLIC_.
    2. `lib/apps/abrigo/somnia/agent1-inputs.ts`: `export const AGENT1_INPUTS = { dataKey: '0x...', consensus: <int256>, userIntent: '...' } as const` — PIN from the live on-chain datum + the v1 trace (decisions 4083729/4083997, MacroOracle 0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f, delivered datum scaledValue=584). Cite the source (decision ids + the read) in a comment. These are CONCRETE pinned values, NOT a TODO(O-1). Because the datum is delivered, latest(dataKey).deliveredAt != 0.
    3. `app/api/abrigo/agent1/route.ts`:
       - `export const runtime = 'nodejs'`.
       - RATE LIMIT (v5 fix-6): a simple in-memory token-bucket / min-interval per operator (e.g. reject a 2nd call within N seconds with 429) — this route auto-spends operator STT per call, so a tight default cap protects the funded account. Add a `// replay/nonce note:` comment documenting that concurrent calls share the operator nonce and must be serialized.
       - `export async function POST(req)`: validate the shared-secret header (`x-agent1-secret === env.AGENT1_ROUTE_SECRET`); 401 on mismatch. If `SOMNIA_OPERATOR_PK`/`AGENT1_ROUTE_SECRET` unset → 503 typed `{ ok:false, reason:'route not configured' }` (the non-operator-deploy guard).
       - Build an operator wallet client from `SOMNIA_OPERATOR_PK` + somniaTestnet (viem `privateKeyToAccount` + `createWalletClient`) targeting the LIVE strategist 0xf0570C…7b1D. Read seam = somniaClient.
       - (a) oracle freshness: read `MacroOracle.latest(AGENT1_INPUTS.dataKey).deliveredAt` (via the codegen/inline ABI from 09-01); if 0 → return typed `{ ok:false, reason:'oracle stale' }` (do NOT attempt the school request — it would revert UnknownKey).
       - (b) `requestSchoolDecision(userIntent, dataKey, consensus)`; waitForTransactionReceipt; `readDecisionRequested(logs,'school')` → decisionId + school requestId.
       - (c) poll `decisionState(decisionId).schoolSet` in a BOUNDED loop driven by `evaluateLegOutcome({...elapsedMs, timeoutMs:LEG_TIMEOUT_MS})`; each tick also scans for a DecisionFailed correlated to the school requestId → terminal. On the silent no-show (the EXPECTED live state) the loop hits LEG_TIMEOUT_MS → terminal `{ ok:false, reason:'school timeout', leg:'school', spentSchoolStt:true }`. NEVER `while(true)` without the timeout break.
       - (d) `requestNotionalDecision(decisionId)`; waitForTransactionReceipt; capture notional requestId.
       - (e) poll for StrategistDecided (or decisionState.notionalSet) bounded by LEG_TIMEOUT_MS; correlate notional DecisionFailed (terminal); `schoolSet && !notionalSet` past timeout → `{ ok:false, reason:'partial mandate', leg:'notional' }`; on success `parseStrategistDecided` → school + mandate.
       - Return `{ ok:true, somniaSchoolTx, somniaNotionalTx, school, mandate: serializeMandate(mandate) }`. Wrap in try/catch → typed `{ ok:false, reason }`.
       - This file MUST NOT be imported by any client component (server-only; key never reaches the browser).
    4. `.env.example`: add `SOMNIA_OPERATOR_PK=` and `AGENT1_ROUTE_SECRET=` with comments (server-only; funded test account >50 STT; route MUST stay unconfigured/503 on non-operator deploys).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm build && grep -q "runtime = 'nodejs'" app/api/abrigo/agent1/route.ts && grep -q "SOMNIA_OPERATOR_PK" lib/env.ts && grep -q "LEG_TIMEOUT_MS" app/api/abrigo/agent1/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - route.ts has `export const runtime = 'nodejs'`; validates `x-agent1-secret`; 503 when env unset; rate-limits (grep a 429 / min-interval guard) + a replay/nonce comment
    - SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET are in lib/env.ts `server` block + runtimeEnv map; NEITHER appears in the `client` block (grep both absent from client)
    - AGENT1_INPUTS pins concrete dataKey/consensus/userIntent with a source-citing comment (decisions 4083729/4083997 + MacroOracle 0xAcA75144…); NO `TODO(O-1)` placeholder remains (grep `TODO(O-1)` empty)
    - route implements oracle-freshness pre-check (grep `deliveredAt`), requestSchoolDecision, poll decisionState, requestNotionalDecision, parseStrategistDecided (grep each)
    - bounded poll loop driven by evaluateLegOutcome/LEG_TIMEOUT_MS (grep; no `while(true)` without a timeout break); silent no-show returns terminal {ok:false} that surfaces spentSchoolStt
    - route targets the LIVE strategist 0xf0570CcB1271FFaFf4caCA628F3632257f177b1D (grep the address or its sourced constant)
    - route returns serializeMandate output (grep `serializeMandate`); mandate.chainId NOT overridden here (override is browser-side per D4/09-03)
    - no client component imports app/api/abrigo/agent1/route.ts (grep imports across components/ returns empty)
    - .env.example documents SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET + the 503-on-non-operator-deploy note; `pnpm build` succeeds without them set (optional schema)
  </acceptance_criteria>
  <done>The live Agent-1 Somnia route is server-only, shared-secret-gated, rate-limited, implements the full two-leg sequence against the live strategist with terminal per-leg DecisionFailed + bounded timeout (the expected silent no-show terminates gracefully + surfaces spent STT) + partial-mandate handling, pinned O-1 inputs, and returns the serialized mandate built against the KNOWN two-leg ABI.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/cornerstone/agent1-route-logic.test.ts` green (incl. the silent-no-show timeout + partial-mandate terminals); `pnpm tsc --noEmit` + `pnpm build` pass.
- grep: SOMNIA_OPERATOR_PK only in env server block + .env.example (never NEXT_PUBLIC_); route runtime nodejs; route never imported by a client component; AGENT1_INPUTS has no TODO(O-1); route targets 0xf0570C…7b1D.
- decodeEventLog strict:false; serializeMandate emits strings (no raw bigint/bytes32 across JSON); bounded timeout (LEG_TIMEOUT_MS) — no hang.
</verification>

<success_criteria>
A server-only, rate-limited POST /api/abrigo/agent1 is BUILT to run Agent-1 live on Somnia against the live strategist 0xf0570C…7b1D (oracle-fresh → school → poll → notional → StrategistDecided), handles DecisionFailed + the expected silent-no-show timeout + partial-mandate terminally per leg (surfacing spent STT, never hanging), pins O-1 inputs from the live datum + v1 trace, and returns the two tx hashes + serialized mandate — built against the codegen two-leg ABI, unit-tested with mocked logs, operator key never leaving the server. The LIVE on-chain RUN is ⊘ DEFERRED (09-05), gated on external Somnia recovery — not a Phase-9 gate.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cornerstone-live-tx-integration/09-02-SUMMARY.md`.
No rendered route in this plan (a server API + pure logic). Verify with `curl`/`browser_network_request` against /api/abrigo/agent1 (expect 401 without the secret, 503 when unconfigured, 429 on rapid repeat) rather than browser_snapshot — note so per CLAUDE.md "Hooks/MCP tools/background workflows" skip clause. The LIVE two-leg RUN is ⊘ DEFERRED (09-05), gated on external Somnia validator recovery — the route is BUILT + unit-proven now.
</output>
</content>

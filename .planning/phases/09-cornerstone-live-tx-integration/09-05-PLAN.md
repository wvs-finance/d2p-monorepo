---
phase: 09-cornerstone-live-tx-integration
plan: 05
type: execute
wave: 2
depends_on: ["09-04"]
files_modified:
  - .env.example
  - docs/cornerstone-operator-runbook.md
  - .planning/phases/09-cornerstone-live-tx-integration/09-LIVE-VERIFICATION.md
autonomous: false
requirements: [MOD5-AGENT1LIVE, MOD5-LIVE]
user_setup:
  - service: somnia
    why: "Agent-1 runs live on Somnia 50312 via the operator route against the live strategist 0xf0570CcB1271FFaFf4caCA628F3632257f177b1D — needs a funded test account (>50 STT) and the operator key. The live RUN is gated on external Somnia validator-callback recovery (⊘ DEFERRED), not on this env."
    env_vars:
      - name: SOMNIA_OPERATOR_PK
        source: "Operator's funded Somnia testnet account (>50 STT) — server-only, never NEXT_PUBLIC_; the route MUST stay unconfigured (503) on any non-operator deploy"
      - name: AGENT1_ROUTE_SECRET
        source: "Self-generated shared secret gating POST /api/abrigo/agent1 (rate-limited)"
  - service: buildbear
    why: "Agent-2 live mint needs a freshly provisioned fork (numberOfLegs(executor)==0) per governing plan T7 / spec §4b"
    dashboard_config:
      - task: "Run abrigo-somnia provision-buildbear-demo.sh (fresh-executor / --no-mint variant), then mirror the new buildbear-deployments.json into lib/apps/abrigo/cornerstone/ and commit"
        location: "abrigo-somnia/contracts/script (backend owner); FE owner re-mirrors"
must_haves:
  truths:
    - "Phase-9 in-phase acceptance = replay (the GUARANTEED artifact, verified now) + the live two-chain path BUILT/wired/unit-proven (09-01..04). The live on-chain RUN is ⊘ DEFERRED — NOT a Phase-9 completion gate"
    - "an operator runbook reproduces a live two-chain run from env once Somnia recovers: provision BuildBear, set SOMNIA_OPERATOR_PK/AGENT1_ROUTE_SECRET/BuildBear RPC+addrs/dataKey/consensus, pnpm dev, browser steps, expected TWO explorer links (Somnia + BuildBear)"
    - "the runbook warns: /api/abrigo/agent1 auto-spends operator STT per call (rate-limited) and MUST be unconfigured (503) on any non-operator deploy"
    - ".env.example documents every required var (SOMNIA_OPERATOR_PK, AGENT1_ROUTE_SECRET, BuildBear RPC/addrs, dataKey/consensus)"
    - "09-05 completes on replay+mock DOM verdicts ✓ + the live two-leg RUN row explicitly ⊘ DEFERRED with the gating reason (external Somnia validator-callback outage); NO criterion requires a landed live StrategistDecided/mint within the phase"
    - "when Somnia recovers, the live DOM shows the real Somnia tx (Agent-1) + BuildBear tx (Agent-2) + tokenId + nonErgodicDisclosed + TEMPLATE rationale, all §0 honesty invariants holding — verified by a later (post-phase) Evidence Collector run"
  artifacts:
    - path: "docs/cornerstone-operator-runbook.md"
      provides: "T7 runbook — the run-it steps + env + STT-spend/503 warning + two-explorer expectations + the ⊘-deferred live-RUN note"
      min_lines: 40
    - path: ".planning/phases/09-cornerstone-live-tx-integration/09-LIVE-VERIFICATION.md"
      provides: "Evidence Collector verdicts (replay/mock ✓ now; live two-leg RUN ⊘ DEFERRED with reason)"
      min_lines: 10
  key_links:
    - from: "docs/cornerstone-operator-runbook.md"
      to: "app/api/abrigo/agent1/route.ts + lib/apps/abrigo/cornerstone/buildbear-deployments.json"
      via: "env + provision steps that make the live two-chain path runnable on Somnia recovery"
      pattern: "SOMNIA_OPERATOR_PK|buildbear-deployments"
---

<objective>
T7 (governing plan §4) + the live-DOM gate REFRAMED: write the operator runbook + finalize env documentation, then run the Evidence Collector verification — verifying the replay + mock DOM NOW (replay is the GUARANTEED in-phase artifact) and recording the LIVE two-leg on-chain RUN as ⊘ DEFERRED. The deploy precondition is RESOLVED (the two-leg strategist is live at 0xf0570CcB1271FFaFf4caCA628F3632257f177b1D); the live RUN is gated ONLY on external Somnia validator-callback recovery — which is NOT a Phase-9 completion gate.

Purpose: make the live two-chain showcase reproducible by an operator (it auto-works the moment Somnia recovers, no code change), and honor CLAUDE.md's live-DOM ground-truth gate for the surfaces verifiable now. Phase 9 completes on replay+mock ✓ + the live-RUN row explicitly ⊘ DEFERRED with the gating reason recorded.
Output: the runbook, finalized .env.example, and 09-LIVE-VERIFICATION.md with the ⊘-deferred live-RUN note + the currently-verifiable verdicts.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/cornerstone-live-twochain-PLAN.md
@docs/superpowers/specs/2026-06-07-module5-cornerstone-live-tx-design.md
@.planning/phases/09-cornerstone-live-tx-integration/09-02-SUMMARY.md
@.planning/phases/09-cornerstone-live-tx-integration/09-04-SUMMARY.md

<interfaces>
<!-- Produced by Waves 0/1/2. -->
Live Agent-1 route: POST /api/abrigo/agent1 (09-02) — runtime nodejs; rate-limited; needs SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET; pinned AGENT1_INPUTS (dataKey/consensus/userIntent, O-1 pinned from the live datum + v1 trace); targets the live strategist 0xf0570CcB1271FFaFf4caCA628F3632257f177b1D; MUST be 503 on non-operator deploys.
Agent-2 fork mint: runWorkflowLive (09-03) on the BuildBear fork (31337), resolveFromMandate(mandate,0n,1_000_000n) with mandate.chainId overridden to 31337 + economicTheory pinned to PKE 0x06.
Wired page: app/(defi)/apps/abrigo/cornerstone/page.tsx (09-04) — live|replay|mock; switch-to-31337-before-write; live degrades to replay (aria-live announce) when Agent-1 unavailable.
Artifact: lib/apps/abrigo/cornerstone/buildbear-deployments.json (mirrored; isExpired honored).
Provision script (backend): abrigo-somnia/contracts/script/provision-buildbear-demo.sh (+ the fresh-executor/--no-mint variant — open item §6/O-2).
REFRAME (v5): the deploy precondition is RESOLVED (strategist live at 0xf0570C…7b1D, oracle datum delivered scaledValue=584). The live on-chain RUN is BLOCKED only by the external Somnia validator-callback outage (silent no-show; backend's "18-02"); no owner/ETA. → live RUN verdict ⊘ DEFERRED; replay is the verified in-phase gate.
CLAUDE.md: Evidence Collector live-verification mode; target https://www.d2pfinance.xyz (fallback pnpm start :3040); verdicts ✓/⚠/✗/⊘; screenshots /tmp/d2p-verify/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Operator runbook (+ STT-spend/503 warning) + finalize .env.example</name>
  <read_first>
    - docs/cornerstone-live-twochain-PLAN.md T7 + §4b re-provision runbook + §6 open items (O-1 dataKey/consensus/userIntent — now PINNED; O-2 BuildBear TTL/re-provision)
    - spec §4b (provision → mirror → timing buffer → owner+when) + §0.2 disclosure (the two-explorer expectation) + the v5 reframe (deploy RESOLVED; live RUN ⊘ DEFERRED on validator recovery) + v5 fix-6 (STT auto-spend; rate-limit; 503 on non-operator deploy)
    - lib/env.ts (09-02 — the server vars added) + .env.example (current) + lib/apps/abrigo/somnia/agent1-inputs.ts (AGENT1_INPUTS — pinned)
    - app/api/abrigo/agent1/route.ts (the shared-secret header + 503-when-unconfigured + rate-limit behavior)
  </read_first>
  <action>
    1. Write `docs/cornerstone-operator-runbook.md` (es-CO + en headers acceptable; operator-facing ops, not user-facing copy) with the exact run-it sequence:
       - REFRAMED precondition: the two-leg StrategistDecided strategist IS deployed live on Somnia (0xf0570CcB1271FFaFf4caCA628F3632257f177b1D); the oracle datum is delivered. The LIVE RUN is currently ⊘ DEFERRED because Somnia validator inference callbacks are silently not landing (external infra; no ETA). Until they recover, the live path honestly degrades to replay (the guaranteed artifact); state this. The runbook works with NO code change once Somnia recovers.
       - Provision BuildBear (backend owner): run provision-buildbear-demo.sh (fresh-executor / --no-mint variant so numberOfLegs(executor)==0 at demo time); FE owner mirrors the new buildbear-deployments.json into lib/apps/abrigo/cornerstone/ and commits.
       - Env: set SOMNIA_OPERATOR_PK (funded >50 STT), AGENT1_ROUTE_SECRET, the BuildBear RPC + addresses (from the mirrored artifact), the dataKey/consensus/userIntent (AGENT1_INPUTS / O-1 — pinned).
       - STT / security WARNING (v5 fix-6): `/api/abrigo/agent1` auto-spends operator STT per call (2 inference-leg deposits) and is rate-limited; it MUST stay unconfigured (returns 503) on any non-operator/public deploy — never expose the operator-key path on a shared deployment. Serialize calls (shared operator nonce).
       - Run: `pnpm dev`; open /apps/abrigo/cornerstone in live mode; connect wallet; switch to fork 31337 (the page switches before the write); submit a prompt → Confirm.
       - Expected (on Somnia recovery): TWO explorer links — the Somnia tx (Agent-1 school+notional) and the BuildBear tx (Agent-2 mint) + tokenId + nonErgodicDisclosed + (TEMPLATE) rationale. Until recovery: the live path degrades to replay (aria-live announce); expect the replay artifact, no live tx hash.
       - Timing buffer: provision morning-of within the 3-day TTL; if the live gate is unavailable at showtime, replay carries the demo honestly (T0).
       - Name the owners (backend = provision/deploy; FE = mirror/run) per §4b "owner + when".
    2. Finalize `.env.example`: ensure SOMNIA_OPERATOR_PK, AGENT1_ROUTE_SECRET are present (from 09-02) with the server-only/503-on-non-operator-deploy note, and add commented placeholders for the BuildBear RPC/addresses + dataKey/consensus references (pointing at the mirrored artifact + AGENT1_INPUTS as the source of truth). No secrets committed — placeholders only.
    3. Seed a pointer in docs/copy-review.md or the runbook to the abrigo-somnia README "run it" section.
  </action>
  <verify>
    <automated>grep -q "SOMNIA_OPERATOR_PK" .env.example && grep -q "AGENT1_ROUTE_SECRET" .env.example && grep -qi "buildbear" docs/cornerstone-operator-runbook.md && grep -qi "somnia" docs/cornerstone-operator-runbook.md && grep -qi "503" docs/cornerstone-operator-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/cornerstone-operator-runbook.md exists with provision → env → run → expected-two-explorer-links steps and the REFRAMED precondition (deploy resolved; live RUN ⊘ deferred on validator recovery) — grep "0xf0570C" or "deploy" + "BuildBear" + "Somnia"
    - the runbook carries the STT-spend / rate-limit / 503-on-non-operator-deploy WARNING (grep "503" + "STT")
    - the runbook names owners (backend provision/deploy, FE mirror/run) and the timing buffer / replay fallback
    - .env.example documents SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET + BuildBear RPC/addr + dataKey/consensus placeholders (no real secrets)
    - the runbook states the live RUN is ⊘ DEFERRED on Somnia validator recovery; replay is the verified in-phase gate (NOT the deploy precondition, which is resolved)
  </acceptance_criteria>
  <done>An operator can reproduce (on Somnia recovery) or honestly degrade the live two-chain run from documented env + provision steps, with the two-explorer expectation, the STT/503 warning, and the ⊘-deferred live-RUN reframe spelled out.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Evidence Collector live-DOM gate (replay/mock ✓ now; live two-leg RUN ⊘ DEFERRED)</name>
  <what-built>
    The wired cornerstone live|replay|mock surface: mode banner + verbatim no-bridge disclosure + dual explorer links (live), live-tx state rows, on-chain evidence panel (real hash/strike/TokenId), expandable executor-rationale panel, static cost placeholder, freshness-gate confirm affordances (switch-to-31337-before-write), and the D1 card split — with the live Agent-1 Somnia route (rate-limited, against 0xf0570C…7b1D) + Agent-2 fork mint (PKE-pinned, chainId 31337) behind the unchanged workflow-store seam.
  </what-built>
  <action>This is a verification checkpoint. Build work is done in 09-01..04. Here the team runs the Evidence Collector live-DOM gate per CLAUDE.md. The deploy precondition is RESOLVED, but the LIVE two-leg on-chain RUN is ⊘ DEFERRED because Somnia validator inference callbacks are not landing (external infra). Verify replay + mock DOM now (replay = the guaranteed in-phase artifact); record the live two-leg RUN as ⊘ DEFERRED with the gating reason. Phase 9 completes on replay+mock ✓ + the ⊘-deferred row — NO landed live StrategistDecided/mint is required in-phase.</action>
  <how-to-verify>
    1. Live two-leg on-chain RUN: ⊘ DEFERRED — Somnia validator inference callbacks silently no-show (school-leg mines status 0x1, decisionState stays (false,false,0,"")); external infra, no ETA. Do NOT block Phase 9 on it. (When Somnia recovers: run the fresh-executor/--no-mint BuildBear provision so numberOfLegs(executor)==0; FE re-mirrors buildbear-deployments.json + commits; then run the live EC pass below — post-phase.)
    2. Launch the Evidence Collector agent per CLAUDE.md live-verification mode against /apps/abrigo/cornerstone (prod https://www.d2pfinance.xyz, fallback local pnpm start :3040):
       - REPLAY mode (verifiable NOW — the guaranteed artifact): assert the recorded snapshot decision + mint (strike 360360) render end-to-end (the T0 artifact); banner shows "modo repetición · recibos reales"; NO fake live tx hash.
       - MOCK mode (verifiable NOW): banner shows "modo demostración (sin cadena)"; NO tx hash / block link.
       - Assert the mode banner + verbatim §0.2 disclosure (es-CO + en) in the DOM; HedgeDecisionCardV2 shows nonErgodicDisclosed pill + "(TEMPLATE)" rationale at full weight, NOT inside <details>; cost panel reads "not deployed for this demo" with no numbers; the live→replay degradation announces via aria-live (force the Agent-1 route unavailable).
       - LIVE mode (DEFERRED until Somnia recovery; run post-phase when available): Confirm → assert the real Somnia tx (Agent-1) + BuildBear tx (Agent-2) + decoded ExecutorDecided rows + PositionMinted + On-Chain Evidence Panel (strike 360360 + TokenId) + quoteMargin margins; both explorer links present.
       - Honesty greps on the DOM (no executed/realized/ejecutad/realizad, no `$` PnL, no 0x000…0, fork-verified pill not green, pills color+icon+text).
       - Screenshots → /tmp/d2p-verify/; append verdicts (✓/⚠/✗/⊘) to .planning/phases/09-cornerstone-live-tx-integration/09-LIVE-VERIFICATION.md, marking the live two-leg RUN verdict ⊘ DEFERRED with the external-Somnia-validator-outage reason.
  </how-to-verify>
  <resume-signal>Type "approved" when the replay + mock DOM verdicts are ✓ and the live two-leg on-chain RUN verdict is explicitly ⊘ DEFERRED with the gating reason (external Somnia validator-callback outage) recorded in 09-LIVE-VERIFICATION.md; else describe the ✗ to fix. (The live RUN is NOT a Phase-9 gate.)</resume-signal>
</task>

</tasks>

<verification>
- docs/cornerstone-operator-runbook.md reproduces the live two-chain run (or its honest replay degradation) from env, with the STT/503 warning + the ⊘-deferred reframe.
- .env.example documents all required vars; no secrets committed.
- 09-LIVE-VERIFICATION.md: replay + mock DOM verdicts ✓ now; live two-leg on-chain RUN ⊘ DEFERRED (gated on external Somnia validator recovery) with the reason recorded.
- replay smoke (09-01 T0) remains the verified CI gate.
</verification>

<success_criteria>
An operator runbook + finalized env make the live two-chain showcase reproducible (Somnia Agent-1 + BuildBear Agent-2, two explorer links) on Somnia recovery, or honestly degrade to replay; the Evidence Collector confirms the replay + mock DOM now with every §0 honesty invariant verified, and the live two-leg on-chain RUN is explicitly ⊘ DEFERRED (external Somnia validator-callback outage) — NOT a Phase-9 completion gate. Phase 9 completes on replay+mock ✓ + built-and-wired live path + the ⊘-deferred row.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cornerstone-live-tx-integration/09-05-SUMMARY.md` and `.planning/phases/09-cornerstone-live-tx-integration/09-LIVE-VERIFICATION.md` (Evidence Collector verdicts incl. the ⊘-deferred live-RUN note + reason).
</output>
</content>

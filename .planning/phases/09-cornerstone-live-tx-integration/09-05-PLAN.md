---
phase: 09-cornerstone-live-tx-integration
plan: 05
type: execute
wave: 2
depends_on: ["09-04"]
files_modified:
  - .env.example
  - docs/cornerstone-operator-runbook.md
  - .planning/phases/09-cornerstone-live-tx-integration/09-LIVE-VERIFICATION.md
autonomous: false
requirements: [MOD5-AGENT1LIVE, MOD5-LIVE]
user_setup:
  - service: somnia
    why: "Agent-1 runs live on Somnia 50312 via the operator route — needs a funded test account (>50 STT) and the operator key"
    env_vars:
      - name: SOMNIA_OPERATOR_PK
        source: "Operator's funded Somnia testnet account (>50 STT) — server-only, never NEXT_PUBLIC_"
      - name: AGENT1_ROUTE_SECRET
        source: "Self-generated shared secret gating POST /api/abrigo/agent1"
  - service: buildbear
    why: "Agent-2 live mint needs a freshly provisioned fork (numberOfLegs(executor)==0) per governing plan T7 / spec §4b"
    dashboard_config:
      - task: "Run abrigo-somnia provision-buildbear-demo.sh (fresh-executor / --no-mint variant), then mirror the new buildbear-deployments.json into lib/apps/abrigo/cornerstone/ and commit"
        location: "abrigo-somnia/contracts/script (backend owner); FE owner re-mirrors"
must_haves:
  truths:
    - "an operator runbook reproduces a live two-chain run from env: provision BuildBear, set SOMNIA_OPERATOR_PK/AGENT1_ROUTE_SECRET/BuildBear RPC+addrs/dataKey/consensus, pnpm dev, browser steps, expected TWO explorer links (Somnia + BuildBear)"
    - ".env.example documents every required var (SOMNIA_OPERATOR_PK, AGENT1_ROUTE_SECRET, BuildBear RPC/addrs, dataKey/consensus)"
    - "the live Evidence-Collector verification is explicitly DEFERRED to a run once the two-leg StrategistDecided strategist is published on Somnia; until then replay (T0) is the verified demo gate"
    - "when live is reachable, the live DOM shows the real Somnia tx (Agent-1) + BuildBear tx (Agent-2) + tokenId + nonErgodicDisclosed + TEMPLATE rationale, all §0 honesty invariants holding"
  artifacts:
    - path: "docs/cornerstone-operator-runbook.md"
      provides: "T7 runbook — the run-it steps + env + two-explorer expectations + the cross-repo precondition"
      min_lines: 40
    - path: ".planning/phases/09-cornerstone-live-tx-integration/09-LIVE-VERIFICATION.md"
      provides: "Evidence Collector verdicts (deferred-live note + replay/mock DOM verdicts available now)"
      min_lines: 10
  key_links:
    - from: "docs/cornerstone-operator-runbook.md"
      to: "app/api/abrigo/agent1/route.ts + lib/apps/abrigo/cornerstone/buildbear-deployments.json"
      via: "env + provision steps that make the live two-chain path runnable"
      pattern: "SOMNIA_OPERATOR_PK|buildbear-deployments"
---

<objective>
T7 (governing plan §4) + the live-DOM gate: write the operator runbook + finalize env documentation, then run the Evidence Collector live-verification — explicitly deferring the LIVE two-leg run to a workflow once the cross-repo strategist is published, while verifying the replay/mock DOM (and the live path against a reachable fork) now.

Purpose: make the live two-chain showcase reproducible by an operator, and honor CLAUDE.md's live-DOM ground-truth gate. Because the two-leg StrategistDecided strategist is implemented + offline-proven but NOT yet deployed on Somnia, the live Evidence-Collector pass is gated on that deploy; replay (T0) is the verified guaranteed artifact until then.
Output: the runbook, finalized .env.example, and 09-LIVE-VERIFICATION.md with the deferred-live note + the currently-verifiable verdicts.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/cornerstone-live-twochain-PLAN.md
@docs/superpowers/specs/2026-06-07-module5-cornerstone-live-tx-design.md
@.planning/phases/09-cornerstone-live-tx-integration/09-02-SUMMARY.md
@.planning/phases/09-cornerstone-live-tx-integration/09-04-SUMMARY.md

<interfaces>
<!-- Produced by Waves 0/1/2. -->
Live Agent-1 route: POST /api/abrigo/agent1 (09-02) — runtime nodejs; needs SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET; pinned AGENT1_INPUTS (dataKey/consensus/userIntent, O-1).
Agent-2 fork mint: runWorkflowLive (09-03) on the BuildBear fork (31337), resolveFromMandate(mandate,0n,1_000_000n) with mandate.chainId overridden to 31337.
Wired page: app/(defi)/apps/abrigo/cornerstone/page.tsx (09-04) — live|replay|mock; live degrades to replay when Agent-1 unavailable.
Artifact: lib/apps/abrigo/cornerstone/buildbear-deployments.json (mirrored; isExpired honored).
Provision script (backend): abrigo-somnia/contracts/script/provision-buildbear-demo.sh (+ the fresh-executor/--no-mint variant — open item §6/O-2).
CROSS-REPO PRECONDITION: two-leg StrategistDecided strategist NOT yet deployed on Somnia (backend request: abrigo-somnia/docs/FRONTEND-REQUEST-2026-06-07-strategist-live-deploy.md). Live-verify is gated on this deploy.
CLAUDE.md: Evidence Collector live-verification mode; target https://www.d2pfinance.xyz (fallback pnpm start :3040); verdicts ✓/⚠/✗/⊘; screenshots /tmp/d2p-verify/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Operator runbook + finalize .env.example</name>
  <read_first>
    - docs/cornerstone-live-twochain-PLAN.md T7 + §4b re-provision runbook + §6 open items (O-1 dataKey/consensus/userIntent; O-2 BuildBear TTL/re-provision)
    - spec §4b (provision → mirror → timing buffer → owner+when) + §0.2 disclosure (the two-explorer expectation)
    - lib/env.ts (09-02 — the server vars added) + .env.example (current) + lib/apps/abrigo/somnia/agent1-inputs.ts (AGENT1_INPUTS, O-1)
    - app/api/abrigo/agent1/route.ts (the shared-secret header + 503-when-unconfigured behavior)
  </read_first>
  <action>
    1. Write `docs/cornerstone-operator-runbook.md` (es-CO + en headers acceptable; this is operator-facing ops, not user-facing copy) with the exact run-it sequence:
       - Cross-repo precondition: confirm the two-leg StrategistDecided strategist is deployed on Somnia (new address/ABI/inputs published per the backend request) — if NOT yet, the live path honestly degrades to replay; state this.
       - Provision BuildBear (backend owner): run provision-buildbear-demo.sh (fresh-executor / --no-mint variant so numberOfLegs(executor)==0 at demo time); FE owner mirrors the new buildbear-deployments.json into lib/apps/abrigo/cornerstone/ and commits.
       - Env: set SOMNIA_OPERATOR_PK (funded >50 STT), AGENT1_ROUTE_SECRET, the BuildBear RPC + addresses (from the mirrored artifact), the dataKey/consensus/userIntent (AGENT1_INPUTS / O-1).
       - Run: `pnpm dev`; open /apps/abrigo/cornerstone in live mode; connect wallet; switch to fork 31337; submit a prompt → Confirm.
       - Expected: TWO explorer links — the Somnia tx (Agent-1 school+notional) and the BuildBear tx (Agent-2 mint) + tokenId + nonErgodicDisclosed + (TEMPLATE) rationale.
       - Timing buffer: provision morning-of within the 3-day TTL; if the gate fails at showtime, replay carries the demo honestly (T0).
       - Name the owners (backend = provision/deploy; FE = mirror/run) per §4b "owner + when".
    2. Finalize `.env.example`: ensure SOMNIA_OPERATOR_PK, AGENT1_ROUTE_SECRET are present (from 09-02) and add commented placeholders for the BuildBear RPC/addresses + dataKey/consensus references (pointing at the mirrored artifact + AGENT1_INPUTS as the source of truth). No secrets committed — placeholders only.
    3. Seed a pointer in docs/copy-review.md or the runbook to the abrigo-somnia README "run it" section.
  </action>
  <verify>
    <automated>grep -q "SOMNIA_OPERATOR_PK" .env.example && grep -q "AGENT1_ROUTE_SECRET" .env.example && grep -qi "buildbear" docs/cornerstone-operator-runbook.md && grep -qi "somnia" docs/cornerstone-operator-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/cornerstone-operator-runbook.md exists with provision → env → run → expected-two-explorer-links steps and the cross-repo precondition (grep "StrategistDecided" or "deploy" + "BuildBear" + "Somnia")
    - the runbook names owners (backend provision/deploy, FE mirror/run) and the timing buffer / replay fallback
    - .env.example documents SOMNIA_OPERATOR_PK + AGENT1_ROUTE_SECRET + BuildBear RPC/addr + dataKey/consensus placeholders (no real secrets)
    - the runbook states live-verify is deferred to the strategist deploy; replay is the verified gate until then
  </acceptance_criteria>
  <done>An operator can reproduce (or honestly degrade) the live two-chain run from documented env + provision steps, with the two-explorer expectation and the cross-repo precondition spelled out.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Evidence Collector live-DOM gate (deferred-live; replay/mock now)</name>
  <what-built>
    The wired cornerstone live|replay|mock surface: mode banner + verbatim no-bridge disclosure + dual explorer links (live), live-tx state rows, on-chain evidence panel (real hash/strike/TokenId), expandable executor-rationale panel, static cost placeholder, freshness-gate confirm affordances, and the D1 card split — with the live Agent-1 Somnia route + Agent-2 fork mint behind the unchanged workflow-store seam.
  </what-built>
  <action>This is a verification checkpoint. Build work is done in 09-01..04. Here the team runs the Evidence Collector live-DOM gate per CLAUDE.md. Because the two-leg StrategistDecided strategist is NOT yet deployed on Somnia, the LIVE two-leg run is DEFERRED to a workflow_dispatch run once the strategist address is published; verify replay + mock DOM now, and the live path opportunistically if a reachable fork + (eventually) the deployed strategist exist.</action>
  <how-to-verify>
    1. Backend owner: confirm/await the two-leg StrategistDecided strategist deploy on Somnia (per abrigo-somnia/docs/FRONTEND-REQUEST-2026-06-07-strategist-live-deploy.md). If deployed: also run the fresh-executor/--no-mint BuildBear provision so numberOfLegs(executor)==0; FE owner re-mirrors buildbear-deployments.json + commits. If NOT deployed: the live two-leg verdict is ⊘ DEFERRED — verify replay + mock instead (replay is the guaranteed artifact).
    2. Launch the Evidence Collector agent per CLAUDE.md live-verification mode against /apps/abrigo/cornerstone (prod https://www.d2pfinance.xyz, fallback local pnpm start :3040):
       - REPLAY mode (verifiable NOW): assert the recorded snapshot decision + mint render end-to-end (the T0 guaranteed artifact); banner shows "modo repetición · recibos reales"; NO fake live tx hash.
       - MOCK mode (verifiable NOW): banner shows "modo demostración (sin cadena)"; NO tx hash / block link.
       - Assert the mode banner + verbatim §0.2 disclosure (es-CO + en) in the DOM; HedgeDecisionCardV2 shows nonErgodicDisclosed pill + "(TEMPLATE)" rationale at full weight, NOT inside <details>; cost panel reads "not deployed for this demo" with no numbers.
       - LIVE mode (DEFERRED until the strategist deploy; run when available): Confirm → assert the real Somnia tx (Agent-1) + BuildBear tx (Agent-2) + decoded ExecutorDecided rows + PositionMinted + On-Chain Evidence Panel (strike+TokenId) + quoteMargin margins; both explorer links present.
       - Honesty greps on the live DOM (no executed/realized/ejecutad/realizad, no `$` PnL, no 0x000…0, fork-verified pill not green, pills color+icon+text).
       - Screenshots → /tmp/d2p-verify/; append verdicts (✓/⚠/✗/⊘) to .planning/phases/09-cornerstone-live-tx-integration/09-LIVE-VERIFICATION.md, marking the live two-leg verdict ⊘ DEFERRED if the strategist isn't deployed yet.
  </how-to-verify>
  <resume-signal>Type "approved" when replay + mock DOM verdicts are ✓ and the live two-leg verdict is either ✓ (strategist deployed + verified) or explicitly ⊘ DEFERRED with the gating reason recorded in 09-LIVE-VERIFICATION.md; else describe the ✗ to fix.</resume-signal>
</task>

</tasks>

<verification>
- docs/cornerstone-operator-runbook.md reproduces the live two-chain run (or its honest replay degradation) from env.
- .env.example documents all required vars; no secrets committed.
- 09-LIVE-VERIFICATION.md: replay + mock DOM verdicts ✓ now; live two-leg ✓ or ⊘ DEFERRED (gated on the strategist deploy) with the reason recorded.
- replay smoke (09-01 T0) remains the verified CI gate.
</verification>

<success_criteria>
An operator runbook + finalized env make the live two-chain showcase reproducible (Somnia Agent-1 + BuildBear Agent-2, two explorer links) or honestly degrade to replay; the Evidence Collector confirms the replay + mock DOM now and the live DOM once the cross-repo strategist deploy lands, with every §0 honesty invariant verified and the live two-leg verify explicitly deferred until then.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cornerstone-live-tx-integration/09-05-SUMMARY.md` and `.planning/phases/09-cornerstone-live-tx-integration/09-LIVE-VERIFICATION.md` (Evidence Collector verdicts incl. the deferred-live note).
</output>

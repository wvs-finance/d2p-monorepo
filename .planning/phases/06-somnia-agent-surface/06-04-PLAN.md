---
phase: 06-somnia-agent-surface
plan: 04
type: execute
wave: 3
depends_on: ["06-00", "06-02"]
files_modified:
  - components/defi/somnia/HedgeDecisionBridge.tsx
  - lib/apps/abrigo/somnia/bridge.ts
  - app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx
  - messages/es-CO/somnia.json
  - messages/en/somnia.json
  - tests/unit/somnia-bridge.test.tsx
  - tests/e2e/somnia-bridge.spec.ts
  - tsconfig.json
  - docs/copy-review.md
autonomous: true
requirements: [SOMNIA-B, CROSS-09, CROSS-10, CROSS-01, DEFI-08]
user_setup: []

must_haves:
  truths:
    - "On the module-1 cCOP/USD simulated instrument page, a bridge joins one recorded decision to the SIMULATED convex position: operator-supplied surprise → ADD_LONG_GAMMA @ sizeBps → an ILLUSTRATIVE mapping of sizeBps to fraction-of-max on this simulated position (NOT a realized/executed position delta)"
    - "The bridge mounts ONLY on the kind==='simulated' branch of the instrument detail page (BEFORE aggregateAllChains); a simulated entry is never passed to the multicall"
    - "consensus is labeled operator-supplied (not market); surprise is gated behind that caveat; the bridge is honestly tagged testnet-agent (POC), distinct from the module-1 schematic/SIMULADO tiers"
    - "The position delta is derived from sizeBps (BigInt) and labeled as schematic/illustrative — no fabricated notional/current price; null renders em-dash, never 0"
  artifacts:
    - path: "lib/apps/abrigo/somnia/bridge.ts"
      provides: "Pure mapping: HedgeDecisionView → position-delta view (sizeBps→fraction-of-MAX_SIZE_BPS; action→direction); BigInt math, formatted at the edge"
      exports: ["decisionToPositionDelta"]
    - path: "components/defi/somnia/HedgeDecisionBridge.tsx"
      provides: "RSC bridge card: surprise → action @ sizeBps → schematic position delta, testnet-agent pill, equal-weight, em-dash nulls"
      min_lines: 40
    - path: "app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx"
      provides: "Bridge mounted inside the kind==='simulated' early-return branch (before the live path)"
      contains: "HedgeDecisionBridge"
  key_links:
    - from: "components/defi/somnia/HedgeDecisionBridge.tsx"
      to: "lib/apps/abrigo/somnia/reader.ts"
      via: "getHedgeDecisions (the ADD_LONG_GAMMA decision)"
      pattern: "getHedgeDecisions"
    - from: "components/defi/somnia/HedgeDecisionBridge.tsx"
      to: "lib/apps/abrigo/somnia/bridge.ts"
      via: "decisionToPositionDelta (sizeBps→delta, BigInt)"
      pattern: "decisionToPositionDelta"
    - from: "app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx"
      to: "components/defi/somnia/HedgeDecisionBridge.tsx"
      via: "rendered inside the kind==='simulated' branch only"
      pattern: "HedgeDecisionBridge"
---

<objective>
Component B — the surprise→decision→instrument bridge: a card that joins ONE recorded Somnia hedge decision to module-1's cCOP/USD long-gamma instrument, rendering "this operator-supplied surprise → ADD_LONG_GAMMA @ sizeBps → an ILLUSTRATIVE mapping of sizeBps to fraction-of-max on this SIMULATED convex position." (M6: the delta is an illustrative mapping on a simulated position — the copy must NOT imply a realized or executed position; `fractionOfMaxBps` is honest-real arithmetic on the real sizeBps, but it is labeled illustrative, not "the position delta".) It mounts on the EXISTING simulated instrument detail page, inside the `kind==='simulated'` early-return branch.

Purpose: Close the narrative loop — the macro datum (D) and the decision feed (A) culminate in a concrete effect on the convex instrument a visitor is already looking at. This is the LAST component because it joins A's surface to module 1; it depends on the Wave-0 reader (06-00) and the decision view shape exercised by A (06-02). It must preserve every honesty invariant: operator-supplied consensus, gated surprise, schematic/illustrative position delta (no fabricated notional), testnet-agent provenance distinct from the module-1 schematic tier, and the anti-fishing equal-weight + em-dash discipline.

Output: a pure decisionToPositionDelta mapping (BigInt), the HedgeDecisionBridge RSC card, the bridge mounted in the simulated branch of the instrument page, es-CO-first copy, the unit test (flips the Wave-0 RED stub GREEN) + an e2e that asserts the bridge renders on the instrument detail page.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-06-02-somnia-agent-surface-phase6-design.md
@.planning/phases/06-somnia-agent-surface/06-00-SUMMARY.md
@.planning/phases/06-somnia-agent-surface/06-02-SUMMARY.md

<interfaces>
From lib/apps/abrigo/somnia/reader.ts (Wave 0):
```typescript
export function getHedgeDecisions(dataKey?: string): HedgeDecisionView[]
// HedgeDecisionView { decisionId; action: 'HOLD'|'ADD_LONG_GAMMA'|'REDUCE'|'EXIT'; sizeBps: bigint; macroValue: bigint; consensus: bigint; decidedAt: Date | null; pending: boolean; sourceTxHash: string }
```
From lib/apps/abrigo/somnia/surprise.ts (Wave 0):
```typescript
export function computeSurprise(macroValue: bigint, consensus: bigint): bigint  // macroValue - consensus
export function formatSurprise(s: bigint, scale?: number): string
```
From components/defi/ProvenanceBadge.tsx (Wave 0): ProvenancePill tier="testnet-agent" subState="recorded" (NEUTRAL token, NEVER green).

Recorded decision the bridge ties to the instrument (snapshot, 06-00): decision#1 = ADD_LONG_GAMMA, sizeBps=6800, macro=568, consensus=500 → surprise +68. MAX_SIZE_BPS = 10000 (contract constant), so sizeBps 6800 = 68% of the max position size. Component B picks the ADD_LONG_GAMMA decision (the one that maps onto the long-gamma instrument). REDUCE/EXIT decisions are NOT the focus of this bridge (the instrument is a long-gamma position); if only a non-ADD decision exists, render an honest "no long-gamma decision recorded" state — never fabricate one.

THE MOUNT POINT (the binding pin — app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx):
- The page already has `if (instrument.kind === 'simulated') { ... return ( <main>…</main> ) }` which returns EARLY, BEFORE `aggregateAllChains()` is ever called. A simulated entry is therefore never passed to the multicall (DEFI-08 invariant).
- Inside that branch the existing left column (`<div className="w-full md:w-3/5 space-y-6">`) already holds: InstrumentParams, the schematic PayoffDiagram section, SnapshotPoolPanel, CashFlowWaterfall. Mount the bridge as a NEW `<section>` inside this same left column, AFTER the PayoffDiagram section (so the narrative reads payoff → "and here is how a recorded agent decision moves a position on it"). Do NOT touch the LIVE path below the early return.
- The branch already builds translated strings via `const t = await getTranslations('instruments')` and `const locale = await getLocale()`. Add a SECOND `getTranslations('somnia')` call for the bridge strings (the somnia namespace is registered by 06-01); pass a bridge-strings object + locale into the component, mirroring the existing presentational-strings idiom.
- The instrument is the SimulatedInstrument (lib/apps/abrigo/instruments.ts): { kind:'simulated'; id:'ccop-usd-long-gamma'; name; nameEn; chainId:8453; fixtureKey }.

EXISTING anti-fishing precedent (CLAUDE.md + STATE 02-06/03-02): equal visual weight; no <details> collapse; status pills color+icon+text+aria; null → '—' em-dash, never 0. The bridge's position-delta is SCHEMATIC/illustrative — it carries a schematic-or-testnet-agent provenance pill and explicit "illustrative" copy; it must NOT present a fabricated dollar notional or a fabricated current price.

EXISTING module-1 provenance distinction: the simulated instrument page uses tiers fork-fixture / spec / schematic (ProvenanceBadge). The bridge introduces testnet-agent (POC) provenance — it is a DIFFERENT, real-but-testnet source (an actual on-chain decision), so it MUST use the testnet-agent pill, not the schematic pill, to stay honest about where the decision came from.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Un-skip RED stub + build decisionToPositionDelta (BigInt) + HedgeDecisionBridge RSC</name>
  <read_first>
    - tests/unit/somnia-bridge.test.tsx (the Wave-0 RED stub to un-skip + implement against)
    - lib/apps/abrigo/somnia/reader.ts + types.ts (getHedgeDecisions / HedgeDecisionView; MAX_SIZE_BPS context)
    - lib/apps/abrigo/somnia/surprise.ts (computeSurprise / formatSurprise — surprise is BigInt, formatted at the edge)
    - components/defi/somnia/HedgeDecisionCard.tsx (06-02 — copy the equal-weight card shell, action→label map, consensus-operator caveat, surprise gating, em-dash pending idiom so the bridge is visually consistent with the feed)
    - components/defi/ProvenanceBadge.tsx (ProvenancePill tier="testnet-agent" subState)
    - components/defi/CashFlowWaterfall.tsx (existing equal-weight RSC card with spec pills + em-dash — row idiom)
  </read_first>
  <action>
    A. lib/apps/abrigo/somnia/bridge.ts — a PURE mapping module (no React, no I/O). Export:
       - `const MAX_SIZE_BPS = 10000n` (BigInt; the contract constant — do NOT import the ABI here, just the constant with a source comment).
       - `decisionToPositionDelta(decision: HedgeDecisionView): PositionDeltaView` where
         `PositionDeltaView = { direction: 'increase' | 'decrease' | 'flat'; sizeBps: bigint; fractionOfMaxBps: bigint; /* sizeBps*10000n / MAX_SIZE_BPS, integer bps-of-max */ schematic: true }`.
         Map action→direction: ADD_LONG_GAMMA→'increase', REDUCE/EXIT→'decrease', HOLD→'flat'. Compute fractionOfMaxBps in BigInt (`decision.sizeBps * 10000n / MAX_SIZE_BPS`) — NEVER coerce to Number before the division. `schematic: true` is a literal flag the component reads to force the "illustrative" label.
       - `formatFractionOfMax(fractionBps: bigint): string` — edge-formatter rendering e.g. "68%" from 6800 (decimal-shift at the edge; no Number coercion until the final string build).
       Add an explicit comment (M6): this is an ILLUSTRATIVE mapping of the real on-chain `sizeBps` to a fraction-of-MAX_SIZE_BPS on a SIMULATED position; `fractionOfMaxBps` is honest-real arithmetic (sizeBps is the real on-chain input), but it is NOT a dollar notional, NOT a real/executed position size, and NOT a realized position delta — module-1's instrument is simulated. The copy that consumes this MUST say "illustrative"/"ilustrativo" and must NOT imply a realized or executed position.

    B. Remove tests/unit/somnia-bridge.test.tsx from the tsconfig.json `exclude` array and un-skip it. Implement assertions:
       - decisionToPositionDelta on the ADD_LONG_GAMMA/6800 decision → { direction:'increase', sizeBps:6800n, fractionOfMaxBps:6800n, schematic:true }; assert typeof fractionOfMaxBps === 'bigint'.
       - decisionToPositionDelta on a REDUCE/568 decision → direction:'decrease', fractionOfMaxBps:568n.
       - formatFractionOfMax(6800n) === '68%' (or the asserted edge form).
       - BigInt exactness: a constructed sizeBps above Number.MAX_SAFE_INTEGER stays exact (no Number coercion).
       - Rendering HedgeDecisionBridge with the snapshot ADD_LONG_GAMMA decision: shows the surprise (+68, sign preserved), the action label (ADD_LONG_GAMMA human label), sizeBps 6800, and the schematic position-delta ("68%" + an "illustrative/esquemático" marker); the consensus carries the operator-supplied caveat (getByText /operator|operador|no.*mercado|not.*market/i); the surprise label appears ONLY within a subtree that also contains the operator caveat (gating assertion); a testnet-agent ProvenancePill is present (assert className includes the neutral token, NOT green/emerald/status-pass; assert its aria-label).
       - Honest empty: when getHedgeDecisions returns no ADD_LONG_GAMMA decision, the bridge renders an honest "no long-gamma decision recorded" message (em-dash for the delta), NOT a fabricated one.

    C. Build components/defi/somnia/HedgeDecisionBridge.tsx (RSC, no 'use client'):
       - Props: `{ instrument: SimulatedInstrument; labels: BridgeStrings; locale: string }` (presentational; RSC parent passes translated strings + locale).
       - Calls getHedgeDecisions() and selects the ADD_LONG_GAMMA decision (the long-gamma-relevant one). If none, render the honest empty state.
       - Renders the narrative chain at EQUAL visual weight (one shared card shell; no emphasis class that would make the bridge louder than the surrounding sections): macro print → operator-supplied consensus (caveat inline) → surprise (computeSurprise + formatSurprise, sign preserved) → action badge + sizeBps → an ILLUSTRATIVE fraction-of-max delta (decisionToPositionDelta + formatFractionOfMax, explicitly labeled "illustrative"/"ilustrativo", tied by copy to "this SIMULATED convex position" / instrument.name). M6: the copy must NOT say "position delta" unqualified or imply a realized/executed position — frame it as "an illustrative mapping of sizeBps→fraction-of-max on this simulated position".
       - CROSS-09: a testnet-agent ProvenancePill (subState="recorded") with the operator-honest aria-label; color+icon+text+aria.
       - Null / pending fields → em-dash, never 0.
       - Do NOT render a dollar notional or a current price. The only numbers are: macroValue (CPI), consensus, surprise, sizeBps, and the schematic fraction-of-max.
  </action>
  <behavior>
    - decisionToPositionDelta(ADD_LONG_GAMMA/6800) → increase, fractionOfMaxBps 6800n (bigint)
    - decisionToPositionDelta(REDUCE/568) → decrease, 568n
    - formatFractionOfMax(6800n) === '68%'
    - bridge renders surprise +68, ADD_LONG_GAMMA, sizeBps 6800, schematic "68%" delta, operator-supplied consensus, testnet-agent pill
    - surprise label only appears alongside the operator caveat
    - no fabricated notional/current price; null → em-dash; honest empty when no ADD decision
  </behavior>
  <verify>
    <automated>pnpm vitest run tests/unit/somnia-bridge.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "somnia-bridge" tsconfig.json` === 0 (un-excluded)
    - `grep -c "test.skip\|describe.skip" tests/unit/somnia-bridge.test.tsx` === 0
    - `grep "decisionToPositionDelta" lib/apps/abrigo/somnia/bridge.ts` matches AND `grep "MAX_SIZE_BPS = 10000n" lib/apps/abrigo/somnia/bridge.ts` matches (BigInt constant)
    - `grep -E "Number\(" lib/apps/abrigo/somnia/bridge.ts` returns NO match inside the division (BigInt-space math; Number only at the format edge if at all)
    - `grep "getHedgeDecisions" components/defi/somnia/HedgeDecisionBridge.tsx && grep "decisionToPositionDelta" components/defi/somnia/HedgeDecisionBridge.tsx` both match
    - `grep "computeSurprise" components/defi/somnia/HedgeDecisionBridge.tsx` matches
    - `grep 'tier="testnet-agent"' components/defi/somnia/HedgeDecisionBridge.tsx` matches; `grep -iE "green|emerald|status-pass" components/defi/somnia/HedgeDecisionBridge.tsx` returns NO match
    - `grep -iE "schematic|esquemátic|illustrative|ilustrativ" components/defi/somnia/HedgeDecisionBridge.tsx` matches (delta labeled illustrative)
    - `grep "—" components/defi/somnia/HedgeDecisionBridge.tsx` matches (em-dash null/empty path)
    - **M4 — no consensus-verified:** `grep -ric "consensus-verified" components/defi/somnia/HedgeDecisionBridge.tsx lib/apps/abrigo/somnia/bridge.ts` === 0
    - **M6 — illustrative, not realized:** `grep -iE "illustrative|ilustrativ" components/defi/somnia/HedgeDecisionBridge.tsx` matches AND the rendered delta copy does NOT use "executed"/"ejecutad"/"realized"/"realizad": `grep -ic "executed|ejecutad|realized|realizad" components/defi/somnia/HedgeDecisionBridge.tsx` === 0
    - **untested-path note (documented, accepted):** the `DecisionFailed` / `actionSet==false` path is not exercised (no failed tx in snapshot); the bridge only selects the ADD_LONG_GAMMA decision. Record this as a known untested branch in the SUMMARY; do NOT fabricate a failed decision.
    - `pnpm vitest run tests/unit/somnia-bridge.test.tsx` exits 0
  </acceptance_criteria>
  <done>decisionToPositionDelta is a pure BigInt mapping (sizeBps→fraction-of-max, action→direction); HedgeDecisionBridge renders the full narrative chain (surprise→ADD_LONG_GAMMA@sizeBps→schematic delta) with operator-supplied consensus, gated surprise, testnet-agent pill, em-dash nulls, honest empty; no fabricated notional; unit stub GREEN + un-excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the bridge in the simulated branch of the instrument page + es-CO-first copy + copy-review row + e2e</name>
  <read_first>
    - app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx (the kind==='simulated' early-return branch and its left column — the EXACT mount point; note it returns BEFORE aggregateAllChains)
    - messages/es-CO/somnia.json + messages/en/somnia.json (06-01/06-02 added the somnia namespace; extend es-CO FIRST with bridge keys, then mirror en)
    - tests/e2e/instrument-page.spec.ts (the existing simulated-instrument e2e — reuse the production-webServer + NEXT_LOCALE cookie pattern + the route /apps/abrigo/instruments/ccop-usd-long-gamma/8453)
    - docs/copy-review.md (the somnia row from 06-01; append/note the bridge keys)
  </read_first>
  <action>
    A. messages/es-CO/somnia.json — add a `bridge` sub-section UNDER the existing top-level `"somnia"` key (es-CO FIRST, authored, no machine translation; do NOT introduce flat keys — extend the nested `somnia` object 06-01 created): bridge heading (e.g. "De la sorpresa macro a la posición"), the chain-step labels (print / consenso (operador) / sorpresa / acción / delta ilustrativo de posición), the operator-supplied caveat (reuse the existing somnia caveat key if present — do NOT duplicate copy), the "ilustrativo/esquemático" delta marker (M6 — frame as illustrative on a SIMULATED position, never "ejecutada"/"realizada"/"consensus-verified"), the "Somnia testnet · decisión de agente (POC)" provenance label + aria, the honest "no se registró decisión de gamma larga" empty-state. Mirror keys into messages/en/somnia.json.
    **M3 — shared serialized edits:** `tsconfig.json` (un-exclude only `somnia-bridge`), `messages/{es-CO,en}/somnia.json` (extend the nested `somnia` object), and `docs/copy-review.md` (append the bridge note) are wave-shared files; 06-04 runs in wave 3 alone so no intra-wave contention, but it must APPEND/extend (never clobber) the 06-01/06-02 edits to these files.

    B. app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx — INSIDE the `if (instrument.kind === 'simulated')` branch ONLY (before the early `return`; the LIVE path below the return is untouched):
       - Add `const tSomnia = await getTranslations('somnia')` (the somnia namespace is registered).
       - Build a `BridgeStrings` object from tSomnia('bridge.*') (+ reuse the operator caveat key).
       - In the left column (`<div className="w-full md:w-3/5 space-y-6">`), add a NEW `<section aria-label={…}>` AFTER the PayoffDiagram section and before/around SnapshotPoolPanel, rendering `<HedgeDecisionBridge instrument={instrument} labels={bridgeStrings} locale={locale} />`. instrument here is already narrowed to SimulatedInstrument by the branch.
       - Do NOT call aggregateAllChains or any multicall in this branch (it already returns early — preserve that). Do NOT add the bridge to the live path.

    C. docs/copy-review.md — note the bridge keys under the existing somnia row (es-CO authored by developer; native Colombian-Spanish reviewer sign-off pending — non-blocking for execution, required before production deploy, per the somnia/instruments precedent).

    D. tests/e2e/somnia-bridge.spec.ts — Playwright e2e against the production webServer (pnpm build && start, per STATE 03-02), deterministic (snapshot-backed; no SOMNIA_LIVE). Navigate to /apps/abrigo/instruments/ccop-usd-long-gamma/8453 (the simulated route). Assert: the bridge heading renders; the ADD_LONG_GAMMA action label + sizeBps 6800 render; the surprise +68 renders; the operator-supplied consensus caveat text is present near the consensus; the schematic/illustrative delta marker (e.g. "68%" + ilustrativo) renders; the testnet-agent pill is present (read its node + aria-label + icon — color+icon+text); set NEXT_LOCALE=en in a second case and assert en copy. Also assert the page still shows the existing SIMULADO badge (the bridge must NOT have replaced the module-1 surface) — proving co-existence.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/somnia-bridge.test.tsx && pnpm exec next build 2>&1 | grep -E "instruments|Compiled|error" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "HedgeDecisionBridge" "app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx"` matches
    - The HedgeDecisionBridge usage appears BEFORE the `aggregateAllChains` call in the file: `awk '/aggregateAllChains\(\)/{a=NR} /HedgeDecisionBridge/{b=NR} END{exit !(b<a)}' "app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx"` exits 0 (bridge is in the early simulated branch, not the live path)
    - `grep "getTranslations('somnia')\|getTranslations(\"somnia\")" "app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx"` matches
    - `grep -iE "bridge|sorpresa|gamma larga|posición" messages/es-CO/somnia.json` matches (bridge copy es-CO) AND the same keys present in messages/en/somnia.json
    - **M4/M6 — honest bridge copy:** `grep -ric "consensus-verified" messages/es-CO/somnia.json messages/en/somnia.json` === 0 AND the bridge copy contains an "ilustrativo"/"illustrative" marker (`grep -iE "ilustrativ|illustrative" messages/es-CO/somnia.json messages/en/somnia.json` matches) and does NOT frame the delta as executed/realized (`grep -ic "ejecutad|realizad|executed|realized" messages/es-CO/somnia.json messages/en/somnia.json` === 0)
    - `grep -i "somnia" docs/copy-review.md` matches (sign-off row covers the bridge keys)
    - `test -f tests/e2e/somnia-bridge.spec.ts` AND `grep -iE "ccop-usd-long-gamma|6800|\\+68|testnet-agent|SIMULADO" tests/e2e/somnia-bridge.spec.ts` matches
    - `git diff "app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx"` shows NO addition inside or after the LIVE path (changes confined to the simulated branch)
  </acceptance_criteria>
  <done>The bridge is mounted in the simulated branch only (before aggregateAllChains, never on the live/multicall path), reading the ADD_LONG_GAMMA decision; bridge copy is es-CO-first in both locales; copy-review row covers it; the e2e asserts the bridge + the surviving SIMULADO badge on /apps/abrigo/instruments/ccop-usd-long-gamma/8453; build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/somnia-bridge.test.tsx` — GREEN
- `pnpm tsc --noEmit` — clean (all 4 downstream Wave-0 RED stubs now un-excluded across 06-01..06-04)
- `pnpm biome check components/defi/somnia/ lib/apps/abrigo/somnia/bridge.ts "app/(defi)/apps/abrigo/instruments/"` — clean
- Production build emits the simulated instrument route with the bridge
- The bridge appears ONLY in the kind==='simulated' branch (before aggregateAllChains) — the live multicall path is untouched (DEFI-08 invariant: a simulated entry is never passed to the multicall)
- LIVE VERIFICATION (CLAUDE.md): after commit, run the Evidence Collector against /apps/abrigo/instruments/ccop-usd-long-gamma/8453 — assert the bridge card (surprise +68 → ADD_LONG_GAMMA @ 6800 → schematic 68% delta), the operator-supplied consensus caveat, the testnet-agent pill (read aria-label + icon node, confirm NEUTRAL non-green token), the illustrative/schematic delta marker, AND the still-present SIMULADO badge (module-1 surface co-exists). Confirm em-dash for any null. Screenshots to /tmp/d2p-verify/06-04-*.png; append verdicts to 06-LIVE-VERIFICATION.md.
</verification>

<success_criteria>
A visitor on the module-1 cCOP/USD simulated instrument page sees a bridge tying a recorded Somnia decision to the convex position: operator-supplied surprise (+68) → ADD_LONG_GAMMA @ sizeBps 6800 → a schematic (illustrative) position-delta on this long-gamma payoff, under a testnet-agent (POC) provenance pill distinct from the module-1 schematic tier. consensus is honestly labeled operator-supplied; surprise is gated; the delta is BigInt-derived from sizeBps with no fabricated notional; nulls are em-dash; the bridge mounts only in the simulated branch (never on the multicall path) and co-exists with the existing SIMULADO surface.
</success_criteria>

<output>
After completion, create `.planning/phases/06-somnia-agent-surface/06-04-SUMMARY.md`.
</output>

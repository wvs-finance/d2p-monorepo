---
phase: 06-somnia-agent-surface
plan: 01
type: execute
wave: 1
depends_on: ["06-00"]
files_modified:
  - components/defi/somnia/MacroDataPanel.tsx
  - app/(defi)/apps/abrigo/agent/page.tsx
  - messages/es-CO/somnia.json
  - messages/en/somnia.json
  - i18n/request.ts
  - tests/unit/somnia-macro-panel.test.tsx
  - tests/e2e/somnia-macro-panel.spec.ts
  - tsconfig.json
  - docs/copy-review.md
autonomous: true
requirements: [SOMNIA-D, CROSS-09, CROSS-10, CROSS-01]
user_setup: []

must_haves:
  truths:
    - "A visitor sees the latest Somnia-testnet CPI print (co/inflation-rate, 5.68%) with a testnet-agent provenance pill and recorded MacroReceived history"
    - "Only CPI is shown; capacity-utilization is NOT rendered (it is not wired and would be fabricated)"
    - "Null / missing macro fields render as em-dash, never 0"
    - "The panel reads through the Wave-0 reader (snapshot default); no fabricated numbers"
  artifacts:
    - path: "components/defi/somnia/MacroDataPanel.tsx"
      provides: "RSC panel rendering latest CPI + MacroReceived history with testnet-agent pills + em-dash nulls"
      min_lines: 40
    - path: "app/(defi)/apps/abrigo/agent/page.tsx"
      provides: "The /apps/abrigo/agent route mounting MacroDataPanel (agent surface IA)"
      contains: "MacroDataPanel"
    - path: "messages/es-CO/somnia.json"
      provides: "es-CO-first authored copy for the somnia namespace"
      contains: "co/inflation-rate"
  key_links:
    - from: "components/defi/somnia/MacroDataPanel.tsx"
      to: "lib/apps/abrigo/somnia/reader.ts"
      via: "getLatestMacroPrint + getMacroHistory"
      pattern: "getLatestMacroPrint|getMacroHistory"
    - from: "components/defi/somnia/MacroDataPanel.tsx"
      to: "components/defi/ProvenanceBadge.tsx"
      via: "ProvenancePill tier=testnet-agent subState=recorded"
      pattern: "tier=\"testnet-agent\"|tier='testnet-agent'"
---

<objective>
Component D — the live macro-data panel: an RSC that renders the latest MacroOracle CPI print (`co/inflation-rate`, Colombia) plus the recorded `MacroReceived` history, each tagged with the `testnet-agent` provenance pill, mounted on a new `/apps/abrigo/agent` route.

Purpose: Surface the honest macro datum that the hedge agent reasons over. CPI is the ONLY wired key — capacity-utilization is deliberately absent (rendering it would be fabrication). Smallest component; no dependency on A/B/C; reads through the Wave-0 reader.

Output: MacroDataPanel RSC, the /apps/abrigo/agent page, es-CO-first + en copy, unit test (flips the Wave-0 RED stub GREEN) + e2e render test.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-06-02-somnia-agent-surface-phase6-design.md
@.planning/phases/06-somnia-agent-surface/06-00-SUMMARY.md

<interfaces>
From lib/apps/abrigo/somnia/reader.ts (Wave 0):
```typescript
export function getLatestMacroPrint(dataKey?: string): MacroPrintView
export function getMacroHistory(dataKey?: string): MacroPrintView[]
export function getSnapshotProvenance(): { tier: 'testnet-agent'; subState: 'recorded'; capturedAt: Date; chainId: 50312 }
// MacroPrintView { dataKey; dataKeyLabel: 'co/inflation-rate'; scaledValue: bigint; observedAt: Date | null; capturedAt: Date; sourceTxHash: string | null }
```
From components/defi/ProvenanceBadge.tsx (Wave 0):
```typescript
export function ProvenancePill({ tier, fieldName, label, ariaLabel, subState }: { tier: ProvenanceTier; fieldName: string; label: string; ariaLabel: string; subState?: 'live'|'recorded' })
```
EXISTING route-group placement (STATE 05-03 decision): /apps/abrigo/instruments lives under (defi) (wallet providers). The agent surface is read-only data (no wallet needed) but lives under (defi) for IA consistency with instruments — place app/(defi)/apps/abrigo/agent/page.tsx. RSC; no 'use client'.
EXISTING formatting: lib/format wrappers (CROSS-07/08 locale-aware Intl). scaledValue is scaled int (568 = 5.68%); format at the edge — divide by scale and render via Intl.NumberFormat percent.
EXISTING i18n: messages/{es-CO,en}/*.json nested objects; getTranslations('somnia') at RSC render; i18n/request.ts registers namespaces.
Anti-fishing null rule (STATE 03-02): null → '—' em-dash; never render 0 for missing data.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Un-skip the RED stub + build MacroDataPanel RSC (CPI-only, em-dash nulls, testnet-agent pill)</name>
  <read_first>
    - tests/unit/somnia-macro-panel.test.tsx (the Wave-0 RED stub to un-skip + implement against)
    - lib/apps/abrigo/somnia/reader.ts + types.ts (getLatestMacroPrint / MacroPrintView)
    - components/defi/ProvenanceBadge.tsx (ProvenancePill tier + subState)
    - components/defi/SnapshotPoolPanel.tsx (an existing RSC panel rendering fixture rows with pills + em-dash — copy the table/row + pill layout idiom)
    - lib/format.ts (locale-aware Intl wrappers for the percent edge-format)
  </read_first>
  <action>
    Remove tests/unit/somnia-macro-panel.test.tsx from the tsconfig.json exclude array and un-skip it. Implement assertions: the panel renders the dataKeyLabel "co/inflation-rate"; renders scaledValue 568 formatted as a percent (5.68%); renders a ProvenancePill with tier="testnet-agent"; renders the MacroReceived history rows; when a MacroPrintView field (e.g. observedAt) is null it renders "—" (em-dash) NOT "0"; the panel does NOT render any capacity-utilization label/row (assert getByText(/capacity/i) throws / queryByText is null).

    Build components/defi/somnia/MacroDataPanel.tsx (RSC, no 'use client'):
    - Props: `{ locale: string; strings: MacroPanelStrings }` (RSC parent passes translated strings + locale, mirroring the InstrumentDetail pattern so the component stays presentational).
    - Calls getLatestMacroPrint() + getMacroHistory() + getSnapshotProvenance() from the reader.
    - Renders ONLY CPI (`co/inflation-rate`). Do NOT add a capacity-utilization row or label anywhere.
    - Latest print: dataKeyLabel + scaledValue formatted as percent at the edge (scaledValue is bigint; convert via `Number(scaledValue) / 100` ONLY at the format edge, then Intl percent) + observedAt date (locale-aware) OR em-dash when null.
    - A ProvenancePill tier="testnet-agent" subState="recorded" with the operator-honest aria-label ("Somnia testnet · agent macro print (POC) · recorded").
    - History: map getMacroHistory() rows at EQUAL visual weight; each null field → em-dash.
    - CROSS-09: every status/provenance indicator carries color+icon+text+aria.
  </action>
  <behavior>
    - Renders "co/inflation-rate" and "5.68%" (or locale percent form)
    - Renders a testnet-agent provenance pill
    - Renders MacroReceived history rows from the reader
    - Null fields render em-dash, never 0
    - No "capacity" text anywhere in the rendered output
  </behavior>
  <verify>
    <automated>pnpm vitest run tests/unit/somnia-macro-panel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "somnia-macro-panel" tsconfig.json` === 0 (un-excluded)
    - `grep -c "test.skip\|describe.skip" tests/unit/somnia-macro-panel.test.tsx` === 0
    - `grep "getLatestMacroPrint\|getMacroHistory" components/defi/somnia/MacroDataPanel.tsx` matches
    - `grep 'tier="testnet-agent"' components/defi/somnia/MacroDataPanel.tsx` matches
    - `grep -i "capacity" components/defi/somnia/MacroDataPanel.tsx` returns NO match (CPI-only honesty invariant)
    - `grep "—" components/defi/somnia/MacroDataPanel.tsx` matches (em-dash null path)
    - `pnpm vitest run tests/unit/somnia-macro-panel.test.tsx` exits 0
  </acceptance_criteria>
  <done>MacroDataPanel renders latest CPI + history with testnet-agent pills + em-dash nulls; capacity-utilization absent; unit stub GREEN + un-excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Mount /apps/abrigo/agent route + es-CO-first somnia i18n + register namespace + copy-review row + e2e render</name>
  <read_first>
    - app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx (RSC page idiom: getLocale + getTranslations + building a strings object + dynamic='force-dynamic' / runtime='nodejs')
    - messages/es-CO/instruments.json + messages/en/instruments.json (nested-object structure + es-CO-first authoring tone to mirror)
    - i18n/request.ts (how namespaces are registered/loaded)
    - tests/e2e/ (an existing simulated-route e2e for the production-webServer + NEXT_LOCALE cookie pattern)
    - docs/copy-review.md (the row format for native-reviewer sign-off)
  </read_first>
  <action>
    A. messages/es-CO/somnia.json — author the `somnia` namespace es-CO FIRST (no machine translation): macro panel heading, dataKey label "co/inflation-rate", "valor más reciente", "histórico", provenance pill label ("Somnia testnet · decisión de agente (POC)") + aria, the operator-supplied caveat copy, em-dash empty-state copy. Then messages/en/somnia.json mirroring keys.
    B. i18n/request.ts — register the `somnia` namespace so getTranslations('somnia') resolves in both locales.
    C. app/(defi)/apps/abrigo/agent/page.tsx — RSC. `dynamic='force-dynamic'`, `runtime='nodejs'`. getLocale + getTranslations('somnia'); build a MacroPanelStrings object from t(...); render `<MacroDataPanel locale={locale} strings={...} />`. h1 + a short honest sub-heading ("Somnia testnet · POC · consensus = operator-supplied"). This route is the agent surface for D now; component A (06-02) will add the decision feed to the SAME page in a later wave (leave a clearly-marked mount slot comment).
    D. docs/copy-review.md — add a row for the somnia namespace (es-CO authored by developer; native Colombian-Spanish reviewer sign-off pending — non-blocking for execution, required before production deploy, per the existing instruments-namespace precedent).
    E. tests/e2e/somnia-macro-panel.spec.ts — Playwright e2e against the production webServer (pnpm build && start, per STATE 03-02): navigate to /apps/abrigo/agent; assert the CPI label and percent value render; assert the testnet-agent pill is present (color+icon+text — read the pill node + its aria-label); assert NO "capacity" text; set NEXT_LOCALE=en cookie in a second case and assert the en copy. Keep it deterministic (snapshot-backed; no SOMNIA_LIVE).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/somnia-macro-panel.test.tsx && pnpm exec next build 2>&1 | grep -E "/apps/abrigo/agent|Compiled|error" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/\(defi\)/apps/abrigo/agent/page.tsx` (route exists)
    - `grep "MacroDataPanel" "app/(defi)/apps/abrigo/agent/page.tsx"` matches
    - `grep "co/inflation-rate" messages/es-CO/somnia.json && grep "co/inflation-rate" messages/en/somnia.json` both match
    - `grep "somnia" i18n/request.ts` matches (namespace registered)
    - `grep -i "somnia" docs/copy-review.md` matches (sign-off row)
    - `grep -i "capacity" "app/(defi)/apps/abrigo/agent/page.tsx" messages/es-CO/somnia.json messages/en/somnia.json` returns NO match
    - `test -f tests/e2e/somnia-macro-panel.spec.ts`
  </acceptance_criteria>
  <done>/apps/abrigo/agent renders MacroDataPanel in both locales; somnia namespace authored es-CO-first + registered; copy-review row added; e2e render spec present; build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/somnia-macro-panel.test.tsx` — GREEN
- `pnpm tsc --noEmit` — clean
- `pnpm biome check components/defi/somnia/ "app/(defi)/apps/abrigo/agent/"` — clean
- Production build emits the /apps/abrigo/agent route
- LIVE VERIFICATION (CLAUDE.md): after commit, run the Evidence Collector against /apps/abrigo/agent — assert the CPI percent tile, the testnet-agent pill (read its aria-label + icon node), the absence of any capacity-utilization row, and em-dash for null fields; screenshots to /tmp/d2p-verify/06-01-*.png; append verdicts to 06-LIVE-VERIFICATION.md.
</verification>

<success_criteria>
A visitor at /apps/abrigo/agent sees the latest Somnia-testnet CPI print (5.68%) + recorded MacroReceived history, each with a testnet-agent provenance pill; capacity-utilization is absent; null fields are em-dash; copy is es-CO-first; the data comes through the Wave-0 reader (snapshot, no network).
</success_criteria>

<output>
After completion, create `.planning/phases/06-somnia-agent-surface/06-01-SUMMARY.md`.
</output>

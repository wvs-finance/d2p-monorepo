---
phase: 06-somnia-agent-surface
plan: 02
type: execute
wave: 1
depends_on: ["06-00"]
files_modified:
  - components/defi/somnia/HedgeDecisionFeed.tsx
  - components/defi/somnia/HedgeDecisionCard.tsx
  - app/(defi)/apps/abrigo/agent/page.tsx
  - messages/es-CO/somnia.json
  - messages/en/somnia.json
  - tests/unit/somnia-decision-feed.test.tsx
  - tests/e2e/somnia-decision-feed.spec.ts
  - tsconfig.json
autonomous: true
requirements: [SOMNIA-A, CROSS-09, CROSS-10, CROSS-01]
user_setup: []

must_haves:
  truths:
    - "A visitor sees the two recorded hedge decisions: macro print → (operator-supplied) consensus → surprise → action + sizeBps"
    - "consensus is explicitly labeled operator-supplied POC input (NOT market consensus); the word surprise is gated behind that caveat"
    - "All four actions (HOLD/ADD_LONG_GAMMA/REDUCE/EXIT) render at EQUAL visual weight (anti-fishing CROSS-09)"
    - "A pending decision (decidedAt==0) renders as pending with em-dash, never 0"
  artifacts:
    - path: "components/defi/somnia/HedgeDecisionCard.tsx"
      provides: "Per-decision card: print → consensus(operator) → surprise → action badge + sizeBps, equal-weight"
      min_lines: 40
    - path: "components/defi/somnia/HedgeDecisionFeed.tsx"
      provides: "Maps reader decisions into equal-weight cards with testnet-agent pills"
      min_lines: 20
  key_links:
    - from: "components/defi/somnia/HedgeDecisionFeed.tsx"
      to: "lib/apps/abrigo/somnia/reader.ts"
      via: "getHedgeDecisions"
      pattern: "getHedgeDecisions"
    - from: "components/defi/somnia/HedgeDecisionCard.tsx"
      to: "lib/apps/abrigo/somnia/surprise.ts"
      via: "computeSurprise (BigInt) formatted at edge"
      pattern: "computeSurprise"
---

<objective>
Component A — the hedge-decision feed: render the recorded `HedgeDecisionMade` decisions as equal-weight cards, each showing macro print → operator-supplied consensus → surprise → action + sizeBps, with a `testnet-agent` provenance pill. The honesty invariant: `consensus` is labeled operator-supplied POC input (not market consensus), and "surprise" is gated behind that caveat.

Purpose: Make the agent's reasoning visible and honest. Equal visual weight across all four actions prevents anti-fishing (a REDUCE/EXIT must look exactly as prominent as ADD_LONG_GAMMA). Independent of B/C/D; reads through the Wave-0 reader; mounts on the same /apps/abrigo/agent route created by 06-01.

Output: HedgeDecisionFeed + HedgeDecisionCard, the feed mounted on the agent page, es-CO-first copy additions, unit test (flips the Wave-0 RED stub GREEN) + e2e equal-weight test.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-06-02-somnia-agent-surface-phase6-design.md
@.planning/phases/06-somnia-agent-surface/06-00-SUMMARY.md

<interfaces>
From lib/apps/abrigo/somnia/reader.ts (Wave 0):
```typescript
export function getHedgeDecisions(dataKey?: string): HedgeDecisionView[]
// HedgeDecisionView { decisionId; action: 'HOLD'|'ADD_LONG_GAMMA'|'REDUCE'|'EXIT'; sizeBps: bigint; macroValue: bigint; consensus: bigint; decidedAt: Date | null; pending: boolean; sourceTxHash: string }
```
From lib/apps/abrigo/somnia/surprise.ts (Wave 0):
```typescript
export function computeSurprise(macroValue: bigint, consensus: bigint): bigint  // macroValue - consensus
export function formatSurprise(s: bigint, scale?: number): string
```
From components/defi/ProvenanceBadge.tsx: ProvenancePill tier="testnet-agent" subState="recorded".
Recorded data the feed must show (snapshot): decision#1 ADD_LONG_GAMMA sizeBps=6800 macro=568 consensus=500 → surprise +68; decision#2 REDUCE sizeBps=568 macro=568 consensus=900 → surprise -332.
EXISTING anti-fishing precedent (STATE 02-06 / CLAUDE.md): FAIL and PASS render at identical visual weight; no <details> collapse; status pills color+icon+text. The action badge must NOT de-emphasize REDUCE/EXIT relative to ADD_LONG_GAMMA — same card dimensions, same typography hierarchy.
EXISTING agent page (06-01): app/(defi)/apps/abrigo/agent/page.tsx has a marked mount slot for the feed. RSC; getTranslations('somnia').
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Un-skip RED stub + build HedgeDecisionCard + HedgeDecisionFeed (equal-weight, consensus=operator, surprise gated)</name>
  <read_first>
    - tests/unit/somnia-decision-feed.test.tsx (Wave-0 RED stub to un-skip + implement against)
    - lib/apps/abrigo/somnia/reader.ts + types.ts (getHedgeDecisions / HedgeDecisionView)
    - lib/apps/abrigo/somnia/surprise.ts (computeSurprise / formatSurprise)
    - components/defi/CashFlowWaterfall.tsx (an existing equal-weight RSC card layout with spec pills + em-dash — copy the row/badge idiom + the equal-weight discipline)
    - tests/e2e/iteration-fx-vol-fail.spec.ts or fail-equal-weight spec (the existing equal-weight assertion pattern: bounding-box / typography parity)
  </read_first>
  <action>
    Remove tests/unit/somnia-decision-feed.test.tsx from tsconfig exclude + un-skip. Implement assertions:
    - getHedgeDecisions() drives the feed → 2 cards.
    - Card#1: action label maps ADD_LONG_GAMMA (assert the human label, e.g. es-CO "Añadir gamma larga"); sizeBps renders "6800" (or formatted bps); macro 568, consensus 500, surprise "+68" (sign preserved); the consensus field carries an operator-supplied caveat (assert getByText matches /operator|operador|no.*mercado|not.*market/i near the consensus value).
    - Card#2: action REDUCE; sizeBps 568; surprise "-332".
    - The word "surprise"/"sorpresa" appears ONLY where the operator-supplied caveat is also present (gating assertion: every node containing the surprise label is within a subtree that also contains the operator caveat).
    - Equal weight: both cards have the same root className (assert className equality between the ADD_LONG_GAMMA card and the REDUCE card — they must not differ by an emphasis class).
    - A constructed pending decision (decidedAt===null / pending===true) renders a "pending"/"pendiente" badge + em-dash for the action/size, NOT 0.

    Build components/defi/somnia/HedgeDecisionCard.tsx (RSC):
    - Props: `{ decision: HedgeDecisionView; labels: DecisionCardStrings; locale: string }`.
    - Layout: macro print → consensus (with the operator-supplied caveat inline/adjacent) → surprise (computeSurprise(decision.macroValue, decision.consensus) formatted at the edge via formatSurprise; preserve sign) → action badge + sizeBps.
    - Action→label map for all 4 actions; the action badge uses color+icon+text (CROSS-09) but the SAME visual weight for every action (no larger/bolder ADD_LONG_GAMMA). Define a single shared badge shell; only icon + text + a neutral/equal-prominence color token vary, never size/weight.
    - pending===true → render "pending" badge + em-dash for action/size/surprise; never 0.
    - testnet-agent ProvenancePill (subState recorded).

    Build components/defi/somnia/HedgeDecisionFeed.tsx (RSC): calls getHedgeDecisions(), maps to HedgeDecisionCard at equal weight, honest empty-state copy if zero decisions.
  </action>
  <behavior>
    - 2 cards from the reader; ADD_LONG_GAMMA surprise +68, REDUCE surprise -332
    - consensus is labeled operator-supplied; surprise only appears alongside that caveat
    - ADD_LONG_GAMMA and REDUCE cards share an identical root className (equal weight)
    - a pending decision renders pending + em-dash, never 0
    - surprise computed in BigInt, formatted at the edge with sign
  </behavior>
  <verify>
    <automated>pnpm vitest run tests/unit/somnia-decision-feed.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "somnia-decision-feed" tsconfig.json` === 0; `grep -c "test.skip\|describe.skip" tests/unit/somnia-decision-feed.test.tsx` === 0
    - `grep "getHedgeDecisions" components/defi/somnia/HedgeDecisionFeed.tsx` matches
    - `grep "computeSurprise" components/defi/somnia/HedgeDecisionCard.tsx` matches
    - `grep -iE "operator|operador" components/defi/somnia/HedgeDecisionCard.tsx messages/es-CO/somnia.json messages/en/somnia.json` matches (consensus caveat encoded)
    - `grep -iE "pending|pendiente" components/defi/somnia/HedgeDecisionCard.tsx` matches AND `grep "—" components/defi/somnia/HedgeDecisionCard.tsx` matches (em-dash pending path)
    - `pnpm vitest run tests/unit/somnia-decision-feed.test.tsx` exits 0
  </acceptance_criteria>
  <done>Feed renders 2 equal-weight decision cards (print→operator-consensus→surprise→action+sizeBps); consensus labeled operator-supplied, surprise gated; pending→em-dash; BigInt surprise; unit stub GREEN + un-excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Mount feed on /apps/abrigo/agent + es-CO-first copy + equal-weight e2e</name>
  <read_first>
    - app/(defi)/apps/abrigo/agent/page.tsx (the mount slot left by 06-01; getTranslations('somnia') strings-object idiom)
    - messages/es-CO/somnia.json + messages/en/somnia.json (extend with decision-feed keys, es-CO first)
    - tests/e2e/somnia-macro-panel.spec.ts (06-01 e2e; reuse the production-webServer + NEXT_LOCALE cookie pattern)
    - the existing fail-equal-weight Playwright spec (bounding-box / computed-style equality assertion idiom)
  </read_first>
  <action>
    A. messages/es-CO/somnia.json — add (es-CO first, authored): the 4 action labels, sizeBps label, consensus label + the operator-supplied caveat sentence, surprise label, pending label, empty-state copy, feed heading. Mirror into messages/en/somnia.json.
    B. app/(defi)/apps/abrigo/agent/page.tsx — at the marked mount slot, build DecisionCardStrings/FeedStrings from t('somnia.*') and render `<HedgeDecisionFeed strings={...} locale={locale} />` BELOW the MacroDataPanel. Update docs/copy-review.md somnia row note if needed (decision-feed keys added).
    C. tests/e2e/somnia-decision-feed.spec.ts — Playwright, production webServer, deterministic (snapshot). Navigate to /apps/abrigo/agent; assert both decision cards render (ADD_LONG_GAMMA + REDUCE labels, +68 / -332 surprise); assert the operator-supplied caveat text is present near consensus; assert EQUAL VISUAL WEIGHT via browser_evaluate-style computed style: the two card roots have equal bounding-box width and equal font-size/weight on the action label (the anti-fishing invariant); en-cookie case asserts the en labels.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/somnia-decision-feed.test.tsx && pnpm exec next build 2>&1 | grep -E "/apps/abrigo/agent|Compiled|error" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "HedgeDecisionFeed" "app/(defi)/apps/abrigo/agent/page.tsx"` matches
    - `grep -iE "operator|operador" messages/es-CO/somnia.json` matches (caveat copy authored es-CO)
    - `grep -iE "ADD_LONG_GAMMA|gamma larga|añadir gamma" messages/es-CO/somnia.json` matches AND a REDUCE label present (all 4 actions keyed)
    - `test -f tests/e2e/somnia-decision-feed.spec.ts` AND `grep -iE "equal|weight|boundingBox|getBoundingClientRect|font" tests/e2e/somnia-decision-feed.spec.ts` matches (equal-weight assertion present)
  </acceptance_criteria>
  <done>Feed mounted below the macro panel on /apps/abrigo/agent; decision-feed copy es-CO-first in both locales; equal-weight e2e present; build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/somnia-decision-feed.test.tsx` — GREEN
- `pnpm tsc --noEmit` — clean
- `pnpm biome check components/defi/somnia/` — clean
- Production build emits /apps/abrigo/agent with the feed
- LIVE VERIFICATION (CLAUDE.md): after commit, Evidence Collector against /apps/abrigo/agent — assert both decision cards, the +68/-332 surprises, the operator-supplied consensus caveat, the testnet-agent pills, AND equal visual weight between the ADD_LONG_GAMMA and REDUCE cards (browser_evaluate bounding-box + computed font equality). Screenshots to /tmp/d2p-verify/06-02-*.png; verdicts to 06-LIVE-VERIFICATION.md.
</verification>

<success_criteria>
A visitor at /apps/abrigo/agent sees the two recorded hedge decisions as equal-weight cards (print → operator-supplied consensus → surprise → action + sizeBps) with testnet-agent pills; consensus is honestly labeled operator-supplied (not market); surprise is gated behind that caveat; pending decisions render em-dash; all four actions render at identical visual weight.
</success_criteria>

<output>
After completion, create `.planning/phases/06-somnia-agent-surface/06-02-SUMMARY.md`.
</output>

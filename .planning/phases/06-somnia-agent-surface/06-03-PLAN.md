---
phase: 06-somnia-agent-surface
plan: 03
type: execute
wave: 1
depends_on: ["06-00"]
files_modified:
  - lib/mcp-tools/get-hedge-decisions.ts
  - lib/mcp-tools/get-latest-macro-print.ts
  - lib/mcp-tools/contract.ts
  - lib/mcp-tools/index.ts
  - app/api/mcp/[transport]/route.ts
  - tests/unit/somnia-mcp-tools.test.ts
  - tests/api/somnia-mcp-conformance.test.ts
  - tsconfig.json
autonomous: true
requirements: [SOMNIA-C, AGENT-01, AGENT-02, CROSS-09]
user_setup: []

must_haves:
  truths:
    - "An AI agent can call get_hedge_decisions(dataKey) and get_latest_macro_print(dataKey) on the Phase-4 MCP server and receive the recorded Somnia decisions / latest CPI"
    - "Each tool returns a SINGLE wrapping ZodObject output (never an array or union) plus BOTH content[text] and structuredContent"
    - "consensus is labeled operator-supplied in the tool output; no fabricated numerics; bigint serialized as string"
  artifacts:
    - path: "lib/mcp-tools/get-hedge-decisions.ts"
      provides: "MCP tool registerGetHedgeDecisions — single ZodObject envelope, dual return, reads via the Wave-0 reader"
      exports: ["registerGetHedgeDecisions"]
    - path: "lib/mcp-tools/get-latest-macro-print.ts"
      provides: "MCP tool registerGetLatestMacroPrint — single ZodObject envelope, dual return"
      exports: ["registerGetLatestMacroPrint"]
    - path: "lib/mcp-tools/contract.ts"
      provides: "New HedgeDecisionsEnvelope + LatestMacroPrintEnvelope ZodObjects (wrapping, never array/union)"
      contains: "HedgeDecisionsEnvelope"
  key_links:
    - from: "lib/mcp-tools/get-hedge-decisions.ts"
      to: "lib/apps/abrigo/somnia/reader.ts"
      via: "getHedgeDecisions"
      pattern: "getHedgeDecisions"
    - from: "app/api/mcp/[transport]/route.ts"
      to: "lib/mcp-tools/get-hedge-decisions.ts"
      via: "registerGetHedgeDecisions(server) in createMcpHandler"
      pattern: "registerGetHedgeDecisions"
---

<objective>
Component C — agent-first MCP tools: add `get_hedge_decisions(dataKey)` and `get_latest_macro_print(dataKey)` to the Phase-4 MCP server, reading through the same Wave-0 reader. Each tool returns a SINGLE wrapping ZodObject (never an array/union — the SDK's normalizeObjectSchema only accepts a ZodObject) plus BOTH `content[text]` and `structuredContent`, with a NEW envelope in contract.ts and its own conformance test.

Purpose: Agents are first-class consumers. These tools mirror the existing get_instrument_terms pattern exactly so the route stays drift-free and the existing conformance harness extends naturally. Independent of A/B/D; shares the reader.

Output: two tool modules, two new envelopes in contract.ts, barrel + route edits, unit test (flips the Wave-0 RED stub GREEN) + a real-SDK conformance test.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-06-02-somnia-agent-surface-phase6-design.md
@.planning/phases/06-somnia-agent-surface/06-00-SUMMARY.md
@lib/mcp-tools/get-instrument-terms.ts
@lib/mcp-tools/contract.ts
@lib/mcp-tools/index.ts
@app/api/mcp/[transport]/route.ts
@tests/api/mcp-real-sdk.test.ts

<interfaces>
EXISTING tool pattern (lib/mcp-tools/get-instrument-terms.ts) — copy EXACTLY:
```typescript
export function registerGetInstrumentTerms(server: McpServer): void {
  server.registerTool('get_instrument_terms', {
    title, description,
    inputSchema: z.object({ ... }),
    outputSchema: NotDeployedEnvelope,   // a SINGLE ZodObject, NEVER a union/array
  }, async (input) => {
    const result = NotDeployedEnvelope.parse({ ... })
    return { content: [{ type: 'text' as const, text: JSON.stringify(result) }], structuredContent: result }  // BOTH
  })
}
```
NEW-BLOCKER (STATE 04-03 + mcp-real-sdk.test.ts): a non-object outputSchema → normalizeObjectSchema undefined → TypeError in validateToolOutput on EVERY call. The envelope MUST be a wrapping ZodObject (e.g. `z.object({ decisions: z.array(...) })`), never `z.array(...)` and never a union at the top level.
EXISTING route (app/api/mcp/[transport]/route.ts): createMcpHandler registers each tool inside the (server)=>{} callback; add the two new register calls. runtime='nodejs' already set (reader may touch process.env.SOMNIA_LIVE — server-only, fine here).
From lib/apps/abrigo/somnia/reader.ts (Wave 0): getHedgeDecisions(dataKey?), getLatestMacroPrint(dataKey?) — bigint fields. Serialize bigint→string at the tool boundary (the wire shape is JSON).
From lib/apps/abrigo/somnia/types.ts: HedgeActionLabel, HedgeDecisionView, MacroPrintView.
EXISTING conformance test (tests/api/mcp-real-sdk.test.ts): real McpServer + Client + InMemoryTransport round-trip asserting structuredContent shape — extend this idiom for the two new tools.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New envelopes in contract.ts + two tool modules (single ZodObject, dual return, bigint→string)</name>
  <read_first>
    - lib/mcp-tools/contract.ts (NotDeployedEnvelope / ResearchEntryOut patterns + the dateToIso helper; add the new envelopes here, never re-declare)
    - lib/mcp-tools/get-instrument-terms.ts (the exact register/parse/dual-return idiom to mirror)
    - lib/apps/abrigo/somnia/reader.ts + types.ts (getHedgeDecisions / getLatestMacroPrint / HedgeDecisionView / MacroPrintView)
    - tests/unit/somnia-mcp-tools.test.ts (the Wave-0 RED stub to un-skip + implement against)
  </read_first>
  <action>
    A. lib/mcp-tools/contract.ts — add TWO new envelopes (single wrapping ZodObjects; NEVER array/union at top level):
       - `HedgeDecisionItem = z.object({ decisionId: z.string(), action: z.enum(['HOLD','ADD_LONG_GAMMA','REDUCE','EXIT']), sizeBps: z.string(), macroValue: z.string(), consensus: z.string(), surprise: z.string(), scale: z.number(), surpriseFormatted: z.string(), decidedAt: z.string().nullable(), pending: z.boolean(), sourceTxHash: z.string(), consensusNote: z.string() })` — `consensusNote` carries the honest "operator-supplied POC input, not market consensus" caveat. `scale` is the decimal scale of the raw ints (e.g. 2 → 568 means 5.68) and `surpriseFormatted` is the human form (e.g. "+0.68") so an agent can interpret surprise MAGNITUDE without re-deriving the scale; `surprise` stays the raw-int string. bigints are STRINGS on the wire.
       - `HedgeDecisionsEnvelope = z.object({ status: z.enum(['recorded','live']), chainId: z.literal(50312), dataKey: z.string(), decisions: z.array(HedgeDecisionItem), note: z.string() })`.
       - `LatestMacroPrintEnvelope = z.object({ status: z.enum(['recorded','live']), chainId: z.literal(50312), dataKey: z.string(), dataKeyLabel: z.literal('co/inflation-rate'), scaledValue: z.string(), scale: z.number(), observedAt: z.string().nullable(), capturedAt: z.string(), note: z.string() })`.
       Export types via z.infer. Reuse dateToIso for date boundary.

    B. lib/mcp-tools/get-hedge-decisions.ts — `registerGetHedgeDecisions(server)`. inputSchema `z.object({ dataKey: z.string().default('co/inflation-rate') })`. outputSchema HedgeDecisionsEnvelope. Handler: call reader.getHedgeDecisions(input.dataKey); map each HedgeDecisionView → HedgeDecisionItem (bigint→String(), surprise = String(computeSurprise(macroValue, consensus)), scale = 2, surpriseFormatted = formatSurprise(computeSurprise(macroValue, consensus), 2) [sign preserved], decidedAt via dateToIso or null, consensusNote = operator-supplied caveat). Return BOTH content[text] (JSON.stringify) AND structuredContent. status:'recorded' (snapshot default). note explains POC/testnet/operator-supplied — and NEVER claims "consensus-verified" (M4).

    C. lib/mcp-tools/get-latest-macro-print.ts — `registerGetLatestMacroPrint(server)`. inputSchema `z.object({ dataKey: z.string().default('co/inflation-rate') })`. outputSchema LatestMacroPrintEnvelope. Handler: reader.getLatestMacroPrint(input.dataKey) → serialize bigint→string, dates→ISO, scale (e.g. 2). Dual return. CPI-only (dataKeyLabel literal); if a non-CPI dataKey is passed, still return the honest envelope for co/inflation-rate's latest (the only wired key) with a note — never fabricate a capacity-utilization print.

    Un-skip tests/unit/somnia-mcp-tools.test.ts + remove from tsconfig exclude. Implement assertions (fake-server style mirroring tests/unit/mcp-tools-onchain-panel.test.ts): both tools' outputSchema is a ZodObject (`.shape` is defined — guards the normalizeObjectSchema blocker); get_hedge_decisions returns { decisions: [2 items] } with action ADD_LONG_GAMMA/REDUCE, sizeBps "6800"/"568" (strings), surprise "68"/"-332" (raw-int string), surpriseFormatted present + sign-preserving (e.g. "+0.68"/"-3.32"), scale === 2, consensusNote present; get_latest_macro_print returns scaledValue "568" + dataKeyLabel "co/inflation-rate"; both handlers return content[text] AND structuredContent.
  </action>
  <behavior>
    - get_hedge_decisions → { decisions: [...] } wrapping object (never bare array)
    - get_latest_macro_print → wrapping object, scaledValue "568", dataKeyLabel "co/inflation-rate"
    - all bigints serialized as strings; consensusNote = operator-supplied caveat
    - both handlers return BOTH content[text] and structuredContent
    - outputSchema.shape is defined for both (ZodObject guard)
  </behavior>
  <verify>
    <automated>pnpm vitest run tests/unit/somnia-mcp-tools.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "HedgeDecisionsEnvelope\|LatestMacroPrintEnvelope" lib/mcp-tools/contract.ts` matches
    - `grep -E "z.array|z.union" lib/mcp-tools/get-hedge-decisions.ts` shows NO top-level outputSchema that is an array/union (the outputSchema reference must be the wrapping Envelope object)
    - `grep "structuredContent" lib/mcp-tools/get-hedge-decisions.ts lib/mcp-tools/get-latest-macro-print.ts` matches in BOTH (dual return)
    - `grep "getHedgeDecisions" lib/mcp-tools/get-hedge-decisions.ts && grep "getLatestMacroPrint" lib/mcp-tools/get-latest-macro-print.ts` match
    - `grep -iE "operator|operador" lib/mcp-tools/get-hedge-decisions.ts` matches (consensusNote caveat)
    - `grep -i "capacity" lib/mcp-tools/get-latest-macro-print.ts` returns NO match
    - **scale/surpriseFormatted present:** `grep -E "scale|surpriseFormatted" lib/mcp-tools/contract.ts` matches AND the get_hedge_decisions handler populates both (`grep -E "scale|surpriseFormatted" lib/mcp-tools/get-hedge-decisions.ts` matches) — an agent can read surprise magnitude without re-deriving the scale
    - **M4 — no consensus-verified:** `grep -ric "consensus-verified" lib/mcp-tools/get-hedge-decisions.ts lib/mcp-tools/get-latest-macro-print.ts lib/mcp-tools/contract.ts` === 0
    - `grep -c "somnia-mcp-tools" tsconfig.json` === 0; `pnpm vitest run tests/unit/somnia-mcp-tools.test.ts` exits 0
  </acceptance_criteria>
  <done>Two new wrapping-ZodObject envelopes in contract.ts; both tools read via the reader, serialize bigint→string, label consensus operator-supplied, return dual content+structuredContent; unit stub GREEN + un-excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Register both tools (barrel + route) + real-SDK conformance test</name>
  <read_first>
    - lib/mcp-tools/index.ts (the barrel — add two exports, one line each)
    - app/api/mcp/[transport]/route.ts (the createMcpHandler register callback — add two register calls)
    - tests/api/mcp-real-sdk.test.ts (the real McpServer+Client+InMemoryTransport round-trip idiom to extend; asserts the single-object outputSchema accepted + structuredContent validates)
  </read_first>
  <action>
    A. lib/mcp-tools/index.ts — add `export { registerGetHedgeDecisions } from './get-hedge-decisions'` and `export { registerGetLatestMacroPrint } from './get-latest-macro-print'`.
    B. app/api/mcp/[transport]/route.ts — import both and call `registerGetHedgeDecisions(server)` + `registerGetLatestMacroPrint(server)` inside the createMcpHandler (server)=>{} callback. Keep disableSse:true + runtime='nodejs' + basePath unchanged.
    C. tests/api/somnia-mcp-conformance.test.ts (@vitest-environment node) — real-SDK round-trip (copy mcp-real-sdk.test.ts structure): spin a McpServer, register both new tools, link via InMemoryTransport, and:
       - client.callTool('get_hedge_decisions', { dataKey: 'co/inflation-rate' }) → result.isError falsy; structuredContent.decisions has length 2; decisions[0].action==='ADD_LONG_GAMMA'; sizeBps==='6800' (string); consensusNote present; content[0].text parses to the same object.
       - client.callTool('get_latest_macro_print', {}) → structuredContent.scaledValue==='568'; dataKeyLabel==='co/inflation-rate'.
       - This exercises the REAL registerTool + validateToolOutput path so a non-object outputSchema would fail LOUDLY (the NEW-BLOCKER guard).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/somnia-mcp-tools.test.ts tests/api/somnia-mcp-conformance.test.ts tests/api/mcp-real-sdk.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "registerGetHedgeDecisions\|registerGetLatestMacroPrint" lib/mcp-tools/index.ts` matches both
    - `grep "registerGetHedgeDecisions(server)\|registerGetLatestMacroPrint(server)" "app/api/mcp/[transport]/route.ts"` matches both
    - `test -f tests/api/somnia-mcp-conformance.test.ts` AND `grep "InMemoryTransport\|callTool" tests/api/somnia-mcp-conformance.test.ts` matches (real-SDK round-trip)
    - `grep "get_hedge_decisions\|get_latest_macro_print" tests/api/somnia-mcp-conformance.test.ts` matches both tool names
    - `pnpm vitest run tests/api/somnia-mcp-conformance.test.ts` exits 0
  </acceptance_criteria>
  <done>Both tools registered in the barrel + route; conformance test round-trips both over a real McpServer+Client asserting wrapping-object structuredContent + content parity; existing mcp-real-sdk test still GREEN.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/somnia-mcp-tools.test.ts tests/api/somnia-mcp-conformance.test.ts` — GREEN
- `pnpm tsc --noEmit` — clean
- `pnpm biome check lib/mcp-tools/ "app/api/mcp/"` — clean
- This plan exposes JSON-RPC endpoints, not a rendered route → live Evidence Collector browser_snapshot is SKIPPED; verify via the in-memory conformance round-trip instead (record in SUMMARY per CLAUDE.md "When to skip" — MCP tools have no rendered surface). Optionally curl the deployed /api/mcp/mcp with a tools/call body to confirm.
</verification>

<success_criteria>
An AI agent calling get_hedge_decisions(dataKey) and get_latest_macro_print(dataKey) on the Phase-4 MCP server receives the recorded Somnia decisions / latest CPI as single wrapping ZodObjects with both content[text] and structuredContent; bigints are strings; consensus is labeled operator-supplied; no fabricated numerics; the conformance test guards the single-object-outputSchema blocker.
</success_criteria>

<output>
After completion, create `.planning/phases/06-somnia-agent-surface/06-03-SUMMARY.md`.
</output>

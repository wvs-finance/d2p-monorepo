---
phase: 11-macrohedgestrategist-hedge-decision-agent
plan: 04
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - contracts/test/instrument/CcopUsdcPool.tree
  - contracts/test/instrument/CcopUsdcPool.t.sol
  - contracts/foundry.toml
  - .github/workflows/contracts-ci.yml
autonomous: true
requirements: [AGENT-04]
must_haves:
  truths:
    - "CcopUsdcPool.tree/.t.sol are renamed to CcopUsdcPool.fork.tree/.fork.t.sol so the *fork* exclusion convention actually catches the forking test (it does vm.createSelectFork) and the no-secret CI job stays green"
    - "The renamed .tree/.t.sol pair still bulloak-checks clean (stem still matches; contract name inside the .t.sol unchanged)"
    - "foundry.toml carries rpc_storage_caching = { chains = [8453], endpoints = \"all\" } so the pinned-block Base fork state caches"
    - "contracts-ci.yml build-and-spec job runs forge build + per-file bulloak check over the parseable trees + a SECRET-FREE forge test that excludes every forking test and exits 0"
    - "The bulloak loop names exactly the three un-parseable spec trees it skips (MacroOracle.tree, SomniaAgentConsumer.sendRequest.tree, SomniaAgentConsumer.sweep.tree) and handles the parseable-but-orphaned handleResponse.tree explicitly (deferred with a tracking note) — NOT a false all-spec-un-parseable claim"
    - "The fork job carries the BASE_RPC_URL secret, caches ~/.foundry/cache/rpc keyed on block 46700000, shards forge test, and retries to survive a cold-cache Alchemy 429"
    - "The Somnia-testnet agent e2e is gated behind workflow_dispatch and NEVER runs on push or pull_request"
    - "bulloak is installed (pinned 0.9.2) in CI before the bulloak check step"
  artifacts:
    - path: "contracts/foundry.toml"
      provides: "rpc_storage_caching config for chain 8453 (Base)"
      contains: "rpc_storage_caching"
    - path: ".github/workflows/contracts-ci.yml"
      provides: "build+spec job (secret-free fork-excluded test proven green), sharded/cached/retried/secret-bearing fork job, workflow_dispatch-gated Somnia e2e job"
      contains: "workflow_dispatch"
  key_links:
    - from: ".github/workflows/contracts-ci.yml"
      to: "Foundry RPC cache (~/.foundry/cache/rpc) keyed on pinned block 46700000"
      via: "actions/cache@v4"
      pattern: "foundry-rpc-base-46700000"
    - from: ".github/workflows/contracts-ci.yml build-and-spec job"
      to: "a secret-free forge run that excludes every forking test"
      via: "forge test --no-match-path 'test/**/*fork*' with the renamed CcopUsdcPool.fork.t.sol now caught"
      pattern: "no-match-path"
    - from: ".github/workflows/contracts-ci.yml"
      to: "Base fork tests"
      via: "secrets.BASE_RPC_URL on the fork job only + forge test --shard"
      pattern: "forge test.*--shard"
---

<objective>
Stand up `.github/workflows/contracts-ci.yml` (modeled on `keeper-ci.yml`) that gates the repo's Foundry suite: a SECRET-FREE `build-and-spec` job (`forge build` + per-file `bulloak check` over the parseable trees + a fork-excluded `forge test` that MUST exit 0 with no RPC secret) and a secret-bearing, RPC-cached, sharded, retried `fork` job. Add the missing `rpc_storage_caching` key to `contracts/foundry.toml` so the pinned-block (46700000) Base fork state caches. The Somnia-testnet agent e2e (which spends STT) is gated behind a manual `workflow_dispatch` and NEVER runs on push/PR.

CRITICAL FIX (BLOCKER 2 — verified live): `contracts/test/instrument/CcopUsdcPool.t.sol` does `vm.createSelectFork(vm.rpcUrl("base"), 46700000)` (line 39) but its filename has NO "fork" substring, so `--no-match-path 'test/**/*fork*'` does NOT exclude it — the real CI job (which has NO `contracts/.env` on disk) fails when `CcopUsdcPool`'s `setUp` forks for want of BASE_RPC_URL. This plan RENAMES the pair to `CcopUsdcPool.fork.tree`/`CcopUsdcPool.fork.t.sol` (coupled — bulloak's stem match requires the `.tree` and `.t.sol` stems to agree), so the `*fork*` convention is honest and the no-secret job goes green. The CI acceptance MUST prove the exclusion with a `.env`-INDEPENDENT check — `! (forge test --list --no-match-path 'test/**/*fork*' | grep -q CcopUsdcPool)`. Do NOT use `env -u BASE_RPC_URL forge test ...` as the proof: forge auto-loads `contracts/.env` from disk regardless of the process env, so that command is a false PASS locally (it exits 0 even without the rename). YAML-validity + grep alone cannot catch this class of bug — the `--list` membership assertion can.

CRITICAL bulloak gate (verified live, 2026-06-02): bulloak 0.9.2 PARSES `test/fork/*.tree` + `test/instrument/*.tree` (incl. the new Phase-11 tree and the renamed CcopUsdcPool.fork.tree) AND `test/spec/SomniaAgentConsumer.handleResponse.tree` (which parses cleanly but is ORPHANED — no matching `.t.sol`). Only THREE spec trees FAIL parsing: `test/spec/MacroOracle.tree` (`invalid identifier: /`), `test/spec/SomniaAgentConsumer.sendRequest.tree` (`invalid identifier: .`), `test/spec/SomniaAgentConsumer.sweep.tree` (`unexpected word 'Egress'`). The CI `bulloak check` loop MUST target the parseable trees by explicit path/glob, name exactly those three skipped trees, and handle `handleResponse.tree` explicitly (deferred/orphaned with a tracking note) — NOT a blanket false "all spec trees un-parseable" claim.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-macrohedgestrategist-hedge-decision-agent/11-CONTEXT.md
@.planning/phases/11-macrohedgestrategist-hedge-decision-agent/11-RESEARCH.md
@.planning/phases/11-macrohedgestrategist-hedge-decision-agent/11-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename CcopUsdcPool tree+test to .fork.* (honest *fork* exclusion) + add rpc_storage_caching</name>
  <files>contracts/test/instrument/CcopUsdcPool.tree, contracts/test/instrument/CcopUsdcPool.t.sol, contracts/foundry.toml</files>
  <read_first>
    - contracts/test/instrument/CcopUsdcPool.t.sol (line 39 `vm.createSelectFork(vm.rpcUrl("base"), BASE_FORK_BLOCK)` — the forking call that NEEDS BASE_RPC_URL; the contract is named `CcopUsdcPoolinitializeAndReadState` derived from the tree root)
    - contracts/test/instrument/CcopUsdcPool.tree (the tree whose stem must match the .t.sol stem; bulloak 0.9.2 infers `<stem>.t.sol` strictly same-dir)
    - contracts/test/instrument/PanopticDataSeam.fork.tree (the EXISTING `.fork.tree` precedent — confirm `bulloak check` is clean on a `.fork`-suffixed stem; the contract name inside is independent of the filename)
    - contracts/foundry.toml (the file being modified — currently NO rpc_storage_caching key; has [profile.default] with solc 0.8.24/cancun, [profile.ci] fuzz.runs=256, [rpc_endpoints] base="${BASE_RPC_URL}", [fuzz] runs=256)
    - .planning/phases/11-macrohedgestrategist-hedge-decision-agent/11-RESEARCH.md (§ Q5 — the rpc_storage_caching key)
  </read_first>
  <action>
    A. RENAME the CcopUsdcPool pair so the `*fork*` exclusion catches it (BLOCKER 2). Use `git mv` to preserve history:
        git mv contracts/test/instrument/CcopUsdcPool.tree   contracts/test/instrument/CcopUsdcPool.fork.tree
        git mv contracts/test/instrument/CcopUsdcPool.t.sol  contracts/test/instrument/CcopUsdcPool.fork.t.sol
    Then update the cross-reference INSIDE the renamed `.t.sol`: the `@dev BTT spec:` comment (currently `test/instrument/CcopUsdcPool.tree`) must point at the renamed `test/instrument/CcopUsdcPool.fork.tree`. The Solidity CONTRACT NAME inside (`CcopUsdcPoolinitializeAndReadState`) is independent of the filename and bulloak derives it from the tree ROOT, not the filename — leave the contract name and the tree root UNCHANGED. The `.tree` stem (`CcopUsdcPool.fork`) now matches the `.t.sol` stem (`CcopUsdcPool.fork`), so bulloak's same-stem inference still holds.
    Verify the renamed pair bulloak-checks clean: `bulloak check test/instrument/CcopUsdcPool.fork.tree` must NOT report "missing its matching Solidity file" and must NOT report a parse error.

    B. ADD rpc_storage_caching to foundry.toml. Inside the existing `[profile.default]` block (after `viaIR = false`, before the `[profile.ci]` block), add EXACTLY:

        rpc_storage_caching = { chains = [8453], endpoints = "all" }

    This caches Base mainnet (chain 8453) forked state. Do NOT change solc, evm_version, optimizer, the rpc_endpoints, or the fuzz settings.
  </action>
  <verify>
    <automated>cd contracts && test -f test/instrument/CcopUsdcPool.fork.tree && test -f test/instrument/CcopUsdcPool.fork.t.sol && ! test -f test/instrument/CcopUsdcPool.tree && ! test -f test/instrument/CcopUsdcPool.t.sol && bulloak check test/instrument/CcopUsdcPool.fork.tree && grep -q 'rpc_storage_caching' foundry.toml && grep -q '8453' foundry.toml && forge build && ! (forge test --list --no-match-path 'test/**/*fork*' | grep -q CcopUsdcPool)</automated>
  </verify>
  <acceptance_criteria>
    - `test/instrument/CcopUsdcPool.fork.tree` and `test/instrument/CcopUsdcPool.fork.t.sol` EXIST; the un-suffixed `CcopUsdcPool.tree`/`CcopUsdcPool.t.sol` are GONE
    - `bulloak check test/instrument/CcopUsdcPool.fork.tree` exits 0 (stem still matches; clean correspondence)
    - The renamed `.t.sol` `@dev BTT spec:` comment references `CcopUsdcPool.fork.tree`
    - PROOF the rename excludes the forking test (`.env`-INDEPENDENT): `forge test --list --no-match-path 'test/**/*fork*'` does NOT list any `CcopUsdcPool` test — i.e. `forge test --list --no-match-path 'test/**/*fork*' | grep -q CcopUsdcPool` returns NON-zero. DO NOT use `env -u BASE_RPC_URL forge test ...` as the proof: forge auto-loads `contracts/.env` from disk, so that command exits 0 even WITHOUT the rename (false PASS). The `--list` membership check is the honest, dotenv-independent gate (it compiles + enumerates matched tests but does not run the fork setUp, so it needs no RPC)
    - `contracts/foundry.toml` contains `rpc_storage_caching` and `chains = [8453]` inside `[profile.default]`; existing keys unchanged (`solc = "0.8.24"`, `evm_version = "cancun"`, `base = "${BASE_RPC_URL}"`)
    - `forge build` exits 0
  </acceptance_criteria>
  <done>
    The forking CcopUsdcPool test is renamed so `*fork*` excludes it (proven `.env`-independently by the `forge test --list` membership check showing no `CcopUsdcPool` test in the no-fork set), its `.tree` is renamed in lockstep and still bulloak-checks clean, and foundry.toml caches chain-8453 forked state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author .github/workflows/contracts-ci.yml (secret-free fork-excluded job proven green + cached/sharded/retried fork job)</name>
  <files>.github/workflows/contracts-ci.yml</files>
  <read_first>
    - .github/workflows/keeper-ci.yml (the model — on: push/PR with path filters, permissions: contents: read, defaults working-directory, no pull_request_target, fork-secret posture)
    - .planning/phases/11-macrohedgestrategist-hedge-decision-agent/11-RESEARCH.md (§ Q5 — the contracts-ci.yml shape, cache key foundry-rpc-base-46700000-v1, shard matrix, bulloak install note)
    - .planning/STATE.md (2026-06-02 07-03 decision: bulloak 0.9.2 parse status of the trees)
    - contracts/test/fork/BaseForkHarness.tree, contracts/test/instrument/PanopticDataSeam.fork.tree, contracts/test/instrument/CcopUsdcPool.fork.tree (renamed in Task 1), contracts/test/instrument/MacroHedgeStrategist.tree, contracts/test/spec/SomniaAgentConsumer.handleResponse.tree (the parseable trees the bulloak loop covers — note handleResponse.tree is parseable-but-orphaned)
  </read_first>
  <action>
    Create `.github/workflows/contracts-ci.yml`. Structure (three jobs):

    Top of file:
        name: contracts-ci
        on:
          pull_request:
            paths: ["contracts/**", ".github/workflows/contracts-ci.yml"]
          push:
            branches: [master]
            paths: ["contracts/**", ".github/workflows/contracts-ci.yml"]
          workflow_dispatch:
        permissions:
          contents: read

    Job `build-and-spec` (fast, NO secret, NO RPC — this job MUST stay green with no BASE_RPC_URL):
    - runs-on ubuntu-latest, defaults working-directory contracts
    - actions/checkout@v4 with submodules: recursive
    - foundry-rs/foundry-toolchain@v1
    - Install bulloak PINNED: `cargo install bulloak --version 0.9.2 --locked` (match local parsing behavior)
    - `forge build`
    - bulloak check loop over ONLY the parseable trees. Use an EXPLICIT list (the instrument + fork globs catch the renamed CcopUsdcPool.fork.tree + the new MacroHedgeStrategist.tree) and ALSO check the one parseable spec tree explicitly, with a comment naming exactly the three un-parseable trees that are intentionally skipped:

          # bulloak 0.9.2 parses test/fork + test/instrument trees and test/spec/SomniaAgentConsumer.handleResponse.tree.
          # The THREE un-parseable spec trees are intentionally SKIPPED (pre-date the stricter 0.9.2 parser):
          #   test/spec/MacroOracle.tree                       (invalid identifier: /)
          #   test/spec/SomniaAgentConsumer.sendRequest.tree   (invalid identifier: .)
          #   test/spec/SomniaAgentConsumer.sweep.tree         (unexpected word 'Egress')
          # test/spec/SomniaAgentConsumer.handleResponse.tree PARSES but is ORPHANED (no matching .t.sol);
          # it is checked here in non-blocking mode and tracked for a future .t.sol (do NOT silently bury it).
          for t in test/fork/*.tree test/instrument/*.tree; do
            echo "bulloak check $t"; bulloak check "$t"; done
          echo "bulloak check (orphaned, non-blocking) test/spec/SomniaAgentConsumer.handleResponse.tree"
          bulloak check test/spec/SomniaAgentConsumer.handleResponse.tree || echo "::warning::handleResponse.tree is parseable-but-orphaned (no .t.sol yet) — tracked, not gating"

      (The `||` makes the orphaned-tree check non-blocking but VISIBLE — it is parseable and must not be silently dropped.)
    - SECRET-FREE fork-excluded test (the BLOCKER-2 proof — this is the job that previously red-failed):
          forge test --no-match-path 'test/**/*fork*'
      With CcopUsdcPool renamed to `.fork.t.sol` (Task 1) this now excludes EVERY forking test and exits 0 with no BASE_RPC_URL. This step is what the build-and-spec job's success actually depends on.

    Job `fork` (secret-bearing, cached, sharded, retried):
    - runs-on ubuntu-latest, defaults working-directory contracts
    - strategy: { fail-fast: false, matrix: { shard: [1, 2] } }
    - env: { BASE_RPC_URL: "${{ secrets.BASE_RPC_URL }}" }  (secret on THIS job only)
    - actions/checkout@v4 submodules: recursive; foundry-rs/foundry-toolchain@v1
    - actions/cache@v4 with path: ~/.foundry/cache/rpc, key: foundry-rpc-base-46700000-v1, restore-keys: foundry-rpc-base-
    - Cache-warm step BEFORE the sharded matrix work to dodge the cold-cache 429 (MINOR b): a single non-sharded warm run `forge test --match-path 'test/**/*fork*' --retries 2 --delay 3 -vv || true` (best-effort warm; `|| true` so a transient cold 429 here does not red the job — the sharded step below is the gate), THEN the gating sharded run:
          forge test --match-path 'test/**/*fork*' --shard ${{ matrix.shard }}/2 --retries 2 --delay 3 -vvv

    Job `somnia-e2e` (NEVER on push/PR — spends STT):
    - `if: github.event_name == 'workflow_dispatch'`
    - runs-on ubuntu-latest, defaults working-directory contracts
    - A clearly-commented placeholder documenting the manual run: it would run `bash script/macro-hedge-strategist-e2e.sh` with SOMNIA_TESTNET_PK + MACRO_ORACLE provided as workflow inputs/secrets. (Do NOT wire real STT-spending into automated CI — the job exists to make the manual trigger discoverable, guarded by the workflow_dispatch `if`.)

    The fork-secret posture: BASE_RPC_URL is unreachable from untrusted fork PRs (GitHub platform guarantee, same as keeper-ci.yml) — acceptable; the fork job gates push:master and same-repo PRs.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia && grep -q 'workflow_dispatch' .github/workflows/contracts-ci.yml && grep -q 'forge build' .github/workflows/contracts-ci.yml && grep -q 'bulloak check' .github/workflows/contracts-ci.yml && grep -q 'foundry-rpc-base-46700000' .github/workflows/contracts-ci.yml && grep -qE "forge test.*--shard" .github/workflows/contracts-ci.yml && grep -q 'no-match-path' .github/workflows/contracts-ci.yml && grep -q 'secrets.BASE_RPC_URL' .github/workflows/contracts-ci.yml && grep -q "github.event_name == 'workflow_dispatch'" .github/workflows/contracts-ci.yml && grep -q 'handleResponse.tree' .github/workflows/contracts-ci.yml && grep -q 'sweep.tree' .github/workflows/contracts-ci.yml && grep -qE 'retries' .github/workflows/contracts-ci.yml && ! grep -q "test/spec/\*.tree" .github/workflows/contracts-ci.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/contracts-ci.yml'))" && cd contracts && ! (forge test --list --no-match-path 'test/**/*fork*' | grep -q CcopUsdcPool)</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/contracts-ci.yml` is valid YAML (`python3 -c "import yaml; yaml.safe_load(...)"` exits 0)
    - Contains `workflow_dispatch` in the `on:` block AND `if: github.event_name == 'workflow_dispatch'` guarding the somnia-e2e job
    - Contains `forge build`, `bulloak check`, and a pinned bulloak install step (`cargo install bulloak --version 0.9.2`)
    - The bulloak loop targets `test/fork/*.tree` and `test/instrument/*.tree`, checks `handleResponse.tree` explicitly (non-blocking), and a comment NAMES the three un-parseable trees (`MacroOracle.tree`, `SomniaAgentConsumer.sendRequest.tree`, `SomniaAgentConsumer.sweep.tree`); it does NOT glob `test/spec/*.tree` (`grep "test/spec/\*.tree"` returns non-zero) and does NOT claim all spec trees are un-parseable
    - build-and-spec runs `forge test --no-match-path 'test/**/*fork*'`; the ACCEPTANCE proves the exclusion `.env`-independently via `! (forge test --list --no-match-path 'test/**/*fork*' | grep -q CcopUsdcPool)` (the BLOCKER-2 proof — depends on Task 1's rename). NOTE: `env -u BASE_RPC_URL forge test ...` is NOT used as the proof — forge auto-loads `contracts/.env`, making it a false PASS locally
    - Fork job has `actions/cache@v4` with key `foundry-rpc-base-46700000-v1` and path `~/.foundry/cache/rpc`
    - Fork job runs `forge test ... --shard ${{ matrix.shard }}/2 --retries 2 --delay 3` (or equivalent retry flags) and has `env: BASE_RPC_URL: ${{ secrets.BASE_RPC_URL }}`; a best-effort cache-warm step precedes the sharded matrix
    - The Somnia e2e job NEVER runs on push/pull_request (only the workflow_dispatch `if`)
    - `permissions: contents: read` present; no `pull_request_target`
  </acceptance_criteria>
  <done>
    A valid contracts-ci.yml whose build-and-spec job is PROVEN green in a secret-free env (the renamed fork test is excluded), whose bulloak loop honestly names the three un-parseable trees and tracks the orphaned handleResponse.tree, and whose fork job gates Base-fork tests with the RPC cache + sharding + retries (cold-cache 429 defense); the STT-spending Somnia e2e stays workflow_dispatch.
  </done>
</task>

</tasks>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/contracts-ci.yml'))"` exits 0.
- `forge test --list --no-match-path 'test/**/*fork*'` lists NO `CcopUsdcPool` test (`.env`-independent proof the renamed CcopUsdcPool.fork.t.sol is excluded — the BLOCKER-2 fix). `env -u BASE_RPC_URL forge test ...` is deliberately NOT used (forge auto-loads `contracts/.env` → false PASS).
- `grep -q 'rpc_storage_caching' contracts/foundry.toml` passes; `forge build` green.
- The bulloak loop excludes only the THREE named un-parseable trees and explicitly handles the parseable-but-orphaned handleResponse.tree.
- The Somnia e2e is `workflow_dispatch`-only (never push/PR); the fork job retries to survive a cold-cache 429.
</verification>

<success_criteria>
- AGENT-04: contracts-ci.yml gates the repo with a SECRET-FREE build + parseable-tree bulloak check (proven green via the renamed fork test) + Base-fork tests (BASE_RPC_URL secret + RPC cache on pinned block + sharding + retries); the Somnia agent e2e stays manual workflow_dispatch.
- foundry.toml carries rpc_storage_caching for chain 8453 — the warm-cache 429 defense.
- The forking CcopUsdcPool test is renamed `.fork.*` so the `*fork*` convention is honest and the no-secret job no longer red-fails.
</success_criteria>

<output>
After completion, create `.planning/phases/11-macrohedgestrategist-hedge-decision-agent/11-04-SUMMARY.md`.
</output>

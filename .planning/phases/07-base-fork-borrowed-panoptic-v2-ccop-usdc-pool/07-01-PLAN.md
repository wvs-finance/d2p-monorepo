---
phase: 07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/foundry.toml
  - contracts/remappings.txt
  - contracts/.env.example
  - contracts/NOTICE
  - contracts/.gitignore
autonomous: true
requirements: [FORK-01]
must_haves:
  truths:
    - "forge build compiles the existing repo under a single ^0.8.24 / cancun profile (no version matrix)"
    - "bulloak is installed and on PATH so .tree specs scaffold and `bulloak check` runs"
    - "The FULL transitive dep set is restorable under lib/ via forge install at the PINNED SHAs from 07-RESEARCH-DEPLOY §C (v4-core, 1inch clones-with-immutable-args, solmate, solady, v3-core, v3-periphery, openzeppelin-contracts) — NO v4-periphery and NO permit2 (B-2: the StateView/IStateView read path is dropped; pool state is read via the already-borrowed V4StateReader + StateLibrary, which resolve through the DEFINED v4-core/ alias)"
    - "remappings.txt mirrors the audit's verbatim remapping lines (07-RESEARCH-DEPLOY §C), with @contracts/@libraries/@types/@tokens/@base repointed at panoptic-borrowed/, and reconciled against `forge remappings`"
    - "A committed NOTICE records the borrowed Panoptic V2 commit SHA + BUSL-1.1 Change Date + non-production scope"
  artifacts:
    - path: "contracts/foundry.toml"
      provides: "single cancun profile + base rpc endpoint + fuzz floor"
      contains: "evm_version = \"cancun\""
    - path: "contracts/remappings.txt"
      provides: "audit-verbatim alias map for borrowed core + full v4-core/v3/cwia/solmate/solady/oz transitive set (NO v4-periphery alias — B-2)"
      contains: "clones-with-immutable-args/=lib/clones-with-immutable-args/src/"
    - path: "contracts/NOTICE"
      provides: "BUSL-1.1 provenance for borrowed Panoptic V2"
      contains: "fe557748210a529ae414d7c487b6514be0d9e220"
    - path: "contracts/.env.example"
      provides: "BASE_RPC_URL template (BASE_FORK_BLOCK pinned in test source, not env)"
      contains: "BASE_RPC_URL"
  key_links:
    - from: "contracts/foundry.toml"
      to: "contracts/remappings.txt"
      via: "remappings discovered from file"
      pattern: "remappings"
    - from: "contracts/foundry.toml"
      to: "${BASE_RPC_URL}"
      via: "[rpc_endpoints] base"
      pattern: "base = \"\\$\\{BASE_RPC_URL\\}\""
---

<objective>
Establish the Phase 7 Foundry toolchain and provenance layer: a single `^0.8.24` / `evm_version="cancun"` compile profile, a `[rpc_endpoints] base` fork endpoint, a CI fuzz floor, the FULL transitive dependency set at the PINNED SHAs the borrowed Panoptic V2 + factory import graph needs (07-RESEARCH-DEPLOY §C — including the previously-omitted 1inch `clones-with-immutable-args`, v3-core, v3-periphery, solady, and OZ; but NOT v4-periphery — see B-2), the audit's verbatim remapping lines (repointed at `panoptic-borrowed/`), the `bulloak` toolchain for the evm-tdd loop, and a BUSL-1.1 `NOTICE`. This is Wave 1 — every later plan compiles and forks against what this plan configures.

Purpose: 07-RESEARCH §5 retired the inherited "multi-version solc matrix" — Panoptic V2 is `^0.8.24` for every file, so one cancun compile satisfies repo + borrowed core + v4-core. 07-RESEARCH-DEPLOY §C SUPERSEDES the earlier "pin a tag at build" hand-wave: the deploy path is CWIA proxies via the factory, so the dep set MUST include the 1inch CWIA fork and the full v3/v4-core/solmate/solady/OZ transitive closure at exact SHAs, and remappings MUST be the audit's verbatim file (so the borrowed import graph resolves byte-identically). B-2 (Blockchain Security Auditor): v4-periphery's `StateView`/`IStateView` import the `@uniswap/v4-core/` alias (NOT in remappings) and pull `permit2` (NOT installed) — so v4-periphery WON'T compile and is NOT installed. All pool-state reads use the already-borrowed `V4StateReader` + `StateLibrary` (which resolve through the DEFINED `v4-core/` alias) instead. This plan encodes the single-profile reality, the pinned dep set, the verbatim remappings, and the legal NOTICE before any code is ported.
Output: Modified `foundry.toml`, new `remappings.txt` (audit-verbatim + repointed aliases, reconciled against `forge remappings`), new `NOTICE`, new `.env.example`, installed deps at pinned SHAs (`lib/v4-core`, `lib/clones-with-immutable-args`, `lib/solmate`, `lib/solady`, `lib/v3-core`, `lib/v3-periphery`, `lib/openzeppelin-contracts` — NO `lib/v4-periphery`), and `bulloak` on PATH.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-RESEARCH-DEPLOY.md
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-RESEARCH.md
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-VALIDATION.md
@contracts/foundry.toml
@contracts/.gitignore

<interfaces>
<!-- 07-RESEARCH-DEPLOY §C — the FULL transitive dep set + EXACT pinned SHAs + the audit's verbatim
     remappings.txt. Lift these VERBATIM; do NOT pin from memory.
     B-2 (Blockchain Security Auditor): v4-periphery is REMOVED — its StateView/IStateView pull the
     undefined @uniswap/v4-core/ alias + permit2 (not installed). Pool state is read via the already-
     borrowed V4StateReader + StateLibrary, both resolving through the DEFINED v4-core/ alias. -->

forge install commands (each at its pinned SHA, run from contracts/):
  forge install Uniswap/v4-core@e50237c43811bd9b526eff40f26772152a42daba
  forge install 1inch/clones-with-immutable-args@196f1ecc6485c1bf2d41677fa01d3df4927ff9ce   # PREVIOUSLY OMITTED — now mandatory (CWIA clone2/clone3/addressOfClone3)
  forge install transmissions11/solmate@eaa7041378f9a6c12f943de08a6c41b31a9870fc
  forge install vectorized/solady@adfad66656a6ef8c65b2a412d849bbf7f7a59842
  forge install Uniswap/v3-core@6562c52e8f75f0c10f9deaf44861847585fc8129
  forge install Uniswap/v3-periphery@b325bb0905d922ae61fcc7df85ee802e8df5e96c
  forge install openzeppelin/openzeppelin-contracts@0a25c1940ca220686588c4af3ec526f725fe2582
  # B-2: NO `forge install Uniswap/v4-periphery` and NO permit2 — the StateView read path is dropped.

remappings.txt (07-RESEARCH-DEPLOY §C — audit verbatim; ONLY the @contracts/@libraries/@base/@tokens/@types
left-hand aliases are repointed at our panoptic-borrowed/ layout. NO v4-periphery/ alias — B-2.
VERIFY each RHS against §C, not memory):
  forge-std/=lib/forge-std/src/
  @openzeppelin/=lib/v4-core/lib/openzeppelin-contracts/      # points INTO v4-core's vendored OZ (verified §C)
  solmate/=lib/solmate/
  clones-with-immutable-args/=lib/clones-with-immutable-args/src/
  univ3-core/=lib/v3-core/contracts
  univ3-periphery/=lib/v3-periphery/contracts
  v4-core/=lib/v4-core/src
  @contracts/=panoptic-borrowed/
  @libraries/=panoptic-borrowed/libraries/
  @base/=panoptic-borrowed/base/
  @tokens/=panoptic-borrowed/tokens/
  @types/=panoptic-borrowed/types/
</interfaces>

<notes>
- `lib/` is gitignored (`# Vendored deps — restore with forge install`). ALL seven forge-install deps go under `lib/` (restorable). The borrowed Panoptic core (Plan 02) goes in the COMMITTED `panoptic-borrowed/` dir, NOT lib/.
- 07-RESEARCH-DEPLOY §C SUPERSEDES 07-RESEARCH's Installation section: the earlier list omitted CWIA, v3-core, v3-periphery, solady, and the explicit OZ pin, and said "pin a tag at build" — all now resolved to exact SHAs. Use §C.
- B-2 (Blockchain Security Auditor BLOCKER): v4-periphery is NOT installed and gets NO remapping. Its `StateView`/`IStateView` import the `@uniswap/v4-core/` alias (which is NOT in §C's remappings) and transitively pull `permit2` (NOT installed) — so adding v4-periphery would break the build. Nothing in §F's borrowed file set imports v4-periphery. Pool-state reads in Plans 03/04 use the already-borrowed `V4StateReader.getSqrtPriceX96(...)` + `v4-core`'s `StateLibrary.getLiquidity(...)`, both of which resolve through the DEFINED `v4-core/=lib/v4-core/src` alias. Do NOT add v4-periphery or permit2.
- The `@openzeppelin/` remapping points at v4-core's VENDORED OZ (`lib/v4-core/lib/openzeppelin-contracts/`), NOT a top-level install — verified §C. The top-level `forge install openzeppelin/openzeppelin-contracts@0a25c194…` is for safety/transitive resolution; the remapping still targets v4-core's copy. If a borrowed file's OZ import fails to resolve, prefer keeping the audit path over repointing. (Note: `@openzeppelin/contracts/interfaces/IERC4626.sol` — needed by Plan 05's seam test — resolves through this same alias into v4-core's bundled OZ; B-1.)
- The audit's `remappings.txt` switches Foundry to file-only remapping mode (auto-detection from `lib/` is dropped once `remappings.txt` exists). After `forge install`, run `forge remappings` and reconcile: ensure no auto-detected entry a borrowed file relies on is silently lost — if `forge remappings` shows an alias the borrowed graph needs that is NOT in our file, add it.
- The roadmap's FORK-01 criterion mentions a "multi-version solc matrix." 07-RESEARCH §5 proved that obsolete (V1 was `=0.8.18`; V2 is `^0.8.24`). This plan ships a SINGLE-version profile; the NOTICE/SUMMARY must state this supersedes the roadmap matrix wording so the checker does not flag a false gap. (This supersession was gate-confirmed correct — KEEP it.)
- `BASE_FORK_BLOCK` mechanism (resolves MAJOR M-4): commit to the SOLIDITY-CONSTANT path. The fork block is a `uint256 constant BASE_FORK_BLOCK` pinned IN the test source (Plan 03), NOT an env var. `.env` carries only `BASE_RPC_URL`. The acceptance greps already target the Solidity constant. `.env.example` documents only `BASE_RPC_URL` plus a comment that the block lives in the test source.
- BUSL NOTICE byte-preservation (resolves MINOR mn-1): when Plan 02 rewrites import paths in borrowed files, the NOTICE/SPDX header lines must stay byte-identical to source — anchor any sed on `^import .*from "` lines ONLY (curly-brace form), never `//` (comment/SPDX) lines. State this in the NOTICE task so the executor carries it forward.
- Existing repo files (`src/MacroOracle.sol` etc.) are all `^0.8.24` — confirmed; do not touch them.
- `cargo` is present at `~/.cargo/bin/cargo`; `bulloak` is ABSENT (`which bulloak` fails). `forge` is `1.5.1-stable`.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the FULL transitive dep set at pinned SHAs (§C, NO v4-periphery — B-2) + bulloak</name>
  <files>contracts/lib/v4-core/, contracts/lib/clones-with-immutable-args/, contracts/lib/solmate/, contracts/lib/solady/, contracts/lib/v3-core/, contracts/lib/v3-periphery/, contracts/lib/openzeppelin-contracts/ (bulloak installed on PATH)</files>
  <read_first>
    - 07-RESEARCH-DEPLOY.md §C "Full dependency set + remappings (resolved SHAs)" — the FULL forge-install command table with EXACT pinned SHAs (THE AUTHORITY; supersedes 07-RESEARCH Installation)
    - 07-RESEARCH-DEPLOY.md §C "Critical facts" (v4-core layout is `src/`; OZ remap points INTO v4-core's vendored OZ; v3-core/periphery + solmate are REQUIRED even for the V4 path; solmate import path is `solmate/src/...`)
    - 07-RESEARCH-DEPLOY.md §A.4 + §F (why CWIA, the factory, and the full closure are now mandatory; confirm §F's borrowed-file list relies on `V4StateReader` + `StateLibrary` — NOT v4-periphery's StateView)
    - contracts/.gitignore (confirm `lib/` is ignored — these deps are restorable, not committed)
  </read_first>
  <action>
    From `contracts/`, install the full transitive set, EACH at the exact pinned SHA from §C (lift verbatim). B-2: do NOT install v4-periphery and do NOT add permit2 — the StateView read path is dropped in favour of the already-borrowed V4StateReader/StateLibrary (resolve through the `v4-core/` alias):
    - `forge install Uniswap/v4-core@e50237c43811bd9b526eff40f26772152a42daba`
    - `forge install 1inch/clones-with-immutable-args@196f1ecc6485c1bf2d41677fa01d3df4927ff9ce` (the 1inch fork — has `clone2`/`clone3`/`addressOfClone3`; PREVIOUSLY OMITTED, now mandatory because PanopticPool/CollateralTracker inherit `Clone` and the factory uses `ClonesWithImmutableArgs`)
    - `forge install transmissions11/solmate@eaa7041378f9a6c12f943de08a6c41b31a9870fc`
    - `forge install vectorized/solady@adfad66656a6ef8c65b2a412d849bbf7f7a59842`
    - `forge install Uniswap/v3-core@6562c52e8f75f0c10f9deaf44861847585fc8129`
    - `forge install Uniswap/v3-periphery@b325bb0905d922ae61fcc7df85ee802e8df5e96c`
    - `forge install openzeppelin/openzeppelin-contracts@0a25c1940ca220686588c4af3ec526f725fe2582`
    Then install bulloak via cargo (cargo is at `~/.cargo/bin/cargo`, on PATH):
    - `cargo install bulloak`
    - Run `bulloak --version` and record the exact version string (it becomes the pinned tooling note in the SUMMARY).
    Record ALL resolved versions/commits (the seven deps + bulloak) in the SUMMARY. (NOTICE in Task 3 needs only the Panoptic SHA, not these.)
  </action>
  <verify>
    <automated>cd contracts && test -d lib/v4-core && test -d lib/clones-with-immutable-args && test -d lib/solmate && test -d lib/solady && test -d lib/v3-core && test -d lib/v3-periphery && test -d lib/openzeppelin-contracts && ! test -d lib/v4-periphery && bulloak --version</automated>
  </verify>
  <acceptance_criteria>
    - `ls` succeeds for all seven: `lib/v4-core lib/clones-with-immutable-args lib/solmate lib/solady lib/v3-core lib/v3-periphery lib/openzeppelin-contracts`
    - `! test -d lib/v4-periphery` succeeds (B-2: v4-periphery NOT installed — its StateView pulls the undefined `@uniswap/v4-core/` alias + permit2)
    - `lib/clones-with-immutable-args/src/` exists with `clone2`/`clone3` machinery (the 1inch fork — sanity-grep `addressOfClone3` somewhere under it)
    - `lib/v4-core/src/` exists (the layout the `v4-core/=lib/v4-core/src` remapping targets) and contains `StateLibrary` (`grep -rl "library StateLibrary" lib/v4-core/src` non-empty) — the read path Plans 03/04 use instead of StateView
    - `bulloak --version` prints a version (exit 0); the version string is recorded in the SUMMARY
    - The resolved SHA of each `forge install` is recorded in the SUMMARY
  </acceptance_criteria>
  <done>All seven pinned deps exist under lib/ at their §C SHAs (incl. the 1inch CWIA fork, v3-core, v3-periphery, solady, OZ); v4-periphery is NOT installed (B-2); `bulloak` is on PATH and prints a version; all resolved versions recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Single-version cancun foundry.toml + audit-verbatim remappings.txt (NO v4-periphery — B-2) + .env.example</name>
  <files>contracts/foundry.toml, contracts/remappings.txt, contracts/.env.example, contracts/.gitignore</files>
  <read_first>
    - contracts/foundry.toml (the current default profile — you are extending it, not dropping the existing keys)
    - 07-RESEARCH.md §"Pattern 1: Single-version cancun compile" (the `[profile.default]` + `[profile.ci]` + `[rpc_endpoints]` + `[fuzz]` block — STILL CORRECT, keep)
    - 07-RESEARCH-DEPLOY.md §C — the AUDIT'S VERBATIM `remappings.txt` (THE AUTHORITY for the RHS of every alias) + the note that the audit's `foundry.toml` uses `optimizer_runs = 9_999_999`, `viaIR = false`
    - 07-RESEARCH-DEPLOY.md §C "Critical facts" (v4-core `src/`; `@openzeppelin/` → INTO v4-core's vendored OZ; `solmate/=lib/solmate/` with `solmate/src/...` import paths; `univ3-core/=lib/v3-core/contracts`)
    - contracts/.env (existing keys SOMNIA_TESTNET_ADDRESS / SOMNIA_TESTNET_PK — .env.example must NOT leak secrets, only key names)
  </read_first>
  <action>
    Rewrite `contracts/foundry.toml` to keep `src`/`out`/`libs` and add the cancun single profile + rpc + fuzz floor:
    ```toml
    [profile.default]
    src = "src"
    out = "out"
    libs = ["lib"]
    solc = "0.8.24"
    evm_version = "cancun"
    optimizer = true
    optimizer_runs = 200
    viaIR = false

    [profile.ci]
    fuzz = { runs = 256 }

    [rpc_endpoints]
    base = "${BASE_RPC_URL}"

    [fuzz]
    runs = 256
    ```
    (If the borrowed core + factory hit the 24KB contract-size limit at `optimizer_runs = 200`, raise to the audit's `optimizer_runs = 9_999_999` per §C and note it in the SUMMARY.)

    Create `contracts/remappings.txt` from the AUDIT'S VERBATIM file (07-RESEARCH-DEPLOY §C), repointing ONLY the `@contracts/@libraries/@base/@tokens/@types` left-hand aliases at our `panoptic-borrowed/` layout. NO `v4-periphery/` alias (B-2 — v4-periphery is not installed; pool state reads go through `V4StateReader` + `StateLibrary` under the `v4-core/` alias). VERIFY each RHS against §C (not memory):
    ```
    forge-std/=lib/forge-std/src/
    @openzeppelin/=lib/v4-core/lib/openzeppelin-contracts/
    solmate/=lib/solmate/
    clones-with-immutable-args/=lib/clones-with-immutable-args/src/
    univ3-core/=lib/v3-core/contracts
    univ3-periphery/=lib/v3-periphery/contracts
    v4-core/=lib/v4-core/src
    @contracts/=panoptic-borrowed/
    @libraries/=panoptic-borrowed/libraries/
    @base/=panoptic-borrowed/base/
    @tokens/=panoptic-borrowed/tokens/
    @types/=panoptic-borrowed/types/
    ```
    Do NOT add a `v4-periphery/` line (B-2).
    THEN run `forge remappings` and reconcile: since `remappings.txt` switches Foundry to file-only mode, confirm no auto-detected alias the borrowed/factory import graph relies on is dropped. If `forge remappings` surfaces a needed alias absent from the file, ADD it (but NOT v4-periphery — if anything appears to need it, that is a signal a StateView import slipped in; reroute it to V4StateReader/StateLibrary instead). Record the reconciliation outcome in the SUMMARY.

    Create `contracts/.env.example` (key names + comments only, NO secret values):
    ```
    # Base mainnet archive RPC for fork tests (Alchemy/dRPC/Dwellir free tier serve archive).
    BASE_RPC_URL=
    # NOTE: the fork block is pinned as a Solidity `uint256 constant BASE_FORK_BLOCK` in the test source
    # (test/fork/BaseForkHarness.t.sol), NOT here. Re-pin there if the RPC cannot serve that block.
    # Existing Somnia testnet keys (do NOT commit real values):
    SOMNIA_TESTNET_ADDRESS=
    SOMNIA_TESTNET_PK=
    ```
    Confirm `.gitignore` ignores `.env` (it ignores `out/ cache/ broadcast/ lib/`; add a `.env` line if absent).
  </action>
  <verify>
    <automated>cd contracts && grep -q 'evm_version = "cancun"' foundry.toml && grep -q 'base = "${BASE_RPC_URL}"' foundry.toml && grep -q 'v4-core/=lib/v4-core/src' remappings.txt && grep -q 'clones-with-immutable-args/=lib/clones-with-immutable-args/src/' remappings.txt && grep -q '@contracts/=panoptic-borrowed/' remappings.txt && ! grep -q 'v4-periphery' remappings.txt && grep -q 'BASE_RPC_URL' .env.example && forge build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'evm_version = "cancun"' contracts/foundry.toml` succeeds
    - `grep -q 'solc = "0.8.24"' contracts/foundry.toml` succeeds
    - `grep -q 'base = "${BASE_RPC_URL}"' contracts/foundry.toml` succeeds
    - `grep -q '\[fuzz\]' contracts/foundry.toml` succeeds (fuzz floor present)
    - `grep -q 'clones-with-immutable-args/=lib/clones-with-immutable-args/src/' contracts/remappings.txt` succeeds (CWIA aliased — previously omitted)
    - `grep -q 'v4-core/=lib/v4-core/src' contracts/remappings.txt` and `grep -q 'univ3-core/=lib/v3-core/contracts' contracts/remappings.txt` and `grep -q '@openzeppelin/=lib/v4-core/lib/openzeppelin-contracts/' contracts/remappings.txt` succeed (audit-verbatim RHS)
    - `! grep -q 'v4-periphery' contracts/remappings.txt` succeeds (B-2: no v4-periphery alias)
    - `grep -q '@contracts/=panoptic-borrowed/' contracts/remappings.txt` succeeds (alias repointed at our layout)
    - `grep -q 'BASE_RPC_URL' contracts/.env.example` succeeds AND `! grep -q 'BASE_FORK_BLOCK *=' contracts/.env.example` (block is NOT an env key — it lives in the test source per M-4)
    - `grep -qE '^\.env' contracts/.gitignore` succeeds (real .env ignored)
    - `forge build` exits 0 (existing repo still compiles under the new cancun single profile; reconciliation against `forge remappings` recorded in SUMMARY)
  </acceptance_criteria>
  <done>foundry.toml carries a single cancun/0.8.24 profile + base rpc endpoint + fuzz floor; remappings.txt is the audit's verbatim file (CWIA + v3 + OZ-into-v4-core + v4-core/src, NO v4-periphery per B-2) with @contracts/@libraries/@base/@tokens/@types repointed at panoptic-borrowed/ and reconciled against `forge remappings`; .env.example documents BASE_RPC_URL only (block pinned in test source per M-4); `forge build` is green.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the BUSL-1.1 NOTICE (with the byte-preservation guard) for the borrowed Panoptic V2 core</name>
  <files>contracts/NOTICE</files>
  <read_first>
    - 07-RESEARCH.md §7 "BUSL NOTICE" (the exact provenance fields: source repo, commit SHA, canonical mirror, License terms, Change Date, scope statement) — STILL CORRECT
    - 07-RESEARCH.md §3.1 "Provenance" (commit `fe557748210a529ae414d7c487b6514be0d9e220`, mirror `panoptic-labs/panoptic-v2-core @ d20b0aed127ab5d3e5ca17c5399782aad2f0ff4c`, Licensor Axicon Labs Limited, Change Date ≤ 2027-09-07, GPL change license, Additional Use Grant `v1-license-grants.panoptic.eth`)
    - CLAUDE.md "Domain non-negotiables" → "v2 milestone is HACKATHON demo/testnet/fork-ONLY → Panoptic BUSL-1.1 borrowing is permitted, keep a NOTICE"
  </read_first>
  <action>
    Create `contracts/NOTICE` recording (verbatim values from 07-RESEARCH §3.1/§7):
    - Borrowed source: `github.com/code-423n4/2025-12-panoptic` @ commit `fe557748210a529ae414d7c487b6514be0d9e220` (Code4rena "Panoptic: Next Core" audit snapshot, frozen 2025-12-23).
    - Canonical mirror: `github.com/panoptic-labs/panoptic-v2-core` @ `d20b0aed127ab5d3e5ca17c5399782aad2f0ff4c` (main).
    - License: `BUSL-1.1`; Licensor: Axicon Labs Limited; Change Date = earlier of `2027-09-07` or `v1-license-date.panoptic.eth`; Change License: GPL v2.0-or-later; Additional Use Grant at `v1-license-grants.panoptic.eth`.
    - Scope statement: borrowed Panoptic V2 files under `contracts/panoptic-borrowed/` are used in NON-PRODUCTION fork/demo only (the permitted BUSL mode); every borrowed file retains its `// SPDX-License-Identifier: BUSL-1.1` header; this repo's wrapper/interface code (`src/instrument/**`) is separately licensed (MIT).
    - BYTE-PRESERVATION GUARD (MINOR mn-1, carried into Plan 02): when Plan 02 rewrites borrowed import paths, NOTICE/SPDX header lines MUST remain byte-identical to source. Any in-place rewrite anchors on `^import .*from "` lines ONLY (the curly-brace form V2 uses) — never `//` (comment/SPDX) lines. State this in the NOTICE so the provenance guard is discoverable.
    - A one-line note: "FORK-01's roadmap wording references a multi-version solc matrix; 07-RESEARCH §5 retired it — Panoptic V2 is `^0.8.24` for every file, so a single cancun compile is used. This is intentional, not a gap."
  </action>
  <verify>
    <automated>cd contracts && grep -q "fe557748210a529ae414d7c487b6514be0d9e220" NOTICE && grep -q "BUSL-1.1" NOTICE && grep -q "2027-09-07" NOTICE && grep -qi "non-production" NOTICE</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "fe557748210a529ae414d7c487b6514be0d9e220" contracts/NOTICE` succeeds (FORK-01 process-gate command, verbatim from 07-VALIDATION)
    - `grep -q "BUSL-1.1" contracts/NOTICE` succeeds (FORK-01 process-gate command, verbatim from 07-VALIDATION)
    - `grep -q "2027-09-07" contracts/NOTICE` succeeds (Change Date present)
    - `grep -qi "non-production" contracts/NOTICE` succeeds (scope statement present)
    - `grep -q "d20b0aed127ab5d3e5ca17c5399782aad2f0ff4c" contracts/NOTICE` succeeds (mirror commit present)
    - `grep -qi "byte-identical" contracts/NOTICE` succeeds (byte-preservation guard recorded for Plan 02)
  </acceptance_criteria>
  <done>NOTICE names the borrowed commit SHA, the canonical mirror commit, BUSL-1.1 + Licensor + Change Date + GPL change license + Additional Use Grant, the non-production fork scope, and the byte-preservation guard for Plan 02's import rewrite; both VALIDATION grep commands pass.</done>
</task>

</tasks>

<verification>
- `forge build` green under the single cancun/0.8.24 profile (existing repo unchanged behaviourally).
- `bulloak --version` prints a version (toolchain operable for downstream `.tree` scaffolding/check).
- `grep -q "fe557748210a529ae414d7c487b6514be0d9e220" contracts/NOTICE && grep -q "BUSL-1.1" contracts/NOTICE` passes (the two FORK-01 NOTICE commands from 07-VALIDATION).
- All seven pinned deps present under lib/ (v4-core, clones-with-immutable-args, solmate, solady, v3-core, v3-periphery, openzeppelin-contracts); CWIA and v3 are the newly-mandatory ones; v4-periphery is NOT present (B-2).
- `remappings.txt` is the audit's verbatim file (per §C) with the five `@`-aliases repointed at `panoptic-borrowed/` and NO v4-periphery alias, reconciled against `forge remappings`.
</verification>

<success_criteria>
- Single-version cancun `foundry.toml` + `[rpc_endpoints] base` + `[fuzz]` floor committed.
- The §C transitive dep set installed at exact SHAs (incl. 1inch CWIA, v3-core, v3-periphery, solady, OZ; NOT v4-periphery — B-2); resolved versions recorded.
- `remappings.txt` is the audit-verbatim map (CWIA + v3 + OZ-into-v4-core + v4-core/src, no v4-periphery) with `@contracts/@libraries/@base/@tokens/@types` repointed at `panoptic-borrowed/`, reconciled against `forge remappings`.
- `.env.example` documents `BASE_RPC_URL` only; `BASE_FORK_BLOCK` is a Solidity constant in the test source (M-4); real `.env` gitignored.
- `NOTICE` carries full BUSL-1.1 provenance + the byte-preservation guard for Plan 02.
- SUMMARY notes the single-compile supersedes the roadmap's multi-version-matrix wording.
</success_criteria>

<output>
After completion, create `.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-01-SUMMARY.md`
</output>

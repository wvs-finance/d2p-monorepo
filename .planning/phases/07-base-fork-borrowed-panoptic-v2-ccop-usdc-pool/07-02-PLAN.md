---
phase: 07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - contracts/panoptic-borrowed/
  - contracts/src/instrument/interfaces/IPanopticData.sol
  - contracts/test/mocks/MockCcop.sol
autonomous: true
requirements: [FORK-03]
must_haves:
  truths:
    - "The minimal borrowed Panoptic V2 core — incl. PanopticFactoryV4 + the CWIA Clone usage + FactoryNFT/MetadataStore + Pointer + V4StateReader + StateLibrary usage — compiles under the cancun profile with BUSL headers byte-intact (07-RESEARCH-DEPLOY §F file set)"
    - "IPanopticData is authored against the REAL V2 ABI verified to EXIST in 07-RESEARCH-DEPLOY §E (dispatch / dispatchFrom payable / getAccumulatedFeesAndPositionsData / getCurrentTick / getTWAP / numberOfLegs), byte-for-byte to §E param types and order"
    - "IPanopticData depends only on V2 value types (TokenId/PositionBalance/LeftRightUnsigned), never on the BUSL concrete contracts"
    - "Compile-time conformance proof = the interface compiles against the borrowed V2 type imports via `forge build` (NOT an `IPanopticData(addr)` cast, which proves nothing at compile time)"
    - "MockCcop is an 18-decimal ERC20 mintable to the test harness"
  artifacts:
    - path: "contracts/src/instrument/interfaces/IPanopticData.sol"
      provides: "the swap-seam interface (V2 ABI subset, all fns confirmed to exist per §E)"
      contains: "function dispatch("
    - path: "contracts/panoptic-borrowed/PanopticPool.sol"
      provides: "borrowed V2 concrete (BUSL-1.1 header byte-intact)"
      contains: "BUSL-1.1"
    - path: "contracts/panoptic-borrowed/PanopticFactoryV4.sol"
      provides: "the CWIA clone+init factory — NOW REQUIRED (the only deploy path; §F)"
      contains: "deployNewPool"
    - path: "contracts/test/mocks/MockCcop.sol"
      provides: "18-dp mock cCOP ERC20"
      contains: "function decimals"
  key_links:
    - from: "contracts/src/instrument/interfaces/IPanopticData.sol"
      to: "panoptic-borrowed/types/TokenId.sol"
      via: "import of V2 value type only"
      pattern: "import.*TokenId"
    - from: "contracts/src/instrument/interfaces/IPanopticData.sol"
      to: "V2 dispatch ABI (§E L572)"
      via: "byte-for-byte function signature match"
      pattern: "function dispatch\\("
    - from: "contracts/panoptic-borrowed/PanopticFactoryV4.sol"
      to: "panoptic-borrowed/PanopticPool.sol + CollateralTracker.sol via clone2/clone3"
      via: "ClonesWithImmutableArgs.clone2/clone3"
      pattern: "clone3"
---

<objective>
Vendor the minimal borrowed Panoptic V2 core — including the factory `PanopticFactoryV4`, the CWIA `Clone` base, the `FactoryNFT`/`MetadataStore` bases the factory inherits, the `Pointer` type, and the `V4StateReader` + v4-core `StateLibrary` readers Plans 03/04 use instead of v4-periphery's StateView — into the committed `panoptic-borrowed/` tree (BUSL headers byte-intact), author the `IPanopticData` swap-seam interface byte-for-byte against the REAL V2 ABI (07-RESEARCH-DEPLOY §E), and add an 18-dp `MockCcop` ERC20. This is the code-provenance + interface-contract layer; the harness (Plan 03), pool (Plan 04), and seam test (Plan 05) all build against these artifacts.

Purpose: 07-RESEARCH-DEPLOY §A established that `PanopticPool`/`CollateralTracker` are ClonesWithImmutableArgs proxies — there is NO `new PanopticPool(...)` path. A working pool comes ONLY from `PanopticFactoryV4.deployNewPool`, so the factory + CWIA + FactoryNFT/MetadataStore/Pointer are NOW REQUIRED in the minimal set (07-RESEARCH §3.2 wrongly excluded them — §F SUPERSEDES it). The interface is authored against the §E-verified V2 ABI (all six functions EXIST verbatim — no "may not exist" hedge). FORK-03's compile-time proof is that the interface COMPILES against the V2 type imports (`forge build`), not an unchecked `IPanopticData(addr)` cast. B-2: because v4-periphery is NOT installed (Plan 01), Plans 03/04 read pool state via the already-borrowed `V4StateReader.getSqrtPriceX96` + v4-core `StateLibrary.getLiquidity` — confirm both are present in this vendored set (V4StateReader is a §F borrowed lib; StateLibrary lives in `lib/v4-core/src` under the `v4-core/` alias, not vendored here).
Output: `contracts/panoptic-borrowed/**` (compiling, BUSL-headed, factory included), `contracts/src/instrument/interfaces/IPanopticData.sol`, `contracts/test/mocks/MockCcop.sol`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-RESEARCH-DEPLOY.md
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-RESEARCH.md
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-VALIDATION.md
@contracts/remappings.txt
@contracts/NOTICE
@contracts/test/mocks/MockPlatform.sol

<interfaces>
<!-- The EXACT IPanopticData surface to author. 07-RESEARCH-DEPLOY §E confirms ALL SIX functions EXIST
     VERBATIM on V2 PanopticPool @fe55774 — there is NO "may not exist" hedge. Match param types + order
     byte-for-byte to §E. Types come from the borrowed V2 types/ dir. -->

From 07-RESEARCH-DEPLOY §E (verified line refs in PanopticPool.sol@fe55774):

    // SPDX-License-Identifier: MIT  (our interface; the concrete keeps BUSL)
    pragma solidity ^0.8.24;
    import {TokenId} from "@types/TokenId.sol";
    import {PositionBalance} from "@types/PositionBalance.sol";
    import {LeftRightUnsigned} from "@types/LeftRight.sol";

    interface IPanopticData {
        // mint / burn / settle (size-delta disambiguated) — EXISTS @L572
        function dispatch(
            TokenId[] calldata positionIdList,
            TokenId[] calldata finalPositionIdList,
            uint128[] calldata positionSizes,
            int24[3][] calldata tickAndSpreadLimits,
            bool usePremiaAsCollateral,
            uint256 builderCode
        ) external;

        // liquidation / forceExercise / settleLongPremium — EXISTS @L1360, IS external payable
        function dispatchFrom(
            TokenId[] calldata positionIdListFrom,
            address account,
            TokenId[] calldata positionIdListTo,
            TokenId[] calldata positionIdListToFinal,
            LeftRightUnsigned usePremiaAsCollateral
        ) external payable;

        // streamia / premium READ — EXISTS @L221
        function getAccumulatedFeesAndPositionsData(
            address user,
            bool includePendingPremium,
            TokenId[] calldata positionIdList
        ) external view returns (LeftRightUnsigned shortPremium, LeftRightUnsigned longPremium, PositionBalance[] memory balances);

        // pool-state reads — ALL EXIST: getCurrentTick @L1949, getTWAP @L1944, numberOfLegs @L1921
        function getCurrentTick() external view returns (int24);
        function getTWAP() external view returns (int24);
        function numberOfLegs(address user) external view returns (uint256);
    }
</interfaces>

<notes>
- 07-RESEARCH-DEPLOY §F SUPERSEDES 07-RESEARCH §3.2: the minimal set NOW INCLUDES `PanopticFactoryV4.sol` (contract name `PanopticFactory`), `base/FactoryNFT.sol`, `base/MetadataStore.sol`, `types/Pointer.sol`, and depends on the 1inch `clones-with-immutable-args` dep (Plan 01 installed it). The old plan wrongly excluded the factory. Empty metadata arrays make FactoryNFT/MetadataStore inert but they MUST compile/link.
- B-2 read-path confirmation: Plans 03/04 read pool state through `V4StateReader.getSqrtPriceX96(IPoolManager(BASE_POOL_MANAGER), poolId)` (a §F borrowed lib — confirm `libraries/V4StateReader.sol` is vendored) and `StateLibrary.getLiquidity(IPoolManager(BASE_POOL_MANAGER), poolId)` (from `v4-core/src/libraries/StateLibrary.sol` under the `v4-core/` alias — NOT vendored, resolved from `lib/v4-core`). v4-periphery's `StateView`/`IStateView` is NOT used (it pulls the undefined `@uniswap/v4-core/` alias + permit2). Do NOT vendor or import StateView.
- §E retires the prior "thin reads may not exist, record divergence" hedge: `getCurrentTick`/`getTWAP`/`numberOfLegs` ALL EXIST verbatim on V2 PanopticPool. KEEP all three; do NOT rename `numberOfLegs` (there is no `numberOfPositions`). Match param types/order to §E byte-for-byte.
- FORK-03 compile-time conformance framing (Solidity B-3): `IPanopticData(addr)` is an UNCHECKED cast — it proves NOTHING at compile time (any address casts). The REAL compile-time proof = the interface compiles against the V2 type imports (`forge build` exit 0). RUNTIME conformance = Plan 05's non-reverting `dispatch` + `getAccumulatedFeesAndPositionsData` calls. The must_haves/acceptance here reflect that: compile-time truth is `forge build`, not a cast.
- Borrowed core lives in the COMMITTED `contracts/panoptic-borrowed/` (NOT lib/, gitignored) so BUSL provenance travels. Plan 01 aliased `@contracts/ @libraries/ @types/ @tokens/ @base/` at this tree and installed the full §C dep set incl. CWIA.
- IMPORT-REWRITE REALITY (mn-A, Blockchain Security Auditor MINOR): V2 source files use the CURLY-BRACE import form `import {Foo} from "path";` — so an `^import "` (bare-string) sed anchor matches ZERO lines. FURTHERMORE the V2 source likely ALREADY uses the `@contracts/`/`@libraries/`/`@types/`/`@tokens/`/`@base/` aliases that §C defines (Plan 01 repointed those same aliases at `panoptic-borrowed/`), meaning the borrowed files' INTERNAL imports need NO rewrite at all. The ONLY imports that may need reconciling are the THIRD-PARTY `lib/` dep prefixes. See the Task-1 action for the exact procedure.
- BYTE-PRESERVATION (mn-1, from Plan 01 NOTICE): when rewriting any borrowed import path, anchor sed on `^import .*from "` lines ONLY (the curly-brace form) — never `//` (comment/SPDX) lines. SPDX/BUSL headers must stay byte-identical to source.
- MockCcop mirrors the existing `test/mocks/MockPlatform.sol` style (plain, in-tree, MIT). 18 decimals (USDC on Base is 6 dp — that decimal gap is handled in Plan 04's sqrtPriceX96, not here).
- The constructors the factory + master copies use are pinned in 07-RESEARCH-DEPLOY §B; this plan only needs them to COMPILE (Plan 05 wires them). Pull whatever transitive files the §F set imports until `forge build` is green.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the minimal borrowed Panoptic V2 core (incl. factory + CWIA bases + V4StateReader) into panoptic-borrowed/</name>
  <files>contracts/panoptic-borrowed/ (PanopticPool.sol, CollateralTracker.sol, SemiFungiblePositionManagerV4.sol, RiskEngine.sol, PanopticFactoryV4.sol, interfaces/, libraries/, types/, tokens/, base/)</files>
  <read_first>
    - 07-RESEARCH-DEPLOY.md §F "Updated minimal panoptic-borrowed/ file set (real paths @fe55774)" — THE AUTHORITY for the file set; lift the table verbatim incl. the factory, FactoryNFT, MetadataStore, Pointer, V4StateReader, and every transitively-pulled file it enumerates
    - 07-RESEARCH-DEPLOY.md §F "Added vs 07-RESEARCH §3.2" + "NOT needed" lists (PanopticFactoryV4/FactoryNFT/MetadataStore/Pointer/CWIA are NOW IN; the UniV3 SFPM + V3 PanopticFactory + DeployProtocol.s.sol stay OUT)
    - 07-RESEARCH-DEPLOY.md §A.1 (the CWIA verdict: Pool/CT inherit `Clone` from `clones-with-immutable-args`; getters are `_getArgAddress` pure)
    - 07-RESEARCH.md §3.1 (provenance: commit `fe557748210a529ae414d7c487b6514be0d9e220`, repo `code-423n4/2025-12-panoptic`)
    - contracts/remappings.txt (the `@contracts/ @libraries/ @types/ @tokens/ @base/` + `clones-with-immutable-args/` + `v4-core/` aliases the borrowed import graph resolves through — note NO v4-periphery alias per B-2)
    - contracts/NOTICE (the provenance + byte-preservation guard this code must match)
  </read_first>
  <action>
    Fetch the minimal Panoptic V2 set from `code-423n4/2025-12-panoptic @ fe557748210a529ae414d7c487b6514be0d9e220` (use `gh api` or a raw clone AT THAT EXACT SHA — do NOT pull `main`) into `contracts/panoptic-borrowed/`. Pull EXACTLY the §F set, preserving every `// SPDX-License-Identifier: BUSL-1.1` (core) / GPL (interfaces/types) header BYTE-FOR-BYTE:
    - Core concretes (cloned/deployed): `PanopticPool.sol`, `CollateralTracker.sol`, `SemiFungiblePositionManagerV4.sol`, `RiskEngine.sol`
    - FACTORY (NOW REQUIRED — the deploy path): `PanopticFactoryV4.sol` (contract name `PanopticFactory`)
    - Interfaces: `interfaces/IRiskEngine.sol`, `interfaces/ISemiFungiblePositionManager.sol`
    - Base (NOW incl. factory bases): `base/Multicall.sol`, `base/FactoryNFT.sol`, `base/MetadataStore.sol`
    - Tokens: `tokens/ERC1155Minimal.sol`, `tokens/ERC20Minimal.sol`, `tokens/interfaces/IERC20Partial.sol`
    - Libraries: `libraries/{Math,PanopticMath,FeesCalc,V4StateReader,Constants,Errors,CallbackLib,EfficientHash,SafeTransferLib,InteractionHelper}.sol` (plus any §F-enumerated transitive lib). `V4StateReader.sol` is the read path Plans 03/04 use (`getSqrtPriceX96`) instead of v4-periphery's StateView (B-2) — confirm it is vendored.
    - Types: `types/{TokenId,LeftRight,LiquidityChunk,PositionBalance,MarketState,OraclePack,PoolData,RiskParameters,Pointer}.sol` (`Pointer.sol` NOW REQUIRED for factory + metadata)
    Do NOT pull: the non-V4 `SemiFungiblePositionManager.sol` (UniV3), the V3 `PanopticFactory.sol`, `script/DeployProtocol.s.sol`, `coreV3/` tests. Do NOT vendor or import v4-periphery's StateView/IStateView (B-2).

    IMPORT-REWRITE PROCEDURE (mn-A — V2 uses the curly-brace import form, and the source likely ALREADY uses the §C aliases):
    1. FIRST inspect what the vendored files actually import:
       `grep -rhE '^import ' contracts/panoptic-borrowed/ | sort -u`
    2. Check whether internal imports ALREADY use the §C aliases (no rewrite needed if so):
       `grep -rE '^import .*from "@(contracts|libraries|types|tokens|base)/' contracts/panoptic-borrowed/`
       — if the borrowed files already use `@contracts/`/`@libraries/`/`@types/`/`@tokens/`/`@base/`, do NOT touch them (Plan 01 already repointed those aliases at `panoptic-borrowed/`). An `^import "` (bare-string) anchor matches NOTHING here — V2 uses `import {Symbol} from "...";`.
    3. ONLY reconcile the THIRD-PARTY dep import prefixes against the §C remapping RHS — i.e. lines importing from `clones-with-immutable-args/`, `v4-core/`, `solmate/`, `solady/`, `univ3-core/`, `univ3-periphery/`, `@openzeppelin/`. If the source uses a different prefix for one of these (e.g. a bare `openzeppelin-contracts/` vs the §C `@openzeppelin/`), rewrite ONLY those lines. Anchor any sed on `^import .*from "` lines (curly-brace form) — NEVER `//`/SPDX lines (mn-1 byte-preservation).
    4. If a kept file imports something not yet pulled, pull that transitive dependency too and note the addition in the SUMMARY.
    Then `forge build`; the iterate-until-compiles loop is the safety net — iterate import paths / add transitive deps until the WHOLE borrowed core + factory compiles. Do NOT bump any `^0.8.24` pragma; do NOT alter BUSL/GPL headers. Record in the SUMMARY: which imports (if any) needed rewriting vs. which already used the §C aliases.
  </action>
  <verify>
    <automated>cd contracts && test -f panoptic-borrowed/PanopticPool.sol && test -f panoptic-borrowed/SemiFungiblePositionManagerV4.sol && test -f panoptic-borrowed/RiskEngine.sol && test -f panoptic-borrowed/PanopticFactoryV4.sol && test -f panoptic-borrowed/base/FactoryNFT.sol && test -f panoptic-borrowed/types/Pointer.sol && test -f panoptic-borrowed/libraries/V4StateReader.sol && grep -q "BUSL-1.1" panoptic-borrowed/PanopticPool.sol && grep -q "deployNewPool" panoptic-borrowed/PanopticFactoryV4.sol && forge build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f contracts/panoptic-borrowed/PanopticPool.sol` and the other three core concretes exist
    - `test -f contracts/panoptic-borrowed/PanopticFactoryV4.sol` succeeds (factory NOW IN the set) and `grep -q "deployNewPool" contracts/panoptic-borrowed/PanopticFactoryV4.sol` succeeds
    - `test -f contracts/panoptic-borrowed/base/FactoryNFT.sol`, `test -f contracts/panoptic-borrowed/base/MetadataStore.sol`, `test -f contracts/panoptic-borrowed/types/Pointer.sol` all succeed (factory bases + Pointer NOW IN)
    - `test -f contracts/panoptic-borrowed/libraries/V4StateReader.sol` succeeds (the B-2 read path Plans 03/04 use, `getSqrtPriceX96`)
    - `grep -q "BUSL-1.1" contracts/panoptic-borrowed/PanopticPool.sol` succeeds (header byte-preserved)
    - `! test -f contracts/panoptic-borrowed/SemiFungiblePositionManager.sol` (UniV3 SFPM NOT vendored) and `! test -f contracts/panoptic-borrowed/PanopticFactory.sol` (V3 factory NOT vendored)
    - `! grep -rq "v4-periphery" contracts/panoptic-borrowed/` (B-2: no v4-periphery / StateView import in the borrowed set)
    - `grep -q "0.8.24" contracts/panoptic-borrowed/PanopticPool.sol` (pragma not bumped)
    - `forge build` exits 0 (the WHOLE borrowed tree incl. factory + CWIA bases + V4StateReader compiles under cancun/0.8.24 alongside the repo + v4-core + v3-core + CWIA)
    - The SUMMARY lists any transitive files added beyond the §F set AND which imports (if any) needed rewriting vs. already used the §C aliases (mn-A)
  </acceptance_criteria>
  <done>The §F minimal Panoptic V2 set — incl. `PanopticFactoryV4`, `base/FactoryNFT`, `base/MetadataStore`, `types/Pointer`, `libraries/V4StateReader`, and the CWIA-dependent core — is vendored under `panoptic-borrowed/`, BUSL/GPL headers byte-intact, imports reconciled per mn-A (internal §C aliases left intact where already present; only third-party dep prefixes rewritten on `^import .*from "` lines), no v4-periphery, and `forge build` compiles the borrowed core + factory + repo + v4-core + v3-core + CWIA in a single cancun pass.</done>
</task>

<task type="auto">
  <name>Task 2: Author IPanopticData byte-for-byte against the §E V2 ABI + add MockCcop</name>
  <files>contracts/src/instrument/interfaces/IPanopticData.sol, contracts/test/mocks/MockCcop.sol</files>
  <read_first>
    - 07-RESEARCH-DEPLOY.md §E "IPanopticData correction" — THE AUTHORITY: all six functions EXIST verbatim with the line refs (dispatch @L572, dispatchFrom @L1360 payable, getAccumulatedFeesAndPositionsData @L221, getCurrentTick @L1949, getTWAP @L1944, numberOfLegs @L1921); match param types + order byte-for-byte; no "may not exist" hedge
    - the `<interfaces>` block in this plan (the exact surface to author)
    - 07-RESEARCH-DEPLOY.md §E "Two refinements" (optional `getOracleTicks` @L1899 may be declared to reserve the Phase-8 oracle shape; `numberOfLegs` is legs not positions — do NOT rename)
    - 07-RESEARCH.md §"Anti-Patterns" + Pitfall 1 (NEVER `mintOptions`/`burnOptions`)
    - contracts/panoptic-borrowed/types/TokenId.sol + types/PositionBalance.sol + types/LeftRight.sol (confirm the exact exported symbol names `TokenId`, `PositionBalance`, `LeftRightUnsigned` so the imports resolve)
    - contracts/test/mocks/MockPlatform.sol (the existing in-tree mock style: MIT header, plain Solidity)
  </read_first>
  <action>
    Create `contracts/src/instrument/interfaces/IPanopticData.sol` with the EXACT surface from the `<interfaces>` block (header `// SPDX-License-Identifier: MIT`, `pragma solidity ^0.8.24;`), matching §E param types + order BYTE-FOR-BYTE. Note `dispatchFrom` IS `external payable` (§E L1360). Imports resolve through the `@types/` alias to the real borrowed type files confirmed in read_first. The interface MUST contain `function dispatch(`, `function dispatchFrom(`, `function getAccumulatedFeesAndPositionsData(`, `function getCurrentTick(`, `function getTWAP(`, `function numberOfLegs(` and MUST NOT contain `mintOptions` or `burnOptions`. Keep all three thin reads (they EXIST per §E — no divergence to record). Optionally add `getOracleTicks` to reserve the Phase-8 shape (note in SUMMARY if added).
    Create `contracts/test/mocks/MockCcop.sol`: a minimal 18-decimal ERC20 named "Mock cCOP" / symbol "cCOP", MIT header, `pragma solidity ^0.8.24;`, `decimals()` returning `18`, and a public `mint(address,uint256)` for the harness to fund itself. Use `solmate/src/tokens/ERC20.sol` (installed) or a minimal hand-rolled ERC20 mirroring MockPlatform — whichever compiles cleanly under the §C remappings.
  </action>
  <verify>
    <automated>cd contracts && test -f src/instrument/interfaces/IPanopticData.sol && grep -q "function dispatch(" src/instrument/interfaces/IPanopticData.sol && grep -q "function dispatchFrom(" src/instrument/interfaces/IPanopticData.sol && grep -q "external payable" src/instrument/interfaces/IPanopticData.sol && grep -q "function numberOfLegs(" src/instrument/interfaces/IPanopticData.sol && ! grep -qE "mintOptions|burnOptions" src/instrument/interfaces/IPanopticData.sol && grep -q "function decimals" test/mocks/MockCcop.sol && forge build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f contracts/src/instrument/interfaces/IPanopticData.sol` succeeds
    - `grep -q "function dispatch(" ...`, `grep -q "function dispatchFrom(" ...`, `grep -q "function getAccumulatedFeesAndPositionsData(" ...`, `grep -q "function getCurrentTick(" ...`, `grep -q "function getTWAP(" ...`, `grep -q "function numberOfLegs(" ...` all succeed (all six §E functions declared)
    - `grep -q "external payable" contracts/src/instrument/interfaces/IPanopticData.sol` succeeds (dispatchFrom is payable per §E)
    - `! grep -qE "mintOptions|burnOptions" contracts/src/instrument/interfaces/IPanopticData.sol` succeeds (V1 ABI absent — Pitfall 1 guard)
    - `grep -qE "import.*TokenId" contracts/src/instrument/interfaces/IPanopticData.sol` succeeds (depends on V2 value type, not concrete)
    - `! grep -qE "import.*(PanopticPool|SemiFungiblePositionManagerV4|RiskEngine|PanopticFactory)" contracts/src/instrument/interfaces/IPanopticData.sol` succeeds (interface never imports the concrete/factory)
    - `grep -q "function decimals" contracts/test/mocks/MockCcop.sol` and a `mint(` function present
    - `forge build` exits 0 — this IS the FORK-03 compile-time conformance proof (the interface compiles against the V2 type imports; NOT an `IPanopticData(addr)` cast)
  </acceptance_criteria>
  <done>`IPanopticData.sol` declares all six §E-verified functions byte-for-byte (dispatch/dispatchFrom-payable/getAccumulatedFeesAndPositionsData/getCurrentTick/getTWAP/numberOfLegs; no V1 mintOptions/burnOptions), importing only V2 value types; `MockCcop` is an 18-dp mintable ERC20; `forge build` green — the interface compiles against the borrowed V2 types (the compile-time conformance proof).</done>
</task>

</tasks>

<verification>
- `forge build` green (borrowed core + FACTORY + CWIA bases + V4StateReader + interface + mock + repo + v4-core + v3-core compile in one cancun pass).
- `test -f contracts/panoptic-borrowed/PanopticFactoryV4.sol` (the deploy-path factory is vendored — §F).
- `test -f contracts/panoptic-borrowed/libraries/V4StateReader.sol` (the B-2 read path Plans 03/04 use instead of StateView).
- `! grep -rq "v4-periphery" contracts/panoptic-borrowed/` (B-2: no StateView/v4-periphery import in the borrowed set).
- `grep -q "BUSL-1.1" contracts/panoptic-borrowed/PanopticPool.sol` (provenance header byte-intact).
- `! grep -qE "mintOptions|burnOptions" contracts/src/instrument/interfaces/IPanopticData.sol` (V2-ABI fidelity; Pitfall 1 retired).
- `! grep -qE "import.*(PanopticPool|SemiFungiblePositionManagerV4|RiskEngine|PanopticFactory)" contracts/src/instrument/interfaces/IPanopticData.sol` (interface depends on value types only).
- FORK-03 compile-time conformance = `forge build` exit 0 (interface compiles against V2 types), NOT an unchecked cast.
</verification>

<success_criteria>
- The §F minimal Panoptic V2 set — incl. PanopticFactoryV4, FactoryNFT, MetadataStore, Pointer, V4StateReader, CWIA-dependent core — vendored under `panoptic-borrowed/`, BUSL/GPL headers byte-intact, compiling; no v4-periphery (B-2).
- `IPanopticData` authored byte-for-byte against the §E-verified V2 ABI (all six functions EXIST; dispatchFrom payable); no V1 surface; imports only V2 value types.
- `MockCcop` is an 18-dp mintable ERC20.
- Any transitive borrowed files beyond §F recorded in the SUMMARY, plus which imports needed rewriting vs. already used the §C aliases (mn-A).
</success_criteria>

<output>
After completion, create `.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-02-SUMMARY.md`
</output>

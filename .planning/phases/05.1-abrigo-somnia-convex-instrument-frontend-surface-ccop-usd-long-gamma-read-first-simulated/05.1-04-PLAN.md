---
phase: 05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - tests/e2e/instrument-page.spec.ts
  - tests/e2e/wallet-states.spec.ts
  - tests/a11y/defi-instruments.spec.ts
  - .planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-LIVE-VERIFICATION.md
autonomous: false
requirements: [DEFI-08, DEFI-03, DEFI-05, DEFI-02, CROSS-01, CROSS-09]
user_setup: []
must_haves:
  truths:
    - "The simulated route /apps/abrigo/instruments/ccop-usd-long-gamma/8453 renders SIMULADO + waterfall + schematic payoff in both locales (e2e)"
    - "The wallet on the simulated route shows READ_ONLY and never CONNECTED_READY (e2e)"
    - "axe WCAG 2.2 AA passes on the simulated route: 1.3.2 reading order, 1.4.1 pills, 1.4.11 non-text contrast (curve stroke)"
    - "Evidence Collector live-DOM verifies SIMULADO above fold, provenance pills present, no fabricated numbers, wallet not READY"
  artifacts:
    - path: ".planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-LIVE-VERIFICATION.md"
      provides: "Evidence Collector verdicts + screenshot paths"
      contains: "SIMULADO"
  key_links:
    - from: "tests/e2e/instrument-page.spec.ts"
      to: "app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx"
      via: "navigates to the real simulated route (registry now non-empty)"
      pattern: "ccop-usd-long-gamma/8453"
---

<objective>
Turn the deferred WAIVER-05-04 e2e/a11y specs into real integration tests now that the registry has a navigable simulated entry, then run the full suite green and complete the mandatory Evidence Collector live-DOM verification (CLAUDE.md). This is the phase gate.

Purpose: tsc + vitest are necessary but not sufficient — the live DOM is the ground truth (the Phase-2 regression rationale). The slice does not ship until the Evidence Collector confirms SIMULADO above the fold, provenance pills, no fabricated numbers, and a wallet that never reads ready.
Output: filled e2e/a11y specs + a green full suite + the 05.1-LIVE-VERIFICATION.md report.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-RESEARCH.md
@.planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Navigable route now: /apps/abrigo/instruments/ccop-usd-long-gamma/8453 (chainId 8453 = Base). -->
<!-- Existing specs are WAIVER-05-04 placeholders that only assert 404 boundaries / index loads.
     They now get real assertions against the simulated route. -->
<!-- Default locale es-CO; set NEXT_LOCALE=en cookie to assert en copy (Phase-2 decision). -->
<!-- e2e runs against the production webpack build: pnpm build && pnpm start -p 3040 (playwright webServer). -->
<!-- SIMULADO badge: aria-label "Base fork — pool simulado, sin despliegue en cadena"; text "SIMULADO". -->
<!-- READ_ONLY pill label es-CO "Solo lectura"; CONNECTED_READY label "Conectado" must NOT appear. -->
<!-- lighthouserc.cjs EXISTS at repo root — the LCP gate is real (lhci autorun consumes it). -->
<!-- Waves 0-3 are ALREADY committed per-task (CLAUDE.md per-task commit discipline). This wave commits ONLY its own files. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill instrument-page + wallet-states e2e + defi a11y specs for the simulated route</name>
  <read_first>
    - tests/e2e/instrument-page.spec.ts (the file being modified — WAIVER-05-04 placeholder)
    - tests/e2e/wallet-states.spec.ts (the file being modified — WAIVER-05-04 placeholder)
    - tests/a11y/defi-instruments.spec.ts (the file being modified)
    - app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx (the rendered simulated surface from Wave 3)
    - playwright.config.ts (production webServer; chromium project)
  </read_first>
  <action>
    KEEP the existing 404-boundary tests (does-not-exist/42220 → 404; some-id/99999 → 404). ADD real assertions against `/apps/abrigo/instruments/ccop-usd-long-gamma/8453`:

    instrument-page.spec.ts:
    - SIMULADO above the fold at 360px: set viewport 360px wide, navigate, assert `scrollY === 0` and the element with text "SIMULADO" is visible without scrolling (DEFI-05). Assert the RiskCallout heading is also visible above the fold.
    - Schematic payoff: assert a `role="img"` element with an aria-label containing "esquemático" (es-CO) / "schematic" (en, with NEXT_LOCALE=en cookie) is present, and `.recharts-curve` (or the recharts surface) paints with non-zero height (the 05-04 BLOCKER must not regress).
    - SnapshotPoolPanel: assert the seededLiquidity value `1000000000000000000000000` appears as text and a "Fork fixture" provenance pill is present.
    - CashFlowWaterfall: assert the data-cost row renders an em-dash `—` (never 0) for the Phase-9 unbuilt value.
    - No fabricated numbers: assert no `strike`/`slope` numeric appears in the rendered params for the simulated route (those keys are live-only).

    wallet-states.spec.ts:
    - Navigate to the simulated route (disconnected). Assert the WalletPanel shows the READ_ONLY pill (text "Solo lectura" es-CO) and the "sin transacción — fork simulado" copy. Assert "Conectado" (CONNECTED_READY label) is NOT present, and there is NO switch-network button. Assert NO "Conectar billetera" ConnectButton in the read-only panel.

    defi-instruments.spec.ts (a11y):
    - Add an axe run against `/apps/abrigo/instruments/ccop-usd-long-gamma/8453` asserting zero serious/critical violations, specifically covering color-contrast (curve stroke 1.4.11 non-text) and the provenance pills. (The known RainbowKit "Conectar billetera" 3.44:1 issue is NOT on this read-only route since no ConnectButton renders — so the route should be clean.)
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm test:e2e tests/e2e/instrument-page.spec.ts tests/e2e/wallet-states.spec.ts &amp;&amp; pnpm test:a11y tests/a11y/defi-instruments.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ccop-usd-long-gamma/8453" tests/e2e/instrument-page.spec.ts tests/e2e/wallet-states.spec.ts` matches both
    - `grep -n "SIMULADO\|scrollY" tests/e2e/instrument-page.spec.ts` matches (above-fold assertion)
    - `grep -n "Solo lectura\|read_only\|READ_ONLY" tests/e2e/wallet-states.spec.ts` matches; an assertion that "Conectado" is NOT present
    - `pnpm test:e2e tests/e2e/instrument-page.spec.ts tests/e2e/wallet-states.spec.ts` passes against the production build
    - `pnpm test:a11y tests/a11y/defi-instruments.spec.ts` passes (zero serious/critical)
  </acceptance_criteria>
  <done>The e2e/a11y specs assert the real simulated route: SIMULADO above fold, schematic payoff paints, fork-fixture pills, em-dash data-cost, READ_ONLY wallet never CONNECTED_READY; axe clean; all green on the production build.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite green + commit (Wave 4 files only)</name>
  <read_first>
    - package.json (test:all / test:quick script definitions)
    - CLAUDE.md (pre-commit biome+tsc, no --no-verify; per-task commit discipline)
    - lighthouserc.cjs (the LCP gate config consumed by lhci autorun)
  </read_first>
  <action>
    Run the full suite and resolve any remaining failures: `pnpm test:quick` (biome + tsc + vitest) then `pnpm playwright test` then `pnpm lhci autorun` (Lighthouse LCP gate on the new route, per the EXISTING lighthouserc.cjs at repo root — if LCP &gt; 2.5s, record as a WARN per the project lighthouse policy, do not silently pass a hard fail).

    Then commit ONLY this wave's files with a conventional `feat(05.1-04): ...` message via the GSD commit tool (pre-commit hooks must pass; never use --no-verify). Per CLAUDE.md, Waves 0-3 are already committed per-task — do NOT bundle them. Commit exactly the Wave 4 artifacts:
    `node "/home/jmsbpp/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(05.1-04): real e2e/a11y specs for the simulated route + live-verification report" --files tests/e2e/instrument-page.spec.ts tests/e2e/wallet-states.spec.ts tests/a11y/defi-instruments.spec.ts .planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-LIVE-VERIFICATION.md`
    (The LIVE-VERIFICATION.md content is appended in Task 3; if committing before Task 3 completes, commit the specs first and amend/second-commit the report — but prefer committing the specs here and letting Task 3's report be a follow-up commit. Do NOT `git add -A` / do NOT sweep prior-wave files.)
  </action>
  <verify>
    <automated>pnpm test:quick &amp;&amp; pnpm playwright test --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:quick` (biome + tsc + vitest) exits 0
    - `pnpm playwright test --project=chromium` exits 0
    - `git log --oneline -1` shows a `feat(05.1-04)` commit; pre-commit hooks passed (no --no-verify in the commit)
    - `git show --stat HEAD` lists ONLY Wave 4 files (the e2e/a11y specs + 05.1-LIVE-VERIFICATION.md) — NO prior-wave files swept in
    - Lighthouse LCP result recorded (PASS or explicit WARN with the measured value) against lighthouserc.cjs
  </acceptance_criteria>
  <done>Full suite green (biome+tsc+vitest+playwright); Lighthouse recorded; ONLY the Wave 4 files committed through the pre-commit gate (prior waves already committed per-task).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Evidence Collector — live-DOM verification of the simulated surface (both locales)</name>
  <action>Launch the Evidence Collector agent (Playwright MCP) per the CLAUDE.md recipe against the simulated route in both locales; record verdicts + screenshots in 05.1-LIVE-VERIFICATION.md. See &lt;how-to-verify&gt; for the exact steps.</action>
  <what-built>The complete read-first simulated cCOP/USD long-gamma surface at /apps/abrigo/instruments/ccop-usd-long-gamma/8453: SIMULADO badge, three-tier provenance pills, schematic convex payoff, backend-correct CashFlowWaterfall, SnapshotPoolPanel, read-only wallet. Per the CLAUDE.md mandate, live-DOM ground-truth verification is required after the commit.</what-built>
  <how-to-verify>
    Launch the Evidence Collector agent (Playwright MCP) per the CLAUDE.md recipe against the production build (`pnpm build &amp;&amp; pnpm start -p 3040`, fall back to https://www.d2pfinance.xyz if the route has propagated). Target route: `/apps/abrigo/instruments/ccop-usd-long-gamma/8453`. For es-CO and en (NEXT_LOCALE cookie):
    1. browser_navigate at 360px; browser_snapshot — assert the SIMULADO badge is ABOVE THE FOLD (scrollY===0) next to RiskCallout (DEFI-05).
    2. Assert all three provenance tiers render as pills with color + icon + text + aria-label (read the aria-label nodes); fork-fixture on pool fields, spec on cashflow, schematic on the payoff (CROSS-09 / 1.4.1).
    3. Assert the schematic payoff curve paints (`.recharts-curve` non-zero height) with the --accent-text stroke; no React #418 in browser_console_messages.
    4. Assert NO fabricated numbers: the data-cost row is an em-dash (not 0); JSON-LD has no strike/slope/address; params show no strike/slope for the simulated entry.
    5. Assert the wallet shows READ_ONLY ("Solo lectura") and never CONNECTED_READY ("Conectado"); no switch-network CTA; no ConnectButton.
    6. browser_take_screenshot to /tmp/d2p-verify/05.1-*.png; append verdicts (✓/⚠/✗/⊘) + screenshot paths to 05.1-LIVE-VERIFICATION.md.
    A task is not complete until every claim is ✓ or has an explicit recorded waiver.
  </how-to-verify>
  <acceptance_criteria>
    - 05.1-LIVE-VERIFICATION.md exists with per-claim verdicts and screenshot paths under /tmp/d2p-verify/
    - SIMULADO-above-fold, provenance-pills, no-fabricated-numbers, and wallet-not-READY claims are each ✓ (or carry an explicit waiver)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the Evidence Collector records all four honesty claims as ✓ in 05.1-LIVE-VERIFICATION.md, or describe the ✗/⚠ findings to fix.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test:all` (biome + tsc + vitest + playwright + lighthouse) green or with recorded waivers
- e2e asserts SIMULADO above fold, schematic payoff paints, READ_ONLY wallet on the real route in both locales
- a11y axe clean on the simulated route (1.3.2 / 1.4.1 / 1.4.11)
- 05.1-LIVE-VERIFICATION.md records all four honesty claims ✓
- the Wave-4 commit contains ONLY Wave-4 files (prior waves committed per-task)
</verification>

<success_criteria>
The full suite is green; the e2e/a11y specs assert the real simulated route; the Evidence Collector confirms via live DOM that the surface is honest (SIMULADO above fold, provenance pills, no fabricated numbers, wallet never ready) in both locales. The phase is shippable.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-04-SUMMARY.md`
</output>
</content>
</invoke>

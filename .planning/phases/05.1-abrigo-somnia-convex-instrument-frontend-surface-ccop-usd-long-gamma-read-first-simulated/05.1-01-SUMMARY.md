---
phase: 05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated
plan: "01"
subsystem: defi-data-layer
tags: [fixture, cashflow, instruments-union, consumer-narrowing, anti-fishing, wave-1]
dependency_graph:
  requires: ["05.1-00"]
  provides: ["05.1-02", "05.1-03", "05.1-04"]
  affects: ["lib/apps/abrigo/*", "lib/dashboard/aggregator.ts", "components/defi/Instrument*", "app/(defi)/apps/abrigo/instruments/*"]
tech_stack:
  added: []
  patterns:
    - "discriminated union on kind ('live'|'simulated') — TypeScript type predicate in filter"
    - "FixtureValue<T> wrapper with tier/source/note — provenance tagging at the value level"
    - "bigint-as-string encoding for on-chain integers (seededLiquidity, tickSpacing, etc.)"
    - "2-param residual contract: computeResidual(surviving, dataCost) — streamia/commission never subtracted"
    - "schematic parabolic payoff independent of generatePayoffData live-path"
key_files:
  created:
    - lib/apps/abrigo/cashflow.ts
  modified:
    - lib/apps/abrigo/fixture.ts
    - lib/apps/abrigo/payoff.ts
    - lib/apps/abrigo/instruments.ts
    - lib/dashboard/aggregator.ts
    - components/defi/InstrumentParams.tsx
    - components/defi/InstrumentJsonLd.tsx
    - app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx
    - app/(defi)/apps/abrigo/instruments/page.tsx
    - tests/unit/instruments-index.test.ts
    - tests/unit/payoff-curve.test.tsx
    - tsconfig.json
decisions:
  - "Type predicate (i): i is LiveInstrument used in aggregator filter — plain callback does not narrow; predicate required for tsc"
  - "Tasks 2+3 committed atomically (pre-commit tsc gate rejects mid-state where union exists but consumers unnarrowed)"
  - "generateSchematicConvexPayoff is fully independent — does NOT call generatePayoffData"
  - "fixture-celo-01 removed from instruments.ts data AND trailing comment (acceptance criteria: grep returns 0 matches)"
metrics:
  duration_minutes: 6
  completed_date: "2026-06-02"
  tasks_completed: 3
  files_modified: 11
---

# Phase 05.1 Plan 01: Data Layer Freeze (Wave 1) Summary

Wave-1 data-layer freeze: cashflow.ts (2-param residual), schematic convex payoff generator, AbrigoInstrument discriminated union, and all five consumer files narrowed — 55 tests green, typecheck clean, Wave-0 RED stubs flipped to GREEN.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | cashflow.ts + unlock Wave-0 stubs | b5a49d2 | cashflow.ts, tsconfig.json |
| 2+3 | discriminated union + all 5 consumers | e4fcaca | payoff.ts, instruments.ts, fixture.ts, aggregator.ts, InstrumentParams.tsx, InstrumentJsonLd.tsx, detail page.tsx, index page.tsx, 2 test files |

## What Was Built

### Task 1: cashflow.ts (NEW)

`computeResidual(survivingCollateral: bigint, wrapperMeteredDataCost: bigint | null): bigint`
- Returns `max(survivingCollateral - wrapperMeteredDataCost, 0n)` when cost is non-null
- Returns `survivingCollateral` unchanged when cost is null (Phase 9 unbuilt)
- streamia and commission are NOT parameters — already netted by pool share-burn (08-RESEARCH §residual)
- `CashFlowBreakdown` informational type exported for Wave-2 waterfall

`tsconfig.json` — removed fixture.test.ts and cashflow.test.ts from exclude list (modules now exist).

Wave-0 RED stubs: 19/19 tests now GREEN.

### Task 2: payoff.ts + instruments.ts

`generateSchematicConvexPayoff(centerPrice, premium, curvature=0.000005, points=200)`:
- Independent parabolic generator: `payoff = max(curvature * (price - centerPrice)^2 - premium, 0)`
- Does NOT call `generatePayoffData` (independent path)
- `generatePayoffData` kept unchanged (live path)

`AbrigoInstrument` discriminated union:
- `LiveInstrument` — kind:'live' + address, deployedAt, strike, slope
- `SimulatedInstrument` — kind:'simulated' + fixtureKey (NO live-only fields)
- `fixture-celo-01` removed from registry; one `kind:'simulated'` ccop-usd-long-gamma entry added

Test updates: instruments-index.test.ts (toHaveLength(0) → assertions for length 1, kind simulated, no strike/slope/address); payoff-curve.test.tsx (added schematic shape/convexity/symmetry/independence suite).

### Task 3: Five consumers narrowed

1. **aggregator.ts** — `filter((i): i is LiveInstrument => i.chainId === chainId && i.kind === 'live')` type predicate ensures only LiveInstrument reaches multicall; simulated instruments never passed to on-chain reader (honesty invariant)

2. **InstrumentJsonLd.tsx** — Live branch: full strike/slope/address/deployedAt PropertyValues. Simulated branch: chainId + `simulated:true` + provenance PropertyValue only — NO fabricated numerics (AGENT-10/CROSS-09)

3. **InstrumentParams.tsx** — Live: 6 rows (name/id/chain/strike/slope/deployedAt). Simulated: 3 rows (name/id/chain only); full Panoptic fork-param rows deferred to Wave 3

4. **instruments/[id]/[chain]/page.tsx** — `currentPrice` guarded: live reads `instrument.strike`, simulated reads `FIXTURES[fixtureKey].pool.humanRate.value`. `PayoffDiagramClient` props guarded on kind

5. **instruments/page.tsx** — `deployedAt` read inside `kind === 'live'` guard; simulated card shows `SIMULADO — —` as honest placeholder (CROSS-09)

## Deviations from Plan

**1. [Rule 3 - Blocking] Tasks 2+3 committed atomically**
- **Found during:** Task 2 commit attempt
- **Issue:** Pre-commit lefthook runs `tsc --noEmit`; with the discriminated union in place but consumers un-narrowed, tsc exits 2 and blocks the commit
- **Fix:** Executed all 5 consumer narrowings before creating any commit, then committed Tasks 2+3 together
- **Files modified:** All 10 files listed in Task 2+3
- **Commit:** e4fcaca

**2. [Rule 1 - Bug] Type predicate required in aggregator filter**
- **Found during:** Task 3, first typecheck pass
- **Issue:** `ABRIGO_INSTRUMENTS.filter((i) => i.kind === 'live')` does NOT narrow the array type — TypeScript still infers `AbrigoInstrument[]`, not `LiveInstrument[]`, causing `inst.address` errors on L63-65/L84
- **Fix:** Changed to type predicate: `filter((i): i is LiveInstrument => ...)` — TypeScript now narrows to `LiveInstrument[]`
- **Files modified:** lib/dashboard/aggregator.ts
- **Commit:** e4fcaca (same atomic commit)

**3. [Rule 3 - Blocking] fixture-celo-01 comment in instruments.ts**
- **Found during:** Post-Task 3 acceptance criteria grep check
- **Issue:** Plan acceptance criterion requires `grep -n "fixture-celo-01" lib/apps/abrigo/instruments.ts` returns NO matches; a comment remained
- **Fix:** Removed the comment
- **Files modified:** lib/apps/abrigo/instruments.ts
- **Commit:** e4fcaca

## Self-Check

Files exist:
- lib/apps/abrigo/cashflow.ts: created
- lib/apps/abrigo/fixture.ts: modified (formatting)
- lib/apps/abrigo/payoff.ts: modified (schematic generator added)
- lib/apps/abrigo/instruments.ts: migrated to union

Commits:
- b5a49d2: cashflow.ts + tsconfig
- e4fcaca: union + all 5 consumers

## Self-Check: PASSED

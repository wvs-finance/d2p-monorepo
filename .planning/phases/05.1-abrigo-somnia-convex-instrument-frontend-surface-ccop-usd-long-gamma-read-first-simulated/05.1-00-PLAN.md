---
phase: 05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - components/defi/PayoffDiagram.tsx
  - components/defi/PayoffDiagramClient.tsx
  - tests/unit/fixture.test.ts
  - tests/unit/cashflow.test.ts
  - tests/unit/wallet-read-only.test.tsx
  - tests/unit/provenance-badge.test.tsx
autonomous: false
requirements: [DEFI-04, CROSS-01, CROSS-09, DEFI-08]
must_haves:
  truths:
    - "The PayoffDiagram curve paints at a non-zero height at 360px and 1280px (05-04 BLOCKER cleared)"
    - "The payoff curve stroke clears WCAG 1.4.3 4.5:1 contrast"
    - "No React #418 hydration mismatch on the instrument route"
    - "The chunk-strike fixture value is resolved (or recorded as an honest offset placeholder with a note)"
    - "Four failing test stubs exist and run (RED) so Wave 1+2 can turn them GREEN"
  artifacts:
    - path: "tests/unit/fixture.test.ts"
      provides: "Failing stub for fixture bigint-as-string type guards"
      contains: "fixture"
    - path: "tests/unit/cashflow.test.ts"
      provides: "Failing stub for residual arithmetic"
      contains: "residual"
    - path: "tests/unit/wallet-read-only.test.tsx"
      provides: "Failing stub for WalletPanel readOnly READ_ONLY state"
      contains: "READ_ONLY"
    - path: "tests/unit/provenance-badge.test.tsx"
      provides: "Failing stub for ProvenancePill + SimuladoBadge color+icon+text"
      contains: "provenance"
  key_links:
    - from: "components/defi/PayoffDiagramClient.tsx"
      to: "components/defi/PayoffDiagram.tsx"
      via: "loading skeleton class must equal mounted wrapper class (h-[240px] sm:h-[320px])"
      pattern: "h-\\[240px\\] sm:h-\\[320px\\]"
---

<objective>
Clear the 05-04 PayoffDiagram live-verification BLOCKER (0-height chart, sub-AA curve stroke contrast, React #418 hydration mismatch) and scaffold the four Wave-0 failing test stubs that downstream waves will turn green. Resolve the chunk-strike integer gap (RESEARCH Open Question 1).

This wave runs BEFORE the temp `fixture-celo-01` entry is removed (Wave 1) so the Evidence Collector can re-verify the existing 05-04 route `/apps/abrigo/instruments/fixture-celo-01/42220` on the FIXED chart — removing the temp entry first would 404 that route (RESEARCH Open Question 3).

Purpose: a slice never ships on a chart that does not paint; tests must exist (RED) before the data/components that satisfy them (Nyquist).
Output: fixed PayoffDiagram render path, 4 test stubs, a resolved chunk-strike value for Wave 1.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-RESEARCH.md
@.planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-VALIDATION.md
@.planning/phases/05-read-first-wallet-and-defi-surface/05-LIVE-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- Tokens (from app/globals.css :root) the executor needs for the contrast fix: -->
--accent-default: oklch(0.6 0.08 70)   /* = #a87c3a, 3.44:1 on bg-canvas — SUB-AA, do NOT use for curve stroke */
--accent-text:    oklch(0.48 0.08 70)  /* clears 4.5:1 on bg-canvas — USE for curve stroke */
--text-primary:   oklch(0.2 0.01 80)   /* ~9:1 — also acceptable */

<!-- Current PayoffDiagram.tsx wrapper (line 62) ALREADY uses h-[240px] sm:h-[320px]. -->
<!-- Current PayoffDiagramClient.tsx loading skeleton (line 17) STILL uses min-h-* — this is the live (committed) bug. -->
<!-- Current curve <Line> (lines 128-135) uses type="linear" stroke="var(--accent-default)". -->

<!-- payoff.ts PayoffPoint (the data shape the test stubs reference): -->
export interface PayoffPoint { price: number; payoff: number }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the 05-04 PayoffDiagram BLOCKER (0-height + contrast + hydration)</name>
  <read_first>
    - components/defi/PayoffDiagram.tsx (the file being modified)
    - components/defi/PayoffDiagramClient.tsx (the file being modified)
    - .planning/phases/05-read-first-wallet-and-defi-surface/05-LIVE-VERIFICATION.md (claim 4 ✗ FAIL root-cause analysis, lines 76-95)
    - app/globals.css (token values for --accent-text)
  </read_first>
  <action>
    Three mechanical fixes, all in the two PayoffDiagram files:

    1. HYDRATION (React #418): In `components/defi/PayoffDiagramClient.tsx` line 17, change the loading skeleton class from `min-h-[240px] sm:min-h-[320px]` to EXACTLY `h-[240px] sm:h-[320px]` so the SSR skeleton class string matches the mounted component's wrapper div (PayoffDiagram.tsx line 62 already uses `h-[240px] sm:h-[320px]`). Resulting line:
       `loading: () => <div className="h-[240px] sm:h-[320px]" aria-hidden="true" />,`

    2. CONTRAST (WCAG 1.4.3): In `components/defi/PayoffDiagram.tsx`, change the payoff curve `<Line>` (currently lines 128-135) `stroke="var(--accent-default)"` to `stroke="var(--accent-text)"` and the `activeDot` `fill: 'var(--accent-default)'` to `fill: 'var(--accent-text)'`. `--accent-text` = oklch(0.48 0.08 70) clears 4.5:1 on bg-canvas; `--accent-default` (#a87c3a) is 3.44:1 and fails. Leave the "Current" ReferenceLine label fill (already `var(--accent-text)`) unchanged. Update the stale comment on line 8 that says `stroke="var(--accent-default)"` to reflect `--accent-text`.

    3. 0-HEIGHT (already partially fixed in working tree): Verify the wrapper div on PayoffDiagram.tsx line 62 reads `className="h-[240px] sm:h-[320px]"` (NOT `min-h-*`). If it still says `min-h-*`, change to `h-[240px] sm:h-[320px]`. Update the stale comment on line 9 that references `min-h`.

    Do NOT change props, data flow, or the sr-only table in this task — those change in Wave 2. This task is render-correctness only so the Evidence Collector can re-verify the existing fixture route.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/payoff-curve.test.tsx &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "min-h-" components/defi/PayoffDiagramClient.tsx` returns NO matches
    - `grep -n "min-h-" components/defi/PayoffDiagram.tsx` returns NO matches
    - `grep -c "var(--accent-default)" components/defi/PayoffDiagram.tsx` returns 0 (curve + activeDot now use --accent-text)
    - `grep -n "h-\[240px\] sm:h-\[320px\]" components/defi/PayoffDiagramClient.tsx` returns the loading line
    - `pnpm vitest run tests/unit/payoff-curve.test.tsx` passes (existing render test still green)
  </acceptance_criteria>
  <done>PayoffDiagramClient loading skeleton uses h-[240px] sm:h-[320px]; curve stroke + activeDot use var(--accent-text); no min-h-* in either file; existing payoff-curve render test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve chunk-strike + scaffold 4 Wave-0 failing test stubs</name>
  <read_first>
    - .planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-VALIDATION.md (Wave 0 Requirements, lines 63-71)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/test/instrument/PanopticDataSeam.fork.t.sol (lines 39-79 — STRIKE_OFFSET=2000, width=2, strike = ((currentTick + 2000)/tickSpacing)*tickSpacing)
    - tests/unit/wallet-state.test.ts (existing pattern for wallet test shape)
    - tests/unit/payoff-curve.test.tsx (existing render-test pattern with @vitest-environment jsdom)
  </read_first>
  <behavior>
    These are FAILING stubs (RED). They import symbols that do not exist yet (created in Wave 1/2). Each must `it(...)` with a real assertion that will pass once the implementation lands. Use `describe.skip` is FORBIDDEN — write live failing tests (import will throw / assertion will fail) so Nyquist sampling sees them.
    - fixture.test.ts: assert `typeof CCOP_USD_LONG_GAMMA_FIXTURE.forkBlock.value === 'string'`, `.pool.seededLiquidity.value === 'string'`, `.chunk.strike.value === 'string'`, `.chunk.width.value === 'string'`, and `typeof .pool.humanRate.value === 'number'`. Assert `.forkBlock.value === '46700000'`, `.pool.humanRate.value === 4000`, `.pool.tickSpacing.value === '10'`, `.pool.seededLiquidity.value === '1000000000000000000000000'`, `.chunk.width.value === '2'`. Assert every FixtureValue has `tier` in `['fork-fixture','spec']` and a non-empty `source`.
    - cashflow.test.ts: assert `computeResidual(1000n, null)` returns `1000n` (dataCost null → residual = surviving); `computeResidual(1000n, 200n)` returns `800n`; `computeResidual(100n, 500n)` returns `0n` (max-floor). Add a test asserting that streamia and commission are NOT parameters of `computeResidual` (it takes exactly survivingCollateral + wrapperMeteredDataCost).
    - wallet-read-only.test.tsx (@vitest-environment jsdom): render `<WalletPanel strings={...} readOnly />` and assert the rendered pill text contains the READ_ONLY label and is NOT the CONNECTED_READY label; assert no "Conectar billetera" button is in the DOM.
    - provenance-badge.test.tsx (@vitest-environment jsdom): render `<ProvenancePill tier="fork-fixture" .../>` for all three tiers and assert each has visible text (the tier label), an svg icon node, and an `aria-label` carrying the full provenance sentence; render `<SimuladoBadge .../>` and assert it shows "SIMULADO" text + icon.
  </behavior>
  <action>
    First resolve the chunk strike: read `PanopticDataSeam.fork.t.sol` L39-79. The strike is NOT a static literal — it is `((currentTick + STRIKE_OFFSET) / tickSpacing) * tickSpacing` with `STRIKE_OFFSET = 2000` and `tickSpacing = 10`. There is NO single deployable integer. Record this finding in the SUMMARY: the honest fixture value for `chunk.strike` is the OFFSET `"2000"` (ticks out-of-money), NOT a fabricated absolute tick, with `note: "+2000-tick OTM offset from the live fork tick, tickSpacing(10)-aligned: ((currentTick+2000)/10)*10. No static absolute — depends on fork tick. fork-test artifact to clear InvalidTickBound."`. Wave 1 fixture.ts will use `value: '2000'` and this note. (This replaces the 'TBD' placeholder from RESEARCH — the gap is now resolved.)

    Then create the four failing test stubs per the <behavior> block. Each imports from the Wave-1/2 module paths: `@/lib/apps/abrigo/fixture`, `@/lib/apps/abrigo/cashflow`, `@/components/defi/WalletPanel`, `@/components/defi/ProvenanceBadge`. These imports WILL fail to resolve now — that is the intended RED state. Use the existing test file headers (`// @vitest-environment jsdom` for .tsx render tests, `// @vitest-environment node` for pure-data tests) matching the established patterns.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/fixture.test.ts tests/unit/cashflow.test.ts tests/unit/wallet-read-only.test.tsx tests/unit/provenance-badge.test.tsx 2>&amp;1 | grep -E "FAIL|Cannot find module|failed" || echo "STUBS-NOT-RED"</automated>
  </verify>
  <acceptance_criteria>
    - The four files exist: `ls tests/unit/fixture.test.ts tests/unit/cashflow.test.ts tests/unit/wallet-read-only.test.tsx tests/unit/provenance-badge.test.tsx` all succeed
    - Each file contains at least one `it(` and zero `describe.skip(` / `it.skip(` (grep returns no `.skip`)
    - `grep -n "2000" tests/unit/fixture.test.ts` — strike offset value present
    - Running the four files produces FAIL/Cannot-find-module output (RED state confirmed), NOT all-green
    - SUMMARY records the resolved chunk-strike finding (offset "2000", not a static absolute)
  </acceptance_criteria>
  <done>Four live failing test stubs exist (no .skip); chunk-strike resolved to offset "2000" with documented note; running the stubs is RED.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Evidence Collector — re-verify the 05-04 PayoffDiagram fix on the existing fixture route</name>
  <action>Launch the Evidence Collector agent (Playwright MCP) per the CLAUDE.md recipe against the existing 05-04 fixture route on the production build; record verdicts + screenshots. See &lt;how-to-verify&gt; for the exact steps.</action>
  <what-built>The 05-04 PayoffDiagram BLOCKER fix (0-height, curve-stroke contrast, React #418 hydration). The existing temp fixture route `/apps/abrigo/instruments/fixture-celo-01/42220` is still present (NOT yet removed — removal is Wave 1) so the chart can be live-verified on the fixed render path.</what-built>
  <how-to-verify>
    Run the Evidence Collector agent (per CLAUDE.md mandate) against the existing 05-04 route BEFORE Wave 1 removes the temp fixture:
    1. `pnpm build &amp;&amp; pnpm start -p 3040` (production webpack build — Turbopack dev can mask route-segment differences)
    2. Evidence Collector navigates to `/apps/abrigo/instruments/fixture-celo-01/42220` at 360px and 1280px
    3. Assert `.recharts-responsive-container` computed height &gt; 0 and `.recharts-curve` SVG node exists (0-height BLOCKER cleared)
    4. Assert the curve stroke computed color corresponds to `--accent-text` (oklch 0.48 0.08 70), and run axe — no color-contrast violation on the curve (1.4.3)
    5. Assert NO React #418 hydration error in `browser_console_messages`
    6. Append verdicts (✓/⚠/✗) + screenshot paths to `.planning/phases/05-read-first-wallet-and-defi-surface/05-LIVE-VERIFICATION.md` (this clears the 05-04 checkpoint Task 3 debt)
    Note: the "Conectar billetera" RainbowKit CTA 3.44:1 contrast (claim 6 ⚠) is a RainbowKit-theme issue OUTSIDE this slice — record as a known WAIVER, do not block on it.
  </how-to-verify>
  <acceptance_criteria>
    - 05-LIVE-VERIFICATION.md has a new section dated this run with the curve-paints ✓ verdict and screenshot path under /tmp/d2p-verify/
    - Claim 4 (DEFI-04 payoff curve) is now ✓ PASS (was ✗ FAIL)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the Evidence Collector records the payoff curve paints (✓) on the fixed route, or describe the remaining issues to fix before Wave 1.</resume-signal>
</task>

</tasks>

<verification>
- `grep -n "min-h-" components/defi/PayoffDiagram.tsx components/defi/PayoffDiagramClient.tsx` → no matches
- `pnpm vitest run tests/unit/payoff-curve.test.tsx` green
- Four Wave-0 stubs exist and are RED
- Evidence Collector re-verified the existing 05-04 route: curve paints, contrast clears, no #418
</verification>

<success_criteria>
The PayoffDiagram render path is fixed and live-verified on the existing fixture route; four failing test stubs are in place; the chunk-strike gap is resolved (offset "2000" with note). Wave 1 can now safely remove the temp fixture and build the data layer.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-00-SUMMARY.md`
</output>

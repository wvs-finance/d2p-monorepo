---
phase: 05.1
slug: abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-02
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `05.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.6 (unit + render) + Playwright 1.60.0 (e2e + a11y) |
| **Config file** | `vitest.config.ts` (jsdom; includes `tests/**/*.{test,spec}.{ts,tsx}`, excludes `tests/e2e/**`, `tests/a11y/**`) |
| **Quick run command** | `pnpm vitest run tests/unit/payoff-curve.test.tsx tests/unit/wallet-state.test.ts tests/unit/instruments-index.test.ts` |
| **Full suite command** | `pnpm test:all` (biome + tsc + vitest + playwright + lighthouse) |
| **Estimated runtime** | ~30s quick (unit + tsc); full suite minutes (incl. production build for e2e) |

---

## Sampling Rate

- **After every task commit:** `pnpm vitest run` (unit) + `pnpm typecheck` (tsc gate) — < 30s.
- **After every plan wave:** `pnpm test:quick` (biome + tsc + vitest) **+ Evidence Collector live verification** (CLAUDE.md mandate).
- **Before `/gsd:verify-work`:** `pnpm test:all` green (vitest + playwright + lighthouse) + all Evidence Collector verdicts ✓.
- **Max feedback latency:** 30 seconds (unit + tsc).

---

## Per-Task Verification Map

| Requirement | Behavior | Test Type | Automated Command | File Exists | Status |
|-------------|----------|-----------|-------------------|-------------|--------|
| DEFI-04 (fix) | PayoffDiagram renders with `h-*` parent; chart paints at 240px | unit render | `vitest run tests/unit/payoff-curve.test.tsx` | ✅ extend | ⬜ pending |
| DEFI-04 (fix) | Curve stroke contrast ≥ 4.5:1 (WCAG 1.4.3) via `--accent-text` | a11y | `pnpm test:a11y tests/a11y/` | ✅ extend | ⬜ pending |
| DEFI-08 (new) | `generateSchematicConvexPayoff` → convex, premium-shifted, non-negative `PayoffPoint[]` | unit | `vitest run tests/unit/payoff-curve.test.tsx` | ✅ extend | ⬜ pending |
| DEFI-08 (new) | `fixture.ts` type-guards: bigint-as-string fields are strings | unit | `vitest run tests/unit/fixture.test.ts` | ❌ W0 | ⬜ pending |
| DEFI-08 (new) | `cashflow.ts` residual: streamia/commission NOT subtracted; `residual = max(surviving − dataCost, 0)` | unit | `vitest run tests/unit/cashflow.test.ts` | ❌ W0 | ⬜ pending |
| DEFI-08 (new) | `AbrigoInstrument` union: simulated variant has no `strike`/`slope`/`address` | unit (tsc) | `pnpm typecheck` | ✅ tsc gate | ⬜ pending |
| DEFI-02 (ext) | `WalletPanel readOnly` renders READ_ONLY; never CONNECTED_READY | unit render | `vitest run tests/unit/wallet-state.test.ts` | ✅ extend | ⬜ pending |
| CROSS-09 | READ_ONLY pill + provenance pills + SIMULADO badge: color + icon + text | unit render | `vitest run tests/unit/provenance-badge.test.tsx`, `tests/unit/wallet-read-only.test.tsx` | ❌ W0 | ⬜ pending |
| DEFI-05 (ext) | SIMULADO badge above the fold at 360px, scrollY=0 | e2e + Evidence Collector | `pnpm test:e2e tests/e2e/instrument-page.spec.ts` | ✅ extend | ⬜ pending |
| DEFI-08 (new) | Provenance pills present on pool fields (fork-fixture tier) | e2e + Evidence Collector | `pnpm test:e2e tests/e2e/instrument-page.spec.ts` | ✅ extend | ⬜ pending |
| DEFI-08 (new) | Wallet never reads CONNECTED_READY on simulated instrument | e2e | `pnpm test:e2e tests/e2e/wallet-states.spec.ts` | ✅ extend | ⬜ pending |
| DEFI-08 (new) | No fabricated numbers in JSON-LD (no strike/slope/address on simulated) | unit | `vitest run tests/unit/agent-jsonld.test.tsx` | ✅ extend | ⬜ pending |
| CROSS-01 | CashFlowWaterfall reading order (1.3.2); pill non-text contrast ≥3:1 (1.4.11) | a11y | `pnpm test:a11y tests/a11y/` | ✅ extend | ⬜ pending |
| DEFI-09 (new) | `docs/book/` excluded from Velite globbing (no collision) | unit | `vitest run tests/unit/velite-research-schema.test.ts` | ✅ extend | ⬜ pending |
| CROSS-10 | New es-CO/en copy authored; native-reviewer sign-off in `docs/copy-review.md` | manual | sign-off recorded | N/A | ⬜ pending |
| (mandate) | SIMULADO above fold; pills; no fabricated numbers; wallet not READY | live DOM (Playwright MCP) | Evidence Collector after commit | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/fixture.test.ts` — bigint-as-string type guards for `LongGammaFixture` (forkBlock, seededLiquidity, strike, width are strings; humanRate is number).
- [ ] `tests/unit/cashflow.test.ts` — residual = `max(survivingCollateral − wrapperMeteredDataCost, 0)`; asserts streamia/commission NOT subtracted; `wrapperMeteredDataCost = null` → residual = surviving.
- [ ] `tests/unit/wallet-read-only.test.tsx` — `WalletPanel readOnly`: never CONNECTED_READY; READ_ONLY pill with icon + text.
- [ ] `tests/unit/provenance-badge.test.tsx` — ProvenancePill + SimuladoBadge: three tiers render color + icon + text; `aria-label` carries full provenance sentence.
- [ ] **Resolve the chunk-strike integer** from Phase-8 LongGammaWrapper test constants before `fixture.ts` is finalized (use `'TBD'` placeholder + note until resolved). *(Open Question 1)*

*Framework already installed — no install task.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live DOM honesty (SIMULADO above fold; pills; no fabricated numbers; wallet not READY; both locales) | Evidence Collector mandate (CLAUDE.md) | Live DOM is the ground truth; tsc/vitest insufficient (Phase-2 regression rationale) | Run Evidence Collector against `/apps/abrigo/instruments/ccop-usd-long-gamma/8453` after each task commit; screenshots to `/tmp/d2p-verify/` |
| es-CO/en copy quality | CROSS-10 | Native-reviewer judgment | Sign-off recorded in `docs/copy-review.md` before route ships |

---

## Validation Sign-Off

- [ ] All tasks have an automated verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (4 test stubs + chunk-strike resolution)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter (after planner wires all tasks)

**Approval:** pending

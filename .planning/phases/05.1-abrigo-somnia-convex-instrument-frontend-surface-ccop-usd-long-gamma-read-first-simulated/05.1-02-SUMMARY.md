---
phase: 05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated
plan: "02"
subsystem: ui
tags: [provenance, wallet, payoff-diagram, wcag, vitest, tdd, wave-2, anti-fishing, read-only]

requires:
  - phase: 05.1
    plan: "01"
    provides: fixture.ts, cashflow.ts, payoff.ts, instruments.ts (frozen Wave-1 contracts)
  - phase: 05.1
    plan: "00"
    provides: Wave-0 RED stubs (provenance-badge, wallet-read-only, payoff-curve)

provides:
  - ProvenanceBadge: SimuladoBadge + ProvenancePill (3 tiers, WCAG 1.4.11, CROSS-09)
  - SnapshotPoolPanel: fork-fixture pool fields dl with per-field pills
  - CashFlowWaterfall: backend-correct dl (streamia/commission informational, em-dash-never-0)
  - PayoffDiagram: data: PayoffPoint[] prop contract, monotone curve, curve-aware sr-only table
  - WalletPanel.readOnly: forces READ_ONLY state (no connect/switch CTA)
  - WalletStatusPill.READ_ONLY: Eye icon, neutral styling, useMounted hydration guard
  - WalletState/WalletStatus unions: READ_ONLY member added to both
  - page.tsx minimal edit: data= prop + statusLabels READ_ONLY entry (tsc green)

affects:
  - 05.1-03 (Wave 3: simulated-branch page layout, WalletPanel readOnly wiring, SIMULADO display)
  - tests/unit/wallet-state.test.ts (ALL_STATES gains READ_ONLY; new deriver-never-READ_ONLY case)

tech-stack:
  added: []
  patterns:
    - "ProvenancePill reuses exact WalletStatusPill pill shell string (inline-flex...ring-1 ring-inset)"
    - "READ_ONLY injected via WalletPanel.readOnly prop; deriveWalletState body untouched (pure 4-output)"
    - "useMounted guard in WalletStatusPill: SSR renders DISCONNECTED until wagmi hydrates (anti-#418)"
    - "PayoffDiagram data= prop: RSC computes PayoffPoint[] and passes to client island (no internal generate)"
    - "wagmi + rainbowkit vi.mock in wallet-read-only.test.tsx for hook-safe unit testing"

key-files:
  created:
    - components/defi/ProvenanceBadge.tsx
    - components/defi/SnapshotPoolPanel.tsx
    - components/defi/CashFlowWaterfall.tsx
  modified:
    - components/defi/PayoffDiagram.tsx
    - components/defi/PayoffDiagramClient.tsx (props forwarded via re-export; no explicit changes needed)
    - components/defi/WalletPanel.tsx
    - components/defi/WalletStatusPill.tsx
    - lib/wallet/state.ts
    - app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx
    - tests/unit/wallet-read-only.test.tsx
    - tests/unit/wallet-state.test.ts
    - tsconfig.json

key-decisions:
  - "ProvenancePill uses FULL-opacity rings (not /30) for WCAG 1.4.11 ≥3:1 non-text contrast"
  - "READ_ONLY is never returned by deriveWalletState; it is injected only by WalletPanel.readOnly=true"
  - "useMounted guard in WalletStatusPill: READ_ONLY bypasses the mount guard (prop-injected, not wagmi-derived); all other statuses fall back to DISCONNECTED until mounted"
  - "wallet-read-only.test.tsx mocks wagmi + rainbowkit to avoid provider tree in unit tests"
  - "strokeWidth bumped to 2.5 in PayoffDiagram (perceptual prominence, deferred from 05.1-00)"
  - "page.tsx statusLabels READ_ONLY uses temporary literal 'Solo lectura'; Wave 3 replaces with t() key"
  - "page.tsx walletStrings readOnlyLabel uses temporary literal; Wave 3 replaces with t() key"

metrics:
  duration: 9min
  completed: 2026-06-02T15:05:41Z
  tasks: 4
  files: 11
---

# Phase 05.1 Plan 02: Wave-2 Honesty Surface Components Summary

**ProvenanceBadge 3-tier pills + SnapshotPoolPanel + CashFlowWaterfall + PayoffDiagram data-prop + read-only wallet path with WalletStatusPill hydration fix**

## Performance

- **Duration:** ~9 min
- **Started:** 2026-06-02T14:56:22Z
- **Completed:** 2026-06-02T15:05:41Z
- **Tasks:** 4 of 4 complete
- **Files modified:** 11

## Accomplishments

- Created `ProvenanceBadge.tsx` with `ProvenancePill` (3 tiers: fork-fixture/spec/schematic; full-opacity rings; color+icon+text+aria-label per CROSS-09) and `SimuladoBadge` (SIMULADO label, FlaskConical icon, status-parked amber)
- Created `SnapshotPoolPanel.tsx` rendering fork-fixture pool fields as a `<dl>`, each row with a `ProvenancePill tier="fork-fixture"`, em-dash for null, bigint-as-string values uncoerced
- Created `CashFlowWaterfall.tsx` with correct reading order (premium → streamia → commission → data-cost → residual), streamia/commission marked informational with "already netted" tag, one spec-tier ProvenancePill on the section header
- Extended `PayoffDiagram.tsx` to `data: PayoffPoint[]` prop (RSC passes pre-computed data), `type="monotone"` for smooth convex curve, conditional `strikeRef`/`currentPriceRef` reference lines, `isSchematic` SVG annotation, curve-aware sr-only table (samples data array across 20 rows), `strokeWidth=2.5` perceptual bump
- Updated `tests/unit/payoff-curve.test.tsx` render assertion from old strike/slope/currentPrice contract to `data={generateSchematicConvexPayoff(4000,0)} ariaLabel="..." locale="es-CO" isSchematic`
- Minimal page edit: `generatePayoffData`/`generateSchematicConvexPayoff` called in RSC body; `PayoffDiagramClient` gets `data=` prop (Wave 3 owns full simulated branch layout)
- Added `READ_ONLY` to `WalletState` union (`lib/wallet/state.ts`) — deriver body untouched
- Added `READ_ONLY` to `WalletStatus` union and `STATUS_CONFIG` in `WalletStatusPill.tsx` (Eye icon, neutral text-muted styling)
- Added `useMounted` guard to `WalletStatusPill`: SSR emits DISCONNECTED; client renders real status after wagmi hydrates — fixes React #418 hydration mismatch from 05.1-00
- Extended `WalletPanel` with `readOnly?: boolean` prop; `readOnly=true` forces `'READ_ONLY' as const` without calling `deriveWalletState`; renders pill + readOnlyLabel text, no ConnectButton, no switch CTA
- Added `READ_ONLY` to page `statusLabels` literal (Record exhaustiveness, tsc green)
- Updated `wallet-read-only.test.tsx`: added wagmi+rainbowkit vi.mock; removed @ts-expect-error stubs; 4/4 tests GREEN
- Updated `wallet-state.test.ts`: READ_ONLY in ALL_STATES; new `deriveWalletState NEVER returns READ_ONLY` case; 9/9 tests GREEN
- All 40 tests across 4 affected test files GREEN

## Task Commits

| Task | Name | Commit | Key Files |
|------|------|--------|-----------|
| 1 | ProvenanceBadge + SnapshotPoolPanel | a9adc5f | ProvenanceBadge.tsx, SnapshotPoolPanel.tsx, tsconfig.json |
| 2a | CashFlowWaterfall | 4130842 | CashFlowWaterfall.tsx |
| 2b | PayoffDiagram props + page minimal edit | 49b28bc | PayoffDiagram.tsx, payoff-curve.test.tsx, page.tsx |
| 3 | Read-only wallet path + hydration fix | 67cb859 | WalletStatusPill.tsx, WalletPanel.tsx, lib/wallet/state.ts, wallet-read-only.test.tsx, wallet-state.test.ts, page.tsx |

## Deviations from Plan

### Injected Deferred Fixes (user-approved 2026-06-02)

**1. [Deferred Fix 1] WalletStatusPill useMounted hydration guard**
- **Origin:** 05.1-00 deferred items, approved for Wave 2
- **Issue:** SSR renders DISCONNECTED, client briefly renders CONNECTING during wagmi auto-reconnect → React #418 hydration mismatch
- **Fix:** Added `useMounted` + `useEffect` guard in `WalletStatusPill.tsx`. `safeStatus` uses `status === 'READ_ONLY' ? status : 'DISCONNECTED'` before mount (READ_ONLY bypasses guard — it's prop-injected, not wagmi-derived). After mount, real status renders.
- **Files:** `components/defi/WalletStatusPill.tsx`
- **Commit:** `67cb859`

**2. [Deferred Fix 2] Curve strokeWidth bump to 2.5**
- **Origin:** 05.1-00 deferred items (perceptual prominence note)
- **Issue:** 2px ochre stroke passes WCAG 1.4.11 but is perceptually thin in full-page screenshots
- **Fix:** `strokeWidth={2.5}` in `PayoffDiagram.tsx` `<Line>` element
- **Files:** `components/defi/PayoffDiagram.tsx`
- **Commit:** `49b28bc`

### Auto-fixed Issues (Rule 3 — blocking)

**1. [Rule 3 - Blocking] Biome formatting violations on Task 1 commit**
- **Found during:** Task 1 pre-commit hook
- **Issue:** Import order (`FlaskConical` before `FileText`), multi-line JSX function signature, tsconfig.json multi-line array
- **Fix:** Sorted lucide-react imports alphabetically, expanded `SnapshotPoolPanel` destructuring to Biome's preferred multi-line format, collapsed tsconfig `exclude` array to single line
- **Files:** `ProvenanceBadge.tsx`, `SnapshotPoolPanel.tsx`, `tsconfig.json`

**2. [Rule 3 - Blocking] wallet-read-only.test.tsx required wagmi mock**
- **Found during:** Task 3 test execution
- **Issue:** `useAccount` + `useSwitchChain` called unconditionally (React hook rules); no wagmi provider in unit test environment → render throws
- **Fix:** Added `vi.mock('wagmi', ...)` + `vi.mock('@rainbow-me/rainbowkit', ...)` in the test file; also updated `STRINGS` fixture to include `readOnlyLabel` + `READ_ONLY` statusLabel (interface now requires them), and removed `@ts-expect-error` stubs (prop is now valid)
- **Files:** `tests/unit/wallet-read-only.test.tsx`

## Invariants Maintained

- `deriveWalletState` body unchanged — 4-output pure function; `READ_ONLY` never in any return branch
- `PoolStatePanel.tsx` untouched (live instruments keep it)
- Wave 3 ownership boundary respected: simulated-branch page layout (kind-guard-before-aggregator return) NOT authored in Wave 2

## Self-Check

See below.

---
phase: 05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated
plan: "00"
subsystem: ui
tags: [recharts, payoff-diagram, wcag, vitest, tdd, wave-0, panoptic, fixture]

requires:
  - phase: 05-read-first-wallet-and-defi-surface
    provides: PayoffDiagram, PayoffDiagramClient, WalletPanel — existing components extended here

provides:
  - Fixed PayoffDiagram render path (0-height resolved, WCAG 1.4.11 contrast, React #418 hydration)
  - Four Wave-0 RED test stubs for downstream waves (fixture, cashflow, wallet-read-only, provenance-badge)
  - Resolved chunk-strike: OTM offset string "2000" (not a static absolute tick)

affects:
  - 05.1-01 (Wave 1: fixture.ts must use value "2000" for chunk.strike with documented note)
  - 05.1-02 (Wave 2: WalletPanel readOnly prop; ProvenanceBadge + SimuladoBadge components)
  - Evidence Collector live verification of /apps/abrigo/instruments/fixture-celo-01/42220

tech-stack:
  added: []
  patterns:
    - "Wave-0 RED stubs excluded from tsconfig when they reference not-yet-created modules (established pattern: structured-data.test.tsx)"
    - "chunk.strike uses OTM offset string not static absolute tick — never fabricate absolute ticks for fork-dependent values"

key-files:
  created:
    - tests/unit/fixture.test.ts
    - tests/unit/cashflow.test.ts
    - tests/unit/wallet-read-only.test.tsx
    - tests/unit/provenance-badge.test.tsx
  modified:
    - components/defi/PayoffDiagram.tsx
    - components/defi/PayoffDiagramClient.tsx
    - tsconfig.json

key-decisions:
  - "chunk.strike fixture value is the OTM offset string '2000' (not a static absolute tick): strike = ((currentTick+2000)/tickSpacing)*tickSpacing, STRIKE_OFFSET=2000, tickSpacing=10 per PanopticDataSeam.fork.t.sol L41+L73. No static absolute exists — depends on live fork tick. Wave 1 fixture.ts uses value: '2000' with note: '+2000-tick OTM offset from the live fork tick, tickSpacing(10)-aligned. fork-test artifact to clear InvalidTickBound.'"
  - "Wave-0 RED stub files that import not-yet-created modules are excluded from tsconfig.json (same pattern as structured-data.test.tsx) so tsc --noEmit passes the pre-commit gate while vitest runs them as failing RED tests"

patterns-established:
  - "RED stub tsconfig exclusion: tests importing Wave-N modules are added to tsconfig exclude until the module is created in Wave N"
  - "WalletPanel readOnly test uses @ts-expect-error on the future prop; renders against existing full WalletPanelStrings to pass tsc"

requirements-completed: [DEFI-04, CROSS-01, CROSS-09, DEFI-08]

duration: 18min
completed: 2026-06-02
---

# Phase 05.1 Plan 00: Wave-0 Blockers + RED Stubs Summary

**Fixed PayoffDiagram BLOCKER (0-height + WCAG 1.4.11 contrast + React #418 hydration) and scaffolded 4 Wave-0 RED test stubs with chunk-strike resolved to OTM offset "2000"**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-06-02T10:20:00Z
- **Completed:** 2026-06-02T10:38:00Z
- **Tasks:** 2 of 3 (Task 3 is checkpoint:human-verify — Evidence Collector)
- **Files modified:** 6

## Accomplishments

- PayoffDiagram 0-height BLOCKER cleared: `min-h-*` replaced with `h-[240px] sm:h-[320px]` in both the wrapper div (PayoffDiagram.tsx) and the loading skeleton (PayoffDiagramClient.tsx), eliminating React #418 hydration mismatch
- WCAG 1.4.11 contrast fix: both `var(--accent-default)` stroke sites in PayoffDiagram.tsx (payoff curve Line+activeDot AND current-price ReferenceLine) changed to `var(--accent-text)` (oklch 0.48 0.08 70, clears 3:1 non-text)
- Four Wave-0 RED test stubs created and confirmed failing; chunk-strike resolved from PanopticDataSeam.fork.t.sol: OTM offset "2000", not a fabricated static absolute tick

## Task Commits

1. **Task 1: Fix the 05-04 PayoffDiagram BLOCKER** — `c00c89f` (fix)
2. **Task 2: Resolve chunk-strike + scaffold 4 Wave-0 failing test stubs** — `0934ad5` (test)

## Files Created/Modified

- `components/defi/PayoffDiagram.tsx` — 0-height fix (h-* class), both accent-default stroke sites → accent-text, updated stale comments
- `components/defi/PayoffDiagramClient.tsx` — loading skeleton min-h-* → h-[240px] sm:h-[320px] (React #418 fix)
- `tests/unit/fixture.test.ts` — DEFI-08 RED stub: bigint-as-string type guards; chunk.strike "2000" OTM offset
- `tests/unit/cashflow.test.ts` — DEFI-08 RED stub: computeResidual arithmetic + 2-param contract
- `tests/unit/wallet-read-only.test.tsx` — DEFI-02/CROSS-09 RED stub: WalletPanel readOnly READ_ONLY state
- `tests/unit/provenance-badge.test.tsx` — CROSS-09/DEFI-08 RED stub: ProvenancePill 3 tiers + SimuladoBadge
- `tsconfig.json` — exclude 3 Wave-0 stubs that reference not-yet-created modules

## Decisions Made

- **chunk.strike = "2000" (OTM offset, not static absolute):** PanopticDataSeam.fork.t.sol L41 defines `STRIKE_OFFSET = 2000`; L73 computes `strike = int24(((currentTick + STRIKE_OFFSET) / tickSpacing) * tickSpacing)`. No static absolute tick exists — it depends on the live fork tick. The honest fixture value is the offset string `"2000"`. Wave 1 must use `value: '2000'` with note: "+2000-tick OTM offset from the live fork tick, tickSpacing(10)-aligned: ((currentTick+2000)/10)*10. No static absolute — depends on fork tick. fork-test artifact to clear InvalidTickBound." This RESOLVES the Open Question 1 from 05.1-VALIDATION.md.
- **Wave-0 stub tsconfig exclusion:** Three stubs import modules from Wave 1/2 that do not exist yet. Excluding them from tsconfig (established pattern: `tests/unit/structured-data.test.tsx`) lets `tsc --noEmit` pass the pre-commit gate. Vitest's esbuild transformer still runs them, and they fail at runtime as intended (RED). The `wallet-read-only.test.tsx` stub imports the existing WalletPanel with `@ts-expect-error` for the not-yet-typed `readOnly` prop.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Biome import-order and formatting violations in test stubs**
- **Found during:** Task 2 commit (pre-commit hook)
- **Issue:** Biome `organizeImports` required `@/...` imports sorted before relative imports; formatting violations on multi-line `toBe(...)` calls
- **Fix:** Reordered imports and collapsed multi-line toBe calls per biome formatter output
- **Files modified:** `tests/unit/fixture.test.ts`, `tests/unit/cashflow.test.ts`, `tests/unit/provenance-badge.test.tsx`
- **Verification:** `biome check` passed with "No fixes applied"
- **Committed in:** `0934ad5` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — biome formatting)
**Impact on plan:** Formatting-only; no logic or behavior change.

## Issues Encountered

- PayoffDiagram.tsx and PayoffDiagramClient.tsx fixes were already in the working tree (from 05-04 in-flight edits). All acceptance criteria passed immediately — no code changes needed for Task 1 beyond committing the existing working-tree state.

## Checkpoint: Task 3 (Evidence Collector)

**Status:** STOPPED — awaiting live verification

**What to verify:** The fixed PayoffDiagram renders correctly on the existing temp fixture route `/apps/abrigo/instruments/fixture-celo-01/42220` before Wave 1 removes it.

**How to run:** Evidence Collector agent per CLAUDE.md mandate — `pnpm build && pnpm start -p 3040`, then navigate to the route at 360px + 1280px viewports. Assert:
1. `.recharts-responsive-container` computed height > 0 and `.recharts-curve` SVG exists (0-height BLOCKER cleared)
2. Curve stroke + current-price reference-line stroke corresponds to `--accent-text` (oklch 0.48 0.08 70); axe reports no color-contrast violation on the curve (WCAG 1.4.11 non-text)
3. No React #418 hydration error in browser console
4. Append verdicts + screenshot paths to `.planning/phases/05-read-first-wallet-and-defi-surface/05-LIVE-VERIFICATION.md`

**Acceptance:** `05-LIVE-VERIFICATION.md` has a new section with curve-paints ✓ verdict and screenshot path. Claim 4 (was ✗ FAIL) is now ✓ PASS.

## Next Phase Readiness

- Wave 1 (05.1-01) can safely remove the temp `fixture-celo-01` entry and create `lib/apps/abrigo/fixture.ts` with `value: '2000'` for `chunk.strike`
- Four RED stubs are in place; Wave 1 turns `fixture.test.ts` and `cashflow.test.ts` GREEN; Wave 2 turns `wallet-read-only.test.tsx` and `provenance-badge.test.tsx` GREEN
- Evidence Collector must verify the PayoffDiagram fix on the live route before Wave 1 proceeds

## Self-Check

All files created/modified exist and commits exist — verified during execution.

---
*Phase: 05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated*
*Completed: 2026-06-02*

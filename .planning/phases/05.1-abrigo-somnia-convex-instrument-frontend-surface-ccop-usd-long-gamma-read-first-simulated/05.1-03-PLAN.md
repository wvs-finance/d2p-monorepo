---
phase: 05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx
  - messages/es-CO/instruments.json
  - messages/en/instruments.json
  - .gitbook.yaml
  - docs/book/README.md
  - docs/book/SUMMARY.md
  - docs/book/apps/abrigo/instruments/ccop-usd-long-gamma.md
  - tests/unit/velite-research-schema.test.ts
  - docs/copy-review.md
autonomous: false
requirements: [DEFI-08, DEFI-03, DEFI-05, DEFI-09, CROSS-10, CROSS-09, AGENT-10]
user_setup: []
must_haves:
  truths:
    - "The detail page branches on kind==='simulated' BEFORE aggregateAllChains — a simulated entry never reaches the multicall"
    - "The simulated page renders SIMULADO badge above the fold next to RiskCallout at 360px"
    - "The simulated page mounts SnapshotPoolPanel + CashFlowWaterfall + schematic PayoffDiagram + WalletPanel readOnly"
    - "All new instruments.* i18n keys exist in BOTH es-CO and en, authored (not machine-translated)"
    - "GitBook page is published from docs/book/ and docs/book/ is excluded from Velite globbing (no build perturbation)"
    - "Native-reviewer sign-off for the new copy is recorded in docs/copy-review.md"
  artifacts:
    - path: ".gitbook.yaml"
      provides: "GitBook root pointing at docs/book/"
      contains: "root: ./docs/book/"
    - path: "docs/book/apps/abrigo/instruments/ccop-usd-long-gamma.md"
      provides: "module documentation page"
      min_lines: 20
    - path: "messages/es-CO/instruments.json"
      provides: "es-CO new keys (simulated, provenance, cashflow, params, wallet.read_only)"
      contains: "simulated"
  key_links:
    - from: "app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx"
      to: "lib/apps/abrigo/fixture.ts"
      via: "simulated branch builds from FIXTURES[fixtureKey], never aggregateAllChains"
      pattern: "kind === 'simulated'"
    - from: "app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx"
      to: "components/defi/WalletPanel.tsx"
      via: "WalletPanel readOnly passed for simulated"
      pattern: "readOnly"
---

<objective>
Wire the simulated page branch and author all new copy. The detail page gains a `kind === 'simulated'` branch placed BEFORE any aggregator call (a simulated entry is never passed to aggregateAllChains/getInstrumentPoolState/multicall), mounting SIMULADO + SnapshotPoolPanel + CashFlowWaterfall + schematic PayoffDiagram + WalletPanel readOnly. Add the new es-CO-first/en i18n keys, the GitBook documentation page (verifying Velite non-collision), and record the native-reviewer copy sign-off.

Purpose: this is the user-visible read-first simulated surface; it must be honest (SIMULADO above fold, em-dash-never-0, read-only wallet) and authored in both locales.
Output: wired page + both message files + GitBook page + Velite-collision test + copy-review row.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-RESEARCH.md
@docs/superpowers/specs/2026-06-02-ccop-usd-long-gamma-instrument-frontend-design.md
@CLAUDE.md

<interfaces>
<!-- page.tsx current flow (after Wave 1/2 minimal edits):
  const instrument = ABRIGO_INSTRUMENTS.find(...)  // union
  if (!instrument) notFound()
  const aggregatorResults = await aggregateAllChains()   // simulated branch must come BEFORE this
  const poolState = getInstrumentPoolState(...) -->

<!-- Wave-2 components to mount (RSC except WalletPanel/PayoffDiagramClient which are client):
  SimuladoBadge({ label, ariaLabel })                          components/defi/ProvenanceBadge
  ProvenancePill({ tier, ariaLabel, label })
  SnapshotPoolPanel({ fixture, labels, pillStrings, locale })  components/defi/SnapshotPoolPanel
  CashFlowWaterfall({ breakdown, labels, specPillStrings, locale }) components/defi/CashFlowWaterfall
  PayoffDiagramClient({ data, strikeRef?, currentPriceRef?, ariaLabel, locale, isSchematic })
  WalletPanel({ strings, readOnly })   readOnly forces READ_ONLY -->

<!-- fixture.ts: FIXTURES['ccop-usd-long-gamma'] -> LongGammaFixture; cashflow.ts: CashFlowBreakdown + computeResidual -->

<!-- EXISTING es-CO instruments.json keys (DO NOT break): index.*, risk.*,
  params.{id,chain,strike,slope,deployed_at,name}, wallet.{disconnected_prompt,connect_label,
  connecting_label,wrong_chain_label,wrong_chain_explanation,switch_network_label,
  connected_ready_label,status_*}, pool.*, errors.* -->

<!-- velite.config.ts: root: 'content', pattern 'research/*.{es,en}.mdx' -- docs/book/ is OUTSIDE content/, no collision. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New instruments.* i18n keys (es-CO authored first, en mirrored)</name>
  <read_first>
    - messages/es-CO/instruments.json (the file being modified)
    - messages/en/instruments.json (the file being modified — must mirror es-CO key structure)
    - docs/superpowers/specs/...-frontend-design.md (section 7 enumerated keys)
    - tests/unit/i18n-coverage.test.ts (the parity test gating es-CO/en key-set equality)
  </read_first>
  <action>
    Author NEW keys in `messages/es-CO/instruments.json` FIRST (primary, hand-authored — no machine translation), then mirror the EXACT key structure into `messages/en/instruments.json`. Add under the existing `instruments` object (keep all existing keys):
    - `simulated`: badge "SIMULADO"; badge_aria "Base fork — pool simulado, sin despliegue en cadena"; caption "Base fork — mock pool, sin despliegue."
    - `provenance`: fork_fixture "Fork fixture"; fork_fixture_aria "Valor sembrado en pruebas sobre pool simulado — no es dato de mercado"; spec "Especificación"; spec_aria "Especificado en planes Phase-8 de abrigo-somnia — aún no está en cadena"; schematic "Esquemático"; schematic_aria "Forma ilustrativa — no es una función de liquidación derivada del contrato"
    - `cashflow`: heading "Flujo de caja"; premium "Prima (depósito)"; streamia "Streamia"; commission "Comisión"; data_cost "Costo de datos medido"; residual "Residual reclamable"; already_netted "ya neteado en el colateral sobreviviente"
    - `params` additions (do NOT reuse strike/slope for simulated): chunk_strike "Strike del chunk (offset)"; width "Ancho del chunk"; tick_spacing "Tick spacing"; pair "Par"; fork_block "Bloque de fork"; seeded_liquidity "Liquidez sembrada"; human_rate "Tasa (semilla)"; fork_params_caption "Parámetros de prueba de fork — no son geometría elegida"
    - `wallet` additions: read_only_status "Solo lectura"; read_only_label "sin transacción — fork simulado"

    en mirrors EXACTLY the same key paths with author-quality English (simulated.badge "SIMULATED"; simulated.caption "Base fork — mock pool, not deployed."; wallet.read_only_label "no transactions — simulated fork"; provenance.schematic_aria "Illustrative shape — not a contract-derived settlement function"; cashflow.already_netted "already netted into surviving collateral"; params.fork_params_caption "Fork-test parameters — not chosen geometry"). No key may exist in one locale and not the other.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/i18n-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/i18n-coverage.test.ts` GREEN (es-CO/en parity maintained)
    - `node -e "const a=require('./messages/es-CO/instruments.json').instruments;const b=require('./messages/en/instruments.json').instruments;for(const g of ['simulated','provenance','cashflow']){if(JSON.stringify(Object.keys(a[g]).sort())!==JSON.stringify(Object.keys(b[g]).sort()))process.exit(1)}"` exits 0
    - `grep -n "read_only_label" messages/es-CO/instruments.json messages/en/instruments.json` matches both files
    - existing keys preserved: `grep -n "wrong_chain_explanation" messages/es-CO/instruments.json` still matches
  </acceptance_criteria>
  <done>New simulated/provenance/cashflow/params/wallet-read-only keys exist in both locales with identical key paths; es-CO authored first; i18n-coverage parity test green.</done>
</task>

<task type="auto">
  <name>Task 2: Detail-page simulated branch (kind guard before aggregator)</name>
  <read_first>
    - app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx (the file being modified)
    - components/defi/ProvenanceBadge.tsx (SimuladoBadge/ProvenancePill from Wave 2)
    - components/defi/SnapshotPoolPanel.tsx, components/defi/CashFlowWaterfall.tsx (Wave 2)
    - lib/apps/abrigo/fixture.ts, lib/apps/abrigo/cashflow.ts, lib/apps/abrigo/payoff.ts (Wave 1)
    - docs/superpowers/specs/...-frontend-design.md (section 5 detail page)
  </read_first>
  <action>
    Add the simulated branch in `page.tsx` IMMEDIATELY after `if (!instrument) notFound()` and BEFORE `await aggregateAllChains()`:

    ```
    if (instrument.kind === 'simulated') {
      const fixture = FIXTURES[instrument.fixtureKey]
      // build i18n strings (locale, t = getTranslations('instruments'))
      // payoffData = generateSchematicConvexPayoff(fixture.pool.humanRate.value, /* premium */ 0)
      // breakdown: CashFlowBreakdown from fixture.cashflow
      return ( /* simulated layout */ )
    }
    // LIVE path unchanged below (aggregateAllChains + getInstrumentPoolState + existing layout)
    ```

    The simulated layout (mirroring the live layout structure for visual parity):
    - `&lt;InstrumentJsonLd instrument={instrument} /&gt;` (Wave-1 simulated branch already emits simulated:true + provenance, no strike/slope/address).
    - `&lt;h1&gt;` displayName.
    - A header block placing the SIMULADO badge NEXT TO RiskCallout, both above the fold at 360px (first content after h1): `&lt;SimuladoBadge label={t('simulated.badge')} ariaLabel={t('simulated.badge_aria')} /&gt;` + caption `t('simulated.caption')` + the existing `&lt;RiskCallout heading={t('risk.heading')} body={t('risk.body')} /&gt;`. Keep them in the `mb-6`/above-the-fold region; do NOT push SIMULADO below the fold.
    - Left column: `&lt;InstrumentParams instrument={instrument} .../&gt;` (Wave-1 simulated rows), then the schematic `&lt;PayoffDiagramClient data={payoffData} ariaLabel={locale.startsWith('es') ? 'Diagrama de rentabilidad esquemático — gamma positiva' : 'Schematic payoff diagram — positive gamma'} locale={locale} isSchematic /&gt;` (NO strikeRef/currentPriceRef — a schematic has no contract strike), then `&lt;SnapshotPoolPanel fixture={fixture} labels={...} pillStrings={{fork_fixture: t('provenance.fork_fixture'), fork_fixture_aria: t('provenance.fork_fixture_aria')}} locale={locale} /&gt;`, then `&lt;CashFlowWaterfall breakdown={breakdown} labels={...} specPillStrings={{label: t('provenance.spec'), aria: t('provenance.spec_aria')}} locale={locale} /&gt;`.
    - Right column: `&lt;WalletPanel strings={walletStrings} readOnly /&gt;` (walletStrings must now include readOnlyLabel: t('wallet.read_only_label') and statusLabels.READ_ONLY: t('wallet.read_only_status')).
    - A simulated entry is NEVER passed to aggregateAllChains/getInstrumentPoolState — pool display is built entirely from `fixture`.
    Reconcile the Wave-2 minimal edit: the live path keeps its existing PayoffDiagramClient `data=` call from Wave 2.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm biome check . &amp;&amp; pnpm vitest run tests/unit/agent-jsonld.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "kind === 'simulated'" app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx` matches, and the line number is BEFORE the `aggregateAllChains` line number (`grep -n "aggregateAllChains" ...` is a LATER line)
    - `grep -n "SimuladoBadge" app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx` matches
    - `grep -n "WalletPanel strings={walletStrings} readOnly" app/(defi)/...page.tsx` (or readOnly on the WalletPanel) matches inside the simulated branch
    - `grep -n "SnapshotPoolPanel\|CashFlowWaterfall" app/(defi)/...page.tsx` both match
    - the simulated branch does NOT call aggregateAllChains/getInstrumentPoolState (those appear only after the `return` in the live path)
    - `pnpm typecheck` + `pnpm biome check .` pass
  </acceptance_criteria>
  <done>page.tsx branches on simulated before the aggregator, mounts SIMULADO+SnapshotPoolPanel+CashFlowWaterfall+schematic PayoffDiagram+WalletPanel readOnly; the simulated entry never reaches the multicall; tsc+biome green.</done>
</task>

<task type="auto">
  <name>Task 3: GitBook page + .gitbook.yaml + Velite non-collision test</name>
  <read_first>
    - velite.config.ts (root: 'content' — confirm docs/book/ is outside it)
    - tests/unit/velite-research-schema.test.ts (the file being modified — add the no-collision assertion)
    - docs/superpowers/specs/...-frontend-design.md (section 8 GitBook)
    - CLAUDE.md (Velite/prebuild rules)
  </read_first>
  <action>
    Create `.gitbook.yaml` at the repo root:
    ```
    root: ./docs/book/
    structure:
      readme: README.md
      summary: SUMMARY.md
    ```
    Create `docs/book/README.md` (short landing: "DS2P Labs — Abrigo module docs" intro paragraph).
    Create `docs/book/SUMMARY.md` (GitBook TOC):
    ```
    # Summary
    * [Introduction](README.md)
    * Apps
      * Abrigo
        * Instruments
          * [cCOP/USD Long-Gamma](apps/abrigo/instruments/ccop-usd-long-gamma.md)
    ```
    Create `docs/book/apps/abrigo/instruments/ccop-usd-long-gamma.md` (the module page, &gt;= 20 lines, authored prose — Claude's discretion on wording): cover what the instrument is (cCOP/USD long-gamma convex hedge on borrowed Panoptic V2), the three provenance tiers (fork-fixture / spec / schematic) and that there is NO fork-verified tier, the schematic payoff (illustrative, not a settlement function), the corrected cash-flow (residual = max(surviving − dataCost, 0); streamia/commission already netted), the fork-only/mock caveat (Base fork, MockCcop/USDC, no deployment, read-only), and a link to the live route `/apps/abrigo/instruments/ccop-usd-long-gamma/8453`. Internal `docs/superpowers/` stays OUT of the GitBook root (it is not under docs/book/).

    Extend `tests/unit/velite-research-schema.test.ts` with an assertion that the Velite research glob does NOT match `docs/book/**`: assert the config `root` is `'content'` and the research collection pattern starts with `research/` (i.e. is scoped under content/, never `docs/`). A simple assertion: the pattern string does not contain `docs/book` and root === 'content'. This proves DEFI-09 non-collision without running a build.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/velite-research-schema.test.ts &amp;&amp; pnpm velite build &amp;&amp; node -e "const fs=require('fs');if(!fs.existsSync('docs/book/apps/abrigo/instruments/ccop-usd-long-gamma.md'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "root: ./docs/book/" .gitbook.yaml` matches
    - `docs/book/apps/abrigo/instruments/ccop-usd-long-gamma.md` exists and is &gt;= 20 lines (`wc -l` &gt;= 20)
    - `pnpm velite build` succeeds and does NOT emit any entry sourced from docs/book/ (the .velite output references only content/research)
    - `pnpm vitest run tests/unit/velite-research-schema.test.ts` GREEN with the new no-collision assertion
    - the module page contains "fork-fixture", "schematic", "residual", and the live route path
  </acceptance_criteria>
  <done>GitBook published from docs/book/ (.gitbook.yaml + README + SUMMARY + module page); velite build unperturbed; velite-research-schema test asserts no docs/book collision.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Native-reviewer copy sign-off (es-CO/en) recorded in docs/copy-review.md</name>
  <action>Review the new es-CO/en instruments.* copy for native quality and record the native-reviewer sign-off row in docs/copy-review.md. See &lt;how-to-verify&gt; for the exact steps.</action>
  <what-built>All new es-CO/en instruments.* copy (simulated badge, provenance tier names + aria sentences, cashflow labels, fork-test param labels, wallet read-only copy). Per CLAUDE.md, all copy is authored es-CO-first with native-reviewer sign-off recorded in docs/copy-review.md before the route ships (CROSS-10).</what-built>
  <how-to-verify>
    1. Review the new keys in messages/es-CO/instruments.json for native Colombian-Spanish quality (no machine-translation artifacts, no AI slop, tone matches the lab's anti-fishing discipline).
    2. Confirm the en mirror reads as author-quality English, not a literal gloss.
    3. Append a row to docs/copy-review.md recording the reviewer, date, scope (instruments.simulated/provenance/cashflow/params/wallet.read_only), and verdict.
  </how-to-verify>
  <acceptance_criteria>
    - docs/copy-review.md has a new dated row covering the Phase 05.1 instruments keys with a sign-off verdict
  </acceptance_criteria>
  <resume-signal>Type "approved" once the native-reviewer sign-off for the new copy is recorded in docs/copy-review.md, or list the copy edits needed.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm typecheck` + `pnpm biome check .` green
- `pnpm vitest run tests/unit/i18n-coverage.test.ts tests/unit/velite-research-schema.test.ts tests/unit/agent-jsonld.test.tsx` green
- `pnpm velite build` succeeds; docs/book/ produces no Velite entries
- simulated branch precedes aggregateAllChains; SIMULADO + read-only wallet wired
- docs/copy-review.md sign-off recorded
</verification>

<success_criteria>
The simulated page renders the full honest surface (SIMULADO above fold, SnapshotPoolPanel + CashFlowWaterfall + schematic payoff + read-only wallet) without ever calling the multicall; both locales carry authored copy with native sign-off; the GitBook page is published and provably does not perturb Velite/prebuild. Wave 4 can run the full suite + Evidence Collector.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-03-SUMMARY.md`
</output>

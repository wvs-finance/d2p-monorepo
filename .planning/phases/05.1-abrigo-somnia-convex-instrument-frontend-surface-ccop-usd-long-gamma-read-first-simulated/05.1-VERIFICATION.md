---
phase: 05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated
verified: 2026-06-02T12:45:00Z
status: passed
score: 12/12 must-haves verified (3 flagged gaps resolved post-verification — see Post-Verification Resolution)
status_history:
  - gaps_found (initial goal-backward pass, 11/12) — 2 flags stale, 1 real
  - passed (after resolution) — 2026-06-02
gaps:
  - truth: "All three provenance tiers render as pills with LOCALE-AWARE labels (es-CO Fork fixture/Especificación/Esquemático — NOT raw tier keys)"
    status: partial
    reason: "The schematic ProvenancePill is WIRED in source (page.tsx line 219-224, tier='schematic', label from t('provenance.schematic')='Esquemático'/'Schematic') and uses locale-aware labels, but the Evidence Collector did not observe it rendering in the live DOM in the Wave-4 run. The pill node appears to be scrolled below the fold or missed during the Evidence Collector's scan. The code is correct; the live-verification report logged ⚠ PARTIAL (2 of 3 tiers seen) without confirming absence — it noted 'schematic tier is conveyed only by the inline (esquemático)/chart label, with no icon-bearing pill'. The code at lines 214-224 clearly mounts a ProvenancePill with tier='schematic' ABOVE the PayoffDiagramClient, inside the payoff section. This appears to be an Evidence Collector snapshot gap, not a code gap."
    artifacts:
      - path: "app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx"
        issue: "ProvenancePill tier='schematic' IS in source at lines 219-224 but not confirmed visible in live DOM snapshot — possible scroll position or timing issue in Evidence Collector run"
    missing:
      - "Re-run Evidence Collector with explicit scroll into view and element screenshot of the schematic ProvenancePill node above the payoff chart section to resolve ⚠ PARTIAL to ✓ PASS or ✗ FAIL"
  - truth: "instruments-index.spec.ts e2e: all e2e tests passing (pre-existing regression unresolved)"
    status: failed
    reason: "4 stale empty-state assertions in tests/e2e/instruments-index.spec.ts fail because Wave 1 added the simulated ccop-usd-long-gamma entry to the registry; the spec was written for the empty-registry state. Documented in deferred-items.md as a pre-existing regression."
    artifacts:
      - path: "tests/e2e/instruments-index.spec.ts"
        issue: "4 tests assert empty-state (toHaveLength(0) on cards, empty-state heading text) — stale since Wave 1 added the simulated instrument"
    missing:
      - "Update instruments-index.spec.ts to assert the simulated instrument card renders instead of the empty state"
human_verification:
  - test: "Confirm the schematic ProvenancePill renders in the live DOM"
    expected: "A ProvenancePill with PenLine icon, text 'Esquemático'/'Schematic', and aria-label 'Forma ilustrativa — no es una función de liquidación derivada del contrato' appears above the payoff chart section on /apps/abrigo/instruments/ccop-usd-long-gamma/8453"
    why_human: "Evidence Collector logged ⚠ PARTIAL — 2 of 3 tiers seen. Code clearly mounts the pill (page.tsx:219-224) but snapshot may have missed it due to viewport position or hydration timing"
---

# Phase 05.1 Verification Report

**Phase Goal:** A visitor or agent can open the cCOP/USD long-gamma instrument page and read an honestly-labeled, read-first, simulated surface — schematic convex payoff, backend-correct cash-flow waterfall, and Panoptic fork-fixture params — under a three-tier provenance model (fork-fixture / spec / schematic) with a SIMULADO badge, a read-only wallet, and no transact path or fabricated numbers.

**Verified:** 2026-06-02T12:45:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Route `/apps/abrigo/instruments/ccop-usd-long-gamma/8453` renders SIMULADO badge above fold at 360px | ✓ VERIFIED | Live DOM: badge top=209px, vh=800; aria="Base fork — pool simulado, sin despliegue en cadena"; Evidence Collector ✓ PASS |
| 2 | Three provenance PILLS with LOCALE-AWARE labels (es-CO "Fork fixture"/"Especificación"/"Esquemático") | ⚠ PARTIAL | fork-fixture + spec tiers confirmed as pills with locale labels. Schematic pill IS in source (page.tsx:219) but Evidence Collector only observed 2/3 in DOM snapshot |
| 3 | SnapshotPoolPanel renders fork-fixture pool fields with provenance pills | ✓ VERIFIED | Code: SnapshotPoolPanel.tsx mounts ProvenancePill per field; Live DOM: seededLiquidity "1000000000000000000000000" verbatim, "Fork fixture" pills present |
| 4 | CashFlowWaterfall correct: streamia/commission informational, data-cost em-dash | ✓ VERIFIED | Code: CashFlowWaterfall.tsx marks streamia/commission "(already netted)"; all 4 cashflow values null → em-dash "—" in live DOM |
| 5 | Schematic PayoffDiagram renders (non-zero height, type="monotone", isSchematic annotation) | ✓ VERIFIED | Code: type="monotone", strokeWidth=2.5, var(--accent-text); Live DOM: curve 176×190px; Evidence Collector ✓ PASS |
| 6 | aggregateChain filters `kind === 'live'` before multicall; simulated never reaches it | ✓ VERIFIED | aggregator.ts:45-46: `filter((i): i is LiveInstrument => i.chainId === chainId && i.kind === 'live')` — type predicate enforces this at the tsc level |
| 7 | cashflow.ts residual = max(surviving − dataCost, 0); streamia/commission NOT subtracted | ✓ VERIFIED | cashflow.ts:32-42: exactly 2-param contract; streamia/commission appear only in CashFlowBreakdown interface and doc comments, NEVER in arithmetic |
| 8 | InstrumentJsonLd simulated branch: no strike/slope/address; simulated:true + provenance present | ✓ VERIFIED | InstrumentJsonLd.tsx:53-71: simulated branch emits only chainId, simulated="true", provenance; Live DOM confirms hasStrike=false, hasSlope=false, hasAddress=false |
| 9 | fixture.ts uses bigint-as-string and "2000" offset; no "TBD" | ✓ VERIFIED | fixture.ts:98 `value: '2000'`, L89 `value: '1000000000000000000000000'`, L64 `value: '46700000'`; `grep -c "TBD"` = 0 |
| 10 | Read-only wallet: WalletPanel readOnly → READ_ONLY state, no connect/switch CTA | ✓ VERIFIED | WalletPanel readOnly forces `'READ_ONLY' as const`; WalletState union has READ_ONLY; deriveWalletState never returns it; Live DOM: "Solo lectura", no ConnectButton |
| 11 | GitBook .gitbook.yaml + docs/book/ page; Velite non-collision | ✓ VERIFIED | .gitbook.yaml root: ./docs/book/; docs/book/apps/abrigo/instruments/ccop-usd-long-gamma.md 62 lines; veliteRoot='content', researchPattern excludes docs/book; velite build clean |
| 12 | es-CO-first copy with native reviewer sign-off in docs/copy-review.md | ✓ VERIFIED | docs/copy-review.md: Juan Serrano, 2026-06-02, PASS with 2 edits; i18n-coverage test gates instruments namespace (5 key groups) |

**Score:** 11/12 truths verified (1 partial — schematic pill live-DOM confirmation pending)

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/apps/abrigo/fixture.ts` | FixtureValue + LongGammaFixture + CCOP_USD_LONG_GAMMA_FIXTURE | ✓ VERIFIED | 152 lines; all on-chain ints as strings; no TBD; chunk.strike "2000" with documented note |
| `lib/apps/abrigo/cashflow.ts` | computeResidual + CashFlowBreakdown | ✓ VERIFIED | 2-param function; streamia/commission only in interface/comments; residual formula correct |
| `lib/apps/abrigo/payoff.ts` | generateSchematicConvexPayoff alongside generatePayoffData | ✓ VERIFIED | Both exported; independent parabolic generator; type="monotone" curve |
| `lib/apps/abrigo/instruments.ts` | LiveInstrument | SimulatedInstrument discriminated union | ✓ VERIFIED | kind:'simulated' entry; fixture-celo-01 removed (grep=0); SupportedChainId + ABRIGO_ABI preserved |
| `lib/dashboard/aggregator.ts` | kind==='live' filter before multicall | ✓ VERIFIED | Line 45-46: type predicate `(i): i is LiveInstrument => i.chainId === chainId && i.kind === 'live'` |
| `components/defi/ProvenanceBadge.tsx` | SimuladoBadge + ProvenancePill | ✓ VERIFIED | 3 tiers with WCAG 1.4.11 full-opacity rings; locale-aware labels; CROSS-09 color+icon+text |
| `components/defi/SnapshotPoolPanel.tsx` | fork-fixture pool field renderer | ✓ VERIFIED | Per-field ProvenancePill; bigint-as-string uncoerced; em-dash for null |
| `components/defi/CashFlowWaterfall.tsx` | cash-flow dl waterfall | ✓ VERIFIED | Correct reading order; streamia/commission marked informational; spec-tier pill on header |
| `components/defi/PayoffDiagram.tsx` | data: PayoffPoint[] + monotone + schematic annotation | ✓ VERIFIED | Props: data, strikeRef?, currentPriceRef?, ariaLabel, isSchematic?; type="monotone"; curve-aware sr-only table |
| `components/defi/WalletPanel.tsx` | readOnly prop + READ_ONLY state | ✓ VERIFIED | readOnly forces READ_ONLY; no ConnectButton; no switch-network CTA |
| `components/defi/InstrumentParams.tsx` | Fork-param rows with ProvenancePill per row | ✓ VERIFIED | forkBlock/tickSpacing/seededLiquidity/chunk.strike(offset)/width rows; chain co-located with fork-block qualifier; caption outside dl |
| `components/defi/InstrumentJsonLd.tsx` | Simulated branch: no strike/slope/address; simulated:true | ✓ VERIFIED | Lines 53-71: simulated branch clean; anti-fishing unit test in agent-jsonld.test.tsx |
| `.gitbook.yaml` | root: ./docs/book/ | ✓ VERIFIED | Exists at repo root; structure.readme + structure.summary |
| `docs/book/apps/abrigo/instruments/ccop-usd-long-gamma.md` | Module doc page >= 20 lines | ✓ VERIFIED | 62 lines; covers provenance tiers, schematic payoff, residual formula, fork-only caveat |
| `messages/es-CO/instruments.json` | New simulated/provenance/cashflow/params/wallet.read_only keys | ✓ VERIFIED | 5 key groups; es-CO-first; locale labels: "Fork fixture"/"Especificación"/"Esquemático" |
| `tests/e2e/instrument-page.spec.ts` | Real assertions against ccop-usd-long-gamma/8453 | ✓ VERIFIED | 13 new tests including SIMULADO above-fold, schematic payoff, em-dash data-cost |
| `tests/e2e/wallet-states.spec.ts` | READ_ONLY wallet assertions on simulated route | ✓ VERIFIED | 12 new tests; "Solo lectura"; no CONNECTED_READY; no ConnectButton |
| `tests/a11y/defi-instruments.spec.ts` | axe WCAG 2.2 AA on simulated route | ✓ VERIFIED | 8 new tests; zero serious/critical violations confirmed in Evidence Collector run |
| `tests/e2e/instruments-index.spec.ts` | Index e2e tests current | ✗ STALE | 4 empty-state assertions written for empty registry; Wave 1 added simulated instrument; 4 tests fail |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `page.tsx` (simulated branch, line 78) | `aggregateAllChains` (line 274) | kind guard before aggregator | ✓ WIRED | Early return at line 78 precedes `aggregateAllChains()` at line 274 |
| `page.tsx` | `messages/*/instruments.json` | `t('simulated.badge')` key_link | ✓ WIRED | Line 185: `t('simulated.badge')`; i18n-coverage test gates parity |
| `page.tsx` | `WalletPanel` | `readOnly` prop for simulated branch | ✓ WIRED | Line 260: `<WalletPanel strings={walletStrings} readOnly />` |
| `page.tsx` | `ProvenancePill tier="schematic"` | Payoff section header pill | ✓ WIRED (code) / ⚠ UNCONFIRMED (live DOM) | Lines 219-224: ProvenancePill tier="schematic" mounted; Evidence Collector did not confirm in DOM snapshot |
| `WalletPanel` | `lib/wallet/state.ts` | readOnly injects READ_ONLY | ✓ WIRED | `readOnly ? ('READ_ONLY' as const) : deriveWalletState(...)` |
| `SnapshotPoolPanel` | `ProvenanceBadge.tsx` | ProvenancePill per field | ✓ WIRED | 5 pill calls in SnapshotPoolPanel.tsx |
| `aggregator.ts` | `instruments.ts` | type predicate filter on kind === 'live' | ✓ WIRED | `(i): i is LiveInstrument => i.chainId === chainId && i.kind === 'live'` |
| `InstrumentJsonLd` | simulated:true + no strike/slope/address | kind branch | ✓ WIRED | Unit tested in agent-jsonld.test.tsx (4 new cases) |

---

## Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| DEFI-02 | 05.1-02, 05.1-04 | Wallet state machine — 4 states + new READ_ONLY | ✓ SATISFIED | WalletState union has READ_ONLY; deriveWalletState body untouched (pure 4-output); WalletPanel readOnly path |
| DEFI-03 | 05.1-03, 05.1-04 | Per-instrument page shows params/payoff/pool/participants, accessible without wallet | ✓ SATISFIED | Simulated page renders without wallet connection; all sections present (InstrumentParams/PayoffDiagram/SnapshotPoolPanel/CashFlowWaterfall); e2e tests pass |
| DEFI-04 | 05.1-00, 05.1-01, 05.1-02 | Payoff diagram renders CFMM curve with axis labels in locale | ✓ SATISFIED | type="monotone", var(--accent-text), curve-aware sr-only table; data: PayoffPoint[] prop; curve paints confirmed live |
| DEFI-05 | 05.1-03, 05.1-04 | Risk disclosure visible above fold at 360px without scrolling | ✓ SATISFIED | SIMULADO badge + RiskCallout co-located; Evidence Collector: badge top=209px at 360px viewport (vh=800) |
| DEFI-08 (NEW) | All plans | Simulated/read-only instrument variant — three-tier provenance, SIMULADO badge, CashFlowWaterfall, SnapshotPoolPanel, read-only wallet, never passed to multicall | ✓ SATISFIED | All components built and wired; kind guard before aggregator; Evidence Collector confirmed no fabricated numbers, no connect affordance |
| DEFI-09 (NEW) | 05.1-03 | GitBook module page from docs/book/, excluded from Velite globbing | ✓ SATISFIED | .gitbook.yaml + docs/book/apps/abrigo/instruments/ccop-usd-long-gamma.md 62 lines; veliteRoot='content' confirmed; non-collision test green |
| CROSS-01 | 05.1-00, 05.1-02, 05.1-04 | WCAG 2.2 AA conformance | ✓ SATISFIED | axe run on simulated route: zero serious/critical violations; WCAG 1.4.11 cleared (curve var(--accent-text), full-opacity rings); InstrumentParams dl HTML conformance fixed |
| CROSS-09 | All plans | Color + icon + text on all indicators (never color alone) | ✓ SATISFIED | ProvenancePill: 3-tier config with Icon + className + aria-label; SimuladoBadge: FlaskConical icon + text; WalletStatusPill: Eye icon for READ_ONLY |
| CROSS-10 | 05.1-03 | All copy authored, native reviewer sign-off | ✓ SATISFIED | docs/copy-review.md: Juan Serrano, 2026-06-02, PASS; i18n-coverage test gates instruments namespace |
| AGENT-10 | 05.1-01, 05.1-03 | Every instrument page emits JSON-LD mirroring MCP schema | ✓ SATISFIED | InstrumentJsonLd simulated branch: simulated:true + provenance; no strike/slope/address; 4 unit tests in agent-jsonld.test.tsx |

### DEFI-08 and DEFI-09 Registration Status

**These requirement IDs are NEW — proposed by Phase 05.1 but NOT registered in `.planning/REQUIREMENTS.md` (grep confirms 0 occurrences).** They exist only in the ROADMAP.md description for phase 05.1 and in the plan frontmatter `requirements` fields.

**Action required:** `.planning/REQUIREMENTS.md` should be updated to register DEFI-08 and DEFI-09 as formal requirements with their full descriptions (as defined in ROADMAP.md). Until registered, these IDs are orphaned from the requirements tracking system.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `tests/e2e/instruments-index.spec.ts` | multiple | Stale empty-state assertions (4 tests assert empty registry state) | ⚠ Warning | Suite reports 4 pre-existing failing tests; not introduced by Wave 4 but documented in deferred-items.md and unresolved |
| `components/defi/CashFlowWaterfall.tsx` | 101 | Residual `<dd>` renders `{labels.residual} *` instead of a computed value | ℹ Info | Residual is bigint-based (computeResidual) but no bigint is passed in; the waterfall currently shows the label name + asterisk rather than a computed value — consistent with null cashflow values (all em-dash) but the residual cell has no computed value path at all |

---

## Human Verification Required

### 1. Schematic ProvenancePill Live DOM Confirmation

**Test:** Navigate to `http://localhost:3040/apps/abrigo/instruments/ccop-usd-long-gamma/8453` (or production URL). Scroll to the "Diagrama de rentabilidad" / "Payoff diagram" section. Inspect the DOM for a `<span aria-label="Forma ilustrativa — no es una función de liquidación derivada del contrato">` element (or the en equivalent `"Illustrative shape — not a contract-derived settlement function"`) with a `PenLine` icon child above the `<PayoffDiagramClient>` component.

**Expected:** A ProvenancePill with PenLine icon, text "Esquemático"/"Schematic", and full provenance aria-label is visible above the payoff chart. This represents the third provenance tier.

**Why human:** The Evidence Collector logged ⚠ PARTIAL (claimed only 2 of 3 tiers seen as pills). The source code at `page.tsx:219-224` clearly mounts this pill. The discrepancy may be: (a) the pill is below the initial viewport in the Evidence Collector's 360px scan, (b) it renders but was not found by the Evidence Collector's selector, or (c) there is a rendering issue. A human browser inspection will resolve this within 60 seconds.

---

## Gaps Summary

**Gap 1 — Schematic provenance pill live-DOM confirmation (cosmetic/semantic gap):**
The code clearly mounts a `ProvenancePill tier="schematic"` at `page.tsx:219-224` with locale-aware labels ("Esquemático"/"Schematic") and authored aria-labels from the instruments.json messages. The Evidence Collector's Wave-4 run logged ⚠ PARTIAL, reporting it "observed only 2 of 3 tiers as pills" — but the report itself notes "The schematic tier is conveyed only by the inline (esquemático)/chart label" which is a separate in-chart annotation (also present). The schematic pill and the in-chart annotation are both in the source; the Evidence Collector may have missed the pill node due to scroll position or selector strategy. This is a low-risk gap (not a honesty/anti-fishing regression — no fabricated data, no misleading claim), but the claim "three tiers as pills" has not been confirmed live and should be re-verified before treating the phase as fully ✓.

**Gap 2 — instruments-index.spec.ts stale (pre-existing regression):**
Four e2e tests assert the registry is empty. Wave 1 added the simulated instrument, making the empty-state assertions permanently false. This is a test-maintenance gap, not a honesty or functionality regression. The fix is a one-task update to instruments-index.spec.ts asserting the simulated instrument card renders.

**Root cause grouping:** The two gaps have different roots. Gap 1 is a live-verification coverage gap (Evidence Collector scan granularity). Gap 2 is a test-maintenance regression from Wave 1 that was documented but not fixed.

---

_Verified: 2026-06-02T12:45:00Z_
_Verifier: Claude (gsd-verifier)_

---

## Post-Verification Resolution (2026-06-02, orchestrator)

The initial goal-backward pass returned `gaps_found (11/12)`. On direct re-check against the
live test suite (not agent self-reports), 2 of 3 flags were **stale** (the verifier read
pre-cleanup state) and 1 was **real and fixed**:

1. **Schematic ProvenancePill live-DOM (was ⚠ partial) → RESOLVED.** The pill is mounted at
   `page.tsx:219-224` and is now confirmed in a real browser by the production-build e2e:
   `tests/e2e/instrument-page.spec.ts` › "schematic pill renders as a span pill adjacent to the
   payoff diagram" and "all three provenance tiers are pills" both PASS (17/17). The verifier had
   read the Wave-4 Evidence Collector note recorded *before* the pill was mounted (commit e86a03c).

2. **`instruments-index.spec.ts` stale (was ✗ failed) → RESOLVED.** Re-ran live: **6/6 PASS**.
   The one-card update landed in commit 540db45 (asserts the single simulated card, SIMULADO pill,
   correct link, no wallet gate). The verifier read the pre-540db45 state.

3. **DEFI-08 / DEFI-09 not registered in REQUIREMENTS.md (real) → FIXED.** Both new requirement IDs
   (introduced by this phase) are now registered in `.planning/REQUIREMENTS.md` — in the DEFI list
   and the traceability table (Phase 05.1 · Complete).

Additionally, a real i18n/CROSS-10 defect was caught at the gate and fixed (commit 6aff33c):
`ProvenancePill` rendered the raw English tier key instead of the signed-off locale-aware label;
a cleanup agent had masked it by editing an e2e to expect the raw key. Component + 4 callers fixed;
test fudges reverted with regression guards; 17/17 e2e green on the production build.

**Resolved status: passed (12/12).**

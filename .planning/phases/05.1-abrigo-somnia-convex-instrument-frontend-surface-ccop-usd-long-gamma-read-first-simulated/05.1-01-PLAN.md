---
phase: 05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated
plan: 01
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - lib/apps/abrigo/fixture.ts
  - lib/apps/abrigo/payoff.ts
  - lib/apps/abrigo/cashflow.ts
  - lib/apps/abrigo/instruments.ts
  - lib/dashboard/aggregator.ts
  - app/(defi)/apps/abrigo/instruments/page.tsx
  - app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx
  - components/defi/InstrumentParams.tsx
  - components/defi/InstrumentJsonLd.tsx
  - tests/unit/instruments-index.test.ts
autonomous: true
requirements: [DEFI-08, DEFI-04, AGENT-10, CROSS-09]
must_haves:
  truths:
    - "fixture.ts exports a dated, provenance-tagged LongGammaFixture with bigint-as-string on-chain integers"
    - "payoff.ts exports a schematic convex (positive-gamma) generator returning PayoffPoint[] net of premium"
    - "cashflow.ts computes residual = max(survivingCollateral − wrapperMeteredDataCost, 0); streamia/commission never subtracted"
    - "AbrigoInstrument is a discriminated union on kind; simulated variant has no strike/slope/address"
    - "The temp fixture-celo-01 entry is removed; one kind:'simulated' ccop-usd-long-gamma entry exists"
    - "All FIVE consumers compile under tsc (detail page, InstrumentParams, InstrumentJsonLd, aggregator, index page) by narrowing on kind before reading strike/slope/address/deployedAt"
    - "aggregateChain filters ABRIGO_INSTRUMENTS on kind === 'live' BEFORE the multicall — a simulated entry is NEVER passed to multicall (honesty invariant)"
  artifacts:
    - path: "lib/apps/abrigo/fixture.ts"
      provides: "FixtureValue<T> + LongGammaFixture + CCOP_USD_LONG_GAMMA_FIXTURE"
      contains: "CCOP_USD_LONG_GAMMA_FIXTURE"
      min_lines: 40
    - path: "lib/apps/abrigo/cashflow.ts"
      provides: "computeResidual + CashFlowBreakdown type"
      contains: "computeResidual"
    - path: "lib/apps/abrigo/payoff.ts"
      provides: "generateSchematicConvexPayoff alongside generatePayoffData"
      contains: "generateSchematicConvexPayoff"
    - path: "lib/apps/abrigo/instruments.ts"
      provides: "LiveInstrument | SimulatedInstrument discriminated union"
      contains: "kind: 'simulated'"
  key_links:
    - from: "app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx"
      to: "lib/apps/abrigo/instruments.ts"
      via: "kind narrowing before reading instrument.strike"
      pattern: "instrument.kind === 'live'"
    - from: "lib/dashboard/aggregator.ts"
      to: "lib/apps/abrigo/instruments.ts"
      via: "filter on chainId AND kind === 'live' before constructing the multicall"
      pattern: "i.kind === 'live'"
    - from: "components/defi/InstrumentJsonLd.tsx"
      to: "lib/apps/abrigo/instruments.ts"
      via: "simulated branch omits strike/slope/address, emits simulated:true"
      pattern: "simulated"
---

<objective>
Build and FREEZE the data layer: the fork-fixture values, the schematic convex payoff generator, the backend-correct cash-flow, and the discriminated-union registry — then atomically narrow ALL FIVE consumer files so the whole tree compiles under the tsc pre-commit gate. This is the data-shape + discriminant freeze the spec mandates before any component work (spec §11 Wave 1).

Removes the temp `fixture-celo-01` entry (the Evidence Collector already re-verified the existing route in Wave 0, so removal no longer 404s anything that matters).

Purpose: downstream components depend on these shapes; freezing them first prevents scavenger-hunt churn.
Output: 4 lib files + 5 narrowed consumers, all green under `pnpm typecheck` + the Wave-0 fixture/cashflow stubs flipped to GREEN.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-RESEARCH.md
@docs/superpowers/specs/2026-06-02-ccop-usd-long-gamma-instrument-frontend-design.md
@CLAUDE.md

<interfaces>
<!-- CURRENT lib/apps/abrigo/instruments.ts (flat interface to migrate): -->
export interface AbrigoInstrument {
  id: string; name: string; nameEn: string
  chainId: SupportedChainId
  address: Address; deployedAt: string
  strike: number; slope: number
}
export const ABRIGO_INSTRUMENTS: AbrigoInstrument[] = [ /* temp fixture-celo-01 — REMOVE */ ]
<!-- SupportedChainId already includes base.id (8453). ABRIGO_ABI export must stay. -->

<!-- CURRENT lib/apps/abrigo/payoff.ts: -->
export interface PayoffPoint { price: number; payoff: number }
export function generatePayoffData(strike: number, slope: number, points = 100): PayoffPoint[]

<!-- CONSUMER 1 — detail page.tsx unsafe reads:  L116 const currentPrice = instrument.strike
     L150-155 <PayoffDiagramClient strike={instrument.strike} slope={instrument.slope} currentPrice={currentPrice} locale={locale} />
     generateMetadata (L41-49) only reads id/name/nameEn/chainId — SAFE, no change. -->

<!-- CONSUMER 2 — InstrumentParams.tsx rows (L29-62): name,id,chain,strike,slope,deployed_at — strike/slope/deployedAt are live-only. -->

<!-- CONSUMER 3 — InstrumentJsonLd.tsx (L34-48): emits strike, slope, address, chainId, deployedAt PropertyValues. -->

<!-- CONSUMER 4 — lib/dashboard/aggregator.ts (aggregateChain, L42, L62-65, L84):
     L42 const instruments = ABRIGO_INSTRUMENTS.filter((i) => i.chainId === chainId)
     L62-65 multicall reads inst.address; L84 reads inst.address again.
     After the kind:'simulated' entry (chainId 8453, NO address) lands, tsc errors on inst.address.
     FIX: change the filter to ALSO require i.kind === 'live' so only live instruments reach the multicall. -->

<!-- CONSUMER 5 — index listing page.tsx (app/(defi)/apps/abrigo/instruments/page.tsx, map branch ~L68-89):
     L79 reads instrument.deployedAt. After the simulated entry lands, tsc errors on deployedAt.
     FIX: guard on kind; simulated card shows SIMULADO pill + em-dash for deployedAt (CROSS-09). -->

<!-- Fork-fixture concrete values (resolved in Wave 0):
  forkBlock        "46700000"  (CcopUsdcPool.t.sol L27 BASE_FORK_BLOCK)            string / fork-fixture
  pool.humanRate   4000        (PoolKeyLib.sol L19 HUMAN_RATE)                     number / fork-fixture
  pool.tickSpacing "10"        (PoolKeyLib.sol L25 TICK_SPACING)                   string / fork-fixture
  pool.seededLiquidity "1000000000000000000000000" (CcopUsdcPool.t.sol L33, 1e6 ether) string / fork-fixture
  chunk.strike     "2000"      (+2000-tick OTM offset, tickSpacing-aligned; PanopticDataSeam.fork.t.sol L41,73) string / fork-fixture
  chunk.width      "2"         (08-RESEARCH Pattern 2; width=2 clears InvalidTickBound) string / fork-fixture
  pair.token0      "MockCcop"  pair.token1 "USDC"
  cashflow.{premium,streamia,commission,dataCost} — all spec tier; dataCost null (Phase 9 unbuilt) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fixture.ts (NEW) + cashflow.ts (NEW)</name>
  <read_first>
    - tests/unit/fixture.test.ts (Wave-0 stub — the contract to satisfy)
    - tests/unit/cashflow.test.ts (Wave-0 stub — the contract to satisfy)
    - docs/superpowers/specs/2026-06-02-ccop-usd-long-gamma-instrument-frontend-design.md (§4.1 FixtureValue/LongGammaFixture, §4.4 residual rule)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/test/instrument/CcopUsdcPool.t.sol (L27, L33 fork values)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/test/instrument/helpers/PoolKeyLib.sol (L19, L25 HUMAN_RATE/TICK_SPACING)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/.planning/phases/08-longgammawrapper-cash-flow/08-RESEARCH.md (§residual Pattern 7 — surviving already nets streamia/commission)
  </read_first>
  <action>
    Create `lib/apps/abrigo/fixture.ts`:
    - `export interface FixtureValue&lt;T&gt; { value: T; tier: 'fork-fixture' | 'spec'; source: string; note?: string }`
    - `export interface LongGammaFixture` per spec §4.1: `capturedFrom: string`, `forkBlock: FixtureValue&lt;string&gt;`, `pair: { token0: string; token1: string }`, `pool: { humanRate: FixtureValue&lt;number&gt;; tickSpacing: FixtureValue&lt;string&gt;; seededLiquidity: FixtureValue&lt;string&gt; }`, `chunk: { strike: FixtureValue&lt;string&gt;; width: FixtureValue&lt;string&gt; }`, `cashflow: { premium: FixtureValue&lt;number&gt;; streamia: FixtureValue&lt;number&gt;; commission: FixtureValue&lt;number&gt;; dataCost: FixtureValue&lt;number | null&gt; }`.
    - `export const CCOP_USD_LONG_GAMMA_FIXTURE: LongGammaFixture` with the EXACT values from the <interfaces> table. On-chain integers are bigint-AS-STRING (never JS number): forkBlock `'46700000'`, tickSpacing `'10'`, seededLiquidity `'1000000000000000000000000'`, chunk.strike `'2000'`, chunk.width `'2'`. humanRate is `4000` (number — display ratio, OK). Each value carries a `source` citing the file+line and a `note`:
        - chunk.strike note: "+2000-tick OTM offset from live fork tick, tickSpacing(10)-aligned ((currentTick+2000)/10)*10; no static absolute — fork-test artifact to clear InvalidTickBound"
        - pool.humanRate note: "author-chosen sqrtPriceX96 seed; ordering sanity check in [3000,5000]; NOT a market rate"
        - chunk.width note: "width=2 (2×tickSpacing=20 ticks) to clear InvalidTickBound; fork alignment artifact"
        - capturedFrom: "abrigo-somnia Phase-7 fork tests @ HEAD (hand-authored, not exported)"
    - cashflow tier values are all `'spec'`. dataCost.value is `null` (Phase 9 unbuilt) with note "metered data-cost; Phase 9 unbuilt → em-dash". premium/streamia/commission: use `null`-able? NO — keep them `FixtureValue&lt;number&gt;`; if no literal exists set a documented spec placeholder per spec §4.4 (premium = "Deposit amount per position open; specified in Phase-8 plans; not yet on-chain"). If a numeric literal is genuinely unknown, model that field's value type as `number | null` and set null — NEVER fabricate a number. Prefer dataCost null; for premium/streamia/commission, if Phase-8 tests have no literal, set value `null` and widen the type to `FixtureValue&lt;number | null&gt;` — em-dash at render. Do NOT invent figures.
    - Export `export const FIXTURES: Record&lt;string, LongGammaFixture&gt; = { 'ccop-usd-long-gamma': CCOP_USD_LONG_GAMMA_FIXTURE }` so the page can look up by fixtureKey.

    Create `lib/apps/abrigo/cashflow.ts`:
    - `export function computeResidual(survivingCollateral: bigint, wrapperMeteredDataCost: bigint | null): bigint` returning `wrapperMeteredDataCost === null ? survivingCollateral : (survivingCollateral &gt; wrapperMeteredDataCost ? survivingCollateral - wrapperMeteredDataCost : 0n)`. NOTE: streamia/commission are NOT parameters — they are already netted into survivingCollateral by the pool's share-burn (08-RESEARCH §residual). Add a doc comment citing `08-RESEARCH §residual` (NOT MATH.md).
    - `export interface CashFlowBreakdown { premium: FixtureValue&lt;number | null&gt;; streamia: FixtureValue&lt;number | null&gt;; commission: FixtureValue&lt;number | null&gt;; dataCost: FixtureValue&lt;number | null&gt;; residualNote: string }` (informational shape consumed by CashFlowWaterfall in Wave 2).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/fixture.test.ts tests/unit/cashflow.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/fixture.test.ts tests/unit/cashflow.test.ts` is GREEN (Wave-0 stubs now pass)
    - `grep -n "1000000000000000000000000" lib/apps/abrigo/fixture.ts` matches (bigint-as-string seeded liquidity)
    - `grep -nE "value: 4000|value: '4000'" lib/apps/abrigo/fixture.ts` — humanRate is the number 4000 (not a string)
    - `grep -n "computeResidual" lib/apps/abrigo/cashflow.ts` matches; the function takes exactly 2 params (surviving, dataCost) — `grep -n "streamia" lib/apps/abrigo/cashflow.ts` shows it only in comments/interface, NEVER inside computeResidual's arithmetic
    - No fabricated numbers: every value with an unknown literal is `null`, never a guessed figure; `grep -n "TBD" lib/apps/abrigo/fixture.ts` returns 0
  </acceptance_criteria>
  <done>fixture.ts and cashflow.ts exist; both Wave-0 stubs GREEN; bigint-as-string honored; residual = max(surviving − dataCost, 0) with streamia/commission never subtracted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: payoff.ts schematic generator + instruments.ts discriminated union</name>
  <read_first>
    - lib/apps/abrigo/payoff.ts (the file being modified — keep generatePayoffData)
    - lib/apps/abrigo/instruments.ts (the file being modified — flat interface + temp fixture)
    - tests/unit/payoff-curve.test.tsx (existing tests must keep passing; add schematic assertions)
    - tests/unit/instruments-index.test.ts (asserts ABRIGO_INSTRUMENTS length 0 — MUST be updated)
    - docs/superpowers/specs/...-frontend-design.md (§4.2 union, §4.3 schematic)
  </read_first>
  <action>
    Extend `lib/apps/abrigo/payoff.ts` — ADD (do not remove generatePayoffData):
    ```
    export function generateSchematicConvexPayoff(
      centerPrice: number, premium: number, curvature = 0.000005, points = 200,
    ): PayoffPoint[] {
      const lo = centerPrice * 0.3
      const hi = centerPrice * 1.7
      return Array.from({ length: points }, (_, i) => {
        const price = lo + (hi - lo) * (i / (points - 1))
        const raw = curvature * (price - centerPrice) ** 2 - premium
        return { price, payoff: Math.max(raw, 0) }
      })
    }
    ```
    It MUST NOT call generatePayoffData (independent generator). Add a doc comment: "Schematic convex (positive-gamma) shape, net of premium — illustrative, NOT a contract-derived settlement function."

    Migrate `lib/apps/abrigo/instruments.ts` to a discriminated union (keep ABRIGO_ABI and SupportedChainId exactly):
    ```
    export type LiveInstrument = { kind: 'live'; id: string; name: string; nameEn: string; chainId: SupportedChainId; address: Address; deployedAt: string; strike: number; slope: number }
    export type SimulatedInstrument = { kind: 'simulated'; id: string; name: string; nameEn: string; chainId: SupportedChainId; fixtureKey: string }
    export type AbrigoInstrument = LiveInstrument | SimulatedInstrument
    ```
    Replace ABRIGO_INSTRUMENTS: REMOVE the temp `fixture-celo-01` entry; add exactly one simulated entry:
    `{ kind: 'simulated', id: 'ccop-usd-long-gamma', name: 'Cobertura larga gamma cCOP/USD', nameEn: 'cCOP/USD Long-Gamma Hedge', chainId: 8453, fixtureKey: 'ccop-usd-long-gamma' }`.

    Update `tests/unit/instruments-index.test.ts`: the existing `expect(ABRIGO_INSTRUMENTS).toHaveLength(0)` is now FALSE. Change the anti-fishing test to assert: exactly one entry, `kind === 'simulated'`, `id === 'ccop-usd-long-gamma'`, `chainId === 8453`, and that the entry has NO `strike`/`slope`/`address` keys (`expect('strike' in entry).toBe(false)`). Keep the array-type assertion. Add schematic-curve assertions to payoff-curve.test.tsx per the Wave-0/RESEARCH contract: all payoff &gt;= 0, at least one payoff &gt; 0, convex (payoff increases as price moves away from center for prices past the minimum), and generateSchematicConvexPayoff returns 200 points by default.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/payoff-curve.test.tsx tests/unit/instruments-index.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "generateSchematicConvexPayoff" lib/apps/abrigo/payoff.ts` matches; `generatePayoffData` still present
    - `grep -n "kind: 'simulated'" lib/apps/abrigo/instruments.ts` matches exactly one entry
    - `grep -n "fixture-celo-01" lib/apps/abrigo/instruments.ts` returns NO matches (temp removed)
    - `grep -n "toHaveLength(0)" tests/unit/instruments-index.test.ts` returns NO matches (assertion updated)
    - `pnpm vitest run tests/unit/payoff-curve.test.tsx tests/unit/instruments-index.test.ts` GREEN
  </acceptance_criteria>
  <done>Schematic convex generator added (independent of generatePayoffData); AbrigoInstrument is a union; temp fixture removed; one simulated entry; instruments-index test updated and green.</done>
</task>

<task type="auto">
  <name>Task 3: Narrow ALL FIVE consumers on kind (detail page, InstrumentParams, InstrumentJsonLd, aggregator, index page)</name>
  <read_first>
    - app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx (the file being modified — L116, L150-155)
    - components/defi/InstrumentParams.tsx (the file being modified — L29-62 rows)
    - components/defi/InstrumentJsonLd.tsx (the file being modified — L34-48)
    - lib/dashboard/aggregator.ts (the file being modified — aggregateChain L42 filter, L62-65/L84 inst.address reads)
    - app/(defi)/apps/abrigo/instruments/page.tsx (the file being modified — index map branch L68-89, L79 deployedAt read)
    - lib/apps/abrigo/instruments.ts (the new union — narrow against it)
    - docs/superpowers/specs/...-frontend-design.md (§4.2 enumerated narrowing sites)
  </read_first>
  <action>
    This task ONLY makes the FIVE consumers COMPILE under the new union (tsc gate). The full simulated-branch page rendering (SnapshotPoolPanel, CashFlowWaterfall, SIMULADO badge, WalletPanel readOnly) is Wave 3 — do NOT build that here. Minimal narrowing only:

    1. `app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx` (detail):
       - L116 `const currentPrice = instrument.strike` → guard: `const currentPrice = instrument.kind === 'live' ? instrument.strike : FIXTURES[instrument.fixtureKey].pool.humanRate.value` (import FIXTURES from `@/lib/apps/abrigo/fixture`).
       - L150-155 PayoffDiagramClient call: to keep tsc green THIS wave, keep the existing `strike`/`slope`/`currentPrice` props guarded on kind: `strike={instrument.kind === 'live' ? instrument.strike : currentPrice} slope={instrument.kind === 'live' ? instrument.slope : 0}`. (Wave 2 switches the component to a `data` prop and edits this call; Wave 3 owns the full simulated-branch PayoffDiagramClient call.) The aggregator call stays unconditional in THIS wave at the page level — the `kind === 'simulated'` branch-before-aggregator is a Wave 3 change. The simulated entry's chainId 8453 has no deployed contract so getInstrumentPoolState returns null safely (PoolStatePanel renders em-dash).
       - paramLabels object: leave as-is (Wave 3 adds the new keys). InstrumentParams receives the union and narrows internally.
    2. `components/defi/InstrumentParams.tsx`: make the rows array variant-aware. For `instrument.kind === 'live'`: keep the existing 6 rows (name,id,chain,strike,slope,deployed_at). For `instrument.kind === 'simulated'`: render name,id,chain rows only (strike/slope/deployed_at rows are LIVE-ONLY — omit; the full Panoptic fork-param rows + provenance pills are added in Wave 3 / Plan 03). Guard every `instrument.strike`/`.slope`/`.deployedAt`/`.address` read inside `if (instrument.kind === 'live')`. The component must compile and render both variants without `any`.
    3. `components/defi/InstrumentJsonLd.tsx`: branch on kind. Live branch: unchanged (strike/slope/address/chainId/deployedAt PropertyValues). Simulated branch: emit `identifier`, `name`, a description WITHOUT strike/slope, `additionalProperty` containing ONLY `chainId` + a `{ name: 'simulated', value: 'true' }` PropertyValue + a `{ name: 'provenance', value: 'fork-fixture: test-seeded mock pool, not market data' }` entry. NO `strike`/`slope`/`address` keys in the simulated branch (no fabricated numerics — AGENT-10 / anti-fishing).
    4. `lib/dashboard/aggregator.ts`: in `aggregateChain` (L42), change the filter from `ABRIGO_INSTRUMENTS.filter((i) => i.chainId === chainId)` to `ABRIGO_INSTRUMENTS.filter((i) => i.chainId === chainId &amp;&amp; i.kind === 'live')`. After this narrowing TypeScript sees only `LiveInstrument` in the array, so the L62-65 + L84 `inst.address` reads compile. This DOUBLY enforces the locked honesty invariant: a simulated entry is NEVER passed to the multicall. Do not change any other aggregator logic.
    5. `app/(defi)/apps/abrigo/instruments/page.tsx` (index listing): the map branch (L68-89) reads `instrument.deployedAt` (L79). Guard on kind: for `instrument.kind === 'live'` keep the existing card (deployedAt line). For `instrument.kind === 'simulated'`, render the same card shell but show a SIMULADO indicator (text "SIMULADO" — a full pill component is Wave 2/3; here just honest text + an em-dash `—` where deployedAt would be). Read `deployedAt` ONLY inside the `kind === 'live'` guard. Keep the existing empty-state branch unchanged.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm vitest run tests/unit/agent-jsonld.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` passes with the union in place across all FIVE consumers (no "Property 'strike'/'address'/'deployedAt' does not exist on type 'SimulatedInstrument'")
    - `grep -cn "instrument.kind === 'live'" app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx` &gt;= 1
    - `grep -n "instrument.kind" components/defi/InstrumentParams.tsx` matches
    - `grep -n "simulated" components/defi/InstrumentJsonLd.tsx` matches; `grep -n "strike" components/defi/InstrumentJsonLd.tsx` shows strike ONLY inside the live branch (not the simulated branch)
    - `grep -n "i.kind === 'live'" lib/dashboard/aggregator.ts` matches (the filter now narrows to live before the multicall)
    - `grep -n "instrument.kind" app/(defi)/apps/abrigo/instruments/page.tsx` matches (index listing guards deployedAt on kind)
    - `pnpm biome check .` passes (pre-commit gate)
  </acceptance_criteria>
  <done>All FIVE consumers compile under the new union; aggregateChain filters on kind === 'live' before the multicall; index listing guards deployedAt on kind (simulated shows SIMULADO + em-dash); InstrumentJsonLd simulated branch emits simulated:true + provenance and NO strike/slope/address; InstrumentParams renders both variants.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` green (union frozen, all FIVE consumers narrow; aggregator filters on kind)
- `pnpm vitest run tests/unit/fixture.test.ts tests/unit/cashflow.test.ts tests/unit/payoff-curve.test.tsx tests/unit/instruments-index.test.ts tests/unit/agent-jsonld.test.tsx` green
- `grep -n "fixture-celo-01"` across lib/ returns nothing
- `grep -n "i.kind === 'live'" lib/dashboard/aggregator.ts` matches (simulated never reaches multicall)
- No fabricated numbers anywhere in fixture.ts (unknown literals are null; no TBD)
</verification>

<success_criteria>
The data shapes + union discriminant are frozen and unit-tested; the temp fixture is removed and replaced by the single simulated ccop-usd-long-gamma entry; all FIVE consumers compile, the aggregator filters simulated entries out of the multicall, and the JSON-LD simulated branch is anti-fishing-clean. Wave 2 can build components against frozen contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-abrigo-somnia-convex-instrument-frontend-surface-ccop-usd-long-gamma-read-first-simulated/05.1-01-SUMMARY.md`
</output>
</content>
</invoke>

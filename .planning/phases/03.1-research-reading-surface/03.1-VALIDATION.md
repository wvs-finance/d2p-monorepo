---
phase: 03.1
slug: research-reading-surface
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-29
---

# Phase 03.1 — Validation Strategy

> Derived from the spec §8 (`docs/superpowers/specs/2026-05-29-research-reading-surface-design.md`). The planner maps every task to a row here.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Frameworks** | Vitest 4.x (unit + RSC + schema), Playwright 1.49 (e2e + a11y, production-build `webServer`), @axe-core/playwright, Lighthouse CI, Biome 1.9, tsc, impeccable v2.1.8 |
| **Quick run** | `pnpm test:quick` (biome + tsc + vitest) |
| **Full suite** | `pnpm test:all` |
| **Estimated** | ~30s quick; ~6–10min full |

> **Ground-truth gate (CLAUDE.md):** automated green is necessary, not sufficient. Each route-producing task ends with Evidence Collector live-DOM verification. Math/render-path changes MUST be validated under `pnpm build && pnpm start` (not just dev) — Turbopack divergence + `throwOnError:false` silent-macro-failure are this phase's burn classes.

---

## Sampling Rate
- After every task commit: `pnpm test:quick`
- After every wave: `pnpm test:all`
- After each route/page task: Evidence Collector live-verify (`/research`, `/research/{slug}`)
- Before verify-work: full suite green on Vercel preview

---

## Per-Task Verification Map (requirement → test guidance; planner fills Task IDs)

| Area (spec §) | Plan | Test Type | Automated Command (guidance) | Evidence |
|---------------|------|-----------|------------------------------|----------|
| **Render-path spike** (§5) | A | e2e + manual | `pnpm build && pnpm start` then `playwright test tests/e2e/research-math-spike.spec.ts` — a `/research/{slug}` renders a `.katex` node AND asserts NOT `.katex-error`; verified under dev-turbopack too | `velite.config.ts` (body + math plugins), resurrected `components/MDXRenderer.tsx`, `.velite/research.json` carries compiled body |
| **Schema additions** (§4) | B | unit | `vitest run tests/unit/research-schema.test.ts` — `track` enum, `arxiv_id` regex (post-2007 match + pre-2007 non-match), `readable_on_site` default false | `velite.config.ts` `researchSchema` |
| **Index + track filter** (§3.1) | B | e2e | `playwright test tests/e2e/research-index.spec.ts` — `/research` 200; server `<Link>` filter sets `?track=`; pasted `?track=cfmm-microstructure` returns filtered state; NO NuqsAdapter import in `(lab)`; empty-track honest panel | `app/(lab)/research/page.tsx` |
| **Content migration** (§7) | B | structural + build | `pnpm build` (velite validates) green with all 3 files carrying `track`; `ls content/research/*.es.mdx content/research/*.en.mdx` (per-locale split); y3 write-up `readable_on_site: true` | `content/research/*.{es,en}.mdx` |
| **i18n parity** (§4) | B + C | unit | `vitest run tests/unit/i18n-coverage.test.ts` — recursive `assertKeyParity('research', …)` (index keys in B, reading-page superset in C); es-CO-first; `docs/copy-review.md` sign-off entry | `messages/{es-CO,en}/research.json` |
| **Reading page anatomy** (§3.2) | C | e2e | `playwright test tests/e2e/research-reading.spec.ts` — `/research/{slug}` 200 on-site article; equation renders (`.katex`, NOT `.katex-error`); TOC present (not `<details>`); theorem block has label+text; both locales render the correct single-locale body; invalid slug → 404 | `app/(lab)/research/[slug]/page.tsx` |
| **No-JS first paint** (§3.2) | C | e2e | `playwright test … javaScriptEnabled:false` — body + footnotes render; no wallet gate | reading page |
| **Paper-bridge** (§3.2 v1) | C | e2e + unit | arXiv+PDF links present; BibTeX copy button works (client island); Mode-B arxiv-landing fixture renders abstract + bridge | `PaperBridge` component |
| **Anti-fishing equal weight** (§3.2) | C | e2e/visual | FAIL memo (`fx-vol-cpi-closed-fail`) reading page renders at equal visual weight to a PASS — no muting/collapse | reading page |
| **a11y** (§5,§8) | C | axe + manual | `playwright test tests/a11y/research.spec.ts` + a recorded NVDA/VoiceOver MathML spot-check in `docs/a11y-audit.md` (axe can't certify MathML) | audit doc entry |
| **perf** (§5,§8) | C | lighthouse | LCP < 2.5s on the reading page (Moto G 3G); KaTeX font subset + `font-display` applied | lighthouserc |

---

## Wave 0 Requirements
- [ ] Math fixtures: author ≥1 research body with real `$…$`/`\tag` math + ≥1 Mode-B fixture (`arxiv_id`) so math/Mode-B tests can pass (spec §7 — existing bodies use Unicode glyphs, not LaTeX).
- [ ] `tests/e2e/research-math-spike.spec.ts` (Plan A), `tests/unit/research-schema.test.ts`, `tests/e2e/research-index.spec.ts`, `tests/e2e/research-reading.spec.ts`, `tests/a11y/research.spec.ts` stubs (`test.fixme`/`it.todo`), filled by their plans.
- [ ] Extend `tests/unit/i18n-coverage.test.ts` with the recursive `research` namespace case.

---

## Manual-Only Verifications

| Behavior | Why Manual | Instructions |
|----------|------------|--------------|
| MathML screen-reader correctness | axe cannot certify MathML | NVDA/VoiceOver one equation; record date+reviewer+result in `docs/a11y-audit.md` (hard exit criterion, Plan C) |
| es-CO research/math copy quality | native-speaker; no machine translation | reviewer signs off all `messages/es-CO/research.json` + body `*.es.mdx` in `docs/copy-review.md` |
| Cross-browser reading render | Playwright single-engine | Chrome/Safari/Firefox smoke of a reading page; record screenshots |

---

## Validation Sign-Off
- [ ] Every task maps to a row above or a Wave-0 dep
- [ ] No 3 consecutive tasks without automated verify
- [ ] Render-path spike (Plan A) gates all downstream work
- [ ] Math fixtures authored so math/Mode-B tests aren't green-by-assumption
- [ ] Citations confirmed deferred to v2 (no rehype-citation/@citation-js in any plan)
- [ ] Evidence Collector live-verify on `/research` + `/research/{slug}`

**Approval:** pending

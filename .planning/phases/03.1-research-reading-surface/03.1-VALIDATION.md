---
phase: 03.1
slug: research-reading-surface
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-29
---

# Phase 03.1 — Validation Strategy

> Derived from the spec §8 (`docs/superpowers/specs/2026-05-29-research-reading-surface-design.md`). The planner maps every task to a row here.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Frameworks** | Vitest 4.x (unit + RSC + schema), Playwright 1.49 (e2e + a11y, production-build `webServer`), @axe-core/playwright, Lighthouse CI (`lighthouserc.cjs`), Biome 1.9, tsc, impeccable v2.1.8 |
| **Quick run** | `pnpm test:quick` (biome + tsc + vitest) |
| **Full suite** | `pnpm test:all` |
| **Estimated** | ~30s quick; ~6–10min full |

> **Ground-truth gate (CLAUDE.md):** automated green is necessary, not sufficient. Each route-producing task ends with Evidence Collector live-DOM verification. Math/render-path changes MUST be validated under `pnpm build && pnpm start` (not just dev) — Turbopack divergence + `throwOnError:false` silent-macro-failure are this phase's burn classes. The Lighthouse gate (`lhci autorun`) runs in the Evidence Collector step (running prod server), NOT in a CI `<automated>` verify.

---

## Sampling Rate
- After every task commit: `pnpm test:quick`
- After every wave: `pnpm test:all`
- After each route/page task: Evidence Collector live-verify (`/research`, `/research/{slug}`)
- Before verify-work: full suite green on Vercel preview

---

## Per-Task Verification Map (requirement → test guidance; planner fills Task IDs)

| Area (spec §) | Plan | Test Type | Automated Command (guidance) | Evidence |
|---------------|------|-----------|------------------------------|----------|
| **Render-path spike** (§5) | A (03.1-01) | e2e + manual | `pnpm build && pnpm start` then `playwright test tests/e2e/research-math-spike.spec.ts` — a `/research/spike` route renders a `.katex` node, asserts NOT `.katex-error`, asserts a custom component rendered via the `components` prop; verified under dev-turbopack too; `gfm:false` honored; next-mdx-remote/rsc fallback exercised | `velite.config.ts` (body + math plugins + gfm:false + s.toc + theorem-directive), extended `components/MDXRenderer.tsx` (components prop), `.velite/research.json` body EVALUATES to a function |
| **Schema additions** (§4) | B (03.1-02) | unit | `vitest run tests/unit/velite-research-schema.test.ts` — `track` enum, `arxiv_id` regex (post-2007 match + pre-2007 non-match), `readable_on_site` default false; `validBase` updated with `track` so baseline cases don't regress | `velite.config.ts` exported `researchSchema` |
| **Index + track filter** (§3.1) | B (03.1-02) | e2e | `playwright test tests/e2e/research-index.spec.ts` — `/research` 200; server `<Link>` filter sets `?track=`; pasted `?track=cfmm-microstructure` returns filtered state; NO nuqs/NuqsAdapter anywhere under `(lab)`; empty-track honest panel | `app/(lab)/research/page.tsx` (await searchParams) |
| **Content migration** (§7) | B (03.1-02) | structural + build | `pnpm exec velite build` green with all 3 files carrying `track`; `ls content/research/*.es.mdx content/research/*.en.mdx` (per-locale split); y3 write-up `readable_on_site: true` | `content/research/*.{es,en}.mdx` |
| **i18n parity — index** (§4) | B (03.1-02) | unit | `vitest run tests/unit/i18n-coverage.test.ts` — recursive `assertKeyParity('research', …)` over the index chrome keys; es-CO-first; `docs/copy-review.md` sign-off entry | `messages/{es-CO,en}/research.json` |
| **Reading page anatomy** (§3.2) | C1 (03.1-03) | e2e | `playwright test tests/e2e/research-reading.spec.ts` — `/research/{slug}` 200 on-site article; equation renders (`.katex`, computed fontFamily includes `KaTeX_Main`, NOT `.katex-error`); TOC from `entry.toc` (not `<details>`); theorem block has label+text; `<script type=ld+json>` ScholarlyArticle; `await params`; both locales render the correct single-locale body; invalid slug → 404 | `app/(lab)/research/[slug]/page.tsx` |
| **No-JS first paint** (§3.2) | C1 (03.1-03) | e2e | `playwright test … javaScriptEnabled:false` — body + footnotes render; no wallet gate | reading page |
| **Paper-bridge** (§3.2 v1) | C1 (03.1-03) | e2e + unit | `vitest run tests/unit/paper-bridge.test.tsx` (BibTeX copy island) + arXiv/PDF links present; Mode-B arxiv-landing fixture renders abstract + bridge | `components/research/PaperBridge.tsx` |
| **Anti-fishing equal weight** (§3.2) | C1 (03.1-03) | e2e/visual | FAIL memo (`fx-vol-cpi-closed-fail`) reading page renders at equal visual weight to a PASS — no muting/collapse, no `<details>` | reading page |
| **i18n parity — reading superset** (§4,§8) | C2 (03.1-04) | unit | `vitest run tests/unit/i18n-coverage.test.ts` — recursive `assertKeyParity('research', …)` over the `reading.*` SUPERSET; es-CO-first; `docs/copy-review.md` reading-page sign-off | `messages/{es-CO,en}/research.json` |
| **a11y** (§5,§8) | C2 (03.1-04) | axe + manual | `playwright test tests/a11y/research.spec.ts` (index + reading page, both locales) + a recorded NVDA/VoiceOver MathML spot-check (or a TRACKED waiver) in `docs/a11y-audit.md` (axe can't certify MathML) | audit doc entry |
| **perf** (§5,§8) | C2 (03.1-04) | lighthouse (Evidence Collector) | `pnpm exec lhci autorun` against a migrated reading-page URL wired into the EXISTING `lighthouserc.cjs`; LCP < 2.5s on the mobile 3G profile; v1 = `font-display:swap` + KaTeX `<link rel=preload>` (NO Python subsetting) | `lighthouserc.cjs`; LCP score in 03.1-LIVE-VERIFICATION.md |

---

## Wave 0 Requirements
- [ ] Math fixtures: author ≥1 research body with real `$…$`/`\tag` math + a custom component (Plan A spike), the migrated pair-d LaTeX body (Plan B), and ≥1 Mode-B fixture (`arxiv_id`, Plan C1) so math/Mode-B tests can pass (spec §7 — existing bodies use Unicode glyphs, not LaTeX).
- [ ] `tests/e2e/research-math-spike.spec.ts` (Plan A), `tests/unit/velite-research-schema.test.ts` (existing — extended in Plan B), `tests/e2e/research-index.spec.ts` (Plan B), `tests/e2e/research-reading.spec.ts` + `tests/unit/paper-bridge.test.tsx` + `tests/unit/theorem-block.test.tsx` (Plan C1), `tests/a11y/research.spec.ts` (Plan C2) stubs (`test.fixme`/`it.todo`), filled by their plans.
- [ ] Extend `tests/unit/i18n-coverage.test.ts` with the recursive `research` namespace case (index keys in B, reading superset in C2).

---

## Manual-Only Verifications

| Behavior | Why Manual | Instructions |
|----------|------------|--------------|
| MathML screen-reader correctness | axe cannot certify MathML | NVDA/VoiceOver one equation; record date+reviewer+result in `docs/a11y-audit.md` (hard exit criterion, Plan C2). If no SR available, record a TRACKED waiver — must not block silently |
| es-CO research/math copy quality | native-speaker; no machine translation | reviewer signs off all `messages/es-CO/research.json` + body `*.es.mdx` in `docs/copy-review.md` |
| Cross-browser reading render | Playwright single-engine | Chrome/Safari/Firefox smoke of a reading page; record screenshots |

---

## Validation Sign-Off
- [ ] Every task maps to a row above or a Wave-0 dep
- [ ] No 3 consecutive tasks without automated verify
- [ ] Render-path spike (Plan A) gates all downstream work
- [ ] Math fixtures authored so math/Mode-B tests aren't green-by-assumption
- [ ] Citations confirmed deferred to v2 (no rehype-citation/@citation-js in any plan)
- [ ] No Python font tooling (fonttools/pyftsubset) in any plan — byte-subsetting deferred to v2
- [ ] Evidence Collector live-verify on `/research` + `/research/{slug}`; Lighthouse `lhci autorun` runs in the Evidence Collector step (lighthouserc.cjs, not .json)

**Approval:** pending

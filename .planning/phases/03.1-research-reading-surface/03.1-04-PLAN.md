---
phase: 03.1-research-reading-surface
plan: 04
type: execute
wave: 4
depends_on: ["03.1-03"]
files_modified:
  - messages/es-CO/research.json
  - messages/en/research.json
  - tests/unit/i18n-coverage.test.ts
  - tests/a11y/research.spec.ts
  - docs/a11y-audit.md
  - docs/copy-review.md
  - app/(lab)/research/layout.tsx
  - lighthouserc.cjs
  - docs/03.1-RENDER-PATH.md
autonomous: false
requirements: [LAB-03]
user_setup: []

must_haves:
  truths:
    - "Reading-page i18n is a full es-CO-first SUPERSET (reading.* keys for TOC/abstract/arxiv/pdf/doi/bibtex/affiliation/figure/theorem labels/footnotes), symmetric across es-CO and en, gated by the recursive i18n-coverage parity test, with a docs/copy-review.md native sign-off entry"
    - "axe scans /research and a /research/{slug} reading page clean (both locales); because axe CANNOT certify MathML, a manual NVDA/VoiceOver spot-check of one equation is recorded in docs/a11y-audit.md with date+reviewer+result (a TRACKED waiver is allowed if no screen reader is available — it must NOT block silently)"
    - "v1 LCP approach is applied: font-display:swap on the KaTeX @font-face + <link rel=preload> for KaTeX_Main-Regular + KaTeX_Math-Italic in app/(lab)/research/layout.tsx (Plan A scaffolded this; confirm/finalize here) + the documented unicode-range fallback — NO fonttools/pyftsubset, NO Python (byte-subsetting deferred to v2)"
    - "The Lighthouse LCP gate runs against a real migrated reading-page URL in the EXISTING lighthouserc.cjs (NOT a new .json), executed in the Evidence Collector / live-verification step (which has a running prod server) via `pnpm exec lhci autorun`, with the LCP score recorded in 03.1-LIVE-VERIFICATION.md"
  artifacts:
    - path: "messages/es-CO/research.json"
      provides: "es-CO-first reading.* superset keys"
      contains: "reading"
    - path: "docs/a11y-audit.md"
      provides: "Recorded NVDA/VoiceOver MathML spot-check entry (date+reviewer+result) OR a tracked waiver — hard exit criterion"
      contains: "MathML"
    - path: "app/(lab)/research/layout.tsx"
      provides: "font-display:swap + preload of KaTeX_Main-Regular + KaTeX_Math-Italic (v1 LCP; no byte-subsetting)"
      contains: "preload"
    - path: "lighthouserc.cjs"
      provides: "the EXISTING lhci config, extended with the reading-page URL; LCP maxNumericValue 2500 already present"
      contains: "largest-contentful-paint"
  key_links:
    - from: "messages/es-CO/research.json + messages/en/research.json"
      to: "tests/unit/i18n-coverage.test.ts assertKeyParity('research')"
      via: "recursive parity gate over the reading.* superset"
      pattern: "reading"
    - from: "app/(lab)/research/layout.tsx"
      to: "katex/dist/fonts/KaTeX_Main-Regular + KaTeX_Math-Italic"
      via: "<link rel=preload as=font> + font-display:swap"
      pattern: "preload"
    - from: "lighthouserc.cjs"
      to: "a migrated /research/{slug} URL"
      via: "lhci autorun in the Evidence Collector step (running prod server)"
      pattern: "research"
---

<objective>
PLAN C2 (i18n superset + a11y + perf gates). On top of Plan C1's reading page, deliver: the reading-page i18n SUPERSET (es-CO-first, native sign-off in docs/copy-review.md), the a11y gate (axe scan of index + a reading page in both locales PLUS a manual MathML screen-reader entry — or a tracked waiver — in docs/a11y-audit.md), and the LCP perf gate (font-display:swap + KaTeX font preload per spec §5 — NO Python font tooling; byte-subsetting deferred to v2), with the Lighthouse gate added to the EXISTING `lighthouserc.cjs` and RUN in the Evidence Collector step against a real migrated reading-page URL.

Purpose: makes the reading surface accessible, locale-correct, and fast on 3G — closing the manual-gate exit criteria (copy sign-off, MathML SR, LCP) that automated tests alone cannot.
Output: reading-page i18n superset + recursive parity green + axe spec + recorded MathML SR entry/waiver + font-display/preload finalized + lighthouserc.cjs reading-page URL + LCP score recorded.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-research-reading-surface/03.1-CONTEXT.md
@.planning/phases/03.1-research-reading-surface/03.1-VALIDATION.md
@docs/superpowers/specs/2026-05-29-research-reading-surface-design.md
@.planning/phases/03.1-research-reading-surface/03.1-03-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- The reading-page components (Plan C1) consume i18n labels via props: PaperBridge.labels,
     TheoremBlock label fallbacks, ArticleTOC heading, Figure prefix. Author the reading.* keys
     to match what those components read. -->

<!-- VERIFIED repo facts: -->
<!-- 1. The Lighthouse config is lighthouserc.cjs (NOT .json). It already asserts
        'largest-contentful-paint': ['error', {maxNumericValue: 2500}], formFactor:'mobile',
        url default localhost:3000. EDIT this file — do NOT create lighthouserc.json. -->
module.exports = { ci: { collect: { url: [process.env.LHCI_COLLECT__URL ?? 'http://localhost:3000'], settings: { formFactor: 'mobile', /* 412×823, 3G throttling */ } }, assert: { assertions: { 'largest-contentful-paint': ['error', {maxNumericValue: 2500}], /* … */ } } } }
<!-- 2. fonttools/pyftsubset is PYTHON and is NOT installed; there is no install task and
        installing Python tooling is out of scope. v1 LCP = font-display:swap + preload (per Plan A
        scaffold) + unicode-range fallback. Byte-level subsetting is a v2/deferred item. -->
<!-- 3. lhci autorun needs a RUNNING prod server — it lives in the Evidence Collector / live step,
        NOT in a CI-style <automated> verify (no server there). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reading-page i18n superset (es-CO-first) + recursive parity + native sign-off entry</name>
  <files>messages/es-CO/research.json, messages/en/research.json, tests/unit/i18n-coverage.test.ts, docs/copy-review.md</files>
  <read_first>
    - messages/{es-CO,en}/research.json (post-Plan-B chrome keys + Plan C1 — this is a ~3–5× SUPERSET, not a small extend; spec §8 reviewer M2)
    - tests/unit/i18n-coverage.test.ts (recursive research parity — gates the superset symmetry)
    - the Plan C1 components (PaperBridge labels, TheoremBlock label fallbacks, ArticleTOC heading, Figure prefix) — author keys to match what they read
    - spec §3.2 / §5 reading-page copy; 02-UI-SPEC.md Copywriting Contract (author's voice, no marketing slop)
    - ./CLAUDE.md (es-CO authored first; native-reviewer sign-off in docs/copy-review.md)
  </read_first>
  <action>
    Author the reading-page i18n SUPERSET in BOTH `messages/es-CO/research.json` (FIRST) and `messages/en/research.json` (second, native-authored — no machine translation). Add reading-page keys under the `research` namespace, symmetric across locales:
      - `reading.toc_heading`, `reading.abstract_heading`, `reading.read_on_arxiv`, `reading.read_pdf`, `reading.doi_label`, `reading.bibtex_copy`, `reading.bibtex_copied`, `reading.affiliation` (DS2P Labs / ∂²Π), `reading.figure_prefix`, `reading.theorem_label.{theorem,definition,lemma,proof}`, `reading.footnote_heading`, plus any label the Plan C1 components consume.
    Record the es-CO reading-page copy entry in `docs/copy-review.md` for native-reviewer sign-off — this sign-off is an EXIT CRITERION of this plan (not optional). Add it as a NEW reading-page section, distinct from Plan B's index-chrome line.
    Confirm `vitest run tests/unit/i18n-coverage.test.ts` is green (recursive research parity gates the superset symmetry).
  </action>
  <acceptance_criteria>
    - `grep -q "reading" messages/es-CO/research.json && grep -q "reading" messages/en/research.json` (superset present both locales)
    - `vitest run tests/unit/i18n-coverage.test.ts` green (research superset symmetric)
    - `grep -q "research" docs/copy-review.md` AND the reading-page section is present (not only the Plan-B chrome line)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/unit/i18n-coverage.test.ts</automated>
  </verify>
  <done>Reading-page i18n superset symmetric (es-CO-first), recursive parity green, native sign-off logged in copy-review.md.</done>
</task>

<task type="auto">
  <name>Task 2: a11y (axe + manual MathML entry/waiver) + perf (font-display+preload, no Python) + Lighthouse gate URL in lighthouserc.cjs</name>
  <files>tests/a11y/research.spec.ts, docs/a11y-audit.md, app/(lab)/research/layout.tsx, lighthouserc.cjs, docs/03.1-RENDER-PATH.md</files>
  <read_first>
    - tests/a11y/ (existing axe specs pattern — homepage.spec.ts, apps-dropdown.spec.ts)
    - spec §5 a11y ("manual NVDA/VoiceOver MathML spot-check is a HARD exit criterion, recorded with date+reviewer+result in docs/a11y-audit.md") + §8 perf ("Lighthouse LCP < 2.5s; documented unicode-range fallback if the gate fails")
    - app/(lab)/research/layout.tsx (Plan A scaffolded font-display:swap + preload of KaTeX_Main-Regular + KaTeX_Math-Italic — CONFIRM/finalize here; do NOT add fonttools/pyftsubset/Python)
    - lighthouserc.cjs (EXISTING — already has LCP 2500 error assertion, formFactor mobile, url default localhost:3000; ADD the reading-page URL; do NOT create lighthouserc.json)
    - docs/a11y-audit.md, docs/copy-review.md (gate docs to record entries)
    - the migrated Mode-A slug from Plan B/C1 to use as the LCP target URL (e.g. /research/abrigo-y3-carbon-basket-writeup)
  </read_first>
  <action>
    a11y: Add `tests/a11y/research.spec.ts`: axe scan of `/research` (index) AND a `/research/{slug}` reading page (both locales). axe CANNOT certify MathML — so ALSO perform a manual NVDA or VoiceOver spot-check of ONE rendered equation and RECORD it in `docs/a11y-audit.md` with date + reviewer + result. If NO screen reader is available in this environment, record a TRACKED waiver in docs/a11y-audit.md (explicit "WAIVED — no SR available, follow-up: {when/how}") — it must NOT block silently, but it must be visible.
    perf — font (v1 approach, NO Python):
      - CONFIRM/finalize in `app/(lab)/research/layout.tsx` the `font-display: swap` on the KaTeX `@font-face` + `<link rel="preload" as="font" type="font/woff2" crossorigin>` for KaTeX_Main-Regular + KaTeX_Math-Italic (Plan A scaffolded these; finalize the exact face paths under `katex/dist/fonts/`). Document the unicode-range fallback (per spec §5) in docs/03.1-RENDER-PATH.md.
      - Do NOT add fonttools/pyftsubset or any Python build step. Byte-level subsetting is DEFERRED to v2 — record that deferral in docs/03.1-RENDER-PATH.md.
    perf — Lighthouse gate:
      - EDIT the existing `lighthouserc.cjs` (NOT a new .json): add a real migrated reading-page URL to the `collect.url` array (e.g. `http://localhost:3040/research/abrigo-y3-carbon-basket-writeup` — match the prod-build port from playwright.config). The LCP `maxNumericValue: 2500` error assertion already exists; keep it.
      - The actual `pnpm exec lhci autorun` RUN happens in the Evidence Collector / live-verification step (Task 3), which has a running prod server — do NOT put `lhci autorun` in this task's `<automated>` verify (there is no server there). Just wire the URL.
  </action>
  <acceptance_criteria>
    - `test -f tests/a11y/research.spec.ts`
    - `grep -qi "mathml" docs/a11y-audit.md` (manual SR entry OR tracked waiver recorded — hard exit criterion, not silent)
    - `grep -qi "font-display\|preload" 'app/(lab)/research/layout.tsx'` (v1 font perf applied)
    - `! grep -rqi "pyftsubset\|fonttools" .` (NO Python font tooling anywhere — byte-subsetting deferred to v2)
    - `grep -q "research" lighthouserc.cjs` (reading-page URL added to the EXISTING cjs config) AND `! test -f lighthouserc.json` (no stray json config created)
    - `grep -q "largest-contentful-paint" lighthouserc.cjs` (LCP gate present)
    - `pnpm exec playwright test tests/a11y/research.spec.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec playwright test tests/a11y/research.spec.ts && grep -q 'research' lighthouserc.cjs && ! test -f lighthouserc.json && ! grep -rqi 'pyftsubset\|fonttools' .</automated>
  </verify>
  <done>axe spec green (index + reading page, both locales); manual MathML SR entry OR tracked waiver in a11y-audit.md; font-display:swap + KaTeX preload finalized with NO Python tooling; reading-page URL wired into the existing lighthouserc.cjs (no stray .json).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 3: Evidence Collector live-verify — a11y + Lighthouse LCP gate (runs lhci against the prod server)</name>
<action>Invoke the Evidence Collector agent (live-verification mode) per ./CLAUDE.md against /research/{slug}. It RUNS the Lighthouse gate (the only place with a running prod server). See <how-to-verify> for the exact lhci + locale + MathML-confirmation steps and verdicts.</action>
  <what-built>
    The reading-page i18n superset (es-CO-first, native sign-off), the a11y gate (axe + manual MathML entry/waiver), and the v1 LCP perf (font-display:swap + KaTeX preload, no Python subsetting) with the reading-page URL wired into the existing lighthouserc.cjs.
  </what-built>
  <how-to-verify>
    Invoke the Evidence Collector agent per ./CLAUDE.md (live-verification mode) against `/research/{slug}` — this is the ONLY step with a running prod server, so the Lighthouse gate RUNS here:
    1. Ensure a production build is serving (deployed, or local `pnpm build && pnpm start -p 3040` — production build is ground truth).
    2. Run the Lighthouse gate: `pnpm exec lhci autorun` (the reading-page URL is wired in lighthouserc.cjs). Capture the LCP numeric value.
    3. Locale: assert the reading-page chrome (TOC heading, abstract heading, PaperBridge labels, footnote heading) renders in es-CO under the es-CO cookie and in en under the en cookie — the superset is live, not just present in JSON.
    4. Confirm the manual MathML SR entry (or tracked waiver) is recorded in docs/a11y-audit.md and the es-CO reading-page sign-off is in docs/copy-review.md BEFORE approving.
    5. `browser_console_messages`; `browser_take_screenshot` → `/tmp/d2p-verify/03.1-04-{lcp,locale-es,locale-en}.png`.
    6. Append verdicts (✓/⚠/✗/⊘) + the LCP score + screenshot paths to `.planning/phases/03.1-research-reading-surface/03.1-LIVE-VERIFICATION.md`.
    Expected: LCP < 2.5s recorded; reading-page chrome localizes correctly in both locales; MathML SR entry/waiver + es-CO sign-off present.
  </how-to-verify>
  <resume-signal>Type "approved" once the Evidence Collector records the LCP score (< 2.5s, or a documented fallback decision) AND the docs/a11y-audit.md MathML entry/waiver + docs/copy-review.md es-CO sign-off are present (or describe the ✗ for a fix).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm exec vitest run tests/unit/i18n-coverage.test.ts` green (reading-page superset symmetric).
- `pnpm exec playwright test tests/a11y/research.spec.ts` green (index + reading page, both locales).
- `grep -q 'research' lighthouserc.cjs && ! test -f lighthouserc.json` (gate wired into the existing config; no stray json).
- `! grep -rqi 'pyftsubset\|fonttools' .` (no Python font tooling — byte-subsetting deferred to v2).
- Lighthouse LCP < 2.5s recorded in 03.1-LIVE-VERIFICATION.md (run by the Evidence Collector against the prod server).
- docs/a11y-audit.md has the MathML SR entry or a tracked waiver; docs/copy-review.md has the es-CO reading-page sign-off; Evidence Collector ✓ in 03.1-LIVE-VERIFICATION.md.
</verification>

<success_criteria>
- Reading-page i18n is a symmetric es-CO-first superset with native sign-off; the recursive parity test gates it.
- axe scans the index + a reading page clean in both locales; the manual MathML a11y entry (or a tracked, visible waiver) is recorded.
- v1 LCP approach (font-display:swap + KaTeX preload + unicode-range fallback, NO Python subsetting) is applied; the Lighthouse LCP < 2.5s gate is wired into the existing lighthouserc.cjs and RUN by the Evidence Collector against the prod server.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-research-reading-surface/03.1-04-SUMMARY.md` recording: the reading.* superset key set, the MathML SR entry/waiver, the es-CO native sign-off, the font-display/preload finalization (and the v2 byte-subsetting deferral), the lighthouserc.cjs URL wiring, and the Lighthouse LCP result from the Evidence Collector.
</output>
</plan>

---
phase: 03.1-research-reading-surface
plan: "02"
subsystem: research-index-and-track-filter
tags:
  - research-index
  - track-filter
  - locale-split-migration
  - server-link
  - i18n
  - publication-card
  - crash-recovery
dependency_graph:
  requires:
    - "03.1-01 (locale-split glob research/*.{es,en}.mdx + derived locale field + extended schema base)"
  provides:
    - "/research reverse-chron list with server-rendered, shareable track filter (?track=)"
    - "per-locale dedupe (one card per slug for the active locale)"
    - "extended PublicationCard (track badge + readable_on_site in/out link logic)"
    - "required `track` schema field + readable_on_site default(false)"
    - "es-CO-first index i18n keys (research.h1 / subheading / track_label.* / type_label.* / cta.* / empty_*)"
  affects:
    - "03.1-03 (Plan C) — MUST build /research/[slug] reading page; card CTAs currently 404 until then"
status: complete-with-recorded-waiver
---

# 03.1-02 — Research index + server track filter (Plan B)

## What shipped

**Final schema field set** (`velite.config.ts` `researchSchema`): `slug`, `title_es`, `title_en`,
`authors`, `date` (coerce.date), `type`, **`track` (REQUIRED enum: cfmm-microstructure | abrigo-hedge-design | notes)**,
**`readable_on_site` (boolean, default false)**, `external_url?`, `summary_es`, `summary_en`,
`abstract_es?`, `abstract_en?`, `arxiv_id?` (post-2007 regex), `pdf_url?`, `doi?`, `bibtex?`,
`tags` (default []), `order?`. (abstract/arxiv/pdf/doi/bibtex are forward-declared for Plan C/v2; unused by the index.)

**6 migrated files** (atomic with the schema change, commit `548bcf2`) — each split per-locale:

| slug | track | readable_on_site |
|------|-------|------------------|
| `pair-d-dispatch-brief.{es,en}.mdx` | abrigo-hedge-design | true |
| `fx-vol-cpi-closed-fail.{es,en}.mdx` | abrigo-hedge-design | true |
| `abrigo-y3-carbon-basket-writeup.{es,en}.mdx` | abrigo-hedge-design | true |

(`spike-katex.{es,en}.mdx` from 03.1-01 carries `track: notes` but is explicitly excluded from the index until Plan C retires it.)

**Body carrying LaTeX:** `pair-d-dispatch-brief` (3 `$…$` spans: the M-sketch β/p result). `spike-katex` carries 7 (the render-path fixture). The other two migrated pieces are prose-only.

**TrackFilter approach:** pure server `<Link>` row (no client island, no nuqs/NuqsAdapter anywhere under `(lab)` — FOUND-11 honored). Active track resolved from awaited `searchParams.track` against a `VALID_TRACKS` set; "All" = undefined. Index dedupes to one card per slug (prefers the active locale, falls back to es), filters by active track, sorts by `order` then `date` desc. Empty-per-track renders an honest `research.empty_track.*` state (verified: cfmm → "Sin publicaciones en esta línea todavía", no crash).

**validBase update:** `tests/unit/research-math-pipeline.test.ts` `validBase` gained `track: 'notes'` (the `548bcf2` atomic commit had missed this fixture — fixed in recovery commit `af027e5`). `tests/unit/velite-research-schema.test.ts` validBase was updated in `548bcf2`.

## Commits

- `548bcf2` feat — atomic schema (track required) + 3 bilingual MDX split per-locale
- `c10fcd0` feat — server TrackFilter + await-searchParams filtering + extended PublicationCard
- `49e50fc` feat — index i18n keys (es-CO-first) + research-index e2e + copy-review entry
- `af027e5` fix — post-crash recovery: track fixture, impeccable@2.1.8 pin, spike-page border, e2e nav-await
- `73f6dc3` fix — remove duplicate (lab) site header + nested `<main>` landmark

## Crash + recovery

The Plan B executor was killed mid-run during its Evidence-Collector step ("the production server died" /
Chromium "Page crashed" — a browser/OOM event from spawning a build+server under memory pressure, **not** an app
fault). All 3 feature commits had already landed and `git fsck` was clean. On resume: clean `velite build` (8 entries
correct), clean `pnpm build` (exit 0, all routes 200) — confirming no app crash. Recovery then fixed the loose ends
(missed fixture, drifted test tool, and the chrome defects the crashed verification never reached).

Two test failures surfaced during recovery, both **pre-existing / environmental**, not Plan B regressions:
- `anti-patterns.test.ts` shelled out via unpinned `npx impeccable`, which drifted from the documented `2.1.8` to
  `2.3.1` (renamed the pure-black/#000000 rule). Pinned to `@2.1.8` to restore the documented contract.
- The same detector found a real `app/` anti-pattern: a `3px solid` accent left-rule on the throwaway spike-page
  `TheoremBlock` demo (a one-sided "AI-tell" side-tab) — softened to a subtle full hairline.

## Evidence Collector verdict (live, localhost:3040 — Plan B is committed but unpushed)

| # | Claim | Verdict |
|---|-------|---------|
| 1 | 3 deduped cards (not 6) | ✓ PASS |
| 2 | es-CO default; 3 Spanish titles exact | ✓ PASS |
| 3 | All + 3 track chips, correct hrefs | ✓ PASS |
| 4 | abrigo → 3 cards; cfmm → honest empty-state, no crash | ✓ PASS |
| 5 | Mono/uppercase track badge + type badge, human-readable | ✓ PASS |
| 6 | Cards link to `/research/<slug>` | ⚠ PARTIAL → recorded waiver (see below) |
| 7 | Single site header | ✓ PASS (was ✗ FAIL; fixed in `73f6dc3`, re-verified: 1 nav, 1 `<main>`) |
| 8 | Locked tokens (ochre accent, Plex fonts) | ✓ PASS |

Screenshots: `/tmp/d2p-verify/03.1-02-index.png`, `-abrigo-filter.png`, `-cfmm-empty.png`, `-singleheader.png`.
Server-side filter confirmed by BOTH direct `?track=` URL and in-page `<Link>` click.

## Recorded waiver (claim #6) — carried to Plan C (03.1-03)

The 3 card CTAs (`readable_on_site: true`) link to `/research/<slug>`, which **404s** because the reading page is
**Plan C scope and not yet built**. This is the intended wave sequencing: closing the "dead card" requires the
reading surface Plan C delivers. The 3 console 404s on the default `/research` view are RSC prefetches of those same
not-yet-existing routes (the cfmm empty-state route, which renders no card links, is console-clean — isolating the
cause). **Plan C (03.1-03) MUST build `app/(lab)/research/[slug]/page.tsx`; this waiver closes when it lands.**

## Verification state

- `pnpm test:quick` (biome + tsc + vitest): **123/123 green**
- `pnpm run build`: exit 0; `/research`, `/research?track=*`, `/research/spike` all 200
- `velite build`: 8 entries, all with correct `track` / `readable_on_site`

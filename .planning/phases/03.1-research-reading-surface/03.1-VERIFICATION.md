---
phase: 03.1-research-reading-surface
verified: 2026-05-29T19:12:37Z
status: human_needed
score: 11/11 code truths verified; 3 tracked non-code residuals
re_verification: false
human_verification:
  - test: "LCP < 2500ms on a real CDN (Vercel preview deploy of /research and /research/{slug})"
    expected: "LCP ≤ 2.5s on the mobile-3G profile; local dev-box measured ~2.9–3.2s (noisy 2.87–3.66s under simulated 3G on a loaded box). FCP ~1.2s and TBT <200ms are healthy, fonts preloaded 200."
    why_human: "Local Lighthouse on a loaded dev box is not representative; the spec's LCP gate must be measured on real-CDN edge delivery. Author chose Vercel preview as ground truth. Deferred, not waived."
  - test: "es-CO native-Colombian-Spanish copy sign-off of messages/{es-CO,en}/research.json + the *.es.mdx bodies"
    expected: "A native Colombian Spanish reviewer signs off the C2 reading.* superset rows in docs/copy-review.md (currently _pending_)."
    why_human: "No-machine-translation rule (CLAUDE.md) requires a human native reviewer; i18n key parity + anti-slop grep are automated-green but cannot certify register/idiom quality."
  - test: "MathML screen-reader announcement of one display equation (NVDA / VoiceOver / Orca)"
    expected: "The screen reader announces the equation via the KaTeX MathML <math>/<annotation> subtree, not raw $…$ LaTeX, and not silently skipped."
    why_human: "axe-core has no MathML rule; no screen reader exists in the headless Linux env. Recorded as a TRACKED, non-silent waiver in docs/a11y-audit.md (2026-05-29). htmlAndMathml output is wired + build-verified, so confidence is high but the live announcement is unconfirmed."
tracked_followups:
  - "LCP re-measure on Vercel preview (real CDN) — author-chosen ground truth; defer the 2500ms verdict until then."
  - "es-CO native copy sign-off — docs/copy-review.md C2 (research.json + *.es.mdx) checkboxes open."
  - "MathML screen-reader spot-check — tracked waiver in docs/a11y-audit.md; close on first SR-capable run / before first formal structured paper."
  - "Component live-content coverage (TOC / footnotes / TheoremBlock / Figure) — author-approved waiver: unit-tested + Plan-A render-path-proven, but unexercised by any currently-published body (real memos are short flat prose; no fabricated demo paper per the IA rule). Empty-TOC degrades gracefully (rail hidden). Close on the first real structured paper."
---

# Phase 03.1: Research Reading Surface — Verification Report

**Phase Goal:** A public, locale-correct (es-CO + en), paper-grade research reading surface — a `/research` index (reverse-chron list + server-rendered track filter) and `/research/[slug]` reading pages (build-time KaTeX, TOC, footnotes, theorem callouts, figures, PaperBridge arXiv/BibTeX bridge, Mode A on-site / Mode B arXiv-landing / notFound) — without re-introducing the per-iteration econometric exercise UI.
**Verified:** 2026-05-29T19:12:37Z (HEAD `d34d0a9`, 12 commits on `main`, NOT pushed)
**Status:** human_needed — GOAL MET WITH TRACKED RESIDUALS
**Re-verification:** No — initial verification.

> Method: goal-backward, code + LIVE-VERIFICATION record + a fresh `pnpm test:quick`. Per the orchestrator's instruction (a prior browser/Lighthouse run OOM-crashed the machine) NO Playwright/Lighthouse/long-lived server was launched; live-DOM evidence is drawn from the recorded Evidence-Collector runs in `03.1-LIVE-VERIFICATION.md`, cross-checked against the committed source.

## Goal Achievement

### Observable Truths

| #   | Truth (from spec §1 goal + §3/§5/§8 + LAB-03)                                                          | Status      | Evidence |
| --- | ----------------------------------------------------------------------------------------------------- | ----------- | -------- |
| 1   | `/research` is a reverse-chron LIST (not grid), 3 deduped es-CO cards, with track badges + type badges | ✓ VERIFIED  | `page.tsx` dedupes per-slug + sorts order/date desc; LIVE 03.1-02 claim #1/#2/#5 PASS (3 cards, exact es-CO titles, mono uppercase track badge) |
| 2   | Track filter is server-rendered (`<Link href="?track=">`), shareable, zero filter JS, no nuqs under `(lab)` | ✓ VERIFIED  | `TrackFilter` server `<Link>` row; `grep nuqs\|NuqsAdapter app/(lab)/` → 0 hits; LIVE 03.1-02 claim #3/#4 PASS (URL + in-page click both filter; cfmm empty-state, no crash) |
| 3   | Build-time KaTeX renders real `.katex`/`.katex-display` (NOT `.katex-error`) with `KaTeX_Main` glyphs, zero math JS | ✓ VERIFIED  | `velite.config.ts` `s.mdx({gfm:false, rehypePlugins:[rehypeKatex,{output:'htmlAndMathml',throwOnError:false}]})`; LIVE 03.1-01 claims #2/#3/#4 + 03.1-03 claim #1 PASS (`.katex`=3/8, error=0, computed `KaTeX_Main`, `document.fonts` loaded) |
| 4   | Mode A renders the full localized on-site MDX body (one `<article>`); Mode B renders abstract + bridge with ZERO `<article>` | ✓ VERIFIED  | `[slug]/page.tsx` `hasBody`/`hasBridge` branch logic; LIVE 03.1-03 claim #6 PASS (Mode-A 1 `<article>` 1205–1262 chars; Mode-B cfmm `<article>`=0) |
| 5   | Invalid slug AND retired spike route → `notFound()` (404); the dead-card gap is closed | ✓ VERIFIED  | `if (!hasBody && !hasBridge) notFound()`; spike dir removed (`ls` → no such file); LIVE 03.1-03 claim #9 PASS + HTTP table (`/nope-404`=404, `/spike`=404, 3 abrigo slugs=200) |
| 6   | FAIL memo (`fx-vol-cpi-closed-fail`) renders at IDENTICAL visual weight to a PASS — no muting, no collapse, no `<details>` (CROSS-09 / LAB-05) | ✓ VERIFIED  | `[slug]/page.tsx` single render path, no opacity/collapse branch on disposition; LIVE 03.1-03 claim #7 PASS (H1 color/opacity/font-size/weight + main/article opacity all identical PASS↔FAIL; `<details>`=0) |
| 7   | PaperBridge is the SOLE `'use client'` island; arXiv/PDF/DOI links + copyable BibTeX | ✓ VERIFIED  | only `PaperBridge.tsx` carries `'use client'` under `components/research/`; `deriveArxivUrls` is hermetic (string concat, no fetch); LIVE 03.1-03 claim #5 PASS (abs/pdf/doi + BibTeX + exactly 1 button) — copy success-branch fires in a real browser only (headless clipboard blocked) |
| 8   | ScholarlyArticle JSON-LD on every reading page; locale-correct single-language body | ✓ VERIFIED  | `[slug]/page.tsx` builds `WithContext<ScholarlyArticle>` (XSS-escaped inline script); LIVE 03.1-03 claim #8 PASS (all 4 routes) + locale-toggle PASS (`lang=en`, 0 es markers / 20 en markers) |
| 9   | Single site chrome (one nav, one `<main>`) on the reading route; locked tokens (muted ochre, Plex only) | ✓ VERIFIED  | `[slug]/page.tsx` renders its own single `<main>`; LIVE 03.1-03 claim #10 PASS (1 nav, 1 main, Plex only, ochre accent, 0 console errors). NOTE: the index's double-header (LIVE 03.1-02 claim #7 FAIL) was fixed in `73f6dc3` and re-verified ✓. |
| 10  | i18n parity es-CO↔en across `research.*` (index chrome + `reading.*` superset), es-CO authored first | ✓ VERIFIED  | recursive `assertKeyParity('research')` green; fresh `pnpm test:quick` 132/132 PASS; LIVE 03.1-04 i18n gate 4/4 PASS |
| 11  | Schema additions correct + atomic: `track` REQUIRED enum, `readable_on_site` default false, `arxiv_id` post-2007 regex, no `s.coerce.date()` on new string fields | ✓ VERIFIED  | `velite.config.ts` L61–77 (track enum, readable_on_site default, arxiv regex `/^\d{4}\.\d{4,5}(v\d+)?$/`, bibtex/doi/pdf strings); `velite build` 8 entries; schema unit tests green |

**Score:** 11/11 code-verifiable truths VERIFIED. 3 non-code residuals require human/CDN confirmation (LCP, native copy, MathML SR) — see Human Verification.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `app/(lab)/research/page.tsx` | index + server track filter (await searchParams) | ✓ VERIFIED | dedupe + filter + sort + empty-state; wired to `TrackFilter`, `PublicationCard` |
| `app/(lab)/research/[slug]/page.tsx` | locale-aware Mode A/B/notFound reading page | ✓ VERIFIED | 208 lines, substantive; `generateStaticParams`/`generateMetadata`/JSON-LD/TOC-gating all present + wired |
| `app/(lab)/research/layout.tsx` | route-scoped KaTeX CSS + stable-path font preload | ✓ VERIFIED | present; LIVE 03.1-04 font preload 200 (Wave-1 404 burn reconciled) |
| `components/research/{ResearchArticle,ArticleTOC,Sidenote,TheoremBlock,Figure,mdx-components,PaperBridge}.tsx` | 6 components + 1 island | ✓ VERIFIED (3 unexercised by content — waived) | all 7 present + imported by `[slug]/page.tsx` / `mdx-components`; TOC/footnotes/TheoremBlock/Figure unit-tested + Plan-A render-proven but dark on current content (tracked waiver) |
| `lib/research/arxiv.ts` | hermetic `deriveArxivUrls` | ✓ VERIFIED | imported + used in `[slug]/page.tsx` |
| `velite.config.ts` | schema + s.mdx math pipeline + s.toc + locale-split glob | ✓ VERIFIED | track/readable_on_site/arxiv fields; `[remarkMath,remarkGfm,remarkDirective,remarkTheoremDirective]`; `gfm:false` |
| `content/research/*.{es,en}.mdx` (8 files) | 3 abrigo migrated + Mode-B cfmm fixture, per-locale split | ✓ VERIFIED | `ls` confirms 4 slugs × {es,en}; spike fixtures deleted |
| `public/static/katex/KaTeX_{Main-Regular,Math-Italic}.woff2` | LCP-critical faces at stable path | ✓ VERIFIED | both present (26272 + 16440 B); LIVE 03.1-04 curl 200 |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `[slug]/page.tsx` | `MDXRenderer`/`ResearchArticle` | `components` PROP (not useMDXComponents) | ✓ WIRED | `getResearchComponents(locale)` passed; LIVE 24 `.katex` nodes on pair-d Mode A |
| `[slug]/page.tsx` | `entry.toc` | `ArticleTOC`, gated `hasToc` | ✓ WIRED | `entry.toc.length > 0` guard (empty-rail fix `572cad3`); degrades gracefully |
| `[slug]/page.tsx` | `deriveArxivUrls` + `PaperBridge` | derived abs/pdf props | ✓ WIRED | hermetic; LIVE Mode-B bridge PASS |
| index `page.tsx` | `/research/[slug]` | `PublicationCard` href | ✓ WIRED | 03.1-02 PARTIAL (404) waiver CLOSED by 03.1-03 reading route (HTTP 200 on all 3 abrigo slugs) |
| `velite.config.ts` | rendered `.katex` HTML | build-time `rehypeKatex` (Node, Turbopack-safe) | ✓ WIRED | zero math JS shipped; LIVE error-count 0 |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| LAB-03 | 03.1-01/02/03/04 (all declare `requirements: [LAB-03]`) | Publications page at `/research` indexes papers, decision memos, iteration write-ups | ✓ SATISFIED (and substantially exceeded) | Index lists 3 deduped pieces with type+track badges; reading pages render the bodies on-site (Mode A) or bridge to arXiv (Mode B). REQUIREMENTS.md already marks LAB-03 "Complete" (Phase 2); Phase 03.1 elevates the same requirement to the paper-grade surface the spec scopes. No orphaned requirement IDs for this phase. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `components/research/PaperBridge.tsx` | 82 | `return null` | ℹ️ Info | Legitimate guard — renders nothing when no arXiv/PDF/DOI/BibTeX present; NOT a stub |
| `components/research/ArticleTOC.tsx` | 47 | `return null` | ℹ️ Info | Legitimate guard — hides the rail when `toc` empty (this is the graceful-degrade fix); NOT a stub |
| `tests/unit/fixtures/anti-patterns.html` | — | Inter / #000000 / purple-gradient (8 hits) | ℹ️ Info | These are PLANTED fixtures proving the impeccable detector fires; they are NOT in `app/` source. `npx impeccable@2.1.8 detect app/` exits 0; `grep` for inter/#000000/purple in `app/(lab)/research/` → 0 hits. |

No blocker anti-patterns. No TODO/FIXME/PLACEHOLDER in phase source. Non-goals all honored: zero nuqs/NuqsAdapter under `(lab)`, zero `rehype-citation`/`@citation-js`, spike route retired, no per-iteration exercise UI reconstructed.

### Human Verification Required

1. **LCP on Vercel preview (real CDN).** Deploy a preview and run Lighthouse mobile-3G against `/research` and a reading page. Expected LCP ≤ 2500ms. Local dev-box measured ~2.9–3.2s (noisy), unrepresentative of edge delivery; author-chosen ground truth is the preview. Tracked, not waived.
2. **es-CO native copy sign-off.** A native Colombian Spanish reviewer signs off `messages/{es-CO,en}/research.json` + `*.es.mdx` in `docs/copy-review.md` (C2 rows currently `_pending_`). Required by the no-machine-translation rule; key parity + anti-slop grep are automated-green only.
3. **MathML screen-reader spot-check.** On an SR-capable machine, confirm one display equation is announced via the KaTeX MathML subtree (NVDA/VoiceOver/Orca). Tracked waiver recorded in `docs/a11y-audit.md` (2026-05-29) — headless Linux has no SR.

### Gaps Summary

No goal-blocking gaps. Every observable truth the surface owns is verified in committed code and corroborated by the recorded Evidence-Collector live runs: the index lists + filters server-side, build-time KaTeX renders without errors, Mode A/B/notFound branch correctly, the FAIL memo holds equal visual weight, PaperBridge is the sole island, JSON-LD + locale-correct bodies emit, i18n parity is green (fresh 132/132), and the schema/migration landed atomically. The two cross-cutting defects the live runs flagged (index double-header; card-CTA 404s) were both resolved and re-verified (`73f6dc3`; reading route HTTP 200). The axe small-text contrast failures were fixed (`4705766`) with the locked `--accent-default` untouched, re-run 4/4 PASS.

Three residuals remain — all NON-SILENT, tracked, and owned outside code: (1) the LCP 2500ms verdict is deferred to a Vercel-preview re-measure (real CDN), (2) es-CO native copy sign-off awaits a human reviewer, (3) the MathML SR announcement is a recorded waiver. A fourth tracked item — TOC/footnotes/TheoremBlock/Figure live-content coverage — is an author-approved waiver: the components are unit-tested and Plan-A render-path-proven, but no currently-published body exercises them, and per the IA rule no demo paper was fabricated; the empty-TOC artifact that made this user-visible is fixed (graceful degrade). None of these block the phase GOAL.

**Overall verdict: GOAL MET WITH TRACKED RESIDUALS.**

---

_Verified: 2026-05-29T19:12:37Z_
_Verifier: Claude (gsd-verifier)_

---
phase: 03.1-research-reading-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - velite.config.ts
  - lib/velite-shim.ts
  - components/MDXRenderer.tsx
  - app/(lab)/research/layout.tsx
  - app/(lab)/research/_spike/page.tsx
  - content/research/spike-katex.es.mdx
  - tests/e2e/research-math-spike.spec.ts
  - docs/03.1-RENDER-PATH.md
autonomous: false
requirements: [LAB-03]
user_setup: []

must_haves:
  truths:
    - "A research MDX body containing a real $…$/\\tag display equation renders as a .katex node (not .katex-error) on a live /research/* route under BOTH `next dev --turbopack` AND `pnpm build && pnpm start`"
    - "The research collection emits a compiled MDX body string into .velite/research.json that an RSC can evaluate with new Function(code)(runtime).default"
    - "The locale-split mechanism is settled: the collection globs *.{es,en}.mdx and carries a derived `locale` field"
    - "rehype-citation / @citation-js appear NOWHERE in the dependency tree or config (v2 deferral)"
  artifacts:
    - path: "velite.config.ts"
      provides: "research collection with s.mdx() compiled body + the full spec §0 plugin arrays in order; *.{es,en}.mdx glob; derived locale field"
      contains: "remarkMath"
    - path: "components/MDXRenderer.tsx"
      provides: "Resurrected RSC renderer — new Function(code)(runtime).default"
      contains: "new Function"
    - path: "app/(lab)/research/layout.tsx"
      provides: "Route-scoped katex.min.css import (scopes KaTeX CSS to /research/*)"
      contains: "katex/dist/katex.min.css"
    - path: "content/research/spike-katex.es.mdx"
      provides: "One fixture body with a real $…$ + \\tag display equation to exercise the math pipeline"
      contains: "\\tag"
    - path: "docs/03.1-RENDER-PATH.md"
      provides: "Recorded resolved versions of katex/remark-math/rehype-katex vs Velite's bundled unified majors + spike verdict + fallback note"
  key_links:
    - from: "velite.config.ts s.mdx() rehypePlugins"
      to: "rehype-katex"
      via: "KaTeX renders at Velite build time (Node prebuild/beforeCompile), not the webpack/Turbopack resolver"
      pattern: "rehypeKatex"
    - from: "app/(lab)/research/layout.tsx"
      to: "katex/dist/katex.min.css"
      via: "route-scoped CSS import"
      pattern: "katex/dist/katex.min.css"
---

<objective>
GATING SPIKE. Prove the build-time KaTeX math render path works end-to-end before any downstream `/research` work begins. Install the math dependencies (pinned + version-checked against Velite's bundled unified/mdx majors), add a compiled MDX body to the `research` collection with the full spec §0 plugin chain, resurrect the deleted RSC `MDXRenderer`, scope KaTeX CSS to `/research/*`, settle the locale-split glob mechanism, and author ONE fixture body with a real display equation. Verify a `.katex` node renders (and is NOT `.katex-error`) under BOTH `next dev --turbopack` AND `pnpm build && pnpm start`, then confirm live with the Evidence Collector.

Purpose: Phase 2/3 shipped regressions that passed tsc + vitest but produced HTTP 500 on the deployed site (Turbopack ignored webpack config; JSON↔Date round-trips; `throwOnError:false` hid bad macros). This plan de-risks ALL of those classes before Plans B and C build on top. Nothing downstream builds until this plan returns ✓.

Output: math deps installed + pinned, `velite.config.ts` compiling a body with KaTeX + the spec §0 plugin order, `components/MDXRenderer.tsx`, `app/(lab)/research/layout.tsx`, one `*.es.mdx` spike fixture, a passing `tests/e2e/research-math-spike.spec.ts`, and `docs/03.1-RENDER-PATH.md` recording resolved versions + the spike verdict.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-research-reading-surface/03.1-CONTEXT.md
@.planning/phases/03.1-research-reading-surface/03.1-VALIDATION.md
@docs/superpowers/specs/2026-05-29-research-reading-surface-design.md
@./CLAUDE.md

<interfaces>
<!-- Current velite.config.ts research collection — NO body field exists yet. -->
From velite.config.ts (current state — the real starting point):
```ts
export const researchSchema = s.object({
  slug: s.string().regex(/^[a-z0-9-]+$/),
  title_es: s.string().min(1),
  title_en: s.string().min(1),
  authors: s.array(s.string()).min(1),
  date: s.coerce.date(),
  type: s.enum(['paper', 'decision-memo', 'write-up', 'talk']),
  external_url: s.string().url().optional(),
  summary_es: s.string().min(1),
  summary_en: s.string().min(1),
  tags: s.array(s.string()).default([]),
  order: s.number().int().positive().optional(),
})
const research = defineCollection({
  name: 'Research',
  pattern: 'research/*.mdx',           // changes to research/*.{es,en}.mdx in this plan
  schema: researchSchema,               // gains a compiled `body` field + derived `locale` on the COLLECTION (not the exported schema)
})
```

<!-- The exact MDXRenderer to resurrect from git 5056bb7^:components/MDXRenderer.tsx -->
From git 5056bb7^:components/MDXRenderer.tsx (recover verbatim):
```tsx
// Server Component (RSC) — no client directive.
import * as runtime from 'react/jsx-runtime'
export interface MDXRendererProps { code: string }
export function MDXRenderer({ code }: MDXRendererProps): React.JSX.Element {
  const MdxComponent = new Function(code)(runtime).default as () => React.JSX.Element
  return <MdxComponent />
}
```

<!-- velite-shim.ts revives `date` only; body is a string and round-trips fine — do NOT add Date logic for new fields -->
From lib/velite-shim.ts: `research.map(r => ({ ...r, date: new Date(r.date) }))` — KEEP AS-IS. The compiled body is a string; it round-trips through JSON without coercion.
</interfaces>

# Version-pin reality (verified at planning time):
# - velite@0.3.1 bundles the unified/mdast/hast majors; @mdx-js/mdx is a NESTED dep (NOT top-level). Do NOT add @mdx-js/mdx top-level.
# - remark-math + rehype-katex must resolve against those nested unified majors. Verify before proceeding.
# - next-mdx-remote@^6.0.0 already installed (fallback path only).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install + pin math deps, version-check against Velite's unified majors</name>
  <files>package.json, pnpm-lock.yaml, docs/03.1-RENDER-PATH.md</files>
  <read_first>
    - package.json, pnpm-lock.yaml (current deps; velite@^0.3.1, next-mdx-remote@^6.0.0 present; katex/remark-math/rehype-katex NOT installed)
    - spec §0 "Adopt" + §5 "Dependency add (Plan A, reviewer BLOCKER-1)" — pin versions, verify resolution against Velite's bundled unified/mdast/hast majors; @mdx-js/mdx resolves as a nested dep (no top-level add)
    - spec §0 "Deferred to v2" — NO rehype-citation, NO @citation-js
  </read_first>
  <action>
    Run: `pnpm add katex remark-math rehype-katex remark-gfm remark-directive rehype-slug rehype-autolink-headings rehype-pretty-code`
    Then PIN exact versions in package.json (replace any caret ranges these add with the exact versions pnpm resolved).
    Verify unified-major compatibility (the Phase-2/3 burn class): run `pnpm why unified` and `pnpm why mdast-util-from-markdown` and confirm remark-math/rehype-katex resolved against the SAME unified major Velite bundles (no duplicate unified majors). If a duplicate major appears, add a pnpm `overrides` entry to dedupe to Velite's major and document it.
    Create `docs/03.1-RENDER-PATH.md` and record: the exact resolved versions of katex, remark-math, rehype-katex (and the other plugins); the unified major they resolved against; the `pnpm why` output summary; and a one-line note that the documented fallback (`next-mdx-remote/rsc` + `s.raw()`, deps already present) was NOT needed (or WAS, with reason).
    Do NOT add @mdx-js/mdx top-level. Do NOT add rehype-citation or @citation-js anywhere.
  </action>
  <acceptance_criteria>
    - `grep -q '"katex"' package.json && grep -q '"remark-math"' package.json && grep -q '"rehype-katex"' package.json`
    - `grep -q '"remark-gfm"' package.json && grep -q '"rehype-slug"' package.json` (full §0 chain present)
    - `! grep -q 'rehype-citation' package.json && ! grep -q '@citation-js' package.json`
    - `! grep -q '"@mdx-js/mdx"' package.json` (stays nested)
    - `test -f docs/03.1-RENDER-PATH.md && grep -qi 'unified' docs/03.1-RENDER-PATH.md`
    - `pnpm install` exits 0 (lockfile consistent)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"rehype-katex"' package.json && ! grep -q 'rehype-citation' package.json && test -f docs/03.1-RENDER-PATH.md</automated>
  </verify>
  <done>Math + §0 plugins installed and pinned, no citation libs anywhere, unified major dedup verified and recorded in docs/03.1-RENDER-PATH.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire compiled body + spec §0 plugin chain + locale-split glob into velite.config.ts; resurrect MDXRenderer; route-scoped KaTeX CSS; author spike fixture</name>
  <files>velite.config.ts, components/MDXRenderer.tsx, app/(lab)/research/layout.tsx, content/research/spike-katex.es.mdx, lib/velite-shim.ts, app/(lab)/research/page.tsx</files>
  <read_first>
    - velite.config.ts (current — no body field; pattern `research/*.mdx`; schema exported for tests)
    - lib/velite-shim.ts (Date revival only — body is a string, do NOT add Date logic)
    - git `5056bb7^:components/MDXRenderer.tsx` (resurrect verbatim — see <interfaces>)
    - spec §5 "Render path" + "KaTeX options" + "CSS/fonts" + §0 plugin-order block
    - spec §3.2 locale-split (glob `*.{es,en}.mdx`, derive `locale` field)
    - app/(lab)/research/page.tsx, app/(lab)/layout.tsx (RSC-pure; FOUND-11 — NO wagmi/nuqs imports under (lab))
  </read_first>
  <behavior>
    - velite build emits `.velite/research.json` where each entry has a `body` string field whose code, when run via `new Function(body)(runtime).default`, renders the fixture's $…$/\tag equation as KaTeX HTML containing `.katex`.
    - Each entry carries a derived `locale` field equal to `es` or `en` (parsed from the `*.{es,en}.mdx` filename).
    - The exported `researchSchema` (used by unit tests without the build pipeline) is unchanged in this plan — body/locale live on the COLLECTION transform, not the exported schema (frontmatter field additions land in Plan B).
  </behavior>
  <action>
    In `velite.config.ts`:
    1. Import plugins at top:
       `import remarkMath from 'remark-math'`, `import remarkGfm from 'remark-gfm'`, `import remarkDirective from 'remark-directive'`,
       `import rehypeSlug from 'rehype-slug'`, `import rehypeAutolinkHeadings from 'rehype-autolink-headings'`, `import rehypeKatex from 'rehype-katex'`, `import rehypePrettyCode from 'rehype-pretty-code'`.
    2. Change the collection `pattern` to `'research/*.{es,en}.mdx'`.
    3. Add a compiled body + derived locale to the collection. Use `s.mdx()` with the EXACT plugin order from spec §0:
       remarkPlugins: `[remarkMath, remarkGfm, remarkDirective]`
       rehypePlugins: `[rehypeSlug, rehypeAutolinkHeadings, [rehypeKatex, { output: 'htmlAndMathml', throwOnError: false }], rehypePrettyCode]`
       Extend the collection schema object with `body: s.mdx({ remarkPlugins: [...], rehypePlugins: [...] })` and a computed `locale` field derived from the source filename (`.es.mdx`→`'es'` / `.en.mdx`→`'en'`) via `s.path()` + a `.transform()` (use whichever Velite 0.3.1 supports; record the chosen mechanism in docs/03.1-RENDER-PATH.md).
       KaTeX options are LOCKED: `output: 'htmlAndMathml'`, `throwOnError: false`.
    4. Do NOT touch the exported `researchSchema` const (Plan B owns frontmatter field additions).
    Resurrect `components/MDXRenderer.tsx` verbatim from git `5056bb7^` (the 6-line `new Function(code)(runtime).default` RSC pattern — see <interfaces>). No 'use client'.
    Create `app/(lab)/research/layout.tsx` as a pure RSC layout that imports `'katex/dist/katex.min.css'` and renders `{children}` (scopes KaTeX CSS to `/research/*`; Turbopack-safe). NO wagmi/nuqs/RainbowKit imports (FOUND-11).
    Author `content/research/spike-katex.es.mdx`: frontmatter matching the CURRENT researchSchema — slug `spike-katex` (slug regex `^[a-z0-9-]+$` forbids underscore, so the slug FIELD is `spike-katex`; the filename is `spike-katex.es.mdx`), title_es/title_en, authors, date (string), type `write-up`, summary_es/summary_en, order omitted. Body MUST contain a real display equation with `\tag`, e.g.:
      `$$\Pi(\sigma_T) \approx \hat{K}\cdot\sigma_T \tag{1}$$`
      plus an inline `$\hat{\beta}=+0.137$`.
    Update `app/(lab)/research/page.tsx` ONLY if the new `body`/`locale` fields break the existing sort/render — keep it type-checking and rendering; do NOT build the filter (Plan B). The spike fixture WILL now appear in the index list; that is acceptable until Plan B's migration.
    Update `lib/velite-shim.ts` ONLY if the added fields break the typed map — body is a string and round-trips; do NOT add Date coercion for any new field.
  </action>
  <acceptance_criteria>
    - `grep -q 'remarkMath' velite.config.ts && grep -q 'rehypeKatex' velite.config.ts`
    - `grep -q "htmlAndMathml" velite.config.ts && grep -q 'throwOnError' velite.config.ts`
    - `grep -q 'es,en' velite.config.ts` (locale-split glob `research/*.{es,en}.mdx`)
    - `grep -q 'new Function' components/MDXRenderer.tsx`
    - `grep -q 'katex/dist/katex.min.css' 'app/(lab)/research/layout.tsx'`
    - `grep -q '\\tag' content/research/spike-katex.es.mdx`
    - `! grep -rq 'NuqsAdapter' 'app/(lab)/'` (FOUND-11 barrier intact)
    - `pnpm exec velite build` exits 0 and `node -e "const d=require('./.velite/research.json'); process.exit(d.some(e=>typeof e.body==='string'&&e.body.length>0)?0:1)"` exits 0
    - `pnpm test:quick` (biome + tsc + vitest) green
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec velite build && node -e "const d=require('./.velite/research.json'); process.exit(d.some(e=>typeof e.body==='string'&&e.body.includes('katex'))?0:1)" && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>velite build compiles a KaTeX-rendered body into .velite/research.json (body string contains `katex` markup), MDXRenderer resurrected, KaTeX CSS route-scoped, locale-split glob in place, spike fixture authored, tsc + vitest green.</done>
</task>

<task type="auto">
  <name>Task 3: Render-path e2e — assert .katex present AND NOT .katex-error under production build</name>
  <files>app/(lab)/research/_spike/page.tsx, tests/e2e/research-math-spike.spec.ts, docs/03.1-RENDER-PATH.md</files>
  <read_first>
    - 03.1-VALIDATION.md "Render-path spike (§5)" row — `playwright test tests/e2e/research-math-spike.spec.ts`; a `/research/*` route renders `.katex` AND asserts NOT `.katex-error`; verified under production build
    - spec §8 e2e bullet ("renders a `.katex`/`math` node AND asserts it is NOT `.katex-error`")
    - spec §9 risk row (validate under both `dev --turbopack` and `build && start`)
    - playwright.config.ts (production `webServer`: `pnpm build && pnpm start -p 3040` — Decision from STATE.md Plan 03-02)
    - components/MDXRenderer.tsx (renders the compiled body), @/.velite shim (exports `research` with `body`/`locale`)
  </read_first>
  <action>
    Create a minimal render surface so e2e can hit a real KaTeX DOM node BEFORE the full `[slug]` page exists (Plan C builds that): add `app/(lab)/research/_spike/page.tsx` — a pure RSC that imports `{ research }` from `@/.velite`, finds the `spike-katex` (`locale === 'es'`) entry, and renders `<MDXRenderer code={entry.body} />`. It inherits the route-scoped KaTeX CSS from `app/(lab)/research/layout.tsx`. (This temporary route is consumed/removed by Plan C — note that in the SUMMARY.)
    Write `tests/e2e/research-math-spike.spec.ts` (production-build webServer):
      - navigate to `/research/_spike`; assert response 200
      - assert `page.locator('.katex')` count ≥ 1 (display equation rendered)
      - assert `page.locator('.katex-error')` count === 0 (BURN-CLASS GUARD: `throwOnError:false` + `ignoreBuildErrors` can hide bad macros — this assertion is mandatory)
      - assert the `\tag{1}` number `(1)` text is present in the rendered DOM
    Run against the production build: `pnpm exec playwright test tests/e2e/research-math-spike.spec.ts` (config webServer does `pnpm build && pnpm start`).
    SEPARATELY verify the dev path once and RECORD it in docs/03.1-RENDER-PATH.md: `next dev --turbopack`, visit `/research/_spike`, confirm `.katex` present + no `.katex-error` — record verdict + date (the two render engines diverging is the documented burn class).
  </action>
  <acceptance_criteria>
    - `test -f tests/e2e/research-math-spike.spec.ts`
    - `grep -q "katex-error" tests/e2e/research-math-spike.spec.ts` (the NOT-error assertion exists)
    - `pnpm exec playwright test tests/e2e/research-math-spike.spec.ts` passes (production webServer)
    - `grep -qi 'turbopack' docs/03.1-RENDER-PATH.md` (dev-path verdict recorded)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/research-math-spike.spec.ts</automated>
  </verify>
  <done>e2e proves a `.katex` node renders and NO `.katex-error` appears under the production build; dev-turbopack verdict recorded in docs/03.1-RENDER-PATH.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 4: Evidence Collector live-verify — KaTeX render path (GATING)</name>
<action>Invoke the Evidence Collector agent (live-verification mode) per ./CLAUDE.md against /research/_spike. See <how-to-verify> for the exact browser_navigate / browser_snapshot / browser_evaluate steps and pass/fail verdicts. This GATING gate blocks Plans B and C until ✓.</action>
  <what-built>
    Build-time KaTeX render path: math deps pinned, velite.config compiles a body with the spec §0 plugin chain, MDXRenderer resurrected, KaTeX CSS scoped to /research/*, locale-split glob settled, a spike fixture with a real display equation, and a passing production-build e2e asserting `.katex` present + NOT `.katex-error`.
  </what-built>
  <how-to-verify>
    Invoke the Evidence Collector agent per ./CLAUDE.md (live-verification mode) — this is the GATING gate; downstream plans are blocked on its ✓:
    1. Target the deployed `/research/_spike` (fall back to local `pnpm build && pnpm start -p 3040` if the deploy hasn't propagated — the deployed prod build, never `next dev`, is the ground truth here).
    2. `browser_navigate` to `/research/_spike`.
    3. `browser_snapshot` — assert a `.katex` display node is present and visually typeset (not raw `$$…$$` source text).
    4. `browser_evaluate` — assert `document.querySelectorAll('.katex-error').length === 0` (silent-macro-failure burn-class guard).
    5. `browser_console_messages` — surface any runtime errors.
    6. `browser_take_screenshot` → `/tmp/d2p-verify/03.1-01-katex-spike.png`.
    7. Append verdicts (✓/⚠/✗/⊘) + screenshot path to `.planning/phases/03.1-research-reading-surface/03.1-LIVE-VERIFICATION.md`.
    Expected: equation visually typeset, zero `.katex-error`, no console errors.
  </how-to-verify>
  <resume-signal>Type "approved" once the Evidence Collector records ✓ for the KaTeX render (or describe the ✗ so the render path is fixed before any downstream plan starts).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm exec velite build` exits 0; `.velite/research.json` entries carry a non-empty compiled `body` string containing KaTeX markup.
- `pnpm exec playwright test tests/e2e/research-math-spike.spec.ts` passes against the production webServer: `.katex` present, `.katex-error` count 0.
- `! grep -rq 'NuqsAdapter' app/(lab)/` and `! grep -rq 'rehype-citation\|@citation-js' .` (FOUND-11 + v2 deferral intact).
- Evidence Collector ✓ recorded in 03.1-LIVE-VERIFICATION.md.
- docs/03.1-RENDER-PATH.md records resolved versions, unified dedup, locale-derivation mechanism, and the dev-turbopack verdict.
</verification>

<success_criteria>
- Build-time KaTeX renders one display equation as a `.katex` node (NOT `.katex-error`) under BOTH `pnpm build && pnpm start` (e2e) AND `next dev --turbopack` (recorded manual verdict), live-confirmed by Evidence Collector.
- The locale-split glob (`research/*.{es,en}.mdx`) + derived `locale` field is settled and documented.
- No citation libraries anywhere; FOUND-11 (lab) bundle barrier intact.
- Downstream Plans B and C are unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-research-reading-surface/03.1-01-SUMMARY.md` recording: resolved dep versions, the chosen locale-derivation mechanism, the spike e2e result, the Evidence Collector verdict, and whether the temporary `_spike` route is kept or handed to Plan C for removal.
</output>

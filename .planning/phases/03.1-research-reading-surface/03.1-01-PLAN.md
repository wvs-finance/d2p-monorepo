---
phase: 03.1-research-reading-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - velite.config.ts
  - lib/velite-shim.ts
  - components/MDXRenderer.tsx
  - app/(lab)/research/layout.tsx
  - app/(lab)/research/spike/page.tsx
  - content/research/spike-katex.es.mdx
  - content/research/spike-katex.en.mdx
  - tests/e2e/research-math-spike.spec.ts
  - docs/03.1-RENDER-PATH.md
autonomous: false
requirements: [LAB-03]
user_setup: []

must_haves:
  truths:
    - "A research MDX body containing a real $…$/\\tag display equation renders as a .katex node (not .katex-error) on a live /research/* route under BOTH `next dev --turbopack` AND `pnpm build && pnpm start`"
    - "A research body containing at least one CUSTOM component (a `<Figure>` element or a `:::theorem` directive) renders correctly when a stub `components` map is passed to MDXRenderer — proving the components-PROP injection path Plan C depends on (Velite compiles with outputFormat:'function-body' + NO providerImportSource, so custom components resolve via the prop, never useMDXComponents)"
    - "The research collection emits a compiled MDX body string into .velite/research.json that an RSC can evaluate with `new Function(code)(runtime).default` and render via `<C components={map} />`"
    - "The locale-split mechanism is settled: the collection globs research/*.{es,en}.mdx and carries a derived `locale` field equal to 'es' or 'en' for every entry (gated by a node check over .velite/research.json), authored from BOTH a .es.mdx AND .en.mdx arm"
    - "Velite's auto-gfm is disabled (`gfm: false`) so the spec §0 remark order [remarkMath, remarkGfm, remarkDirective] is honored — remarkMath runs first, remarkGfm is registered exactly once"
    - "rehype-citation / @citation-js appear NOWHERE in the dependency tree or config (v2 deferral)"
  artifacts:
    - path: "velite.config.ts"
      provides: "research COLLECTION schema gains s.mdx() compiled body (gfm:false + spec §0 plugin arrays in order) + s.toc() + derived locale field; *.{es,en}.mdx glob; a remark-directive→hName plugin mapping :::theorem to TheoremBlock; the exported researchSchema const is SPLIT out and unchanged in this plan"
      contains: "gfm: false"
    - path: "components/MDXRenderer.tsx"
      provides: "Resurrected RSC renderer EXTENDED with an optional `components` prop — `const C = new Function(code)(runtime).default; return <C components={components} />`"
      contains: "components"
    - path: "app/(lab)/research/layout.tsx"
      provides: "Route-scoped katex.min.css import + <link rel=preload> for KaTeX_Main-Regular + KaTeX_Math-Italic + font-display:swap (v1 LCP approach; byte-subsetting deferred to v2)"
      contains: "katex/dist/katex.min.css"
    - path: "content/research/spike-katex.es.mdx"
      provides: "One es fixture body with a real $…$ + \\tag display equation AND one custom component (<Figure> or :::theorem) to exercise both the math pipeline AND the components-prop injection path"
      contains: "\\tag"
    - path: "content/research/spike-katex.en.mdx"
      provides: "The en arm of the spike fixture — proves the *.{es,en}.mdx glob + derived locale across both arms"
      contains: "\\tag"
    - path: "docs/03.1-RENDER-PATH.md"
      provides: "Resolved versions of katex/remark-math/rehype-katex vs Velite's bundled unified majors; the locale-derivation mechanism; the components-prop injection finding; the next-mdx-remote/rsc fallback EXERCISE result (or explicit waiver); spike verdict + dev-turbopack verdict"
  key_links:
    - from: "velite.config.ts s.mdx() rehypePlugins"
      to: "rehype-katex"
      via: "KaTeX renders at Velite build time (Node prebuild/beforeCompile), not the webpack/Turbopack resolver"
      pattern: "rehypeKatex"
    - from: "components/MDXRenderer.tsx"
      to: "the compiled body's custom components"
      via: "components PROP passed to the compiled default export (function-body output, no providerImportSource)"
      pattern: "components"
    - from: "app/(lab)/research/layout.tsx"
      to: "katex/dist/katex.min.css"
      via: "route-scoped CSS import + font preload"
      pattern: "katex/dist/katex.min.css"
---

<objective>
GATING SPIKE. Prove the build-time KaTeX math render path AND the custom-component injection path work end-to-end before any downstream `/research` work begins. Install the math dependencies (pinned + version-checked against Velite's bundled unified/mdx majors), add a compiled MDX body to the `research` collection with the full spec §0 plugin chain (with Velite's auto-gfm disabled so the order is honored), resurrect AND EXTEND the deleted RSC `MDXRenderer` to accept a `components` prop, add a remark-directive→`TheoremBlock` hName plugin, scope KaTeX CSS to `/research/*` with font preload + `font-display:swap`, settle the locale-split glob across BOTH a `.es.mdx` and `.en.mdx` arm, and author a spike fixture whose body contains a real display equation AND a custom component. Verify a `.katex` node renders (NOT `.katex-error`) AND the custom component renders, under BOTH `next dev --turbopack` AND `pnpm build && pnpm start`, then confirm live with the Evidence Collector. Also EXERCISE (not assume) the `next-mdx-remote/rsc` + `s.raw()` fallback once and record the result.

Purpose: Phase 2/3 shipped regressions that passed tsc + vitest but produced HTTP 500 on the deployed site (Turbopack ignored webpack config; JSON↔Date round-trips; `throwOnError:false` hid bad macros; and — newly verified against velite@0.3.1 source — Velite compiles MDX with `outputFormat:'function-body'` and NO `providerImportSource`, so a no-prop renderer THROWS on any custom component, and Velite auto-prepends `remarkGfm` before user plugins, breaking the math-first order). This plan de-risks ALL of those classes before Plans B and C build on top. Nothing downstream builds until this plan returns ✓.

Output: math deps installed + pinned, `velite.config.ts` compiling a body with KaTeX + the spec §0 plugin order (gfm:false) + s.toc() + the theorem-directive plugin, an EXTENDED `components/MDXRenderer.tsx`, a font-preloading `app/(lab)/research/layout.tsx`, `*.es.mdx` + `*.en.mdx` spike fixtures with math + a custom component, a passing `tests/e2e/research-math-spike.spec.ts`, and `docs/03.1-RENDER-PATH.md` recording resolved versions, the injection finding, the fallback exercise, and the spike verdict.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-research-reading-surface/03.1-CONTEXT.md
@.planning/phases/03.1-research-reading-surface/03.1-VALIDATION.md
@docs/superpowers/specs/2026-05-29-research-reading-surface-design.md
@./CLAUDE.md

<interfaces>
<!-- Current velite.config.ts — researchSchema and the collection are the SAME object today (`schema: researchSchema`). This plan SPLITS them: the COLLECTION schema gains body/locale/toc; the exported `researchSchema` const (consumed by unit tests) keeps the Phase-2 frontmatter shape unchanged in this plan. -->
From velite.config.ts (current state — the real starting point):
```ts
export const researchSchema = s.object({
  slug: s.string().regex(/^[a-z0-9-]+$/),
  title_es: s.string().min(1),
  title_en: s.string().min(1),
  authors: s.array(s.string()).min(1),
  date: s.coerce.date(),
  type: s.enum(['paper', 'decision-memo', 'write-up', 'talk']),
  external_url: s.string().url().optional(),
  summary_es: s.string().min(1),
  summary_en: s.string().min(1),
  tags: s.array(s.string()).default([]),
  order: s.number().int().positive().optional(),
})
const research = defineCollection({
  name: 'Research',
  pattern: 'research/*.mdx',           // → research/*.{es,en}.mdx in this plan
  schema: researchSchema,              // → SPLIT: collection schema = researchSchema fields + body + locale + toc
})
```

<!-- MDXRenderer to resurrect from git 5056bb7^:components/MDXRenderer.tsx, then EXTEND with a components prop.
     VERIFIED against velite@0.3.1 source (node_modules/velite/dist/index.js ~L4982-4999):
     s.mdx() compiles with outputFormat:'function-body' and passes NO providerImportSource.
     The compiled default export resolves custom components via the `components` PROP, NOT useMDXComponents().
     A no-prop renderer therefore THROWS on any custom component. Extend it: -->
Target shape of components/MDXRenderer.tsx (resurrect the 6-line base, then extend):
```tsx
// Server Component (RSC) — no client directive.
import * as runtime from 'react/jsx-runtime'
export interface MDXRendererProps {
  code: string
  components?: Record<string, React.ComponentType<any>>
}
export function MDXRenderer({ code, components }: MDXRendererProps): React.JSX.Element {
  const C = new Function(code)(runtime).default as (props: { components?: Record<string, React.ComponentType<any>> }) => React.JSX.Element
  return <C components={components} />
}
```

<!-- VERIFIED velite@0.3.1 mdx behavior (node_modules/velite/dist/index.js ~L4982, ~L4989):
     `enableGfm = options.gfm ?? mdx.gfm ?? true` and `if (enableGfm) remarkPlugins.push(remarkGfm)` runs
     BEFORE `remarkPlugins.push(...options.remarkPlugins)`. So WITHOUT `gfm:false`, your
     [remarkMath, remarkGfm, remarkDirective] actually runs as
     [remarkGfm(auto), remarkRemoveComments, remarkCopyLinkedFiles, remarkMath, remarkGfm(dup), remarkDirective]
     — math no longer first, remarkGfm double-registered. FIX: set gfm:false and list remarkGfm yourself. -->

<!-- velite ships s.toc() and s.path() — VERIFIED present in `Object.keys(velite.s)`. Use s.toc() for ArticleTOC's data source (Plan C consumes entry.toc); use s.path() to derive locale. -->

<!-- velite-shim.ts revives `date` only; body is a string and round-trips fine — do NOT add Date logic for new fields -->
From lib/velite-shim.ts: `research.map(r => ({ ...r, date: new Date(r.date) }))` — KEEP AS-IS. The compiled body is a string; it round-trips through JSON without coercion.
</interfaces>

# Version-pin reality (verified at planning time):
# - velite@0.3.1 bundles the unified/mdast/hast majors; @mdx-js/mdx is a NESTED dep (NOT top-level). Do NOT add @mdx-js/mdx top-level.
# - remark-math + rehype-katex must resolve against those nested unified majors. Verify before proceeding.
# - next-mdx-remote@^6.0.0 already installed (fallback path — EXERCISE it once, do not merely note it).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install + pin math deps, version-check against Velite's unified majors</name>
  <files>package.json, pnpm-lock.yaml, docs/03.1-RENDER-PATH.md</files>
  <read_first>
    - package.json, pnpm-lock.yaml (current deps; velite@^0.3.1, next-mdx-remote@^6.0.0 present; katex/remark-math/rehype-katex NOT installed)
    - spec §0 "Adopt" + §5 "Dependency add (Plan A, reviewer BLOCKER-1)" — pin versions, verify resolution against Velite's bundled unified/mdast/hast majors; @mdx-js/mdx resolves as a nested dep (no top-level add)
    - spec §0 "Deferred to v2" — NO rehype-citation, NO @citation-js
    - the MINOR on rehype-pretty-code: ONLY install/wire it if a fixture has a fenced code block; otherwise DROP it from v1 (Shiki adds LCP weight) — decide in Task 2 and record in docs/03.1-RENDER-PATH.md. (If undecided here, install it but you MAY remove it in Task 2 if no fixture uses code.)
  </read_first>
  <action>
    Run: `pnpm add katex remark-math rehype-katex remark-gfm remark-directive rehype-slug rehype-autolink-headings` (rehype-pretty-code only if you will wire it — see read_first; otherwise omit it).
    Then PIN exact versions in package.json (replace any caret ranges these add with the exact versions pnpm resolved).
    Verify unified-major compatibility (the Phase-2/3 burn class): run `pnpm why unified` and `pnpm why mdast-util-from-markdown` and confirm remark-math/rehype-katex resolved against the SAME unified major Velite bundles (no duplicate unified majors). If a duplicate major appears, add a pnpm `overrides` entry to dedupe to Velite's major and document it.
    Create `docs/03.1-RENDER-PATH.md` and record: the exact resolved versions of katex, remark-math, rehype-katex (and the other plugins); the unified major they resolved against; the `pnpm why` output summary; whether rehype-pretty-code is in v1 or dropped; and a placeholder section for the components-prop finding + the next-mdx-remote/rsc fallback exercise (filled in Task 2/3).
    Do NOT add @mdx-js/mdx top-level. Do NOT add rehype-citation or @citation-js anywhere.
  </action>
  <acceptance_criteria>
    - `grep -q '"katex"' package.json && grep -q '"remark-math"' package.json && grep -q '"rehype-katex"' package.json`
    - `grep -q '"remark-gfm"' package.json && grep -q '"rehype-slug"' package.json && grep -q '"remark-directive"' package.json` (full §0 chain present)
    - `! grep -q 'rehype-citation' package.json && ! grep -q '@citation-js' package.json`
    - `! grep -q '"@mdx-js/mdx"' package.json` (stays nested)
    - `test -f docs/03.1-RENDER-PATH.md && grep -qi 'unified' docs/03.1-RENDER-PATH.md`
    - `pnpm install` exits 0 (lockfile consistent)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"rehype-katex"' package.json && ! grep -q 'rehype-citation' package.json && test -f docs/03.1-RENDER-PATH.md</automated>
  </verify>
  <done>Math + §0 plugins installed and pinned, no citation libs anywhere, unified major dedup verified and recorded in docs/03.1-RENDER-PATH.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire compiled body (gfm:false + spec §0 order) + s.toc() + theorem-directive plugin + locale-split glob into velite.config.ts; SPLIT + extend MDXRenderer; font-preloading KaTeX layout; author es+en spike fixtures with math + a custom component</name>
  <files>velite.config.ts, components/MDXRenderer.tsx, app/(lab)/research/layout.tsx, content/research/spike-katex.es.mdx, content/research/spike-katex.en.mdx, lib/velite-shim.ts, app/(lab)/research/page.tsx, docs/03.1-RENDER-PATH.md</files>
  <read_first>
    - velite.config.ts (current — no body field; pattern `research/*.mdx`; `schema: researchSchema` — collection and exported schema are the SAME object today)
    - node_modules/velite/dist/index.js ~L4982-4999 (the gfm-auto-prepend + outputFormat:'function-body' + no-providerImportSource behavior — see <interfaces>)
    - lib/velite-shim.ts (Date revival only — body is a string, do NOT add Date logic)
    - git `5056bb7^:components/MDXRenderer.tsx` (resurrect the 6-line base, then EXTEND with a components prop — see <interfaces>)
    - spec §5 "Render path" + "KaTeX options" + "CSS/fonts" + §0 plugin-order block; spec §5 unicode-range fallback for KaTeX fonts
    - spec §3.2 locale-split (glob `*.{es,en}.mdx`, derive `locale` field)
    - app/(lab)/research/page.tsx, app/(lab)/layout.tsx (RSC-pure; FOUND-11 — NO wagmi/nuqs imports under (lab))
  </read_first>
  <behavior>
    - velite build emits `.velite/research.json` where each entry has a `body` string whose code, when run via `new Function(body)(runtime).default` and rendered with a `components` map, renders the fixture's $…$/\tag equation as KaTeX HTML containing `.katex` AND renders the custom component (Figure/TheoremBlock) using the passed map.
    - With `gfm: false` set on `s.mdx({...})`, the remark order is exactly `[remarkMath, remarkGfm, remarkDirective]` — math is evaluated first, gfm registered once.
    - Each entry carries a derived `locale` field equal to `es` or `en` (parsed from the `*.{es,en}.mdx` filename via s.path()).
    - Each entry carries a `toc` field from s.toc() (Plan C's ArticleTOC consumes it).
    - The exported `researchSchema` const (used by unit tests without the build pipeline) is SPLIT out and UNCHANGED in this plan — body/locale/toc live on the COLLECTION schema only; frontmatter field additions land in Plan B.
  </behavior>
  <action>
    In `velite.config.ts`:
    1. Import plugins at top:
       `import remarkMath from 'remark-math'`, `import remarkGfm from 'remark-gfm'`, `import remarkDirective from 'remark-directive'`,
       `import rehypeSlug from 'rehype-slug'`, `import rehypeAutolinkHeadings from 'rehype-autolink-headings'`, `import rehypeKatex from 'rehype-katex'`. (Import `rehypePrettyCode` ONLY if you keep it per the Task-1 decision; if no fixture uses fenced code, DROP it — record the decision in docs/03.1-RENDER-PATH.md.)
       Also import the directive AST helpers you need (e.g. `import { visit } from 'unist-util-visit'` — already transitively available via unified; add it as a direct dep if `pnpm why unist-util-visit` shows it's only nested and you import it directly).
    2. Add a small inline remark plugin `remarkTheoremDirective` in velite.config.ts: visit `containerDirective` nodes; for names in {theorem, definition, lemma, proof} set `node.data.hName = 'TheoremBlock'` and `node.data.hProperties = { kind: node.name, label: node.attributes?.label }`. (remark-directive only emits generic containerDirective nodes — this is what wires `:::theorem` to the `TheoremBlock` component map in Plan C. Do this in the BUILD layer here, not as a Plan C component-map entry.)
    3. SPLIT the schema: keep the exported `researchSchema` const as-is (Phase-2 frontmatter shape). Define the COLLECTION schema as `researchSchema.extend({ body: s.mdx({...}), toc: s.toc(), locale: s.path().transform(p => p.endsWith('.es.mdx') ? 'es' : 'en') })` (verify `s.path()` returns the source path INCLUDING the `.es`/`.en` segment; if it returns a slug only, derive locale from `meta` or the filename via the documented Velite mechanism and record which you used in docs/03.1-RENDER-PATH.md).
    4. Change the collection `pattern` to `'research/*.{es,en}.mdx'`.
    5. The `s.mdx({...})` options MUST be:
       `gfm: false`   ← CRITICAL (suppresses Velite's auto-remarkGfm prepend; verified at velite/dist/index.js L4982/L4989)
       `remarkPlugins: [remarkMath, remarkGfm, remarkDirective, remarkTheoremDirective]`  (spec §0 order; remarkGfm now listed exactly once; theorem plugin after remark-directive)
       `rehypePlugins: [rehypeSlug, rehypeAutolinkHeadings, [rehypeKatex, { output: 'htmlAndMathml', throwOnError: false }]]`  (+ rehypePrettyCode last ONLY if kept)
       KaTeX options are LOCKED: `output: 'htmlAndMathml'`, `throwOnError: false`.
    6. Do NOT touch the exported `researchSchema` const beyond extracting it (Plan B owns frontmatter field additions).
    Resurrect + EXTEND `components/MDXRenderer.tsx` to the shape in <interfaces>: optional `components` prop, `const C = new Function(code)(runtime).default; return <C components={components} />`. Still RSC, no 'use client'.
    Create `app/(lab)/research/layout.tsx` as a pure RSC layout that:
      - imports `'katex/dist/katex.min.css'` (scopes KaTeX CSS to `/research/*`; Turbopack-safe),
      - emits `<link rel="preload" as="font" type="font/woff2" crossorigin>` for the 2–3 most-used KaTeX faces (KaTeX_Main-Regular, KaTeX_Math-Italic — paths under `katex/dist/fonts/`), and applies `font-display: swap` on those faces (per spec §5; v1 LCP approach). Byte-level subsetting via fonttools/pyftsubset is DEFERRED to v2 — do NOT add any Python tooling.
      - NO wagmi/nuqs/RainbowKit imports (FOUND-11).
    Author `content/research/spike-katex.es.mdx` AND `content/research/spike-katex.en.mdx` (BOTH arms — proves the glob + derived locale). Frontmatter matching the CURRENT researchSchema — slug `spike-katex` (slug regex `^[a-z0-9-]+$` forbids underscore, so the slug FIELD is `spike-katex`; filenames are `spike-katex.es.mdx` / `spike-katex.en.mdx`), title_es/title_en, authors, date (string), type `write-up`, summary_es/summary_en, order omitted. EACH body MUST contain:
      - a real display equation with `\tag`, e.g. `$$\Pi(\sigma_T) \approx \hat{K}\cdot\sigma_T \tag{1}$$`, plus an inline `$\hat{\beta}=+0.137$`, AND
      - at least ONE custom component to exercise the components-prop injection: either a `<Figure>…</Figure>` element OR a `:::theorem` directive block (prefer `:::theorem` so the theorem-directive plugin is also exercised). This is the path Plan C depends on.
    Update `app/(lab)/research/page.tsx` ONLY if the new `body`/`locale`/`toc` fields break the existing sort/render — keep it type-checking and rendering; do NOT build the filter (Plan B). The spike fixtures WILL now appear in the index list; acceptable until Plan B's migration.
    Update `lib/velite-shim.ts` ONLY if the added fields break the typed map — body/toc/locale round-trip as plain JSON; do NOT add Date coercion for any new field.
    In docs/03.1-RENDER-PATH.md record: the chosen locale-derivation mechanism, the rehype-pretty-code keep/drop decision, and a one-paragraph "components-prop injection finding" confirming the compiled body resolves `<Figure>`/`TheoremBlock` via the prop (not useMDXComponents).
  </action>
  <acceptance_criteria>
    - `grep -q 'remarkMath' velite.config.ts && grep -q 'rehypeKatex' velite.config.ts`
    - `grep -q 'gfm: false' velite.config.ts` (auto-gfm suppressed — spec §0 order honored)
    - `grep -q 's.toc()' velite.config.ts` (TOC data source for Plan C)
    - `grep -q 'hName' velite.config.ts` (theorem-directive→TheoremBlock plugin present)
    - `grep -q "htmlAndMathml" velite.config.ts && grep -q 'throwOnError' velite.config.ts`
    - `grep -q 'es,en' velite.config.ts` (locale-split glob `research/*.{es,en}.mdx`)
    - `grep -q 'components' components/MDXRenderer.tsx && grep -q 'new Function' components/MDXRenderer.tsx`
    - `grep -q 'katex/dist/katex.min.css' 'app/(lab)/research/layout.tsx' && grep -qi 'preload' 'app/(lab)/research/layout.tsx'`
    - `grep -q '\\tag' content/research/spike-katex.es.mdx && grep -q '\\tag' content/research/spike-katex.en.mdx`
    - `grep -Eq ':::theorem|<Figure' content/research/spike-katex.es.mdx` (custom component present to exercise injection)
    - `! grep -rq 'NuqsAdapter' 'app/(lab)/'` (FOUND-11 barrier intact)
    - `pnpm exec velite build` exits 0 and the body EVALUATES to a function:
      `node -e "const d=require('./.velite/research.json'); const C=new Function(d[0].body)(require('react/jsx-runtime')); process.exit(typeof C.default==='function'?0:1)"`
    - locale gate: `node -e "const d=require('./.velite/research.json'); process.exit(d.every(e=>e.locale==='es'||e.locale==='en')?0:1)"`
    - `pnpm test:quick` (biome + tsc + vitest) green
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'gfm: false' velite.config.ts && pnpm exec velite build && node -e "const d=require('./.velite/research.json'); const C=new Function(d[0].body)(require('react/jsx-runtime')); process.exit(typeof C.default==='function'?0:1)" && node -e "const d=require('./.velite/research.json'); process.exit(d.every(e=>e.locale==='es'||e.locale==='en')?0:1)" && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>velite build compiles a KaTeX+custom-component body into .velite/research.json (body evaluates to a function; gfm:false honored; s.toc + theorem-directive wired), MDXRenderer split out + extended with a components prop, KaTeX CSS route-scoped with font preload + font-display:swap, locale-split glob across es+en arms, both spike fixtures authored with math + a custom component, tsc + vitest green.</done>
</task>

<task type="auto">
  <name>Task 3: Render-path e2e — assert .katex present AND NOT .katex-error AND the custom component rendered, under production build; EXERCISE the next-mdx-remote/rsc fallback</name>
  <files>app/(lab)/research/spike/page.tsx, tests/e2e/research-math-spike.spec.ts, docs/03.1-RENDER-PATH.md</files>
  <read_first>
    - 03.1-VALIDATION.md "Render-path spike (§5)" row — `playwright test tests/e2e/research-math-spike.spec.ts`; a `/research/*` route renders `.katex` AND asserts NOT `.katex-error`; verified under production build
    - spec §8 e2e bullet ("renders a `.katex`/`math` node AND asserts it is NOT `.katex-error`")
    - spec §9 risk row (validate under both `dev --turbopack` and `build && start`); spec §9 "exercised, not assumed" (the next-mdx-remote/rsc fallback must be EXERCISED, not noted)
    - playwright.config.ts (production `webServer`: `pnpm build && pnpm start -p 3040` — Decision from STATE.md Plan 03-02)
    - components/MDXRenderer.tsx (renders the compiled body via the components prop), @/.velite shim (exports `research` with `body`/`locale`/`toc`)
    - NOTE: underscore-prefixed folders are NON-routable in Next — use `app/(lab)/research/spike/` (NO underscore) so the e2e 200 works
  </read_first>
  <action>
    Create a minimal render surface so e2e can hit a real KaTeX DOM node BEFORE the full `[slug]` page exists (Plan C builds that): add `app/(lab)/research/spike/page.tsx` (NO underscore — `_spike` would be non-routable) — a pure RSC that imports `{ research }` from `@/.velite`, finds the `spike-katex` (`locale === 'es'`) entry, and renders `<MDXRenderer code={entry.body} components={{ Figure: StubFigure, TheoremBlock: StubTheorem }} />` with minimal local stub components (so the custom-component injection path is exercised end-to-end before Plan C's real components exist). It inherits the route-scoped KaTeX CSS from `app/(lab)/research/layout.tsx`. (This temporary route is consumed/removed by Plan C — note that in the SUMMARY.)
    Write `tests/e2e/research-math-spike.spec.ts` (production-build webServer):
      - navigate to `/research/spike`; assert response 200
      - assert `page.locator('.katex')` count ≥ 1 (display equation rendered)
      - assert `page.locator('.katex-error')` count === 0 (BURN-CLASS GUARD: `throwOnError:false` + `ignoreBuildErrors` can hide bad macros — mandatory)
      - assert the `\tag{1}` number `(1)` text is present in the rendered DOM
      - assert the custom component rendered (a node from StubFigure/StubTheorem — e.g. a data-testid the stub emits) — proves the components-prop path works under prod build
    Run against the production build: `pnpm exec playwright test tests/e2e/research-math-spike.spec.ts` (config webServer does `pnpm build && pnpm start`).
    SEPARATELY verify the dev path once and RECORD it in docs/03.1-RENDER-PATH.md: `next dev --turbopack`, visit `/research/spike`, confirm `.katex` present + no `.katex-error` + custom component renders — record verdict + date.
    EXERCISE the documented fallback (spec §9 "exercised, not assumed"): in a throwaway node script OR an RSC unit test, compile ONE fixture body via `next-mdx-remote/rsc` (using `s.raw()` source — you may temporarily add a `raw: s.raw()` field to the collection, or read the .mdx source directly) and confirm it renders the same equation. Record PASS/FAIL + the path in docs/03.1-RENDER-PATH.md. If you genuinely cannot exercise it (e.g. RSC test harness gap), record an EXPLICIT waiver with the reason — do not silently skip.
  </action>
  <acceptance_criteria>
    - `test -f 'app/(lab)/research/spike/page.tsx'` (NO underscore — routable)
    - `test -f tests/e2e/research-math-spike.spec.ts`
    - `grep -q "katex-error" tests/e2e/research-math-spike.spec.ts` (the NOT-error assertion exists)
    - `pnpm exec playwright test tests/e2e/research-math-spike.spec.ts` passes (production webServer)
    - `grep -qi 'turbopack' docs/03.1-RENDER-PATH.md` (dev-path verdict recorded)
    - `grep -qi 'next-mdx-remote\|fallback' docs/03.1-RENDER-PATH.md` (fallback exercise/waiver recorded)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/research-math-spike.spec.ts</automated>
  </verify>
  <done>e2e proves a `.katex` node renders, NO `.katex-error` appears, AND the custom component renders under the production build; dev-turbopack verdict + next-mdx-remote/rsc fallback exercise (or explicit waiver) recorded in docs/03.1-RENDER-PATH.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 4: Evidence Collector live-verify — KaTeX render path (GATING)</name>
<action>Invoke the Evidence Collector agent (live-verification mode) per ./CLAUDE.md against /research/spike. See <how-to-verify> for the exact browser_navigate / browser_snapshot / browser_evaluate steps and pass/fail verdicts. This GATING gate blocks Plans B and C until ✓.</action>
  <what-built>
    Build-time KaTeX render path + custom-component injection: math deps pinned, velite.config compiles a body with the spec §0 plugin chain (gfm:false) + s.toc() + a theorem-directive plugin, MDXRenderer split out + extended with a components prop, KaTeX CSS scoped to /research/* with font preload + font-display:swap, es+en spike fixtures with a real display equation AND a custom component, and a passing production-build e2e asserting `.katex` present + NOT `.katex-error` + custom component rendered.
  </what-built>
  <how-to-verify>
    Invoke the Evidence Collector agent per ./CLAUDE.md (live-verification mode) — this is the GATING gate; downstream plans are blocked on its ✓:
    1. Target the deployed `/research/spike` (fall back to local `pnpm build && pnpm start -p 3040` if the deploy hasn't propagated — the deployed prod build, never `next dev`, is the ground truth here).
    2. `browser_navigate` to `/research/spike`.
    3. `browser_snapshot` — assert a `.katex` display node is present and visually typeset (not raw `$$…$$` source text), and the custom component (Figure/Theorem stub) is present.
    4. `browser_evaluate` — assert `document.querySelectorAll('.katex-error').length === 0` (silent-macro-failure burn-class guard) AND `getComputedStyle(document.querySelector('.katex')).fontFamily` includes `KaTeX_Main` (proves the KaTeX math font loaded and was NOT reset by Tailwind v4 @theme/preflight) AND the `\tag` `(1)` text node is present.
    5. `browser_console_messages` — surface any runtime errors.
    6. `browser_take_screenshot` → `/tmp/d2p-verify/03.1-01-katex-spike.png`.
    7. Append verdicts (✓/⚠/✗/⊘) + screenshot path to `.planning/phases/03.1-research-reading-surface/03.1-LIVE-VERIFICATION.md`.
    Expected: equation visually typeset with KaTeX_Main font family, zero `.katex-error`, custom component rendered, no console errors.
  </how-to-verify>
  <resume-signal>Type "approved" once the Evidence Collector records ✓ for the KaTeX render + KaTeX_Main font + custom component (or describe the ✗ so the render path is fixed before any downstream plan starts).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm exec velite build` exits 0; `.velite/research.json` entries carry a compiled `body` that EVALUATES to a function and every entry has `locale` ∈ {es,en} and a `toc`.
- `grep -q 'gfm: false' velite.config.ts` (spec §0 remark order honored — auto-gfm suppressed).
- `pnpm exec playwright test tests/e2e/research-math-spike.spec.ts` passes against the production webServer: `.katex` present, `.katex-error` count 0, custom component rendered.
- `! grep -rq 'NuqsAdapter' app/(lab)/` and `! grep -rq 'rehype-citation\|@citation-js' .` (FOUND-11 + v2 deferral intact).
- Evidence Collector ✓ recorded in 03.1-LIVE-VERIFICATION.md (KaTeX_Main font family asserted).
- docs/03.1-RENDER-PATH.md records resolved versions, unified dedup, locale-derivation mechanism, the components-prop injection finding, the next-mdx-remote/rsc fallback exercise (or waiver), and the dev-turbopack verdict.
</verification>

<success_criteria>
- Build-time KaTeX renders one display equation as a `.katex` node (NOT `.katex-error`) AND a custom component renders via the components prop, under BOTH `pnpm build && pnpm start` (e2e) AND `next dev --turbopack` (recorded manual verdict), live-confirmed by Evidence Collector.
- The locale-split glob (`research/*.{es,en}.mdx`) + derived `locale` field (across es+en arms) + s.toc() are settled and documented.
- No citation libraries anywhere; FOUND-11 (lab) bundle barrier intact; no Python font tooling (byte-subsetting deferred to v2).
- Downstream Plans B and C are unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-research-reading-surface/03.1-01-SUMMARY.md` recording: resolved dep versions, the chosen locale-derivation mechanism, the components-prop injection finding, the rehype-pretty-code keep/drop decision, the next-mdx-remote/rsc fallback exercise result, the spike e2e result, the Evidence Collector verdict, and that the temporary `spike` route (no underscore) is handed to Plan C for removal.
</output>
</plan>

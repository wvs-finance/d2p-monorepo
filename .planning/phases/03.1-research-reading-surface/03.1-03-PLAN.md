---
phase: 03.1-research-reading-surface
plan: 03
type: execute
wave: 3
depends_on: ["03.1-01", "03.1-02"]
files_modified:
  - components/research/ResearchArticle.tsx
  - components/research/ArticleTOC.tsx
  - components/research/Sidenote.tsx
  - components/research/TheoremBlock.tsx
  - components/research/Figure.tsx
  - components/research/mdx-components.tsx
  - components/research/PaperBridge.tsx
  - app/(lab)/research/[slug]/page.tsx
  - app/(lab)/research/layout.tsx
  - lib/research/arxiv.ts
  - content/research/cfmm-microstructure-fixture.es.mdx
  - content/research/cfmm-microstructure-fixture.en.mdx
  - messages/es-CO/research.json
  - messages/en/research.json
  - tests/unit/i18n-coverage.test.ts
  - tests/unit/paper-bridge.test.tsx
  - tests/unit/theorem-block.test.tsx
  - tests/e2e/research-reading.spec.ts
  - tests/a11y/research.spec.ts
  - docs/a11y-audit.md
  - docs/copy-review.md
  - lighthouserc.json
autonomous: false
requirements: [LAB-03]
user_setup: []

must_haves:
  truths:
    - "A visitor at /research/{slug} reads a paper-grade page: ~64ch measure, H2/H3 TOC (sticky lg+, inline narrow, NEVER <details>), KaTeX display equations (.katex, NOT .katex-error), Tufte sidenotes→footnotes (CSS-only, works no-JS), theorem/definition/lemma callouts (ochre left-rule + text label), numbered figures — rendered from the active NEXT_LOCALE's single-locale body"
    - "PaperBridge (the SOLE 'use client' island) shows arXiv + PDF links (derived from arxiv_id) and a copy-able static BibTeX block + DOI link — NO rehype-citation, NO @citation-js"
    - "Mode A (readable_on_site:true) renders the on-site body; Mode B (false + arxiv_id) renders abstract + bridge; neither body nor any bridge → notFound(); the FAIL memo renders at IDENTICAL visual weight to a PASS (no muting/collapse)"
    - "Reading-page i18n is a full es-CO-first superset with docs/copy-review.md native sign-off; a manual MathML screen-reader spot-check is recorded in docs/a11y-audit.md; Lighthouse LCP < 2.5s with KaTeX font subset + font-display"
  artifacts:
    - path: "app/(lab)/research/[slug]/page.tsx"
      provides: "Locale-aware hybrid reading page; generateStaticParams over slug×locale; generateMetadata; Mode A/B/notFound; MDXRenderer + component map"
      contains: "generateStaticParams"
    - path: "components/research/PaperBridge.tsx"
      provides: "Sole 'use client' island — BibTeX copy-to-clipboard (ReplicationHash pattern) + arXiv/PDF/DOI links"
      contains: "use client"
    - path: "components/research/mdx-components.tsx"
      provides: "Single module mapping h2/h3, Figure, TheoremBlock, Sidenote — ALL server components (only PaperBridge is client)"
      contains: "TheoremBlock"
    - path: "docs/a11y-audit.md"
      provides: "Recorded NVDA/VoiceOver MathML spot-check entry (date+reviewer+result) — hard exit criterion"
    - path: "lighthouserc.json"
      provides: "Reading-page LCP < 2.5s assertion; KaTeX font subset + font-display applied"
  key_links:
    - from: "app/(lab)/research/[slug]/page.tsx"
      to: "MDXRenderer + mdx-components map"
      via: "renders entry.body for the active locale via the resurrected RSC renderer"
      pattern: "MDXRenderer"
    - from: "components/research/PaperBridge.tsx"
      to: "navigator.clipboard.writeText(bibtex)"
      via: "copy-to-clipboard island (ReplicationHash pattern)"
      pattern: "clipboard"
    - from: "app/(lab)/research/[slug]/page.tsx"
      to: "arxiv_id → arXiv abs/PDF URLs"
      via: "lib/research/arxiv.ts deriveArxivUrls (no build-time fetch)"
      pattern: "arxiv"
---

<objective>
Build the locale-aware `/research/[slug]` reading page and its paper-grade anatomy on top of Plan A's proven render path and Plan B's migrated content. Deliver: `ResearchArticle` (RSC, renders the compiled MDX body via the resurrected `MDXRenderer` + a single server-component map), `ArticleTOC` (H2/H3, sticky lg+, inline narrow, never `<details>`), CSS-only `Sidenote`/footnotes, `TheoremBlock` (ochre left-rule + text label), numbered `Figure`, and `PaperBridge` (the SOLE `'use client'` island — arXiv/PDF/DOI links + copy-able static BibTeX). Wire Mode A (on-site body) / Mode B (arXiv landing) / `notFound()` fallback, with the FAIL memo at identical visual weight to a PASS. Add a Mode-B arXiv fixture, the reading-page i18n superset (es-CO-first + copy-review sign-off), the manual MathML a11y entry, and the Lighthouse LCP gate.

Purpose: completes the paper-grade public reading surface — the venue DS2P Labs publishes finished research into — accessible, fast on 3G, and locale-correct, without re-introducing the per-iteration econometric exercise UI.
Output: the reading-page route + 6 research components + PaperBridge island + arXiv URL deriver + Mode-B fixture + reading e2e/a11y + i18n superset + LCP gate + recorded a11y/copy sign-offs.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-research-reading-surface/03.1-CONTEXT.md
@.planning/phases/03.1-research-reading-surface/03.1-VALIDATION.md
@docs/superpowers/specs/2026-05-29-research-reading-surface-design.md
@.planning/phases/03.1-research-reading-surface/03.1-01-SUMMARY.md
@.planning/phases/03.1-research-reading-surface/03.1-02-SUMMARY.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md
@./CLAUDE.md

<interfaces>
<!-- MDXRenderer resurrected in Plan A — render the compiled body with a component map -->
From components/MDXRenderer.tsx:
```tsx
export function MDXRenderer({ code }: { code: string }): React.JSX.Element // new Function(code)(runtime).default
```
NOTE: to pass a custom component map, the map is provided to the runtime. With Velite s.mdx() the standard pattern is `new Function(code)({ ...runtime, useMDXComponents: () => components }).default` OR wrap children in an MDXProvider-free map. Confirm the exact injection against the Plan A spike (the renderer may need a `components` prop added). If MDXRenderer needs a `components` param, extend it minimally (still RSC, no 'use client').

<!-- research collection entry shape after Plans A+B -->
Each .velite/research.json entry: { slug, locale: 'es'|'en', title_es, title_en, authors, date, type, track, readable_on_site, external_url?, abstract_es?, abstract_en?, arxiv_id?, pdf_url?, doi?, bibtex?, summary_es, summary_en, tags, order?, body: string }

<!-- ReplicationHash clipboard island pattern (git 5056bb7^) — reuse for PaperBridge BibTeX copy -->
From git 5056bb7^:components/ReplicationHash.tsx (the 'use client' clipboard pattern):
```tsx
'use client'
import { useState } from 'react'
async function handleCopy() {
  try { await navigator.clipboard.writeText(value); setCopied(true); setTimeout(()=>setCopied(false),1500) }
  catch { /* swallow */ }
}
// copied state → transient <output> tooltip; ClipboardCopy icon; aria-labels translated
```

<!-- arXiv URL derivation (no build-time fetch — hermetic builds, spec §4) -->
lib/research/arxiv.ts: deriveArxivUrls(arxiv_id) → { abs: `https://arxiv.org/abs/${id}`, pdf: `https://arxiv.org/pdf/${id}` }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Research component map (all server) + PaperBridge client island + arXiv URL deriver</name>
  <files>lib/research/arxiv.ts, components/research/PaperBridge.tsx, components/research/Figure.tsx, components/research/TheoremBlock.tsx, components/research/Sidenote.tsx, components/research/ArticleTOC.tsx, components/research/mdx-components.tsx, components/research/ResearchArticle.tsx, components/MDXRenderer.tsx, tests/unit/theorem-block.test.tsx, tests/unit/paper-bridge.test.tsx</files>
  <read_first>
    - components/MDXRenderer.tsx (Plan A) — confirm how to inject a component map (see <interfaces>)
    - git 5056bb7^:components/ReplicationHash.tsx (the clipboard 'use client' pattern to reuse)
    - spec §6 "Components" + "MDX component-map note" (ALL map entries server components; ONLY PaperBridge 'use client'; keep the map in ONE module so a stray 'use client' can't pull the whole map client)
    - spec §5 (display equations centered, `\tag` numbers ochre flush-right; `.katex-display { overflow-x:auto }` mobile)
    - spec §3.2 (TheoremBlock = ochre left-rule + text label color+text; Sidenote→footnote CSS-only; numbered Figure)
    - 02-UI-SPEC.md tokens (muted ochre oklch(0.6 0.08 70); accent-reserved list; status color matrix; CROSS-09 color+icon+text)
    - VALIDATION map rows: "Paper-bridge" (unit: BibTeX copy island), "Reading page anatomy" (theorem block label+text)
  </read_first>
  <behavior>
    - TheoremBlock renders a visible text label (e.g. "Theorem 1." / "Teorema 1.") AND an ochre left-rule — never color-alone (unit test asserts the label text is present, not just a class).
    - PaperBridge copy button copies the exact BibTeX string to the clipboard and shows a transient "Copied!"/"¡Copiado!" tooltip; on clipboard failure it swallows the error (no crash).
    - deriveArxivUrls('2401.12345') → abs `https://arxiv.org/abs/2401.12345`, pdf `https://arxiv.org/pdf/2401.12345`.
  </behavior>
  <action>
    Create `lib/research/arxiv.ts` exporting `deriveArxivUrls(id: string)` → `{ abs, pdf }` (string concat only; NO fetch — hermetic builds per spec §4).
    Create `components/research/PaperBridge.tsx` — the SOLE `'use client'` island. Props: `{ arxivAbs?, arxivPdf?, pdfUrl?, doi?, bibtex?, labels: {...} }`. Render: "Read the full paper on arXiv" → arxivAbs; PDF link → arxivPdf/pdfUrl; DOI link → `https://doi.org/${doi}`; if `bibtex`, a `<pre>`+copy button reusing the ReplicationHash clipboard pattern (navigator.clipboard.writeText(bibtex), transient tooltip, swallow errors, translated aria-labels). External links: `target="_blank" rel="noopener noreferrer"` + ArrowUpRight.
    Create the server-component map + blocks in `components/research/`:
      - `Figure.tsx` (RSC): semantic `<figure>`/`<figcaption>`, auto-numbered caption ("Figure N" / "Figura N" via CSS counter or passed index).
      - `TheoremBlock.tsx` (RSC): ochre left-rule (`border-l-2 border-accent-default pl-4`) + a bold text label from a `kind` prop (theorem/definition/lemma/proof) — label is TEXT (CROSS-09), parsed from `remark-directive` `:::theorem` (the directive handler maps to this component).
      - `Sidenote.tsx` (RSC): CSS-only sidenote→footnote (margin note on lg+, inline numbered footnote on narrow); no-JS baseline; ARIA `role`/`aria-describedby` wiring. remark-gfm already gives `[^n]` footnotes — Sidenote presents them.
      - `ArticleTOC.tsx` (RSC): builds a list from H2/H3 (rehype-slug anchors from Plan A); sticky rail `lg+`, inline-expanded on narrow; NEVER a `<details>` element (spec hard rule).
      - `mdx-components.tsx`: a SINGLE module exporting the component map object — `{ h2, h3 (with anchor), Figure, TheoremBlock, Sidenote }` — ALL server components; import PaperBridge ONLY where the page composes it, NOT into this map (keep the map free of 'use client' so it can't pull client).
    Create `ResearchArticle.tsx` (RSC): takes `{ code, components }`, renders `<MDXRenderer code={code} components={components} />` inside the ~64ch measure container (`max-w-[64ch]` prose; figures/equations opt into `.fullwidth` wider bleed). If MDXRenderer needs a `components` param, extend it minimally in Plan A's file (still RSC).
    Write unit tests: `tests/unit/theorem-block.test.tsx` (asserts label text present, not color-alone) and `tests/unit/paper-bridge.test.tsx` (asserts BibTeX copy calls clipboard.writeText with the exact string; mock navigator.clipboard).
  </action>
  <acceptance_criteria>
    - `grep -q "use client" components/research/PaperBridge.tsx` AND `! grep -rq "use client" components/research/mdx-components.tsx components/research/TheoremBlock.tsx components/research/Figure.tsx components/research/Sidenote.tsx components/research/ArticleTOC.tsx`
    - `grep -q "clipboard" components/research/PaperBridge.tsx`
    - `! grep -q "details" components/research/ArticleTOC.tsx` (TOC is never <details>)
    - `grep -q "border-accent-default" components/research/TheoremBlock.tsx` (ochre left-rule) AND TheoremBlock renders a text label
    - `grep -q "arxiv.org/abs" lib/research/arxiv.ts && grep -q "arxiv.org/pdf" lib/research/arxiv.ts`
    - `! grep -rq "rehype-citation\|@citation-js" components/research/` (v2 deferral)
    - `vitest run tests/unit/theorem-block.test.tsx tests/unit/paper-bridge.test.tsx` green
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/unit/theorem-block.test.tsx tests/unit/paper-bridge.test.tsx && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Server component map (h2/h3/Figure/TheoremBlock/Sidenote/ArticleTOC) + sole PaperBridge client island + arXiv URL deriver built; theorem label is text+color, BibTeX copies, no citation libs, unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /research/[slug] reading page — generateStaticParams slug×locale, Mode A/B/notFound, generateMetadata, Mode-B fixture, reading e2e</name>
  <files>app/(lab)/research/[slug]/page.tsx, app/(lab)/research/_spike/page.tsx, content/research/spike-katex.es.mdx, content/research/cfmm-microstructure-fixture.es.mdx, content/research/cfmm-microstructure-fixture.en.mdx, tests/e2e/research-reading.spec.ts</files>
  <read_first>
    - app/(lab)/research/layout.tsx (Plan A — route-scoped katex.min.css; reading page lives under it)
    - app/(lab)/research/_spike/page.tsx (Plan A temporary — RETIRE it here: delete the route + content/research/spike-katex.es.mdx, and the spike e2e if it referenced _spike; the migrated pair-d body now exercises math)
    - @/.velite shim (research entries with body+locale+new fields)
    - spec §3.2 (Mode A on-site body / Mode B arxiv landing / notFound fallback; slug×locale generateStaticParams; render the body matching active NEXT_LOCALE; FAIL memo equal visual weight)
    - spec §6 routes (generateStaticParams over slug×locale; generateMetadata OG + canonical)
    - components/StructuredData.tsx (existing JSON-LD pattern — emit ScholarlyArticle for the reading page; pre-build html string before return per STATE.md Plan 02-05 decision)
    - spec §7 ("one Mode-B fixture with arxiv_id" so §8 Mode-B test passes)
    - VALIDATION map: "Reading page anatomy", "No-JS first paint", "Paper-bridge", "Anti-fishing equal weight"
    - STATE.md decisions: locale default es-CO; e2e set NEXT_LOCALE cookie for en; production webServer; .first() for repeating labels
  </read_first>
  <behavior>
    - `/research/abrigo-y3-carbon-basket-writeup` (Mode A, readable_on_site:true) → 200, renders the on-site body with TOC + any KaTeX (.katex, NOT .katex-error).
    - `/research/{mode-b-fixture}` (readable_on_site:false + arxiv_id) → 200, renders abstract + PaperBridge ("Read the full paper on arXiv"), NOT a full body.
    - `/research/{unknown}` → 404 (notFound); a piece with neither body nor any bridge → 404.
    - Both locales render the correct SINGLE-locale body (es body under es-CO cookie, en body under en cookie).
    - The FAIL memo (`fx-vol-cpi-closed-fail`) reading page renders at identical structural weight to a PASS (no `<details>`, no muting, full body visible).
  </behavior>
  <action>
    RETIRE the Plan A spike: delete `app/(lab)/research/_spike/page.tsx` and `content/research/spike-katex.es.mdx`; if `tests/e2e/research-math-spike.spec.ts` targeted `/research/_spike`, retarget it to a migrated Mode-A slug (or remove it — the reading e2e below now covers the `.katex`/NOT-`.katex-error` assertion). Note the retirement in the SUMMARY.
    Create `app/(lab)/research/[slug]/page.tsx`:
      - `generateStaticParams`: enumerate distinct `slug` values (lean static per CONTEXT discretion). Locale comes from the cookie/NEXT_LOCALE, not the URL (the route is `[slug]` only) — select the entry matching `{slug, locale}` at render; fall back to es if the active-locale body is missing.
      - In the page: `await params`; find the entry for the active locale. Branch:
          Mode A (`readable_on_site`) → render `<ResearchArticle code={entry.body} components={map}/>` + `<ArticleTOC/>` + header (title + `YYYY.MM.DD · Author` Plex Mono + type/track badges + abstract) + `<PaperBridge .../>` (with derived arXiv/PDF/DOI/bibtex if present) + DS2P Labs/∂²Π footer.
          Mode B (`!readable_on_site && arxiv_id`) → render header + abstract (abstract_es/en, may contain math) + key figures + `<PaperBridge arxivAbs={deriveArxivUrls(arxiv_id).abs} .../>` with "Read the full paper on arXiv".
          Fallback (no body AND no arxiv_id/pdf_url/external_url) → `notFound()`.
      - `generateMetadata`: OG (`og:title` title — DS2P Labs; `og:type` article; canonical `/research/{slug}`).
      - Emit `<StructuredData>` ScholarlyArticle (pre-build html const before return).
      - ~64ch measure on prose; equations/figures/tables `.fullwidth`.
      - The FAIL memo MUST render at identical weight to a PASS — no collapse, no muting, full body. (Reviewer gate checks this.)
    Author the Mode-B fixture `content/research/cfmm-microstructure-fixture.{es,en}.mdx`: track `cfmm-microstructure` (also un-empties that track for the index), `readable_on_site: false`, a valid `arxiv_id` (e.g. `2401.12345`), `abstract_es`/`abstract_en` containing one inline `$…$`, and a static `bibtex` block. (es-CO authored first.)
    Create `tests/e2e/research-reading.spec.ts` (production webServer) per VALIDATION map:
      - Mode-A slug → 200; `.katex` count ≥1 AND `.katex-error` count 0; TOC present and NOT a `<details>`; theorem block shows label text; both locales render the correct single-locale body (NEXT_LOCALE cookie).
      - invalid slug → 404.
      - Mode-B fixture → 200, renders abstract + PaperBridge arXiv link; does NOT render a full on-site body.
      - No-JS (`javaScriptEnabled:false`): Mode-A body + footnotes render; no wallet gate.
      - Anti-fishing: `/research/fx-vol-cpi-closed-fail` renders full body, no `<details>`, no muted-opacity wrapper (assert the disposition prose uses text-text-primary, not text-text-muted, and there is no `<details>`).
  </action>
  <acceptance_criteria>
    - `grep -q "generateStaticParams" 'app/(lab)/research/[slug]/page.tsx'`
    - `grep -q "notFound" 'app/(lab)/research/[slug]/page.tsx'`
    - `grep -q "readable_on_site" 'app/(lab)/research/[slug]/page.tsx'` (Mode branch)
    - `! test -f 'app/(lab)/research/_spike/page.tsx' && ! test -f content/research/spike-katex.es.mdx` (spike retired)
    - `grep -q "arxiv_id: " content/research/cfmm-microstructure-fixture.es.mdx`
    - `test -f tests/e2e/research-reading.spec.ts && grep -q "katex-error" tests/e2e/research-reading.spec.ts && grep -q "details" tests/e2e/research-reading.spec.ts` (TOC-not-details + FAIL-not-details guards)
    - `pnpm exec velite build` green (Mode-B fixture validates)
    - `pnpm exec playwright test tests/e2e/research-reading.spec.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec velite build && pnpm exec playwright test tests/e2e/research-reading.spec.ts</automated>
  </verify>
  <done>Reading page renders Mode A/B with notFound fallback, locale-correct single-locale bodies, KaTeX (no error spans), TOC-not-details, FAIL-at-equal-weight; Mode-B fixture + reading e2e green; spike retired.</done>
</task>

<task type="auto">
  <name>Task 3: Reading-page i18n superset (es-CO-first) + manual MathML a11y entry + Lighthouse LCP gate + Evidence-Collector-ready perf</name>
  <files>messages/es-CO/research.json, messages/en/research.json, tests/unit/i18n-coverage.test.ts, tests/a11y/research.spec.ts, docs/a11y-audit.md, docs/copy-review.md, app/(lab)/research/layout.tsx, lighthouserc.json</files>
  <read_first>
    - messages/{es-CO,en}/research.json (post-Plan-B chrome keys) — this is a ~3–5× SUPERSET rewrite, not an extend (spec §8 reviewer M2)
    - tests/unit/i18n-coverage.test.ts (recursive research parity — gates the superset symmetry)
    - spec §5 a11y ("manual NVDA/VoiceOver MathML spot-check is a HARD exit criterion of Plan C, recorded with date+reviewer+result in docs/a11y-audit.md") + §8 perf ("Lighthouse LCP < 2.5s; quantify the KaTeX font subset + font-display; documented unicode-range fallback if the gate fails")
    - spec §0 fonttools/pyftsubset (build-CLI subset IBM Plex + KaTeX WOFF2)
    - docs/a11y-audit.md, docs/copy-review.md (currently unfilled gate docs — record entries)
    - lighthouserc.json / CI lighthouse config (FOUND-08 Moto G Power 3G; LCP < 2.5s)
    - tests/a11y/ (existing axe specs pattern — homepage.spec.ts, apps-dropdown.spec.ts)
    - ./CLAUDE.md (es-CO authored first; native-reviewer sign-off in docs/copy-review.md)
  </read_first>
  <action>
    Author the reading-page i18n SUPERSET in BOTH `messages/es-CO/research.json` (FIRST) and `messages/en/research.json` (second, native-authored — no machine translation). Add reading-page keys under the `research` namespace, symmetric across locales:
      - `reading.toc_heading`, `reading.abstract_heading`, `reading.read_on_arxiv`, `reading.read_pdf`, `reading.doi_label`, `reading.bibtex_copy`, `reading.bibtex_copied`, `reading.affiliation` (DS2P Labs / ∂²Π), `reading.figure_prefix`, `reading.theorem_label.{theorem,definition,lemma,proof}`, `reading.footnote_heading`, plus any label the components consume.
    Record the es-CO reading-page copy entry in `docs/copy-review.md` for native-reviewer sign-off — this sign-off is an EXIT CRITERION of this plan (not optional).
    Confirm `vitest run tests/unit/i18n-coverage.test.ts` is green (recursive research parity gates the superset symmetry).
    Add `tests/a11y/research.spec.ts`: axe scan of `/research` (index) AND a `/research/{slug}` reading page (both locales). axe CANNOT certify MathML — so ALSO perform a manual NVDA or VoiceOver spot-check of ONE rendered equation and RECORD it in `docs/a11y-audit.md` with date + reviewer + result (the hard exit criterion). The audit-doc entry is required even though it's manual.
    Perf: apply the KaTeX font subset + `font-display`:
      - Add a build-CLI subset step (fonttools/pyftsubset) for IBM Plex + the KaTeX WOFF2 faces actually referenced by rendered glyphs; set `font-display` (block/short-swap) and preload the 2–3 most-used faces in `app/(lab)/research/layout.tsx`.
      - QUANTIFY the subset (record before/after font byte sizes in docs/03.1-RENDER-PATH.md or the SUMMARY).
      - Add/confirm a Lighthouse assertion in `lighthouserc.json` for the reading page: LCP < 2.5s on the Moto G Power 3G profile (FOUND-08). Document the unicode-range subsetting fallback if the gate fails.
    Run the Lighthouse gate against the production build for a reading page.
  </action>
  <acceptance_criteria>
    - `grep -q "reading" messages/es-CO/research.json && grep -q "reading" messages/en/research.json` (superset present both locales)
    - `vitest run tests/unit/i18n-coverage.test.ts` green (research superset symmetric)
    - `test -f tests/a11y/research.spec.ts`
    - `grep -qi "mathml" docs/a11y-audit.md` (manual SR entry recorded — hard exit criterion)
    - `grep -q "research" docs/copy-review.md` (es-CO reading-page copy logged for native sign-off) — and the reading-page section present, not only the Plan-B chrome line
    - `grep -qi "font-display\|preload" 'app/(lab)/research/layout.tsx'` (font perf applied)
    - `grep -qi "lcp\|2.5\|2500" lighthouserc.json` (LCP gate present for the reading page)
    - `pnpm exec playwright test tests/a11y/research.spec.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/unit/i18n-coverage.test.ts && pnpm exec playwright test tests/a11y/research.spec.ts</automated>
  </verify>
  <done>Reading-page i18n superset symmetric (es-CO-first, copy-review sign-off logged), manual MathML SR entry recorded in a11y-audit.md, KaTeX font subset + font-display applied, Lighthouse LCP < 2.5s gate present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 4: Evidence Collector live-verify — /research/[slug] reading page + a11y/perf gates</name>
<action>Invoke the Evidence Collector agent (live-verification mode) per ./CLAUDE.md against /research/{slug}. See <how-to-verify> for the exact Mode-A / KaTeX / theorem / PaperBridge / mobile-scroll / anti-fishing-equal-weight / locale / Mode-B steps and verdicts.</action>
  <what-built>
    The /research/[slug] reading page: ~64ch measure, H2/H3 TOC (sticky lg+, inline narrow, never <details>), KaTeX display equations, Tufte sidenotes→footnotes (no-JS baseline), theorem/definition callouts (ochre rule + label), numbered figures, PaperBridge (sole client island: arXiv/PDF/DOI + BibTeX copy), Mode A/B/notFound, reading-page i18n superset, font-subset perf, FAIL memo at equal weight.
  </what-built>
  <how-to-verify>
    Invoke the Evidence Collector agent per ./CLAUDE.md (live-verification mode) against `/research/{slug}`:
    1. Target deployed reading pages (fall back to local `pnpm build && pnpm start -p 3040` — production build is ground truth).
    2. Mode A: `browser_navigate` to `/research/abrigo-y3-carbon-basket-writeup`; `browser_snapshot` — assert TOC visible (NOT a <details> collapse), prose at ~64ch, body rendered.
    3. `browser_evaluate` — assert `document.querySelectorAll('.katex').length >= 1` AND `document.querySelectorAll('.katex-error').length === 0` (math typeset, no silent macro failure).
    4. Assert a theorem/definition callout (if present) shows an ochre left-rule AND a text label (read the label node — color+text, not color-alone).
    5. PaperBridge: click the BibTeX copy button; assert clipboard contains the BibTeX and the "Copied!"/"¡Copiado!" tooltip appears; assert arXiv/PDF/DOI links present where applicable.
    6. Mobile: set 360px viewport; assert `.katex-display` horizontally scrolls (no clipped equation).
    7. Anti-fishing: `browser_navigate` to `/research/fx-vol-cpi-closed-fail`; `browser_evaluate` bounding-box / computed-style comparison vs a PASS reading page — assert IDENTICAL visual weight (no <details>, no reduced opacity, full body visible).
    8. Locale toggle: assert the es body renders under es-CO and the en body under en (single-locale per page).
    9. Mode B: `browser_navigate` to the cfmm-microstructure fixture; assert abstract + "Read the full paper on arXiv" bridge, NOT a full on-site body.
    10. `browser_console_messages`; `browser_take_screenshot` → `/tmp/d2p-verify/03.1-03-{mode-a,fail-equal-weight,mode-b}.png`.
    11. Append verdicts (✓/⚠/✗/⊘) + screenshot paths to `.planning/phases/03.1-research-reading-surface/03.1-LIVE-VERIFICATION.md`.
    Expected: math typeset (no error spans), TOC not collapsed, BibTeX copies, mobile equation scrolls, FAIL == PASS weight, locale-correct body, Mode-B bridge. ALSO confirm the manual MathML SR entry + es-CO native sign-off are recorded before approving.
  </how-to-verify>
  <resume-signal>Type "approved" once the Evidence Collector records ✓ for the reading page AND the docs/a11y-audit.md MathML entry + docs/copy-review.md es-CO sign-off are present (or describe the ✗ for a fix).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm exec velite build` green (Mode-B fixture validates).
- `pnpm exec playwright test tests/e2e/research-reading.spec.ts tests/a11y/research.spec.ts` green.
- `pnpm exec vitest run tests/unit/i18n-coverage.test.ts tests/unit/theorem-block.test.tsx tests/unit/paper-bridge.test.tsx` green.
- `! grep -rq "rehype-citation\|@citation-js" .` (v2 deferral); `! grep -rq "NuqsAdapter" app/(lab)/`; only PaperBridge carries 'use client' under components/research/.
- Lighthouse LCP < 2.5s on a reading page (Moto G 3G).
- docs/a11y-audit.md has the MathML SR entry; docs/copy-review.md has the es-CO reading-page sign-off; Evidence Collector ✓ in 03.1-LIVE-VERIFICATION.md.
</verification>

<success_criteria>
- A visitor reads a paper-grade `/research/{slug}`: locale-correct body, build-time KaTeX (no error spans), H2/H3 TOC (never <details>), CSS-only sidenotes/footnotes (no-JS baseline), ochre theorem callouts with text labels, numbered figures, and a PaperBridge with arXiv/PDF/DOI + copy-able BibTeX — the SOLE client island.
- Mode A / Mode B / notFound all behave; the FAIL memo renders at identical visual weight to a PASS.
- Reading-page i18n is a symmetric es-CO-first superset with native sign-off; the manual MathML a11y entry is recorded; Lighthouse LCP < 2.5s with the KaTeX font subset + font-display.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-research-reading-surface/03.1-03-SUMMARY.md` recording: the component map + island split, the arXiv URL deriver, Mode A/B branch behavior, the Mode-B fixture, the font-subset byte savings, the MathML SR + es-CO sign-off entries, the Lighthouse LCP result, and the Evidence Collector verdict.
</output>

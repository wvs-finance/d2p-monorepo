---
phase: 03.1-research-reading-surface
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - velite.config.ts
  - tests/unit/velite-research-schema.test.ts
  - content/research/pair-d-dispatch-brief.es.mdx
  - content/research/pair-d-dispatch-brief.en.mdx
  - content/research/fx-vol-cpi-closed-fail.es.mdx
  - content/research/fx-vol-cpi-closed-fail.en.mdx
  - content/research/abrigo-y3-carbon-basket-writeup.es.mdx
  - content/research/abrigo-y3-carbon-basket-writeup.en.mdx
  - components/PublicationCard.tsx
  - components/TrackFilter.tsx
  - app/(lab)/research/page.tsx
  - messages/es-CO/research.json
  - messages/en/research.json
  - tests/unit/i18n-coverage.test.ts
  - tests/e2e/research-index.spec.ts
autonomous: false
requirements: [LAB-03]
user_setup: []

must_haves:
  truths:
    - "A visitor at /research sees a reverse-chron list with a server-rendered segmented track filter (All · CFMM Microstructure · Abrigo Hedge-Design · Notes) built from <Link href=\"?track=…\"> — NO nuqs, NO client island, NO NuqsAdapter in (lab)"
    - "Pasting /research?track=cfmm-microstructure returns the filtered server-rendered state (shareable + agent-readable); the empty CFMM track shows an honest 'no publications in this track yet' panel, never a fabricated card"
    - "All 3 existing research pieces are split into per-locale {slug}.es.mdx + {slug}.en.mdx files, each carrying the required `track` enum; velite build is green (schema change + migration landed atomically)"
    - "abrigo-y3-carbon-basket-writeup has readable_on_site:true (closes the dead-card); each PublicationCard links IN to /research/{slug} when readable_on_site, else OUT to external_url, with a track tag"
  artifacts:
    - path: "velite.config.ts"
      provides: "researchSchema gains track (required enum), readable_on_site (default false), abstract_es/en?, arxiv_id? (regex), pdf_url?/doi?/bibtex? (strings)"
      contains: "cfmm-microstructure"
    - path: "components/TrackFilter.tsx"
      provides: "Server-rendered segmented control of <Link href=/research?track=…> — RSC, no 'use client'"
      contains: "Link"
    - path: "app/(lab)/research/page.tsx"
      provides: "searchParams.track server filter + TrackFilter + extended PublicationCard list + honest empty-per-track state"
      contains: "searchParams"
    - path: "content/research/abrigo-y3-carbon-basket-writeup.es.mdx"
      provides: "Migrated es body with track + readable_on_site:true"
      contains: "readable_on_site"
  key_links:
    - from: "app/(lab)/research/page.tsx"
      to: "searchParams.track"
      via: "server-side filter on research[].track; no client JS"
      pattern: "searchParams"
    - from: "components/TrackFilter.tsx"
      to: "/research?track="
      via: "next/link <Link> segmented control"
      pattern: "href=.*track="
    - from: "components/PublicationCard.tsx"
      to: "/research/{slug} OR external_url"
      via: "readable_on_site ? internal Link : external anchor"
      pattern: "readable_on_site"
---

<objective>
Turn `/research` into a reverse-chron index with a server-rendered track filter and migrate the 3 existing bilingual research files into per-locale files carrying the new schema fields. Add the `track` (required enum), `readable_on_site`, abstract/arxiv/pdf/doi/bibtex fields to `researchSchema`; because `track` is required with no default, the schema change and the 3-file migration MUST land in ONE atomic commit or `velite build` breaks. Extend `PublicationCard` (track tag, in/out link), add `TrackFilter` (server `<Link>` segmented control — NO nuqs, NO NuqsAdapter in `(lab)`), wire `searchParams.track` server filtering, add index i18n keys, extend the recursive i18n-coverage parity test, and render an honest empty-per-track state.

Purpose: closes the dead-card gap (y3 write-up becomes readable on-site), establishes the two-track + Notes IA, and gives every piece a shareable/agent-readable filtered URL without breaking the `(lab)` RSC bundle barrier (FOUND-11).
Output: extended schema + atomic migration (6 per-locale files), `TrackFilter`, extended `PublicationCard`, server-filtered index page, index i18n keys, passing schema/i18n unit tests + research-index e2e.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-research-reading-surface/03.1-CONTEXT.md
@.planning/phases/03.1-research-reading-surface/03.1-VALIDATION.md
@docs/superpowers/specs/2026-05-29-research-reading-surface-design.md
@.planning/phases/03.1-research-reading-surface/03.1-01-SUMMARY.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md
@./CLAUDE.md

<interfaces>
<!-- researchSchema after Plan A (body/locale on collection; frontmatter schema still the Phase-2 shape). Plan B ADDS these fields per spec §4: -->
Spec §4 additions to researchSchema (verbatim field definitions):
```ts
track: s.enum(['cfmm-microstructure', 'abrigo-hedge-design', 'notes']),  // REQUIRED — breaks build until all files migrated → atomic
readable_on_site: s.boolean().default(false),
abstract_es: s.string().optional(),
abstract_en: s.string().optional(),
arxiv_id: s.string().regex(/^\d{4}\.\d{4,5}(v\d+)?$/).optional(),  // post-2007 ids; abs/PDF URLs DERIVED at render time
pdf_url: s.string().url().optional(),
doi: s.string().optional(),
bibtex: s.string().optional(),
```

<!-- Current PublicationCard interface (extend, do not rewrite) -->
From components/PublicationCard.tsx:
```ts
export interface PublicationCardResearch {
  slug; title_es; title_en; authors; date: Date;
  type: 'paper'|'decision-memo'|'write-up'|'talk';
  external_url?; summary_es; summary_en; tags; order?
}
// ADD: track: 'cfmm-microstructure'|'abrigo-hedge-design'|'notes'; readable_on_site: boolean
// Link logic: readable_on_site ? <Link href={`/research/${slug}`}> (in) : external_url ? external anchor (out) : no link
```

<!-- i18n-coverage.test.ts already runs assertKeyParity('research', es-CO/research.json, en/research.json) — collectKeyPaths is recursive. Just add the new keys to BOTH locale files; the parity test will gate symmetry. -->

<!-- The 3 existing bilingual files share the `## Español` / `## English` H2 boundary. Split mechanically on that boundary. -->
Existing files (single bilingual bodies, Abrigo-track, Unicode glyphs):
- content/research/pair-d-dispatch-brief.mdx       (decision-memo, order 1, has external_url)
- content/research/fx-vol-cpi-closed-fail.mdx      (FAIL memo — equal-weight rule)
- content/research/abrigo-y3-carbon-basket-writeup.mdx (write-up, NO external_url → dead card today)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ATOMIC — extend researchSchema (track required + new fields) AND migrate all 3 files into per-locale split with track; close the dead card</name>
  <files>velite.config.ts, tests/unit/velite-research-schema.test.ts, content/research/pair-d-dispatch-brief.es.mdx, content/research/pair-d-dispatch-brief.en.mdx, content/research/fx-vol-cpi-closed-fail.es.mdx, content/research/fx-vol-cpi-closed-fail.en.mdx, content/research/abrigo-y3-carbon-basket-writeup.es.mdx, content/research/abrigo-y3-carbon-basket-writeup.en.mdx, lib/velite-shim.ts</files>
  <read_first>
    - velite.config.ts (post-Plan-A: collection has body+locale; exported researchSchema is the Phase-2 shape)
    - tests/unit/velite-research-schema.test.ts (existing — safeParse on researchSchema; ADD cases here, do NOT create research-schema.test.ts; the VALIDATION map's `research-schema.test.ts` maps to this existing file)
    - spec §4 (field defs — see <interfaces>) + the "atomic with migration" note (track required → breaks build until 3 files carry it)
    - spec §7 migration steps (split on `## Español`/`## English`; all 3 → track `abrigo-hedge-design`; y3 write-up → readable_on_site:true; two memos readable_on_site decided per file — bodies are substantive → true, FAIL memo at equal weight)
    - content/research/*.mdx (the 3 bilingual sources)
    - spec §7 "author at least one body with real $…$/\tag math" — when splitting pair-d, convert `Π(σ_T) ≈ K̂·σ_T` to LaTeX `$\Pi(\sigma_T) \approx \hat{K}\cdot\sigma_T$` and `β = +0.137` to `$\hat{\beta}=+0.137$` so a migrated body exercises the pipeline (the spike fixture can then be retired by Plan C)
  </read_first>
  <behavior>
    - researchSchema.safeParse rejects an entry missing `track`; accepts the 3 enum values; rejects any other string.
    - arxiv_id regex matches `2401.12345` and `2401.12345v2`; does NOT match a pre-2007 id like `math/0211159` (documented non-match case).
    - readable_on_site defaults to false when omitted.
    - `pnpm exec velite build` is GREEN with all 6 per-locale files present (the build is the integration gate — track required would fail it otherwise).
  </behavior>
  <action>
    THIS IS ONE ATOMIC TASK/COMMIT. Do the schema change and the file migration together; never commit the schema change without the migrated files.
    1. In `velite.config.ts`, add the spec §4 fields to BOTH the collection schema AND the exported `researchSchema` const (the export is what unit tests parse). track is REQUIRED with NO default. (Optionally use a temporary `.default('notes')` ONLY during the edit, removed before commit — but since you migrate the files in the same task, prefer no default.) Keep `arxiv_id`/`pdf_url`/`doi`/`bibtex` as STRINGS — NEVER `s.coerce.date()`; add NO new Date logic to lib/velite-shim.ts (spec §4 + hard constraint 8).
    2. Migrate each of the 3 files. For each `{slug}.mdx`:
       - Split the body on the `## Español` / `## English` H2 boundary into `{slug}.es.mdx` (Spanish body) and `{slug}.en.mdx` (English body). Each new file carries the FULL frontmatter (es file and en file share frontmatter — title_es/title_en/summary_es/summary_en stay on both; the locale is derived from the filename per Plan A).
       - Add `track: abrigo-hedge-design` to all 6 files.
       - Set `readable_on_site: true` on `abrigo-y3-carbon-basket-writeup.{es,en}.mdx` (it has no external artifact → closes the dead-card gap). Set `readable_on_site: true` on both memos too (bodies are substantive; FAIL memo at EQUAL weight per spec §3.2 anti-fishing) — keep their existing `external_url` as the secondary bridge.
       - DELETE the 3 original single-file `{slug}.mdx` (the glob is now `*.{es,en}.mdx`).
       - In `pair-d-dispatch-brief.{es,en}.mdx`, convert the Unicode-glyph math to real LaTeX (`$\Pi(\sigma_T)\approx\hat{K}\cdot\sigma_T$`, `$\hat{\beta}=+0.137$`) so a MIGRATED body exercises KaTeX (spec §7); leave prose results as running text (anti-fishing: prose mention allowed, exercise UI forbidden).
       - Add `abstract_es`/`abstract_en` where the spec's reading-page benefits (at least on the y3 write-up).
    3. Extend `tests/unit/velite-research-schema.test.ts` with cases from <behavior>: track-required rejection, the 3 enum accepts + 1 reject, arxiv_id post-2007 match + pre-2007 non-match (documented), readable_on_site default false.
    4. Run `pnpm exec velite build` — MUST be green (the real atomicity gate).
  </action>
  <acceptance_criteria>
    - `grep -q "cfmm-microstructure" velite.config.ts` (track enum added to schema)
    - `grep -q "readable_on_site" velite.config.ts`
    - `! grep -q "s.coerce.date" velite.config.ts` on any of arxiv_id/pdf_url/doi/bibtex lines (strings only)
    - `ls content/research/*.es.mdx content/research/*.en.mdx | wc -l` ≥ 6 AND `! ls content/research/pair-d-dispatch-brief.mdx 2>/dev/null` (originals removed)
    - `grep -q "track: abrigo-hedge-design" content/research/fx-vol-cpi-closed-fail.es.mdx`
    - `grep -q "readable_on_site: true" content/research/abrigo-y3-carbon-basket-writeup.es.mdx`
    - `grep -q '\\\\hat{\\\\beta}' content/research/pair-d-dispatch-brief.es.mdx || grep -q 'sigma_T' content/research/pair-d-dispatch-brief.es.mdx` (migrated body carries LaTeX)
    - `pnpm exec velite build` exits 0
    - `vitest run tests/unit/velite-research-schema.test.ts` green
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec velite build && pnpm exec vitest run tests/unit/velite-research-schema.test.ts</automated>
  </verify>
  <done>Schema + 6 per-locale migrated files land together; velite build green; dead card closed (y3 readable_on_site:true); one migrated body carries real LaTeX; schema unit cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Server-rendered TrackFilter + searchParams filtering + extended PublicationCard (in/out link + track tag)</name>
  <files>components/TrackFilter.tsx, components/PublicationCard.tsx, app/(lab)/research/page.tsx</files>
  <read_first>
    - app/(lab)/research/page.tsx (current index — sorts by order, renders PublicationCard list)
    - app/(lab)/layout.tsx (RSC-pure, NO NuqsAdapter — FOUND-11 bundle barrier; explicit non-goal: do NOT add NuqsAdapter to (lab))
    - components/PublicationCard.tsx (extend interface + link logic — see <interfaces>)
    - spec §3.1 (server <Link> segmented control: All · CFMM Microstructure · Abrigo Hedge-Design · Notes; reads searchParams.track; default All; each option encoded as TEXT not color; empty-per-track honest panel)
    - 02-UI-SPEC.md PublicationCard composition + accent-reserved list (no colored side-borders; no nested cards; subtle hover only) + status color matrix
    - existing nuqs usage is ONLY in (apps) — confirm you import nothing from nuqs here
  </read_first>
  <action>
    Create `components/TrackFilter.tsx` — a PURE RSC (no 'use client'): a segmented control rendering 4 `next/link` `<Link>`s: `All` → `/research`, and one each → `/research?track=cfmm-microstructure`, `?track=abrigo-hedge-design`, `?track=notes`. The active option (matching the current `track` prop) gets a non-color affordance (e.g. ochre bottom-border + aria-current="page") — encode active state as text/border + aria-current, never color-alone (CROSS-09). Labels come from i18n (Task 3). Touch target min-h-[44px]. NO nuqs, NO client island.
    Extend `components/PublicationCard.tsx`:
      - Add `track` + `readable_on_site` to `PublicationCardResearch` (types: track union, readable_on_site boolean).
      - Link logic: if `readable_on_site` → wrap the title in `<Link href={`/research/${slug}`}>` (links IN); else if `external_url` → keep the existing external anchor (links OUT, ArrowUpRight); else no link.
      - Render a track tag (Plex-Mono micro-caps `Badge variant="outline"`), distinct from the type badge. Track label from i18n.
      - Keep it an RSC (no 'use client'); keep the no-nested-card / subtle-hover constraints.
    Rewrite `app/(lab)/research/page.tsx`:
      - Accept `searchParams` (Next 16 async searchParams — `await searchParams`); read `track` (string | undefined).
      - Dedupe per-locale entries to one card per slug for the active locale (entries are now per-locale; show the card for the current `locale`, falling back to es if en missing).
      - Filter `research` by `track` server-side when `track` is set and valid; show All when unset/invalid. Sort by `order ?? 999` then date desc.
      - Render `<TrackFilter track={track} … />` above the list.
      - Empty-per-track: when the filtered list is empty (e.g. cfmm-microstructure), render an honest "No publications in this track yet" panel (i18n) — NEVER a fabricated card.
      - Do NOT exclude the spike fixture by hand here; if Plan A left `spike-katex`, Plan C removes it — but if it pollutes the index, filter it by slug `spike-katex` with a `// temporary: removed when Plan C retires the spike fixture` comment.
  </action>
  <acceptance_criteria>
    - `test -f components/TrackFilter.tsx && ! grep -q "use client" components/TrackFilter.tsx`
    - `grep -q "track=cfmm-microstructure" components/TrackFilter.tsx && grep -q "track=abrigo-hedge-design" components/TrackFilter.tsx && grep -q "track=notes" components/TrackFilter.tsx`
    - `grep -q "searchParams" 'app/(lab)/research/page.tsx'`
    - `grep -q "readable_on_site" components/PublicationCard.tsx`
    - `! grep -rq "nuqs" 'app/(lab)/research/' components/TrackFilter.tsx && ! grep -rq "NuqsAdapter" 'app/(lab)/'`
    - `pnpm test:quick` green (biome + tsc + vitest)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit && ! grep -rq "nuqs" components/TrackFilter.tsx 'app/(lab)/research/'</automated>
  </verify>
  <done>Server <Link> track filter + searchParams server filtering live; PublicationCard links in/out by readable_on_site with a track tag; honest empty-per-track panel; no nuqs/NuqsAdapter in (lab).</done>
</task>

<task type="auto">
  <name>Task 3: Index i18n keys (es-CO-first) + extend recursive i18n-coverage parity + research-index e2e</name>
  <files>messages/es-CO/research.json, messages/en/research.json, tests/unit/i18n-coverage.test.ts, tests/e2e/research-index.spec.ts, docs/copy-review.md</files>
  <read_first>
    - messages/es-CO/research.json + messages/en/research.json (current keys: h1, subheading, empty_state, cta.read_document, type_label.*)
    - tests/unit/i18n-coverage.test.ts (already runs assertKeyParity('research', …) recursively via collectKeyPaths — just keep BOTH locale files symmetric; the test gates it)
    - spec §3.1 (track labels, empty-per-track copy) + 02-UI-SPEC.md Publications copy table + Copywriting Contract (author's voice, no marketing slop)
    - tests/e2e/research-page.spec.ts (existing LAB-03 baseline — keep passing); VALIDATION map row "Index + track filter (§3.1)" → tests/e2e/research-index.spec.ts
    - ./CLAUDE.md (es-CO authored first; docs/copy-review.md sign-off; native reviewer)
  </read_first>
  <action>
    Add to BOTH `messages/es-CO/research.json` and `messages/en/research.json` (es-CO authored FIRST, then en — no machine translation), under the `research` namespace, symmetric keys for:
      - `track_filter.all`, `track_filter.cfmm-microstructure`, `track_filter.abrigo-hedge-design`, `track_filter.notes`
      - `track_label.cfmm-microstructure`, `track_label.abrigo-hedge-design`, `track_label.notes` (card tag labels)
      - `empty_track.heading`, `empty_track.body` (honest per-track empty state)
      - `cta.read_on_site` (the in-link label, distinct from existing `cta.read_document` out-link)
    Use the UI-SPEC register (es-CO: "Investigación", track names in author's voice). Record the es-CO copy entry in `docs/copy-review.md` for native-reviewer sign-off (index chrome only — the reading-page superset is Plan C).
    The recursive parity test already covers `research`; confirm `vitest run tests/unit/i18n-coverage.test.ts` passes with the new symmetric keys (if it was previously a stub for new keys, it now exercises them).
    Create `tests/e2e/research-index.spec.ts` (production webServer), per VALIDATION map:
      - `/research` returns 200
      - the server `<Link>` filter sets `?track=` — click "Abrigo Hedge-Design", assert URL contains `?track=abrigo-hedge-design` and only abrigo-track cards render
      - pasted `/research?track=cfmm-microstructure` returns the filtered (empty) state with the honest empty panel
      - assert NO NuqsAdapter / nuqs in the (lab) page bundle (grep the route source as a guard OR assert the filter works with javaScriptEnabled:false — server <Link> must navigate without JS)
      - both locales render the correct chrome (set NEXT_LOCALE cookie)
  </action>
  <acceptance_criteria>
    - `grep -q "track_filter" messages/es-CO/research.json && grep -q "track_filter" messages/en/research.json`
    - `grep -q "empty_track" messages/es-CO/research.json`
    - `vitest run tests/unit/i18n-coverage.test.ts` green (research namespace symmetric)
    - `test -f tests/e2e/research-index.spec.ts && grep -q "track=" tests/e2e/research-index.spec.ts`
    - `grep -q "research" docs/copy-review.md` (es-CO index copy logged for sign-off)
    - `pnpm exec playwright test tests/e2e/research-index.spec.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/unit/i18n-coverage.test.ts && pnpm exec playwright test tests/e2e/research-index.spec.ts</automated>
  </verify>
  <done>Index i18n keys symmetric across locales (es-CO-first, logged in copy-review.md), recursive parity test green, research-index e2e proves server-<Link> filtering + empty-per-track + no-JS navigation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 4: Evidence Collector live-verify — /research index + track filter</name>
<action>Invoke the Evidence Collector agent (live-verification mode) per ./CLAUDE.md against /research. See <how-to-verify> for the exact browser_navigate / track-filter-click / empty-track / locale-toggle steps and verdicts.</action>
  <what-built>
    The /research index: server-rendered track filter (All · CFMM Microstructure · Abrigo Hedge-Design · Notes) via <Link>, searchParams server filtering, extended PublicationCard (track tag + in/out link), migrated per-locale content with the new schema, honest empty-per-track state. No nuqs, no NuqsAdapter in (lab).
  </what-built>
  <how-to-verify>
    Invoke the Evidence Collector agent per ./CLAUDE.md (live-verification mode) against `/research`:
    1. Target deployed `/research` (fall back to local `pnpm build && pnpm start -p 3040`).
    2. `browser_navigate` to `/research`; `browser_snapshot` — assert the reverse-chron list + the 4-option segmented track filter render; each PublicationCard shows a track tag.
    3. Click "Abrigo Hedge-Design"; assert URL is `/research?track=abrigo-hedge-design` and only abrigo-track cards remain (shareable/agent-readable URL).
    4. Navigate to `/research?track=cfmm-microstructure`; assert the honest "no publications in this track yet" panel (NOT a fabricated card).
    5. Assert the y3 write-up card links IN to `/research/abrigo-y3-carbon-basket-writeup` (dead-card closed), and the memos show the in-link.
    6. Toggle locale; assert track-filter labels + empty-state copy switch.
    7. `browser_console_messages`; `browser_take_screenshot` → `/tmp/d2p-verify/03.1-02-index.png` (+ one for the empty track).
    8. Append verdicts (✓/⚠/✗/⊘) + screenshot paths to `.planning/phases/03.1-research-reading-surface/03.1-LIVE-VERIFICATION.md`.
    Expected: filter navigates server-side (URL updates, no client island), empty track honest, in-links present, locale toggle switches chrome, no console errors.
  </how-to-verify>
  <resume-signal>Type "approved" once the Evidence Collector records ✓ for the index + filter (or describe the ✗ for a fix before Plan C).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm exec velite build` green with 6 per-locale files (atomic schema+migration landed).
- `vitest run tests/unit/velite-research-schema.test.ts tests/unit/i18n-coverage.test.ts` green.
- `pnpm exec playwright test tests/e2e/research-index.spec.ts tests/e2e/research-page.spec.ts` green.
- `! grep -rq "nuqs\|NuqsAdapter" app/(lab)/` (FOUND-11 intact); `! grep -rq "rehype-citation\|@citation-js" .` (v2 deferral).
- Evidence Collector ✓ recorded in 03.1-LIVE-VERIFICATION.md.
- docs/copy-review.md has the es-CO index-chrome entry for native sign-off.
</verification>

<success_criteria>
- /research is a reverse-chron list with a shareable, agent-readable, server-rendered track filter and honest empty-per-track states — built entirely with server `<Link>`, no client filter island, no NuqsAdapter in (lab).
- All 3 pieces are split per-locale, carry the required `track`, and the y3 write-up is readable on-site (dead card closed) — all in one atomic schema+migration commit.
- Index i18n keys are symmetric (es-CO-first) and the recursive parity test gates them.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-research-reading-surface/03.1-02-SUMMARY.md` recording: the final schema field set, the 6 migrated filenames + their track/readable_on_site values, which body now carries LaTeX, the TrackFilter approach, and the Evidence Collector verdict.
</output>

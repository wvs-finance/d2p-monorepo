---
phase: 03-subgraph-indexing
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - indexing/completeness.py
  - indexing/ordering.py
  - indexing/parity.py
  - indexing/liveness.py
  - tests/test_index01_completeness.py
  - tests/test_index01_ordering.py
  - tests/test_index01_parity.py
  - tests/test_index01_liveness.py
autonomous: true
requirements: [INDEX-01]
must_haves:
  truths:
    - "leg-b structural-floor logic HALTS only when the RequestCreated(0xb623) COUNT is implausibly LOW vs the RequestFinalized(0x65db) COUNT — |count(RequestCreated) − count(RequestFinalized)| / max(...) exceeds a stated structural tolerance (proves MISSING RequestCreated events) — NOT a proportion-CI gate; this is the operative leg-b gate"
    - "the descriptive count-ratio RequestCreated(0xb623) COUNT ÷ the indexer's OWN distinct-tx count is LOGGED-AND-EMITTED per stratum (deploy/mid/head) as an INDEX-01 OUTPUT, never a pass/halt gate; an in-range count-ratio that exceeds 1.0 (0xb623 fires 1–5×/tx → numerator is a COUNT, not a proportion) does NOT halt"
    - "the 0.716 figure is kept ONLY as a documented descriptive reference with an explicit 'NOT a gate — see structural floor' note; the Wilson band is RETIRED as the operative gate (count ratio is dimensionally not a Bernoulli proportion)"
    - "tx-anchor monotonicity floor FAILS a fresh transactions_count < 234999 and PASSES ≥ 234999"
    - "cursor-contiguity check over the host's PROCESSED-block ranges (_meta) on [283417317, head] detects an injected skipped PROCESSED range (NOT a missing event-bearing block)"
    - "B3 invariant rejects any row whose log_address != proxy 0x5E5205CF…163E6"
    - "(block_number, log_index) tuple ordering matches a recorded RPC tuple sequence on a multi-log block; block_ts_utc is coarse-secondary (never the sort key)"
    - "get_block_receipts→None degrades to eth_getLogs and flags weakened completeness assurance"
    - "liveness gap math escalates on gap>60 for 3 consecutive polls, not on ≤60"
    - "safe_block_depth rule: observed_max=0 → depth=1; observed>0 → max(1, observed+margin)"
  artifacts:
    - path: "indexing/completeness.py"
      provides: "leg-b structural floor (RequestCreated vs RequestFinalized count balance) + non-gating descriptive count-ratio log, tx-anchor floor, cursor-contiguity over processed ranges, B3 proxy invariant (pure logic)"
      min_lines: 50
    - path: "indexing/ordering.py"
      provides: "(block_number, log_index) tuple-ordering verifier + coarse-secondary timestamp rule"
    - path: "indexing/parity.py"
      provides: "receipts-None degradation fallback flagging weakened assurance"
    - path: "indexing/liveness.py"
      provides: "gap-escalation + safe_block_depth pure-logic rules"
  key_links:
    - from: "indexing/completeness.py::structural_ratio_status"
      to: "RequestCreated(0xb623) count vs RequestFinalized(0x65db) count balance"
      via: "structural floor on count(RequestCreated) vs count(RequestFinalized); descriptive count-ratio ÷ own distinct-tx is logged, not gated; never external 234999"
      pattern: "structural|RequestFinalized|request_created_count"
    - from: "indexing/completeness.py::tx_anchor_floor"
      to: "Blockscout transactions_count"
      via: "monotonicity floor ≥ 234999"
      pattern: "234999|234_999"
    - from: "indexing/completeness.py::cursor_contiguity"
      to: "host _meta processed-block ranges"
      via: "proves the host advanced its cursor over [deploy, head] with zero skipped ranges (NOT event-block presence)"
      pattern: "cursor_contiguity|processed"
---

<objective>
Wave 0 (parallel with 03-01): the pure-logic validation engine for INDEX-01's three correctness gates — completeness (3-leg), ordering, parity-degradation, liveness/rollback — each a network-free function with its CI test. These are the EXACT functions the LIVE probes in Plan 03-04 call to process the real indexed store + RPC + Blockscout outputs into the `indexing/*.md` artifacts. CI asserts the logic against synthetic fixtures + recorded constants (03-VALIDATION.md CI-vs-LIVE boundary).

Purpose: the completeness/ordering/parity/liveness MATH must be tested independently of the live deploy so a bug in the gate logic is caught in CI, not discovered after a 36.3M-block backfill. Encodes decision 4 (proceed+log; leg-b is non-blocking — legs a+c carry binding completeness) and the locked parity mechanism. Leg-b uses the RESOLVED topic0 roles (RequestCreated = 0xb623, RequestFinalized = 0x65db; ROADMAP SC#6(b) correction 2026-05-29 / topic0_map_v1.json) — NOT the scout's inverted labels. CRITICAL (gate-review #2 M-3): leg-b's OPERATIVE gate is a STRUCTURAL FLOOR on the RequestCreated-vs-RequestFinalized COUNT balance (a real "missing-RequestCreated" signal), NOT a Bernoulli-proportion Wilson CI — 0xb623 fires 1–5×/tx so its COUNT ÷ distinct-tx is NOT a proportion (can exceed 1.0) and a healthy indexer would be FALSE-HALTED by a proportion band.
Output: `indexing/{completeness,ordering,parity,liveness}.py` + four `tests/test_index01_*.py` modules.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-subgraph-indexing/03-RESEARCH.md
@.planning/phases/03-subgraph-indexing/03-VALIDATION.md
@.planning/scout/2026-05-29/event_count_addendum.md
@.planning/scout/2026-05-29/somnia_finality_semantics.md
@schemas/topic0_map_v1.json
@schemas/event_schema_v1.md
@probes/somnia_rpc.py
@probes/blockscout.py
@tests/conftest.py

<interfaces>
<!-- Concrete constants the executor copies into code — no derivation needed. -->

Resolved topic0 roles (schemas/topic0_map_v1.json — impl 0x9af5…3edd; ROADMAP SC#6(b) correction 2026-05-29):
  RequestCreated        = 0xb62339927ed9948fd837358a55f5b9a824f7b047043faece66965593ed726889  (fires 1–5×/tx)
  RequestFinalized      = 0x65db1ef5b3bcd84fe4fb8dbbe1cadc9fe6643bb261ab2e01d65c281c3d466af2
  CommitteeDepositFailed= 0x5c090ef48df2b4d8a01bd0639355d62c318b623aed749bdd12325f789e37a2cf
  The scout addendum's "request pair (0x65db+0x5c09)" labelling was INVERTED — that pair is the
  FINALIZATION pair (RequestFinalized + CommitteeDepositFailed). Leg-b's numerator is the resolved
  RequestCreated (0xb623) COUNT, NOT the finalization-pair tx-appearance rate.

Completeness gate (03-RESEARCH §"Architecture Patterns", ROADMAP SC#6, 03-CONTEXT decision 4):
  - PROXY = "0x5E5205CF39E766118C01636bED000A54D93163E6"
  - DEPLOY_BLOCK = 283417317
  - TX_ANCHOR_FLOOR = 234999            # Blockscout transactions_count monotonicity floor (≥)
  - leg (a) tx-anchor: fresh transactions_count MUST be ≥ 234999 (live-requeried; NOT scaled)
  - leg (b) — STRUCTURAL FLOOR, NOT a proportion CI (gate-review #2 M-3):
            The OPERATIVE leg-b gate is the RequestCreated-vs-RequestFinalized COUNT balance.
            The addendum's per-tx composition (66 {0x5c09:1,0x65db:1} + 20 {0xb623:1} + 17 all-three +
            12 {0xb623 ×2/3/5}) gives a 1:1:1 LIFETIME balance: count(RequestCreated 0xb623) ≈
            count(RequestFinalized 0x65db) (each request eventually emits one of each). So the real
            structural signal of MISSING RequestCreated events is a COUNT IMBALANCE vs RequestFinalized:
              imbalance = |count(RequestCreated) − count(RequestFinalized)| / max(count(RequestCreated), count(RequestFinalized))
              HALT only if imbalance > STRUCTURAL_TOLERANCE (a stated structural tolerance proving
              missing RequestCreated relative to its 1:1:1 partner) — else PASS.
            WHY NOT a Wilson proportion band: 0xb623 fires 1–5×/tx, so RequestCreated COUNT ÷ distinct-tx
            is a COUNT ratio, NOT a Bernoulli proportion bounded [0,1]; the count ratio can exceed 1.0,
            and a healthy indexer would be FALSE-HALTED by the [0.554,0.878] gross band. The Wilson band
            [0.628,0.790] / point 0.716 is dimensionally mis-applied to a count ratio → RETIRED as a gate.
            DESCRIPTIVE OUTPUT (NOT a gate): record the observed RequestCreated(0xb623) COUNT ÷ the
            indexer's OWN distinct-tx count PER STRATUM (deploy/mid/head) as an INDEX-01 OUTPUT in
            completeness_proof.md. The 0.716 figure may be kept ONLY as a documented descriptive
            reference with an explicit "NOT a gate — see structural floor" note. EVALUATE the descriptive
            ratio against the indexer's OWN distinct-tx count, NEVER the external 234999 (circularity).
            leg-b stays NON-BLOCKING in the decision-4 sense (legs a+c carry binding completeness); the
            exact RequestCreated lifetime count is an INDEX-01 OUTPUT, not a pre-known constant.
            STRUCTURAL_TOLERANCE = 0.15 (a 15% count imbalance vs the 1:1:1 RequestFinalized partner is
            the stated structural floor proving materially-missing RequestCreated events; basis: the
            addendum 66/20/17/12 composition + 83/83/83 1:1:1 in-sample balance).
  - leg (c) cursor-contiguity: the HOST'S PROCESSED-block cursor (_meta processed-range) advanced over
            [DEPLOY_BLOCK, head] with ZERO skipped processed ranges ⇒ host believes it indexed every block.
            This is NOT contiguity of event-bearing block_numbers (~0.0005 RequestCreated/block → most blocks
            carry NO proxy log; naive event-block interval-fill would flood false gaps). Cursor-contiguity is
            completeness-bearing ONLY conjoined with leg (a).
  - B3 invariant (PITFALLS B3): every indexed row's log_address == PROXY (lowercased compare).

Parity (locked: eth_getBlockReceipts AVAILABLE; 03-RESEARCH Pitfall 4):
  - probes/somnia_rpc.get_block_receipts(blk) returns None on method-not-found.
  - On None → fall back to eth_getLogs over the window AND flag completeness assurance weakened to contiguity.

Liveness (PITFALLS B2) + safe_block_depth (KPD-09-empirical, somnia_finality_semantics.md):
  - poll _meta.block.number vs eth_blockNumber every 5 min; gap = rpc_head - indexer_head.
  - escalate if gap > 60 for 3 CONSECUTIVE polls.
  - safe_block_depth: observed_max==0 → 1 (PBFT provisional); observed>0 → max(1, observed_max + margin), margin default 1.

From probes/somnia_rpc.py: head_block(), get_block_receipts(blk)→list|None, get_logs(addr,from,to).
From probes/blockscout.py: tx_count(addr)→int  (reads /addresses/{addr}/counters → transactions_count).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Completeness logic (3-leg) + CI tests</name>
  <read_first>
    - .planning/scout/2026-05-29/event_count_addendum.md (the resolved-roles reconciliation note + the 66/20/17/12 per-tx composition + the 83/83/83 1:1:1 balance + 234999 tx anchor; RequestCreated=0xb623, RequestFinalized=0x65db — the basis for leg-b's STRUCTURAL FLOOR)
    - schemas/topic0_map_v1.json (the RESOLVED topic0→role map — RequestCreated 0xb623 + RequestFinalized 0x65db, leg-b's structural-floor operands)
    - .planning/ROADMAP.md §"Phase 3" SC#6(b) (the 2026-05-29 resolved-roles correction this leg-b must match)
    - .planning/phases/03-subgraph-indexing/03-RESEARCH.md §"Pattern 2/Pitfall 6" + ROADMAP SC#6 (the three legs)
    - .planning/phases/03-subgraph-indexing/03-CONTEXT.md §"Completeness-anomaly (leg-b) handling" (decision 4: leg-b non-blocking; legs a+c bind)
    - tests/conftest.py (N2 fixture contract)
    - tests/test_event_schema.py (test style to mirror)
  </read_first>
  <behavior>
    - test_structural_floor_balanced: a BALANCED count pair (request_created_count=83, request_finalized_count=83) → status "pass"; a count pair within tolerance (83 vs 90, imbalance ≈ 0.078 < 0.15) → "pass".
    - test_structural_floor_halts_on_missing_request_created: an IMBALANCED pair proving missing RequestCreated (request_created_count=40, request_finalized_count=83, imbalance ≈ 0.518 > 0.15) → status "halt". This is the real structural signal (RequestCreated materially below its 1:1:1 RequestFinalized partner), NOT a proportion-CI breach.
    - test_count_ratio_over_1_does_not_halt: a count ratio that EXCEEDS 1.0 (e.g. request_created_count=200 vs indexer_distinct_tx=116 → ratio ≈ 1.72, because 0xb623 fires 1–5×/tx) does NOT halt — the descriptive count-ratio is LOG-ONLY; assert `structural_ratio_status` returns "pass" when the COUNT balance with RequestFinalized is healthy regardless of the >1.0 descriptive ratio.
    - test_descriptive_ratio_is_emitted_not_gated: the descriptive `request_created_count / indexer_distinct_tx` ratio is COMPUTED and returned for logging (e.g. via a helper `descriptive_count_ratio(request_created_count, indexer_distinct_tx) -> float`) but is NEVER the pass/halt discriminator — assert the helper exists and that the gate verdict is driven by the RequestFinalized count, not by this ratio.
    - test_uses_own_tx_count_for_descriptive: the descriptive ratio takes the indexer's OWN distinct-tx count as the denominator; passing the external 234999 anchor as the denominator is rejected/guarded — assert the function signature names it `indexer_distinct_tx`, not the anchor.
    - test_tx_anchor_floor: fresh transactions_count 234998 → FAIL; 234999 → PASS; 300000 → PASS.
    - test_cursor_contiguity_detects_skipped_range: PROCESSED ranges [(283417317, 283417318), (283417320, 283417321)] over [283417317, 283417321] (the processed-block 283417319 skipped) → reports a gap; a single fully-contiguous processed range [(283417317, 283417321)] → no gap. The injected defect is a skipped PROCESSED range, NOT a missing event-bearing block.
    - test_proxy_address_invariant: a row with log_address == PROXY (any case) passes; a foreign address is rejected.
  </behavior>
  <action>
    Create `indexing/completeness.py` (pure, no network):
    - Module docstring: cites "ROADMAP SC#6(b) correction 2026-05-29 / topic0_map_v1.json"; states RequestCreated = 0xb623 and RequestFinalized = 0x65db; states the scout addendum's "request pair (0x65db+0x5c09)" was the INVERTED label for the FINALIZATION pair (RequestFinalized + CommitteeDepositFailed). CRITICALLY documents (gate-review #2 M-3): leg-b's OPERATIVE gate is the RequestCreated-vs-RequestFinalized COUNT BALANCE (a structural floor proving missing RequestCreated), NOT a Bernoulli-proportion Wilson CI — because 0xb623 fires 1–5×/tx, its COUNT ÷ distinct-tx is a count ratio (can exceed 1.0), so a proportion band would FALSE-HALT a healthy indexer. The 0.716 figure (= 83/116, the finalization-pair tx-appearance rate under the in-sample 83/83/83 balance) is kept ONLY as a documented descriptive reference with an explicit "NOT a gate — see structural floor" note.
    - `STRUCTURAL_TOLERANCE = 0.15` (count-imbalance floor vs the 1:1:1 RequestFinalized partner), `DESCRIPTIVE_RATIO_REFERENCE = 0.716`  (documented descriptive figure — NOT a gate; comment it), `TX_ANCHOR_FLOOR = 234999`, `PROXY = "0x5e5205cf39e766118c01636bed000a54d93163e6"` (lowercased), `REQUEST_CREATED_TOPIC0 = "0xb62339927ed9948fd837358a55f5b9a824f7b047043faece66965593ed726889"`, `REQUEST_FINALIZED_TOPIC0 = "0x65db1ef5b3bcd84fe4fb8dbbe1cadc9fe6643bb261ab2e01d65c281c3d466af2"` (leg-b's structural-floor operands).
    - `structural_ratio_status(request_created_count: int, request_finalized_count: int, indexer_distinct_tx: int) -> str`: the OPERATIVE leg-b gate. Compute `imbalance = abs(request_created_count - request_finalized_count) / max(request_created_count, request_finalized_count, 1)`; return `"halt"` if `imbalance > STRUCTURAL_TOLERANCE` (materially-missing RequestCreated vs its 1:1:1 RequestFinalized partner), else `"pass"`. `indexer_distinct_tx` is used ONLY to compute the DESCRIPTIVE log ratio (see below) — it is NEVER the pass/halt discriminator, and the external 234999 must NEVER be passed here (circularity). Docstring: "OPERATIVE leg-b gate = RequestCreated-vs-RequestFinalized COUNT balance (structural floor). NOT a proportion CI: 0xb623 fires 1–5×/tx so request_created_count/indexer_distinct_tx is a COUNT ratio (can exceed 1.0) — a Wilson proportion band would FALSE-HALT a healthy indexer. Wilson [0.628,0.790]/0.716 RETIRED as the gate. leg-b stays non-blocking in decision-4 terms (legs a+c bind); the exact RequestCreated lifetime count is an INDEX-01 output."
    - `descriptive_count_ratio(request_created_count: int, indexer_distinct_tx: int) -> float`: returns `request_created_count / indexer_distinct_tx` — the LOG-AND-EMIT descriptive figure per stratum (deploy/mid/head), explicitly NOT a gate (may exceed 1.0). Docstring: "descriptive INDEX-01 OUTPUT for completeness_proof.md — NOT a pass/halt gate; denominator is the indexer's OWN distinct-tx, NEVER the external 234999."
    - `tx_anchor_ok(fresh_tx_count: int) -> bool`: `fresh_tx_count >= TX_ANCHOR_FLOOR`.
    - `cursor_contiguity(processed_ranges: list[tuple[int,int]], deploy: int, head: int) -> list[tuple[int,int]]`: takes the host's PROCESSED-block ranges (from `_meta`, inclusive `(lo, hi)` spans), returns the missing spans inside `[deploy, head]` not covered by any processed range (empty list = the host cursor advanced contiguously over the whole range). Docstring: "proves the HOST CURSOR advanced over every block (host belief), NOT that an event was emitted in every block; over a sparse log population (~0.0005 RequestCreated/block) most blocks carry no log, so event-block interval-fill would flood false gaps — that is why this checks PROCESSED ranges, not event_block_numbers. Completeness-bearing only conjoined with leg (a)."
    - `is_proxy_row(log_address: str) -> bool`: `log_address.lower() == PROXY`.
    Create `tests/test_index01_completeness.py` per the <behavior> block (consume conftest fixtures as params for any artifact reads; the logic tests are pure-Python and need no fixtures).
  </action>
  <verify>
    <automated>uv run pytest tests/test_index01_completeness.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_index01_completeness.py -x` exits 0
    - `grep -q "indexer_distinct_tx" indexing/completeness.py` (own-count param, anti-circularity — descriptive only)
    - `grep -qE "234999|234_999" indexing/completeness.py`
    - `grep -q "request_finalized_count" indexing/completeness.py` (leg-b structural floor uses the RequestFinalized count, not a Wilson band)
    - `grep -q "STRUCTURAL_TOLERANCE" indexing/completeness.py` (structural floor, not a proportion CI)
    - `grep -q "0x65db1ef5b3bcd84fe4fb8dbbe1cadc9fe6643bb261ab2e01d65c281c3d466af2" indexing/completeness.py` (RequestFinalized=0x65db, the structural-floor partner)
    - `grep -q "0xb62339927ed9948fd837358a55f5b9a824f7b047043faece66965593ed726889" indexing/completeness.py` (RequestCreated=0xb623)
    - `grep -q "SC#6(b)" indexing/completeness.py` (cites the 2026-05-29 resolved-roles correction)
    - `grep -q "def cursor_contiguity" indexing/completeness.py` (processed-range contiguity, NOT event-block interval-fill)
    - `grep -qi "NOT a gate\|not a proportion\|RETIRED\|structural floor" indexing/completeness.py` (Wilson retired; structural-floor semantics documented)
    - `grep -q "halt" tests/test_index01_completeness.py` (the structural-floor halt branch is tested on a missing-RequestCreated synthetic)
    - `grep -qi "over_1\|exceed\|> 1\|1.72\|ratio" tests/test_index01_completeness.py` (the in-range count-ratio >1.0 does-NOT-halt case is tested)
  </acceptance_criteria>
  <done>3-leg completeness logic implemented and green: leg-b's OPERATIVE gate is the RequestCreated-vs-RequestFinalized COUNT-balance structural floor (halt only on materially-missing RequestCreated), the descriptive RequestCreated-count ÷ own-distinct-tx ratio is LOG-ONLY (may exceed 1.0, never halts), the Wilson proportion band is RETIRED; tx-anchor floor, cursor-contiguity over processed ranges, B3 proxy invariant all present; ratios evaluated against the indexer's own tx count, never the external anchor; leg-c proves host cursor advancement, not event-block presence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ordering + parity-degradation logic + CI tests</name>
  <read_first>
    - schemas/event_schema_v1.md §"Arrival-ordering contract" (the (block_number, log_index) key; block_ts_utc coarse secondary)
    - .planning/phases/03-subgraph-indexing/03-RESEARCH.md §"Pattern parity / Pitfall 4" (receipts-None degradation)
    - ROADMAP SC#7 (tuple-for-tuple ordering on multi-log blocks) + SC#2 (parity mechanism)
    - probes/somnia_rpc.py (get_block_receipts returns None on method-not-found)
  </read_first>
  <behavior>
    - test_tuple_ordering: given indexer rows and a recorded RPC tuple sequence for a multi-log block, `verify_ordering` returns match when the (block_number, log_index) sequences are tuple-for-tuple equal; mismatch when a tuple differs.
    - test_timestamp_is_coarse_secondary: ordering uses (block_number, log_index) ONLY — two rows in the same block with the same block_ts_utc but different log_index sort by log_index, and a timestamp-only sort would scramble them (asserted: the sort key function ignores ts).
    - test_receipts_none_degrades: `parity_reference(receipts=None, ...)` returns a result flagged `degraded=True` with `mechanism="eth_getLogs"` and `assurance="weakened_to_contiguity"`; `receipts=[...]` returns `degraded=False, mechanism="eth_getBlockReceipts"`.
  </behavior>
  <action>
    Create `indexing/ordering.py`:
    - `arrival_sort_key(row) -> tuple[int,int]`: returns `(row["block_number"], row["log_index"])` — NEVER includes block_ts_utc.
    - `verify_ordering(indexer_tuples: list[tuple[int,int]], rpc_tuples: list[tuple[int,int]]) -> bool`: tuple-for-tuple equality of the two ordered sequences.
    Create `indexing/parity.py`:
    - `parity_reference(receipts, get_logs_fn, proxy, blk) -> dict`: if `receipts is None`, call `get_logs_fn` and return `{"degraded": True, "mechanism": "eth_getLogs", "assurance": "weakened_to_contiguity"}`; else extract proxy logs from receipts and return `{"degraded": False, "mechanism": "eth_getBlockReceipts", "assurance": "full"}`.
    Create `tests/test_index01_ordering.py` and `tests/test_index01_parity.py` per <behavior> (mock `get_logs_fn` — no real network).
  </action>
  <verify>
    <automated>uv run pytest tests/test_index01_ordering.py tests/test_index01_parity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_index01_ordering.py tests/test_index01_parity.py -x` exits 0
    - `grep -q "(block_number, log_index)" indexing/ordering.py` OR `grep -q "block_number.*log_index" indexing/ordering.py`
    - `grep -q "weakened_to_contiguity" indexing/parity.py`
    - `grep -q "eth_getBlockReceipts" indexing/parity.py`
  </acceptance_criteria>
  <done>Ordering verifier sorts on `(block_number, log_index)` only (ts coarse-secondary) and matches recorded RPC tuples; parity-degradation flags the receipts-None fallback with weakened assurance. Both modules green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Liveness gap + safe_block_depth logic + CI tests</name>
  <read_first>
    - .planning/scout/2026-05-29/somnia_finality_semantics.md (PBFT provisional safe_block_depth=1)
    - .planning/phases/03-subgraph-indexing/03-CONTEXT.md §"Liveness + rollback observation window" (representative window; poll params)
    - .planning/phases/03-subgraph-indexing/03-RESEARCH.md §"Pattern 4 safe_block_depth" (max(1, observed+margin))
    - ROADMAP PITFALLS B2 (gap>60 for 3 consecutive)
  </read_first>
  <behavior>
    - test_gap_escalation: a poll sequence with three consecutive gaps >60 → escalate True; a sequence where the run is broken (e.g. 61,61,40,61) → escalate False; gaps all ≤60 → escalate False.
    - test_safe_block_depth_rule: observed_max=0 → 1; observed_max=3, margin=1 → 4; observed_max=0 with margin>0 still → 1 (the max(1,...) floor + the zero-observed special case both hold).
  </behavior>
  <action>
    Create `indexing/liveness.py`:
    - `GAP_THRESHOLD = 60`, `CONSECUTIVE = 3`.
    - `should_escalate(gaps: list[int]) -> bool`: True iff there exists a run of `CONSECUTIVE` consecutive polls each with `gap > GAP_THRESHOLD`.
    - `safe_block_depth(observed_max: int, margin: int = 1) -> int`: if `observed_max == 0` return `1`; else `max(1, observed_max + margin)`.
    Create `tests/test_index01_liveness.py` per <behavior> (pure logic, no network).
  </action>
  <verify>
    <automated>uv run pytest tests/test_index01_liveness.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_index01_liveness.py -x` exits 0
    - `grep -q "GAP_THRESHOLD = 60" indexing/liveness.py`
    - `grep -q "def safe_block_depth" indexing/liveness.py`
    - `grep -q "def should_escalate" indexing/liveness.py`
  </acceptance_criteria>
  <done>Liveness gap-escalation (gap>60 ×3 consecutive) and safe_block_depth rule (observed_max=0→1; else max(1,observed+margin)) implemented and green.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_index01_completeness.py tests/test_index01_ordering.py tests/test_index01_parity.py tests/test_index01_liveness.py -x` green.
- `uv run pytest tests/ -q` green (no regressions; no live network in any new module).
- All four `indexing/*.py` logic modules are pure (no `urllib`/RPC import in the logic functions; `get_logs_fn` is injected for parity).
- Leg-b's OPERATIVE gate is the RequestCreated(0xb623)-vs-RequestFinalized(0x65db) COUNT-balance structural floor (cites SC#6(b)); the Wilson proportion band is RETIRED; the RequestCreated-count ÷ own-distinct-tx ratio is descriptive log-only (may exceed 1.0). leg-c is cursor-contiguity over processed ranges, NOT event-block interval-fill.
</verification>

<success_criteria>
- Maps 03-VALIDATION.md rows #4 (leg-b structural-floor count-balance), #5 (tx-anchor floor), #6 (cursor-contiguity), #7 (B3 invariant), #8 (ordering), #9 (parity degradation), #10 (liveness gap), #11 (safe_block_depth).
- decision 4 encoded: leg-b OPERATIVE gate = RequestCreated-vs-RequestFinalized COUNT-balance structural floor (halt only on materially-missing RequestCreated); the descriptive count ratio is LOG-AND-EMIT only (NOT a proportion CI, may exceed 1.0); leg-b non-blocking (legs a+c bind; exact RequestCreated lifetime count is an INDEX-01 output); ratio against the indexer's own tx count (anti-circularity).
- These functions are the tested code path the Plan 03-04 LIVE probes call to emit `indexing/*.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-subgraph-indexing/03-02-SUMMARY.md`.
Commits are LOCAL only — NEVER push.
</output>
</content>
</invoke>

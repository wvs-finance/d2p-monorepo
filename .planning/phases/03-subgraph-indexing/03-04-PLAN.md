---
phase: 03-subgraph-indexing
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - probes/index01_live.py
  - indexing/completeness_proof.md
  - indexing/ordering_verification.md
  - indexing/parity_report.md
  - .planning/scout/2026-05-29/rollback_observation.md
  - subgraphs/iagentrequester/abis/IAgentRequester.json
  - tests/fixtures/getrequest_response.json
autonomous: false
requirements: [INDEX-01]
user_setup:
  - service: ormi-0xgraph
    why: "Subgraph host (DATA-SOURCE-01 selected; Ormi free Developer provisional). Free tier needs an account + deploy key; the $75/mo Production crossing is a FORCED, user-confirmed event gated on projected-overage (no auto-spend)."
    env_vars:
      - name: ORMI_API_KEY
        source: "Ormi 0xGraph dashboard → API key / deploy key (subgraph.somnia.network)"
    dashboard_config:
      - task: "Create the free Developer account; disable free-tier auto-upgrade to Production at signup (Pitfall 5 — prevents a silent paid crossing)"
        location: "Ormi 0xGraph dashboard"
      - task: "Resolve the Somnia-MAINNET deploy slug + endpoint (deploy-probe: somnia vs somnia-mainnet; api.subgraph.somnia.network/deploy) — vendor docs only confirm testnet"
        location: "Ormi 0xGraph / Somnia subgraph docs"
must_haves:
  truths:
    - "the subgraph deploys to Somnia mainnet (slug resolved at deploy time) and backfills 283417317 → head"
    - "the three-leg completeness gate runs against the REAL store: fresh transactions_count ≥ 234999 (leg a), structural ratios within the Wilson band vs the indexer's OWN tx count (leg b, proceed+log / halt>2×), and full-range contiguity (leg c)"
    - "(block_number, log_index) tuple-for-tuple ordering matches direct-RPC on real multi-log blocks; block_ts_utc matches eth_getBlockByNumber exactly"
    - "per-block-receipt parity over a stratified ≥300-window sample is within 0.5% of direct-RPC with zero indexer=0 ∧ RPC>0 windows; a few-hour liveness window shows head-lag <60 for ≥99% of polls"
    - "safe_block_depth_observed_max is recorded over a ≥1h rollback observation"
    - "the projected-overage entity-count check runs after the first backfill chunk; if the projection crosses 300k the run PAUSES and surfaces entity-count evidence for the $75/mo sign-off (no auto-spend)"
    - "the aggregate Σ executionCost is filled off-chain via batched getRequest (selector 0xc58343ef) at the finalized block (Arch B)"
  artifacts:
    - path: "indexing/completeness_proof.md"
      provides: "SC#6 three-leg gate against the real indexed store (leg a/b/c, with the leg-b derivation basis + gross-deviation multiplier stated)"
      contains: "234999"
    - path: "indexing/ordering_verification.md"
      provides: "SC#7 (block_number, log_index) tuple-for-tuple + block_ts_utc exact on real multi-log blocks"
    - path: "indexing/parity_report.md"
      provides: "SC#2 per-block-receipt parity over stratified ≥300 windows + the liveness window readout; cites the data_sourcing_matrix.yaml key"
    - path: ".planning/scout/2026-05-29/rollback_observation.md"
      provides: "KPD-09-empirical safe_block_depth_observed_max over ≥1h"
      contains: "safe_block_depth_observed_max"
    - path: "probes/index01_live.py"
      provides: "the __main__-only live orchestrator: deploy poll + backfill + getRequest fill + the 3 gates + liveness/rollback + projected-overage, calling the Plan 03-02 logic functions"
  key_links:
    - from: "probes/index01_live.py"
      to: "indexing/completeness.py + ordering.py + parity.py + liveness.py"
      via: "the live probe imports the CI-tested logic; never re-implements it"
      pattern: "from indexing"
    - from: "probes/index01_live.py"
      to: "subgraph GraphQL Request totalCount + 300k cap"
      via: "projected-overage extrapolation → pause + surface (no auto-spend)"
      pattern: "300000|300_000|totalCount"
---

<objective>
Wave 2 (LIVE): deploy the subgraph to Somnia mainnet, backfill 283417317 → head, fill the aggregate Σ executionCost off-chain (Arch B, selector 0xc58343ef at the finalized block), and run the three correctness gates + liveness + rollback against the REAL store — emitting `indexing/completeness_proof.md`, `indexing/ordering_verification.md`, `indexing/parity_report.md`, and `.planning/scout/2026-05-29/rollback_observation.md`. All live work runs through `probes/index01_live.py` (`__main__`-only, per the repo convention), which IMPORTS the CI-tested logic from Plan 03-02 — it never re-implements the gate math. Includes the projected-overage entity-count check and the $75/mo no-auto-spend payment checkpoint (decision 2 + Phase-2 note 3 — the user instructed this be on the plan).

Purpose: this is the binding INDEX-01 phase gate. The CI logic (03-01/03-02) is proven; this plan produces the live artifacts `/gsd:verify-work` checks. The deploy slug + free-tier entity-count read are DEPLOY-PROBES (03-RESEARCH Open Q1/Q2, MEDIUM doc-gaps) — resolved live, not hard-coded.
Output: `probes/index01_live.py` + the four live artifacts + the re-frozen real `getrequest_response.json` fixture.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-subgraph-indexing/03-RESEARCH.md
@.planning/phases/03-subgraph-indexing/03-CONTEXT.md
@.planning/phases/03-subgraph-indexing/03-VALIDATION.md
@.planning/phases/02-topic-implementation-provenance/02-FORWARD-NOTES.md
@research/DATA_SOURCING.md
@probes/somnia_rpc.py
@probes/blockscout.py
@indexing/decode.py
@indexing/completeness.py
@indexing/ordering.py
@indexing/parity.py
@indexing/liveness.py

<interfaces>
<!-- The CI-tested logic this live probe orchestrates. Import — never re-implement. -->

From indexing/decode.py:    decode_get_request(raw_hex), sum_execution_cost(request_tuple), GETREQUEST_SELECTOR="0xc58343ef"
From indexing/completeness.py: structural_ratio_status(request_created_count, indexer_distinct_tx), tx_anchor_ok(fresh_tx_count), contiguity_gaps(block_numbers, lo, hi), is_proxy_row(log_address)
From indexing/ordering.py:  arrival_sort_key(row), verify_ordering(indexer_tuples, rpc_tuples)
From indexing/parity.py:    parity_reference(receipts, get_logs_fn, proxy, blk)
From indexing/liveness.py:  should_escalate(gaps), safe_block_depth(observed_max, margin=1)
From probes/somnia_rpc.py:  head_block(), get_block_receipts(blk)→list|None, get_logs(addr,from,to), get_request(request_id, block), get_block(blk), DEPLOYMENT_BLOCK=283417317, PROXY
From probes/blockscout.py:  tx_count(addr)→int  (live re-query, rate-limited — budget backoff)

Deploy (03-RESEARCH §"Standard Stack" + Open Q1 — DEPLOY-PROBE):
  npm install -g @graphprotocol/graph-cli
  graph codegen && graph build   (in subgraphs/iagentrequester/)
  graph deploy iagentrequester --node https://api.subgraph.somnia.network/deploy \
    --ipfs https://api.subgraph.somnia.network/ipfs --deploy-key $ORMI_API_KEY
  network slug: try "somnia"; if rejected try "somnia-mainnet"; if mainnet free-tier deploy unavailable → checkpoint (decision 2 sign-off) or GetBlock sub-plan re-author.
  indexed head + health: GraphQL { _meta { block { number hash timestamp } hasIndexingErrors } }

Completeness gate parameters (03-CONTEXT decision 4 — STATE THESE in completeness_proof.md):
  leg (a): fresh transactions_count ≥ 234999 (monotonicity floor; live re-query at indexed head, NOT scaled).
  leg (b): RequestCreated/txs ratio vs Wilson 95% CI [0.628, 0.790] (basis wilson_95ci_n116, point 0.716),
           evaluated against the indexer's OWN distinct-tx count; PROCEED+LOG inside band;
           HALT only if deviation > 2× band (gross band ≈ [0.554, 0.878]); re-measure per stratum (deploy/mid/head).
  leg (c): COUNT(DISTINCT block_number) over [283417317, head] contiguous; completeness-bearing only WITH leg (a).

Parity (SC#2): per-block-receipt scan over a stratified ≥300-window sample (deploy/mid/head thirds);
  on get_block_receipts→None, fall back to eth_getLogs + flag weakened assurance (parity.parity_reference).
Liveness (B2): poll _meta vs eth_blockNumber every 5min; escalate gap>60 ×3 consecutive; few-hour representative window.
safe_block_depth (KPD-09-empirical): ≥1h head-watch; observed_max=0 → 1 (PBFT provisional, somnia_finality_semantics.md).
Projected-overage (decision 2): after first backfill chunk, GraphQL Request totalCount over the chunk block range,
  extrapolate linearly to the full 36.3M span vs 300000; if projection crosses → PAUSE + surface evidence (checkpoint).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live orchestrator probe (__main__-only) — deploy poll, backfill, Arch-B fill, 3 gates, liveness/rollback, projected-overage</name>
  <read_first>
    - probes/somnia_rpc.py + probes/blockscout.py (reusable RPC/Blockscout helpers — reuse, never duplicate)
    - indexing/completeness.py, ordering.py, parity.py, liveness.py, decode.py (the CI-tested logic to import)
    - .planning/phases/03-subgraph-indexing/03-RESEARCH.md §"Code Examples" (the GraphQL _meta query, per-block-receipt parity, off-chain getRequest, tx-anchor snippets) + §"Common Pitfalls 1-6"
    - .planning/phases/03-subgraph-indexing/03-VALIDATION.md §"Manual-Only Verifications" (what each live step must emit)
    - .planning/phases/03-subgraph-indexing/03-CONTEXT.md decisions 2 + 4
  </read_first>
  <action>
    Create `probes/index01_live.py` — stdlib + the repo's own modules only; ALL network/IO inside the `if __name__ == "__main__":` block (per the `probes/*.py` convention; NEVER imported by CI). Structure it as labelled, independently-runnable functions called from `__main__` with CLI subcommands (e.g. `deploy-check`, `backfill`, `fill-cost`, `gate-completeness`, `gate-ordering`, `gate-parity`, `liveness`, `rollback`, `overage`):
    - `graphql_meta()` — POST the `{ _meta { block { number hash timestamp } hasIndexingErrors } }` query to the Ormi endpoint (Authorization: $ORMI_API_KEY) → indexer head.
    - `deploy_check()` — print the resolved network slug attempt order (somnia → somnia-mainnet) + deploy endpoint; print `_meta` once deployed. On slug/endpoint rejection, print a clear DEPLOY-PROBE-FAILED message pointing to the checkpoint.
    - `fill_cost(request_ids, finalized_blocks)` — for each finalized request, `get_request(rid, finalized_block)` (selector 0xc58343ef at the FINALIZED block — Pitfall 3), `decode_get_request` + `sum_execution_cost`; write `request_id, sum_execution_cost` as `pl.Utf8`/Python-int (per-member detail summed + discarded — decision 1). ALSO capture ONE real raw return and re-freeze `tests/fixtures/getrequest_response.json` with the real `raw` + recomputed `expected_sum_execution_cost` (replaces the synthetic fixture from Plan 03-01; keep the field names identical so the decode test still passes).
    - `gate_completeness()` — leg(a) `blockscout.tx_count(PROXY)` ≥ 234999; leg(b) `structural_ratio_status(rc_count, indexer_distinct_tx)` per stratum (proceed+log / halt); leg(c) `contiguity_gaps(...)` over [283417317, head]. Emit `indexing/completeness_proof.md` stating the leg-b derivation basis (wilson_95ci_n116) + the gross-deviation multiplier (2×) explicitly.
    - `gate_ordering()` — on the SC#2 sampled multi-log blocks, build indexer (block_number, log_index) tuples + the RPC tuples (from receipts/getLogs), `verify_ordering(...)` tuple-for-tuple, and compare `block_ts_utc` vs `get_block(blk)` timestamp exactly. Emit `indexing/ordering_verification.md`.
    - `gate_parity()` — stratified ≥300 windows (deploy/mid/head thirds); per window `parity_reference(get_block_receipts(blk), get_logs, PROXY, blk)`; assert within 0.5% + zero indexer=0 ∧ RPC>0; record degradation if any window returns None. Cite the `data_sourcing_matrix.yaml` key for the mechanism. Append the few-hour liveness readout (head-lag <60 for ≥99% of polls, `should_escalate`) → `indexing/parity_report.md`.
    - `rollback()` — ≥1h head-watch; record `safe_block_depth_observed_max` (0 if no rollback → `safe_block_depth(0)==1`) → `.planning/scout/2026-05-29/rollback_observation.md` with provenance (source_url + utc_fetch_ts). After writing, re-pin it in `.planning/scout/2026-05-29/PROVENANCE.sha256` (the set-equality test in tests/test_scout_archive.py is `.md`-scoped — a new `.md` MUST be re-pinned or that test fails by design).
    - `overage()` — after the first backfill chunk, GraphQL Request `totalCount` over the chunk range, extrapolate linearly to the 36.3M span vs 300000; if the projection crosses, PRINT the entity-count evidence and EXIT with a non-zero `OVERAGE_PROJECTED` signal (the human checkpoint in Task 2 consumes this; NO auto-spend, NO auto-provision).
    Each emitter stamps `source_url + utc_fetch_ts` provenance. Keep the gate MATH in the imported `indexing/*` functions — this probe only orchestrates IO + formats the artifacts.
  </action>
  <verify>
    <automated>uv run python -c "import ast; ast.parse(open('probes/index01_live.py').read())" && grep -q "from indexing" probes/index01_live.py && grep -q "0xc58343ef\|GETREQUEST_SELECTOR\|get_request" probes/index01_live.py && grep -q "__main__" probes/index01_live.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('probes/index01_live.py').read())"` exits 0 (valid Python)
    - `grep -q "from indexing" probes/index01_live.py` (imports the CI-tested logic, not re-implemented)
    - `grep -qE "300000|300_000" probes/index01_live.py` (projected-overage 300k cap)
    - `grep -q "OVERAGE_PROJECTED\|no auto-spend\|no-auto-spend" probes/index01_live.py`
    - `grep -qE "234999|234_999" probes/index01_live.py`
    - `grep -q "if __name__" probes/index01_live.py` and `! grep -q "^import urllib" probes/index01_live.py || grep -q "__main__" probes/index01_live.py` (network confined to __main__ — manual review confirms no module-level network call)
  </acceptance_criteria>
  <done>`probes/index01_live.py` orchestrates deploy-check, backfill, Arch-B getRequest fill (0xc58343ef at finalized block), the 3 gates, liveness, rollback, and projected-overage — importing the Plan 03-02 logic, with all network confined to `__main__`, and surfacing `OVERAGE_PROJECTED` (no auto-spend).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the live deploy + backfill + gates; produce the four artifacts (+ entity-overage / payment checkpoint)</name>
  <what-built>
    `probes/index01_live.py` (Task 1) drives the live sequence. This checkpoint is where the human runs it against real Ormi/Somnia + RPC + Blockscout, confirms the four artifacts, and handles the projected-overage payment decision if it fires. The deploy slug + free-tier entity-count read are DEPLOY-PROBES (resolved here, not pre-assumed).
  </what-built>
  <how-to-verify>
    1. Ensure `ORMI_API_KEY` is set and the Ormi free Developer account exists with auto-upgrade DISABLED (Pitfall 5). Resolve the mainnet deploy slug:
       `cd subgraphs/iagentrequester && npm install -g @graphprotocol/graph-cli && graph codegen && graph build`
       `graph deploy iagentrequester --node https://api.subgraph.somnia.network/deploy --ipfs https://api.subgraph.somnia.network/ipfs --deploy-key $ORMI_API_KEY`  (try slug `somnia`, then `somnia-mainnet`).
       If mainnet free-tier deploy is UNAVAILABLE → STOP and report (decision 2 sign-off, or GetBlock sub-plan re-author).
    2. Backfill 283417317 → head; after the FIRST chunk run `uv run python -m probes.index01_live overage`. If it prints `OVERAGE_PROJECTED`: the projection crosses 300k → the run is PAUSED; surface the entity-count evidence to the user. The user pays $75/mo Ormi Production and CONFIRMS before any provisioning. NO auto-spend under any circumstance. If under cap, proceed.
    3. Run the off-chain Σ executionCost fill: `uv run python -m probes.index01_live fill-cost` (selector 0xc58343ef at the finalized block); confirm `tests/fixtures/getrequest_response.json` was re-frozen with a REAL return and `uv run pytest tests/test_index01_decode.py -x` still passes.
    4. Run the three gates + liveness + rollback:
       `uv run python -m probes.index01_live gate-completeness gate-ordering gate-parity liveness rollback`
       Confirm the four artifacts exist and contain the required fields:
       - `indexing/completeness_proof.md` — leg(a) fresh transactions_count ≥ 234999; leg(b) per-stratum ratio status (proceed+log / halt>2×) vs the indexer's OWN tx count, with the wilson_95ci_n116 basis + 2× multiplier stated; leg(c) contiguity over [283417317, head].
       - `indexing/ordering_verification.md` — (block_number, log_index) tuple-for-tuple match + block_ts_utc exact on real multi-log blocks.
       - `indexing/parity_report.md` — per-block-receipt parity over ≥300 stratified windows within 0.5%, zero indexer=0 ∧ RPC>0, mechanism cited by matrix key; liveness head-lag <60 ≥99%.
       - `.planning/scout/2026-05-29/rollback_observation.md` — `safe_block_depth_observed_max` over ≥1h.
    5. Re-pin the new rollback `.md` in `.planning/scout/2026-05-29/PROVENANCE.sha256` and confirm `uv run pytest tests/test_scout_archive.py -x` passes (set-equality test).
    6. Run the full suite: `uv run pytest tests/ -q` — all green.
  </how-to-verify>
  <resume-signal>Type "approved" once the four artifacts exist with the required fields and the full suite is green — OR describe the deploy-probe / overage outcome (e.g. "mainnet free-tier deploy unavailable", "OVERAGE_PROJECTED — user declined $75/mo", "leg-b GROSS deviation — halt") so the orchestrator routes to the sign-off / sub-plan-re-author / paid-swap path.</resume-signal>
  <files>indexing/completeness_proof.md, indexing/ordering_verification.md, indexing/parity_report.md, .planning/scout/2026-05-29/rollback_observation.md</files>
  <action>Human-run live sequence per <how-to-verify>: deploy (resolve mainnet slug somnia→somnia-mainnet), backfill 283417317→head, run the projected-overage check after the first chunk (pause + surface $75/mo evidence on OVERAGE_PROJECTED — NO auto-spend), fill Σ executionCost off-chain (0xc58343ef at the finalized block), then run gate-completeness / gate-ordering / gate-parity / liveness / rollback. Each emits its indexing/*.md (and the rollback .md re-pinned in PROVENANCE.sha256).</action>
  <verify>
    <automated>test -f indexing/completeness_proof.md && test -f indexing/ordering_verification.md && test -f indexing/parity_report.md && test -f .planning/scout/2026-05-29/rollback_observation.md && grep -q 234999 indexing/completeness_proof.md && grep -q safe_block_depth_observed_max .planning/scout/2026-05-29/rollback_observation.md && uv run pytest tests/ -q</automated>
  </verify>
  <done>The four live artifacts exist with the required fields (leg a/b/c, tuple-ordering, parity+liveness, safe_block_depth_observed_max); the projected-overage/$75/mo decision was handled with no auto-spend; the scout PROVENANCE set-equality + full suite are green.</done>
</task>

</tasks>

<verification>
- The four live artifacts exist with the fields enumerated above (`indexing/completeness_proof.md`, `indexing/ordering_verification.md`, `indexing/parity_report.md`, `.planning/scout/2026-05-29/rollback_observation.md`).
- `grep -q "234999" indexing/completeness_proof.md` ; `grep -q "safe_block_depth_observed_max" .planning/scout/2026-05-29/rollback_observation.md`.
- `uv run pytest tests/ -q` green (incl. the re-frozen real getRequest fixture + the re-pinned scout PROVENANCE set-equality).
- The projected-overage / $75/mo decision was handled with NO auto-spend (user-confirmed if it fired).
</verification>

<success_criteria>
- Maps 03-VALIDATION.md LIVE rows L1 (deploy+backfill), L2 (3-leg completeness), L3 (liveness window), L4 (rollback), L5 (projected-overage) + INDEX-01 SC#1–SC#7.
- Architecture B executed: aggregate Σ executionCost filled off-chain (0xc58343ef at finalized block), no in-subgraph eth_call.
- The deploy slug + entity-count read were resolved as DEPLOY-PROBES (not hard-coded); the payment protocol checkpoint is explicit and no-auto-spend.
</success_criteria>

<output>
After completion, create `.planning/phases/03-subgraph-indexing/03-04-SUMMARY.md` recording: the resolved mainnet deploy slug, the realized entity total vs 300k (and whether the overage checkpoint fired), the leg-a fresh transactions_count, the per-stratum leg-b statuses, observed safe_block_depth, and the parity/liveness results.
Commits are LOCAL only — NEVER push (the user opens PRs to upstream per CLAUDE.md).
</output>

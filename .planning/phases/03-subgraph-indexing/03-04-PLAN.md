---
phase: 03-subgraph-indexing
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - probes/index01_live.py
  - indexing/completeness_proof.md
  - indexing/ordering_verification.md
  - indexing/parity_report.md
  - .planning/scout/2026-05-29/rollback_observation.md
  - subgraphs/iagentrequester/abis/IAgentRequester.json
  - tests/fixtures/getrequest_response_real.json
autonomous: false
requirements: [INDEX-01]
user_setup:
  - service: ormi-0xgraph
    why: "Subgraph host (DATA-SOURCE-01 selected; Ormi free Developer provisional). Free tier needs an account + deploy key; the $75/mo Production crossing is a FORCED, user-confirmed event gated on projected-overage (no auto-spend)."
    env_vars:
      - name: ORMI_API_KEY
        source: "Ormi 0xGraph dashboard → API key / deploy key (subgraph.somnia.network)"
    dashboard_config:
      - task: "Create the free Developer account; disable free-tier auto-upgrade to Production at signup (Pitfall 5 — prevents a silent paid crossing)"
        location: "Ormi 0xGraph dashboard"
      - task: "Resolve the Somnia-MAINNET deploy slug + endpoint (deploy-probe: somnia vs somnia-mainnet; api.subgraph.somnia.network/deploy) — vendor docs only confirm testnet"
        location: "Ormi 0xGraph / Somnia subgraph docs"
must_haves:
  truths:
    - "the subgraph deploys to Somnia mainnet (slug resolved at deploy time) and backfills 283417317 → head in a DEPLOY-AND-DETACH flow: deploy+start, then a resume-signal BACKFILL_SYNCED only once `_meta.block.number` reaches head (hours-to-days, possibly multi-session — Ormi managed-sync wall-clock is a DEPLOY-PROBE); backfill_wait breaks-and-surfaces PRIMARILY on _meta.hasIndexingErrors == true (the true-wedge signal), and treats the no-progress arm (N consecutive polls) as ADVISORY (a 36.3M-block managed sync can legitimately pause during compaction)"
    - "the three-leg completeness gate runs ONLY against the FULLY-synced store (head reached): fresh transactions_count ≥ 234999 (leg a), leg b = the RequestCreated(0xb623)-vs-RequestFinalized(0x65db) COUNT-balance ADVISORY+DIRECTIONAL structural floor (NEVER auto-halts; surfaces an advisory flag for HUMAN REVIEW only when RequestCreated is materially BELOW RequestFinalized; the RequestCreated COUNT ÷ the indexer's OWN id-paginated distinct-txHash count is LOGGED descriptively, never a gate, may exceed 1.0), and cursor-contiguity over the host's PROCESSED ranges (leg c)"
    - "(block_number, log_index) tuple-for-tuple ordering matches direct-RPC on a real multi-log block sample that PROVABLY includes ≥1 block with ≥2 proxy logs; block_ts_utc matches eth_getBlockByNumber to whole-second granularity"
    - "per-block-receipt parity over a stratified ≥300-window sample is within 0.5% of direct-RPC with zero indexer=0 ∧ RPC>0 windows; when receipts degrade to capped eth_getLogs the parity report pins the MINIMUM surviving completeness claim (leg a + leg c only); a few-hour liveness window (run AFTER sync-complete) shows head-lag <60 for ≥99% of polls"
    - "safe_block_depth_observed_max is recorded over a ≥1h rollback observation run AFTER sync-complete (never concurrent with an in-progress backfill)"
    - "the projected-overage entity-count check re-projects at EACH defined chunk with a conservative multiplier DERIVED FROM THE REALIZED RAMP (multiplier ≥ observed chunk-K density ÷ observed chunk-0 density, measured live — not a guessed constant), with a SMALL-DENOMINATOR GUARD: if the chunk-0 (deploy-era, sparsest) entity count is below a stated floor, fall back to an absolute-density projection instead of dividing by a near-zero denominator; if ANY chunk's re-projection crosses 300k the run PAUSES and surfaces entity-count evidence for the $75/mo sign-off (no auto-spend)"
    - "the aggregate Σ executionCost is filled off-chain via batched getRequest (selector 0xc58343ef) at the finalized block (Arch B); sum_execution_cost is written as pl.Utf8 (decimal string — uint256-derived wei sum, identical to the uint256→Utf8 id rule), NEVER pl.Decimal(38,0); the REAL return is re-frozen into a SEPARATE file tests/fixtures/getrequest_response_real.json (NOT overwriting the synthetic getrequest_response.json), DISPLAYS the decoded per-member executionCost, RECOMPUTES the sum via the decode path, and bound-checks NON-STRICT 0 <= sum <= subSize × perAgentBudget (a Failed/TimedOut finalized request with Σ=0 is legitimate — RequestFinalized fires for Success/Failed/TimedOut)"
  artifacts:
    - path: "indexing/completeness_proof.md"
      provides: "SC#6 three-leg gate against the FULLY-synced real store (leg a/b/c; leg-b = RequestCreated(0xb623)-vs-RequestFinalized(0x65db) COUNT-balance ADVISORY+DIRECTIONAL structural floor (never auto-halts) + a descriptive RequestCreated-count ÷ id-paginated-distinct-tx log; leg-c = cursor-contiguity over processed ranges)"
      contains: "234999"
    - path: "indexing/ordering_verification.md"
      provides: "SC#7 (block_number, log_index) tuple-for-tuple on a sample that includes ≥1 block with ≥2 proxy logs + block_ts_utc whole-second match"
    - path: "indexing/parity_report.md"
      provides: "SC#2 per-block-receipt parity over stratified ≥300 windows + the receipts-degraded MINIMUM completeness floor (leg a + leg c) + the liveness window readout; cites the data_sourcing_matrix.yaml key"
    - path: ".planning/scout/2026-05-29/rollback_observation.md"
      provides: "KPD-09-empirical safe_block_depth_observed_max over ≥1h, run AFTER sync-complete"
      contains: "safe_block_depth_observed_max"
    - path: "tests/fixtures/getrequest_response_real.json"
      provides: "the REAL recorded getRequest return (SEPARATE from the synthetic fixture; decoded per-member costs displayed, sum recomputed via the decode path, bound-checked NON-STRICT 0 <= sum <= subSize × perAgentBudget — a zero-cost Failed/TimedOut finalized request is legitimate)"
    - path: "probes/index01_live.py"
      provides: "the __main__-only live orchestrator: deploy+detach backfill (with hasIndexingErrors-primary stall-detection) + getRequest fill (sum_execution_cost as Utf8; real fixture re-frozen to getrequest_response_real.json, non-strict bound) + the 3 gates + liveness/rollback + per-chunk realized-ramp conservative projected-overage (small-denominator guarded), calling the Plan 03-02 logic functions"
  key_links:
    - from: "probes/index01_live.py"
      to: "indexing/completeness.py + ordering.py + parity.py + liveness.py"
      via: "the live probe imports the CI-tested logic; never re-implements it"
      pattern: "from indexing"
    - from: "probes/index01_live.py"
      to: "subgraph GraphQL paginated Request scan + 300k cap"
      via: "per-chunk realized-ramp conservative re-projection (small-denominator guarded) → pause + surface (no auto-spend)"
      pattern: "300000|300_000"
---

<objective>
Wave 2 (LIVE): deploy the subgraph to Somnia mainnet, backfill 283417317 → head as a DEPLOY-AND-DETACH flow (start the managed sync, detach, return only when `_meta.block.number` reaches head — Ormi managed-sync wall-clock for 36.3M blocks is UNQUANTIFIED and is a DEPLOY-PROBE; DATA_SOURCING only quantifies the ~42-day direct-RPC FALLBACK), fill the aggregate Σ executionCost off-chain (Arch B, selector 0xc58343ef at the finalized block), and run the three correctness gates + liveness + rollback against the FULLY-synced real store — emitting `indexing/completeness_proof.md`, `indexing/ordering_verification.md`, `indexing/parity_report.md`, and `.planning/scout/2026-05-29/rollback_observation.md`. All live work runs through `probes/index01_live.py` (`__main__`-only, per the repo convention), which IMPORTS the CI-tested logic from Plan 03-02 — it never re-implements the gate math. Includes the per-chunk realized-ramp conservative projected-overage check (small-denominator guarded) and the $75/mo no-auto-spend payment checkpoint (decision 2 + Phase-2 note 3 — the user instructed this be on the plan).

Purpose: this is the binding INDEX-01 phase gate. The CI logic (03-01/03-02) is proven; this plan produces the live artifacts `/gsd:verify-work` checks. The deploy slug, Ormi managed-sync wall-clock, and free-tier entity-count read are DEPLOY-PROBES (03-RESEARCH Open Q1/Q2, MEDIUM doc-gaps) — resolved live, not hard-coded. The gates run ONLY after sync-complete: a mid-backfill head-lag reads a huge gap and would false-trigger liveness/rollback escalation. Gate-review #3 fixes baked in: BLOCKER-1 two-fixture split (real return → getrequest_response_real.json, non-strict 0≤Σ bound — a Failed/TimedOut finalized request has Σ=0 legitimately), MAJOR-1 leg-b ADVISORY+DIRECTIONAL never-auto-halt (reconciled with ROADMAP SC#6(b)), MINOR-2 overage small-denominator guard, MINOR-3 stall-detection keyed PRIMARILY on hasIndexingErrors (no-progress arm advisory), MINOR-4 whitespace-tolerant negative guard + positive id_gt assertion. Prior-gate fixes preserved: leg-b distinct-tx via LOSSLESS id-pagination, sum_execution_cost as Utf8 not Decimal(38,0), Architecture B, ~165k entity model.
Output: `probes/index01_live.py` + the four live artifacts + the SEPARATE real `getrequest_response_real.json` fixture (evidence-bearing: decoded members displayed, sum recomputed, bound-checked non-strict).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-subgraph-indexing/03-RESEARCH.md
@.planning/phases/03-subgraph-indexing/03-CONTEXT.md
@.planning/phases/03-subgraph-indexing/03-VALIDATION.md
@.planning/phases/02-topic-implementation-provenance/02-FORWARD-NOTES.md
@research/DATA_SOURCING.md
@probes/somnia_rpc.py
@probes/blockscout.py
@indexing/decode.py
@indexing/completeness.py
@indexing/ordering.py
@indexing/parity.py
@indexing/liveness.py

<interfaces>
<!-- The CI-tested logic this live probe orchestrates. Import — never re-implement. -->

From indexing/decode.py:    decode_get_request(raw_hex), sum_execution_cost(request_tuple), per_member_execution_costs(request_tuple), GETREQUEST_SELECTOR="0xc58343ef", REQUEST_TUPLE_TYPE
From indexing/completeness.py: structural_ratio_status(request_created_count, request_finalized_count, indexer_distinct_tx) [ADVISORY: returns "ok"|"advisory_review", NEVER "halt"], descriptive_count_ratio(request_created_count, indexer_distinct_tx) [divide-by-zero guarded], count_balance(request_created_count, request_finalized_count), tx_anchor_ok(fresh_tx_count), cursor_contiguity(processed_ranges, deploy, head), is_proxy_row(log_address)
From indexing/ordering.py:  arrival_sort_key(row), verify_ordering(indexer_tuples, rpc_tuples)
From indexing/parity.py:    parity_reference(receipts, get_logs_fn, proxy, blk)
From indexing/liveness.py:  should_escalate(gaps), safe_block_depth(observed_max, margin=1)
From probes/somnia_rpc.py:  head_block(), get_block_receipts(blk)→list|None, get_logs(addr,from,to), get_request(request_id, block), get_block(blk), DEPLOYMENT_BLOCK=283417317, PROXY
From probes/blockscout.py:  tx_count(addr)→int  (live re-query, rate-limited — budget backoff)

Deploy + DEPLOY-AND-DETACH backfill (03-RESEARCH §"Standard Stack" + Open Q1 — DEPLOY-PROBE):
  npm install -g @graphprotocol/graph-cli
  graph codegen && graph build   (in subgraphs/iagentrequester/)
  graph deploy iagentrequester --node https://api.subgraph.somnia.network/deploy \
    --ipfs https://api.subgraph.somnia.network/ipfs --deploy-key $ORMI_API_KEY
  network slug: try "somnia"; if rejected try "somnia-mainnet"; if mainnet free-tier deploy unavailable → checkpoint (decision 2 sign-off) or GetBlock sub-plan re-author.
  Managed-sync wall-clock for 36.3M blocks (283417317 → head): UNQUANTIFIED — a DEPLOY-PROBE (the ~42-day
  figure in DATA_SOURCING is the direct-RPC FALLBACK, not Ormi managed-sync). Record the OBSERVED sync rate
  after the first chunk; DO NOT assume a duration. Backfill detaches; resume only when head is reached.
  indexed head + health: GraphQL { _meta { block { number hash timestamp } hasIndexingErrors } }

  STALL-DETECTION (gate-review #3 MINOR-3): backfill_wait MUST break-and-surface PRIMARILY on
    (i) `_meta.hasIndexingErrors == true` — the TRUE-WEDGE signal → print STALL_DETECTED + the last indexed
        head, do NOT treat as BACKFILL_SYNCED.
    The no-progress arm (ii) `_meta.block.number` shows ZERO advance over N consecutive polls is ADVISORY,
    not a hard stall: a 36.3M-block managed sync can LEGITIMATELY pause during compaction. Default N = 12
    (≈60 min at the 5-min cadence — WIDENED from 6 with a note that N is a deploy-probe to tune against the
    OBSERVED first-chunk rate) and print a STALL_ADVISORY (not STALL_DETECTED) with the observed rate, then
    KEEP POLLING. Only hasIndexingErrors==true is a hard STALL_DETECTED. Only a genuine head-reached state
    prints BACKFILL_SYNCED.

  CHUNK DEFINITION (for the projected-overage probe): a "chunk" = the first N indexed blocks reported by
  `_meta.block.number` advancing past DEPLOYMENT_BLOCK. N = 1_000_000 (first 1M blocks of the indexed range).
  The probe fires on the FIRST such chunk and re-fires per subsequent 1M-block chunk until sync-complete.

Completeness gate parameters (03-CONTEXT decision 4 + ROADMAP SC#6(b) — STATE THESE in completeness_proof.md; run ONLY post-sync):
  leg (a): fresh transactions_count ≥ 234999 (monotonicity floor; live re-query at indexed head, NOT scaled).
  leg (b) — ADVISORY + DIRECTIONAL structural floor, NEVER an auto-halt (gate-review #3 MAJOR-1; ROADMAP SC#6(b)):
           structural_ratio_status(request_created_count, request_finalized_count, indexer_distinct_tx)
           returns "ok" or "advisory_review", NEVER "halt". It surfaces an advisory flag for HUMAN REVIEW
           only when the RequestCreated(0xb623) COUNT is materially BELOW its RequestFinalized(0x65db)
           partner — rc < rf − TOL·max(rf,1) (the missing-RequestCreated direction). The rc > rf in-flight
           finalization-lag tail is EXPECTED (finalization lags creation over [deploy, head]) and is logged,
           never flagged. The RequestCreated COUNT ÷ the indexer's OWN distinct-txHash count is a DESCRIPTIVE
           LOG per stratum (deploy/mid/head) — descriptive_count_ratio(...) — NOT a gate; it is a COUNT ratio
           (0xb623 fires 1–5×/tx) that CAN exceed 1.0, so the retired Wilson [0.628,0.790]/0.716 proportion
           band would FALSE-flag a healthy indexer. leg-b stays NON-BLOCKING (legs a+c bind); the exact
           RequestCreated lifetime count is an INDEX-01 OUTPUT (1:1:1 disclaimed in-sample-only). (ROADMAP
           SC#6(b) correction 2026-05-29 / topic0_map_v1.json; basis = addendum 66/20/17/12 composition +
           83/83/83 in-sample-only balance.) EVALUATE the descriptive ratio against the indexer's OWN
           distinct-tx count, NEVER the external 234999 (circularity). indexer_distinct_tx is computed by
           LOSSLESS id-PAGINATION (see below). request_created_count and request_finalized_count are the
           indexed COUNTs of 0xb623 / 0x65db. The advisory routes to the human checkpoint in Task 2 — no
           auto-halt, no auto-spend.
  leg (c): cursor_contiguity(processed_ranges, DEPLOYMENT_BLOCK, head) over the host's PROCESSED-block ranges
           (`_meta` processed-range = host advanced over [283417317, head] with zero skipped ranges) — NOT
           contiguity of event-bearing block_numbers (~0.0005 RequestCreated/block → most blocks carry NO proxy
           log; naive event-block interval-fill floods false gaps). Completeness-bearing ONLY conjoined with leg (a).
  B3 structural argument: zero foreign-address rows (every indexed row's log_address == proxy by the manifest pin)
           is a structural completeness input — state it in completeness_proof.md (INFO-6: the zero-foreign-row argument).

indexer_distinct_tx via LOSSLESS id-PAGINATION (NO GraphQL COUNT(DISTINCT) exists — M1 fix gate-review #2):
  paginate `Request(first:1000, where:{id_gt:$last_id}, orderBy:id, orderDirection:asc)`, advancing `$last_id`
  to the LAST entity's `id` each page, collect every `txHash` into a Python set → `indexer_distinct_tx = len(set)`.
  ~165k entities / 1000 ≈ ~165 paginated calls. This is leg-b's denominator (descriptive) — NOT the external
  Blockscout 234999. CRITICAL: do NOT page by `blockNumber_gt` advancing by the last page's max blockNumber —
  multiple Request entities share a blockNumber (1–5×/tx at ~100ms blocks), so a strictly-greater blockNumber
  cursor SILENTLY DROPS the same-block Requests that fell on the next page → set-dedup cannot recover
  never-fetched rows → under-counts distinct-tx. `id` is unique per entity ⇒ no same-key boundary ambiguity ⇒
  lossless full scan. (blockNumber-windowing is reserved ONLY for the bounded-range overage COUNT below.)

Parity (SC#2): per-block-receipt scan over a stratified ≥300-window sample (deploy/mid/head thirds);
  on get_block_receipts→None, fall back to eth_getLogs + flag weakened assurance (parity.parity_reference).
  RECEIPTS-DEGRADED FLOOR (M6): if any window degrades, parity_report.md MUST pin the MINIMUM surviving
  completeness claim explicitly — leg (a) tx-anchor + leg (c) cursor-contiguity ONLY (parity bounds AGREEMENT,
  not completeness) — and that this weaker assurance is a retroactive free-vs-paid input.
Liveness (B2): poll _meta vs eth_blockNumber every 5min; escalate gap>60 ×3 consecutive; few-hour representative
  window — run AFTER sync-complete (a mid-backfill head-lag reads a huge gap and false-triggers escalation).
safe_block_depth (KPD-09-empirical): ≥1h head-watch AFTER sync-complete; observed_max=0 → 1 (PBFT provisional, somnia_finality_semantics.md).
Projected-overage (decision 2 — CONSERVATIVE, REALIZED-RAMP multiplier, per-chunk, SMALL-DENOMINATOR GUARDED; M4 + gate-review #3 MINOR-2):
  after EACH 1M-block chunk, page the Request entity COUNT over the chunk's block range
  (`Request(first:1000, where:{blockNumber_gte:$lo, blockNumber_lt:$hi}, orderBy:blockNumber)` — a bounded
  block-range count, distinct from the lossless id-scan above), then re-project to the full 36.3M span with a
  CONSERVATIVE multiplier DERIVED FROM THE REALIZED RAMP, not a guessed constant: a young chain ramps
  (density is monotone-increasing), so the multiplier = (observed density of the LATEST chunk K) ÷ (observed
  density of chunk 0), measured LIVE. SMALL-DENOMINATOR GUARD (MINOR-2): chunk_0 is the deploy-era SPARSEST
  chunk, so its density (the denominator) can be near-zero and the ratio explodes / ZeroDivisionErrors. If the
  chunk_0 ENTITY COUNT is below a stated floor (CHUNK0_MIN_ENTITIES = 50 — state it inline), do NOT divide by
  chunk_0 density; instead fall back to an ABSOLUTE-density projection: project using the LATEST chunk-K density
  directly across the full span (chunk-K density × total_chunks), which is conservative (the latest chunk is the
  densest so far on a ramping chain). Otherwise multiplier = max(1.0, density(chunk_K) / density(chunk_0)). If ANY
  chunk's re-projection crosses 300000 → PAUSE + surface evidence (checkpoint). NOT a one-shot first-chunk linear
  extrapolation, and NOT a guessed multiplier; conservatism toward PAUSING is preserved.

Σ executionCost DTYPE (M-2 gate-review #2 — schemas/event_schema_v1.md DTYPE SCOPE RULE):
  sum_execution_cost is a uint256-DERIVED wei SUM (Σ of responses[].executionCost across members across requests).
  A real >38-digit executionCost → Decimal(38,0) OverflowError at PANEL-01 materialization AFTER the backfill
  spend. PIN sum_execution_cost to pl.Utf8 (decimal string) in the fill/emit contract — identical to the
  uint256→Utf8 id rule — NOT pl.Decimal(38,0). Any Decimal(38,0) coercion is DEFERRED to a PANEL-01 concern
  with an explicit ≤38-digit guard + quarantine path.

REAL-FIXTURE RE-FREEZE — SEPARATE FILE, NON-STRICT BOUND (gate-review #3 BLOCKER-1):
  capture ONE real raw getRequest return and re-freeze it into tests/fixtures/getrequest_response_real.json —
  a SEPARATE file; do NOT overwrite the synthetic tests/fixtures/getrequest_response.json (that synthetic
  fixture is 03-01's structural mis-slice tripwire and is NEVER re-frozen). For the real fixture: (i) DISPLAY
  (print) the decoded PER-MEMBER responses[].executionCost via per_member_execution_costs(decode_get_request(raw));
  (ii) set expected_sum_execution_cost by RECOMPUTING it through sum_execution_cost(decode_get_request(raw)) (a
  DECIMAL STRING) — never hand-entered; (iii) record per_agent_budget_native + sub_size; (iv) bound-check
  NON-STRICT `0 <= int(expected_sum_execution_cost) <= sub_size * int(per_agent_budget_native)` — a real
  Failed/TimedOut finalized request legitimately has Σ == 0 (RequestFinalized fires for Success/Failed/TimedOut;
  a no-response TimedOut → Σ=0), so the lower bound is `0 <=`, NEVER `0 <`. The 03-01 synthetic decode test
  (strict structural mis-slice guard) MUST NOT run against this real fixture — keep them in separate files so a
  real zero-cost finalized request does NOT false-fail CI after the backfill spend.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live orchestrator probe (__main__-only) — deploy poll, deploy-and-detach backfill (hasIndexingErrors-primary stall-detection), Arch-B fill (Utf8 sum; real fixture → separate file, non-strict bound), 3 gates (leg-b ADVISORY+DIRECTIONAL + id-paginated distinct-tx), liveness/rollback, per-chunk realized-ramp projected-overage (small-denominator guarded)</name>
  <read_first>
    - probes/somnia_rpc.py + probes/blockscout.py (reusable RPC/Blockscout helpers — reuse, never duplicate)
    - indexing/completeness.py, ordering.py, parity.py, liveness.py, decode.py (the CI-tested logic to import — note structural_ratio_status is ADVISORY: returns "ok"|"advisory_review", NEVER "halt"; descriptive_count_ratio is log-only + divide-by-zero guarded; cursor_contiguity, NOT contiguity_gaps; per_member_execution_costs exists for the fixture display)
    - .planning/phases/03-subgraph-indexing/03-RESEARCH.md §"Code Examples" (the GraphQL _meta query, per-block-receipt parity, off-chain getRequest, tx-anchor snippets) + §"Common Pitfalls 1-6"
    - .planning/phases/03-subgraph-indexing/03-VALIDATION.md §"Manual-Only Verifications" (what each live step must emit)
    - .planning/phases/03-subgraph-indexing/03-CONTEXT.md decisions 2 + 4
    - .planning/ROADMAP.md §"Phase 3" SC#6(b) (leg-b non-blocking; never auto-halt)
    - references/interfaces/IAgentRequester.sol (ResponseStatus Success=2/Failed=3/TimedOut=4; RequestFinalized fires for all terminal statuses; a no-response TimedOut → Σ=0 legitimately — the non-strict bound)
  </read_first>
  <action>
    Create `probes/index01_live.py` — stdlib + the repo's own modules only; ALL network/IO inside the `if __name__ == "__main__":` block (per the `probes/*.py` convention; NEVER imported by CI). NOTE: `probes/somnia_rpc.py` / `probes/blockscout.py` are imported at module scope and they touch the network — so the network-confinement check below is "no NETWORK LIBRARY imported at module scope" (urllib/requests/httpx/aiohttp), NOT "no network at import". Structure it as labelled, independently-runnable functions called from `__main__` with CLI subcommands (e.g. `deploy-check`, `backfill-start`, `backfill-wait`, `fill-cost`, `gate-completeness`, `gate-ordering`, `gate-parity`, `liveness`, `rollback`, `overage`):
    - `graphql_meta()` — POST the `{ _meta { block { number hash timestamp } hasIndexingErrors } }` query to the Ormi endpoint (Authorization: $ORMI_API_KEY) → indexer head + hasIndexingErrors.
    - `deploy_check()` — print the resolved network slug attempt order (somnia → somnia-mainnet) + deploy endpoint; print `_meta` once deployed. On slug/endpoint rejection, print a clear DEPLOY-PROBE-FAILED message pointing to the checkpoint.
    - `backfill_start()` — kick the managed sync (deploy already done) and print a DETACH notice: "backfill running; Ormi managed-sync wall-clock for 36.3M blocks is a DEPLOY-PROBE (unverified) — return via `backfill-wait`". Do NOT block.
    - `backfill_wait()` — poll `graphql_meta()` until `_meta.block.number` reaches `head_block()` (head re-queried each poll); record the OBSERVED indexed-blocks/elapsed RATE after the first 1M-block chunk; print `BACKFILL_SYNCED` ONLY when head is reached (hours-to-days; may be multiple invocations across sessions — print progress + the projected remaining time from the observed rate each poll). STALL-DETECTION (gate-review #3 MINOR-3): break-and-surface with a HARD `STALL_DETECTED` message (NOT BACKFILL_SYNCED) ONLY if `_meta.hasIndexingErrors == true` (the true-wedge signal) — print the last indexed head + the error so a wedged Ormi sync surfaces. The no-progress arm — `_meta.block.number` shows ZERO advance over N=12 consecutive polls (≈60 min at the 5-min cadence; define N as a constant inline and note it is a deploy-probe to TUNE AGAINST THE OBSERVED FIRST-CHUNK RATE) — is ADVISORY: print a `STALL_ADVISORY` (a 36.3M-block managed sync can legitimately pause during compaction) with the observed rate and KEEP POLLING, do NOT hard-stop. The three gates + liveness + rollback MUST NOT run until this prints `BACKFILL_SYNCED`.
    - `fill_cost(request_ids, finalized_blocks)` — for each finalized request, `get_request(rid, finalized_block)` (selector 0xc58343ef at the FINALIZED block — Pitfall 3), `decode_get_request` + `sum_execution_cost`; write `request_id, sum_execution_cost` where sum_execution_cost is a `pl.Utf8` DECIMAL STRING (M-2 gate-review #2 — it is a uint256-derived wei sum, identical to the uint256→Utf8 id rule; do NOT coerce to `pl.Decimal(38,0)` here — a real >38-digit sum would OverflowError at PANEL-01 after the backfill spend; Decimal(38,0) coercion with a ≤38-digit guard + quarantine path is a DEFERRED PANEL-01 concern). Per-member detail summed + discarded (decision 1). ALSO capture ONE real raw return and re-freeze it into the SEPARATE file `tests/fixtures/getrequest_response_real.json` (gate-review #3 BLOCKER-1 — do NOT overwrite the synthetic `tests/fixtures/getrequest_response.json`, which is 03-01's structural mis-slice tripwire and is NEVER re-frozen) EVIDENCE-BEARING: (i) DISPLAY (print) the decoded PER-MEMBER `responses[].executionCost` list via `per_member_execution_costs(decode_get_request(raw))`; (ii) set `expected_sum_execution_cost` by RECOMPUTING it through `sum_execution_cost(decode_get_request(raw))` (a DECIMAL STRING) — never hand-entered; (iii) record `per_agent_budget_native` and `sub_size`; (iv) bound-check NON-STRICT `0 <= int(expected_sum_execution_cost) <= sub_size * int(per_agent_budget_native)` — a real Failed/TimedOut finalized request legitimately has Σ == 0 (RequestFinalized fires for Success/Failed/TimedOut; a no-response TimedOut → Σ=0), so the lower bound is `0 <=`, NEVER strict `0 <`. Use field NAMES consistent with the synthetic fixture (request_id, block, raw, expected_sum_execution_cost, per_member_execution_costs, per_agent_budget_native, sub_size) but write to the SEPARATE real file. The 03-01 synthetic decode test does NOT read this real file.
    - `gate_completeness()` — RUN ONLY POST-SYNC. leg(a) `blockscout.tx_count(PROXY)` ≥ 234999; leg(b) ADVISORY+DIRECTIONAL `structural_ratio_status(rc_count, rf_count, indexer_distinct_tx)` (returns "ok"|"advisory_review", NEVER "halt"; surfaces an advisory flag for HUMAN REVIEW only when the RequestCreated COUNT is materially BELOW its RequestFinalized partner — rc < rf − TOL·max(rf,1); the rc>rf in-flight finalization-lag tail is logged, never flagged; proportion/Wilson is RETIRED), where `rc_count` = the indexed RequestCreated(0xb623) COUNT, `rf_count` = the indexed RequestFinalized(0x65db) COUNT, and `indexer_distinct_tx` = the indexer's OWN distinct-txHash count computed by LOSSLESS id-PAGINATION over `Request(first:1000, where:{id_gt:$last_id}, orderBy:id, orderDirection:asc)` advancing `$last_id` to the last entity's `id` and deduping txHash in a Python set (~165 calls) — NOT `blockscout.tx_count`/the external 234999 (using the external anchor as the leg-b denominator makes leg-b circular with leg-a; see the anti-circularity docstring in completeness.py) and NOT a `blockNumber_gt` cursor (which silently drops same-block Requests → under-counts). ALSO compute and LOG (per stratum deploy/mid/head) the descriptive `descriptive_count_ratio(rc_count, indexer_distinct_tx)` (divide-by-zero guarded) AND the signed `count_balance(rc_count, rf_count)` — record them as INDEX-01 OUTPUTS, explicitly NOT gates (the ratio may exceed 1.0). leg(c) `cursor_contiguity(processed_ranges, DEPLOYMENT_BLOCK, head)` over the host's `_meta` PROCESSED ranges (NOT event-block interval-fill). Emit `indexing/completeness_proof.md` stating: leg-b is the RequestCreated(0xb623)-vs-RequestFinalized(0x65db) COUNT-balance ADVISORY+DIRECTIONAL structural floor that NEVER auto-halts (cite SC#6(b)/topic0_map_v1.json + the addendum in-sample-only basis), surfacing an advisory flag for human review only in the missing-RequestCreated direction; the per-stratum descriptive RequestCreated-count ÷ id-paginated-distinct-tx ratio + the signed count_balance (NON-gating, ratio may exceed 1.0, the exact lifetime ratio is an INDEX-01 output); that the Wilson proportion band is RETIRED (with the 0.716 kept only as a "NOT a gate" descriptive reference); leg-c is cursor-contiguity over PROCESSED ranges; and the B3 zero-foreign-row structural argument (INFO-6). If the leg-b verdict is "advisory_review", surface it to the Task-2 human checkpoint (NO auto-halt, NO auto-spend).
    - `gate_ordering()` — RUN ONLY POST-SYNC. On the SC#2 sampled multi-log blocks, ASSERT the sample provably selects ≥1 (ideally several) block(s) with ≥2 proxy logs (a lone RequestCreated block has 1 log → tuple-for-tuple ordering is vacuously trivial; if no ≥2-log block is in the stratified sample, EXPAND the sample until one is found or record why the full range has none). Build indexer (block_number, log_index) tuples + the RPC tuples (from receipts/getLogs), `verify_ordering(...)` tuple-for-tuple, and compare `block_ts_utc` vs `get_block(blk)` timestamp at WHOLE-SECOND granularity (subgraph blockTimestamp is a whole-second BigInt; "exact" = whole-second ↔ whole-second). Emit `indexing/ordering_verification.md` with a one-line block_ts_utc unit-reconciliation note.
    - `gate_parity()` — RUN ONLY POST-SYNC. stratified ≥300 windows (deploy/mid/head thirds); per window `parity_reference(get_block_receipts(blk), get_logs, PROXY, blk)`; assert within 0.5% + zero indexer=0 ∧ RPC>0; if ANY window returns None (degraded to capped eth_getLogs) record it AND pin the MINIMUM surviving completeness floor: leg (a) tx-anchor + leg (c) cursor-contiguity ONLY (parity bounds AGREEMENT not COMPLETENESS), flagged as a retroactive free-vs-paid input. Cite the `data_sourcing_matrix.yaml` key for the mechanism. Append the few-hour liveness readout (head-lag <60 for ≥99% of polls, `should_escalate`) → `indexing/parity_report.md`.
    - `rollback()` — RUN ONLY POST-SYNC (a mid-backfill head-lag false-triggers). ≥1h head-watch; record `safe_block_depth_observed_max` (0 if no rollback → `safe_block_depth(0)==1`) → `.planning/scout/2026-05-29/rollback_observation.md` with provenance (source_url + utc_fetch_ts). After writing, re-pin it in `.planning/scout/2026-05-29/PROVENANCE.sha256` (the set-equality test in tests/test_scout_archive.py is `.md`-scoped — a new `.md` MUST be re-pinned or that test fails by design).
    - `overage()` — CONSERVATIVE, REALIZED-RAMP multiplier, PER-CHUNK, SMALL-DENOMINATOR GUARDED (M4 + gate-review #3 MINOR-2). After EACH 1M-block chunk (chunk = first/next 1M indexed blocks past DEPLOYMENT_BLOCK per `_meta.block.number`), page the Request entity COUNT over the chunk's block range (`Request(first:1000, where:{blockNumber_gte:$lo, blockNumber_lt:$hi}, orderBy:blockNumber)` — a bounded block-RANGE count, distinct from the lossless id-scan in gate_completeness), compute the chunk's entity DENSITY (entities ÷ 1M blocks), and re-project to the 36.3M span. SMALL-DENOMINATOR GUARD: chunk_0 (deploy-era) is the SPARSEST chunk, so its density (the multiplier denominator) can be near-zero. Define `CHUNK0_MIN_ENTITIES = 50` (state inline) — if the chunk_0 entity count is below this floor, do NOT divide by chunk_0 density; instead fall back to an ABSOLUTE-density projection: project using the LATEST chunk-K density directly across the full span (chunk-K density × total_chunks), which is conservative on a ramping chain (the latest chunk is densest so far). Otherwise `multiplier = max(1.0, observed_density(chunk_K) / observed_density(chunk_0))` measured LIVE (a young chain ramps → density is monotone-increasing → the deploy-era chunk-0 under-estimates; the multiplier captures the realized chunk-K/chunk-0 growth, NOT a guessed constant — the per-chunk re-fire self-corrects, this removes the hand-wave). If ANY chunk's re-projection crosses 300000 → PRINT the entity-count evidence + the realized multiplier (or the absolute-density fallback note) and EXIT with a non-zero `OVERAGE_PROJECTED` signal (the human checkpoint in Task 2 consumes this; NO auto-spend, NO auto-provision). Document the realized-multiplier derivation + the small-denominator guard inline.
    Each emitter stamps `source_url + utc_fetch_ts` provenance. Keep the gate MATH in the imported `indexing/*` functions — this probe only orchestrates IO + formats the artifacts.
  </action>
  <verify>
    <automated>uv run python -c "import ast; ast.parse(open('probes/index01_live.py').read())" && grep -q "from indexing" probes/index01_live.py && grep -q "cursor_contiguity" probes/index01_live.py && grep -q "0xc58343ef\|GETREQUEST_SELECTOR\|get_request" probes/index01_live.py && grep -q "__main__" probes/index01_live.py && grep -qi "BACKFILL_SYNCED" probes/index01_live.py && grep -q "id_gt" probes/index01_live.py && grep -qi "STALL_DETECTED\|hasIndexingErrors" probes/index01_live.py && grep -q "request_finalized_count\|rf_count" probes/index01_live.py && grep -qi "Utf8\|decimal string\|str(" probes/index01_live.py && grep -q "getrequest_response_real" probes/index01_live.py && grep -qE "0 <= int|0<=int|<= sub_size" probes/index01_live.py && uv run python -c "import ast,sys; t=ast.parse(open('probes/index01_live.py').read()); sys.exit(1 if any(isinstance(n,(ast.Import,ast.ImportFrom)) and n.col_offset==0 and ((isinstance(n,ast.Import) and any(a.name.split('.')[0] in ('urllib','requests','httpx','aiohttp') for a in n.names)) or (isinstance(n,ast.ImportFrom) and (n.module or '').split('.')[0] in ('urllib','requests','httpx','aiohttp'))) for n in ast.walk(t)) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('probes/index01_live.py').read())"` exits 0 (valid Python)
    - `grep -q "from indexing" probes/index01_live.py` (imports the CI-tested logic, not re-implemented)
    - `grep -q "cursor_contiguity" probes/index01_live.py` (leg-c = processed-range contiguity, NOT event-block interval-fill)
    - `grep -qi "BACKFILL_SYNCED" probes/index01_live.py` (deploy-and-detach: gates wait for head)
    - `grep -qi "STALL_DETECTED\|hasIndexingErrors" probes/index01_live.py` (MINOR-3: backfill_wait HARD stall keyed on hasIndexingErrors, not infinite poll)
    - `grep -qi "STALL_ADVISORY\|advisory" probes/index01_live.py` (MINOR-3: the no-progress arm is ADVISORY, not a hard stall)
    - `grep -q "id_gt" probes/index01_live.py` (M-1: leg-b distinct-tx via LOSSLESS id-pagination; POSITIVE assertion the distinct-tx scan uses id_gt)
    - `! grep -qE "blockNumber_gt:" probes/index01_live.py` (MINOR-4 v2: rejects ONLY the lossy distinct-tx `blockNumber_gt:` cursor operator — `blockNumber_gte:`/`blockNumber_lt:` range-windowing and the `id_gt:` distinct-tx scan all pass cleanly. The prior `blockNumber_gt[^=]*(…: *\$…)` form false-matched the plan's own legitimate `blockNumber_gte:$lo` overage query because `blockNumber_gt` is a literal prefix of `blockNumber_gte`.)
    - `grep -q "request_finalized_count\|rf_count" probes/index01_live.py` (MAJOR-1: leg-b advisory floor uses the RequestFinalized count, not a proportion band)
    - `! grep -qiE "\"halt\"|'halt'|leg-b.*halt|structural-floor HALT" probes/index01_live.py` (MAJOR-1: no leg-b auto-halt anywhere)
    - `grep -qi "advisory_review\|advisory flag\|human review" probes/index01_live.py` (MAJOR-1: leg-b advisory routes to the human checkpoint)
    - `grep -qiE "Utf8|decimal string|str\\(" probes/index01_live.py` (M-2: sum_execution_cost written as a decimal string, not Decimal(38,0))
    - `! grep -q "pl.Decimal(38" probes/index01_live.py` (M-2: no Decimal(38,0) coercion of sum_execution_cost in the fill)
    - `grep -q "getrequest_response_real" probes/index01_live.py` (BLOCKER-1: real return re-frozen to a SEPARATE file)
    - `! grep -q "tests/fixtures/getrequest_response.json" probes/index01_live.py` (BLOCKER-1: the synthetic fixture is NEVER overwritten by this probe)
    - `grep -qE "0 <= int|0<=int|<= sub_size" probes/index01_live.py` (BLOCKER-1: NON-STRICT bound 0 <= sum <= subSize × perAgentBudget — a zero-cost Failed/TimedOut finalized request is legitimate)
    - `grep -qE "300000|300_000" probes/index01_live.py` (projected-overage 300k cap)
    - `grep -q "OVERAGE_PROJECTED\|no auto-spend\|no-auto-spend" probes/index01_live.py`
    - `grep -qiE "realized|density|multiplier" probes/index01_live.py` (per-chunk REALIZED-RAMP conservative re-projection, not a guessed constant)
    - `grep -qiE "CHUNK0_MIN_ENTITIES|small.denominator|absolute.density|chunk_0" probes/index01_live.py` (MINOR-2: small-denominator guard on chunk_0)
    - `grep -qE "234999|234_999" probes/index01_live.py`
    - `grep -q "if __name__" probes/index01_live.py`
    - Network confinement (AST module-scope check — fails ONLY if a network LIBRARY is imported at module scope; live calls live inside `if __name__ == "__main__":`; probes/* ARE imported at module scope and touch the network, so this proves "no network LIB imported at module scope", NOT "no network at import"): `python -c "import ast,sys; t=ast.parse(open('probes/index01_live.py').read()); sys.exit(1 if any(isinstance(n,(ast.Import,ast.ImportFrom)) and n.col_offset==0 and ((isinstance(n,ast.Import) and any(a.name.split('.')[0] in ('urllib','requests','httpx','aiohttp') for a in n.names)) or (isinstance(n,ast.ImportFrom) and (n.module or '').split('.')[0] in ('urllib','requests','httpx','aiohttp'))) for n in ast.walk(t)) else 0)"` exits 0
  </acceptance_criteria>
  <done>`probes/index01_live.py` orchestrates deploy-check, deploy-and-detach backfill (backfill-start + backfill-wait → BACKFILL_SYNCED at head, with HARD STALL_DETECTED on hasIndexingErrors and ADVISORY STALL_ADVISORY on no-progress), Arch-B getRequest fill (0xc58343ef at finalized block; sum_execution_cost written as a Utf8 decimal string, NOT Decimal(38,0); the REAL return re-frozen to the SEPARATE getrequest_response_real.json with the decoded per-member list displayed, sum recomputed, NON-STRICT 0≤Σ bound — the synthetic fixture is never overwritten), the 3 gates (run only post-sync; leg-b ADVISORY+DIRECTIONAL structural floor that NEVER auto-halts and surfaces a human-review advisory only when RequestCreated is materially below RequestFinalized, descriptive count-ratio logged via LOSSLESS id-paginated indexer_distinct_tx, leg-c uses cursor_contiguity over processed ranges), liveness + rollback (post-sync only), and per-chunk REALIZED-RAMP conservative projected-overage with the chunk_0 small-denominator guard — importing the Plan 03-02 logic, with no NETWORK LIBRARY imported at module scope (live calls under `if __name__ == "__main__":`), surfacing `OVERAGE_PROJECTED` (no auto-spend).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy + start backfill, DETACH; on BACKFILL_SYNCED run the gates; produce the four artifacts (+ per-chunk realized-ramp entity-overage / payment checkpoint)</name>
  <what-built>
    `probes/index01_live.py` (Task 1) drives the live sequence. This checkpoint is a DEPLOY-AND-DETACH flow: the human (1) deploys + starts the backfill, (2) lets the per-chunk realized-ramp projected-overage probe fire BEFORE any genuine spend risk, (3) returns on the `BACKFILL_SYNCED` resume-signal once `_meta.block.number` reaches head (hours-to-days later, possibly a multi-session wait — the Ormi managed-sync wall-clock is a DEPLOY-PROBE; a wedged sync surfaces as STALL_DETECTED on hasIndexingErrors, a benign compaction pause surfaces as STALL_ADVISORY and keeps polling), then (4) runs the three-leg gate + parity + ordering + liveness + rollback ONLY against the FULLY-synced store. The deploy slug + free-tier entity-count read are DEPLOY-PROBES (resolved here, not pre-assumed).
  </what-built>
  <how-to-verify>
    1. Ensure `ORMI_API_KEY` is set and the Ormi free Developer account exists with auto-upgrade DISABLED (Pitfall 5). Resolve the mainnet deploy slug:
       `cd subgraphs/iagentrequester && npm install -g @graphprotocol/graph-cli && graph codegen && graph build`
       `graph deploy iagentrequester --node https://api.subgraph.somnia.network/deploy --ipfs https://api.subgraph.somnia.network/ipfs --deploy-key $ORMI_API_KEY`  (try slug `somnia`, then `somnia-mainnet`).
       If mainnet free-tier deploy is UNAVAILABLE → STOP and report (decision 2 sign-off, or GetBlock sub-plan re-author).
    2. START the backfill and DETACH: `uv run python -m probes.index01_live backfill-start`. The backfill of 283417317 → head runs on Ormi (managed-sync wall-clock UNQUANTIFIED — a DEPLOY-PROBE; record the observed rate after the first 1M-block chunk). Do NOT block the session on it.
    3. PER-CHUNK overage probe (fires BEFORE genuine spend risk): after EACH 1M-block chunk run `uv run python -m probes.index01_live overage`. It re-projects the full-span entity count at every chunk with a CONSERVATIVE multiplier DERIVED FROM THE REALIZED RAMP (observed chunk-K density ÷ chunk-0 density, measured live — a young chain ramps, so the deploy-era early chunk under-estimates; if chunk_0 is below CHUNK0_MIN_ENTITIES it falls back to a conservative absolute-density projection rather than dividing by a near-zero denominator). If ANY chunk prints `OVERAGE_PROJECTED`: the projection crosses 300k → the run is PAUSED; surface the entity-count evidence + the realized multiplier to the user. The user pays $75/mo Ormi Production and CONFIRMS before any provisioning. NO auto-spend under any circumstance. If under cap, continue waiting.
    4. WAIT FOR SYNC: `uv run python -m probes.index01_live backfill-wait` — returns `BACKFILL_SYNCED` only when `_meta.block.number` reaches head (hours-to-days; may span multiple sessions — re-invoke as needed). If it instead prints `STALL_DETECTED` (hasIndexingErrors==true — a wedged sync), STOP and report. A `STALL_ADVISORY` (no head progress over N polls) is benign (managed-sync compaction pause) — it keeps polling; only escalate if it persists far beyond the observed first-chunk rate. DO NOT run the gates until this prints `BACKFILL_SYNCED`.
    5. (Post-sync) Run the off-chain Σ executionCost fill: `uv run python -m probes.index01_live fill-cost` (selector 0xc58343ef at the finalized block); confirm `sum_execution_cost` is written as a DECIMAL STRING (Utf8, NOT Decimal(38,0)) and that `tests/fixtures/getrequest_response_real.json` (a SEPARATE file, NOT the synthetic `getrequest_response.json`) was re-frozen with a REAL return whose `expected_sum_execution_cost` was RECOMPUTED by the decode path (the run DISPLAYS the decoded per-member executionCost list) and satisfies the NON-STRICT `0 <= int(sum) <= sub_size * int(per_agent_budget_native)` (a Failed/TimedOut finalized request with Σ=0 is legitimate), and `uv run pytest tests/test_index01_decode.py -x` still passes (the synthetic decode test reads only the synthetic fixture; it does NOT read the real file).
    6. (Post-sync, head reached) Run the three gates + liveness + rollback against the FULLY-synced store:
       `uv run python -m probes.index01_live gate-completeness gate-ordering gate-parity liveness rollback`
       Confirm the four artifacts exist and contain the required fields:
       - `indexing/completeness_proof.md` — leg(a) fresh transactions_count ≥ 234999; leg(b) the RequestCreated(0xb623)-vs-RequestFinalized(0x65db) COUNT-balance ADVISORY+DIRECTIONAL structural floor (NEVER auto-halts, NON-BLOCKING in decision-4 terms; surfaces an advisory flag for HUMAN REVIEW only when RequestCreated is materially below RequestFinalized) + the per-stratum DESCRIPTIVE RequestCreated COUNT ÷ the indexer's OWN id-PAGINATED distinct-txHash count (NOT 234999, may exceed 1.0, explicitly NOT a gate) + the signed count_balance, with the SC#6(b)/topic0_map_v1.json citation + the "Wilson RETIRED / 0.716 is descriptive-only" note + the addendum in-sample-only basis; leg(c) cursor-contiguity over the host's PROCESSED ranges on [283417317, head]; the B3 zero-foreign-row structural argument.
       - `indexing/ordering_verification.md` — (block_number, log_index) tuple-for-tuple match on a sample that PROVABLY includes ≥1 block with ≥2 proxy logs + block_ts_utc whole-second match (with the unit-reconciliation note).
       - `indexing/parity_report.md` — per-block-receipt parity over ≥300 stratified windows within 0.5%, zero indexer=0 ∧ RPC>0, mechanism cited by `data_sourcing_matrix.yaml` key; IF receipts degraded, the pinned MINIMUM completeness floor (leg a + leg c only); liveness head-lag <60 ≥99%.
       - `.planning/scout/2026-05-29/rollback_observation.md` — `safe_block_depth_observed_max` over ≥1h (run AFTER sync-complete).
    7. Re-pin the new rollback `.md` in `.planning/scout/2026-05-29/PROVENANCE.sha256` and confirm `uv run pytest tests/test_scout_archive.py -x` passes (set-equality test).
    8. Run the full suite: `uv run pytest tests/ -q` — all green.
  </how-to-verify>
  <resume-signal>Type "approved" once the backfill reached head (BACKFILL_SYNCED), the four artifacts exist with the required fields, and the full suite is green — OR describe the deploy-probe / overage / stall outcome (e.g. "mainnet free-tier deploy unavailable", "OVERAGE_PROJECTED at chunk K (realized multiplier M) — user declined $75/mo", "leg-b ADVISORY flag surfaced for human review — RequestCreated materially below RequestFinalized (NOT an auto-halt; legs a+c carry binding completeness)", "STALL_DETECTED — Ormi sync wedged (hasIndexingErrors)", "still syncing, multi-session / STALL_ADVISORY compaction pause") so the orchestrator routes to the sign-off / sub-plan-re-author / paid-swap / wait path.</resume-signal>
  <files>indexing/completeness_proof.md, indexing/ordering_verification.md, indexing/parity_report.md, .planning/scout/2026-05-29/rollback_observation.md</files>
  <action>Human-run DEPLOY-AND-DETACH live sequence per <how-to-verify>: deploy (resolve mainnet slug somnia→somnia-mainnet), START backfill 283417317→head and detach, run the per-chunk CONSERVATIVE REALIZED-RAMP projected-overage check after each 1M-block chunk (small-denominator guarded; pause + surface $75/mo evidence + the realized multiplier on OVERAGE_PROJECTED — NO auto-spend), WAIT for BACKFILL_SYNCED (head reached; may be multi-session; HARD STALL_DETECTED on hasIndexingErrors, benign STALL_ADVISORY on a compaction pause), then (post-sync only) fill Σ executionCost off-chain (0xc58343ef at the finalized block; sum written as Utf8 decimal string, NOT Decimal(38,0); real return re-frozen to the SEPARATE getrequest_response_real.json with a NON-STRICT 0≤Σ bound), and run gate-completeness / gate-ordering / gate-parity / liveness / rollback against the FULLY-synced store. Each emits its indexing/*.md (and the rollback .md re-pinned in PROVENANCE.sha256). Liveness/rollback NEVER run concurrently with an in-progress backfill. leg-b NEVER auto-halts — an advisory flag is surfaced for human review only when RequestCreated is materially below RequestFinalized.</action>
  <verify>
    <automated>test -f indexing/completeness_proof.md && test -f indexing/ordering_verification.md && test -f indexing/parity_report.md && test -f .planning/scout/2026-05-29/rollback_observation.md && grep -q 234999 indexing/completeness_proof.md && grep -qi "0xb623\|RequestCreated" indexing/completeness_proof.md && grep -qi "RequestFinalized\|0x65db\|structural floor" indexing/completeness_proof.md && grep -qi "advisory\|never.*halt\|non-blocking" indexing/completeness_proof.md && grep -qi "cursor" indexing/completeness_proof.md && grep -q "data_sourcing_matrix" indexing/parity_report.md && grep -q safe_block_depth_observed_max .planning/scout/2026-05-29/rollback_observation.md && uv run pytest tests/ -q</automated>
  </verify>
  <done>The backfill reached head (BACKFILL_SYNCED; no unresolved STALL_DETECTED) and the four live artifacts exist with the required fields (leg a/b/c with leg-b = the RequestCreated(0xb623)-vs-RequestFinalized(0x65db) COUNT-balance ADVISORY+DIRECTIONAL structural floor that NEVER auto-halts + the descriptive RequestCreated-count ÷ id-paginated distinct-tx log + the signed count_balance + SC#6(b) cite + "Wilson RETIRED" note, leg-c cursor-contiguity over processed ranges, B3 zero-foreign-row argument; tuple-ordering on a ≥2-log block with whole-second ts match; parity citing the matrix key + the receipts-degraded floor + liveness; safe_block_depth_observed_max post-sync); sum_execution_cost was written as a Utf8 decimal string; the REAL fixture was re-frozen to the SEPARATE getrequest_response_real.json with a NON-STRICT 0≤Σ bound (synthetic fixture untouched); the per-chunk realized-ramp conservative (small-denominator-guarded) projected-overage / $75/mo decision was handled with no auto-spend; the scout PROVENANCE set-equality + full suite are green.</done>
</task>

</tasks>

<verification>
- The four live artifacts exist with the fields enumerated above (`indexing/completeness_proof.md`, `indexing/ordering_verification.md`, `indexing/parity_report.md`, `.planning/scout/2026-05-29/rollback_observation.md`), produced ONLY against the fully-synced (head-reached) store.
- `grep -q "234999" indexing/completeness_proof.md` + the leg-b RequestCreated(0xb623)-vs-RequestFinalized(0x65db) COUNT-balance ADVISORY+DIRECTIONAL structural floor (never auto-halts) + the descriptive-count-ratio log + the "Wilson RETIRED" note + cursor-contiguity present ; `grep -q "data_sourcing_matrix" indexing/parity_report.md` ; `grep -q "safe_block_depth_observed_max" .planning/scout/2026-05-29/rollback_observation.md`.
- `uv run pytest tests/ -q` green (incl. the SEPARATE re-frozen real getrequest_response_real.json fixture with the recomputed + NON-STRICT-bound-checked sum + the re-pinned scout PROVENANCE set-equality; the synthetic getrequest_response.json is untouched and 03-01's strict structural guard still passes).
- sum_execution_cost is written as a Utf8 decimal string (NOT Decimal(38,0)) — the at-scale OverflowError is deferred to a guarded PANEL-01 concern.
- The per-chunk REALIZED-RAMP conservative (small-denominator-guarded) projected-overage / $75/mo decision was handled with NO auto-spend (user-confirmed if it fired).
- Liveness/rollback ran AFTER sync-complete, never concurrent with an in-progress backfill; a wedged sync surfaces as STALL_DETECTED (hasIndexingErrors), a benign compaction pause as STALL_ADVISORY.
- leg-b NEVER auto-halts (ROADMAP SC#6(b)): the only surfaced flag is an advisory for human review in the missing-RequestCreated direction.
</verification>

<success_criteria>
- Maps 03-VALIDATION.md LIVE rows L1 (deploy+detach backfill with hasIndexingErrors-primary stall-detection), L2 (3-leg completeness post-sync; leg-b advisory+directional), L3 (liveness window post-sync), L4 (rollback post-sync), L5 (per-chunk realized-ramp small-denominator-guarded projected-overage) + INDEX-01 SC#1–SC#7.
- SC#1 maps to the corrected (2026-05-29 Phase-2-resolved) event list: RequestCreated / RequestFinalized / CommitteeDepositFailed — ResponseReceived does not exist; NativeTransferFailed/SubcommitteePaid are ABI-carried but unhandled (never emitted); Upgraded/AdminChanged/BeaconUpgraded deferred per 03-CONTEXT.
- Architecture B executed: aggregate Σ executionCost filled off-chain (0xc58343ef at finalized block), no in-subgraph eth_call; sum_execution_cost written as a Utf8 decimal string (M-2); the re-frozen REAL fixture is a SEPARATE file (getrequest_response_real.json), evidence-bearing (decoded members displayed, sum recomputed, NON-STRICT 0≤Σ bound — a zero-cost Failed/TimedOut finalized request is legitimate), leaving the synthetic mis-slice tripwire intact (BLOCKER-1).
- The deploy slug, Ormi managed-sync wall-clock, and entity-count read were resolved as DEPLOY-PROBES (not hard-coded); the payment checkpoint is explicit, per-chunk, REALIZED-RAMP conservative, small-denominator-guarded, and no-auto-spend.
- leg-b is ADVISORY + DIRECTIONAL and NEVER auto-halts (MAJOR-1; ROADMAP SC#6(b): legs a+c carry binding completeness); the descriptive denominator = the indexer's OWN LOSSLESS id-paginated distinct-txHash count (M-1; NO GraphQL COUNT(DISTINCT), NO lossy blockNumber_gt cursor); leg-c = cursor-contiguity over the host's PROCESSED ranges.
</success_criteria>

<output>
After completion, create `.planning/phases/03-subgraph-indexing/03-04-SUMMARY.md` recording: the resolved mainnet deploy slug, the OBSERVED Ormi managed-sync rate + total backfill wall-clock (or any STALL_DETECTED / STALL_ADVISORY outcome), the realized entity total vs 300k (and whether the per-chunk realized-ramp overage checkpoint fired, at which chunk, the realized multiplier or absolute-density fallback), the leg-a fresh transactions_count, the leg-b advisory verdict (ok / advisory_review) + per-stratum descriptive count ratios (RequestCreated(0xb623) count ÷ id-paginated distinct-tx) + the signed count_balance, observed safe_block_depth, and the parity/liveness results (incl. any receipts-degraded floor).
Commits are LOCAL only — NEVER push (the user opens PRs to upstream per CLAUDE.md).
</output>

---
phase: 03-subgraph-indexing
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - probes/somnia_rpc.py
  - tests/fixtures/getrequest_response.json
  - indexing/decode.py
  - tests/test_index01_decode.py
  - tests/test_index01_constants.py
autonomous: true
requirements: [INDEX-01]
must_haves:
  truths:
    - "A recorded getRequest return decodes and sums responses[].executionCost correctly (selector 0xc58343ef, Response field index 5)"
    - "uint256 ids round-trip through pl.Utf8 and are rejected by pl.Decimal(38,0)"
    - "bounded wei amounts fit pl.Decimal(38,0)"
    - "for a FINALIZED request with ≥1 successful response the decoded Σ executionCost is STRICTLY > 0 (not just ≥ 0) — so a structurally-zero mis-slice (e.g. reading responses[3]=receipt, always 0, instead of responses[5]=executionCost) FAILS CI"
    - "the recorded constants (deploy block 283417317, selector 0xc58343ef, getLogs cap 1000, getBlockReceipts AVAILABLE) are asserted against the scout/matrix artifacts"
  artifacts:
    - path: "indexing/decode.py"
      provides: "pure getRequest tuple-decode + Σ executionCost + dtype-discipline helpers (no live network)"
      min_lines: 40
    - path: "probes/somnia_rpc.py"
      provides: "get_request(request_id, block) live helper (selector 0xc58343ef), __main__-only"
      contains: "0xc58343ef"
    - path: "tests/fixtures/getrequest_response.json"
      provides: "one synthetic/recorded getRequest raw eth_call return for the decode fixture"
    - path: "tests/test_index01_decode.py"
      provides: "decode + Σ executionCost + uint256→Utf8 + wei→Decimal(38,0) tests"
    - path: "tests/test_index01_constants.py"
      provides: "recorded-constant assertions against scout + matrix"
  key_links:
    - from: "indexing/decode.py"
      to: "tests/fixtures/getrequest_response.json"
      via: "decode_get_request reads the recorded return, sums responses[].executionCost"
      pattern: "responses"
    - from: "probes/somnia_rpc.py::get_request"
      to: "RPC eth_call"
      via: "selector 0xc58343ef + abi_encode(uint256)"
      pattern: "0xc58343ef"
---

<objective>
Wave 0 for INDEX-01: scaffold the CI-testable decode surface and the live `get_request` probe helper BEFORE any subgraph or live work. This plan delivers the recorded `getRequest` fixture, the pure-Python `getRequest`→`Σ executionCost` decode (with the uint256/wei dtype discipline), the recorded-constant assertions, and the `get_request(request_id, block)` helper on `probes/somnia_rpc.py`.

Purpose: the Nyquist contract (03-VALIDATION.md) requires the decode/dtype/constants LOGIC to be CI-fixtured (no live network) so the LIVE state-fill in Plan 03-04 has a tested code path. This is the `aggregate-only Σ executionCost` decision (03-CONTEXT.md decision 1) made executable.
Output: `indexing/decode.py`, `probes/somnia_rpc.py::get_request`, `tests/fixtures/getrequest_response.json`, `tests/test_index01_decode.py`, `tests/test_index01_constants.py`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-subgraph-indexing/03-RESEARCH.md
@.planning/phases/03-subgraph-indexing/03-VALIDATION.md
@references/interfaces/IAgentRequester.sol
@schemas/event_schema_v1.md
@probes/somnia_rpc.py
@tests/conftest.py

<interfaces>
<!-- Source-of-truth contracts. The executor works from these directly — no codebase exploration needed. -->

From references/interfaces/IAgentRequester.sol — getRequest(uint256) → Request memory:
  struct Request (15 fields, in SOURCE order):
    0 uint256   id
    1 address   requester
    2 address   callbackAddress
    3 bytes4    callbackSelector
    4 address[] subcommittee
    5 Response[] responses          <-- field index 5
    6 uint256   responseCount
    7 uint256   failureCount
    8 uint256   threshold
    9 uint256   createdAt
    10 uint256  deadline
    11 ResponseStatus status        (enum → uint8)
    12 ConsensusType consensusType  (enum → uint8)
    13 uint256  remainingBudget
    14 uint256  perAgentBudget
  struct Response (6 fields, in SOURCE order):
    0 address  validator
    1 bytes    result
    2 ResponseStatus status         (enum → uint8)
    3 uint256  receipt
    4 uint256  timestamp
    5 uint256  executionCost        <-- field index 5; self-reported, capped at perAgentBudget

  eth_abi tuple type for Request:
    "(uint256,address,address,bytes4,address[],(address,bytes,uint8,uint256,uint256,uint256)[],uint256,uint256,uint256,uint256,uint256,uint8,uint8,uint256,uint256)"
  The aggregate is: sum(r[5] for r in request_tuple[5])   # Σ responses[].executionCost

From probes/somnia_rpc.py (reuse — do NOT duplicate):
  RPC = "https://api.infra.mainnet.somnia.network/"
  PROXY = "0x5E5205CF39E766118C01636bED000A54D93163E6"
  GETLOGS_MAX_WINDOW = 1000
  DEPLOYMENT_BLOCK = 283_417_317
  _rpc(method, params, *, rpc=RPC, timeout=30.0)   # single JSON-RPC POST, __main__-tolerant
  _hex_int(value) -> str

From schemas/event_schema_v1.md — DTYPE SCOPE RULE (verbatim, load-bearing):
  "Decimal(38,0) permitted ONLY for wei amounts provably ≤ 38 digits;
   requestId/agentId/topic-derived ids are Utf8-only (uint256 = up to 78 digits >
   Decimal128's 38)."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the get_request probe helper + record/freeze the getRequest fixture</name>
  <read_first>
    - probes/somnia_rpc.py (the module being modified; reuse `_rpc`, `_hex_int`, `PROXY`, constants)
    - references/interfaces/IAgentRequester.sol (the Request/Response struct shape for the eth_call return)
    - .planning/phases/03-subgraph-indexing/03-RESEARCH.md §"Off-chain getRequest state-fill (Arch B)" + §"Wave 0 Gaps"
  </read_first>
  <action>
    1. Add `get_request(request_id: int, block: int | str = "latest", *, rpc: str = RPC) -> str` to `probes/somnia_rpc.py`. Body: `data = "0xc58343ef" + f"{request_id:064x}"` then `return _rpc("eth_call", [{"to": PROXY, "data": data}, (block if isinstance(block, str) else _hex_int(block))], rpc=rpc)`. Selector is EXACTLY `0xc58343ef` (getRequest(uint256)). Keep it network-touching ONLY — it is called from the `__main__` block / Plan 03-04 live probe, NEVER from CI. Add a docstring noting "read at the RequestFinalized block (or any block ≥ finalization); reading pre-finalization undercounts executionCost — RESEARCH Pitfall 3".
    2. Extend the `__main__` block with a guarded demo line that prints `get_request(<a real finalized requestId>)` ONLY when invoked directly (wrap in try/except so a bad id does not crash the probe).
    3. Create `tests/fixtures/getrequest_response.json` — a FROZEN object `{"request_id": "<decimal string>", "block": <int>, "raw": "0x<abi-encoded Request tuple hex>", "expected_sum_execution_cost": "<decimal string>", "per_agent_budget_native": "<decimal string>", "source": "synthetic — abi-encoded from a 3-member Response[] with executionCosts summing to expected_sum_execution_cost; replace with a real recorded return captured via `uv run python -m probes.somnia_rpc` at deploy time (Plan 03-04) then re-freeze", "note": "Response field index 5 = executionCost; subSize_default=3; this synthetic fixture is a FINALIZED request with 3 successful responses so Σ executionCost is STRICTLY > 0"}`. Build `raw` by abi-encoding a Request tuple whose `responses` is a 3-element array with known executionCosts (e.g. 30000000000000000, 70000000000000000, 100000000000000000 wei) so `expected_sum_execution_cost = 200000000000000000` (strictly > 0). Use `eth_abi.encode` in a one-off (you MAY add `eth-abi` to the dev deps if not present; it is already needed for decode) to produce the hex, then hard-code it into the JSON so the fixture is static. Set `per_agent_budget_native = "100000000000000000"` (0.1 SOMI — ≥ each member's executionCost) so the M5 budget bound `0 < Σ executionCost ≤ subSize_default × per_agent_budget_native` holds deterministically for the synthetic fixture (0 < 0.2 ≤ 3 × 0.1 = 0.3).
  </action>
  <verify>
    <automated>uv run python -c "import probes.somnia_rpc as r; assert hasattr(r,'get_request'); import json; d=json.load(open('tests/fixtures/getrequest_response.json')); assert d['raw'].startswith('0x') and d['expected_sum_execution_cost'] and int(d['expected_sum_execution_cost'])>0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "0xc58343ef" probes/somnia_rpc.py`
    - `grep -q "def get_request" probes/somnia_rpc.py`
    - `test -f tests/fixtures/getrequest_response.json`
    - `python -c "import json; d=json.load(open('tests/fixtures/getrequest_response.json')); assert d['raw'].startswith('0x'); assert d['expected_sum_execution_cost']=='200000000000000000'"` exits 0
  </acceptance_criteria>
  <done>`get_request` exists with selector `0xc58343ef`, is `__main__`/probe-only (no CI network), and a frozen `getrequest_response.json` fixture with a known STRICTLY-POSITIVE `expected_sum_execution_cost` exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure decode + dtype-discipline module + its CI tests</name>
  <read_first>
    - tests/fixtures/getrequest_response.json (from Task 1 — the decode input)
    - references/interfaces/IAgentRequester.sol (the Request/Response tuple type — see <interfaces> block)
    - schemas/event_schema_v1.md §"uint256 overflow (HARD constraint)" + the DTYPE SCOPE RULE
    - tests/test_event_schema.py (lines 1-60 — the established uint256 round-trip test pattern to mirror)
    - tests/conftest.py (the N2 fixture contract — consume `load_json` as a test PARAMETER, not an import)
  </read_first>
  <behavior>
    - test_sum_execution_cost: decode the fixture `raw` and assert the summed `responses[].executionCost` equals `expected_sum_execution_cost` (Python int, then str) = "200000000000000000".
    - test_uint256_utf8: a 78-digit uint256 request_id survives a `pl.Series(..., dtype=pl.Utf8)` round-trip intact, AND `pl.Series([2**256-1], dtype=pl.Decimal(38,0))` raises (proves the Utf8-only rule empirically).
    - test_wei_decimal_ok: a SOMI-wei value (the fixture `per_agent_budget_native`, ≤38 digits) constructs in `pl.Decimal(38,0)` without error.
    - test_bound_check (M5 + m-3 evidence enforcement): assert `0 < int(d['expected_sum_execution_cost']) <= 3 * int(d['per_agent_budget_native'])` (subSize_default=3) on the fixture — a STRICT lower bound (> 0, NOT ≥ 0) because the fixture is a FINALIZED request with ≥1 successful response, so a STRUCTURALLY-ZERO mis-slice (e.g. reading `responses[3]=receipt` — always 0 — instead of `responses[5]=executionCost`, the Pitfall-3 named layout trap) FAILS CI here rather than silently passing as a matching-but-wrong sum. This is the CI surface that makes Plan 03-04's real-fixture re-freeze evidence-bearing.
  </behavior>
  <action>
    1. Create `indexing/decode.py` (new top-level `indexing/` package dir — add `indexing/__init__.py`). Define `REQUEST_TUPLE_TYPE` (the eth_abi type string from the <interfaces> block), `RESPONSE_EXECUTION_COST_INDEX = 5`, `RESPONSES_FIELD_INDEX = 5`, `GETREQUEST_SELECTOR = "0xc58343ef"`. Implement `decode_get_request(raw_hex: str) -> tuple` (strips `0x`, `eth_abi.decode([REQUEST_TUPLE_TYPE], bytes.fromhex(...))[0]`) and `sum_execution_cost(request_tuple) -> int` (`sum(r[RESPONSE_EXECUTION_COST_INDEX] for r in request_tuple[RESPONSES_FIELD_INDEX])`). Add `to_uint256_utf8(value: int) -> str` (returns `str(value)` — the documented uint256→Utf8 path) and `wei_to_decimal_str(value: int) -> str` (decimal string for `pl.Decimal(38,0)`-safe wei). Pure functions only — NO network, NO file IO inside the functions.
    2. Create `tests/test_index01_decode.py` per the <behavior> block. Consume `load_json` + a path to the fixture (build the path from a literal `tests/fixtures/getrequest_response.json` or via a small fixture path) as TEST PARAMETERS per the N2 contract. Mirror the empirical-round-trip style of `tests/test_event_schema.py::test_uint256_not_int64` (assert `pl.Decimal(38,0)` raises on the 78-digit value via `pytest.raises`).
    3. Ensure `eth-abi` and `polars` are importable in the dev env (`uv add --dev eth-abi` if eth-abi is missing; polars is already a dev dep).
  </action>
  <verify>
    <automated>uv run pytest tests/test_index01_decode.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_index01_decode.py -x` exits 0
    - `grep -q "0xc58343ef" indexing/decode.py`
    - `grep -q "RESPONSE_EXECUTION_COST_INDEX = 5" indexing/decode.py`
    - `grep -q "def sum_execution_cost" indexing/decode.py`
    - `grep -q "pl.Decimal(38" tests/test_index01_decode.py` (the empirical overflow assertion is present)
    - `grep -q "def test_bound_check" tests/test_index01_decode.py` (the M5 budget-bound assertion is present)
    - `grep -qE "0 < int|< int\\(d\\['expected_sum|> 0" tests/test_index01_decode.py` (m-3: the STRICT lower bound `Σ executionCost > 0` is asserted — a structurally-zero mis-slice FAILS CI)
  </acceptance_criteria>
  <done>`indexing/decode.py` decodes the recorded `getRequest` tuple and sums `responses[].executionCost` to 200000000000000000; uint256→Utf8 and wei→Decimal(38,0) discipline is empirically asserted; the budget bound is STRICTLY positive (`0 < Σ ≤ subSize×per_agent_budget`) so a structurally-zero mis-slice fails CI; `tests/test_index01_decode.py` green.</done>
</task>

<task type="auto">
  <name>Task 3: Recorded-constant assertions module</name>
  <read_first>
    - .planning/scout/2026-05-29/rpc_capability_probe.md (getBlockReceipts AVAILABLE, getLogs cap 1000 provenance)
    - .planning/scout/2026-05-29/deployment_block.md (deploy block 283417317)
    - research/data_sourcing_matrix.yaml (the machine-checkable capability matrix rows)
    - probes/somnia_rpc.py (DEPLOYMENT_BLOCK, GETLOGS_MAX_WINDOW constants)
    - tests/conftest.py (scout_dir/research_dir/load_yaml/read_text fixtures — consume as params)
  </read_first>
  <action>
    Create `tests/test_index01_constants.py` asserting the load-bearing recorded constants are present and consistent across the scout archive + matrix + probe module (pure fixture reads, no network):
    - `test_deploy_block`: `probes.somnia_rpc.DEPLOYMENT_BLOCK == 283417317` AND the string `283417317` (or `283,417,317`) appears in `scout_dir/deployment_block.md`.
    - `test_getrequest_selector`: `"0xc58343ef"` appears in `probes/somnia_rpc.py` (read via `read_text`) and in `indexing/decode.py`.
    - `test_getlogs_cap`: `probes.somnia_rpc.GETLOGS_MAX_WINDOW == 1000`.
    - `test_getblockreceipts_available`: `"AVAILABLE"` appears for `eth_getBlockReceipts` in `scout_dir/rpc_capability_probe.md` (and/or the matrix yaml has a `pass: true` row for it — load via `load_yaml(research_dir / "data_sourcing_matrix.yaml")` and assert a row mentions `getBlockReceipts`/`get_block_receipts` with a truthy pass).
    Consume `scout_dir`, `research_dir`, `load_yaml`, `read_text` as TEST PARAMETERS (N2).
  </action>
  <verify>
    <automated>uv run pytest tests/test_index01_constants.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_index01_constants.py -x` exits 0
    - `grep -q "283417317" tests/test_index01_constants.py`
    - `grep -q "0xc58343ef" tests/test_index01_constants.py`
    - `grep -q "AVAILABLE" tests/test_index01_constants.py`
  </acceptance_criteria>
  <done>The four recorded constants (deploy block, getRequest selector, getLogs cap, getBlockReceipts AVAILABLE) are asserted against the scout archive + matrix + probe module; module green.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_index01_decode.py tests/test_index01_constants.py -x` green.
- `uv run pytest tests/ -q` green (existing 56 + new INDEX-01 decode/constants tests; no regressions).
- `grep -q "0xc58343ef" probes/somnia_rpc.py indexing/decode.py tests/test_index01_decode.py tests/test_index01_constants.py`.
- The decode bound-check asserts `Σ executionCost > 0` (strict) on the finalized-with-responses fixture (m-3) — a structurally-zero mis-slice fails CI.
- No live network in CI: the new test modules touch no RPC/Blockscout; `get_request` is `__main__`/probe-only.
</verification>

<success_criteria>
- Maps 03-VALIDATION.md rows #1 (sum_execution_cost), #2 (uint256→Utf8), #3 (wei→Decimal), #12 (recorded constants).
- `indexing/decode.py` is a pure, network-free, CI-fixtured decode path for the aggregate Σ executionCost (decision 1).
- `probes/somnia_rpc.py::get_request` exists with selector `0xc58343ef`, read-at-finalized-block documented (Pitfall 3).
- The budget bound is STRICTLY positive (`0 < Σ ≤ subSize×per_agent_budget`) so a structurally-zero mis-slice fails CI (m-3).
</success_criteria>

<output>
After completion, create `.planning/phases/03-subgraph-indexing/03-01-SUMMARY.md`.
Commits are LOCAL only (per CLAUDE.md fork/upstream workflow) — NEVER push.
</output>

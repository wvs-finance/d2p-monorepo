---
phase: 03-subgraph-indexing
plan: 03
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - subgraphs/iagentrequester/subgraph.yaml
  - subgraphs/iagentrequester/schema.graphql
  - subgraphs/iagentrequester/networks.json
  - subgraphs/iagentrequester/package.json
  - subgraphs/iagentrequester/abis/IAgentRequester.json
  - subgraphs/iagentrequester/src/mapping.ts
  - subgraphs/iagentrequester/README.md
  - tests/test_index01_manifest.py
autonomous: true
requirements: [INDEX-01]
must_haves:
  truths:
    - "subgraph.yaml pins the proxy 0x5E5205CF…163E6 (never address:null, B3) and startBlock 283417317"
    - "the manifest registers handlers for the three OBSERVED events using the topic0_map_v1.json resolved roles (RequestCreated, RequestFinalized(uint256,uint8), CommitteeDepositFailed)"
    - "the entity model folds CommitteeDepositFailed onto its Request via a requestId-keyed, NON-LOSSY accumulator (committeeDepositFailedCount counter + committeeDepositFailedAttemptedTotal/last-attempted), so a RETRYABLE CommitteeDepositFailed (>1× per requestId — NatSpec: fires when the committee-deposit call reverts) ACCUMULATES instead of overwriting; one Request per requestId keeps the entity total ≈165k under the 300k cap (the counter is a field, not a new entity)"
    - "uint256 ids are GraphQL BigInt (serialize as decimal string → pl.Utf8 downstream), never a numeric narrow type"
    - "the mapping never treats CommitteeDepositFailed as an error (structural invariant, Phase-2 note 2) and increments the counter on every occurrence (retryable, never overwrites)"
    - "the README documents the LOSSLESS distinct-tx full-scan idiom: paginate by the globally-unique monotone entity id (id_gt, orderBy:id) — NOT blockNumber_gt (multiple Requests/block at ~100ms blocks → a strictly-greater blockNumber cursor silently drops same-block rows on the next page → under-counts distinct-tx)"
  artifacts:
    - path: "subgraphs/iagentrequester/subgraph.yaml"
      provides: "manifest: proxy-pinned dataSource, startBlock 283417317, 3 event handlers, spec/api versions"
      contains: "0x5E5205CF39E766118C01636bED000A54D93163E6"
    - path: "subgraphs/iagentrequester/schema.graphql"
      provides: "flat Request entity (folds CommitteeDepositFailed via a non-lossy requestId-keyed counter) — no per-member responses entity"
      contains: "type Request"
    - path: "subgraphs/iagentrequester/networks.json"
      provides: "chain 5031 address + startBlock pin (kept out of manifest for reuse)"
      contains: "283417317"
    - path: "subgraphs/iagentrequester/src/mapping.ts"
      provides: "handleRequestCreated / handleRequestFinalized / handleCommitteeDepositFailed AssemblyScript handlers (CommitteeDepositFailed accumulates, never overwrites)"
    - path: "subgraphs/iagentrequester/README.md"
      provides: "deploy-probe + off-chain Arch-B fill + LOSSLESS id-paginated distinct-tx scan (id_gt, NOT blockNumber_gt) + block-range overage-count scan caveats"
      contains: "id_gt"
    - path: "subgraphs/iagentrequester/abis/IAgentRequester.json"
      provides: "ABI incl. getRequest + the 5 events (for Arch-A getRequest binding + completeness)"
  key_links:
    - from: "subgraphs/iagentrequester/subgraph.yaml"
      to: "schemas/topic0_map_v1.json resolved roles"
      via: "eventHandlers use resolved signatures, not the scout's inverted labels"
      pattern: "RequestFinalized\\(uint256"
    - from: "subgraphs/iagentrequester/schema.graphql"
      to: "300k entity cap"
      via: "CommitteeDepositFailed folded (requestId-keyed counter, non-lossy) into Request → ~165k entities"
      pattern: "committeeDepositFailedCount"
    - from: "subgraphs/iagentrequester/README.md"
      to: "leg-b indexer_distinct_tx full scan (Plan 03-04)"
      via: "LOSSLESS id_gt pagination (orderBy:id) — never blockNumber_gt for the distinct-tx scan"
      pattern: "id_gt"
---

<objective>
Wave 1: author the Ormi/Somnia subgraph `subgraphs/iagentrequester/` (KPD-08) — manifest, schema.graphql, networks.json, ABI, AssemblyScript mappings, package.json — OFFLINE (no deploy; deploy is Plan 03-04's live step). This is the indexer product: proxy-pinned (B3), startBlock 283417317, three observed-event handlers using the `topic0_map_v1.json` resolved roles, a FLAT entity model that folds `CommitteeDepositFailed` onto its `Request` via a NON-LOSSY requestId-keyed accumulator (a counter + running totals — CommitteeDepositFailed is RETRYABLE and can fire >1× per requestId, so a scalar overwrite would lose attempts) so the entity total stays ≈165k (under the 300k free cap), and the `sumExecutionCost` field left for the off-chain Arch-B post-pass (Plan 03-04).

Purpose: Architecture B is locked (03-RESEARCH §"executionCost decode fork") — the subgraph indexes the arrival series; the aggregate Σ executionCost is filled off-chain in Python. This plan delivers a manifest that `graph codegen && graph build` can validate at deploy time and a static manifest-lint CI test that needs no toolchain. The README documents the LOSSLESS distinct-tx full-scan idiom (gate-review #2 M-1: id-paginate, never blockNumber_gt).
Output: the `subgraphs/iagentrequester/` tree + `tests/test_index01_manifest.py`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-subgraph-indexing/03-RESEARCH.md
@.planning/phases/03-subgraph-indexing/03-CONTEXT.md
@references/interfaces/IAgentRequester.sol
@schemas/topic0_map_v1.json
@schemas/event_schema_v1.md
@.planning/phases/02-topic-implementation-provenance/02-FORWARD-NOTES.md
@tests/conftest.py

<interfaces>
<!-- Resolved roles (topic0_map_v1.json) + manifest constants. Use these verbatim — do NOT use the scout's inverted labels. -->

Proxy (B3, never null): 0x5E5205CF39E766118C01636bED000A54D93163E6   (chain 5031)
startBlock: 283417317

Resolved event signatures (schemas/topic0_map_v1.json — impl 0x9af5…3edd):
  RequestCreated(uint256 indexed requestId, uint256 indexed agentId, uint256 perAgentBudget, bytes payload, address[] subcommittee)
    topic0 0xb62339927ed9948fd837358a55f5b9a824f7b047043faece66965593ed726889
  RequestFinalized(uint256 indexed requestId, uint8 status)            # ResponseStatus enum → uint8
    topic0 0x65db1ef5b3bcd84fe4fb8dbbe1cadc9fe6643bb261ab2e01d65c281c3d466af2
  CommitteeDepositFailed(uint256 indexed requestId, uint256 attemptedAmount)
    topic0 0x5c090ef48df2b4d8a01bd0639355d62c318b623aed749bdd12325f789e37a2cf
  (SubcommitteePaid, NativeTransferFailed = ABI-defined but NEVER emitted — keep in abis/ for getRequest/completeness, do NOT register as handlers)

Entity-cap lever + NON-LOSSY fold (03-RESEARCH §"Pattern 2", 03-CONTEXT decision 2 + Phase-2 note 2):
  CommitteeDepositFailed is a structural invariant (NOT an error) AND is RETRYABLE — per the NatSpec it
  fires when the committee-deposit call reverts, so it can fire >1× per requestId (multiple deposit attempts).
  FOLD KEY = requestId (1:1 with Request — this keeps the ENTITY COUNT safe regardless of multiplicity:
  one Request per requestId, the counter is a FIELD, not a new entity ⇒ ~165k entities, under 300k).
  But the folded FIELD MUST be NON-LOSSY — do NOT use a scalar amount that a retry silently overwrites.
  Use an ACCUMULATOR: committeeDepositFailedCount (Int, ++ each occurrence),
                      committeeDepositFailedAttemptedTotal (BigInt, += attemptedAmount each occurrence),
                      committeeDepositFailedLastAmount (BigInt, last-attempted, for reference).
  A first-class CommitteeEvent entity (per-occurrence) would push ≈330k+, brushing the cap — REJECTED.

DTYPE downstream (schemas/event_schema_v1.md DTYPE SCOPE RULE):
  GraphQL BigInt serializes as a decimal string → consumed as pl.Utf8 (uint256 ids) in the panel.
  blockTimestamp is COARSE SECONDARY — never the arrival sort key (use blockNumber+logIndex).

DISTINCT-TX FULL SCAN — LOSSLESS id-pagination idiom (gate-review #2 M-1; Plan 03-04 consumes this):
  To compute the leg-b `indexer_distinct_tx` (the indexer's OWN distinct proxy-tx count), the README MUST
  document paging by the globally-unique monotone entity `id`, NOT by blockNumber:
    Request(first:1000, where:{id_gt:$last_id}, orderBy:id, orderDirection:asc)  — advance $last_id to the
    last entity's `id` each page; collect every txHash into a Python set → indexer_distinct_tx = len(set).
  WHY NOT blockNumber_gt: multiple Request entities share a blockNumber (1–5×/tx at ~100ms blocks); a
  strictly-greater blockNumber cursor advancing by the last page's max blockNumber SILENTLY DROPS the
  same-block Requests that fell on the next page → set-dedup CANNOT recover never-fetched rows →
  under-counts distinct-tx → INFLATES the leg-b ratio. `id` is unique per entity ⇒ no same-key boundary
  ambiguity ⇒ lossless full scan. (blockNumber-windowing stays ONLY for the bounded-range overage COUNT.)

Manifest versions (03-RESEARCH §"Standard Stack", verify at plan time):
  specVersion: 1.2.0   (≥1.2.0 only strictly required if a declared eth_call is used — Arch B uses none, but pin 1.2.0)
  mapping.apiVersion: 0.0.9 ; language: wasm/assemblyscript
  Pin graph-cli/graph-ts in package.json: run `npm view @graphprotocol/graph-cli version` + `npm view @graphprotocol/graph-ts version`.

Network slug CAVEAT (03-RESEARCH Open Q1 — DEPLOY-PROBE, do NOT hard-assume mainnet):
  testnet docs show "somnia-testnet"; mainnet slug "somnia" vs "somnia-mainnet" is UNVERIFIED.
  Use network: somnia in the manifest but FLAG it in README as a deploy-probe item resolved at deploy time (Plan 03-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manifest + networks.json + ABI + package.json (proxy-pinned, B3, startBlock)</name>
  <read_first>
    - schemas/topic0_map_v1.json (resolved event signatures — use these, NOT scout labels)
    - references/interfaces/IAgentRequester.sol (the ABI source: 5 events + getRequest)
    - .planning/phases/03-subgraph-indexing/03-RESEARCH.md §"Pattern 1" + §"Standard Stack" (manifest sketch, version pins, deploy CAVEAT)
    - .planning/phases/03-subgraph-indexing/03-CONTEXT.md decision (Claude's discretion: networks.json keeps the pin out of the manifest)
  </read_first>
  <action>
    1. `subgraphs/iagentrequester/abis/IAgentRequester.json` — a standard JSON ABI array with the 5 events (RequestCreated, RequestFinalized, SubcommitteePaid, CommitteeDepositFailed, NativeTransferFailed) AND the `getRequest(uint256)` view returning the full `Request` tuple (incl. nested `Response[]`). Derive field types EXACTLY from `references/interfaces/IAgentRequester.sol` (RequestFinalized status is `uint8` per the resolved role; enums canonicalize to uint8).
    2. `subgraphs/iagentrequester/networks.json` — `{ "somnia": { "IAgentRequester": { "address": "0x5E5205CF39E766118C01636bED000A54D93163E6", "startBlock": 283417317 } } }`.
    3. `subgraphs/iagentrequester/subgraph.yaml` — specVersion 1.2.0; one ethereum dataSource named IAgentRequester; `network: somnia`; `source.address: "0x5E5205CF39E766118C01636bED000A54D93163E6"` (NEVER null — B3) + `startBlock: 283417317`; mapping apiVersion 0.0.9, language wasm/assemblyscript, entities `[Request]`; abis → IAgentRequester → ./abis/IAgentRequester.json; eventHandlers for the THREE observed events ONLY: `RequestCreated(uint256 indexed,uint256 indexed,uint256,bytes,address[])`→handleRequestCreated, `RequestFinalized(uint256 indexed,uint8)`→handleRequestFinalized, `CommitteeDepositFailed(uint256 indexed,uint256)`→handleCommitteeDepositFailed; file ./src/mapping.ts. Add a comment line referencing the network-slug DEPLOY-PROBE caveat.
    4. `subgraphs/iagentrequester/package.json` — name, private, scripts (codegen/build/deploy), and `dependencies` pinning `@graphprotocol/graph-cli` + `@graphprotocol/graph-ts` (resolve exact versions via `npm view @graphprotocol/graph-cli version` / `npm view @graphprotocol/graph-ts version`; record the resolved versions). If npm is unavailable in the sandbox, pin to a known-recent caret range and note the verify-at-deploy requirement in README.
  </action>
  <verify>
    <automated>uv run python -c "import yaml,json; m=yaml.safe_load(open('subgraphs/iagentrequester/subgraph.yaml')); ds=m['dataSources'][0]; assert ds['source']['address'].lower()=='0x5e5205cf39e766118c01636bed000a54d93163e6'; assert ds['source']['startBlock']==283417317; hs={h['handler'] for h in ds['mapping']['eventHandlers']}; assert hs=={'handleRequestCreated','handleRequestFinalized','handleCommitteeDepositFailed'}; n=json.load(open('subgraphs/iagentrequester/networks.json')); assert n['somnia']['IAgentRequester']['startBlock']==283417317"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "0x5E5205CF39E766118C01636bED000A54D93163E6" subgraphs/iagentrequester/subgraph.yaml`
    - `grep -q "startBlock: 283417317" subgraphs/iagentrequester/subgraph.yaml` (or startBlock pinned via networks.json — the yaml assert above covers either)
    - `grep -q "RequestFinalized(uint256 indexed,uint8)" subgraphs/iagentrequester/subgraph.yaml`
    - `! grep -q "address: null" subgraphs/iagentrequester/subgraph.yaml` (B3)
    - `grep -q "getRequest" subgraphs/iagentrequester/abis/IAgentRequester.json`
  </acceptance_criteria>
  <done>Manifest pins the proxy (never null, B3) + startBlock 283417317, registers exactly the 3 observed-event handlers with resolved signatures, ABI includes getRequest + 5 events, networks.json + package.json present.</done>
</task>

<task type="auto">
  <name>Task 2: schema.graphql (flat Request, CommitteeDepositFailed non-lossy fold) + AssemblyScript mappings</name>
  <read_first>
    - .planning/phases/03-subgraph-indexing/03-RESEARCH.md §"Pattern 2" (flat entity, 300k-cap lever) + §"Pattern 3 Arch B" (sumExecutionCost left for off-chain)
    - .planning/phases/02-topic-implementation-provenance/02-FORWARD-NOTES.md Note 2 (CommitteeDepositFailed = structural invariant, never an error)
    - references/interfaces/IAgentRequester.sol (CommitteeDepositFailed NatSpec — fires when the committee-deposit call reverts; retryable, so >1× per requestId is possible)
    - schemas/event_schema_v1.md §"Arrival-ordering contract" (blockNumber+logIndex ordering; blockTimestamp coarse secondary)
  </read_first>
  <action>
    1. `subgraphs/iagentrequester/schema.graphql` — ONE `type Request @entity` with: `id: ID!` (requestId decimal string), `agentId: BigInt!`, `perAgentBudget: BigInt!`, `subcommittee: [Bytes!]!`, `blockNumber: BigInt!`, `logIndex: BigInt!`, `txHash: Bytes!`, `blockTimestamp: BigInt!` (coarse secondary — comment it), `status: Int` (set on finalize), `finalizedBlock: BigInt`, `finalizedLogIndex: BigInt`, and the NON-LOSSY CommitteeDepositFailed fold (requestId-keyed accumulator, NOT a scalar that overwrites — CommitteeDepositFailed is retryable, fires ≥1× per requestId): `committeeDepositFailedCount: Int!` (default 0; ++ each occurrence), `committeeDepositFailedAttemptedTotal: BigInt!` (default 0; += attemptedAmount each occurrence), `committeeDepositFailedLastAmount: BigInt` (last-attempted, nullable), `sumExecutionCost: BigInt` (left NULL — filled off-chain by the Arch-B post-pass in Plan 03-04). Do NOT define a per-member `responses` entity and do NOT define a first-class `CommitteeEvent` entity (decisions 1 + entity-cap lever). Add a header comment: "Entity model: ~165k Request entities (CommitteeDepositFailed folded by requestId into a non-lossy accumulator — counter is a FIELD, not a new entity, so the count is multiplicity-independent) → under the 300k free cap. sumExecutionCost filled off-chain (Arch B)."
    2. `subgraphs/iagentrequester/src/mapping.ts` — AssemblyScript:
       - `handleRequestCreated(event)`: create/load `Request` by `event.params.requestId.toString()`; set agentId, perAgentBudget, subcommittee, blockNumber, logIndex (`event.logIndex`), txHash, blockTimestamp from `event.block`/`event.transaction`/`event.params`; initialise `committeeDepositFailedCount = 0` and `committeeDepositFailedAttemptedTotal = BigInt.zero()` if newly created; `req.save()`.
       - `handleRequestFinalized(event)`: load by requestId; if null, create (a finalize without a seen create is still a row — comment why; initialise the two CommitteeDepositFailed accumulators to 0/zero so a later increment is well-defined); set `status = event.params.status`, `finalizedBlock`, `finalizedLogIndex`; save. Do NOT call getRequest here (Arch B is off-chain).
       - `handleCommitteeDepositFailed(event)`: load/create the Request by requestId (initialise accumulators to 0/zero on create); ACCUMULATE (NEVER overwrite): `committeeDepositFailedCount = committeeDepositFailedCount + 1`, `committeeDepositFailedAttemptedTotal = committeeDepositFailedAttemptedTotal.plus(event.params.attemptedAmount)`, `committeeDepositFailedLastAmount = event.params.attemptedAmount`; save. Comment: "structural invariant (NOT an error — Phase-2 note 2) AND retryable (NatSpec: fires when the committee-deposit call reverts) → fold is requestId-keyed and ACCUMULATES; a scalar overwrite would lose retry attempts. Entity count stays 1 per requestId."
       Add a top comment that codegen/build is a deploy-time/toolchain step (graph-cli not vendored in CI); the generated types live under ./generated after `graph codegen`.
    3. `subgraphs/iagentrequester/README.md` — document: the Arch-B off-chain sumExecutionCost fill (selector 0xc58343ef, Plan 03-04); the network-slug DEPLOY-PROBE caveat (somnia vs somnia-mainnet, resolve at deploy); the requestId-keyed non-lossy CommitteeDepositFailed fold (counter + running total, retryable, entity count unaffected); and the TWO distinct paginated read mechanisms (there is NO GraphQL `COUNT(DISTINCT)`; 03-RESEARCH Pitfall 2), which use DIFFERENT cursors for a reason:
       (a) leg-b DISTINCT-TX FULL SCAN — paginate by the globally-unique monotone entity `id`: `Request(first:1000, where:{id_gt:$last_id}, orderBy:id, orderDirection:asc)`, advancing `$last_id` to the LAST entity's `id` each page, collecting `txHash` into a Python set → `indexer_distinct_tx = len(set)`. This is the LOSSLESS subgraph full-scan idiom: paging by a globally-unique monotone key has NO same-key boundary ambiguity. CRITICAL (gate-review #2 M-1): do NOT page this scan by `blockNumber_gt` advancing by the last page's max blockNumber — multiple Request entities share a blockNumber (1–5×/tx at ~100ms blocks), so a strictly-greater blockNumber cursor SILENTLY DROPS the same-block Requests that fell on the next page; set-dedup on txHash CANNOT recover never-fetched rows → it under-counts `indexer_distinct_tx` and INFLATES the leg-b ratio. `id` pagination is non-lossy because `id` is unique per entity.
       (b) projected-overage ENTITY-COUNT scan — a genuine block-RANGE query over a single 1M-block chunk MAY page by `Request(first:1000, where:{blockNumber_gte:$lo, blockNumber_lt:$hi}, orderBy:blockNumber)` because it counts entities in a bounded window (not a distinct-key full scan); the per-chunk total feeds the conservative re-projection (Plan 03-04). Keep `blockNumber`-windowing ONLY here, where it is genuinely a block-range query — NOT for the distinct-tx full scan in (a).
       And document that `graph codegen && graph build` is the toolchain validity check (LIVE/deploy step, not CI).
  </action>
  <verify>
    <automated>uv run python -c "s=open('subgraphs/iagentrequester/schema.graphql').read(); assert 'type Request' in s; assert 'committeeDepositFailedCount' in s; assert 'committeeDepositFailedAttemptedTotal' in s; assert 'sumExecutionCost' in s; assert not any(l.strip().startswith('type Response') for l in s.splitlines()); m=open('subgraphs/iagentrequester/src/mapping.ts').read(); assert 'handleRequestCreated' in m and 'handleRequestFinalized' in m and 'handleCommitteeDepositFailed' in m; assert '+ 1' in m or '.plus(' in m; r=open('subgraphs/iagentrequester/README.md').read(); assert 'id_gt' in r"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "type Request" subgraphs/iagentrequester/schema.graphql`
    - `grep -q "committeeDepositFailedCount" subgraphs/iagentrequester/schema.graphql` (non-lossy counter, not a scalar overwrite)
    - `grep -q "committeeDepositFailedAttemptedTotal" subgraphs/iagentrequester/schema.graphql`
    - `grep -q "sumExecutionCost" subgraphs/iagentrequester/schema.graphql`
    - `! grep -qi "type CommitteeEvent" subgraphs/iagentrequester/schema.graphql` (folded, not first-class)
    - `! grep -qi "type Response" subgraphs/iagentrequester/schema.graphql` (no per-member entity)
    - `grep -q "handleCommitteeDepositFailed" subgraphs/iagentrequester/src/mapping.ts`
    - `grep -qE "\\+ 1|\\.plus\\(" subgraphs/iagentrequester/src/mapping.ts` (accumulates, never overwrites)
    - `grep -q "id_gt" subgraphs/iagentrequester/README.md` (LOSSLESS distinct-tx scan pages by unique id, NOT blockNumber)
    - `grep -qi "orderBy:id\|orderBy: id" subgraphs/iagentrequester/README.md` (id-pagination idiom documented)
    - `grep -qi "deploy-probe\|somnia-mainnet\|0xc58343ef" subgraphs/iagentrequester/README.md`
  </acceptance_criteria>
  <done>Flat Request entity with CommitteeDepositFailed folded by requestId into a NON-LOSSY accumulator (count + running total, never overwrites; entity count stays ~165k under cap), no per-member/CommitteeEvent entities, sumExecutionCost reserved for Arch-B; three AssemblyScript handlers (CommitteeDepositFailed increments, not overwrites); README documents the deploy-probe + off-chain fill + the LOSSLESS id_gt distinct-tx scan (never blockNumber_gt) + the bounded-range overage-count scan.</done>
</task>

<task type="auto">
  <name>Task 3: Static manifest-lint CI test (no toolchain)</name>
  <read_first>
    - subgraphs/iagentrequester/subgraph.yaml + schema.graphql (the artifacts under test)
    - schemas/topic0_map_v1.json (the resolved roles the manifest must match)
    - tests/conftest.py (N2 fixture contract; add a `Path("subgraphs/iagentrequester")` literal or local fixture)
    - tests/test_topic_resolution.py (style of a JSON-driven assertion test to mirror)
  </read_first>
  <action>
    Create `tests/test_index01_manifest.py` — pure file/parse assertions (no graph-cli, no network):
    - `test_proxy_pinned_never_null`: parse subgraph.yaml; `dataSources[0].source.address` lowercased == proxy; assert the literal `address: null` does NOT appear in the file text (B3).
    - `test_startblock_resolved`: `startBlock == 283417317` in the manifest OR networks.json.
    - `test_handlers_match_resolved_roles`: the three registered event signatures match the resolved signatures in `topic0_map_v1.json` for the three OBSERVED topic0s (RequestCreated/RequestFinalized(uint256,uint8)/CommitteeDepositFailed); SubcommitteePaid + NativeTransferFailed are NOT registered as handlers.
    - `test_entity_model_under_cap`: schema.graphql has `type Request`, has `committeeDepositFailedCount` AND `committeeDepositFailedAttemptedTotal` (the non-lossy fold), and has NEITHER `type CommitteeEvent` NOR a per-member `type Response` entity (the 300k-cap lever).
    - `test_fold_is_non_lossy`: schema.graphql's fold is an accumulator, NOT a single scalar overwrite — assert `committeeDepositFailedCount` is present (a counter) and that the mapping increments it (read `src/mapping.ts`, assert `committeeDepositFailedCount` is reassigned via `+ 1` and the total via `.plus(`).
    - `test_distinct_tx_scan_is_id_paginated`: read `README.md`; assert the distinct-tx full-scan idiom is documented as `id_gt` / `orderBy:id` (the LOSSLESS cursor) AND assert the README explicitly warns AGAINST a `blockNumber_gt`-advanced distinct-tx scan (the lossy cursor) — e.g. assert both `id_gt` is present and the text mentions the blockNumber same-block drop hazard.
    - `test_sumExecutionCost_reserved`: `sumExecutionCost` field present (filled off-chain).
    Consume `load_json` + a schemas_dir path for `topic0_map_v1.json` as TEST PARAMETERS (N2).
  </action>
  <verify>
    <automated>uv run pytest tests/test_index01_manifest.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_index01_manifest.py -x` exits 0
    - `grep -q "address: null" tests/test_index01_manifest.py` (the negative B3 assertion is present)
    - `grep -q "283417317" tests/test_index01_manifest.py`
    - `grep -qi "CommitteeEvent\|committeeDepositFailedCount" tests/test_index01_manifest.py`
    - `grep -q "non_lossy\|\\.plus(\|+ 1" tests/test_index01_manifest.py` (the accumulator-not-overwrite assertion is present)
    - `grep -q "id_gt" tests/test_index01_manifest.py` (the LOSSLESS distinct-tx id-pagination assertion is present)
  </acceptance_criteria>
  <done>A toolchain-free CI test lints the manifest (proxy-pinned B3, startBlock, resolved-role handlers, entity-cap model with the NON-LOSSY committeeDepositFailedCount accumulator, sumExecutionCost reserved) AND asserts the README documents the LOSSLESS id_gt distinct-tx scan over the lossy blockNumber_gt cursor; green.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_index01_manifest.py -x` green; `uv run pytest tests/ -q` green (no regressions).
- subgraph.yaml parses, pins the proxy (never null), startBlock 283417317, registers exactly the 3 observed-event handlers with resolved signatures.
- schema.graphql defines a single Request entity (CommitteeDepositFailed folded by requestId into a non-lossy accumulator — counter + running total, no per-member/CommitteeEvent entities), sumExecutionCost reserved.
- README documents the LOSSLESS distinct-tx full-scan idiom (id_gt / orderBy:id, NOT blockNumber_gt) — the leg-b denominator Plan 03-04 consumes.
- Toolchain validity (`graph codegen && graph build`) is documented as a deploy-time step (Plan 03-04), NOT a CI gate (graph-cli not vendored).
</verification>

<success_criteria>
- Maps INDEX-01 KPD-08 (subgraph authorship) + SC#1 (manifest registers the proxy + events) + the B3 proxy-pin invariant + the entity-cap lever (decision 2 input).
- SC#1 maps to the corrected (2026-05-29 Phase-2-resolved) event list: RequestCreated / RequestFinalized / CommitteeDepositFailed — ResponseReceived does not exist; NativeTransferFailed/SubcommitteePaid are ABI-carried but unhandled (never emitted); Upgraded/AdminChanged/BeaconUpgraded deferred per 03-CONTEXT.
- Architecture B respected: no in-subgraph getRequest eth_call; sumExecutionCost left for the off-chain post-pass.
- CommitteeDepositFailed fold is requestId-keyed and NON-LOSSY (retryable event accumulates, never overwrites; entity count stays ~165k).
- The README documents the LOSSLESS id-paginated distinct-tx scan (id_gt, never blockNumber_gt) — gate-review #2 M-1.
- Resolved topic0_map_v1.json roles used (NOT the scout's inverted labels).
</success_criteria>

<output>
After completion, create `.planning/phases/03-subgraph-indexing/03-03-SUMMARY.md`.
Commits are LOCAL only — NEVER push.
</output>
</content>
</invoke>

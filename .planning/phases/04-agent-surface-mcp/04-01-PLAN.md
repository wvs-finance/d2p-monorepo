---
phase: 04-agent-surface-mcp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - lib/dashboard/contract.ts
  - app/api/dashboard/route.ts
  - app/api/status/route.ts
  - lib/mcp-tools/contract.ts
  - tests/unit/mcp-tools-contract.test.ts
  - tests/api/mcp.test.ts
  - tests/api/openapi.test.ts
  - tests/api/openapi-conformance.test.ts
  - tests/api/mcp-real-sdk.test.ts
  - tests/architecture/mcp-no-duplication.test.ts
  - tests/e2e/agent-stubs.spec.ts
autonomous: true
requirements: [AGENT-01]
must_haves:
  truths:
    - "A new install of @asteasolutions/zod-to-openapi resolves to 7.3.4 (Zod v3 compatible), not v8, AND extendZodWithOpenApi(z) runs at runtime (version-pin smoke)"
    - "js-yaml + @types/js-yaml are installed and importable"
    - "lib/dashboard/contract.ts is the SINGLE source of truth: canonical Zod envelopes (Dashboard/Status/ChainAggregation/ChainHealth/InstrumentState/Health) live here as Zod, X = z.infer<...>; the hand-written DashboardResponse/StatusResponse interfaces are DELETED; the REST routes import the inferred type from here; lib/mcp-tools/contract.ts + lib/openapi/schemas.ts (Plan 04) IMPORT these SAME schemas — no re-declaration (AGENT-01 / reviewer B3 no-duplication)"
    - "extendZodWithOpenApi(z) is called exactly ONCE, in lib/dashboard/contract.ts (its natural home); lib/mcp-tools/contract.ts re-exports those schemas + adds the tool-only envelopes; an architecture test forbids a second extend call"
    - "Wave 0 test scaffolds exist and fail/red for the un-built tools, so later plans turn them green; a contract-conformance test round-trips a real /api/dashboard + /api/status response through the canonical Zod schema; a REAL-SDK test round-trips get_iteration_state through McpServer.registerTool + tools/call (found + not_found) so outputSchema/structuredContent incompatibility is caught in CI"
  artifacts:
    - path: "lib/dashboard/contract.ts"
      provides: "Canonical Zod source of truth (B3): DashboardResponseSchema/StatusResponseSchema/ChainAggregationResultSchema/ChainHealthSchema/InstrumentStateSchema/HealthResponseSchema + the single extendZodWithOpenApi(z) call; inferred TS types replace the deleted interfaces"
      min_lines: 60
    - path: "lib/mcp-tools/contract.ts"
      provides: "Tool-only envelopes (NotDeployedEnvelope, UnavailableEnvelope, AppEntryOut, ResearchEntryOut, IterationDetailOut) + dateToIso; RE-EXPORTS the canonical REST schemas from @/lib/dashboard/contract (no re-declaration, no second extend)"
      min_lines: 60
    - path: "tests/api/mcp-real-sdk.test.ts"
      provides: "REAL McpServer.registerTool + tools/call round-trip for get_iteration_state (found + not_found) — proves the single-object outputSchema is accepted by normalizeObjectSchema and structuredContent validates"
    - path: "tests/api/openapi-conformance.test.ts"
      provides: "B3 single-source proof — round-trips real /api/dashboard + /api/status responses through the canonical Zod schemas"
    - path: "tests/unit/mcp-tools-contract.test.ts"
      provides: "Asserts contract.ts schemas parse the documented envelope shapes; date round-trips as an ISO string"
  key_links:
    - from: "lib/dashboard/contract.ts"
      to: "@asteasolutions/zod-to-openapi"
      via: "extendZodWithOpenApi(z) at module load (the ONLY call site)"
      pattern: "extendZodWithOpenApi"
    - from: "lib/mcp-tools/contract.ts"
      to: "lib/dashboard/contract.ts"
      via: "re-exports DashboardResponseSchema/StatusResponseSchema/etc — no re-declaration"
      pattern: "from '@/lib/dashboard/contract'"
    - from: "app/api/dashboard/route.ts + app/api/status/route.ts"
      to: "lib/dashboard/contract.ts"
      via: "type-only import of the inferred DashboardResponse/StatusResponse (deleted interfaces replaced by z.infer)"
      pattern: "import type \\{ (DashboardResponse|StatusResponse) \\} from '@/lib/dashboard/contract'"
---

<objective>
Lay the Phase-4 foundation: install the exact OpenAPI + YAML pins (with a RUNTIME smoke, not just a version check), make `lib/dashboard/contract.ts` the TRUE single Zod-first source of truth for the REST envelopes (promote its hand-written interfaces to Zod, delete the dup interfaces, re-point the routes), add the tool-only envelopes in `lib/mcp-tools/contract.ts` that RE-EXPORT (never re-declare) the canonical schemas, and write the Wave-0 test scaffolds the downstream tool/route plans turn green — including a contract-conformance test (spec ≡ live route) and a REAL-SDK get_iteration_state round-trip (outputSchema/structuredContent compatibility).

Purpose: A single Zod-schema source of truth prevents the OpenAPI spec from drifting from tool output AND from the REST routes (the Phase-2/3 burn class). Reviewer B3: the canonical schemas must have ONE definition at their natural home (lib/dashboard/contract.ts), not a hand-written interface in parallel with a Zod copy. Pinning zod-to-openapi to 7.3.4 avoids the Zod-v4 incompatibility that breaks extendZodWithOpenApi at runtime — but a version pin is NOT runtime-compat (project burn pattern), so we also smoke-run `extendZodWithOpenApi(z); z.string().openapi({})`.
Output: package.json/pnpm-lock with pins, lib/dashboard/contract.ts (Zod-first canonical envelopes, dup interfaces deleted), re-pointed dashboard/status routes, lib/mcp-tools/contract.ts (tool envelopes + re-exports), and the Wave-0 test files incl. conformance + real-SDK.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-surface-mcp/04-RESEARCH.md
@.planning/phases/04-agent-surface-mcp/04-VALIDATION.md

<interfaces>
<!-- GROUND-TRUTH shapes the contract module wraps. Extracted from codebase — no exploration needed. -->

From lib/dashboard/contract.ts (CURRENT — TS interfaces; this plan PROMOTES these to Zod IN THIS SAME FILE and DELETES the interfaces):
```typescript
export interface DashboardResponse {
  version: 1
  app: string
  status: 'ok' | 'degraded'
  chains: ChainAggregationResult[]
  fetchedAt: string
}
export interface StatusResponse {
  version: 1
  status: 'ok' | 'degraded'
  build: string
  timestamp: string
  chains: ChainHealth[]
  apps: Record<string, { status: string; instrumentsDeployed: number }>
}
```

CURRENT consumers of those interfaces (re-point both to the inferred type, type-only):
```typescript
// app/api/dashboard/route.ts: import type { DashboardResponse } from '@/lib/dashboard/contract'; const body: DashboardResponse = {...}
// app/api/status/route.ts:    import type { StatusResponse }    from '@/lib/dashboard/contract'; const body: StatusResponse = {...}
```

From lib/dashboard/aggregator.ts:
```typescript
export interface InstrumentState {
  id: string; name: string; nameEn: string; address: string
  poolBalance: string | null; settlementCount: string | null; lpPositionCount: string | null
}
export interface ChainAggregationResult {
  chainId: number; chainName: string
  status: 'healthy' | 'degraded' | 'empty'
  instruments: InstrumentState[]
  lastBlockSynced: string | null; error?: string; fetchedAt: string
}
```

From lib/status/health.ts:
```typescript
export interface ChainHealth { chainId: number; name: string; status: 'healthy'|'degraded'; blockNumber?: string; latencyMs?: number; error?: string }
```

From lib/apps/registry.ts (GROUND TRUTH — there is NO `description` field; it is `description_key`):
```typescript
export type AppStatus = 'active' | 'coming-soon' | 'archived'
export interface AppEntry {
  slug: string            // 'abrigo'
  name: string            // 'Abrigo'
  description_key: string // 'apps.abrigo.description'  ← an i18n KEY, never prose
  status: AppStatus
  internal_path: `/apps/${string}`
  external_url?: string   // 'https://x.com/d2pfinabrigo' (optional)
}
export const apps: readonly AppEntry[]
```

From lib/velite-shim.ts (GROUND TRUTH — `date` is a Date INSTANCE at runtime, not a string):
```typescript
// research[].date is `new Date(r.date)` — a Date object. Any tool emitting it MUST
// normalize to ISO: row.date instanceof Date ? row.date.toISOString() : String(row.date)
```

From .velite/research.json (GROUND TRUTH — 4 distinct slugs × 2 locales = 8 rows):
```typescript
// EXISTING fields: slug, title_es, title_en, authors, date, type, track, readable_on_site,
//   summary_es, summary_en, abstract_es?, abstract_en?, tags, order, body, toc, locale
// SOMETIMES present: external_url (the notebook/analytics link), arxiv_id (cfmm rows only)
// ABSENT on EVERY row: beta, pValue, version, replication_hash, notebook_url, pdf_url
// Tracks present: 'abrigo-hedge-design' (3 slugs), 'cfmm-microstructure' (1 slug)
```

From @modelcontextprotocol/sdk/server/zod-compat.js (GROUND TRUTH — why a union outputSchema breaks):
```typescript
// normalizeObjectSchema(schema): for a Zod-v3 schema it returns `schema` ONLY when `schema.shape !== undefined`.
// A ZodObject HAS `.shape`. A ZodUnion does NOT → normalizeObjectSchema returns `undefined`.
// Consequence in mcp.js validateToolOutput: `safeParseAsync(undefined, structuredContent)` →
//   TypeError: Cannot read properties of undefined (reading '_zod'). Output schemas MUST be a single ZodObject.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install OpenAPI + YAML pins + runtime smoke</name>
  <read_first>
    - package.json (current deps: mcp-handler ^1.1.0, @modelcontextprotocol/sdk ^1.29.0, zod ^3.25.76; no zod-to-openapi, no js-yaml)
    - 04-RESEARCH.md § "Standard Stack" and § "Common Pitfalls / Pitfall 3" (Zod v4 incompatibility)
  </read_first>
  <action>
    Run exactly: `pnpm add @asteasolutions/zod-to-openapi@7.3.4 js-yaml && pnpm add -D @types/js-yaml`
    Do NOT run `pnpm add @asteasolutions/zod-to-openapi` unpinned — the npm `latest` tag points to v8.5.0 which requires Zod v4 and breaks `extendZodWithOpenApi(z)` at runtime (project is on Zod v3.25.76). The `7.3.4` pin is mandatory.
    `openapi3-ts@^4.1.2` arrives automatically as a peer dep of zod-to-openapi@7.3.4 — do not install it separately.
    After install, run BOTH a version check AND a RUNTIME smoke (M6 — a version pin is not runtime-compat; this is the documented project burn pattern): the verify command below loads `extendZodWithOpenApi`, calls it on `z`, and confirms `z.string().openapi({})` does not throw.
  </action>
  <verify>
    <automated>node -e "const v=require('@asteasolutions/zod-to-openapi/package.json').version; if(v!=='7.3.4'){console.error('WRONG VERSION '+v);process.exit(1)} require('js-yaml'); const {extendZodWithOpenApi}=require('@asteasolutions/zod-to-openapi'); const {z}=require('zod'); extendZodWithOpenApi(z); z.string().openapi({description:'smoke'}); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains `"@asteasolutions/zod-to-openapi": "7.3.4"` (exact, no caret)
    - `package.json` dependencies contains `"js-yaml"`
    - `package.json` devDependencies contains `"@types/js-yaml"`
    - the verify command exits 0 (version is 7.3.4 AND `extendZodWithOpenApi(z)` + `.openapi()` run without throwing)
  </acceptance_criteria>
  <done>zod-to-openapi@7.3.4 + js-yaml + @types/js-yaml installed; version-pin AND runtime-smoke verification command exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canonical Zod in lib/dashboard/contract.ts (B3 true unification) + tool envelopes re-exporting it (AGENT-01)</name>
  <files>lib/dashboard/contract.ts, app/api/dashboard/route.ts, app/api/status/route.ts, lib/mcp-tools/contract.ts, tests/unit/mcp-tools-contract.test.ts</files>
  <read_first>
    - lib/dashboard/contract.ts (DashboardResponse / StatusResponse version:1 interfaces to PROMOTE to Zod in this file and DELETE)
    - app/api/dashboard/route.ts + app/api/status/route.ts (the two `import type` consumers to re-point)
    - lib/dashboard/aggregator.ts (ChainAggregationResult / InstrumentState shapes)
    - lib/status/health.ts (ChainHealth shape)
    - lib/apps/registry.ts (AppEntry — `description_key`, NOT `description`; `external_url` optional)
    - lib/velite-shim.ts (research[].date is a Date instance — ISO-normalize at the tool boundary)
    - .velite/research.json (NO beta/pValue/version/replication_hash/notebook_url fields)
    - node_modules/@modelcontextprotocol/sdk/dist/esm/server/zod-compat.js `normalizeObjectSchema` (object-only — informs Task placement: REST output schemas are ZodObjects, fine)
    - 04-RESEARCH.md § "Pattern 4: OpenAPI Generation from Zod" + § "Code Examples / @asteasolutions/zod-to-openapi v7 pattern"
    - 04-CONTEXT.md § decisions (not_deployed / unavailable envelopes)
  </read_first>
  <behavior>
    - After importing lib/dashboard/contract.ts, `z.string().openapi` is a function (extendZodWithOpenApi ran exactly once, HERE)
    - `DashboardResponseSchema.parse({ version:1, app:'abrigo', status:'ok', chains:[], fetchedAt:'2026-05-29T00:00:00.000Z' })` succeeds; `type DashboardResponse = z.infer<typeof DashboardResponseSchema>` is structurally identical to the old interface, so `app/api/dashboard/route.ts` still typechecks with zero body changes
    - `StatusResponseSchema.parse({ version:1, status:'ok', build:'abc', timestamp:'2026-05-29T00:00:00.000Z', chains:[], apps:{} })` succeeds; the route's `const body: StatusResponse = {...}` still typechecks
    - After importing lib/mcp-tools/contract.ts, the re-exported `DashboardResponseSchema` is referentially the SAME object as the one from lib/dashboard/contract.ts (`import('@/lib/mcp-tools/contract').DashboardResponseSchema === import('@/lib/dashboard/contract').DashboardResponseSchema`) — proves no re-declaration
    - `NotDeployedEnvelope.parse({ status:'not_deployed', instrument_id:'x', chain:'celo', terms:null, pool:null, note:'…' })` succeeds
    - `NotDeployedEnvelope.parse({ status:'not_deployed', instrument_id:'x', chain:'celo', terms:{}, pool:null, note:'…' })` FAILS — terms must be the null literal (anti-fishing: no fabricated terms object)
    - `UnavailableEnvelope.parse({ status:'unavailable', app:'abrigo', panel:'p', note:'…' })` succeeds
    - `AppEntryOut.parse({ slug:'abrigo', name:'Abrigo', status:'active', external_url:'https://x.com/d2pfinabrigo' })` succeeds — NO `description` key required; `description` is NOT in the schema (registry has `description_key` only). `AppEntryOut.parse({ slug:'abrigo', name:'Abrigo', status:'active', external_url:null, description_key:'apps.abrigo.description' })` also succeeds (description_key OPTIONAL)
    - `ResearchEntryOut.parse({ slug:'pair-d-dispatch-brief', title_es:'…', title_en:'…', type:'decision-memo', track:'abrigo-hedge-design', date:'2026-01-01T00:00:00.000Z', authors:['…'], summary_es:'…', summary_en:'…', external_url:'https://github.com/…', arxiv_id:null })` succeeds — `date` is an ISO STRING here (NO beta/pValue/version/replication_hash/notebook_url keys in the schema)
    - DATE ROUND-TRIP (B1): given a real `new Date('2026-04-30T00:00:00.000Z')`, the normalizer `dateToIso(d) = d instanceof Date ? d.toISOString() : String(d)` returns `'2026-04-30T00:00:00.000Z'`, and `ResearchEntryOut.parse({...date: dateToIso(new Date('2026-04-30T00:00:00.000Z'))})` succeeds; passing a raw `Date` object as `date` FAILS the parse (proves the boundary is real, not bypassed)
  </behavior>
  <action>
    STEP A — Promote lib/dashboard/contract.ts to Zod-first (B3 single source of truth, natural home).
    At top (the ONLY extend call site in the codebase):
    `import { extendZodWithOpenApi } from '@asteasolutions/zod-to-openapi'; import { z } from 'zod'; extendZodWithOpenApi(z)`.
    Add a top-of-file comment: "SINGLE SOURCE OF TRUTH (AGENT-01 / reviewer B3). extendZodWithOpenApi(z) is called ONLY here. lib/mcp-tools/contract.ts re-exports these schemas; lib/openapi/schemas.ts (Plan 04) imports them. NO module re-declares them or re-calls extendZodWithOpenApi."
    Define + export the canonical REST schemas (these were the hand-written interfaces — now Zod, with `.openapi()` annotations so Plan 04's generator consumes them directly):
      - `ChainHealthSchema` = z.object({ chainId: z.number(), name: z.string(), status: z.enum(['healthy','degraded']), blockNumber: z.string().optional(), latencyMs: z.number().optional(), error: z.string().optional() })
      - `InstrumentStateSchema` = z.object({ id: z.string(), name: z.string(), nameEn: z.string(), address: z.string(), poolBalance: z.string().nullable(), settlementCount: z.string().nullable(), lpPositionCount: z.string().nullable() })
      - `ChainAggregationResultSchema` = z.object({ chainId: z.number(), chainName: z.string(), status: z.enum(['healthy','degraded','empty']), instruments: z.array(InstrumentStateSchema), lastBlockSynced: z.string().nullable(), error: z.string().optional(), fetchedAt: z.string() })
      - `DashboardResponseSchema` = z.object({ version: z.literal(1), app: z.string(), status: z.enum(['ok','degraded']), chains: z.array(ChainAggregationResultSchema), fetchedAt: z.string() })
      - `StatusResponseSchema` = z.object({ version: z.literal(1), status: z.enum(['ok','degraded']), build: z.string(), timestamp: z.string(), chains: z.array(ChainHealthSchema), apps: z.record(z.object({ status: z.string(), instrumentsDeployed: z.number() })) })
      - `HealthResponseSchema` = z.object({ status: z.string(), build: z.string(), runtime: z.string(), timestamp: z.string() }) — note: live /api/health returns `runtime: 'node'` (M5); Plan 04's example MUST use `'node'`.
    Add `.openapi({ description: '…' })` on each. Then export the inferred types, REPLACING the deleted interfaces — keep the SAME names so consumers don't change:
      `export type DashboardResponse = z.infer<typeof DashboardResponseSchema>` and `export type StatusResponse = z.infer<typeof StatusResponseSchema>` (also export InstrumentState/ChainAggregationResult/ChainHealth inferred types if any consumer imports them — grep first; aggregator.ts/health.ts keep their own interfaces, so re-export only what was previously imported from contract.ts).
    DELETE the old `export interface DashboardResponse {...}` and `export interface StatusResponse {...}` blocks and the now-unused `import type { ChainAggregationResult } from '@/lib/dashboard/aggregator'` / `import type { ChainHealth } from '@/lib/status/health'` lines (the Zod schemas are self-contained). The `z.infer` types are structurally identical to the deleted interfaces, so the routes keep compiling.

    STEP B — Re-point the two routes (type-only, zero runtime cost).
    `app/api/dashboard/route.ts`: keep `import type { DashboardResponse } from '@/lib/dashboard/contract'` (now resolves to the inferred type) — no body change needed because the inferred type is identical.
    `app/api/status/route.ts`: keep `import type { StatusResponse } from '@/lib/dashboard/contract'` (now the inferred type). If `tsc` flags any drift between the literal route body and the inferred type, fix the route body to match the schema (e.g. `version` must be the literal `1`). Run `pnpm exec tsc --noEmit` to confirm both routes still compile.

    STEP C — lib/mcp-tools/contract.ts = tool-only envelopes + RE-EXPORT of the canonical schemas (no re-declaration, no inverted dependency).
    Do NOT call extendZodWithOpenApi here and do NOT re-declare the REST schemas. Instead:
    `import { z } from 'zod'` and re-export the canonical schemas/types so every existing `from '@/lib/mcp-tools/contract'` import site keeps working:
    `export { DashboardResponseSchema, StatusResponseSchema, ChainAggregationResultSchema, ChainHealthSchema, InstrumentStateSchema, HealthResponseSchema, type DashboardResponse, type StatusResponse } from '@/lib/dashboard/contract'`.
    (Because lib/dashboard/contract.ts already ran extendZodWithOpenApi at module load, importing it here guarantees `z` is extended before any tool annotates a schema — no second call needed.)
    Export a boundary helper: `export const dateToIso = (d: Date | string): string => (d instanceof Date ? d.toISOString() : String(d))` — every tool emitting a research date routes through this (B1).
    Export the tool-only envelopes + inferred types:
      - `NotDeployedEnvelope` = z.object({ status: z.literal('not_deployed'), instrument_id: z.string(), chain: z.string(), terms: z.null(), pool: z.null(), note: z.string() }) — terms/pool are `z.null()` LITERALS (anti-fishing).
      - `UnavailableEnvelope` = z.object({ status: z.literal('unavailable'), app: z.string(), panel: z.string(), note: z.string() }).
      - `AppEntryOut` = z.object({ slug: z.string(), name: z.string(), status: z.enum(['active','coming-soon','archived']), external_url: z.string().nullable(), description_key: z.string().optional() }) — B2: mirrors registry AppEntry. NO `description` field; never emit a raw i18n key as prose.
      - `ResearchEntryOut` = z.object({ slug: z.string(), title_es: z.string(), title_en: z.string(), type: z.enum(['paper','decision-memo','write-up','talk']), track: z.enum(['cfmm-microstructure','abrigo-hedge-design','notes']), date: z.string(), authors: z.array(z.string()), summary_es: z.string(), summary_en: z.string(), external_url: z.string().nullable(), arxiv_id: z.string().nullable() }) — `date` is z.string() (ISO; tools normalize via dateToIso). NO beta/pValue/version keys (descoped ITER-* exercise, 2026-05-13 IA correction). (The `'notes'` track is a v1 superset — currently zero rows; harmless.)
      - `IterationDetailOut` = `ResearchEntryOut.extend({ body: z.string().nullable() })` — M2: NO `replication_hash`/`notebook_url` (absent on every research row). Comment: "replication_hash/notebook_url do NOT exist in the on-site data model (IA correction); external_url is the notebook link, arxiv_id the citable id."
    Top-of-file comment: "Tool output envelopes. Canonical REST schemas (Dashboard/Status/Chain*/Instrument*/Health) live in @/lib/dashboard/contract and are RE-EXPORTED here — never re-declared. extendZodWithOpenApi(z) is NOT called here (owned by dashboard/contract.ts, M6). list_iterations/get_iteration_state map to the Velite `research` collection per the 2026-05-13 IA correction; β/p-value/version/replication_hash/notebook_url do not exist and are intentionally absent — never fabricate them."

    STEP D — tests/unit/mcp-tools-contract.test.ts (`// @vitest-environment node` first line). Cover every <behavior> bullet, INCLUDING: the canonical REST schema parses; the re-export referential-identity check (mcp-tools re-export `===` dashboard/contract source); the date round-trip using a real `new Date(...)`; the AppEntryOut no-`description` case.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && vitest run tests/unit/mcp-tools-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/dashboard/contract.ts` contains the literal `extendZodWithOpenApi(z)` (exactly once in the codebase) and exports `DashboardResponseSchema`, `StatusResponseSchema`, `ChainAggregationResultSchema`, `ChainHealthSchema`, `InstrumentStateSchema`, `HealthResponseSchema`, plus `type DashboardResponse = z.infer<...>` and `type StatusResponse = z.infer<...>`
    - `lib/dashboard/contract.ts` no longer contains `export interface DashboardResponse` or `export interface StatusResponse` (grep no match — duplicate interfaces deleted)
    - `lib/mcp-tools/contract.ts` does NOT contain `extendZodWithOpenApi` (grep no match) and does NOT contain `const DashboardResponseSchema =` (re-exported, not re-declared); it DOES contain `from '@/lib/dashboard/contract'`
    - `lib/mcp-tools/contract.ts` exports `NotDeployedEnvelope`, `UnavailableEnvelope`, `AppEntryOut`, `ResearchEntryOut`, `IterationDetailOut`, `dateToIso` and re-exports `DashboardResponseSchema`/`StatusResponseSchema` (grep each)
    - `app/api/dashboard/route.ts` + `app/api/status/route.ts` still `import type { … } from '@/lib/dashboard/contract'` (now the inferred type); `pnpm exec tsc --noEmit` exits 0
    - neither contract file contains the substrings `description:` inside AppEntryOut, nor `beta`, `pValue`, `p_value`, `replication_hash`, `notebook_url`
    - `vitest run tests/unit/mcp-tools-contract.test.ts` exits 0
  </acceptance_criteria>
  <done>lib/dashboard/contract.ts is the single Zod source (interfaces deleted, routes re-pointed, extend called once); lib/mcp-tools/contract.ts adds the tool envelopes + dateToIso and re-exports the canonical schemas with no re-declaration; tsc + unit test pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test scaffolds (conformance + REAL-SDK get_iteration_state) + re-point SSE fixme</name>
  <files>tests/api/mcp.test.ts, tests/api/openapi.test.ts, tests/api/openapi-conformance.test.ts, tests/api/mcp-real-sdk.test.ts, tests/architecture/mcp-no-duplication.test.ts, tests/e2e/agent-stubs.spec.ts</files>
  <read_first>
    - tests/e2e/agent-stubs.spec.ts (current file — contains `test.fixme('/api/mcp/sse route exists (no 404)')` at line ~38 to re-point)
    - tests/api/dashboard.test.ts (the `// @vitest-environment node` route-handler test pattern to copy; reuse it for the conformance test — it already drives GET on /api/dashboard)
    - app/api/mcp/[transport]/route.ts (current stub — handler exported as GET/POST/DELETE)
    - app/api/status/route.ts (the live /api/status handler — the conformance test drives it)
    - node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.js (McpServer, registerTool, connect; validateToolOutput ~L185) + the SDK's in-memory transport export (search `InMemoryTransport` under @modelcontextprotocol/sdk/dist/esm/inMemory.js) and Client under @modelcontextprotocol/sdk/dist/esm/client/index.js
    - 04-VALIDATION.md § "Wave 0 Requirements" and § "Per-Task Verification Map" (AGENT-02 + AGENT-05 + AGENT-08 conformance expectations)
  </read_first>
  <action>
    Create SIX test scaffolds. They reference tools/routes/schemas that do not exist yet (or whose route is unwired), so they are RED now and turned GREEN by Plans 02–05. Mark assertions that need an un-built route/tool with `test.fixme(...)` so `pnpm test:quick` stays green for Plan 01's own commit. Add a top comment in each: "Wave 0 scaffold — fixme assertions are un-fixme'd by Plan 0X."

    1. `tests/api/mcp.test.ts` (`// @vitest-environment node`): import `{ POST, GET }` from `@/app/api/mcp/[transport]/route`. `test.fixme('POST /api/mcp/mcp initialize returns 200 JSON-RPC result')` — builds a JSON-RPC `initialize` Request to `http://localhost/api/mcp/mcp`, asserts 200. `test.fixme('tools/list returns all six tool names')` asserting the array contains `list_apps`,`list_iterations`,`get_iteration_state`,`get_instrument_terms`,`get_pool_state`,`query_econometric_panel`. `test.fixme('GET /api/mcp/sse returns 404 (disableSse)')` asserting status 404. (Plan 05 un-fixmes these.)

    2. `tests/api/openapi.test.ts` (`// @vitest-environment node`): import the generator from `@/lib/openapi/generate` (created by Plan 04). `test.fixme('openapi.yaml contains openapi: 3.1.0 and all four endpoint paths')` — calls `generateOpenApiYaml()`, asserts the string contains `openapi: 3.1.0`, `/api/dashboard`, `/api/status`, `/api/health`, `/api/mcp`. (Plan 04 un-fixmes.)

    3. `tests/api/openapi-conformance.test.ts` (`// @vitest-environment node`) — B3 SINGLE-SOURCE PROOF. Import `{ DashboardResponseSchema, StatusResponseSchema }` from `@/lib/mcp-tools/contract` (these exist NOW via re-export from dashboard/contract, from Task 2) and the live route handlers `{ GET as dashboardGET }` from `@/app/api/dashboard/route`, `{ GET as statusGET }` from `@/app/api/status/route`. `test.fixme('live /api/dashboard response conforms to the canonical Zod schema')` — call the handler, `await res.json()`, assert `DashboardResponseSchema.parse(body)` does not throw. Same for `/api/status` with `StatusResponseSchema`. Mark `test.fixme` for now ONLY IF the live routes are not callable in the node test env without setup; otherwise keep them LIVE (they short-circuit on the empty registry, no network). Prefer LIVE. (If the dashboard handler requires a Request with searchParams, pass `new Request('http://localhost/api/dashboard?app=abrigo')`.)

    4. `tests/api/mcp-real-sdk.test.ts` (`// @vitest-environment node`) — NEW-BLOCKER guard. This is the REAL-SDK round-trip that the fake-server unit tests cannot catch: it exercises the actual `McpServer.registerTool` + `validateToolOutput` path so a non-ZodObject outputSchema (which `normalizeObjectSchema` turns into `undefined`) fails LOUDLY in CI. Import `{ McpServer } from '@modelcontextprotocol/sdk/server/mcp.js'`, `{ Client } from '@modelcontextprotocol/sdk/client/index.js'`, `{ InMemoryTransport } from '@modelcontextprotocol/sdk/inMemory.js'` (confirm the exact export path while reading; fall back to `.../dist/esm/inMemory.js` if the package export map differs), and `{ registerGetIterationState } from '@/lib/mcp-tools/get-iteration-state'` (created by Plan 02). `test.fixme('get_iteration_state tools/call round-trips a FOUND slug through the real SDK (single-object outputSchema accepted, structuredContent validates)')` — create a `new McpServer({ name:'test', version:'0' })`, `registerGetIterationState(server)`, create a linked `InMemoryTransport` pair, connect server + a `Client`, then `await client.callTool({ name:'get_iteration_state', arguments:{ slug:'pair-d-dispatch-brief' } })`; assert NO throw, and the result's `structuredContent.status === 'found'` with a non-null `detail`. `test.fixme('get_iteration_state tools/call round-trips a NOT-FOUND slug')` — same setup, `arguments:{ slug:'does-not-exist' }`; assert NO throw and `structuredContent.status === 'not_found'` (and `detail === null`). Add a comment: "Guards the union-outputSchema blocker: a ZodUnion outputSchema → normalizeObjectSchema undefined → TypeError in validateToolOutput on EVERY call. The single-object discriminated outputSchema must keep this green. Plan 02 un-fixmes after get_iteration_state ships."

    5. `tests/architecture/mcp-no-duplication.test.ts` (Vitest, node): reads each `lib/mcp-tools/*.ts` source with `fs.readFileSync` and asserts NONE contains an internal `await fetch(` pointing at `/api/`. ALSO assert (live) that the ONLY file containing `extendZodWithOpenApi(` is `lib/dashboard/contract.ts` (read the repo's lib/**/*.ts; assert exactly one match and it is in dashboard/contract.ts — M6 single extend), and that `lib/openapi/schemas.ts`, when it exists, does NOT contain `extendZodWithOpenApi(`; tolerate that file not existing yet (skip if absent). ALSO assert that `lib/mcp-tools/contract.ts` does NOT contain `const DashboardResponseSchema =` (re-exported, not re-declared — B3 no-duplication). `test.fixme` the assertion that `lib/mcp-tools/index.ts` re-exports the six `registerXxx` functions (index.ts created by Plan 05). Keep the "no internal fetch" glob assertion live but tolerant of zero files today.

    6. `tests/e2e/agent-stubs.spec.ts`: REPLACE the `test.fixme('/api/mcp/sse route exists (no 404)')` block with TWO tests still marked `test.fixme` (Plan 05 un-fixmes): `test.fixme('/api/mcp/mcp streamable-http handshake — POST initialize is not 404')` (POST a JSON-RPC initialize, assert `r.status()` !== 404) and `test.fixme('/api/mcp/sse returns 404 (disableSse, no Redis)')` (GET, assert `r.status()` === 404). Keep the existing llms.txt / mcp.json / openapi.yaml passing tests untouched. Update the FIXME comment to reference `disableSse: true` instead of "provision Redis".
  </action>
  <verify>
    <automated>pnpm test:quick</automated>
  </verify>
  <acceptance_criteria>
    - `tests/api/mcp.test.ts` exists and contains the strings `list_apps` and `disableSse`
    - `tests/api/openapi.test.ts` exists and contains `openapi: 3.1.0`
    - `tests/api/openapi-conformance.test.ts` exists and contains `DashboardResponseSchema` and `StatusResponseSchema`
    - `tests/api/mcp-real-sdk.test.ts` exists and contains `McpServer`, `get_iteration_state`, `structuredContent`, and both `'found'` and `'not_found'`
    - `tests/architecture/mcp-no-duplication.test.ts` exists, contains `mcp-tools`, asserts a single `extendZodWithOpenApi(` (in dashboard/contract.ts), and asserts no `const DashboardResponseSchema =` in `lib/mcp-tools/contract.ts`
    - `tests/e2e/agent-stubs.spec.ts` no longer contains `provision/mock Redis`; contains `disableSse` and `/api/mcp/mcp`
    - `pnpm test:quick` exits 0
  </acceptance_criteria>
  <done>Six Wave-0 scaffolds committed (B3 conformance proof + REAL-SDK get_iteration_state round-trip); SSE fixme re-pointed to streamable-http + disableSse; `pnpm test:quick` green.</done>
</task>

</tasks>

<verification>
- `pnpm test:quick` exits 0
- `pnpm exec tsc --noEmit` exits 0 (routes re-pointed to inferred types still compile)
- version+runtime smoke command prints `ok` (7.3.4 AND extendZodWithOpenApi runs)
- `lib/dashboard/contract.ts` is the single Zod source (interfaces deleted, extend called once); `lib/mcp-tools/contract.ts` re-exports + adds tool envelopes + dateToIso; omits β/p-value/replication-hash/notebook-url; AppEntryOut has no `description`
- All six Wave-0 test files exist; agent-stubs.spec.ts SSE fixme re-pointed; conformance + real-SDK tests reference the canonical schemas / real McpServer
</verification>

<success_criteria>
- zod-to-openapi pinned to 7.3.4 (NOT v8) AND runtime-verified; js-yaml installed
- lib/dashboard/contract.ts is the TRUE single Zod source of truth (B3 — one definition at its natural home, dup interfaces deleted, routes re-pointed); lib/mcp-tools/contract.ts re-exports it (no inverted dependency, no re-declaration); extendZodWithOpenApi called once
- AppEntryOut matches the real registry (description_key, no fabricated description — B2); ResearchEntryOut/IterationDetailOut carry NO β/p-value/version/replication-hash/notebook-url (M2, IA-correction-honest); date normalized via dateToIso (B1)
- Wave-0 scaffolds red-via-fixme + the conformance proof + the REAL-SDK get_iteration_state guard, ready for Plans 02–05
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-surface-mcp/04-01-SUMMARY.md`
</output>
</content>
</invoke>

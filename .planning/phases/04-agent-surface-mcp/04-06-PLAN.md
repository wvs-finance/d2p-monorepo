---
phase: 04-agent-surface-mcp
plan: 06
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - components/AgentStateJsonLd.tsx
  - app/(apps)/apps/abrigo/dashboard/page.tsx
  - tests/unit/agent-jsonld.test.tsx
  - tests/e2e/agent-stubs.spec.ts
autonomous: false
requirements: [AGENT-10]
must_haves:
  truths:
    - "The Abrigo dashboard page emits JSON-LD whose fields mirror the MCP tool output schema (the not_deployed/pool-state shape) — an agent can extract protocol state from the HTML without a tool call"
    - "The JSON-LD reflects the honest pre-launch state (status not_deployed / empty) — no fabricated balances in structured data, consistent with anti-fishing"
    - "JSON-LD is XSS-escaped via the established escapeJsonLd pattern and the html string is pre-built before the JSX return (biome noDangerouslySetInnerHtml constraint)"
  artifacts:
    - path: "components/AgentStateJsonLd.tsx"
      provides: "Server component rendering SoftwareApplication/Dataset JSON-LD that mirrors tool output fields"
      min_lines: 30
    - path: "app/(apps)/apps/abrigo/dashboard/page.tsx"
      provides: "Dashboard page rendering <AgentStateJsonLd> with the aggregation result"
  key_links:
    - from: "app/(apps)/apps/abrigo/dashboard/page.tsx"
      to: "components/AgentStateJsonLd.tsx"
      via: "render <AgentStateJsonLd app='abrigo' chains={results} />"
      pattern: "AgentStateJsonLd"
    - from: "components/AgentStateJsonLd.tsx"
      to: "MCP tool output schema (lib/mcp-tools/contract.ts shapes)"
      via: "JSON-LD additionalProperty fields mirror get_instrument_terms/get_pool_state status fields"
      pattern: "not_deployed"
---

<objective>
Emit JSON-LD on the Abrigo dashboard page whose fields mirror the MCP tool output schema (AGENT-10), so a crawler/agent can read live protocol state from the HTML without an MCP tool call — and have that structured data honestly reflect the pre-launch `not_deployed`/`empty` state with no fabricated numbers.

Purpose: Closes the agent-accessibility loop — the human page, the MCP tool, and the structured data all carry the same honest shape. Reuses the established escapeJsonLd + pre-built-html-string pattern (biome constraint, Plan 02-05 decision).
Output: AgentStateJsonLd component, dashboard page wiring, unit + e2e tests, Evidence-Collector route gate.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-agent-surface-mcp/04-RESEARCH.md

<interfaces>
From components/StructuredData.tsx (the established escapeJsonLd pattern to follow):
```typescript
function escapeJsonLd(obj: object): string  // JSON.stringify then escape < → <
// biome-ignore lint/security/noDangerouslySetInnerHtml on a SINGLE-LINE <script> JSX element;
// pre-build the html string as const BEFORE the JSX return (Plan 02-05 decision — multi-line JSX can't be suppressed)
```
From app/(apps)/apps/abrigo/dashboard/page.tsx (current):
```typescript
import { aggregateAllChains } from '@/lib/dashboard/aggregator'
export const dynamic = 'force-dynamic'; export const runtime = 'nodejs'
export default async function DashboardPage({ ... }) { /* renders ChainSelector + DashboardContent */ }
// aggregateAllChains() returns ChainAggregationResult[] — every chain status:'empty' pre-launch
```
From lib/mcp-tools/contract.ts (Plan 01) — the shapes JSON-LD must mirror:
```typescript
NotDeployedEnvelope { status:'not_deployed', instrument_id, chain, terms:null, pool:null, note }
ChainAggregationResult { chainId, chainName, status:'healthy'|'degraded'|'empty', instruments, lastBlockSynced }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentStateJsonLd component + wire into dashboard (AGENT-10)</name>
  <files>components/AgentStateJsonLd.tsx, app/(apps)/apps/abrigo/dashboard/page.tsx, tests/unit/agent-jsonld.test.tsx</files>
  <read_first>
    - components/StructuredData.tsx (escapeJsonLd + biome suppression + pre-built-html pattern)
    - app/(apps)/apps/abrigo/dashboard/page.tsx (where to render the JSON-LD; aggregateAllChains usage)
    - lib/mcp-tools/contract.ts (NotDeployedEnvelope / aggregation shapes to mirror)
    - lib/dashboard/aggregator.ts (ChainAggregationResult fields)
    - 04-RESEARCH.md § "Pattern 5: JSON-LD Mirroring Tool Output" + STATE.md Plan 02-05 decision (JSON-LD html extracted to const before JSX return)
  </read_first>
  <behavior>
    - AgentStateJsonLd given `{ app:'abrigo', chains:[{ chainId:42220, chainName:'Celo', status:'empty', instruments:[], lastBlockSynced:null }] }` renders a single `<script type="application/ld+json">` whose JSON parses to a `SoftwareApplication` (or `Dataset`) object with `name:'Abrigo'`, `applicationCategory:'FinanceApplication'`, and `additionalProperty` entries mirroring tool output: at least `{ '@type':'PropertyValue', name:'status', value:'not_deployed' }` while all chains are empty
    - The emitted JSON contains NO fabricated numeric pool balance (assert the script text does not contain a numeric `poolBalance` value; null/absent only)
    - The html is XSS-escaped: a `<` in any field becomes `<` (escapeJsonLd)
  </behavior>
  <action>
    `components/AgentStateJsonLd.tsx`: server component (no 'use client'). Props `{ app: string; chains: ChainAggregationResult[] }` (import the type from `@/lib/dashboard/aggregator`). Derive `const anyDeployed = chains.some((c) => c.instruments.length > 0)` and `const overallStatus = anyDeployed ? 'live' : 'not_deployed'`. Build a `SoftwareApplication` JSON-LD object: `{ '@context':'https://schema.org', '@type':'SoftwareApplication', name:'Abrigo', applicationCategory:'FinanceApplication', description:'Convex hedges for Colombian wage-earners — ∂²Π gamma', additionalProperty:[ { '@type':'PropertyValue', name:'status', value: overallStatus }, { '@type':'PropertyValue', name:'chainsConfigured', value: String(chains.length) }, ...per-chain { name:`chain:${c.chainName}`, value: c.status } ] }`. Reuse the `escapeJsonLd` approach from StructuredData.tsx (copy the small helper or import if exported). Pre-build `const html = escapeJsonLd(jsonLd)` as a const BEFORE the return; render `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: html }} />` on a SINGLE line with the `// biome-ignore lint/security/noDangerouslySetInnerHtml: …` comment directly above (per the Plan 02-05 decision). Do NOT fabricate balances — only echo `status`/`empty` states.
    Wire into `app/(apps)/apps/abrigo/dashboard/page.tsx`: the page already calls (or should call) `aggregateAllChains()` to render the dashboard — capture `const results = await aggregateAllChains()` and render `<AgentStateJsonLd app="abrigo" chains={results} />` near the top of the returned JSX (before/alongside DashboardContent). If the page currently aggregates inside DashboardContent, lift the call (or add a second call — it short-circuits on the empty registry, so cost is negligible) so the JSON-LD reflects the same data.
    `tests/unit/agent-jsonld.test.tsx`: render `<AgentStateJsonLd>` with the empty-chains fixture, extract the script innerHTML, `JSON.parse` it, assert per <behavior>. Use the existing structured-data test pattern (note tests/unit/structured-data.test.tsx is excluded from tsconfig but run by vitest — follow that pattern; .tsx extension required).
  </action>
  <verify>
    <automated>vitest run tests/unit/agent-jsonld.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `components/AgentStateJsonLd.tsx` contains `application/ld+json`, `SoftwareApplication`, `additionalProperty`, `not_deployed`, and a `biome-ignore lint/security/noDangerouslySetInnerHtml` comment
    - component does NOT contain a hardcoded numeric pool balance (grep for `poolBalance` returns no fabricated value; null/status only)
    - `app/(apps)/apps/abrigo/dashboard/page.tsx` contains `AgentStateJsonLd`
    - `vitest run tests/unit/agent-jsonld.test.tsx` exits 0
  </acceptance_criteria>
  <done>Dashboard emits tool-mirroring JSON-LD reflecting the honest not_deployed state; XSS-escaped; unit test passes.</done>
</task>

<task type="auto">
  <name>Task 2: e2e assertion for dashboard JSON-LD (AGENT-10)</name>
  <files>tests/e2e/agent-stubs.spec.ts</files>
  <read_first>
    - tests/e2e/agent-stubs.spec.ts (current — has a fixme'd root JSON-LD test; add a dashboard-scoped one)
    - app/(apps)/apps/abrigo/dashboard/page.tsx (Task 1 — emits the script)
    - STATE.md Plan 03-02 decision (e2e runs against the production webpack build via playwright.config webServer)
  </read_first>
  <action>
    Add a test to `tests/e2e/agent-stubs.spec.ts`: `test('/apps/abrigo/dashboard emits SoftwareApplication JSON-LD mirroring tool output')` — `await page.goto('/apps/abrigo/dashboard')`, locate `script[type="application/ld+json"]`, find the one whose text contains `SoftwareApplication`, `JSON.parse` its content, assert `@type === 'SoftwareApplication'`, `name === 'Abrigo'`, and that `additionalProperty` contains a `status` PropertyValue equal to `not_deployed` (pre-launch). Assert the script text does NOT contain a fabricated numeric balance. Keep all existing tests green. (Do NOT un-fixme the root-page JSON-LD test — that is FOUND/StructuredData scope, out of Phase 4.)
  </action>
  <verify>
    <automated>pnpm test:quick</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/agent-stubs.spec.ts` contains `/apps/abrigo/dashboard` and `SoftwareApplication`
    - `pnpm test:quick` exits 0
  </acceptance_criteria>
  <done>e2e asserts the dashboard JSON-LD mirrors tool output with honest not_deployed status; quick suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Dashboard JSON-LD live verification (checkpoint)</name>
  <action>Run the Evidence-Collector live verification described in <how-to-verify> below; this is a manual checkpoint, no code change. Record verdicts in 04-LIVE-VERIFICATION.md.</action>
  <what-built>
    JSON-LD on /apps/abrigo/dashboard mirroring the MCP tool output schema (SoftwareApplication with status:not_deployed), honest pre-launch (no fabricated balances). This is the per-route Evidence-Collector live-verification gate from CLAUDE.md.
  </what-built>
  <how-to-verify>
    Invoke the Evidence Collector agent in live-verification mode against the dashboard route (CLAUDE.md invocation block):
      Working dir: /home/jmsbpp/apps/d2p/frontend
      Target: https://www.d2pfinance.xyz/apps/abrigo/dashboard (fall back to local `pnpm build && pnpm start -p 3040` if the deployed route 4xx/5xx)
      Task: 04-06 — dashboard JSON-LD mirroring MCP tool output (AGENT-10)
      For the route:
        1. browser_navigate to /apps/abrigo/dashboard
        2. browser_evaluate: read all script[type="application/ld+json"], parse, assert one is @type SoftwareApplication name Abrigo with additionalProperty status === 'not_deployed'
        3. Assert NO fabricated numeric pool balance appears in the JSON-LD (anti-fishing)
        4. browser_console_messages — surface runtime errors
        5. browser_take_screenshot → /tmp/d2p-verify/04-06-dashboard.png
      Append verdicts (✓/⚠/✗/⊘) + screenshot path to .planning/phases/04-agent-surface-mcp/04-LIVE-VERIFICATION.md
  </how-to-verify>
  <resume-signal>Type "approved" once the dashboard JSON-LD is observed (status not_deployed, no fabricated numbers) with a screenshot, or describe failures.</resume-signal>
</task>

</tasks>

<verification>
- `vitest run tests/unit/agent-jsonld.test.tsx` exits 0
- `pnpm test:quick` exits 0
- Live: dashboard HTML carries SoftwareApplication JSON-LD with status not_deployed, no fabricated balances (Evidence Collector verdict in 04-LIVE-VERIFICATION.md)
</verification>

<success_criteria>
- AGENT-10: dashboard JSON-LD mirrors MCP tool output schema; honest not_deployed state; XSS-escaped
- No fabricated numbers in structured data (anti-fishing)
- Evidence-Collector route gate recorded
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-surface-mcp/04-06-SUMMARY.md`
</output>

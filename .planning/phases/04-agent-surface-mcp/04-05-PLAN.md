---
phase: 04-agent-surface-mcp
plan: 05
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - lib/mcp-tools/index.ts
  - app/api/mcp/[transport]/route.ts
  - tests/api/mcp.test.ts
  - tests/architecture/mcp-no-duplication.test.ts
  - tests/e2e/agent-stubs.spec.ts
autonomous: false
requirements: [AGENT-01, AGENT-02]
must_haves:
  truths:
    - "lib/mcp-tools/index.ts barrel re-exports all six registerXxx functions; the MCP route and any future chat consumer import tools from this one module (AGENT-01 no-duplication)"
    - "POST /api/mcp/mcp completes a JSON-RPC initialize + tools/list handshake returning all six tool names (AGENT-02)"
    - "GET /api/mcp/sse returns 404 (disableSse:true) — never crashes on a missing Redis (no unhandledRejection)"
    - "The MCP route declares runtime='nodejs' so viem-backed tools work on Vercel"
  artifacts:
    - path: "lib/mcp-tools/index.ts"
      provides: "Barrel re-exporting registerListApps/ListIterations/GetIterationState/GetInstrumentTerms/GetPoolState/QueryEconometricPanel"
      exports: ["registerListApps","registerListIterations","registerGetIterationState","registerGetInstrumentTerms","registerGetPoolState","registerQueryEconometricPanel"]
    - path: "app/api/mcp/[transport]/route.ts"
      provides: "createMcpHandler registering all six tools with disableSse:true + runtime nodejs"
      contains: "disableSse: true"
  key_links:
    - from: "app/api/mcp/[transport]/route.ts"
      to: "lib/mcp-tools/index.ts"
      via: "import the six register functions from the barrel"
      pattern: "from '@/lib/mcp-tools'"
    - from: "app/api/mcp/[transport]/route.ts"
      to: "mcp-handler createMcpHandler"
      via: "third-arg config { basePath:'/api/mcp', disableSse:true }"
      pattern: "disableSse: true"
---

<objective>
Wire the six tools into the live MCP route via a single `lib/mcp-tools/index.ts` barrel (the AGENT-01 no-duplication seam), register them in `createMcpHandler` with `disableSse: true` + `runtime='nodejs'`, un-fixme the route + e2e handshake tests, and gate completion on a real streamable-http MCP handshake (per CLAUDE.md live-verification rule — the MCP surface has no rendered DOM, so verify via JSON-RPC, not browser_snapshot).

Purpose: This is the integration plan — agents can now `initialize` + `tools/list` over `/api/mcp/mcp`, and `/api/mcp/sse` returns a clean 404 instead of the Phase-3.1 Redis unhandledRejection that cascaded into unrelated timeouts.
Output: barrel, wired route, un-fixme'd mcp/architecture/e2e tests, live-handshake checkpoint.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-agent-surface-mcp/04-RESEARCH.md
@.planning/phases/04-agent-surface-mcp/04-VALIDATION.md

<interfaces>
From the six tool files (Plans 02 + 03):
```typescript
export function registerListApps(server: McpServer): void
export function registerListIterations(server: McpServer): void
export function registerGetIterationState(server: McpServer): void
export function registerGetInstrumentTerms(server: McpServer): void
export function registerGetPoolState(server: McpServer): void
export function registerQueryEconometricPanel(server: McpServer): void
```
From mcp-handler (04-RESEARCH § Pattern 3 / Code Examples):
```typescript
createMcpHandler(register: (server)=>void, serverOptions, config: { basePath:string; disableSse?:boolean })
// disableSse:true → GET /api/mcp/sse returns 404 before initializeRedis() is ever called
```
Current app/api/mcp/[transport]/route.ts: stub with empty callback, config `{ basePath:'/api/mcp' }`, exports handler as GET/POST/DELETE. No runtime declared.
Wave-0 scaffolds (Plan 01) to un-fixme: tests/api/mcp.test.ts, tests/architecture/mcp-no-duplication.test.ts (index.ts assertion), tests/e2e/agent-stubs.spec.ts (the two streamable-http/sse handshake tests).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barrel + wire MCP route (AGENT-01, AGENT-02)</name>
  <files>lib/mcp-tools/index.ts, app/api/mcp/[transport]/route.ts</files>
  <read_first>
    - the six lib/mcp-tools/*.ts files (Plans 02 + 03) for exact register-fn names
    - app/api/mcp/[transport]/route.ts (current stub; basePath comment — do NOT change basePath)
    - app/api/dashboard/route.ts (the `export const runtime = 'nodejs'` pattern to copy)
    - 04-RESEARCH.md § "Pattern 3: MCP Route Handler with disableSse" + § "Code Examples / createMcpHandler" + § "Pitfall 1 (SSE Redis)" + § "Pitfall 2 (runtime nodejs)"
  </read_first>
  <action>
    `lib/mcp-tools/index.ts`: barrel — `export { registerListApps } from './list-apps'` ... one re-export line per tool, all six. Add a top comment: "AGENT-01 single import surface — the MCP route handler AND any future chat/tool consumer (CHAT-01) import tools from here; no tool logic is duplicated."
    `app/api/mcp/[transport]/route.ts`: add `export const dynamic = 'force-dynamic'` and `export const runtime = 'nodejs'` at top (REQUIRED — tools import viem clients). Import the six register fns from `@/lib/mcp-tools` (the barrel). Replace the empty callback with one that calls all six `registerXxx(server)`. Keep `basePath: '/api/mcp'` and ADD `disableSse: true` to the third config arg (this makes `/api/mcp/sse` return 404 without ever calling `initializeRedis()`). Add a `serverInfo: { name:'d2p Finance MCP Server', version:'1.0.0' }` second arg. Keep `export { handler as GET, handler as POST, handler as DELETE }`.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm biome check app/api/mcp lib/mcp-tools</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mcp-tools/index.ts` contains all six `registerXxx` names (grep each)
    - `app/api/mcp/[transport]/route.ts` contains `disableSse: true`, `runtime = 'nodejs'`, and `from '@/lib/mcp-tools'`
    - `app/api/mcp/[transport]/route.ts` still contains `basePath: '/api/mcp'` (unchanged)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Barrel exports six tools; route registers all six with disableSse:true + runtime nodejs; typecheck + biome clean.</done>
</task>

<task type="auto">
  <name>Task 2: Un-fixme route + architecture + e2e handshake tests (AGENT-01, AGENT-02)</name>
  <files>tests/api/mcp.test.ts, tests/architecture/mcp-no-duplication.test.ts, tests/e2e/agent-stubs.spec.ts</files>
  <read_first>
    - tests/api/mcp.test.ts (Plan 01 scaffold — fixme initialize/tools-list/sse-404 tests to un-fixme)
    - tests/architecture/mcp-no-duplication.test.ts (Plan 01 scaffold — the index.ts re-export assertion to un-fixme)
    - tests/e2e/agent-stubs.spec.ts (Plan 01 — the two fixme handshake tests to un-fixme)
    - app/api/mcp/[transport]/route.ts (wired in Task 1)
    - 04-VALIDATION.md § "Per-Task Verification Map" AGENT-02 row + § "Wave 0 Requirements"
  </read_first>
  <action>
    Un-fixme the Wave-0 assertions now that the route is wired:
    1. `tests/api/mcp.test.ts`: convert the three `test.fixme(...)` to `test(...)`. The initialize test POSTs a JSON-RPC body `{ jsonrpc:'2.0', id:1, method:'initialize', params:{ protocolVersion:'2025-03-26', capabilities:{}, clientInfo:{ name:'test', version:'0' } } }` to `new Request('http://localhost/api/mcp/mcp', { method:'POST', headers:{ 'content-type':'application/json', 'accept':'application/json, text/event-stream' }, body: JSON.stringify(...) })`, calls `POST(req)`, asserts status 200. The tools/list test sends `method:'tools/list'` and asserts the parsed result's tool names include all six. The SSE test calls `GET(new Request('http://localhost/api/mcp/sse', { method:'GET' }))` (passing a `{ params: Promise.resolve({ transport:'sse' }) }` route-context if the handler signature requires it) and asserts status 404. If the streamable-http response is an SSE/event-stream body, parse the first `data:` JSON line. Keep tests robust to either a single JSON body or an event-stream framing.
    2. `tests/architecture/mcp-no-duplication.test.ts`: un-fixme the assertion that `lib/mcp-tools/index.ts` re-exports the six register fns (read the file, assert each name present). Keep the "no internal fetch to /api/ inside tool handlers" assertion live across all `lib/mcp-tools/*.ts`.
    3. `tests/e2e/agent-stubs.spec.ts`: convert the two `test.fixme` handshake tests to `test`. The streamable-http test POSTs a JSON-RPC initialize to `/api/mcp/mcp` via `request.post` and asserts `r.status()` is not 404 (200 expected). The SSE test GETs `/api/mcp/sse` and asserts `r.status() === 404`. Keep the existing llms.txt/mcp.json/openapi.yaml tests green.
  </action>
  <verify>
    <automated>vitest run tests/api/mcp.test.ts tests/architecture/mcp-no-duplication.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/api/mcp.test.ts` contains no `test.fixme` (grep no match); contains `tools/list` and `initialize`
    - `tests/architecture/mcp-no-duplication.test.ts` contains no `test.fixme` (grep no match)
    - `tests/e2e/agent-stubs.spec.ts` contains no `test.fixme` for the MCP handshake tests; contains `/api/mcp/mcp` and `404`
    - `vitest run tests/api/mcp.test.ts tests/architecture/mcp-no-duplication.test.ts` exits 0
  </acceptance_criteria>
  <done>Route/architecture vitest tests green for the wired handshake + no-duplication; e2e handshake tests un-fixme'd.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live MCP handshake verification (checkpoint)</name>
  <action>Run the live MCP handshake verification described in <how-to-verify> below; this is a manual checkpoint, no code change. Record verdicts in 04-LIVE-VERIFICATION.md.</action>
  <what-built>
    The live MCP server at /api/mcp/mcp with all six tools registered, disableSse:true (SSE→404), and runtime nodejs. AGENT-01 barrel + AGENT-02 handshake. This is the per-route live-verification gate from CLAUDE.md; the MCP surface has no rendered DOM so it is verified via a JSON-RPC handshake / curl, not browser_snapshot.
  </what-built>
  <how-to-verify>
    Build + start the production server, then drive a real JSON-RPC handshake (the project runs e2e against a local prod build):
    1. `pnpm build && pnpm start -p 3040` (separate shell)
    2. Initialize + list tools over streamable-http:
       `curl -sS -X POST http://localhost:3040/api/mcp/mcp -H 'content-type: application/json' -H 'accept: application/json, text/event-stream' -d '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}'`
       EXPECT: a JSON-RPC result whose `result.tools[].name` set equals {list_apps, list_iterations, get_iteration_state, get_instrument_terms, get_pool_state, query_econometric_panel}.
    3. Call an honest-empty tool:
       `curl -sS -X POST http://localhost:3040/api/mcp/mcp -H 'content-type: application/json' -H 'accept: application/json, text/event-stream' -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"get_pool_state","arguments":{"chain":"celo"}}}'`
       EXPECT: text content parsing to `{ status:'not_deployed', pool:null, ... }` — NO fabricated reserves.
    4. SSE returns 404 (no Redis crash):
       `curl -sS -o /dev/null -w '%{http_code}' http://localhost:3040/api/mcp/sse` → EXPECT `404`. Confirm server logs show NO `redisUrl is required` unhandledRejection.
    5. Record verdicts (✓/⚠/✗) in `.planning/phases/04-agent-surface-mcp/04-LIVE-VERIFICATION.md`. Optionally point a real MCP client (Claude Desktop/Cursor) at the deployed `/api/mcp/mcp` and confirm tools list (manual waiver per 04-VALIDATION if not deployed yet).
  </how-to-verify>
  <resume-signal>Type "approved" once tools/list returns all six and SSE is a clean 404, or describe failures (file a fix before next plan).</resume-signal>
</task>

</tasks>

<verification>
- `vitest run tests/api/mcp.test.ts tests/architecture/mcp-no-duplication.test.ts` exits 0
- Live: tools/list returns all six tool names; get_pool_state returns not_deployed (no fabricated numbers); /api/mcp/sse → 404 with no Redis unhandledRejection
- `app/api/mcp/[transport]/route.ts` contains disableSse:true + runtime nodejs
</verification>

<success_criteria>
- AGENT-01: single lib/mcp-tools barrel is the only tool import surface; no duplication
- AGENT-02: streamable-http handshake works; SSE 404 clean (no Redis)
- Live-verification gate recorded in 04-LIVE-VERIFICATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-surface-mcp/04-05-SUMMARY.md`
</output>

---
phase: 04-agent-surface-mcp
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/mcp-tools/get-instrument-terms.ts
  - lib/mcp-tools/get-pool-state.ts
  - lib/mcp-tools/query-econometric-panel.ts
  - tests/unit/mcp-tools-onchain-panel.test.ts
autonomous: true
requirements: [AGENT-06, AGENT-07-pool, AGENT-07]
must_haves:
  truths:
    - "get_instrument_terms returns a structured not_deployed envelope (terms:null) against the empty ABRIGO_INSTRUMENTS registry — never an MCP error, never fabricated terms"
    - "get_pool_state returns a structured not_deployed envelope (pool:null) against the empty registry; any bigint output serializes as a string via lib/chains/serialize.ts"
    - "query_econometric_panel returns a structured unavailable envelope (no HF dataset published) after a guarded /is-valid probe — never fabricated rows"
    - "isError:true is reserved for genuine failures (bad args, RPC down), never for the normal pre-launch empty state"
  artifacts:
    - path: "lib/mcp-tools/get-instrument-terms.ts"
      provides: "registerGetInstrumentTerms(server) — AGENT-06"
      exports: ["registerGetInstrumentTerms"]
    - path: "lib/mcp-tools/get-pool-state.ts"
      provides: "registerGetPoolState(server) — AGENT-07-pool"
      exports: ["registerGetPoolState"]
    - path: "lib/mcp-tools/query-econometric-panel.ts"
      provides: "registerQueryEconometricPanel(server) — AGENT-07"
      exports: ["registerQueryEconometricPanel"]
  key_links:
    - from: "lib/mcp-tools/get-pool-state.ts"
      to: "lib/dashboard/aggregator.ts + lib/chains/serialize.ts"
      via: "direct import of aggregateAllChains + serializeBigints (no HTTP hop)"
      pattern: "serializeBigints"
    - from: "lib/mcp-tools/get-instrument-terms.ts"
      to: "lib/apps/abrigo/instruments.ts"
      via: "import ABRIGO_INSTRUMENTS (empty → not_deployed)"
      pattern: "ABRIGO_INSTRUMENTS"
---

<objective>
Implement the three on-chain / econometric MCP tools — `get_instrument_terms`, `get_pool_state`, `query_econometric_panel` — each importing the Phase-3 lib functions directly (no BFF hop, AGENT-01). With no Abrigo contracts deployed and no HuggingFace panel published, all three return honest structured envelopes (`not_deployed` / `unavailable`) — never fabricated reserves, terms, or rows (CROSS-09 anti-fishing).

Purpose: Agents get an honest, branch-on-`status` answer about pre-launch protocol state instead of zero-filled fakes or hard errors.
Output: three tool files + one unit test file.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-agent-surface-mcp/04-RESEARCH.md

<interfaces>
From lib/apps/abrigo/instruments.ts:
```typescript
export const ABRIGO_INSTRUMENTS: AbrigoInstrument[] = []  // EMPTY at launch
export interface AbrigoInstrument { id:string; name:string; nameEn:string; chainId:SupportedChainId; address:Address; deployedAt:string }
export const ABRIGO_ABI  // poolBalance, settlementCount, lpPositionCount view fns
```
From lib/dashboard/aggregator.ts:
```typescript
export async function aggregateAllChains(): Promise<ChainAggregationResult[]>
// ChainAggregationResult.status === 'empty' on every chain while registry is empty (short-circuits before touching RPC)
```
From lib/chains/serialize.ts:
```typescript
export function serializeBigints<T>(value: T): T  // bigint → string; MANDATORY on any viem-derived tool output
```
From lib/mcp-tools/contract.ts (Plan 01):
```typescript
export const NotDeployedEnvelope, UnavailableEnvelope  // Zod schemas; terms/pool are z.null() literals
```
HuggingFace datasets-server (04-RESEARCH § Code Examples):
```
GET https://datasets-server.huggingface.co/is-valid?dataset=<org/name>  → 4xx/error when unpublished
GET .../rows?dataset=&config=default&split=train&offset=0&length=<=100   → rows (max 100/page)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_instrument_terms + get_pool_state (AGENT-06, AGENT-07-pool)</name>
  <files>lib/mcp-tools/get-instrument-terms.ts, lib/mcp-tools/get-pool-state.ts, tests/unit/mcp-tools-onchain-panel.test.ts</files>
  <read_first>
    - lib/apps/abrigo/instruments.ts (ABRIGO_INSTRUMENTS is [])
    - lib/dashboard/aggregator.ts (aggregateAllChains; ChainAggregationResult.status 'empty')
    - lib/chains/serialize.ts (serializeBigints)
    - lib/mcp-tools/contract.ts (NotDeployedEnvelope — terms/pool are z.null())
    - 04-RESEARCH.md § "Pattern 2: not_deployed Honest Envelope" + § "Pattern 6: bigint-Safe Tool Output" + § "Pitfall 7: Tool Output Must Be Text"
  </read_first>
  <behavior>
    - registerGetInstrumentTerms handler `{ instrument_id:'abrigo-celo-1', chain:'celo' }` (app defaults 'abrigo') → `{ content:[{type:'text',text}] }`, `JSON.parse(text)` === `{ status:'not_deployed', instrument_id:'abrigo-celo-1', chain:'celo', terms:null, pool:null, note:<string> }` and passes `NotDeployedEnvelope.parse`; result has NO `isError:true`
    - registerGetPoolState handler `{ chain:'celo', pool_address:'0xabc' }` → `not_deployed` envelope (pool:null) passing `NotDeployedEnvelope.parse`; result is not an MCP error
    - For get_pool_state, the handler calls `aggregateAllChains()`, and because every chain is `status:'empty'`, returns not_deployed; the returned text contains NO fabricated numeric reserves (assert text does not contain a non-null `poolBalance` value)
    - bad chain arg (e.g. `chain:'solana'`) → Zod rejects at inputSchema (enum), so the SDK surfaces a validation error — acceptable
  </behavior>
  <action>
    Both files export `registerXxx(server: McpServer): void` using `server.registerTool` (NOT `server.tool`). Import `type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js'`, `{ z } from 'zod'`, `{ NotDeployedEnvelope } from '@/lib/mcp-tools/contract'`.

    `get-instrument-terms.ts` — `registerGetInstrumentTerms`: name `get_instrument_terms`. `inputSchema: z.object({ app: z.string().default('abrigo'), instrument_id: z.string(), chain: z.enum(['celo','ethereum','base','arbitrum','optimism']) })`. Handler: import `{ ABRIGO_INSTRUMENTS } from '@/lib/apps/abrigo/instruments'`. Find instrument by `id === instrument_id`. If not found (always true today, registry empty), build `const result = NotDeployedEnvelope.parse({ status:'not_deployed', instrument_id: input.instrument_id, chain: input.chain, terms:null, pool:null, note:'No Abrigo instrument is deployed under this id on this chain. Terms become available after contract launch.' })` and return `{ content:[{ type:'text' as const, text: JSON.stringify(result) }] }`.

    `get-pool-state.ts` — `registerGetPoolState`: name `get_pool_state`. `inputSchema: z.object({ app: z.string().default('abrigo'), chain: z.enum(['celo','ethereum','base','arbitrum','optimism']), pool_address: z.string().optional() })`. Handler: import `{ aggregateAllChains } from '@/lib/dashboard/aggregator'` and `{ serializeBigints } from '@/lib/chains/serialize'`. `const results = await aggregateAllChains()`; find the matching chain by name; if that chain (or all) is `status:'empty'` OR no instruments, return `NotDeployedEnvelope.parse({ status:'not_deployed', instrument_id: input.pool_address ?? null === null ? '' : input.pool_address!, ... })` — to satisfy the schema (instrument_id is a string), set `instrument_id: input.pool_address ?? 'unknown'`, `chain: input.chain`, `terms:null, pool:null, note:'No Abrigo pool is live on this chain yet. Reserves become readable after contract launch.'`. When a future deployment exists (instruments present), wrap the aggregation result with `serializeBigints(...)` before `JSON.stringify` — bigint-safe. Always `return { content:[{ type:'text' as const, text: JSON.stringify(result) }] }`.

    Write `tests/unit/mcp-tools-onchain-panel.test.ts` (`// @vitest-environment node`). Capture-the-callback fake server (same pattern as Plan 02). Cover both <behavior> tool cases. For get_pool_state, also assert the returned text does NOT match `/"poolBalance":"?\d/` (no fabricated numbers).
  </action>
  <verify>
    <automated>vitest run tests/unit/mcp-tools-onchain-panel.test.ts -t "get_instrument_terms|get_pool_state"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mcp-tools/get-instrument-terms.ts` contains `'get_instrument_terms'`, `'not_deployed'`, `ABRIGO_INSTRUMENTS`, `NotDeployedEnvelope`
    - `lib/mcp-tools/get-pool-state.ts` contains `'get_pool_state'`, `aggregateAllChains`, `serializeBigints`, `NotDeployedEnvelope`
    - neither file contains `server.tool(` (grep no match)
    - `vitest run tests/unit/mcp-tools-onchain-panel.test.ts -t "get_instrument_terms|get_pool_state"` exits 0
  </acceptance_criteria>
  <done>Both on-chain tools return not_deployed envelopes against the empty registry; bigint serialization wired; no fabricated reserves; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: query_econometric_panel (AGENT-07)</name>
  <files>lib/mcp-tools/query-econometric-panel.ts, tests/unit/mcp-tools-onchain-panel.test.ts</files>
  <read_first>
    - lib/mcp-tools/contract.ts (UnavailableEnvelope)
    - 04-RESEARCH.md § "Code Examples / HuggingFace datasets-server /rows endpoint" + § "Open Questions Q2" (dataset not published → unavailable envelope)
    - 04-VALIDATION.md § "Manual-Only Verifications" (HF rows is a deferred manual check)
  </read_first>
  <behavior>
    - registerQueryEconometricPanel handler `{ panel:'pair-d' }` (app defaults 'abrigo') → `{ content:[{type:'text',text}] }`, `JSON.parse(text)` passes `UnavailableEnvelope.parse` and equals `{ status:'unavailable', app:'abrigo', panel:'pair-d', note:<string mentioning huggingface.co/wvs-finance> }`
    - the handler MUST NOT throw when the network probe fails; a rejected/4xx `/is-valid` fetch resolves to the unavailable envelope (guarded try/catch)
    - paging args `offset`/`length` are accepted and validated (length capped at 100) but produce the unavailable envelope today
    - returned text contains NO fabricated row data
  </behavior>
  <action>
    `lib/mcp-tools/query-econometric-panel.ts` — export `registerQueryEconometricPanel(server: McpServer)`. Name `query_econometric_panel`. `inputSchema: z.object({ app: z.string().default('abrigo'), panel: z.string(), filters: z.record(z.string()).optional(), offset: z.number().int().min(0).default(0), length: z.number().int().min(1).max(100).default(100) })`. Handler: there is no published dataset name in the codebase (04-RESEARCH Q2). Optionally probe HF: wrap in try/catch — `const probe = await fetch('https://datasets-server.huggingface.co/is-valid?dataset=wvs-finance/abrigo-panel').catch(() => null)`. Regardless of probe outcome today (dataset unpublished → not ok), return `const result = UnavailableEnvelope.parse({ status:'unavailable', app: input.app, panel: input.panel, note:'Panel dataset not yet published to HuggingFace. Check https://huggingface.co/wvs-finance for future availability.' })` and `return { content:[{ type:'text' as const, text: JSON.stringify(result) }] }`. Leave a clearly-marked TODO comment block describing the future `/rows` wiring (offset/length paging, Bearer HF_API_TOKEN, max 100/page) so the manual follow-up (04-VALIDATION) is documented in-code. Never throw on network failure.
    Extend `tests/unit/mcp-tools-onchain-panel.test.ts` with the query_econometric_panel <behavior> cases. Mock or tolerate the fetch (the test environment may have no network — the try/catch must make the handler resolve regardless; assert on the envelope, not the probe).
  </action>
  <verify>
    <automated>vitest run tests/unit/mcp-tools-onchain-panel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mcp-tools/query-econometric-panel.ts` contains `'query_econometric_panel'`, `'unavailable'`, `UnavailableEnvelope`, `huggingface.co/wvs-finance`, and `.max(100)`
    - file contains a try/catch around the fetch (grep `catch`)
    - file does NOT contain `server.tool(` (grep no match)
    - `vitest run tests/unit/mcp-tools-onchain-panel.test.ts` exits 0 (all three on-chain/panel tools green)
  </acceptance_criteria>
  <done>query_econometric_panel returns an honest unavailable envelope, never throws on network failure, and documents the future /rows paging path; tests pass.</done>
</task>

</tasks>

<verification>
- `vitest run tests/unit/mcp-tools-onchain-panel.test.ts` exits 0
- No tool file uses deprecated `server.tool(`
- get_pool_state output contains no fabricated numeric reserves; query_econometric_panel never throws on network failure
</verification>

<success_criteria>
- AGENT-06/07-pool/07 tools implemented, direct lib imports (no HTTP hop)
- Honest not_deployed / unavailable envelopes; bigint-safe via serializeBigints
- isError reserved for genuine failures only; CROSS-09 anti-fishing upheld
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-surface-mcp/04-03-SUMMARY.md`
</output>

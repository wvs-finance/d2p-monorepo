---
phase: 04-agent-surface-mcp
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/mcp-tools/get-instrument-terms.ts
  - lib/mcp-tools/get-pool-state.ts
  - lib/mcp-tools/query-econometric-panel.ts
  - tests/unit/mcp-tools-onchain-panel.test.ts
autonomous: true
requirements: [AGENT-06, AGENT-07-pool, AGENT-07]
must_haves:
  truths:
    - "get_instrument_terms returns a structured not_deployed envelope (terms:null) against the empty ABRIGO_INSTRUMENTS registry — never an MCP error, never fabricated terms"
    - "get_pool_state returns a structured not_deployed envelope (pool:null) against the empty registry; bigint output serializes as a string via lib/chains/serialize.ts (future-path; not exercised on the empty path today)"
    - "query_econometric_panel returns a structured unavailable envelope after a guarded /is-valid probe — never fabricated rows; filters accept string|number|boolean; the dataset name is a single UNVERIFIED constant, not baked into a shipped probe URL/note"
    - "each tool registers outputSchema (the shared Zod) AND returns BOTH content[text] and structuredContent (M1)"
    - "isError:true is reserved for genuine failures (bad args, RPC down), never for the normal pre-launch empty state"
  artifacts:
    - path: "lib/mcp-tools/get-instrument-terms.ts"
      provides: "registerGetInstrumentTerms(server) — AGENT-06"
      exports: ["registerGetInstrumentTerms"]
    - path: "lib/mcp-tools/get-pool-state.ts"
      provides: "registerGetPoolState(server) — AGENT-07-pool"
      exports: ["registerGetPoolState"]
    - path: "lib/mcp-tools/query-econometric-panel.ts"
      provides: "registerQueryEconometricPanel(server) — AGENT-07"
      exports: ["registerQueryEconometricPanel"]
  key_links:
    - from: "lib/mcp-tools/get-pool-state.ts"
      to: "lib/dashboard/aggregator.ts + lib/chains/serialize.ts"
      via: "direct import of aggregateAllChains + serializeBigints (no HTTP hop)"
      pattern: "serializeBigints"
    - from: "lib/mcp-tools/get-instrument-terms.ts"
      to: "lib/apps/abrigo/instruments.ts"
      via: "import ABRIGO_INSTRUMENTS (empty → not_deployed)"
      pattern: "ABRIGO_INSTRUMENTS"
---

<objective>
Implement the three on-chain / econometric MCP tools — `get_instrument_terms`, `get_pool_state`, `query_econometric_panel` — each importing the Phase-3 lib functions directly (no BFF hop, AGENT-01). With no Abrigo contracts deployed and no HuggingFace panel published, all three return honest structured envelopes (`not_deployed` / `unavailable`) — never fabricated reserves, terms, or rows (CROSS-09 anti-fishing).

Purpose: Agents get an honest, branch-on-`status` answer about pre-launch protocol state instead of zero-filled fakes or hard errors.
Output: three tool files + one unit test file.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-agent-surface-mcp/04-RESEARCH.md

<interfaces>
From lib/apps/abrigo/instruments.ts:
```typescript
export const ABRIGO_INSTRUMENTS: AbrigoInstrument[] = []  // EMPTY at launch
export interface AbrigoInstrument { id:string; name:string; nameEn:string; chainId:SupportedChainId; address:Address; deployedAt:string }
export const ABRIGO_ABI  // poolBalance, settlementCount, lpPositionCount view fns
```
From lib/dashboard/aggregator.ts:
```typescript
export async function aggregateAllChains(): Promise<ChainAggregationResult[]>
// ChainAggregationResult.status === 'empty' on every chain while registry is empty (short-circuits before touching RPC)
```
From lib/chains/serialize.ts:
```typescript
export function serializeBigints<T>(value: T): T  // bigint → string; deep-walk
// NOTE: on the empty-registry path there are NO bigints to serialize today; this wiring is the
// FUTURE-DEPLOYMENT path, present so the boundary is correct when contracts land. Do not overstate.
```
From @modelcontextprotocol/sdk/server/mcp.js (GROUND TRUTH):
```typescript
// server.registerTool(name, { ..., outputSchema? }, handler)
// validateToolOutput: outputSchema present but NO structuredContent → throws McpError(InvalidParams).
// → when registering outputSchema, the handler MUST return BOTH content[text] AND structuredContent.
```
From lib/mcp-tools/contract.ts (Plan 01):
```typescript
export const NotDeployedEnvelope, UnavailableEnvelope  // Zod schemas; terms/pool are z.null() literals
```
HuggingFace datasets-server (04-RESEARCH § Code Examples):
```
GET https://datasets-server.huggingface.co/is-valid?dataset=<org/name>  → 4xx/error when unpublished
GET .../rows?dataset=&config=default&split=train&offset=0&length=<=100   → rows (max 100/page)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_instrument_terms + get_pool_state (AGENT-06, AGENT-07-pool)</name>
  <files>lib/mcp-tools/get-instrument-terms.ts, lib/mcp-tools/get-pool-state.ts, tests/unit/mcp-tools-onchain-panel.test.ts</files>
  <read_first>
    - lib/apps/abrigo/instruments.ts (ABRIGO_INSTRUMENTS is [])
    - lib/dashboard/aggregator.ts (aggregateAllChains; ChainAggregationResult.status 'empty')
    - lib/chains/serialize.ts (serializeBigints — future path)
    - lib/mcp-tools/contract.ts (NotDeployedEnvelope — terms/pool are z.null())
    - 04-RESEARCH.md § "Pattern 2: not_deployed Honest Envelope" + § "Pattern 6: bigint-Safe Tool Output" + § "Pitfall 7: Tool Output Must Be Text"
  </read_first>
  <behavior>
    - registerGetInstrumentTerms handler `{ instrument_id:'abrigo-celo-1', chain:'celo' }` (app defaults 'abrigo') → `{ content:[{type:'text',text}], structuredContent }`, `JSON.parse(text)` deep-equals `structuredContent` === `{ status:'not_deployed', instrument_id:'abrigo-celo-1', chain:'celo', terms:null, pool:null, note:<string> }` and passes `NotDeployedEnvelope.parse`; result has NO `isError:true`
    - registerGetPoolState handler `{ chain:'celo', pool_address:'0xabc' }` → `not_deployed` envelope (pool:null) passing `NotDeployedEnvelope.parse`, with `structuredContent`; result is not an MCP error; `instrument_id` is the pool_address string (`'0xabc'`)
    - registerGetPoolState handler `{ chain:'celo' }` (no pool_address) → `not_deployed` envelope with `instrument_id:'unknown'` (the schema requires a string; we substitute the literal `'unknown'`, never `null`)
    - For get_pool_state the handler calls `aggregateAllChains()`; because every chain is `status:'empty'` it returns not_deployed; the returned text contains NO fabricated numeric reserves (assert text does not match `/"poolBalance":"?\d/`)
    - bad chain arg (e.g. `chain:'solana'`) → Zod rejects at inputSchema (enum), the SDK surfaces a validation error — acceptable
  </behavior>
  <action>
    Both files export `registerXxx(server: McpServer): void` using `server.registerTool` (NOT `server.tool`). Import `type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js'`, `{ z } from 'zod'`, `{ NotDeployedEnvelope } from '@/lib/mcp-tools/contract'`.

    M1: register `outputSchema: NotDeployedEnvelope` and the handler MUST return `structuredContent: result` alongside `content:[{type:'text', text: JSON.stringify(result)}]` (validateToolOutput throws otherwise).

    `get-instrument-terms.ts` — `registerGetInstrumentTerms`: name `get_instrument_terms`. `inputSchema: z.object({ app: z.string().default('abrigo'), instrument_id: z.string(), chain: z.enum(['celo','ethereum','base','arbitrum','optimism']) })`. Handler: import `{ ABRIGO_INSTRUMENTS } from '@/lib/apps/abrigo/instruments'`. Find instrument by `id === instrument_id`. If not found (always true today, registry empty), `const result = NotDeployedEnvelope.parse({ status:'not_deployed', instrument_id: input.instrument_id, chain: input.chain, terms:null, pool:null, note:'No Abrigo instrument is deployed under this id on this chain. Terms become available after contract launch.' })` and `return { content:[{ type:'text' as const, text: JSON.stringify(result) }], structuredContent: result }`.

    `get-pool-state.ts` — `registerGetPoolState`: name `get_pool_state`. `inputSchema: z.object({ app: z.string().default('abrigo'), chain: z.enum(['celo','ethereum','base','arbitrum','optimism']), pool_address: z.string().optional() })`. Handler: import `{ aggregateAllChains } from '@/lib/dashboard/aggregator'` and `{ serializeBigints } from '@/lib/chains/serialize'`. `const results = await aggregateAllChains()`; find the matching chain by name; when that chain (or all) is `status:'empty'` OR has no instruments, return a not_deployed envelope. M4 (FIX the malformed expression): set `instrument_id: input.pool_address ?? 'unknown'` — exactly this, do NOT write the broken `?? null === null ? '' : ...` form. (Note: stuffing a pool address into `instrument_id` is a mild schema smell; it is acceptable for v1 because the envelope is shared and `instrument_id` is the generic identifier slot. If a future deployment lands, revisit a dedicated `subject_id` field.) Build: `const result = NotDeployedEnvelope.parse({ status:'not_deployed', instrument_id: input.pool_address ?? 'unknown', chain: input.chain, terms:null, pool:null, note:'No Abrigo pool is live on this chain yet. Reserves become readable after contract launch.' })`. When a future deployment exists (instruments present), wrap the aggregation result with `serializeBigints(...)` before `JSON.stringify` (FUTURE PATH — not exercised on the empty registry today; comment it as such). Always `return { content:[{ type:'text' as const, text: JSON.stringify(result) }], structuredContent: result }`.

    Write `tests/unit/mcp-tools-onchain-panel.test.ts` (`// @vitest-environment node`). Capture-the-callback fake server (same pattern as Plan 02). Cover both <behavior> tool cases, asserting `content[0].text` deep-equals `structuredContent`, the `instrument_id:'unknown'` substitution for the no-pool_address case, and (for get_pool_state) that the returned text does NOT match `/"poolBalance":"?\d/`.
  </action>
  <verify>
    <automated>vitest run tests/unit/mcp-tools-onchain-panel.test.ts -t "get_instrument_terms|get_pool_state"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mcp-tools/get-instrument-terms.ts` contains `'get_instrument_terms'`, `'not_deployed'`, `ABRIGO_INSTRUMENTS`, `NotDeployedEnvelope`, `structuredContent`
    - `lib/mcp-tools/get-pool-state.ts` contains `'get_pool_state'`, `aggregateAllChains`, `serializeBigints`, `NotDeployedEnvelope`, `structuredContent`, and `?? 'unknown'`
    - `lib/mcp-tools/get-pool-state.ts` does NOT contain the broken substring `=== null ?` (grep no match)
    - neither file contains `server.tool(` (grep no match)
    - `vitest run tests/unit/mcp-tools-onchain-panel.test.ts -t "get_instrument_terms|get_pool_state"` exits 0
  </acceptance_criteria>
  <done>Both on-chain tools return not_deployed envelopes (with structuredContent) against the empty registry; instrument_id substitution is the simple `?? 'unknown'`; bigint serialization wired as the future path; no fabricated reserves; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: query_econometric_panel (AGENT-07)</name>
  <files>lib/mcp-tools/query-econometric-panel.ts, tests/unit/mcp-tools-onchain-panel.test.ts</files>
  <read_first>
    - lib/mcp-tools/contract.ts (UnavailableEnvelope)
    - 04-RESEARCH.md § "Code Examples / HuggingFace datasets-server /rows endpoint" + § "Open Questions Q2" (dataset name UNKNOWN → unavailable envelope)
    - 04-VALIDATION.md § "Manual-Only Verifications" (HF rows is a deferred manual check; dataset name unverified)
  </read_first>
  <behavior>
    - registerQueryEconometricPanel handler `{ panel:'pair-d' }` (app defaults 'abrigo') → `{ content:[{type:'text',text}], structuredContent }`, `JSON.parse(text)` deep-equals `structuredContent`, passes `UnavailableEnvelope.parse` and equals `{ status:'unavailable', app:'abrigo', panel:'pair-d', note:<string mentioning huggingface.co/wvs-finance> }`
    - the `note` does NOT assert a specific dataset PATH (e.g. it must NOT claim `wvs-finance/abrigo-panel` is the dataset) — it points at the org `huggingface.co/wvs-finance` only, because the dataset name is unverified (B4)
    - filters accept string|number|boolean values (B4): `{ panel:'pair-d', filters:{ pair:'D', minN:120, passed:true } }` is ACCEPTED by inputSchema (no Zod rejection) and still returns the unavailable envelope
    - the handler MUST NOT throw when the network probe fails; a rejected/4xx `/is-valid` fetch resolves to the unavailable envelope (guarded try/catch)
    - paging args `offset`/`length` are accepted and validated (length capped at 100) but produce the unavailable envelope today
    - returned text contains NO fabricated row data
  </behavior>
  <action>
    `lib/mcp-tools/query-econometric-panel.ts` — export `registerQueryEconometricPanel(server: McpServer)`. Name `query_econometric_panel`. `inputSchema: z.object({ app: z.string().default('abrigo'), panel: z.string(), filters: z.record(z.union([z.string(), z.number(), z.boolean()])).optional(), offset: z.number().int().min(0).default(0), length: z.number().int().min(1).max(100).default(100) })` — B4: `filters` is `z.record(z.union([z.string(), z.number(), z.boolean()]))`, NOT `z.record(z.string())` (numeric/range filters must be accepted). `outputSchema: UnavailableEnvelope` (M1 — return structuredContent too).

    B4 — dataset name is UNKNOWN (research Q2). Declare exactly ONE named constant at module top: `// UNVERIFIED — confirm against huggingface.co/wvs-finance before enabling /rows. Do not ship in a probe URL/note as a fact.` followed by `const HF_PANEL_DATASET = 'wvs-finance/abrigo-panel' // UNVERIFIED`. Do NOT inline an invented path anywhere else.

    Handler: optionally probe HF guarded — `const probe = await fetch(\`https://datasets-server.huggingface.co/is-valid?dataset=\${HF_PANEL_DATASET}\`).catch(() => null)`. Regardless of probe outcome today (dataset unverified/unpublished → not ok), return `const result = UnavailableEnvelope.parse({ status:'unavailable', app: input.app, panel: input.panel, note:'Panel dataset is not yet published / confirmed. Check https://huggingface.co/wvs-finance for future availability.' })` (the note points at the ORG, never asserts the specific dataset path) and `return { content:[{ type:'text' as const, text: JSON.stringify(result) }], structuredContent: result }`. Leave a clearly-marked TODO block describing the future `/rows` wiring (offset/length paging, Bearer HF_API_TOKEN, max 100/page, and "confirm HF_PANEL_DATASET first") so the manual follow-up (04-VALIDATION) is documented in-code. Never throw on network failure.
    Extend `tests/unit/mcp-tools-onchain-panel.test.ts` with the query_econometric_panel <behavior> cases, INCLUDING the numeric/boolean filters acceptance case and an assertion that the note does NOT contain the substring `abrigo-panel` (no asserted dataset path). The try/catch must make the handler resolve regardless of network — assert on the envelope, not the probe.
  </action>
  <verify>
    <automated>vitest run tests/unit/mcp-tools-onchain-panel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mcp-tools/query-econometric-panel.ts` contains `'query_econometric_panel'`, `'unavailable'`, `UnavailableEnvelope`, `huggingface.co/wvs-finance`, `.max(100)`, `z.union(`, `structuredContent`, and the `UNVERIFIED` comment on `HF_PANEL_DATASET`
    - the `note` string does NOT contain `abrigo-panel` (grep the note text — the dataset path is not asserted as fact)
    - file contains a try/catch around the fetch (grep `catch`)
    - file does NOT contain `server.tool(` (grep no match)
    - `vitest run tests/unit/mcp-tools-onchain-panel.test.ts` exits 0 (all three on-chain/panel tools green)
  </acceptance_criteria>
  <done>query_econometric_panel accepts string|number|boolean filters, returns an honest unavailable envelope (org-only note, dataset name an UNVERIFIED constant), with structuredContent, never throws on network failure, and documents the future /rows path; tests pass.</done>
</task>

</tasks>

<verification>
- `vitest run tests/unit/mcp-tools-onchain-panel.test.ts` exits 0
- No tool file uses deprecated `server.tool(`; each returns structuredContent
- get_pool_state output contains no fabricated numeric reserves and uses `?? 'unknown'` (no broken `=== null ?`); query_econometric_panel filters accept number/boolean, note asserts no dataset path, never throws on network failure
</verification>

<success_criteria>
- AGENT-06/07-pool/07 tools implemented, direct lib imports (no HTTP hop), outputSchema + structuredContent (M1)
- Honest not_deployed / unavailable envelopes; bigint-safe via serializeBigints (future path, not overstated)
- filters accept string|number|boolean (B4); dataset name a single UNVERIFIED constant, not baked into a shipped note (B4); instrument_id substitution fixed (M4)
- isError reserved for genuine failures only; CROSS-09 anti-fishing upheld
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-surface-mcp/04-03-SUMMARY.md`
</output>

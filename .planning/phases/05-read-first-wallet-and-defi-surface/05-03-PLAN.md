---
phase: 05-read-first-wallet-and-defi-surface
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/(defi)/apps/abrigo/instruments/page.tsx
  - components/defi/RiskCallout.tsx
  - components/defi/InstrumentParams.tsx
  - messages/es-CO/instruments.json
  - messages/en/instruments.json
  - i18n/request.ts
  - docs/copy-review.md
  - tests/e2e/instruments-index.spec.ts
  - tests/a11y/defi-instruments.spec.ts
autonomous: false
requirements: [DEFI-03, DEFI-05]
must_haves:
  truths:
    - "A visitor at /apps/abrigo/instruments sees an honest 'no instruments deployed yet' state — no ghost cards, no example payoff"
    - "RiskCallout labels the instrument 'hedging product, not leverage' in es-CO and en, full 4-side ochre hairline, not dismissible"
    - "The instruments index page lives physically under app/(defi)/ so it inherits the wallet provider context"
    - "axe is clean on the instruments index + risk callout"
  artifacts:
    - path: "app/(defi)/apps/abrigo/instruments/page.tsx"
      provides: "Honest-empty instruments index (RSC)"
      contains: "instruments"
    - path: "components/defi/RiskCallout.tsx"
      provides: "Persistent risk disclosure, full 4-side border-accent-default"
      contains: "border-accent-default"
    - path: "components/defi/InstrumentParams.tsx"
      provides: "Param table RSC, IBM Plex Mono numeric values"
    - path: "messages/es-CO/instruments.json"
      provides: "es-CO-first instrument/risk/empty copy"
  key_links:
    - from: "app/(defi)/apps/abrigo/instruments/page.tsx"
      to: "messages/es-CO/instruments.json"
      via: "getTranslations('instruments')"
      pattern: "getTranslations"
    - from: "components/defi/RiskCallout.tsx"
      to: "instruments.json"
      via: "translated heading + body"
      pattern: "border-accent-default"
---

<objective>
Build the read-only, wallet-free surface pieces: the honest-empty instruments index, the persistent RiskCallout (DEFI-05), the InstrumentParams RSC, and their es-CO-first i18n — all physically under `app/(defi)/` so they inherit the wallet provider context (FOUND-11 clean, URL-transparent). Note: `/apps/abrigo/*` is INTENTIONALLY split across two route groups — `/apps/abrigo/dashboard` under `(apps)` (no wallet tree) and `/apps/abrigo/instruments` under `(defi)` (wallet tree). They have different provider trees; do NOT consolidate them. End with an Evidence-Collector live verify of the index + the 360px above-fold risk disclosure.

Purpose: 05-04 wires the per-instrument detail page from RiskCallout + InstrumentParams (built here) plus WalletPanel/PayoffDiagram/PoolStatePanel (built there). Splitting the RSC/no-wallet pieces from the client/wallet pieces keeps each plan in budget and lets the index ship + verify independently.
Output: index route (honest empty); RiskCallout; InstrumentParams; instruments.json (both locales) + copy-review sign-off row; index e2e + a11y specs filled.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-read-first-wallet-and-defi-surface/05-UI-SPEC.md
@.planning/phases/05-read-first-wallet-and-defi-surface/05-VALIDATION.md

<interfaces>
<!-- Extracted from codebase — use directly. -->

EXISTING RSC page pattern (mirror app/(apps)/apps/abrigo/dashboard/page.tsx):
```typescript
import { getLocale, getTranslations } from 'next-intl/server'
export const dynamic = 'force-dynamic'
export const runtime = 'nodejs'
export default async function Page() {
  const t = await getTranslations('instruments')
  const locale = await getLocale()   // 'es-CO' (default via NEXT_LOCALE cookie) | 'en'
  ...
}
```
i18n: locale resolved from NEXT_LOCALE cookie in i18n/request.ts; default es-CO. Message files are NESTED JSON objects (next-intl v4) under messages/{locale}/. You must register the new `instruments` namespace in i18n/request.ts alongside the existing ones (about/common/dashboard/lab/nav/research/team).

EXISTING type (from 05-01): AbrigoInstrument { id, name, nameEn, chainId, address, deployedAt, strike, slope }; ABRIGO_INSTRUMENTS = [] (empty).

LOCKED COPY (05-UI-SPEC § Copywriting Contract — author es-CO first, verbatim):
RiskCallout es-CO heading: "Instrumento de cobertura — no es apalancamiento"
RiskCallout es-CO body: "Abrigo está diseñado para cubrir exposición cambiaria y macroeconómica. No amplifica pérdidas ni ganancias: es un instrumento de cobertura convexo, no un producto de apalancamiento."
RiskCallout en heading: "Hedging product — not leverage"
RiskCallout en body: "Abrigo is designed to hedge against currency and macroeconomic exposure. It does not amplify gains or losses: it is a convex hedging instrument, not a leveraged product."
Index h1: "Instrumentos Abrigo" / "Abrigo Instruments"
Empty heading: "Aún no hay instrumentos desplegados" / "No instruments deployed yet"
Empty body: "Los instrumentos de cobertura Abrigo se habilitarán una vez que los contratos se desplieguen en cadena. Esta página se actualizará automáticamente." / "Abrigo hedging instruments will become available once contracts are deployed on-chain. This page will update automatically."
GitHub link (Claude's discretion = YES): "Ver contratos pendientes en GitHub" / "View pending contracts on GitHub"
Empty-state icon: PackageSearch (lucide, confirmed installed) at 48px, text-text-muted.

LOCKED TOKENS (05-UI-SPEC § Color / Typography):
- Accent text < 18.66px must use text-accent-text (NOT text-accent-default).
- RiskCallout border: FULL 4-side `border border-accent-default` hairline (NEVER one-sided — impeccable fails one-sided colored borders). Heading uses text-accent-text.
- Mono numeric values: font-mono text-sm font-normal. Weights: 400 + 600 only.
- SVG <text> (none here unless icon) — N/A for this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RiskCallout + InstrumentParams components + instruments i18n (es-CO first) + namespace registration</name>
  <read_first>
    - 05-UI-SPEC.md § "Copywriting Contract" (verbatim copy above) + § "Color" (accent-text rule, 4-side border) + § "Component Inventory" (RiskCallout=RSC, InstrumentParams=RSC)
    - i18n/request.ts (register the new namespace)
    - an existing messages/es-CO/*.json (nested-object shape) + docs/copy-review.md (sign-off format)
    - components/StatusPill.tsx (token + className idiom to mirror)
  </read_first>
  <action>
    1. `messages/es-CO/instruments.json` + `messages/en/instruments.json` — nested-object namespace `instruments` with keys: `index.h1`, `index.empty_heading`, `index.empty_body`, `index.github_link`, `risk.heading`, `risk.body`, plus `params.*` labels (`id`, `chain`, `strike`, `slope`, `deployed_at`, `name` — used by InstrumentParams). Author es-CO FIRST using the verbatim locked copy; en second. No machine translation.

    2. Register `instruments` in `i18n/request.ts` so `getTranslations('instruments')` resolves (add to the messages assembly alongside about/common/dashboard/lab/nav/research/team).

    3. `components/defi/RiskCallout.tsx` (RSC): renders heading (`text-xl font-semibold text-accent-text`) + body (`text-base font-normal text-text-primary leading-relaxed`) inside a container with FULL 4-side hairline `border border-accent-default rounded-[var(--radius)] px-4 py-3`. Accepts the already-translated `heading` + `body` strings as props (caller passes from getTranslations). NOT dismissible — no button, no `<details>`, no toast. Plain `<section>`/`<aside>`.

    4. `components/defi/InstrumentParams.tsx` (RSC): param table for a single `AbrigoInstrument` — id, name (locale-aware via prop), chain, strike, slope, deployed date. Numeric values (strike/slope) in `font-mono text-sm font-normal`; chain address in `font-mono text-sm font-normal text-text-muted`. Labels `text-sm font-normal`. No card-inside-card. Accepts `instrument: AbrigoInstrument`, `labels` (translated), `locale`. (It renders only on the detail page in 05-04; built + typed here so 05-04 only wires it.)

    5. Append a copy-review sign-off row to `docs/copy-review.md` for the Phase-5 instruments namespace (native es-CO reviewer line, per CLAUDE.md).
  </action>
  <verify>
    <automated>grep -q 'Instrumento de cobertura — no es apalancamiento' messages/es-CO/instruments.json && grep -q 'Hedging product — not leverage' messages/en/instruments.json && grep -q 'border-accent-default' components/defi/RiskCallout.tsx && grep -q "'instruments'" i18n/request.ts; grep -q 'font-mono' components/defi/InstrumentParams.tsx && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `messages/es-CO/instruments.json` contains the verbatim es-CO risk heading + body; `messages/en/instruments.json` the en counterparts
    - `components/defi/RiskCallout.tsx` contains `border-accent-default` (full 4-side, grep shows no `border-l-`/`border-t-` one-sided accent), `text-accent-text` for the heading, NO `<details>`/dismiss button
    - `components/defi/InstrumentParams.tsx` contains `font-mono` for numeric values
    - `i18n/request.ts` registers the `instruments` namespace
    - `docs/copy-review.md` has a Phase-5 instruments sign-off row
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>RiskCallout + InstrumentParams compile; instruments namespace registered + both-locale copy authored + sign-off recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Instruments index route (honest empty) + index e2e + a11y specs</name>
  <read_first>
    - app/(apps)/apps/abrigo/dashboard/page.tsx (RSC + getLocale/getTranslations idiom to mirror)
    - 05-UI-SPEC.md § "Page Layouts (a) Instruments Index" (layout, max-width 1200px, padding, PackageSearch 48px)
    - components/defi/RiskCallout.tsx + InstrumentParams.tsx (Task 1)
    - tests/e2e/instruments-index.spec.ts + tests/a11y/defi-instruments.spec.ts (the 05-01 fixme stubs you now fill — actually 05-01 created instrument-page/wallet-states/defi-instruments; create instruments-index.spec.ts fresh and fill defi-instruments.spec.ts)
    - playwright.config.ts (chromium + axe projects; local prod build :3040)
  </read_first>
  <action>
    1. `app/(defi)/apps/abrigo/instruments/page.tsx` (RSC) — URL-transparent path: physically under `(defi)` so it inherits the wallet providers, public URL `/apps/abrigo/instruments`. `export const dynamic='force-dynamic'; export const runtime='nodejs'`. `getTranslations('instruments')` + `getLocale()`. Render:
       - h1 (`text-[28px] font-semibold`) from `index.h1`.
       - Map `ABRIGO_INSTRUMENTS`: it is `[]` → render the honest empty state ONLY (PackageSearch 48px `text-text-muted` + `index.empty_heading` + `index.empty_body` + GitHub link to the wvs-finance contracts repo using `index.github_link`). NO ghost cards, NO example payoff, NO fabricated instrument.
       - When `ABRIGO_INSTRUMENTS.length > 0` (post-deploy branch): render one card per instrument (`bg-elevated border border-border-default`, name + chain badge + deploy date + "Ver instrumento"/"View instrument" link). **B2 — build the link as `` `/apps/abrigo/instruments/${i.id}/${i.chainId}` `` (NUMERIC chainId, the locked `[chain]` segment contract — NOT a chain name/slug).** Using `chainName` here would silently null the pool selector in 05-04. Build the branch but it renders nothing today (empty registry).
       Canvas background; max-w-[1200px]; px-4 at 360px / px-8 at lg.

    2. `tests/e2e/instruments-index.spec.ts` (replace the 05-01 fixme bodies / create real assertions): navigate `/apps/abrigo/instruments`; assert h1 present; assert empty heading text present; assert NO element matching an instrument card selector (honest empty — count 0); assert the page does NOT contain a "connect wallet to view" gate on the index. Run under chromium project against local prod build.

    3. `tests/a11y/defi-instruments.spec.ts` (fill the 05-01 fixme): axe run on `/apps/abrigo/instruments`, expect zero violations (covers DEFI-06 a11y for the index + the empty state). Run under the axe project.

    4. **M5 — route-group coexistence build assertion:** after creating the index route, run `pnpm build` and confirm BOTH `/apps/abrigo/dashboard` (resolved via `(apps)`) AND `/apps/abrigo/instruments` (resolved via `(defi)`) build and resolve — they coexist over the same `/apps/abrigo/*` URL prefix with different provider trees. The build must not error on the route-group overlap.

    Note: the 360px risk-disclosure above-fold assertion lives on the DETAIL page (built in 05-04) — the RiskCallout component itself is verified here via axe + the live EC checkpoint renders it on a detail fixture in 05-04. This index has no RiskCallout (risk lives on the instrument detail page per UI-SPEC layout order).
  </action>
  <verify>
    <automated>test -f "app/(defi)/apps/abrigo/instruments/page.tsx" && grep -q 'ABRIGO_INSTRUMENTS' "app/(defi)/apps/abrigo/instruments/page.tsx" && grep -q 'PackageSearch' "app/(defi)/apps/abrigo/instruments/page.tsx" && pnpm tsc --noEmit && pnpm vitest run tests/architecture/ && pnpm build 2>&1 | grep -E '/apps/abrigo/(dashboard|instruments)'</automated>
  </verify>
  <acceptance_criteria>
    - route file exists at `app/(defi)/apps/abrigo/instruments/page.tsx` (under (defi), NOT (apps))
    - page references `ABRIGO_INSTRUMENTS` and renders `PackageSearch` empty-state icon
    - page contains NO hardcoded example instrument (grep shows no fabricated `strike:`/`slope:` literal in the page)
    - `tests/e2e/instruments-index.spec.ts` asserts empty heading + zero instrument cards + no wallet gate
    - `tests/a11y/defi-instruments.spec.ts` runs axe on the index (no longer test.fixme)
    - `pnpm tsc --noEmit` exits 0; architecture tests stay green (page under (defi) imports are allowed; (lab)/(apps) clean)
    - `pnpm build` succeeds with BOTH `/apps/abrigo/dashboard` (via (apps)) and `/apps/abrigo/instruments` (via (defi)) resolving (M5 coexistence)
  </acceptance_criteria>
  <done>Honest-empty index renders under (defi); index e2e + axe specs are real (not fixme).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Evidence-Collector live verify — instruments index honest-empty + RiskCallout axe</name>
  <what-built>
    The instruments index at /apps/abrigo/instruments rendering the honest "no instruments deployed yet" empty state (PackageSearch + heading + body + GitHub link), with no fabricated cards. RiskCallout + InstrumentParams components built (rendered on the detail page in 05-04).
  </what-built>
  <how-to-verify>
    Launch the Evidence Collector (per CLAUDE.md block):
      Working dir: /home/jmsbpp/apps/d2p/frontend
      Target: https://www.d2pfinance.xyz/apps/abrigo/instruments (fall back to local `pnpm build && pnpm start -p 3040`).
      Task: 05-03 — instruments index honest-empty.
      Plan claims: (1) h1 "Instrumentos Abrigo"/"Abrigo Instruments"; (2) empty heading "Aún no hay instrumentos desplegados"/"No instruments deployed yet"; (3) PackageSearch icon present; (4) ZERO instrument cards (no ghost/fabricated data); (5) NO "connect wallet to view" gate; (6) GitHub link present.
      Steps: browser_navigate → browser_snapshot (assert each claim) → browser_console_messages (no runtime errors) → browser_take_screenshot /tmp/d2p-verify/05-03-instruments-index.png → verify both locales (set NEXT_LOCALE=en cookie for the en pass).
      Append verdicts (✓/⚠/✗/⊘) to 05-LIVE-VERIFICATION.md with screenshot paths.
      Note: the 360px above-fold RiskCallout proof is deferred to 05-04 (the detail page is where RiskCallout renders) — record that here as a forward pointer, not a failure.
  </how-to-verify>
  <files>.planning/phases/05-read-first-wallet-and-defi-surface/05-LIVE-VERIFICATION.md</files>
  <action>Run the Evidence Collector per the how-to-verify block on /apps/abrigo/instruments in both locales; record per-claim verdicts + screenshot paths in 05-LIVE-VERIFICATION.md. Verification-only — no production code written here.</action>
  <verify>Screenshot exists at /tmp/d2p-verify/05-03-instruments-index.png and 05-LIVE-VERIFICATION.md records verdicts for the h1, empty-state, zero-cards, no-wallet-gate, and GitHub-link claims (both locales).</verify>
  <done>Honest-empty index confirmed in es-CO + en with no ghost cards and no wallet gate; verdicts recorded.</done>
  <resume-signal>Type "approved" once the honest-empty index is confirmed in both locales with no ghost cards, or describe the issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` exits 0
- `pnpm vitest run tests/architecture/` green (FOUND-11 holds — page under (defi))
- index e2e asserts honest empty + zero cards; axe clean on index
- Evidence-Collector screenshot of honest-empty index (both locales) at /tmp/d2p-verify/05-03-instruments-index.png
</verification>

<success_criteria>
- DEFI-03 (index half): honest "none deployed yet" index under (defi), URL /apps/abrigo/instruments, no wallet gate, no fabricated cards
- DEFI-05 (component half): RiskCallout built — full 4-side ochre hairline, both locales, not dismissible (above-fold-at-360px proof completes on the detail page in 05-04)
- es-CO-first copy authored + native sign-off in docs/copy-review.md
- impeccable-safe: no one-sided colored border, no card-inside-card
</success_criteria>

<output>
After completion, create `.planning/phases/05-read-first-wallet-and-defi-surface/05-03-SUMMARY.md`. Final task is an Evidence-Collector live route checkpoint → `autonomous: false`.
</output>

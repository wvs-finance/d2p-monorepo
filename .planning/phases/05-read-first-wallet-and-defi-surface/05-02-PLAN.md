---
phase: 05-read-first-wallet-and-defi-surface
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lib/wagmi/config.ts
  - lib/wagmi/Providers.tsx
  - app/(defi)/providers.tsx
  - app/globals.css
  - tests/unit/wagmi-config.test.ts
autonomous: false
requirements: [DEFI-01]
user_setup:
  - service: walletconnect
    why: "WalletConnect v2 mobile deeplinks (MetaMask Mobile, Rainbow, Valora/Celo) require a real Reown projectId; the env var is a dev placeholder today (WAIVER-05-02)"
    env_vars:
      - name: NEXT_PUBLIC_WALLETCONNECT_ID
        source: "Reown/WalletConnect Cloud dashboard → Projects → Project ID"
must_haves:
  truths:
    - "wagmiConfig is built via getDefaultConfig (WalletConnect connectors registered), keeping the same 5 chains and ssr:false"
    - "RainbowKitProvider applies the ochre HEX theme (#a87c3a / #f8f5f0)"
    - "(defi)/providers.tsx exports the full WagmiProvider/QueryClient/RainbowKitProvider tree (no longer a passthrough)"
    - "An open RainbowKit modal renders on the deployed/local route with the ochre theme (live screenshot)"
  artifacts:
    - path: "lib/wagmi/config.ts"
      provides: "getDefaultConfig-based wagmiConfig with WalletConnect connectors"
      contains: "getDefaultConfig"
    - path: "lib/wagmi/Providers.tsx"
      provides: "RainbowKitProvider with HEX lightTheme + locale='es'"
      contains: "accentColor: '#a87c3a'"
    - path: "app/(defi)/providers.tsx"
      provides: "Full provider tree re-export"
      contains: "WagmiProviders"
  key_links:
    - from: "app/(defi)/providers.tsx"
      to: "lib/wagmi/Providers.tsx"
      via: "export { WagmiProviders as DefiProviders }"
      pattern: "WagmiProviders"
    - from: "lib/wagmi/Providers.tsx"
      to: "lib/wagmi/config.ts"
      via: "import { wagmiConfig }"
      pattern: "wagmiConfig"
---

<objective>
Activate the `(defi)` wallet provider tree: migrate the wagmi config to `getDefaultConfig` (adds WalletConnect connectors), theme RainbowKit to the locked ochre HEX tokens, and swap the Phase-1 passthrough `(defi)/providers.tsx` for the real tree. End with an Evidence-Collector screenshot of the open themed modal (WAIVER-05-01).

Purpose: Every per-instrument page in 05-04 needs `useAccount`/`useSwitchChain` to resolve — that only works inside the WagmiProvider context this plan turns on. DEFI-01.
Output: `getDefaultConfig` wagmiConfig; HEX-themed RainbowKitProvider; live `(defi)` provider tree; modal screenshot recorded in LIVE-VERIFICATION.md.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-read-first-wallet-and-defi-surface/05-RESEARCH.md
@.planning/phases/05-read-first-wallet-and-defi-surface/05-UI-SPEC.md

<interfaces>
<!-- Extracted from codebase — use directly. -->

CURRENT lib/wagmi/config.ts (you migrate createConfig → getDefaultConfig, keep transports + ssr:false):
```typescript
import { http, createConfig, fallback } from 'wagmi'
export const chains = [celo, mainnet, base, arbitrum, optimism] as const
export const wagmiConfig = createConfig({ chains, transports: { ... }, ssr: false })
export type WagmiConfig = typeof wagmiConfig
```

CURRENT lib/wagmi/Providers.tsx (you add theme + locale to the existing RainbowKitProvider — tree order already correct):
```typescript
'use client'
import '@rainbow-me/rainbowkit/styles.css'
import { RainbowKitProvider } from '@rainbow-me/rainbowkit'
// WagmiProvider > QueryClientProvider > RainbowKitProvider  (already nested correctly)
export function WagmiProviders({ children }: { children: ReactNode }) { ... }
```

CURRENT app/(defi)/providers.tsx (Phase-1 passthrough you replace):
```typescript
'use client'
export function DefiProviders({ children }) { return <>{children}</> }
```
app/(defi)/layout.tsx already does `import { DefiProviders } from './providers'` — keep that export NAME working.

env (already declared): NEXT_PUBLIC_WALLETCONNECT_ID: z.string().min(1) — present in lib/env.ts.

RainbowKit verified facts (05-RESEARCH Sources): getDefaultConfig is a drop-in for createConfig (same chains/transports + appName+projectId); lightTheme accentColor MUST be HEX (oklch breaks the compositor); RainbowKit Locale supports 'es' (NOT 'es-CO'); RainbowKitProvider has NO `chains` prop in v2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate wagmiConfig to getDefaultConfig + theme RainbowKitProvider + swap (defi)/providers.tsx</name>
  <read_first>
    - lib/wagmi/config.ts (the file you migrate)
    - lib/wagmi/Providers.tsx (the file you theme)
    - app/(defi)/providers.tsx + app/(defi)/layout.tsx (the swap target + its consumer)
    - 05-RESEARCH.md § "getDefaultConfig migration" + § "Pattern 2: RainbowKit Theme Wiring" + § "RainbowKit Provider Tree (final form)" (copy exact)
    - 05-UI-SPEC.md § "RainbowKit Theme — HEX Values Required" (the HEX values + CSS var override list)
    - tests/unit/wagmi-config.test.ts (existing — must stay green; it asserts chains/ids)
  </read_first>
  <action>
    1. `lib/wagmi/config.ts`: replace `createConfig({...})` with `getDefaultConfig({...})` imported from `@rainbow-me/rainbowkit`. Keep the SAME `chains` array (`[celo, mainnet, base, arbitrum, optimism]`), the SAME `transports` (fallback http pairs, verbatim), and `ssr: false`. Add `appName: 'd2p Finance'` and `projectId: env.NEXT_PUBLIC_WALLETCONNECT_ID`. Keep `export const chains` and `export type WagmiConfig = typeof wagmiConfig`.

       M4 clarifications (one line each, do NOT skip):
       - Keep `ssr: false`; do NOT add `cookieToInitialState`/`WagmiProvider initialState` — wallet state is client-only by design (this is what avoids the hydration-mismatch pitfall; Pitfall 2 in RESEARCH).
       - Accept `getDefaultConfig`'s default connector set (injected / walletConnect / coinbaseWallet / safe) as-is. (If you choose to trim `safe`, state that choice explicitly in the SUMMARY — default is keep all four.)
       - Confirm `getDefaultConfig` preserves the existing 5-chain `fallback()` transports — `transports` passes through unchanged; the fallback http pairs must be copied verbatim.

    2. `lib/wagmi/Providers.tsx`: add the HEX theme to the existing `RainbowKitProvider`. Import `lightTheme`:
       ```typescript
       const rbkTheme = lightTheme({
         accentColor: '#a87c3a',          // HEX — RainbowKit compositor; oklch breaks it
         accentColorForeground: '#f8f5f0',
         borderRadius: 'medium',
         fontStack: 'system',
         overlayBlur: 'none',
       })
       ```
       Apply `<RainbowKitProvider theme={rbkTheme} locale="es">` (locale 'es' — RainbowKit has no 'es-CO'; our own copy stays es-CO). Do NOT add a `chains` prop (v2 reads from wagmiConfig).

    3. `app/globals.css`: append the verbatim-rendered RainbowKit modal CSS var overrides scoped under `[data-rk]` (these are rendered verbatim → `var(--token)` is safe here, unlike accentColor):
       ```css
       [data-rk] {
         --rk-fonts-body: var(--font-plex-sans), system-ui, sans-serif;
         --rk-colors-modalBackground: var(--bg-elevated);
         --rk-colors-modalBorder: var(--border-default);
         --rk-colors-modalText: var(--text-primary);
         --rk-colors-modalTextSecondary: var(--text-secondary);
       }
       ```

    4. `app/(defi)/providers.tsx`: replace the passthrough with the one-line re-export (keep export NAME `DefiProviders` so layout.tsx is untouched):
       ```typescript
       'use client'
       export { WagmiProviders as DefiProviders } from '@/lib/wagmi/Providers'
       ```

    5. If `tests/unit/wagmi-config.test.ts` needs `NEXT_PUBLIC_WALLETCONNECT_ID` stubbed, ensure the test setup provides `'test'` (05-RESEARCH notes it is already present); adjust only if the migration breaks it.
  </action>
  <verify>
    <automated>grep -q 'getDefaultConfig' lib/wagmi/config.ts && grep -q "accentColor: '#a87c3a'" lib/wagmi/Providers.tsx && grep -q "WagmiProviders as DefiProviders" "app/(defi)/providers.tsx" && grep -q '\[data-rk\]' app/globals.css && pnpm tsc --noEmit && pnpm vitest run tests/unit/wagmi-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/wagmi/config.ts` contains `getDefaultConfig` and NO `createConfig`; still has 5 chains + `ssr: false`
    - `lib/wagmi/Providers.tsx` contains `accentColor: '#a87c3a'`, `accentColorForeground: '#f8f5f0'`, `locale="es"`
    - `app/globals.css` contains `[data-rk]` with `--rk-colors-modalBackground: var(--bg-elevated)`
    - `app/(defi)/providers.tsx` contains `WagmiProviders as DefiProviders`
    - `pnpm tsc --noEmit` exits 0; `tests/unit/wagmi-config.test.ts` exits 0
    - `tests/architecture/defi-bundle-isolation.test.ts` + `no-wallet-in-lab.test.ts` still green (no wallet leak into (lab)/(apps))
  </acceptance_criteria>
  <done>getDefaultConfig migration done; modal themed in HEX + CSS vars; (defi) tree live; config unit test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Evidence-Collector live verify — RainbowKit modal opens + ochre theme (WAIVER-05-01)</name>
  <what-built>
    The `(defi)` route group now mounts the full WagmiProvider + QueryClientProvider + RainbowKitProvider tree with the ochre HEX theme. The connect modal (RainbowKit's own vanilla-extract dialog) opens from a ConnectButton.
  </what-built>
  <how-to-verify>
    Launch the Evidence Collector (per CLAUDE.md invocation block) in live-verification mode:
      Working dir: /home/jmsbpp/apps/d2p/frontend
      Target: https://www.d2pfinance.xyz (fall back to local `pnpm build && pnpm start -p 3040` if the route 4xx/5xx — wallet state is client-only, expect to drive the modal in the browser).
      Task: 05-02 — RainbowKit provider tree + ochre theme.
      Because the per-instrument route (the only ConnectButton host) does not exist until 05-04, mount the ConnectButton on a throwaway scratch route under app/(defi)/ OR drive it via the 05-04 page once available; if neither is ready this turn, render a minimal ConnectButton on a temporary `app/(defi)/_modal-check/page.tsx` (delete after) to exercise the modal.
      Steps per CLAUDE.md: browser_navigate → click ConnectButton → browser_snapshot (assert modal open, dialog role present) → browser_take_screenshot to /tmp/d2p-verify/05-02-rainbowkit-modal.png → browser_evaluate computed accent color ≈ #a87c3a on the modal accent surface.
      Append verdicts to .planning/phases/05-read-first-wallet-and-defi-surface/05-LIVE-VERIFICATION.md. Record WAIVER-05-01 as cleared (screenshot taken) or still-open with reason. Record WAIVER-05-02 (mobile deeplink not live-verifiable — placeholder projectId) as a noted, accepted waiver.
  </how-to-verify>
  <files>.planning/phases/05-read-first-wallet-and-defi-surface/05-LIVE-VERIFICATION.md</files>
  <action>Run the Evidence Collector per the how-to-verify block; record per-claim verdicts + the modal screenshot path in 05-LIVE-VERIFICATION.md. This is a verification-only task — no production code is written here.</action>
  <verify>Screenshot exists at /tmp/d2p-verify/05-02-rainbowkit-modal.png and 05-LIVE-VERIFICATION.md records a verdict for the modal-open + ochre-theme claim.</verify>
  <done>Open RainbowKit modal observed with ochre theme; WAIVER-05-01 cleared (or issues filed); WAIVER-05-02 recorded as accepted.</done>
  <resume-signal>Type "approved" once the modal screenshot exists and the ochre theme is confirmed, or describe the theme/contrast issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` exits 0; `pnpm vitest run tests/unit/wagmi-config.test.ts tests/architecture/` exits 0
- Evidence-Collector screenshot of the open themed modal exists at /tmp/d2p-verify/05-02-rainbowkit-modal.png
- WAIVER-05-01 recorded (cleared via screenshot); WAIVER-05-02 recorded (accepted — placeholder projectId)
- FOUND-11: no wallet imports leaked into (lab)/(apps)
</verification>

<success_criteria>
- DEFI-01: RainbowKit v2 connect integrated via getDefaultConfig (WalletConnect connectors present); modal themed to ochre HEX; (defi) provider tree live
- WAIVER-05-01 cleared (modal screenshot); WAIVER-05-02 accepted (real Reown projectId is a manual follow-up)
- Existing wagmi-config + architecture tests stay green
</success_criteria>

<output>
After completion, create `.planning/phases/05-read-first-wallet-and-defi-surface/05-02-SUMMARY.md`. This plan's final task is an Evidence-Collector live route checkpoint (modal) → `autonomous: false`. If a temporary scratch route was created for the modal check, note its deletion in the SUMMARY.
</output>

---
phase: 07-agent-reasoning-position-surface
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - lib/apps/abrigo/somnia/abi.ts
  - lib/apps/abrigo/somnia/types.ts
  - lib/apps/abrigo/somnia/reader.ts
  - lib/apps/abrigo/somnia/wrapper-adapter.ts
  - lib/apps/abrigo/somnia/liveness.ts
  - lib/apps/abrigo/somnia/prompt-trace.ts
  - lib/apps/abrigo/somnia/snapshot.json
  - lib/apps/abrigo/somnia/capture-snapshot.ts
  - lib/env.ts
  - components/defi/ProvenanceBadge.tsx
  - tsconfig.json
  - tests/unit/wrapper-adapter.test.ts
  - tests/unit/somnia-liveness.test.ts
  - tests/unit/somnia-prompt-trace.test.ts
  - tests/unit/somnia-decision-trace-reader.test.ts
  - tests/unit/provenance-fork-verified.test.tsx
autonomous: true
requirements: [MOD3-TRACE, MOD3-POS, MOD3-MANAGE, MOD3-LIVE, CROSS-09]

must_haves:
  truths:
    - "The deterministic decision pipeline can be reconstructed for a real decision (the built prompt from actual+consensus matches the on-chain _buildPrompt template byte-for-byte)."
    - "A typed WrapperPositionView + single adaptWrapper chokepoint encodes the real-ABI mapping rules; a stale baseline (lastSurviving*/deposited*) or a non-existent realizedCosts field is structurally impossible to surface."
    - "The position reader returns the not-live empty state by default because WRAPPER_DEPLOYED is false (lazy server read)."
    - "A LivenessSource<T> useSyncExternalStore-shaped contract exists with snapshot (default) and polling (SOMNIA_LIVE) realizations only; no live realization, no honker-node dependency."
    - "The ProvenanceBadge gains a neutral fork-verified tier (never green/emerald/status-pass)."
  artifacts:
    - path: "lib/apps/abrigo/somnia/abi.ts"
      provides: "HedgeDecisionRequested event added verbatim + LongGammaWrapper read-only ABI fragment"
      contains: "HedgeDecisionRequested"
    - path: "lib/apps/abrigo/somnia/wrapper-adapter.ts"
      provides: "WrapperPositionView type + adaptWrapper chokepoint + getWrapperPosition gated reader"
      exports: ["adaptWrapper", "getWrapperPosition"]
    - path: "lib/apps/abrigo/somnia/liveness.ts"
      provides: "LivenessSource<T> contract + snapshotSource + pollingSource realizations"
      exports: ["snapshotSource"]
    - path: "lib/apps/abrigo/somnia/prompt-trace.ts"
      provides: "buildPromptTrace(actual, consensus) deterministic reconstruction + SYSTEM_PROMPT constant"
      exports: ["buildPromptTrace", "SYSTEM_PROMPT"]
    - path: "lib/apps/abrigo/somnia/reader.ts"
      provides: "getDecisionTraceById(id) join + extended snapshot rehydration with leg events"
      exports: ["getDecisionTraceById"]
    - path: "components/defi/ProvenanceBadge.tsx"
      provides: "fork-verified neutral tier in TIER_CONFIG + ProvenanceTier union"
      contains: "fork-verified"
  key_links:
    - from: "lib/apps/abrigo/somnia/wrapper-adapter.ts"
      to: "process.env.WRAPPER_DEPLOYED"
      via: "lazy function-body read (mirror SOMNIA_LIVE)"
      pattern: "process\\.env\\.WRAPPER_DEPLOYED"
    - from: "lib/apps/abrigo/somnia/prompt-trace.ts"
      to: "the on-chain _buildPrompt template"
      via: "string.concat reconstruction"
      pattern: "Choose hedge action and size for a long-gamma cCOP-USD position"
    - from: "lib/apps/abrigo/somnia/reader.ts"
      to: "snapshot.json decision leg events"
      via: "decisionId→requestId[] join"
      pattern: "getDecisionTraceById"
---

<objective>
Freeze the Phase-7 data layer with TDD failing-first stubs. This is the shared foundation every downstream plan consumes: the real `HedgeDecisionRequested` ABI fragment + the read-only `LongGammaWrapper` ABI; the typed `WrapperPositionView` + single `adaptWrapper` chokepoint (gated behind `WRAPPER_DEPLOYED=false`); the deterministic prompt-trace reconstruction; the `LivenessSource<T>` snapshot/polling contract; the extended snapshot capture (real on-chain leg events); and the neutral `fork-verified` provenance tier.

Purpose: Honest data contracts must exist before any UI renders them. The §0 honesty invariants (no stale baselines surfaced, no fabricated numbers, no live read in Phase 7, deterministic prompt not fabricated CoT) are encoded HERE as adapter rules + negative tests so downstream JSX physically cannot violate them.

Output: Extended `somnia/*` data layer, `wrapper-adapter.ts`, `liveness.ts`, `prompt-trace.ts`, extended `snapshot.json` + `capture-snapshot.ts`, `WRAPPER_DEPLOYED` in `lib/env.ts`, `fork-verified` tier in `ProvenanceBadge.tsx`, and 5 failing-first test files (excluded from tsconfig until each module lands, then unexcluded in the same commit — the established Phase 5.1/6 pattern).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agent-reasoning-position-surface/07-CONTEXT.md
@.planning/phases/07-agent-reasoning-position-surface/07-UI-SPEC.md
@docs/superpowers/specs/2026-06-02-module3-agent-reasoning-position-surface-design.md

<interfaces>
<!-- Ground truth verified against compiled artifacts 2026-06-02. Use these directly. -->

EXISTING (do NOT re-declare — extend):
- lib/apps/abrigo/somnia/types.ts → HedgeDecisionView, MacroPrintView, HEDGE_ACTION, RESPONSE_STATUS, SnapshotProvenance
- lib/apps/abrigo/somnia/abi.ts → MacroOracleAbi, HedgeStrategistAbi (HedgeDecisionMade + DecisionFailed only)
- lib/apps/abrigo/somnia/reader.ts → getHedgeDecisions, getLatestMacroPrint, getMacroHistory, getSnapshotProvenance + *Live variants; lazy SOMNIA_LIVE at reader.ts:139
- lib/apps/abrigo/somnia/chain.ts → somniaTestnet (defineChain 50312), somniaClient
- lib/apps/abrigo/somnia/surprise.ts → computeSurprise(macro, consensus), formatSurprise(surprise, scale)
- components/defi/ProvenanceBadge.tsx → ProvenanceTier = 'fork-fixture'|'spec'|'schematic'|'testnet-agent'; TIER_CONFIG; ProvenancePill

REAL ABI to add VERBATIM to abi.ts (from out/MacroHedgeStrategist.sol/MacroHedgeStrategist.json):
```ts
{
  type: 'event',
  name: 'HedgeDecisionRequested',
  inputs: [
    { name: 'requestId', type: 'uint256', indexed: true, internalType: 'uint256' },
    { name: 'decisionId', type: 'bytes32', indexed: true, internalType: 'bytes32' },
    { name: 'leg', type: 'uint8', indexed: false, internalType: 'uint8' },
  ],
  anonymous: false,
}
```
Leg enum (MacroHedgeStrategist.sol L64): None=0, Action=1, Size=2.

REAL on-chain constants (MacroHedgeStrategist.sol L47,L50,L53-54,L266-273):
- LLM_AGENT_ID = 12847293847561029384
- MAX_SIZE_BPS = 10000
- SYSTEM_PROMPT (verbatim): "You are a macro hedging strategist. Given the actual macro print and the consensus expectation, choose a hedge action and size for a long-gamma cCOP-USD position. Be deterministic."
- _buildPrompt(actual, consensus) = string.concat("Actual macro print (scaled int): ", actual, ". Consensus expectation (scaled int): ", consensus, ". Choose hedge action and size for a long-gamma cCOP-USD position.")
  → for decision 4083729: "Actual macro print (scaled int): 568. Consensus expectation (scaled int): 500. Choose hedge action and size for a long-gamma cCOP-USD position."

REAL LongGammaWrapper ABI (out/LongGammaWrapper.sol/LongGammaWrapper.json — fork-verified, NOT deployed):
- functions: claimResidual, claimed, close, costMeter, ct0, ct1, deposit, deposited0, deposited1, lastSurviving0, lastSurviving1, owner, pool, positionTokenId, recordStreamia, setCostMeter, state, syncResidual, user
- events: PositionOpened(address indexed user, uint256 tokenId, uint256 deposited0, uint256 deposited1), ResidualClaimed(address indexed user, uint256 paid0, uint256 paid1), ResidualEroded(address indexed user, uint256 eroded0, uint256 eroded1, bytes32 cause), CostMeterSet(address meter)
- COMPOSED reads (NOT wrapper getters): position legs/health via pool() (PanopticPool); surviving collateral via convertToAssets(balanceOf(wrapper)) on ct0()/ct1()
- token id getter is positionTokenId (NOT storedTokenId)
- NEVER surface as "current": lastSurviving0/1, deposited0/1
- NO realizedCosts*, NO recordedStreamia getter
- ResidualEroded.cause is bytes32 (advisory keccak256("INVOLUNTARY")), NOT a 3-way enum
</interfaces>

<snapshot_decision>
ROUTE-KEY + SNAPSHOT-CAPTURE DECISION (resolves spec §2/§9 open question — implement option (b), the preferred path):
- Route key stays `requestId` (the two real ids 4083729/4083997 are requestIds). The existing snapshot field `decisionId` is RENAMED honestly to `requestId`, and the route param `[id]` resolves a requestId.
- EXTEND the snapshot capture to ALSO record the real `bytes32 decisionId` + both leg `requestId`s + the `HedgeDecisionRequested` events for each decision — sourced ONLY from real on-chain `HedgeDecisionRequested` logs (CROSS-09: no fabrication). This lets the trace show the real two-leg progression (Action leg requestId → Size leg requestId) rather than a structural description.
- Justification: option (b) is spec-preferred and is the only honest way to render stage 3/4 (the two distinct LLM legs) as real data. If the real `HedgeDecisionRequested` logs cannot be located on-chain for a decision, the capture writes the leg block as `null` and the trace renders em-dash for the missing leg requestIds — NEVER an invented requestId. Document this fallback in the capture script comment.
</snapshot_decision>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing-first stubs + ABI + types + env gate + provenance tier</name>
  <read_first>
    - lib/apps/abrigo/somnia/abi.ts (the file being modified — match the verbatim-ABI discipline header)
    - lib/apps/abrigo/somnia/types.ts (the file being modified — HedgeDecisionView/MacroPrintView shapes)
    - lib/env.ts (the file being modified — SOMNIA_LIVE server var pattern at lines 16-19,33)
    - components/defi/ProvenanceBadge.tsx (the file being modified — TIER_CONFIG, ProvenanceTier union, neutral schematic/testnet-agent precedent)
    - tests/unit/somnia-provenance-testnet-agent.test.tsx (mirror this test shape for the new tier)
    - tsconfig.json (exclude array at line 36 — the Wave-0 stub-exclusion pattern)
    - docs/superpowers/specs/2026-06-02-module3-agent-reasoning-position-surface-design.md §2, §5 (ABI ground truth + adapter rules)
    - 07-UI-SPEC.md "New provenance tier — fork-verified / not-live" table (exact className + icon + labels)
  </read_first>
  <behavior>
    - prompt-trace: buildPromptTrace(568n, 500n) returns the exact string "Actual macro print (scaled int): 568. Consensus expectation (scaled int): 500. Choose hedge action and size for a long-gamma cCOP-USD position."; SYSTEM_PROMPT export equals the verbatim on-chain constant.
    - wrapper-adapter: adaptWrapper(raw) returns a WrapperPositionView; negative test asserts the returned object has NO `realizedCosts` key and NO `lastSurviving*`/`deposited*` key surfaced as "current"; ResidualEroded.cause typed as bytes32 string (not a 3-way union); getWrapperPosition() returns the not-live empty state ({ deployed: false }) when WRAPPER_DEPLOYED is unset.
    - liveness: snapshotSource(seed).getSnapshot() === seed; subscribe returns an immediate no-op unsubscribe (snapshot); snapshotSource.liveness === 'snapshot'; pollingSource exists with liveness === 'polling'; no module exports liveness === 'live'.
    - decision-trace-reader: getDecisionTraceById('4083729') returns a trace with action='ADD_LONG_GAMMA', sizeBps=6800n, macroValue=568n, consensus=500n, and the leg block (Action+Size requestIds, or null leg requestIds → em-dash signal); unknown id returns null.
    - provenance: ProvenancePill tier="fork-verified" renders className containing "text-text-muted" and "ring-border-default", NOT matching /status-pass|green|emerald/; renders a ShieldCheck svg icon; aria-label is the honest full sentence.
  </behavior>
  <action>
    Add `HedgeDecisionRequested` to `abi.ts` VERBATIM (the fragment in <interfaces>), keeping the "copied verbatim" header discipline. Add a new `LongGammaWrapperAbi` const (read-only fragment) — copy the functions/events listed in <interfaces> verbatim from out/LongGammaWrapper.sol/LongGammaWrapper.json, with a header comment naming the artifact path and the §2 rule "composed reads through pool()/ct0()/ct1(); lastSurviving*/deposited* are stale baselines, NEVER current; no realizedCosts".

    Add to `lib/env.ts` server block: `WRAPPER_DEPLOYED: z.coerce.boolean().optional()` with the comment "Server-only (NEVER NEXT_PUBLIC_). Default undefined/false → position panel renders not-deployed empty state. Re-derive adaptWrapper from the final ABI before flipping (ABI is mid-dev/moving)." Add the matching `runtimeEnv` line `WRAPPER_DEPLOYED: process.env.WRAPPER_DEPLOYED`.

    Extend `ProvenanceBadge.tsx`: add `'fork-verified'` to the `ProvenanceTier` union and to `TIER_CONFIG` with EXACTLY `{ Icon: ShieldCheck, className: 'text-text-muted ring-border-default bg-bg-surface' }` (import `ShieldCheck` from lucide-react). Neutral token — NEVER status-pass/green/emerald. Do not alter existing tiers.

    Write 5 failing-first test files (RED) referencing the not-yet-created modules:
    `tests/unit/somnia-prompt-trace.test.ts`, `tests/unit/wrapper-adapter.test.ts`, `tests/unit/somnia-liveness.test.ts`, `tests/unit/somnia-decision-trace-reader.test.ts`, `tests/unit/provenance-fork-verified.test.tsx` (jsdom). Each test encodes the <behavior> above with exact expected strings/values. Add the four lib-importing stub tests (prompt-trace, wrapper-adapter, liveness, decision-trace-reader) to the tsconfig `exclude` array (they import Task-2 modules that do not exist yet); leave provenance-fork-verified OUT of exclude (ProvenanceBadge is extended in THIS task, so its test goes GREEN immediately).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/provenance-fork-verified.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "HedgeDecisionRequested" lib/apps/abrigo/somnia/abi.ts` (event added)
    - `grep -q "LongGammaWrapperAbi" lib/apps/abrigo/somnia/abi.ts` (read-only ABI added)
    - `grep -qE "lastSurviving.*stale|stale.*baseline|NEVER.*current" lib/apps/abrigo/somnia/abi.ts` (the §2 honesty rule documented in the header)
    - `grep -q "WRAPPER_DEPLOYED" lib/env.ts` (server gate added) and `! grep -q "NEXT_PUBLIC_WRAPPER" lib/env.ts` (never public)
    - `grep -q "'fork-verified'" components/defi/ProvenanceBadge.tsx` and `grep -q "ShieldCheck" components/defi/ProvenanceBadge.tsx`
    - the fork-verified TIER_CONFIG entry contains exact `text-text-muted ring-border-default bg-bg-surface` and does NOT match `/status-pass|green|emerald/`
    - `pnpm vitest run tests/unit/provenance-fork-verified.test.tsx` passes
    - The 4 lib-importing stub test files are listed in tsconfig `exclude`; `tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>HedgeDecisionRequested + LongGammaWrapperAbi present verbatim; WRAPPER_DEPLOYED server gate added; fork-verified neutral tier renders with ShieldCheck and never-green className; provenance test GREEN; 4 lib stubs RED-but-excluded; tsc/biome pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement prompt-trace + wrapper-adapter + liveness + decision-trace reader + extend snapshot capture</name>
  <read_first>
    - lib/apps/abrigo/somnia/reader.ts (the file being modified — rehydration helpers, lazy SOMNIA_LIVE pattern at line 139, BigInt/Date discipline at the boundary)
    - lib/apps/abrigo/somnia/snapshot.json (the file being modified — current shape; decisions[].decisionId actually holds requestIds 4083729/4083997)
    - lib/apps/abrigo/somnia/capture-snapshot.ts (the file being modified — captureDecision/captureMacroReceived pattern, real tx hashes)
    - lib/apps/abrigo/somnia/types.ts (the file being modified — add WrapperPositionView + DecisionTraceView)
    - lib/apps/abrigo/somnia/abi.ts (HedgeDecisionRequested + LongGammaWrapperAbi from Task 1)
    - lib/apps/abrigo/somnia/surprise.ts (computeSurprise — reuse for the trace surprise field)
    - the 4 RED test files from Task 1 (the contracts to satisfy)
    - docs/superpowers/specs/2026-06-02-module3-agent-reasoning-position-surface-design.md §2 (decisionId→requestId join + snapshot caveat), §5 (adaptWrapper, WRAPPER_DEPLOYED, LivenessSource<T>)
    - <snapshot_decision> block above (the route-key + capture decision)
  </read_first>
  <behavior>
    - Same as Task 1 <behavior>, now turned GREEN by real implementations.
    - WrapperPositionView fields (display-only, bigint where numeric): positionTokenId, legsHealth (composed-via-pool placeholder), survivingCollateral0/1 (composed-via-ct0/ct1 placeholder), residualCause (bytes32 string | null). NO realizedCosts. NO lastSurviving*/deposited* as current.
    - DecisionTraceView fields: requestId (string), action, sizeBps (bigint), macroValue (bigint), consensus (bigint), surprise (bigint), builtPrompt (string), legActionRequestId (string|null), legSizeRequestId (string|null), decisionId (bytes32 string|null), sourceTxHash, decidedAt (Date|null).
  </behavior>
  <action>
    Create `lib/apps/abrigo/somnia/prompt-trace.ts`: export `SYSTEM_PROMPT` (verbatim on-chain constant from <interfaces>) and `buildPromptTrace(actual: bigint, consensus: bigint): string` reconstructing the on-chain `_buildPrompt` via the exact `string.concat` template — header comment naming MacroHedgeStrategist.sol L266-273 as source-of-truth and "deterministic — NOT fabricated chain-of-thought".

    Create `lib/apps/abrigo/somnia/wrapper-adapter.ts`: export the `WrapperPositionView` type + `adaptWrapper(raw): WrapperPositionView` chokepoint (the §2 mapping rules live INSIDE this function — composed reads through pool()/ct0()/ct1(); explicitly never reads lastSurviving*/deposited* as current; cause typed bytes32; no realizedCosts) + `getWrapperPosition()` reading `process.env.WRAPPER_DEPLOYED` LAZILY in the function body (mirror reader.ts:139), returning `{ deployed: false }` when unset (Phase 7 default — no live read executes). Comment: "adaptWrapper unreachable behind WRAPPER_DEPLOYED=false in Phase 7; re-derive from final ABI before the flag flips."

    Create `lib/apps/abrigo/somnia/liveness.ts`: export `LivenessSource<T>` type (the useSyncExternalStore shape from spec §5: getSnapshot, subscribe, readonly liveness), `snapshotSource<T>(seed)` (getSnapshot→seed, subscribe→immediate no-op unsub, liveness 'snapshot'), and `pollingSource<T>(...)` (liveness 'polling', interval-based; for SOMNIA_LIVE callers). Header comment: "Phase 7 ships snapshot + polling ONLY; live (honker-SSE) is DEFERRED — the native honker-node addon must NEVER enter package.json."

    Extend `reader.ts`: add `getDecisionTraceById(id: string): DecisionTraceView | null` that joins snapshot decision rows by requestId, computes surprise (computeSurprise) + builtPrompt (buildPromptTrace), and reads the leg block from the extended snapshot (legActionRequestId/legSizeRequestId/decisionId or null). BigInt/Date rehydration at this boundary.

    Extend `snapshot.json`: rename `decisions[].decisionId` → `decisions[].requestId`; add a per-decision `legs: { decisionId: <bytes32>|null, actionRequestId: <string>|null, sizeRequestId: <string>|null }` block. If real bytes32/leg requestIds are not captured, set them `null` (em-dash at render) — NEVER invent. Update `reader.ts` rehydration (the `decisionId` field reference becomes `requestId`) so existing Phase-6 consumers still resolve (HedgeDecisionView.decisionId may map from requestId — keep the Phase-6 view shape stable). Extend `capture-snapshot.ts` to ALSO scan each decision tx receipt for `HedgeDecisionRequested` logs (real on-chain) and populate the leg block; comment the null fallback.

    Remove the 4 lib-importing stub test files from tsconfig `exclude` in the SAME commit the modules land (atomic — pre-commit tsc gate blocks the mid-state, per the 05.1-01 decision).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/somnia-prompt-trace.test.ts tests/unit/wrapper-adapter.test.ts tests/unit/somnia-liveness.test.ts tests/unit/somnia-decision-trace-reader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Choose hedge action and size for a long-gamma cCOP-USD position" lib/apps/abrigo/somnia/prompt-trace.ts` (verbatim _buildPrompt tail)
    - `grep -q "export const SYSTEM_PROMPT" lib/apps/abrigo/somnia/prompt-trace.ts`
    - `grep -q "process.env.WRAPPER_DEPLOYED" lib/apps/abrigo/somnia/wrapper-adapter.ts` and `grep -q "deployed: false" lib/apps/abrigo/somnia/wrapper-adapter.ts`
    - `! grep -q "realizedCosts" lib/apps/abrigo/somnia/wrapper-adapter.ts` and `! grep -q "realizedCosts" lib/apps/abrigo/somnia/types.ts` (non-existent field never referenced as a real field)
    - `grep -q "'snapshot'" lib/apps/abrigo/somnia/liveness.ts` and `grep -q "'polling'" lib/apps/abrigo/somnia/liveness.ts` and `! grep -qE "liveness: 'live'|liveness = 'live'" lib/apps/abrigo/somnia/liveness.ts`
    - `! grep -qi "honker" package.json` (native addon never enters the frontend manifest)
    - `grep -q "getDecisionTraceById" lib/apps/abrigo/somnia/reader.ts`
    - `grep -q "\"requestId\"" lib/apps/abrigo/somnia/snapshot.json` and `grep -q "\"legs\"" lib/apps/abrigo/somnia/snapshot.json`
    - All 4 named stub tests pass; the 4 files are no longer in tsconfig `exclude`; `tsc --noEmit` exits 0; `biome check .` exits 0
    - Phase-6 unit tests still pass: `pnpm vitest run tests/unit/somnia-reader.test.ts tests/unit/somnia-decision-feed.test.tsx`
  </acceptance_criteria>
  <done>prompt-trace reconstructs the verbatim on-chain prompt; adaptWrapper encodes §2 rules with stale baselines/realizedCosts structurally unreachable; getWrapperPosition returns not-live by default behind lazy WRAPPER_DEPLOYED; LivenessSource ships snapshot+polling only; getDecisionTraceById joins by requestId with real leg events (null fallback, never invented); snapshot field renamed honestly without breaking Phase-6 consumers; all 4 stubs GREEN; tsc/biome pass.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/provenance-fork-verified.test.tsx tests/unit/somnia-prompt-trace.test.ts tests/unit/wrapper-adapter.test.ts tests/unit/somnia-liveness.test.ts tests/unit/somnia-decision-trace-reader.test.ts` — all GREEN
- Phase-6 regression: `pnpm vitest run tests/unit/somnia-reader.test.ts tests/unit/somnia-decision-feed.test.tsx` — GREEN
- `tsc --noEmit` exits 0; `biome check .` exits 0
- `! grep -qi "honker" package.json`
- No live RPC read executes during `pnpm build` (WRAPPER_DEPLOYED unset; SOMNIA_LIVE unset)
</verification>

<success_criteria>
- HedgeDecisionRequested + LongGammaWrapper read-only ABI present verbatim (phase success criteria 1 + 2 prerequisite)
- Typed WrapperPositionView + single adaptWrapper chokepoint; stale baselines + realizedCosts structurally unreachable (phase success criterion 2)
- WRAPPER_DEPLOYED server gate; not-live empty state by default; no live read in Phase 7 (phase success criterion 2)
- LivenessSource<T> snapshot + polling only; honker-node absent from package.json (phase success criterion 4)
- fork-verified neutral provenance tier (phase success criterion 2/3)
- Deterministic prompt-trace reconstruction (phase success criterion 1)
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-reasoning-position-surface/07-00-SUMMARY.md`.
NOTE: This plan produces no user-visible route — per CLAUDE.md, skip the Evidence Collector live agent and say so in the SUMMARY (data-layer-only; verified by unit tests + tsc + biome).
</output>

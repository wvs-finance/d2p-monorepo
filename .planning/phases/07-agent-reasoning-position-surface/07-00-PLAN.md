---
phase: 07-agent-reasoning-position-surface
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - lib/apps/abrigo/somnia/abi.ts
  - lib/apps/abrigo/somnia/types.ts
  - lib/apps/abrigo/somnia/reader.ts
  - lib/apps/abrigo/somnia/wrapper-adapter.ts
  - lib/apps/abrigo/somnia/liveness.ts
  - lib/apps/abrigo/somnia/prompt-trace.ts
  - lib/apps/abrigo/somnia/snapshot.json
  - lib/apps/abrigo/somnia/capture-snapshot.ts
  - lib/env.ts
  - components/defi/ProvenanceBadge.tsx
  - tsconfig.json
  - tests/unit/wrapper-adapter.test.ts
  - tests/unit/somnia-liveness.test.ts
  - tests/unit/somnia-prompt-trace.test.ts
  - tests/unit/somnia-decision-trace-reader.test.ts
  - tests/unit/provenance-fork-verified.test.tsx
autonomous: true
requirements: [MOD3-TRACE, MOD3-POS, MOD3-MANAGE, MOD3-LIVE, CROSS-09]

must_haves:
  truths:
    - "The deterministic decision pipeline can be reconstructed for a real decision (the built prompt from actual+consensus matches the on-chain _buildPrompt template byte-for-byte, with the ROUTE-correct consensus: 500 for 4083729, 900 for 4083997)."
    - "Both legs of a real decision show REAL requestIds: the size-leg requestId is the snapshot id; the action-leg requestId is DERIVED as uint256(decisionId) — never invented. The action-leg TIMESTAMP is em-dash when its log falls outside the bounded getLogs window (4083729), never fabricated."
    - "A typed WrapperPositionView + single adaptWrapper chokepoint encodes the real-ABI mapping rules; a stale baseline (lastSurviving*/deposited*) or a non-existent realizedCosts field is structurally impossible to surface."
    - "The position reader returns the not-live empty state by default because WRAPPER_DEPLOYED is false (lazy server read)."
    - "A LivenessSource<T> useSyncExternalStore-shaped contract exists with snapshot (default) and polling (SOMNIA_LIVE) realizations only; no live realization, no honker-node dependency."
    - "The ProvenanceBadge gains a neutral fork-verified tier (never green/emerald/status-pass)."
    - "HedgeDecisionView.decisionId continues to carry the requestId string (stable Phase-6 view shape) so existing consumers (HedgeDecisionFeed/Card) compile unchanged and the [id] route key + DecisionTraceLink href use that field."
  artifacts:
    - path: "lib/apps/abrigo/somnia/abi.ts"
      provides: "HedgeDecisionRequested event added verbatim + LongGammaWrapper read-only ABI fragment"
      contains: "HedgeDecisionRequested"
    - path: "lib/apps/abrigo/somnia/wrapper-adapter.ts"
      provides: "WrapperPositionView type + adaptWrapper chokepoint + getWrapperPosition gated reader"
      exports: ["adaptWrapper", "getWrapperPosition"]
    - path: "lib/apps/abrigo/somnia/liveness.ts"
      provides: "LivenessSource<T> contract + snapshotSource + pollingSource realizations"
      exports: ["snapshotSource"]
    - path: "lib/apps/abrigo/somnia/prompt-trace.ts"
      provides: "buildPromptTrace(actual, consensus) deterministic reconstruction + SYSTEM_PROMPT constant"
      exports: ["buildPromptTrace", "SYSTEM_PROMPT"]
    - path: "lib/apps/abrigo/somnia/reader.ts"
      provides: "getDecisionTraceById(id) join + extended snapshot rehydration with both leg requestIds + per-leg timestamps"
      exports: ["getDecisionTraceById"]
    - path: "components/defi/ProvenanceBadge.tsx"
      provides: "fork-verified neutral tier in TIER_CONFIG + ProvenanceTier union"
      contains: "fork-verified"
  key_links:
    - from: "lib/apps/abrigo/somnia/wrapper-adapter.ts"
      to: "process.env.WRAPPER_DEPLOYED"
      via: "lazy function-body read (mirror SOMNIA_LIVE)"
      pattern: "process\\.env\\.WRAPPER_DEPLOYED"
    - from: "lib/apps/abrigo/somnia/prompt-trace.ts"
      to: "the on-chain _buildPrompt template"
      via: "string.concat reconstruction"
      pattern: "Choose hedge action and size for a long-gamma cCOP-USD position"
    - from: "lib/apps/abrigo/somnia/reader.ts"
      to: "snapshot.json decision leg events"
      via: "size-leg requestId (snapshot id) + action requestId derived from decisionId topic"
      pattern: "getDecisionTraceById"
---

<objective>
Freeze the Phase-7 data layer with TDD failing-first stubs. This is the shared foundation every downstream plan consumes: the real `HedgeDecisionRequested` ABI fragment + the read-only `LongGammaWrapper` ABI; the typed `WrapperPositionView` + single `adaptWrapper` chokepoint (gated behind `WRAPPER_DEPLOYED=false`); the deterministic prompt-trace reconstruction; the `LivenessSource<T>` snapshot/polling contract; the extended snapshot capture (FULL-FIDELITY real on-chain leg events via the verified `getLogs` recipe); and the neutral `fork-verified` provenance tier.

Purpose: Honest data contracts must exist before any UI renders them. The §0 honesty invariants (no stale baselines surfaced, no fabricated numbers, no live read in Phase 7, deterministic prompt not fabricated CoT, both leg requestIds real with em-dash for an out-of-window action-leg timestamp) are encoded HERE as adapter rules + negative tests so downstream JSX physically cannot violate them.

Output: Extended `somnia/*` data layer, `wrapper-adapter.ts`, `liveness.ts`, `prompt-trace.ts`, extended `snapshot.json` (both leg requestIds + size-leg ts + action-leg ts-or-null per decision) + `capture-snapshot.ts` (the verified `getLogs` recipe), `WRAPPER_DEPLOYED` in `lib/env.ts`, `fork-verified` tier in `ProvenanceBadge.tsx`, and 5 failing-first test files (excluded from tsconfig until each module lands, then unexcluded in the same commit — the established Phase 5.1/6 pattern).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agent-reasoning-position-surface/07-CONTEXT.md
@.planning/phases/07-agent-reasoning-position-surface/07-UI-SPEC.md
@docs/superpowers/specs/2026-06-02-module3-agent-reasoning-position-surface-design.md

<interfaces>
<!-- Ground truth verified against compiled artifacts + on-chain logs 2026-06-02. Use these directly. -->

EXISTING (do NOT re-declare — extend):
- lib/apps/abrigo/somnia/types.ts → HedgeDecisionView, MacroPrintView, HEDGE_ACTION, RESPONSE_STATUS, SnapshotProvenance
- lib/apps/abrigo/somnia/abi.ts → MacroOracleAbi, HedgeStrategistAbi (HedgeDecisionMade + DecisionFailed only)
- lib/apps/abrigo/somnia/reader.ts → getHedgeDecisions, getLatestMacroPrint, getMacroHistory, getSnapshotProvenance + *Live variants; lazy SOMNIA_LIVE at reader.ts:139
- lib/apps/abrigo/somnia/chain.ts → somniaTestnet (defineChain 50312), somniaClient
- lib/apps/abrigo/somnia/surprise.ts → computeSurprise(macro, consensus), formatSurprise(surprise, scale)
- lib/apps/abrigo/somnia/bridge.ts → MAX_SIZE_BPS (10000n), decisionToPositionDelta, formatFractionOfMax — REUSE these for any sizeBps→fraction rendering (BigInt-space; do NOT reinvent)
- components/defi/ProvenanceBadge.tsx → ProvenanceTier = 'fork-fixture'|'spec'|'schematic'|'testnet-agent'; TIER_CONFIG; ProvenancePill

REAL ABI to add VERBATIM to abi.ts (from out/MacroHedgeStrategist.sol/MacroHedgeStrategist.json):
```ts
{
  type: 'event',
  name: 'HedgeDecisionRequested',
  inputs: [
    { name: 'requestId', type: 'uint256', indexed: true, internalType: 'uint256' },
    { name: 'decisionId', type: 'bytes32', indexed: true, internalType: 'bytes32' },
    { name: 'leg', type: 'uint8', indexed: false, internalType: 'uint8' },
  ],
  anonymous: false,
}
```
Leg enum (MacroHedgeStrategist.sol L64-68): None=0, Action=1, Size=2.

REAL on-chain constants (MacroHedgeStrategist.sol L47,L50,L53-54,L266-273):
- LLM_AGENT_ID = 12847293847561029384
- MAX_SIZE_BPS = 10000
- SYSTEM_PROMPT (verbatim): "You are a macro hedging strategist. Given the actual macro print and the consensus expectation, choose a hedge action and size for a long-gamma cCOP-USD position. Be deterministic."
- _buildPrompt(actual, consensus) = string.concat("Actual macro print (scaled int): ", actual, ". Consensus expectation (scaled int): ", consensus, ". Choose hedge action and size for a long-gamma cCOP-USD position.")
  → for decision 4083729 (macro 568 / consensus 500): "Actual macro print (scaled int): 568. Consensus expectation (scaled int): 500. Choose hedge action and size for a long-gamma cCOP-USD position."
  → for decision 4083997 (macro 568 / consensus 900): "Actual macro print (scaled int): 568. Consensus expectation (scaled int): 900. Choose hedge action and size for a long-gamma cCOP-USD position."
  (NOTE: consensus DIFFERS per route — 500 vs 900; macroValue is 568 for both. Tests MUST assert the route-correct consensus, not 500 for both.)

VERIFIED LEG-EVENT RETRIEVAL RECIPE (BLOCKER-1 — confirmed on-chain 2026-06-02; FULL-FIDELITY capture; encode EXACTLY):
- The decision receipt (the snapshot `sourceTxHash` = the `HedgeDecisionMade` callback tx) contains ZERO `HedgeDecisionRequested` logs. The two legs are emitted in SEPARATE, EARLIER txs. DO NOT scan the made-tx receipt for legs — it WILL find nothing.
- Contract semantics: the SIZE-leg requestId is the requestId emitted in `HedgeDecisionMade` (L236) — the snapshot already stores it in its `decisionId` field (= 4083729 / 4083997). `decisionId = bytes32(actionLegRequestId)` (L144) — i.e. the bytes32 `decisionId` topic on EVERY `HedgeDecisionRequested` log IS the ACTION-leg requestId, padded to 32 bytes.
- Retrieval (use viem `getLogs`, NOT receipt scanning; RPC caps `eth_getLogs` at 1000 blocks per call):
  - Inputs per decision: `sizeReqId` (= the snapshot id, 4083729 / 4083997) + `madeBlock` (block of the made-tx receipt).
  - Step 1 — SIZE-leg event: `getLogs(address=strategist, event=HedgeDecisionRequested, args={ requestId: sizeReqId }, fromBlock=madeBlock-1000n, toBlock=madeBlock)` → read its `decisionId` topic (bytes32) + that log's block timestamp (size-leg ts).
  - Step 2 — ACTION-leg requestId is DERIVED, never looked up: `actionReqId = uint256(decisionId)`. ALWAYS available; NO event lookup needed for the id.
  - Step 3 — ACTION-leg TIMESTAMP only (best-effort): `getLogs(args={ requestId: actionReqId }, ...)` in a BOUNDED back-scan (1000-block windows) from the size-leg block. If NOT found within the bound → action-leg timestamp = `null` → render em-dash. NEVER fabricate a timestamp.
- VERIFIED expected values (bake into capture-snapshot.ts + reader test fixtures):
  - 4083729: decisionId `0x00…3e4015` → actionReqId 4079637; sizeReqId 4083729, size-leg ts 1780420468; the action log is ~4000 blocks earlier → OUTSIDE the 1000-block window → action-leg ts = `null` (em-dash — honest). BOTH requestIds are real (action derived from decisionId).
  - 4083997: decisionId `0x00…3e5110` → actionReqId 4083984; action-leg ts 1780420553, size-leg ts 1780420556 (both within window → both real timestamps).
- RESULT: the trace ALWAYS shows real requestIds for BOTH legs (action requestId derived from the decisionId topic). Only the action-leg TIMESTAMP is em-dash when its log falls outside the bounded window (4083729). NEVER an invented requestId; NEVER an invented timestamp.

REAL LongGammaWrapper ABI (out/LongGammaWrapper.sol/LongGammaWrapper.json — fork-verified, NOT deployed):
- functions: claimResidual, claimed, close, costMeter, ct0, ct1, deposit, deposited0, deposited1, lastSurviving0, lastSurviving1, owner, pool, positionTokenId, recordStreamia, setCostMeter, state, syncResidual, user
- events: PositionOpened(address indexed user, uint256 tokenId, uint256 deposited0, uint256 deposited1), ResidualClaimed(address indexed user, uint256 paid0, uint256 paid1), ResidualEroded(address indexed user, uint256 eroded0, uint256 eroded1, bytes32 cause), CostMeterSet(address meter)
- COMPOSED reads (NOT wrapper getters): position legs/health via pool() (PanopticPool); surviving collateral via convertToAssets(balanceOf(wrapper)) on ct0()/ct1()
- token id getter is positionTokenId (NOT storedTokenId)
- NEVER surface as "current": lastSurviving0/1, deposited0/1
- NO realizedCosts*, NO recordedStreamia getter
- ResidualEroded.cause is bytes32 (advisory keccak256("INVOLUNTARY")), NOT a 3-way enum
</interfaces>

<snapshot_decision>
ROUTE-KEY + SNAPSHOT-CAPTURE DECISION (resolves spec §2/§9 open question — FULL-FIDELITY leg capture, the user-chosen path):
- Route key stays `requestId` (the two real ids 4083729/4083997 are the SIZE-leg requestIds — the ids the snapshot already stores in its `decisionId` field). The route param `[id]` resolves one of those.
- COMMITMENT (BLOCKER-7): `HedgeDecisionView.decisionId` CONTINUES to carry the requestId string (the stable Phase-6 view shape). It is NOT renamed and NOT removed — existing consumers (HedgeDecisionFeed maps `key={decision.decisionId}`, HedgeDecisionCard, somnia-reader tests) MUST compile unchanged. The `[id]` route key and the DecisionTraceLink href both read this `decisionId` field.
- The NEW `DecisionTraceView` (the trace-specific shape) carries the FULL-FIDELITY leg block: `requestId` (= the size-leg requestId / route key), `legSizeRequestId` (string, = requestId), `legActionRequestId` (string, DERIVED as uint256(decisionId topic) — always real), `decisionId` (bytes32 topic string), `legSizeTimestamp` (Date), `legActionTimestamp` (Date|null → em-dash). This is ADDITIVE; it does not touch HedgeDecisionView.
- EXTEND `snapshot.json` to record, per decision, a `legs` block sourced ONLY from the verified `getLogs` recipe above: `{ decisionIdTopic: <bytes32>, actionRequestId: <string>, sizeRequestId: <string>, sizeTimestamp: <unix-string>, actionTimestamp: <unix-string>|null }`. The size-leg requestId + size timestamp + decisionId topic + derived action requestId are ALWAYS present; only `actionTimestamp` may be null (4083729 — out of window → em-dash). NEVER invent a requestId or timestamp.
- Justification: FULL-FIDELITY is the only honest way to render stage 3/4 (the two distinct LLM legs) as real data with real requestIds. The em-dash for the action-leg timestamp on 4083729 is the honest fallback documented in the capture-script comment.
</snapshot_decision>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing-first stubs + ABI + types + env gate + provenance tier</name>
  <read_first>
    - lib/apps/abrigo/somnia/abi.ts (the file being modified — match the verbatim-ABI discipline header)
    - lib/apps/abrigo/somnia/types.ts (the file being modified — HedgeDecisionView/MacroPrintView shapes; decisionId STAYS the requestId carrier)
    - lib/apps/abrigo/somnia/bridge.ts (REUSE MAX_SIZE_BPS + decisionToPositionDelta + formatFractionOfMax — do NOT reinvent the fraction math)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/src/instrument/MacroHedgeStrategist.sol (the two-leg / three-tx semantics: decisionId = bytes32(actionLegRequestId) at L144; HedgeDecisionMade carries the SIZE-leg requestId at L236; Leg enum L64-68 — REQUIRED to encode the leg recipe correctly)
    - lib/env.ts (the file being modified — SOMNIA_LIVE server var pattern at lines 16-19,33)
    - components/defi/ProvenanceBadge.tsx (the file being modified — TIER_CONFIG, ProvenanceTier union, neutral schematic/testnet-agent precedent)
    - tests/unit/somnia-provenance-testnet-agent.test.tsx (mirror this test shape for the new tier)
    - tsconfig.json (exclude array at line 36 — the Wave-0 stub-exclusion pattern)
    - docs/superpowers/specs/2026-06-02-module3-agent-reasoning-position-surface-design.md §2, §5 (ABI ground truth + adapter rules)
    - 07-UI-SPEC.md "New provenance tier — fork-verified / not-live" table (exact className + icon + labels)
  </read_first>
  <behavior>
    - prompt-trace: buildPromptTrace(568n, 500n) returns the exact string "Actual macro print (scaled int): 568. Consensus expectation (scaled int): 500. Choose hedge action and size for a long-gamma cCOP-USD position."; buildPromptTrace(568n, 900n) returns the route-4083997 string (consensus 900); SYSTEM_PROMPT export equals the verbatim on-chain constant.
    - wrapper-adapter: adaptWrapper(raw) returns a WrapperPositionView; negative test asserts the returned object has NO `realizedCosts` key and NO `lastSurviving*`/`deposited*` key surfaced as "current"; ResidualEroded.cause typed as bytes32 string (not a 3-way union); getWrapperPosition() returns the not-live empty state ({ deployed: false }) when WRAPPER_DEPLOYED is unset.
    - liveness: snapshotSource(seed).getSnapshot() === seed; getServerSnapshot() === seed (stable seed on FIRST paint — no interval/read before mount); subscribe returns an immediate no-op unsubscribe (snapshot); snapshotSource.liveness === 'snapshot'; pollingSource exists with liveness === 'polling'; no module exports liveness === 'live'.
    - decision-trace-reader: getDecisionTraceById('4083729') returns a DecisionTraceView with action='ADD_LONG_GAMMA', sizeBps=6800n, macroValue=568n, consensus=500n, legSizeRequestId='4083729', legActionRequestId='4079637' (DERIVED — real), legActionTimestamp=null (em-dash), legSizeTimestamp a Date; getDecisionTraceById('4083997') returns action='REDUCE', sizeBps=568n, consensus=900n, legActionRequestId='4083984', BOTH leg timestamps non-null Dates; unknown id returns null.
    - provenance: ProvenancePill tier="fork-verified" renders className containing "text-text-muted" and "ring-border-default", NOT matching /status-pass|green|emerald/; renders a ShieldCheck svg icon; aria-label is the honest full sentence.
  </behavior>
  <action>
    FILE-ORDERING (MINOR-15): create each new module FILE in this task only as a RED stub IF it is one of the four lib modules that land in Task 2; those four test files reference modules-not-yet-created, so they go in tsconfig `exclude` FIRST, then the (already-existing) extended files compile — never unexclude a test before its module exists in the same commit (avoids the pre-commit tsc deadlock; mirrors the 05.1-01 decision).

    Add `HedgeDecisionRequested` to `abi.ts` VERBATIM (the fragment in <interfaces>), keeping the "copied verbatim" header discipline. Add a new `LongGammaWrapperAbi` const (read-only fragment) — copy the functions/events listed in <interfaces> verbatim from out/LongGammaWrapper.sol/LongGammaWrapper.json, with a header comment naming the artifact path and the §2 rule "composed reads through pool()/ct0()/ct1(); lastSurviving*/deposited* are stale baselines, NEVER current; no realizedCosts". (MINOR-14: the negative-grep targets in acceptance criteria match CODE IDENTIFIERS, not the documenting comment text — keep the rule sentence phrased so it does not itself contain a bare `realizedCosts` identifier token that a `! grep` would trip on; phrase it "no realized-costs field".)

    Add to `lib/env.ts` server block: `WRAPPER_DEPLOYED: z.coerce.boolean().optional()` with the comment "Server-only (NEVER NEXT_PUBLIC_). Default undefined/false → position panel renders not-deployed empty state. Re-derive adaptWrapper from the final ABI before flipping (ABI is mid-dev/moving)." Add the matching `runtimeEnv` line `WRAPPER_DEPLOYED: process.env.WRAPPER_DEPLOYED`.

    Extend `ProvenanceBadge.tsx`: add `'fork-verified'` to the `ProvenanceTier` union and to `TIER_CONFIG` with EXACTLY `{ Icon: ShieldCheck, className: 'text-text-muted ring-border-default bg-bg-surface' }` (import `ShieldCheck` from lucide-react). Neutral token — NEVER status-pass/green/emerald. Do not alter existing tiers.

    Write 5 failing-first test files (RED) referencing the not-yet-created modules:
    `tests/unit/somnia-prompt-trace.test.ts`, `tests/unit/wrapper-adapter.test.ts`, `tests/unit/somnia-liveness.test.ts`, `tests/unit/somnia-decision-trace-reader.test.ts`, `tests/unit/provenance-fork-verified.test.tsx` (jsdom). Each test encodes the <behavior> above with exact expected strings/values (including the VERIFIED leg values: 4083729 → legActionRequestId '4079637' + legActionTimestamp null; 4083997 → legActionRequestId '4083984' + both timestamps non-null; route-correct consensus 500 vs 900). Add the four lib-importing stub tests (prompt-trace, wrapper-adapter, liveness, decision-trace-reader) to the tsconfig `exclude` array (they import Task-2 modules that do not exist yet); leave provenance-fork-verified OUT of exclude (ProvenanceBadge is extended in THIS task, so its test goes GREEN immediately).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/provenance-fork-verified.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "HedgeDecisionRequested" lib/apps/abrigo/somnia/abi.ts` (event added)
    - `grep -q "LongGammaWrapperAbi" lib/apps/abrigo/somnia/abi.ts` (read-only ABI added)
    - `grep -qE "lastSurviving.*stale|stale.*baseline|NEVER.*current" lib/apps/abrigo/somnia/abi.ts` (the §2 honesty rule documented in the header — comment text, not a forbidden identifier)
    - `grep -q "WRAPPER_DEPLOYED" lib/env.ts` (server gate added) and `! grep -q "NEXT_PUBLIC_WRAPPER" lib/env.ts` (never public)
    - `grep -q "'fork-verified'" components/defi/ProvenanceBadge.tsx` and `grep -q "ShieldCheck" components/defi/ProvenanceBadge.tsx`
    - the fork-verified TIER_CONFIG entry contains exact `text-text-muted ring-border-default bg-bg-surface` and does NOT match `/status-pass|green|emerald/`
    - `pnpm vitest run tests/unit/provenance-fork-verified.test.tsx` passes
    - The 4 lib-importing stub test files are listed in tsconfig `exclude`; `tsc --noEmit` exits 0
    - HedgeDecisionView.decisionId is UNCHANGED (still the requestId string) — `grep -q "decisionId: string" lib/apps/abrigo/somnia/types.ts`
  </acceptance_criteria>
  <done>HedgeDecisionRequested + LongGammaWrapperAbi present verbatim; WRAPPER_DEPLOYED server gate added; fork-verified neutral tier renders with ShieldCheck and never-green className; provenance test GREEN; 4 lib stubs RED-but-excluded; HedgeDecisionView.decisionId unchanged; tsc/biome pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement prompt-trace + wrapper-adapter + liveness + decision-trace reader + extend snapshot capture (verified leg recipe)</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/src/instrument/MacroHedgeStrategist.sol (decisionId = bytes32(actionLegRequestId) L144; HedgeDecisionMade carries size-leg requestId L236; Leg enum L64-68 — the join semantics the capture/reader MUST honor)
    - lib/apps/abrigo/somnia/reader.ts (the file being modified — rehydration helpers, lazy SOMNIA_LIVE pattern at line 139, BigInt/Date discipline at the boundary)
    - lib/apps/abrigo/somnia/snapshot.json (the file being modified — current shape; decisions[].decisionId holds the SIZE-leg requestIds 4083729/4083997)
    - lib/apps/abrigo/somnia/capture-snapshot.ts (the file being modified — captureDecision/captureMacroReceived pattern, real tx hashes; viem client for getLogs)
    - lib/apps/abrigo/somnia/types.ts (the file being modified — add WrapperPositionView + DecisionTraceView; HedgeDecisionView.decisionId stays as-is)
    - lib/apps/abrigo/somnia/abi.ts (HedgeDecisionRequested + LongGammaWrapperAbi from Task 1)
    - lib/apps/abrigo/somnia/surprise.ts (computeSurprise — reuse for the trace surprise field)
    - lib/apps/abrigo/somnia/bridge.ts (REUSE decisionToPositionDelta + formatFractionOfMax for the illustrative fraction; do NOT recompute)
    - the 4 RED test files from Task 1 (the contracts to satisfy — including the verified leg values)
    - docs/superpowers/specs/2026-06-02-module3-agent-reasoning-position-surface-design.md §2 (decisionId→requestId join + snapshot caveat), §5 (adaptWrapper, WRAPPER_DEPLOYED, LivenessSource<T>)
    - <snapshot_decision> block above (the route-key + FULL-FIDELITY capture decision) and the VERIFIED LEG-EVENT RETRIEVAL RECIPE in <interfaces>
  </read_first>
  <behavior>
    - Same as Task 1 <behavior>, now turned GREEN by real implementations.
    - WrapperPositionView fields (display-only, bigint where numeric): positionTokenId, legsHealth (composed-via-pool placeholder), survivingCollateral0/1 (composed-via-ct0/ct1 placeholder), residualCause (bytes32 string | null). NO realizedCosts. NO lastSurviving*/deposited* as current.
    - DecisionTraceView fields (ADDITIVE — does NOT change HedgeDecisionView): requestId (string, = the route key / size-leg requestId), action, sizeBps (bigint), macroValue (bigint), consensus (bigint), surprise (bigint), builtPrompt (string), legSizeRequestId (string, = requestId), legActionRequestId (string, DERIVED = uint256(decisionId topic) — always real), decisionId (bytes32 string), legSizeTimestamp (Date), legActionTimestamp (Date|null → em-dash), sourceTxHash, decidedAt (Date|null).
  </behavior>
  <action>
    Create `lib/apps/abrigo/somnia/prompt-trace.ts`: export `SYSTEM_PROMPT` (verbatim on-chain constant from <interfaces>) and `buildPromptTrace(actual: bigint, consensus: bigint): string` reconstructing the on-chain `_buildPrompt` via the exact `string.concat` template — header comment naming MacroHedgeStrategist.sol L266-273 as source-of-truth and "deterministic — NOT fabricated chain-of-thought". MUST take consensus as a parameter (so 4083729 renders 500 and 4083997 renders 900).

    Create `lib/apps/abrigo/somnia/wrapper-adapter.ts`: export the `WrapperPositionView` type + `adaptWrapper(raw): WrapperPositionView` chokepoint (the §2 mapping rules live INSIDE this function — composed reads through pool()/ct0()/ct1(); explicitly never reads lastSurviving*/deposited* as current; cause typed bytes32; no realized-costs field) + `getWrapperPosition()` reading `process.env.WRAPPER_DEPLOYED` LAZILY in the function body (mirror reader.ts:139), returning `{ deployed: false }` when unset (Phase 7 default — no live read executes). Comment: "adaptWrapper unreachable behind WRAPPER_DEPLOYED=false in Phase 7; re-derive from final ABI before the flag flips."

    Create `lib/apps/abrigo/somnia/liveness.ts`: export `LivenessSource<T>` type (the useSyncExternalStore shape from spec §5: getSnapshot, getServerSnapshot, subscribe, readonly liveness), `snapshotSource<T>(seed)` (getSnapshot→seed, getServerSnapshot→seed — the SAME stable seed on first paint so the client island hydrates without mismatch; subscribe→immediate no-op unsub; liveness 'snapshot'), and `pollingSource<T>(...)` (liveness 'polling', interval-based; getSnapshot/getServerSnapshot return the seed until the first interval tick AFTER mount — never read the interval value before mount). Header comment: "Phase 7 ships snapshot + polling ONLY; live (honker-SSE) is DEFERRED — the native honker-node addon must NEVER enter package.json. getServerSnapshot returns the stable seed so first paint == server text."

    Extend `reader.ts`: add `getDecisionTraceById(id: string): DecisionTraceView | null` that joins snapshot decision rows by the size-leg requestId (the snapshot `decisionId`/route key), computes surprise (computeSurprise) + builtPrompt (buildPromptTrace with the row's consensus) + the illustrative fraction via bridge.ts decisionToPositionDelta/formatFractionOfMax, and reads the `legs` block from the extended snapshot (legSizeRequestId, legActionRequestId DERIVED, decisionId topic, legSizeTimestamp, legActionTimestamp-or-null). BigInt/Date rehydration at this boundary. HedgeDecisionView.decisionId is NOT touched; existing readers stay intact.

    Extend `snapshot.json`: KEEP `decisions[].decisionId` (= the size-leg requestId / route key — do NOT rename; BLOCKER-7 keeps the Phase-6 view shape). ADD a per-decision `legs` block: `{ decisionIdTopic: <bytes32>, actionRequestId: <string>, sizeRequestId: <string>, sizeTimestamp: <unix-string>, actionTimestamp: <unix-string>|null }` populated with the VERIFIED values (4083729: decisionIdTopic 0x00…3e4015, actionRequestId "4079637", sizeRequestId "4083729", sizeTimestamp "1780420468", actionTimestamp null; 4083997: decisionIdTopic 0x00…3e5110, actionRequestId "4083984", sizeRequestId "4083997", sizeTimestamp "1780420556", actionTimestamp "1780420553"). Extend `capture-snapshot.ts` to populate this block via the VERIFIED `getLogs` recipe (size-leg getLogs by requestId in [madeBlock-1000n, madeBlock] → decisionId topic + sizeTimestamp; actionRequestId = uint256(decisionId topic); action-leg timestamp via bounded back-scan getLogs, null when out of window). Comment the null fallback ("action-leg log outside the 1000-block window → actionTimestamp null → em-dash; NEVER fabricate").

    Remove the 4 lib-importing stub test files from tsconfig `exclude` in the SAME commit the modules land (atomic — pre-commit tsc gate blocks the mid-state, per the 05.1-01 decision).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/somnia-prompt-trace.test.ts tests/unit/wrapper-adapter.test.ts tests/unit/somnia-liveness.test.ts tests/unit/somnia-decision-trace-reader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Choose hedge action and size for a long-gamma cCOP-USD position" lib/apps/abrigo/somnia/prompt-trace.ts` (verbatim _buildPrompt tail)
    - `grep -q "export const SYSTEM_PROMPT" lib/apps/abrigo/somnia/prompt-trace.ts`
    - `grep -q "process.env.WRAPPER_DEPLOYED" lib/apps/abrigo/somnia/wrapper-adapter.ts` and `grep -q "deployed: false" lib/apps/abrigo/somnia/wrapper-adapter.ts`
    - `! grep -q "realizedCosts" lib/apps/abrigo/somnia/wrapper-adapter.ts` and `! grep -q "realizedCosts" lib/apps/abrigo/somnia/types.ts` (non-existent field never referenced as a code identifier — comments use "realized-costs" hyphenated to avoid self-collision per MINOR-14)
    - `grep -q "'snapshot'" lib/apps/abrigo/somnia/liveness.ts` and `grep -q "'polling'" lib/apps/abrigo/somnia/liveness.ts` and `grep -q "getServerSnapshot" lib/apps/abrigo/somnia/liveness.ts` and `! grep -qE "liveness: 'live'|liveness = 'live'" lib/apps/abrigo/somnia/liveness.ts`
    - `! grep -qi "honker" package.json` (native addon never enters the frontend manifest)
    - `grep -q "getDecisionTraceById" lib/apps/abrigo/somnia/reader.ts`
    - `grep -q "\"legs\"" lib/apps/abrigo/somnia/snapshot.json` and `grep -q "4079637" lib/apps/abrigo/somnia/snapshot.json` and `grep -q "4083984" lib/apps/abrigo/somnia/snapshot.json` (real derived action requestIds present)
    - `grep -q "getLogs" lib/apps/abrigo/somnia/capture-snapshot.ts` (verified recipe, not receipt scanning) and `! grep -q "getTransactionReceipt.*HedgeDecisionRequested" lib/apps/abrigo/somnia/capture-snapshot.ts`
    - All 4 named stub tests pass; the 4 files are no longer in tsconfig `exclude`; `tsc --noEmit` exits 0; `biome check .` exits 0
    - Phase-6 unit tests still pass: `pnpm vitest run tests/unit/somnia-reader.test.ts tests/unit/somnia-decision-feed.test.tsx`
  </acceptance_criteria>
  <done>prompt-trace reconstructs the verbatim on-chain prompt with route-correct consensus; adaptWrapper encodes §2 rules with stale baselines/realized-costs structurally unreachable; getWrapperPosition returns not-live by default behind lazy WRAPPER_DEPLOYED; LivenessSource ships snapshot+polling only with getServerSnapshot stable seed; getDecisionTraceById joins by requestId with FULL-FIDELITY leg events (both real requestIds, action-leg ts em-dash for 4083729) via the verified getLogs recipe; HedgeDecisionView.decisionId unchanged; all 4 stubs GREEN; tsc/biome pass.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/provenance-fork-verified.test.tsx tests/unit/somnia-prompt-trace.test.ts tests/unit/wrapper-adapter.test.ts tests/unit/somnia-liveness.test.ts tests/unit/somnia-decision-trace-reader.test.ts` — all GREEN
- Phase-6 regression: `pnpm vitest run tests/unit/somnia-reader.test.ts tests/unit/somnia-decision-feed.test.tsx` — GREEN (HedgeDecisionView.decisionId unchanged)
- `tsc --noEmit` exits 0; `biome check .` exits 0
- `! grep -qi "honker" package.json`
- No live RPC read executes during `pnpm build` (WRAPPER_DEPLOYED unset; SOMNIA_LIVE unset)
- Verified leg recipe encoded: snapshot `legs` block holds 4079637 (4083729 action) + 4083984 (4083997 action); 4083729 actionTimestamp is null; both timestamps present for 4083997
</verification>

<success_criteria>
- HedgeDecisionRequested + LongGammaWrapper read-only ABI present verbatim (phase success criteria 1 + 2 prerequisite)
- Typed WrapperPositionView + single adaptWrapper chokepoint; stale baselines + realized-costs structurally unreachable (phase success criterion 2)
- WRAPPER_DEPLOYED server gate; not-live empty state by default; no live read in Phase 7 (phase success criterion 2)
- LivenessSource<T> snapshot + polling only with getServerSnapshot stable seed; honker-node absent from package.json (phase success criterion 4)
- fork-verified neutral provenance tier (phase success criterion 2/3)
- Deterministic prompt-trace reconstruction with route-correct consensus (phase success criterion 1)
- FULL-FIDELITY leg capture: both leg requestIds real (action derived), action-leg timestamp em-dash when out-of-window — verified values baked in (phase success criterion 1)
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-reasoning-position-surface/07-00-SUMMARY.md`.
NOTE: This plan produces no user-visible route — per CLAUDE.md, skip the Evidence Collector live agent and say so in the SUMMARY (data-layer-only; verified by unit tests + tsc + biome). Acceptance note (MINOR-18): `i18n/request.ts` is intentionally UNTOUCHED — the somnia namespace is already registered + deep-merged (request.ts:48,59), so the new trace.*/position.*/manage.*/liveness.* keys reach the runtime without any request.ts change.
</output>

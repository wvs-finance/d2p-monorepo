---
phase: 07-agent-reasoning-position-surface
plan: 01
type: execute
wave: 1
depends_on: ["07-00"]
files_modified:
  - components/defi/somnia/DecisionPipelineTrace.tsx
  - components/defi/somnia/PipelineStage.tsx
  - components/defi/somnia/SystemPromptDisclosure.tsx
  - messages/es-CO/somnia.json
  - messages/en/somnia.json
  - docs/copy-review.md
  - tests/unit/decision-pipeline-trace.test.tsx
autonomous: true
requirements: [MOD3-TRACE, CROSS-09, CROSS-10, CROSS-01]

must_haves:
  truths:
    - "A visitor sees the deterministic decision pipeline as a vertical stepper of exactly 6 equal-weight stages: macro print, built prompt, action leg, size leg, decision, illustrative position."
    - "The built prompt (stage 2) is the real deterministic reconstruction; the real SYSTEM_PROMPT is viewable via a collapsible disclosure (collapsed by default)."
    - "The illustrative position is sizeBps to fraction-of-max (e.g. 6800 bps to 0.68 of max), NEVER a dollar figure."
    - "consensus is labeled operator-supplied wherever shown; copy says deterministic decision pipeline, never the agent reasoning or thoughts; no fabricated chain-of-thought."
    - "Each stage carries the testnet-agent provenance pill; the trace renders with no JS (RSC, no recharts, no SVG layout lib)."
  artifacts:
    - path: "components/defi/somnia/DecisionPipelineTrace.tsx"
      provides: "RSC vertical-stepper trace of 6 stages from a DecisionTraceView"
      contains: "pipeline-trace"
    - path: "components/defi/somnia/PipelineStage.tsx"
      provides: "Equal-weight stage node (1px border-l rail + 2px marker ring) reusing the HedgeDecisionCard row idiom"
      contains: "pipeline-stage"
    - path: "components/defi/somnia/SystemPromptDisclosure.tsx"
      provides: "Collapsible SYSTEM_PROMPT viewer (accent-text trigger + caret), default collapsed"
      contains: "SYSTEM_PROMPT"
    - path: "messages/es-CO/somnia.json"
      provides: "trace.* es-CO copy (UI-SPEC copy elements)"
      contains: "trace"
  key_links:
    - from: "components/defi/somnia/DecisionPipelineTrace.tsx"
      to: "lib/apps/abrigo/somnia/prompt-trace.ts"
      via: "builtPrompt from the DecisionTraceView (built in the reader/adapter)"
      pattern: "DecisionTraceView"
    - from: "components/defi/somnia/PipelineStage.tsx"
      to: "components/defi/ProvenanceBadge.tsx"
      via: "ProvenancePill tier=testnet-agent per stage"
      pattern: "ProvenancePill"
---

<objective>
Build the centerpiece of Phase 7: the deterministic decision-pipeline trace as a vertical stepper (6 equal-weight stages) plus the collapsible real SYSTEM_PROMPT viewer, and the es-CO-first/en copy for the detail surface. Pure RSC, no client island, no recharts, no SVG layout lib — reflows natively at 360px.

Purpose: Honestly show HOW the agent decides (macro print → deterministic built prompt → two-leg temp-0 inference → decision → illustrative position) using only real data and real arithmetic. The illustrative position is sizeBps→fraction-of-max, never a dollar figure. No fabricated chain-of-thought.

Output: DecisionPipelineTrace.tsx, PipelineStage.tsx, SystemPromptDisclosure.tsx, extended somnia.json (es-CO + en) with the trace.* namespace, a copy-review row, and a unit test.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-agent-reasoning-position-surface/07-UI-SPEC.md
@docs/superpowers/specs/2026-06-02-module3-agent-reasoning-position-surface-design.md

<interfaces>
From lib/apps/abrigo/somnia/types.ts (after 07-00):
- DecisionTraceView = { requestId: string; action: 'HOLD'|'ADD_LONG_GAMMA'|'REDUCE'|'EXIT'; sizeBps: bigint; macroValue: bigint; consensus: bigint; surprise: bigint; builtPrompt: string; legActionRequestId: string|null; legSizeRequestId: string|null; decisionId: string|null; sourceTxHash: string; decidedAt: Date|null }
From lib/apps/abrigo/somnia/prompt-trace.ts: SYSTEM_PROMPT, buildPromptTrace(actual, consensus).
From lib/apps/abrigo/somnia/surprise.ts: formatSurprise(surprise, scale=2).
From components/defi/ProvenanceBadge.tsx: ProvenancePill (tier=testnet-agent, subState=recorded).
MAX_SIZE_BPS = 10000 (illustrative fraction = Number(sizeBps)/10000, e.g. 6800 -> "0.68 of max").

Reuse idioms (match exactly; do NOT re-specify):
- HedgeDecisionCard: card shell `rounded-lg border border-border-default bg-bg-surface p-4 space-y-4`; rows `<dl className="divide-y divide-border-default text-sm">`, dt `text-text-muted`, dd `font-mono text-text-primary`.
- Section heading `text-xl font-semibold text-text-primary`; caption `text-sm text-text-muted`.
- ActionBadge neutral shell from HedgeDecisionCard (reuse for the action-leg badge).
</interfaces>

<ui_spec_bindings>
6 stages (UI-SPEC Component & Layout Contract 1, EXACTLY, equal visual weight — same marker, same title size, same card shell):
1. Macro print — macroValue (mono), dataKey label, captured date (em-dash for observed). testnet-agent pill.
2. Built prompt (deterministic) — builtPrompt in a <pre> mono block; caption trace.stage2Caption. testnet-agent.
3. Action leg (Qwen3-30B, temp 0) — action enum badge (reuse ActionBadge neutral shell); model id 12847293847561029384 as mono caption; leg = Action (legActionRequestId mono, em-dash if null). testnet-agent.
4. Size leg (Qwen3-30B, temp 0) — sizeBps (mono); leg = Size (legSizeRequestId mono, em-dash if null). testnet-agent.
5. Decision — action + sizeBps + surprise (surprise gated behind operator-supplied caveat, reuse Phase-6 rule). testnet-agent.
6. Illustrative position — sizeBps to fraction-of-max (real arithmetic), e.g. "6800 bps -> 0.68 of max" — NEVER a dollar figure; caption trace.illustrativeCaption.

Stepper visual (UI-SPEC Spacing connector exception): vertical connector 1px `border-l border-border-default`; 2px stage marker ring; stages separated by `space-y-6`. Each marker uses accent-default fill paired with the stage title (color + position, NOT color alone).

SYSTEM_PROMPT viewer (UI-SPEC 1): collapsible disclosure above/adjacent to stage 2 — accent-text trigger (es: Ver prompt del sistema / en: View system prompt) + caret; expands to a <pre> mono block on bg-bg-canvas with `border border-border-default rounded-md p-4 whitespace-pre-wrap break-words`. Default COLLAPSED (long constant). The BUILT prompt (stage 2) stays expanded. Use native details/summary (RSC, no client JS) — this is a verbatim constant, NOT a FAIL/PASS disposition, so the anti-fishing no-collapse rule does NOT apply.

Copy (UI-SPEC Copywriting Contract; es-CO authored first; add under somnia namespace as trace.*). Each entry = es-CO value then en value:
- trace.title: Traza de la decisión / Decision trace
- trace.stage1: Impresión macro / Macro print
- trace.stage2: Prompt construido (determinista) / Built prompt (deterministic)
- trace.stage2Caption: Reconstruido de forma determinista a partir del dato real + consenso provisto por el operador / Reconstructed deterministically from actual + operator-supplied consensus
- trace.stage3: Decisión de acción (Qwen3-30B, temp 0) / Action leg (Qwen3-30B, temp 0)
- trace.stage4: Decisión de tamaño (Qwen3-30B, temp 0) / Size leg (Qwen3-30B, temp 0)
- trace.stage5: Decisión / Decision
- trace.stage6: Posición ilustrativa / Illustrative position
- trace.systemPromptTrigger: Ver prompt del sistema / View system prompt
- trace.illustrativeCaption: Ilustrativo — no es una posición ejecutada / Illustrative — not an executed position
- trace.fractionOfMax: {frac} del máximo / {frac} of max  (frac = sizeBps/10000, 2dp)
- trace.legLabel: Pierna / Leg
- trace.modelIdLabel: ID del modelo / Model id
- trace.consensusCaveat: suministrado por el operador — no por el mercado / operator-supplied — not market
- trace.provenanceLabel: Somnia testnet · agente (POC) / Somnia testnet · agent (POC)
- trace.provenanceAriaLabel: Somnia testnet · decisión de agente (POC) · registrada / Somnia testnet · agent decision (POC) · recorded
- trace.emptyState: — / —
</ui_spec_bindings>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PipelineStage + SystemPromptDisclosure + DecisionPipelineTrace components</name>
  <read_first>
    - components/defi/somnia/HedgeDecisionCard.tsx (the row/dl idiom + ActionBadge neutral shell to reuse; equal-weight CARD_CLASS)
    - components/defi/somnia/MacroDataPanel.tsx (section heading + caption conventions)
    - components/defi/ProvenanceBadge.tsx (ProvenancePill tier=testnet-agent, subState=recorded)
    - lib/apps/abrigo/somnia/types.ts (DecisionTraceView from 07-00)
    - lib/apps/abrigo/somnia/prompt-trace.ts (SYSTEM_PROMPT import for the disclosure)
    - lib/apps/abrigo/somnia/surprise.ts (formatSurprise for stage 5)
    - 07-UI-SPEC.md Component & Layout Contract 1 + Spacing connector exception + Typography code/prompt rendering
    - the <ui_spec_bindings> above (the 6-stage table + stepper visual + copy)
  </read_first>
  <action>
    Create components/defi/somnia/PipelineStage.tsx (RSC, no 'use client'): a single stage node with data-testid pipeline-stage, the 1px `border-l border-border-default` rail + a 2px stage marker ring (accent-default fill), the stage title (`text-base font-medium text-text-secondary`), a testnet-agent ProvenancePill, and a `<dl className="divide-y divide-border-default text-sm">` body (dt `text-text-muted`, dd `font-mono text-text-primary`). All 6 stages use the IDENTICAL marker + title size + card shell (CROSS-09 equal weight). Props: title, provenanceLabel/ariaLabel, children (the stage-specific rows). Null/missing values render em-dash.

    Create components/defi/somnia/SystemPromptDisclosure.tsx (RSC — native details/summary, no client JS): default COLLAPSED; summary is the accent-text trigger (`text-accent-text hover:text-accent-hover`) + a caret; expands to a `<pre className="font-mono text-sm bg-bg-canvas border border-border-default rounded-md p-4 whitespace-pre-wrap break-words">` rendering the imported SYSTEM_PROMPT. Props: triggerLabel.

    Create components/defi/somnia/DecisionPipelineTrace.tsx (RSC): takes a DecisionTraceView + a pre-translated TraceStrings object (threaded from the page; NO getTranslations inside the component — match the Phase-6 string-threading pattern). Renders data-testid pipeline-trace containing exactly 6 PipelineStage children in order, separated by `space-y-6`. Stage 2 mounts the SystemPromptDisclosure adjacent + the built prompt in a <pre> mono block (always expanded). Stage 3 reuses the ActionBadge neutral shell + shows the model id 12847293847561029384 as a mono caption + legActionRequestId (em-dash if null). Stage 4 shows sizeBps + legSizeRequestId. Stage 5 shows action + sizeBps + surprise WITH the operator-supplied caveat adjacent (reuse the Phase-6 gating rule). Stage 6 computes Number(sizeBps)/10000 to 2dp and renders the trace.fractionOfMax string (e.g. 0.68 of max) + the illustrativeCaption — NEVER a dollar sign. The component MUST NOT contain the words reasoning / thoughts / chain-of-thought / razonamiento / pensamiento.

    Write the failing-first test tests/unit/decision-pipeline-trace.test.tsx (jsdom): render DecisionPipelineTrace with the 4083729 fixture (ADD_LONG_GAMMA/6800/568/500); assert exactly 6 pipeline-stage nodes; assert the built-prompt pre contains "Actual macro print (scaled int): 568"; assert stage 6 renders 0.68 and no dollar sign; assert the operator-supplied caveat appears; assert no reasoning/razonamiento text. RED first; GREEN after the components land.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/decision-pipeline-trace.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q pipeline-trace components/defi/somnia/DecisionPipelineTrace.tsx
    - grep -q pipeline-stage components/defi/somnia/PipelineStage.tsx
    - grep -q 'border-l border-border-default' components/defi/somnia/PipelineStage.tsx (1px connector rail)
    - grep -q SYSTEM_PROMPT components/defi/somnia/SystemPromptDisclosure.tsx and grep -q details components/defi/somnia/SystemPromptDisclosure.tsx
    - grep -q whitespace-pre-wrap components/defi/somnia/SystemPromptDisclosure.tsx
    - grep -q 12847293847561029384 components/defi/somnia/DecisionPipelineTrace.tsx (real model id caption)
    - grep -q 10000 components/defi/somnia/DecisionPipelineTrace.tsx (fraction-of-max arithmetic)
    - none of the three trace components contain 'use client' (grep -L returns all three)
    - no banned reasoning words: grep -riE 'reasoning|thoughts|chain-of-thought|razonamiento|pensamiento' on the three components returns nothing
    - no literal dollar sign in DecisionPipelineTrace.tsx: grep -F returns nothing for a dollar character
    - pnpm vitest run tests/unit/decision-pipeline-trace.test.tsx passes
  </acceptance_criteria>
  <done>The vertical-stepper trace renders 6 equal-weight stages; SYSTEM_PROMPT collapsible default-collapsed; illustrative position is fraction-of-max not dollars; no client JS; no fabricated-CoT vocabulary; unit test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: es-CO-first + en somnia.trace.* copy + copy-review row</name>
  <read_first>
    - messages/es-CO/somnia.json (the file being modified — existing panel/feed/bridge namespaces; nested-object format per the 01-03 decision)
    - messages/en/somnia.json (the file being modified — recursive parity with es-CO)
    - docs/copy-review.md (append the trace row; es-CO-first sign-off discipline)
    - the <ui_spec_bindings> Copy block above (the exact strings)
    - 07-UI-SPEC.md Copywriting Contract (verbatim es-CO/en pairs)
  </read_first>
  <action>
    Add a trace sub-object under somnia in BOTH messages/es-CO/somnia.json and messages/en/somnia.json with the exact keys + values from the <ui_spec_bindings> Copy block (es-CO authored first; en is the author-quality companion). Keep nested-object format (not flat dot keys). Recursive parity: every key present in both locales. Append a row to docs/copy-review.md for the somnia.trace.* namespace marking es-CO authored / native sign-off pending (non-blocking per the 05-03 precedent).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/decision-pipeline-trace.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q trace messages/es-CO/somnia.json and grep -q trace messages/en/somnia.json (trace namespace added to both locales)
    - es-CO trace.title value is the Spanish string and en trace.title value is Decision trace (verified by reading both files)
    - key parity: the set of keys under somnia.trace is identical in es-CO and en (every UI-SPEC copy key present in both)
    - grep -q 'somnia.trace' docs/copy-review.md (copy-review row appended)
    - tsc --noEmit exits 0 and biome check . exits 0 (next-intl message shape valid)
  </acceptance_criteria>
  <done>somnia.trace.* present in both locales with full key parity and the exact UI-SPEC strings; copy-review row recorded; tsc/biome pass.</done>
</task>

</tasks>

<verification>
- pnpm vitest run tests/unit/decision-pipeline-trace.test.tsx — GREEN
- tsc --noEmit exits 0; biome check . exits 0
- Trace components contain no 'use client', no reasoning/razonamiento vocabulary, no literal dollar sign
- somnia.trace.* key parity across es-CO and en
</verification>

<success_criteria>
- Deterministic decision-pipeline trace renders as a 6-stage vertical stepper (phase success criterion 1)
- Real SYSTEM_PROMPT viewable (collapsible) + built prompt shown deterministically (phase success criterion 1)
- Illustrative position = sizeBps→fraction-of-max, never a dollar figure (phase success criterion 1)
- consensus labeled operator-supplied; no fabricated chain-of-thought (phase success criterion 1, honesty invariant)
</success_criteria>

<output>
After completion, create .planning/phases/07-agent-reasoning-position-surface/07-01-SUMMARY.md.
NOTE: The components are not yet mounted on a route in this plan (07-03 wires the /[id] page). Per CLAUDE.md, the Evidence Collector live-verification runs in 07-03 against the rendered /apps/abrigo/agent/[id] route — note in this SUMMARY that live-verify is deferred to 07-03 (components verified here by unit test + tsc + biome only).
</output>

---
phase: 07-agent-reasoning-position-surface
plan: 02
type: execute
wave: 1
depends_on: ["07-00"]
files_modified:
  - components/defi/somnia/PositionPanel.tsx
  - components/defi/somnia/ManagementControls.tsx
  - components/defi/somnia/LivenessPill.tsx
  - messages/es-CO/somnia.json
  - messages/en/somnia.json
  - docs/copy-review.md
  - tests/unit/position-panel.test.tsx
  - tests/unit/management-controls.test.tsx
  - tests/unit/liveness-pill.test.tsx
autonomous: true
requirements: [MOD3-POS, MOD3-MANAGE, MOD3-LIVE, CROSS-09, CROSS-01, CROSS-10]

must_haves:
  truths:
    - "The position-execution panel renders under the neutral fork-verified / not-live tier showing the NOT-DEPLOYED empty state — every value an em-dash, no fabricated number, no dollar figure, no green token."
    - "Management controls (close / claim / agent) render visible-but-disabled, perceivably disabled beyond color (native disabled + aria-disabled + aria-describedby + Lock icon + persistent inline caption)."
    - "The rendered DOM of the panel + controls contains no executed/realized/placed/ejecutad/realizad text."
    - "The liveness pill renders snapshot (default) or polling (SOMNIA_LIVE) only — never a live state this phase; CROSS-09 color + icon + text + aria-label."
    - "The panel surfaces only the safe WrapperPositionView display labels; lastSurviving*/deposited*/realizedCosts never appear."
  artifacts:
    - path: "components/defi/somnia/PositionPanel.tsx"
      provides: "RSC not-deployed empty-state position panel under the fork-verified tier"
      contains: "fork-verified"
    - path: "components/defi/somnia/ManagementControls.tsx"
      provides: "Three disabled buttons (close/claim/agent) with Lock icon + persistent inline caption + aria-describedby"
      contains: "aria-disabled"
    - path: "components/defi/somnia/LivenessPill.tsx"
      provides: "useSyncExternalStore client island rendering snapshot/polling only"
      contains: "useSyncExternalStore"
    - path: "messages/es-CO/somnia.json"
      provides: "position.* + liveness.* es-CO copy"
      contains: "position"
  key_links:
    - from: "components/defi/somnia/PositionPanel.tsx"
      to: "components/defi/ProvenanceBadge.tsx"
      via: "ProvenancePill tier=fork-verified"
      pattern: "tier=.fork-verified"
    - from: "components/defi/somnia/ManagementControls.tsx"
      to: "the inline caption element"
      via: "aria-describedby referencing the caption id"
      pattern: "aria-describedby"
    - from: "components/defi/somnia/LivenessPill.tsx"
      to: "lib/apps/abrigo/somnia/liveness.ts"
      via: "LivenessSource snapshot/polling via useSyncExternalStore"
      pattern: "snapshotSource|LivenessSource"
---

<objective>
Build the honest position-execution surface: the NOT-DEPLOYED position panel under the new neutral fork-verified / not-live tier, the designed-but-disabled management controls (close / claim / agent) that are perceivably disabled beyond color, and the liveness pill (snapshot/polling only — live is deferred). Every position value is an em-dash; no fabricated number, no dollar figure, no green token; the rendered DOM never implies an executed/realized/placed position.

Purpose: Honestly represent a fork-verified-but-not-deployed contract. The panel encodes the safe WrapperPositionView display labels only (the §2/§5 stale-baseline rules already live inside adaptWrapper from 07-00); management is disabled with an always-rendered reason; liveness is honest.

Output: PositionPanel.tsx, ManagementControls.tsx, LivenessPill.tsx, extended somnia.json (position.* + liveness.* in es-CO + en), a copy-review row, and three unit tests.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-agent-reasoning-position-surface/07-UI-SPEC.md
@docs/superpowers/specs/2026-06-02-module3-agent-reasoning-position-surface-design.md

<interfaces>
From lib/apps/abrigo/somnia/wrapper-adapter.ts (after 07-00): WrapperPositionView type + getWrapperPosition() returning { deployed: false } by default (WRAPPER_DEPLOYED unset). adaptWrapper is unreachable in Phase 7.
From lib/apps/abrigo/somnia/liveness.ts (after 07-00): LivenessSource<T>, snapshotSource(seed) (liveness 'snapshot'), pollingSource(...) (liveness 'polling'). No live realization.
From components/defi/ProvenanceBadge.tsx (after 07-00): ProvenancePill tier='fork-verified' (neutral, ShieldCheck, className text-text-muted ring-border-default bg-bg-surface).
From components/defi/WalletStatusPill.tsx: the CROSS-09 pill shell verbatim `inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-sm font-normal ring-1 ring-inset` + the useMounted/useSyncExternalStore client-island precedent.

Reuse idioms: card shell `rounded-lg border border-border-default bg-bg-surface p-4 space-y-4`; dl row idiom from HedgeDecisionCard; caption `text-sm text-text-muted`.
</interfaces>

<ui_spec_bindings>
Position panel (UI-SPEC Component & Layout Contract 2 — not-deployed empty state):
- Panel shell matches HedgeDecisionCard: `rounded-lg border border-border-default bg-bg-surface p-4 space-y-4`.
- Header: fork-verified / not-live pill + heading (es: Posición (LongGammaWrapper) / en: Position (LongGammaWrapper)).
- Body: WrapperPositionView fields as a <dl> row list (HedgeDecisionCard idiom), every value showing em-dash. Fields surfaced (display labels only, no value computed in Phase 7): position legs/health (composed via pool()), surviving collateral (via ct0()/ct1() convertToAssets), positionTokenId, residual. NEVER surface lastSurviving0/1 or deposited0/1 as current; NO realizedCosts field exists.
- Empty-state heading (es: Sin posición en cadena / en: No on-chain position) + body (es: El contrato está verificado en fork pero no desplegado. Cuando se despliegue, esta vista mostrará la posición real. / en: The contract is fork-verified but not deployed. Once deployed, this view will show the real position.). Caption under heading (es: No desplegado — verificado en fork, sin posición en cadena. / en: Not deployed — fork-verified, no on-chain position.) `text-sm text-text-muted`.
- No dollar sign, no fabricated number, no green token anywhere in this panel.

Management controls (UI-SPEC Component & Layout Contract 3 — visible buttons + persistent inline caption):
- Three real <button disabled> elements (close / claim / agent).
- Native disabled attribute set; aria-disabled=true; aria-describedby pointing at the inline caption id.
- Visual: `text-text-muted border-border-default bg-bg-surface cursor-not-allowed opacity-60` + a lucide-react Lock icon inside each button (color + icon + text). Neutral shell (NOT accent fill).
- Inline caption (one per group, referenced by all three buttons via aria-describedby): es: No disponible — fork-verificado, no desplegado. Sin transacción. / en: Unavailable — fork-verified, not deployed. No transaction.
- Rendered DOM MUST NOT contain executed/realized/placed/ejecutad/realizad.
- Button labels (es / en): close = Cerrar / Close; claim = Reclamar residual / Claim residual; agent = Control del agente / Agent control.

Liveness pill (UI-SPEC Component & Layout Contract 4 + Color liveness table — client island):
- Reuses the WalletStatusPill CROSS-09 shell verbatim.
- snapshot (default): neutral className `text-text-muted ring-border-default bg-bg-surface`, icon CircleDashed, text (es: instantánea · — / en: snapshot · —).
- polling (SOMNIA_LIVE): neutral same className, icon RefreshCw, text (es: sondeo / en: polling).
- live: DEFERRED — MUST NOT render in Phase 7 (a live pill is a regression flag in the honesty greps).
- Driven by LivenessSource via useSyncExternalStore; on snapshot the subscribe is an immediate no-op unsub. Placed in the page header (mounted by 07-03), not inside the trace.

Copy keys to add under somnia (es-CO first / en):
- position.heading: Posición (LongGammaWrapper) / Position (LongGammaWrapper)
- position.emptyHeading: Sin posición en cadena / No on-chain position
- position.emptyBody: El contrato está verificado en fork pero no desplegado. Cuando se despliegue, esta vista mostrará la posición real. / The contract is fork-verified but not deployed. Once deployed, this view will show the real position.
- position.notLiveCaption: No desplegado — verificado en fork, sin posición en cadena. / Not deployed — fork-verified, no on-chain position.
- position.provenanceLabel: verificado en fork · no desplegado / fork-verified · not deployed
- position.provenanceAriaLabel: Fuente: verificado en fork — el contrato LongGammaWrapper no ha sido desplegado en cadena; no existe posición real. / Source: fork-verified — the LongGammaWrapper contract is not deployed on-chain; no real position exists.
- position.fieldLegs: Piernas / posición / Position legs
- position.fieldCollateral: Colateral sobreviviente / Surviving collateral
- position.fieldTokenId: ID del token de posición / Position token id
- position.fieldResidual: Residual / Residual
- position.emptyState: — / —
- manage.close: Cerrar / Close
- manage.claim: Reclamar residual / Claim residual
- manage.agent: Control del agente / Agent control
- manage.caption: No disponible — fork-verificado, no desplegado. Sin transacción. / Unavailable — fork-verified, not deployed. No transaction.
- liveness.snapshot: instantánea · — / snapshot · —
- liveness.polling: sondeo / polling
- liveness.ariaSnapshot: Estado de actualización: instantánea — datos registrados, sin actualización en vivo. / Refresh state: snapshot — recorded data, no live update.
- liveness.ariaPolling: Estado de actualización: sondeo periódico. / Refresh state: periodic polling.
</ui_spec_bindings>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PositionPanel (not-deployed empty state) + ManagementControls (disabled, perceivable)</name>
  <read_first>
    - lib/apps/abrigo/somnia/wrapper-adapter.ts (WrapperPositionView + getWrapperPosition deployed:false default from 07-00)
    - components/defi/ProvenanceBadge.tsx (ProvenancePill tier=fork-verified from 07-00)
    - components/defi/somnia/HedgeDecisionCard.tsx (card shell + dl row idiom to match)
    - components/defi/somnia/MacroDataPanel.tsx (section heading + caption conventions)
    - 07-UI-SPEC.md Component & Layout Contract 2 + 3 + Copywriting Contract
    - the <ui_spec_bindings> Position panel + Management controls blocks above
    - docs/superpowers/specs/2026-06-02-module3-agent-reasoning-position-surface-design.md §2 (the fields that must NEVER surface), §0 invariants 2+5
  </read_first>
  <action>
    Create components/defi/somnia/PositionPanel.tsx (RSC, no 'use client'): card shell `rounded-lg border border-border-default bg-bg-surface p-4 space-y-4`; header = ProvenancePill tier=fork-verified (label/ariaLabel threaded) + heading `text-xl font-semibold text-text-primary`. Directly under the heading: the notLiveCaption (`text-sm text-text-muted`). Body = a <dl className="divide-y divide-border-default text-sm"> listing exactly the safe display fields (position legs/health, surviving collateral, position token id, residual) with dt `text-text-muted` and dd `font-mono text-text-primary` showing the em-dash for every value (not-deployed empty state). Then the empty-state heading + body block. NO dollar sign, no fabricated number, no green token, and the component MUST NOT reference lastSurviving / deposited0 / deposited1 / realizedCosts anywhere. Props: a pre-translated strings object (threaded from the page). data-testid position-panel on the root.

    Create components/defi/somnia/ManagementControls.tsx (RSC, no 'use client'): a control group of three real <button disabled aria-disabled="true"> elements (close / claim / agent), each with a lucide-react Lock icon (color + icon + text) and the neutral shell `text-text-muted border-border-default bg-bg-surface cursor-not-allowed opacity-60` (NOT accent fill). A single persistent inline caption element with a stable id (e.g. management-not-live-caption) rendering manage.caption; all three buttons set aria-describedby to that id. data-testid management-controls on the group. Rendered text MUST NOT contain executed/realized/placed/ejecutad/realizad. Props: pre-translated strings.

    Write failing-first tests tests/unit/position-panel.test.tsx and tests/unit/management-controls.test.tsx (jsdom). PositionPanel test: renders the fork-verified pill, the empty-state heading, every dd is em-dash, no dollar sign, className contains no status-pass/green/emerald, and the rendered text has no lastSurviving/deposited/realizedCosts. ManagementControls test: three disabled buttons, each has aria-disabled=true + aria-describedby pointing to the caption id, each contains a Lock svg, and the rendered text has no executed/realized/placed/ejecutad/realizad. RED first; GREEN after the components land.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/position-panel.test.tsx tests/unit/management-controls.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q position-panel components/defi/somnia/PositionPanel.tsx and grep -q fork-verified components/defi/somnia/PositionPanel.tsx
    - PositionPanel does NOT match status-pass|green|emerald (grep -E returns nothing)
    - PositionPanel references none of lastSurviving, deposited0, deposited1, realizedCosts (grep -E returns nothing)
    - no literal dollar sign in PositionPanel.tsx (grep -F returns nothing)
    - grep -q management-controls components/defi/somnia/ManagementControls.tsx
    - grep -c 'aria-disabled' ManagementControls.tsx is at least 3 (or one shared attr applied to all three; reviewer confirms all three buttons are disabled)
    - grep -q aria-describedby components/defi/somnia/ManagementControls.tsx and grep -q Lock components/defi/somnia/ManagementControls.tsx
    - ManagementControls contains none of executed|realized|placed|ejecutad|realizad (grep -iE returns nothing)
    - pnpm vitest run tests/unit/position-panel.test.tsx tests/unit/management-controls.test.tsx passes
  </acceptance_criteria>
  <done>PositionPanel renders the not-deployed empty state under the neutral fork-verified tier with all em-dash values and zero stale-baseline references; ManagementControls renders three disabled buttons perceivable beyond color via Lock icon + aria-disabled + aria-describedby caption; no executed/realized vocabulary; tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LivenessPill client island (snapshot/polling only) + position/liveness copy</name>
  <read_first>
    - components/defi/WalletStatusPill.tsx (the CROSS-09 pill shell verbatim + useMounted client-island precedent)
    - lib/apps/abrigo/somnia/liveness.ts (LivenessSource, snapshotSource, pollingSource from 07-00)
    - messages/es-CO/somnia.json + messages/en/somnia.json (the files being modified — nested-object format, recursive parity)
    - docs/copy-review.md (append the position/liveness row)
    - 07-UI-SPEC.md Component & Layout Contract 4 + Color liveness pill table + Copywriting Contract
    - the <ui_spec_bindings> Liveness pill + Copy keys blocks above
  </read_first>
  <action>
    Create components/defi/somnia/LivenessPill.tsx ('use client'): a client island that consumes a LivenessSource<T> via React.useSyncExternalStore(source.subscribe, source.getSnapshot, source.getSnapshot) and renders the CROSS-09 pill shell verbatim from WalletStatusPill. Render ONLY two states based on source.liveness: snapshot (CircleDashed icon + liveness.snapshot text + liveness.ariaSnapshot aria-label) and polling (RefreshCw icon + liveness.polling text + liveness.ariaPolling aria-label), both with the neutral className `text-text-muted ring-border-default bg-bg-surface`. The component MUST NOT have any branch that renders a live state or a green/status-pass className (live is deferred this phase). Default source is snapshotSource. Props: source (LivenessSource), pre-translated strings.

    Add the position.* + manage.* + liveness.* keys (exact values from <ui_spec_bindings> Copy keys) under somnia in BOTH messages/es-CO/somnia.json and messages/en/somnia.json (es-CO first, recursive parity). Append a docs/copy-review.md row for the somnia.position/manage/liveness namespaces (es-CO authored / native sign-off pending).

    Write failing-first test tests/unit/liveness-pill.test.tsx (jsdom): render LivenessPill with snapshotSource → asserts CircleDashed svg + snapshot text + neutral className + NO live text; render with a polling-liveness source → asserts RefreshCw + polling text. Assert the component source contains no 'live' state branch and no status-pass/green className. RED first; GREEN after the component lands.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/liveness-pill.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q useSyncExternalStore components/defi/somnia/LivenessPill.tsx
    - grep -q "'use client'" components/defi/somnia/LivenessPill.tsx
    - grep -q CircleDashed components/defi/somnia/LivenessPill.tsx and grep -q RefreshCw components/defi/somnia/LivenessPill.tsx
    - LivenessPill matches none of status-pass|green|emerald and does NOT contain a live render branch (grep for the word live in a render/return context returns nothing; the deferred live state is absent)
    - grep -q 'inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-sm font-normal ring-1 ring-inset' components/defi/somnia/LivenessPill.tsx (verbatim CROSS-09 shell)
    - grep -q position messages/es-CO/somnia.json and grep -q liveness messages/es-CO/somnia.json
    - position/manage/liveness key parity between es-CO and en (reviewer reads both; every UI-SPEC key present in both)
    - grep -qE 'position|liveness' docs/copy-review.md (copy-review row appended)
    - pnpm vitest run tests/unit/liveness-pill.test.tsx passes; tsc --noEmit exits 0; biome check . exits 0
  </acceptance_criteria>
  <done>LivenessPill consumes a LivenessSource via useSyncExternalStore and renders snapshot/polling only (no live branch, neutral color); position/manage/liveness copy present in both locales with parity; copy-review row recorded; test GREEN; tsc/biome pass.</done>
</task>

</tasks>

<verification>
- pnpm vitest run tests/unit/position-panel.test.tsx tests/unit/management-controls.test.tsx tests/unit/liveness-pill.test.tsx — all GREEN
- tsc --noEmit exits 0; biome check . exits 0
- PositionPanel + ManagementControls rendered DOM contains no executed/realized/placed/ejecutad/realizad, no dollar sign, no green token
- PositionPanel references none of lastSurviving/deposited/realizedCosts
- LivenessPill has no live branch and no honker import
</verification>

<success_criteria>
- Position panel renders not-deployed empty state under the neutral fork-verified tier with em-dash values (phase success criterion 2)
- Management controls visible-but-disabled, perceivable beyond color, no transact, no executed/realized vocabulary (phase success criterion 3)
- Liveness pill renders snapshot/polling only with honest CROSS-09 encoding; live deferred (phase success criterion 4)
</success_criteria>

<output>
After completion, create .planning/phases/07-agent-reasoning-position-surface/07-02-SUMMARY.md.
NOTE: Components are not yet mounted on a route here (07-03 wires the /[id] page). Per CLAUDE.md, the Evidence Collector live-verification runs in 07-03 against the rendered /apps/abrigo/agent/[id] route — note in this SUMMARY that live-verify is deferred to 07-03 (components verified here by unit test + tsc + biome only).
</output>

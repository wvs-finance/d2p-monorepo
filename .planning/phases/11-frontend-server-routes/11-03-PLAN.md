---
phase: 11-frontend-server-routes
plan: 03
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - packages/frontend/components/defi/cornerstone/CornerstoneClientShell.tsx
  - packages/frontend/lib/apps/abrigo/cornerstone/workflow-engine.ts
  - packages/frontend/tsconfig.json
  - packages/frontend/tests/architecture/buildbear-decoupling.test.ts
autonomous: true
requirements: [MINT-02, MINT-03]
must_haves:
  truths:
    - "handleLiveConfirm hard-branches on resolvedMode === 'buildbear' as the FIRST statement, returning BEFORE any /api/abrigo/agent1 reference (BuildBear path never calls Somnia on any path)"
    - "The buildbear branch contains ZERO setResolvedMode('replay') calls (Phase 12 owns the fork-used advisory)"
    - "The three existing live-path setResolvedMode('replay') flips are PRESERVED unchanged"
    - "buildUpstreamFromReplayArtifact(presetId) sources the mandate from the recorded replay preset (getPresetById), never from a live Somnia Agent-1 response"
    - "runWorkflowLive signature is unchanged — only the caller-side upstream construction is added"
  artifacts:
    - path: "packages/frontend/components/defi/cornerstone/CornerstoneClientShell.tsx"
      provides: "buildbear early-return branch in handleLiveConfirm + mount-probe guard extension"
      contains: "resolvedMode === 'buildbear'"
    - path: "packages/frontend/lib/apps/abrigo/cornerstone/workflow-engine.ts"
      provides: "exported buildUpstreamFromReplayArtifact(presetId): UpstreamResult"
      exports: ["buildUpstreamFromReplayArtifact"]
    - path: "packages/frontend/tests/architecture/buildbear-decoupling.test.ts"
      provides: "line-order CI check: buildbear branch precedes agent1 fetch; workflow-engine has no agent1 ref"
      min_lines: 25
  key_links:
    - from: "CornerstoneClientShell.tsx handleLiveConfirm"
      to: "/api/cornerstone/buildbear-sign"
      via: "buildbear branch fetch (before agent1)"
      pattern: "/api/cornerstone/buildbear-sign"
    - from: "workflow-engine.ts buildUpstreamFromReplayArtifact"
      to: "getPresetById"
      via: "recorded replay preset → StrategistDecidedView"
      pattern: "getPresetById"
---

<objective>
Make the Somnia decoupling cut (MINT-02) and the MINT-03 mandate-source swap. In `handleLiveConfirm`, add a `resolvedMode === 'buildbear'` branch as the FIRST statement that calls `/api/cornerstone/buildbear-sign` and unconditionally returns BEFORE the existing `/api/abrigo/agent1` fetch — so the BuildBear path can never re-introduce the v2.0 Somnia-outage coupling. In `workflow-engine.ts`, add `buildUpstreamFromReplayArtifact(presetId)` that builds the `UpstreamResult` from the recorded replay preset instead of a live Agent-1 response.

Phase 11 establishes the structural cut only; Phase 12 builds the full one-click RunState wiring on top. The three existing live-path `setResolvedMode('replay')` flips STAY (Phase 12 reworks them).

Purpose: Decouple FIRST (per research order) so the routes are consumed from a Somnia-independent path; pin the mandate to the committed replay snapshot so the demo works while Somnia is outaged.
Output: edited `CornerstoneClientShell.tsx` + `workflow-engine.ts`; a new line-order architecture test; the Wave-0 `workflow-engine-buildbear` RED test turns GREEN.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-server-routes/11-CONTEXT.md
@.planning/phases/11-frontend-server-routes/11-RESEARCH.md
@.planning/phases/11-frontend-server-routes/11-VALIDATION.md

<interfaces>
<!-- Pinned edit sites + contracts — do not re-explore. -->

CornerstoneClientShell.tsx (current, VERIFIED line numbers):
- L151 useEffect mount probe guard: `if (resolvedMode !== 'live') return`
- L189 `async function handleLiveConfirm() {`
- L191 `const response = await fetch('/api/abrigo/agent1', { ... })`
- L208, L221, L237 the three `setResolvedMode('replay')` flips (KEEP all three)

workflow-engine.ts:
- L145 `type UpstreamResult = { ok:true; strategistView: StrategistDecidedView } | { ok:false; reason:string; strategistView:null }`
- L201 `export async function runWorkflowLive(...)` — DO NOT change its signature
- L287 `export async function runWorkflow(presetId, emit, {confirm})` — the MOCK path; its strategistRaw construction (L300-325) is the model for buildUpstreamFromReplayArtifact
- imports already present: `getPresetById` from './presets', `fromMockEvent` from './events', `StrategistDecidedEvent`/`StrategistDecidedView` types

Model construction (lift the shape from runWorkflow L300-325):
```typescript
const strategistRaw: StrategistDecidedEvent = {
  kind: 'StrategistDecided',
  requestId: BigInt(preset.recordedDecisionId),
  thesis: '...verbatim recorded thesis...',
  spec: { underlyingMarket:'0xwcopusdc', strikeWAD:4100n, size:100n,
          economicTheory:'address(0)', chainId:137, isLong:true,
          payoffTerms:{ vol:14400n, horizonBlocks:100, tickSpacing:60, asset:0, riskPartner:0 } },
}
const strategistView = fromMockEvent(strategistRaw)  // → StrategistDecidedView
// enrich with recordedDecisionId then return { ok:true, strategistView: { ...view, recordedDecisionId: preset.recordedDecisionId } }
```

Preset ids: 'infl-surprise-add' (recordedDecisionId '4083729'), 'infl-cooling-reduce' ('4083997').
</interfaces>

# Read these as the edit source-of-truth
@packages/frontend/components/defi/cornerstone/CornerstoneClientShell.tsx
@packages/frontend/lib/apps/abrigo/cornerstone/workflow-engine.ts
@packages/frontend/lib/apps/abrigo/cornerstone/presets.ts
@.planning/phases/11-frontend-server-routes/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MINT-03 — buildUpstreamFromReplayArtifact in workflow-engine.ts</name>
  <files>packages/frontend/lib/apps/abrigo/cornerstone/workflow-engine.ts, packages/frontend/tsconfig.json</files>
  <read_first>
    packages/frontend/lib/apps/abrigo/cornerstone/workflow-engine.ts (L145 UpstreamResult, L287-325 runWorkflow mock construction to mirror)
    packages/frontend/lib/apps/abrigo/cornerstone/presets.ts (getPresetById, the two preset ids)
    packages/frontend/tests/unit/workflow-engine-buildbear.test.ts (the RED contract this turns GREEN)
    .planning/phases/11-frontend-server-routes/11-RESEARCH.md (§MINT-03 Mandate Source Swap)
  </read_first>
  <behavior>
    - buildUpstreamFromReplayArtifact('infl-surprise-add') → { ok:true, strategistView } with strategistView.kind === 'StrategistDecided' and strategistView.recordedDecisionId === '4083729'
    - buildUpstreamFromReplayArtifact('infl-cooling-reduce') → recordedDecisionId === '4083997'
    - buildUpstreamFromReplayArtifact('unknown-id') → { ok:false, reason:<string mentioning the id>, strategistView:null }
    - The function performs ZERO network calls (no fetch, no agent1) — purely synchronous over getPresetById + fromMockEvent
  </behavior>
  <action>
    In `lib/apps/abrigo/cornerstone/workflow-engine.ts`, add an exported function:
    `export function buildUpstreamFromReplayArtifact(presetId: string): UpstreamResult`
    - `const preset = getPresetById(presetId); if (!preset) return { ok:false, reason: \`unknown preset: ${presetId}\`, strategistView: null }`.
    - Build `strategistRaw: StrategistDecidedEvent` mirroring the `runWorkflow` mock construction (L300-325): use `BigInt(preset.recordedDecisionId)` for requestId and the same recorded thesis + spec shape. Run it through `fromMockEvent(strategistRaw)` to get a `StrategistDecidedView`, then spread `recordedDecisionId: preset.recordedDecisionId` onto it.
    - Return `{ ok: true, strategistView }`.
    - This is synchronous and Somnia-free by construction — `buildLiveMandate` re-hydration (which pins economicTheory=PKE) stays untouched in `runWorkflowLive`; only the upstream SOURCE changes. Do NOT modify `runWorkflowLive`'s signature or body.
    Then REMOVE `tests/unit/workflow-engine-buildbear.test.ts` from the tsconfig `exclude` list.
  </action>
  <verify>
    <automated>pnpm --filter d2p-frontend exec vitest run tests/unit/workflow-engine-buildbear.test.ts && pnpm --filter d2p-frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter d2p-frontend exec vitest run tests/unit/workflow-engine-buildbear.test.ts` GREEN (both preset ids + unknown-id).
    - `grep -n "export function buildUpstreamFromReplayArtifact" packages/frontend/lib/apps/abrigo/cornerstone/workflow-engine.ts` present.
    - `grep -n "getPresetById" packages/frontend/lib/apps/abrigo/cornerstone/workflow-engine.ts` — helper uses it (no fetch).
    - `! grep -nE "agent1|abrigo/agent1|50312|fetch\(" packages/frontend/lib/apps/abrigo/cornerstone/workflow-engine.ts` — workflow-engine has zero Somnia/network references (MINT-02 supporting signal from 11-VALIDATION.md).
    - `runWorkflowLive` signature unchanged: `grep -n "export async function runWorkflowLive" packages/frontend/lib/apps/abrigo/cornerstone/workflow-engine.ts` still matches the original.
    - tsc exit 0; the workflow-engine-buildbear test un-excluded.
  </acceptance_criteria>
  <done>The buildbear upstream is sourced from the recorded replay preset (not Somnia); runWorkflowLive untouched; RED upstream test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: MINT-02 — decoupling cut in handleLiveConfirm + line-order arch test</name>
  <files>packages/frontend/components/defi/cornerstone/CornerstoneClientShell.tsx, packages/frontend/tsconfig.json, packages/frontend/tests/architecture/buildbear-decoupling.test.ts</files>
  <read_first>
    packages/frontend/components/defi/cornerstone/CornerstoneClientShell.tsx (L151 mount probe, L189-239 handleLiveConfirm, the 3 setResolvedMode('replay') sites)
    .planning/phases/11-frontend-server-routes/11-RESEARCH.md (§Somnia Decoupling Cut — the exact edit + the 5 critical invariants + the Pitfall 4/5 warnings)
    .planning/phases/11-frontend-server-routes/11-VALIDATION.md (§Decoupling Grep — the python line-order check to encode as a test)
    packages/frontend/app/api/cornerstone/buildbear-sign/route.ts (the BuildBearSignResponse type to import — note: depends_on lists 11-01 only; if 11-02 has not landed, import the type via a local structural type to avoid a hard build dep, OR coordinate so this task runs after 11-02 — see action)
  </read_first>
  <action>
    Edit `components/defi/cornerstone/CornerstoneClientShell.tsx`, function `handleLiveConfirm` (L189). Apply the §Somnia Decoupling Cut from research verbatim, honoring all 5 critical invariants:
    1. Insert, as the VERY FIRST statement of `handleLiveConfirm` (before the `try` that contains the agent1 fetch), a branch:
       ```
       if (resolvedMode === 'buildbear') {
         try {
           const response = await fetch('/api/cornerstone/buildbear-sign', {
             method: 'POST',
             headers: { 'Content-Type': 'application/json' },
             body: JSON.stringify({ mandate: null }), // Phase 12 fills the replay-artifact mandate
           })
           const result = await response.json()
           // Phase 12 maps result.ok / result.reason → RunState transitions.
           // Phase 11: NO setResolvedMode('replay') on ANY buildbear path (HONEST-01 — Phase 12 wires the fork-used advisory).
           void result
         } catch {
           // Phase 12 wires the error state; Phase 11 does NOT degrade to replay here.
         }
         return // ALWAYS return — never fall through to the agent1 block
       }
       ```
    2. Leave the original agent1 `try { ... }` block and ALL THREE `setResolvedMode('replay')` flips (L208/L221/L237) intact, unchanged — they remain the `live`-path behavior. The buildbear branch must contain ZERO `setResolvedMode('replay')` calls (Pitfall 5).
    3. Extend the mount-probe guard at L151 from `if (resolvedMode !== 'live') return` to `if (resolvedMode !== 'live' && resolvedMode !== 'buildbear') return` so the probe also runs for buildbear (Phase 12 refines buildbear-specific probe logic).
    4. For the `result` type: import `BuildBearSignResponse` from `@/app/api/cornerstone/buildbear-sign/route` IF that route exists (11-02 landed); otherwise type `result` as `unknown` with a `// Phase 12 narrows once buildbear-sign route lands` note — the structural cut does not depend on the type. (Prefer importing the type; this plan's depends_on is 11-01, but in practice schedule this task after 11-02 in the same wave to enable the import. If a same-wave race is a concern, the executor may use `unknown` — the line-order invariant is what matters for MINT-02.)
    Then create `tests/architecture/buildbear-decoupling.test.ts` encoding the 11-VALIDATION.md line-order check as a vitest test (node env): read `CornerstoneClientShell.tsx` as text, assert `code.indexOf("resolvedMode === 'buildbear'") !== -1`, `code.indexOf("'/api/abrigo/agent1'") !== -1`, and `indexOf(buildbear) < indexOf(agent1)`. Also assert the buildbear branch contains the `/api/cornerstone/buildbear-sign` string and that the substring between the buildbear branch start and its `return` contains NO `setResolvedMode('replay')`.
  </action>
  <verify>
    <automated>pnpm --filter d2p-frontend exec vitest run tests/architecture/buildbear-decoupling.test.ts && pnpm --filter d2p-frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter d2p-frontend exec vitest run tests/architecture/buildbear-decoupling.test.ts` GREEN.
    - Line-order holds: `python3 -c "c=open('packages/frontend/components/defi/cornerstone/CornerstoneClientShell.tsx').read(); assert c.find(\"resolvedMode === 'buildbear'\") < c.find(\"'/api/abrigo/agent1'\")"` exits 0.
    - `grep -c "setResolvedMode('replay')" packages/frontend/components/defi/cornerstone/CornerstoneClientShell.tsx` still returns 3 (the three live-path flips preserved; none added in the buildbear branch).
    - `grep -n "/api/cornerstone/buildbear-sign" packages/frontend/components/defi/cornerstone/CornerstoneClientShell.tsx` present inside the buildbear branch.
    - `grep -n "resolvedMode !== 'live' && resolvedMode !== 'buildbear'" packages/frontend/components/defi/cornerstone/CornerstoneClientShell.tsx` — mount probe guard extended.
    - `pnpm --filter d2p-frontend exec tsc --noEmit` exit 0; `pnpm --filter d2p-frontend exec biome check .` clean.
  </acceptance_criteria>
  <done>The buildbear path early-returns before any agent1 reference with zero silent replay flips; the three live-path flips are intact; a line-order arch test guards the cut in CI.</done>
</task>

</tasks>

<verification>
- `pnpm --filter d2p-frontend exec vitest run tests/unit/workflow-engine-buildbear.test.ts tests/architecture/buildbear-decoupling.test.ts` — GREEN.
- `pnpm --filter d2p-frontend exec vitest run` — full suite GREEN (existing cornerstone-engine / shell tests unaffected; the 3 live-path flips preserved).
- `pnpm --filter d2p-frontend exec tsc --noEmit` — exit 0; the workflow-engine-buildbear test un-excluded.
- `pnpm --filter d2p-frontend exec biome check .` — clean.
- MINT-02 line-order grep + workflow-engine no-agent1 grep both pass (11-VALIDATION.md).
</verification>

<success_criteria>
- MINT-02: the judge live path (buildbear) never reaches `/api/abrigo/agent1` — structurally guaranteed by the early-return and enforced by the line-order arch test.
- MINT-03: the buildbear mandate is sourced from the recorded replay preset, not a live Somnia Agent-1 response; runWorkflowLive unchanged.
- The three existing live-path `setResolvedMode('replay')` flips are preserved for Phase 12 to rework.
- No HONEST-01 violation introduced: zero silent replay degradation on the buildbear path.
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-server-routes/11-03-SUMMARY.md`. The decoupling cut changes CornerstoneClientShell behavior only in the (not-yet-reachable) buildbear mode — Phase 12 wires the URL opt-in and one-click. Per packages/frontend/CLAUDE.md, a full live-DOM Evidence Collector pass is deferred to Phase 12 (buildbear mode is not user-reachable in Phase 11); note this explicitly and confirm the existing replay/live/mock routes still render unchanged (regression check) if a quick verify is cheap.
</output>

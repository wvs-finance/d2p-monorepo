---
phase: 11-frontend-server-routes
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - packages/frontend/app/api/cornerstone/buildbear-sign/route.ts
  - packages/frontend/app/api/cornerstone/buildbear-reset/route.ts
  - packages/frontend/tsconfig.json
autonomous: true
requirements: [MINT-01]
user_setup:
  - service: buildbear
    why: "Live signing against the fork is Phase 12; Phase 11 routes are unit-tested against mocks. DEMO_SIGNER_PK is only needed at deploy/runtime, never in CI."
    env_vars:
      - name: DEMO_SIGNER_PK
        source: "Operator-provisioned dedicated BuildBear signer key (Phase 10 funded inside the snapshot); set in Vercel env / local .env — NEVER committed, NEVER NEXT_PUBLIC_"
must_haves:
  truths:
    - "POST /api/cornerstone/buildbear-sign returns { ok:false, reason:'not-configured' } on a zero-secret clone (DEMO_SIGNER_PK absent) — cornerstone stays replay"
    - "A 'fork used' revert (EXEC-01) is classified as reason:'fork-used'; other reverts as 'reverted'"
    - "Signer with zero balance fast-fails as reason:'signer-gas' BEFORE simulate"
    - "RPC/network failure is reason:'rpc-unreachable'; a successful mint returns { ok:true, txHash, blockNumber, ...null view stubs }"
    - "POST /api/cornerstone/buildbear-reset is OPEN (no auth): evm_revert(snapshotId) then evm_snapshot, returns { ok:true, newSnapshotId }; { ok:false, reason:'no-snapshot' } when artifact has no snapshotId"
    - "The open-reset shared-sandbox griefable limitation is documented in the route file header (not secured)"
  artifacts:
    - path: "packages/frontend/app/api/cornerstone/buildbear-sign/route.ts"
      provides: "nodejs-runtime server-signing route with discriminated reason codes + signer pre-flight"
      exports: ["POST", "runtime"]
      min_lines: 90
    - path: "packages/frontend/app/api/cornerstone/buildbear-reset/route.ts"
      provides: "open nodejs-runtime evm_revert+evm_snapshot route"
      exports: ["POST", "runtime"]
      min_lines: 50
  key_links:
    - from: "app/api/cornerstone/buildbear-sign/route.ts"
      to: "env.DEMO_SIGNER_PK"
      via: "privateKeyToAccount(env.DEMO_SIGNER_PK)"
      pattern: "privateKeyToAccount\\(env.DEMO_SIGNER_PK"
    - from: "app/api/cornerstone/buildbear-sign/route.ts"
      to: "deployment.rpcUrl / deployment.executor"
      via: "createBuildBearChain + simulateContract before writeContract"
      pattern: "simulateContract"
    - from: "app/api/cornerstone/buildbear-reset/route.ts"
      to: "deployment.rpcUrl"
      via: "evm_revert then evm_snapshot JSON-RPC"
      pattern: "evm_snapshot"
---

<objective>
Build the two Node-runtime API routes (GREEN against the Wave-0 RED tests): `buildbear-sign` (the core MINT-01 deliverable — server-signs `resolveFromMandate` with `DEMO_SIGNER_PK`, discriminated reason codes, signer-balance pre-flight) and `buildbear-reset` (OPEN, no-auth `evm_revert`+`evm_snapshot`).

Purpose: A judge clone with no secret gets `not-configured` and stays in replay; with the key present and the fork fresh, the route signs a real mint; every failure mode maps 1:1 to a Phase-12 RunState reason. The reset route makes the one-use snapshot reusable across sessions.
Output: `app/api/cornerstone/buildbear-sign/route.ts`, `app/api/cornerstone/buildbear-reset/route.ts`; the Wave-0 sign/reset RED tests turn GREEN.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-server-routes/11-CONTEXT.md
@.planning/phases/11-frontend-server-routes/11-RESEARCH.md
@.planning/phases/11-frontend-server-routes/11-VALIDATION.md

<interfaces>
<!-- Pinned contracts — do not re-explore. -->

Discriminated response (must match the Wave-0 tests exactly):
```typescript
export type BuildBearSignResponse =
  | { ok: true; txHash: `0x${string}`; blockNumber: number;
      strategistView: null; executorView: null; positionMinted: null; margins: null }
  | { ok: false; reason: 'fork-used' | 'rpc-unreachable' | 'signer-gas' | 'reverted' | 'not-configured'; detail?: string }
```

Pinned 5-field HedgeMandate ABI tuple + resolveFromMandate signature (from spike-viem-sign.ts, VERIFIED vs HedgeMandate.sol): see 11-RESEARCH.md §Route Skeletons Pattern 1 (economicTheory address, underlyingMarket bytes32, targetNotional uint256, chainId uint32, isLong bool; then legIndex uint256, quoteMargin uint128; outputs positionId uint256). Copy that `resolveFromMandateAbi` const verbatim.

Reason classification (11-RESEARCH.md §Pattern 2 — copy classifyViemError + findInCauseChain verbatim):
- cause-chain ContractFunctionRevertedError.reason includes 'fork used' → 'fork-used'; else → 'reverted'
- InsufficientFundsError (constructor.name or in cause chain) → 'signer-gas'
- HttpRequestError (constructor.name or in cause chain) → 'rpc-unreachable'
- fallback → 'reverted'

JSON-RPC shapes (LIVE-VERIFIED Phase 10):
- evm_snapshot: params [] → hex string '0x1'
- evm_revert: params ['0x1'] → true on success

artifact-loader exports: `deployment.rpcUrl`, `deployment.executor`, `deployment.pool`, `deployment.snapshotId?` (hex string, optional), `deployment.mintTxHash: string | null`.
buildbear.ts exports: `createBuildBearChain(rpcUrl)`, `BuildBearChainId = 31337`.
env: `env.DEMO_SIGNER_PK` (server-only optional, added in 11-01).
</interfaces>

# Read these as the implementation source-of-truth
@packages/frontend/app/api/abrigo/agent1/route.ts
@packages/frontend/scripts/spike-viem-sign.ts
@packages/frontend/app/api/cornerstone/rpc/route.ts
@.planning/phases/11-frontend-server-routes/11-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildbear-sign route — server signing + discriminated reason codes (MINT-01)</name>
  <files>packages/frontend/app/api/cornerstone/buildbear-sign/route.ts, packages/frontend/tsconfig.json</files>
  <read_first>
    packages/frontend/app/api/abrigo/agent1/route.ts (runtime='nodejs', env-read guard, privateKeyToAccount, writeContract pattern)
    packages/frontend/scripts/spike-viem-sign.ts (pinned ABI tuple + createWalletClient/createPublicClient wiring)
    .planning/phases/11-frontend-server-routes/11-RESEARCH.md (§Route Skeletons Pattern 1 + Pattern 2 — the route + classifyViemError to lift)
    packages/frontend/tests/api/buildbear-sign.test.ts (the RED contract this turns GREEN)
  </read_first>
  <behavior>
    - DEMO_SIGNER_PK absent → { ok:false, reason:'not-configured' } (status 503), NO viem calls
    - getBalance(signer) === 0n → { ok:false, reason:'signer-gas' } (pre-flight, BEFORE simulateContract)
    - simulate throws ContractFunctionRevertedError reason 'fork used' → { ok:false, reason:'fork-used' }
    - simulate throws ContractFunctionRevertedError other reason → { ok:false, reason:'reverted' }
    - transport throws HttpRequestError → { ok:false, reason:'rpc-unreachable' }
    - InsufficientFundsError → { ok:false, reason:'signer-gas' }
    - simulate ok → writeContract → receipt.status 'success' → { ok:true, txHash, blockNumber, strategistView:null, executorView:null, positionMinted:null, margins:null }
    - receipt.status 'reverted' → { ok:false, reason:'reverted' }
  </behavior>
  <action>
    Create `app/api/cornerstone/buildbear-sign/route.ts`. First non-comment line: `export const runtime = 'nodejs'`. Lift the route skeleton from 11-RESEARCH.md §Route Skeletons Pattern 1 and the `classifyViemError`/`findInCauseChain` helpers from §Pattern 2 — these are the verified-against-viem-2.48.11 implementations. Concrete requirements:
    - Import `deployment` from `@/lib/apps/abrigo/cornerstone/artifact-loader`, `createBuildBearChain` from `@/lib/apps/abrigo/cornerstone/buildbear`, `env` from `@/lib/env`, viem primitives (`http`, `createPublicClient`, `createWalletClient`) and `privateKeyToAccount` from `viem/accounts`.
    - Use `env.DEMO_SIGNER_PK` (NOT `process.env.DEMO_SIGNER_PK`). Guard `if (!env.DEMO_SIGNER_PK)` → not-configured FIRST, before any client construction.
    - Paste the `resolveFromMandateAbi` const verbatim from the research (5-field tuple + legIndex + quoteMargin).
    - Parse the POST body `{ mandate }`; on JSON parse failure return `{ ok:false, reason:'reverted', detail:'invalid JSON body' }` (400). NOTE: Phase 11 accepts `mandate` in the body but DOES NOT build it from agent1 — Phase 12 sends the replay-artifact mandate; Phase 11 passes `body.mandate as never` into simulate/write args `[mandate, 0n, 1_000_000n]`.
    - Build account/chain/walletClient/publicClient exactly as spike-viem-sign.ts.
    - Signer-balance pre-flight: `const balance = await publicClient.getBalance({ address: account.address }); if (balance === 0n) return signer-gas` — BEFORE simulateContract (OPS-05).
    - `await publicClient.simulateContract({ account, address: deployment.executor as 0x, abi, functionName:'resolveFromMandate', args:[body.mandate as never, 0n, 1_000_000n] })` inside try.
    - `const txHash = await walletClient.writeContract({ ... same args ... })`.
    - `const receipt = await publicClient.waitForTransactionReceipt({ hash: txHash })`; `if (receipt.status === 'reverted')` → reverted.
    - Success → return the `{ ok:true, ... }` envelope with the four view fields as `null` (Phase 12/13 decode them — pinned by the Open Questions in research).
    - `catch (err) { return Response.json(classifyViemError(err)) }`.
    - Export `BuildBearSignResponse` type so the Wave-0 test + Phase 12 import the same union.
    Then REMOVE `tests/api/buildbear-sign.test.ts` from the tsconfig `exclude` list (the module now resolves).
  </action>
  <verify>
    <automated>pnpm --filter d2p-frontend exec vitest run tests/api/buildbear-sign.test.ts && pnpm --filter d2p-frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter d2p-frontend exec vitest run tests/api/buildbear-sign.test.ts` — all branches GREEN (not-configured, fork-used, reverted, signer-gas, rpc-unreachable, ok).
    - `grep -n "export const runtime = 'nodejs'" packages/frontend/app/api/cornerstone/buildbear-sign/route.ts` — present and first export.
    - `grep -n "env.DEMO_SIGNER_PK" packages/frontend/app/api/cornerstone/buildbear-sign/route.ts` — uses `env.` not `process.env.`.
    - `grep -n "simulateContract" packages/frontend/app/api/cornerstone/buildbear-sign/route.ts` — simulate present and precedes `writeContract` lexically.
    - `pnpm --filter d2p-frontend exec vitest run tests/architecture/buildbear-key-leak.test.ts` still GREEN (new route is under app/api/ → path-scope satisfied).
    - `pnpm --filter d2p-frontend exec tsc --noEmit` exit 0; `tests/api/buildbear-sign.test.ts` no longer in tsconfig exclude.
  </acceptance_criteria>
  <done>buildbear-sign signs the mint, fast-fails zero-gas signers, and maps every viem failure to the pinned reason code; the RED sign suite is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildbear-reset route — OPEN evm_revert + evm_snapshot (documented limitation)</name>
  <files>packages/frontend/app/api/cornerstone/buildbear-reset/route.ts, packages/frontend/tsconfig.json</files>
  <read_first>
    packages/frontend/app/api/cornerstone/rpc/route.ts (the keyless cornerstone route + deployment.rpcUrl usage)
    .planning/phases/11-frontend-server-routes/11-RESEARCH.md (§Route Skeletons Pattern 3 — the reset route + jsonRpc helper)
    packages/frontend/tests/api/buildbear-reset.test.ts (the RED contract this turns GREEN)
  </read_first>
  <behavior>
    - deployment.snapshotId absent → { ok:false, reason:'no-snapshot' } (status 409), no fetch
    - evm_revert(snapshotId) called THEN evm_snapshot called (strict order)
    - evm_revert→true, evm_snapshot→'0x2' → { ok:true, newSnapshotId:'0x2' }
    - evm_revert returns non-true → { ok:false, reason:'revert-failed' }
    - fetch throws (HTTP/network) → { ok:false, reason:'rpc-unreachable' }
  </behavior>
  <action>
    Create `app/api/cornerstone/buildbear-reset/route.ts`. First non-comment line: `export const runtime = 'nodejs'`. Lift the skeleton + `jsonRpc` helper verbatim from 11-RESEARCH.md §Route Skeletons Pattern 3.
    - File header comment MUST state the OPEN-route documented limitation explicitly: "OPEN ROUTE — no auth. Documented limitation: on the SHARED hosted sandbox this is griefable (one judge resetting mid-another's run); this is the user's ACCEPTED trade-off (CONTEXT.md). Isolation comes from the OPS-07 per-judge local `pnpm demo` sandbox; the hosted sandbox is cheap to re-provision (OPS-03/04). Do NOT add an auth/shared-secret gate — that was considered and REJECTED for v3.0." Also document the one-use snapshot semantics (evm_revert CONSUMES the id → must re-evm_snapshot; the new id is returned, no KV persistence — RESET-01 Future).
    - Read `deployment.snapshotId`; if falsy → `{ ok:false, reason:'no-snapshot', detail:'artifact has no snapshotId — re-provision with --no-mint' }` (409).
    - `jsonRpc(deployment.rpcUrl, 'evm_revert', [snapshotId])`; if result !== true → `{ ok:false, reason:'revert-failed' }`.
    - `jsonRpc(deployment.rpcUrl, 'evm_snapshot', [])` → `newSnapshotId`; return `{ ok:true, newSnapshotId }`.
    - catch: classify HTTP/fetch/ECONNREFUSED message → `rpc-unreachable`, else `revert-failed`.
    Then REMOVE `tests/api/buildbear-reset.test.ts` from the tsconfig `exclude` list.
  </action>
  <verify>
    <automated>pnpm --filter d2p-frontend exec vitest run tests/api/buildbear-reset.test.ts && pnpm --filter d2p-frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter d2p-frontend exec vitest run tests/api/buildbear-reset.test.ts` GREEN (no-snapshot, reset sequence order, newSnapshotId).
    - `grep -ni "griefable\|OPEN ROUTE\|do not add an auth\|REJECTED" packages/frontend/app/api/cornerstone/buildbear-reset/route.ts` — the documented limitation header is present.
    - `! grep -ni "secret\|authorization\|x-api-key\|bearer" packages/frontend/app/api/cornerstone/buildbear-reset/route.ts` — route stays OPEN (no auth gate added).
    - `grep -n "evm_revert" packages/frontend/app/api/cornerstone/buildbear-reset/route.ts` precedes `grep -n "evm_snapshot"` (revert before re-snapshot).
    - `pnpm --filter d2p-frontend exec tsc --noEmit` exit 0; reset test no longer excluded.
  </acceptance_criteria>
  <done>buildbear-reset is an OPEN route that re-snapshots after every revert and returns the new id; the documented shared-sandbox limitation is in the header; RED reset suite GREEN.</done>
</task>

</tasks>

<verification>
- `pnpm --filter d2p-frontend exec vitest run tests/api/buildbear-sign.test.ts tests/api/buildbear-reset.test.ts tests/architecture/buildbear-key-leak.test.ts` — all GREEN.
- `pnpm --filter d2p-frontend exec vitest run` — full suite GREEN (no regression).
- `pnpm --filter d2p-frontend exec tsc --noEmit` — exit 0; both new test files un-excluded from tsconfig.
- `pnpm --filter d2p-frontend exec biome check .` — clean.
- Key-leak greps (11-VALIDATION.md): `! grep -rn 'NEXT_PUBLIC_DEMO_SIGNER\|DEMO_SIGNER_PK' packages/frontend/{components,lib} --include='*.ts*' | grep -v 'lib/env.ts'` and `privateKeyToAccount` only under `app/api/`.
</verification>

<success_criteria>
- MINT-01 fully satisfied: server-only signing route with the full discriminated reason set, signer pre-flight, key never NEXT_PUBLIC / never in client bundle.
- A zero-secret clone returns `not-configured` and stays replay (no accidental live mode).
- The reset route is OPEN with the limitation documented, re-snapshots correctly, returns the new id.
- No live BuildBear fork touched — routes validated entirely against mocks (CI-clean).
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-server-routes/11-02-SUMMARY.md`. These routes have no rendered surface; per packages/frontend/CLAUDE.md, verify with `curl`/network-request instead of browser_snapshot — Phase 12 wires the UI. Record that the live-DOM Evidence Collector is N/A for this plan (API-only) and Phase 12 owns the integration verify.
</output>

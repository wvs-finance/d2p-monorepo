---
phase: 11-frontend-server-routes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/frontend/lib/apps/abrigo/cornerstone/mode.ts
  - packages/frontend/lib/env.ts
  - packages/frontend/tests/unit/mode.test.ts
  - packages/frontend/tests/api/buildbear-sign.test.ts
  - packages/frontend/tests/api/buildbear-reset.test.ts
  - packages/frontend/tests/unit/workflow-engine-buildbear.test.ts
  - packages/frontend/tests/architecture/buildbear-key-leak.test.ts
autonomous: true
requirements: [MINT-01, MINT-02, MINT-03]
must_haves:
  truths:
    - "parseMode('buildbear') returns 'buildbear'; parseMode(null) still returns 'replay' (default unchanged)"
    - "DEMO_SIGNER_PK is readable server-side via env, never in the NEXT_PUBLIC_ namespace"
    - "Reason-code route tests exist and FAIL first (RED) because the routes/helper do not yet exist"
    - "An architecture test asserts DEMO_SIGNER_PK + privateKeyToAccount stay scoped under app/api/ (no key leak)"
  artifacts:
    - path: "packages/frontend/lib/apps/abrigo/cornerstone/mode.ts"
      provides: "'buildbear' added to CornerstoneMode union + parseMode"
      contains: "buildbear"
    - path: "packages/frontend/lib/env.ts"
      provides: "DEMO_SIGNER_PK server schema + runtimeEnv entry"
      contains: "DEMO_SIGNER_PK"
    - path: "packages/frontend/tests/api/buildbear-sign.test.ts"
      provides: "RED tests for all 5 reason branches + not-configured + ok"
      min_lines: 80
    - path: "packages/frontend/tests/api/buildbear-reset.test.ts"
      provides: "RED tests for no-snapshot, reset sequence, newSnapshotId"
      min_lines: 40
    - path: "packages/frontend/tests/unit/workflow-engine-buildbear.test.ts"
      provides: "RED test for buildUpstreamFromReplayArtifact"
      min_lines: 20
    - path: "packages/frontend/tests/architecture/buildbear-key-leak.test.ts"
      provides: "path-scoped key-leak grep as a vitest arch test"
      min_lines: 20
  key_links:
    - from: "packages/frontend/lib/env.ts"
      to: "process.env.DEMO_SIGNER_PK"
      via: "runtimeEnv mapping"
      pattern: "DEMO_SIGNER_PK: process.env.DEMO_SIGNER_PK"
---

<objective>
Wave 0 for Phase 11: lay the test scaffolding (RED) and the two trivial no-dependency source changes — the `'buildbear'` mode and the `DEMO_SIGNER_PK` env entry — so the Wave-1 route + decoupling plans turn GREEN.

This plan writes failing-first tests for ALL Phase 11 behaviors (the 5 `buildbear-sign` reason branches + `not-configured` + `ok`; the 3 `buildbear-reset` cases; `buildUpstreamFromReplayArtifact`) plus the architecture key-leak grep, and ships the `mode.ts` + `lib/env.ts` changes that those tests partly cover now.

Purpose: Establish the cross-layer reason-code contract and the zero-secret default (parseMode default stays 'replay') as executable RED tests before any route code exists.
Output: 4 new test files, 1 arch test, `mode.ts` extended, `lib/env.ts` extended. mode.test.ts is GREEN at end of this plan; the route/helper tests stay RED (imports of not-yet-created modules) and are excluded from tsconfig until Wave 1 (pattern: `structured-data.test.tsx`).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-server-routes/11-CONTEXT.md
@.planning/phases/11-frontend-server-routes/11-RESEARCH.md
@.planning/phases/11-frontend-server-routes/11-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — do not re-explore. -->

Current mode.ts (lib/apps/abrigo/cornerstone/mode.ts):
```typescript
export type CornerstoneMode = 'live' | 'replay' | 'mock'
export const DEFAULT_MODE: CornerstoneMode = 'replay'
export function parseMode(raw: string | null | undefined): CornerstoneMode {
  if (raw === 'live') return 'live'
  if (raw === 'mock') return 'mock'
  return DEFAULT_MODE
}
```

Current lib/env.ts server block (relevant lines): `server: { ... SOMNIA_OPERATOR_PK: z.string().min(1).optional(), AGENT1_ROUTE_SECRET: z.string().min(1).optional() }`; `runtimeEnv: { ... SOMNIA_OPERATOR_PK: process.env.SOMNIA_OPERATOR_PK, ... }`.

The DISCRIMINATED RESPONSE CONTRACT both Wave-1 plans must satisfy (pin the exact strings now in the tests):
```typescript
export type BuildBearSignResponse =
  | { ok: true; txHash: `0x${string}`; blockNumber: number;
      strategistView: null; executorView: null; positionMinted: null; margins: null }
  | { ok: false; reason: 'fork-used' | 'rpc-unreachable' | 'signer-gas' | 'reverted' | 'not-configured'; detail?: string }
```

Reason-code mapping (verified vs viem 2.48.11 in 11-RESEARCH.md §Pattern 2):
- ContractFunctionRevertedError.reason includes "fork used" → reason: 'fork-used'
- other ContractFunctionRevertedError → reason: 'reverted'
- InsufficientFundsError OR getBalance() === 0n pre-flight → reason: 'signer-gas'
- HttpRequestError → reason: 'rpc-unreachable'
- env.DEMO_SIGNER_PK absent → reason: 'not-configured'

buildbear-reset contract:
```typescript
// { ok:false, reason:'no-snapshot' } when deployment.snapshotId absent
// { ok:true, newSnapshotId } after evm_revert(snapshotId) then evm_snapshot
```

Preset/helper shape (lib/apps/abrigo/cornerstone/presets.ts):
```typescript
export type Preset = { readonly id: string; readonly recordedDecisionId: string; readonly labelEsCO: string; readonly labelEn: string }
export function getPresetById(id: string): Preset | null
// PRESETS ids: 'infl-surprise-add' (recordedDecisionId '4083729'), 'infl-cooling-reduce' ('4083997')
```

StrategistDecidedView (lib/apps/abrigo/cornerstone/events.ts):
```typescript
export type StrategistDecidedView = {
  kind: 'StrategistDecided'; recordedDecisionId: string; thesis: string;
  spec: { marketLabel: string; strikeWAD: string; size: bigint; isLong: boolean; schoolLabel: string }
}
```

UpstreamResult (workflow-engine.ts:145) — the type buildUpstreamFromReplayArtifact must return:
```typescript
type UpstreamResult =
  | { ok: true; strategistView: StrategistDecidedView }
  | { ok: false; reason: string; strategistView: null }
```
</interfaces>

# Patterns to mirror (read these)
@packages/frontend/tests/api/status.test.ts
@packages/frontend/lib/apps/abrigo/cornerstone/mode.ts
@packages/frontend/lib/env.ts
@packages/frontend/tests/architecture/no-wallet-in-lab.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend mode.ts ('buildbear') + lib/env.ts (DEMO_SIGNER_PK) with GREEN mode test</name>
  <files>packages/frontend/lib/apps/abrigo/cornerstone/mode.ts, packages/frontend/lib/env.ts, packages/frontend/tests/unit/mode.test.ts</files>
  <read_first>
    packages/frontend/lib/apps/abrigo/cornerstone/mode.ts
    packages/frontend/lib/env.ts
    packages/frontend/tests/unit/cornerstone-presets.test.ts (vitest style reference)
  </read_first>
  <behavior>
    - parseMode('buildbear') === 'buildbear'
    - parseMode('live') === 'live'; parseMode('mock') === 'mock'
    - parseMode(null) === 'replay'; parseMode(undefined) === 'replay'; parseMode('garbage') === 'replay'  (DEFAULT_MODE unchanged)
    - 'buildbear' is assignable to CornerstoneMode (type-level — covered by tsc)
  </behavior>
  <action>
    1. Edit `lib/apps/abrigo/cornerstone/mode.ts`:
       - Change union to `export type CornerstoneMode = 'live' | 'replay' | 'mock' | 'buildbear'`.
       - Add `if (raw === 'buildbear') return 'buildbear'` to `parseMode`, placed after the `'mock'` check and BEFORE `return DEFAULT_MODE`. Do NOT change `DEFAULT_MODE` (stays 'replay').
    2. Edit `lib/env.ts`:
       - In the `server` block, add: `DEMO_SIGNER_PK: z.string().min(1).optional(),` with a comment `// Phase 11: BuildBear demo signer. NEVER NEXT_PUBLIC_ — server only. Route returns 'not-configured' when absent.`
       - In `runtimeEnv`, add: `DEMO_SIGNER_PK: process.env.DEMO_SIGNER_PK,`
    3. Create `tests/unit/mode.test.ts` (vitest, default jsdom env is fine — pure function, no viem) asserting the behaviors above against the imported `parseMode` from `@/lib/apps/abrigo/cornerstone/mode`.
  </action>
  <verify>
    <automated>pnpm --filter d2p-frontend exec vitest run tests/unit/mode.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter d2p-frontend exec vitest run tests/unit/mode.test.ts` passes (GREEN).
    - `grep -n "'buildbear'" packages/frontend/lib/apps/abrigo/cornerstone/mode.ts` shows both the union member and the parseMode branch.
    - `grep -n "DEMO_SIGNER_PK" packages/frontend/lib/env.ts` shows it in BOTH the server schema and runtimeEnv.
    - `! grep -rn 'NEXT_PUBLIC_DEMO_SIGNER' packages/frontend` (no NEXT_PUBLIC leak).
  </acceptance_criteria>
  <done>mode.ts accepts 'buildbear' with default still 'replay'; DEMO_SIGNER_PK in server env; mode.test.ts GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED test scaffolds for buildbear-sign + buildbear-reset + buildUpstreamFromReplayArtifact</name>
  <files>packages/frontend/tests/api/buildbear-sign.test.ts, packages/frontend/tests/api/buildbear-reset.test.ts, packages/frontend/tests/unit/workflow-engine-buildbear.test.ts, packages/frontend/tsconfig.json</files>
  <read_first>
    packages/frontend/tests/api/status.test.ts (the // @vitest-environment node + vi.mock hoisting pattern)
    .planning/phases/11-frontend-server-routes/11-RESEARCH.md (§Route Skeletons, §Pattern 2, §Code Examples — the mock blocks)
    packages/frontend/lib/apps/abrigo/cornerstone/presets.ts
    packages/frontend/tsconfig.json (the `exclude` list — Wave-0-stub pattern)
  </read_first>
  <behavior>
    buildbear-sign (tests/api/buildbear-sign.test.ts), each via vi.mock'd artifact + viem transport:
    - "not-configured": DEMO_SIGNER_PK unset → { ok:false, reason:'not-configured' }
    - "fork-used": simulate throws an error whose cause-chain has constructor.name 'ContractFunctionRevertedError' with reason "fork used" → { ok:false, reason:'fork-used' }
    - "reverted": same but reason "SomethingElse" → { ok:false, reason:'reverted' }
    - "signer-gas": getBalance mock returns 0n → { ok:false, reason:'signer-gas' } (pre-flight, before simulate)
    - "rpc-unreachable": transport throws an error whose constructor.name is 'HttpRequestError' → { ok:false, reason:'rpc-unreachable' }
    - "ok": simulate passes, writeContract returns a hash, receipt.status==='success' → { ok:true, txHash, blockNumber, strategistView:null, executorView:null, positionMinted:null, margins:null }
    buildbear-reset (tests/api/buildbear-reset.test.ts):
    - "no-snapshot": artifact deployment.snapshotId undefined → { ok:false, reason:'no-snapshot' }
    - "reset sequence": asserts evm_revert(snapshotId) is called THEN evm_snapshot (order asserted via mock call order)
    - "returns newSnapshotId": evm_revert→true, evm_snapshot→'0x2' → { ok:true, newSnapshotId:'0x2' }
    workflow-engine-buildbear (tests/unit/workflow-engine-buildbear.test.ts):
    - buildUpstreamFromReplayArtifact('infl-surprise-add') → { ok:true, strategistView } with strategistView.recordedDecisionId === '4083729' and kind === 'StrategistDecided'
    - buildUpstreamFromReplayArtifact('does-not-exist') → { ok:false, strategistView:null } with a reason string
  </behavior>
  <action>
    Write the four test files as FAILING-FIRST (RED): they import `POST` from `@/app/api/cornerstone/buildbear-sign/route` and `@/app/api/cornerstone/buildbear-reset/route`, and `buildUpstreamFromReplayArtifact` from `@/lib/apps/abrigo/cornerstone/workflow-engine` — none of which exist yet, so the suites error on import (this is the intended RED).

    1. `tests/api/buildbear-sign.test.ts` — header `// @vitest-environment node`. Use the vi.mock blocks for `@/lib/apps/abrigo/cornerstone/artifact-loader` and `@/lib/apps/abrigo/cornerstone/buildbear` EXACTLY as in 11-RESEARCH.md §Code Examples (mock deployment with rpcUrl 'http://mock-rpc.test', executor '0xDeAd…', snapshotId '0x1', mintTxHash null). Mock `@/lib/env` so `env.DEMO_SIGNER_PK` can be toggled per-test (set to Hardhat default key `0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80` for the configured cases, `undefined` for not-configured). Mock viem (`createWalletClient`/`createPublicClient`) so `getBalance`, `simulateContract`, `writeContract`, `waitForTransactionReceipt` are vi.fn()s the test drives per-branch. For the error branches construct throwables with the exact `constructor.name` the classifier keys on (a class `class ContractFunctionRevertedError extends Error { reason = 'fork used' }`, `class HttpRequestError extends Error {}`, etc.) wrapped via `.cause` where the research says viem wraps them.
    2. `tests/api/buildbear-reset.test.ts` — header `// @vitest-environment node`. Mock the artifact-loader (toggle snapshotId presence). Mock `globalThis.fetch` via `vi.spyOn(globalThis,'fetch')` returning JSON-RPC envelopes (`{result:true}` for evm_revert, `{result:'0x2'}` for evm_snapshot). Assert call order by inspecting the JSON body `.method` of each fetch call in sequence.
    3. `tests/unit/workflow-engine-buildbear.test.ts` — default env is fine (pure helper). Import `buildUpstreamFromReplayArtifact`; assert the two behaviors above. Do NOT mock presets — use the real `getPresetById`.
    4. Add the three not-yet-resolvable test files to the `exclude` array in `packages/frontend/tsconfig.json` (same Wave-0 pattern as `tests/unit/structured-data.test.tsx`) so the pre-commit `tsc --noEmit` gate passes while the imported modules don't exist yet. Leave a `// Wave-0 RED stub — un-exclude in 11-02/11-03 when the module lands` note above each added line. `tests/unit/mode.test.ts` is NOT excluded (Task 1 made it green).
  </action>
  <verify>
    <automated>pnpm --filter d2p-frontend exec tsc --noEmit && echo "TSC_OK: stubs excluded, gate passes"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter d2p-frontend exec tsc --noEmit` exits 0 (the 3 RED stubs are tsconfig-excluded; mode.test.ts compiles).
    - The four test files exist and reference the pinned reason strings: `grep -l "fork-used" packages/frontend/tests/api/buildbear-sign.test.ts` and `grep -l "not-configured" packages/frontend/tests/api/buildbear-sign.test.ts` both hit.
    - Running `pnpm --filter d2p-frontend exec vitest run tests/api/buildbear-sign.test.ts` FAILS (RED) with a module-resolution / missing-export error for `@/app/api/cornerstone/buildbear-sign/route` — proving the tests are non-vacuous and the route is genuinely absent.
    - `grep -n "buildbear-sign.test\|buildbear-reset.test\|workflow-engine-buildbear.test" packages/frontend/tsconfig.json` shows all three excluded.
  </acceptance_criteria>
  <done>4 RED test files capture the full reason-code + reset + upstream contract; tsc gate green via tsconfig exclude; the sign suite is provably RED.</done>
</task>

<task type="auto">
  <name>Task 3: Architecture key-leak + privateKeyToAccount path-scope test (CI-checkable)</name>
  <files>packages/frontend/tests/architecture/buildbear-key-leak.test.ts</files>
  <read_first>
    packages/frontend/tests/architecture/no-wallet-in-lab.test.ts (the readFileSync/glob arch-test pattern)
    .planning/phases/11-frontend-server-routes/11-VALIDATION.md (the key-leak grep row)
    packages/frontend/app/api/abrigo/agent1/route.ts (legitimate privateKeyToAccount site)
  </read_first>
  <action>
    Create `tests/architecture/buildbear-key-leak.test.ts` (vitest, node env). It walks `packages/frontend/{app,components,lib}` source files (reuse the directory-walk helper style from `no-wallet-in-lab.test.ts`; resolve the repo paths relative to `process.cwd()`/`import.meta.dirname`) and asserts:
    1. No file anywhere under `packages/frontend` contains the substring `NEXT_PUBLIC_DEMO_SIGNER` (zero hits).
    2. Every file referencing `DEMO_SIGNER_PK` is either under `app/api/` OR is `lib/env.ts` (the schema declaration) OR is a test file under `tests/` — no `DEMO_SIGNER_PK` read in `components/` or non-env `lib/`.
    3. Every file referencing `privateKeyToAccount` is under `app/api/` (the only legitimate sites are `app/api/abrigo/agent1/route.ts` and, after Wave 1, `app/api/cornerstone/buildbear-sign/route.ts`) — no occurrence in `components/` or `lib/`.
    Write the assertions so they PASS right now (no buildbear-sign route yet; agent1 already satisfies rule 3; env.ts satisfies rule 2). Do NOT exclude this file from tsconfig — it must compile and run green in CI now.
  </action>
  <verify>
    <automated>pnpm --filter d2p-frontend exec vitest run tests/architecture/buildbear-key-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter d2p-frontend exec vitest run tests/architecture/buildbear-key-leak.test.ts` passes GREEN now (before any route exists).
    - The test fails if a line `const NEXT_PUBLIC_DEMO_SIGNER_PK = ...` is temporarily added to a component file (non-vacuous — verify once locally, then revert).
    - `pnpm --filter d2p-frontend exec tsc --noEmit` still exits 0.
  </acceptance_criteria>
  <done>Key-leak + privateKeyToAccount path-scope are enforced as a green CI vitest arch test that will keep the Wave-1 route honest.</done>
</task>

</tasks>

<verification>
- `pnpm --filter d2p-frontend exec vitest run tests/unit/mode.test.ts tests/architecture/buildbear-key-leak.test.ts` — both GREEN.
- `pnpm --filter d2p-frontend exec vitest run tests/api/buildbear-sign.test.ts` — RED (route absent) — expected at Wave 0.
- `pnpm --filter d2p-frontend exec tsc --noEmit` — exit 0.
- `pnpm --filter d2p-frontend exec biome check .` — clean.
- Test files contain no `fork` substring in their NAMES (OPS-06): `ls packages/frontend/tests/api/buildbear-*.test.ts packages/frontend/tests/unit/workflow-engine-buildbear.test.ts | ! grep -q fork`.
</verification>

<success_criteria>
- `mode.ts` parses `'buildbear'` and still defaults to `'replay'`.
- `DEMO_SIGNER_PK` is a server-only optional env var, zero NEXT_PUBLIC leak (proven by the arch test).
- All Phase 11 route/helper behaviors exist as RED tests pinning the exact discriminated reason strings.
- The pre-commit tsc + biome gates pass (RED route tests tsconfig-excluded).
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-server-routes/11-01-SUMMARY.md`. Note in the SUMMARY: this plan produces no user-visible route — per packages/frontend/CLAUDE.md, the Evidence Collector live-verify is SKIPPED (type/unit-only Wave-0 scaffold); record that explicitly.
</output>

# Project State: abrigo-somnia M1

**Last updated:** 2026-05-29 #3 (scout-addendum fold-in — resolves the two coupled HIGH findings: `234,999` = **transaction** anchor not event bound; event completeness via the scout's structural ratios [total ≈ 2.15 × txs, topic0s 1:1:1, `RequestCreated` ≈ 0.7 × txs ≈ ~165k]; block-presence sufficiency bar replaced; STATS-01 envelope corrected to ~165k; deep-history retention probe; tx-granularity ordering surface; subgraph-compatible paid swap; awaiting planning-review re-run)

## Project Reference

**Project:** abrigo-somnia — Empirical Somnia Cost Panel (M1)
**Core Value:** Which caller-side inequalities in `somnia_cost_extraction.md §6` (rows #1, #2, #4, #5, #6, #7) bind empirically, where are they slack, and what is the realized `executionCost_i` distribution per agent class versus the docs-quoted `{0.03, 0.07, 0.10}` SOMI prices — across all observed proxy implementation versions?
**M1 primary concern:** Capture the `IAgentRequester` **arrival series** (per-event timing, ordering, completeness) robustly enough that M2's arrival-process estimation (NHPP vs Hawkes) + convex-demand verdict is robust. M1 runs neither the estimation nor the verdict.
**Current Focus:** Scout-addendum (2026-05-29) folded in to resolve the two coupled HIGH findings the last review round left — the count-anchor unit mismatch (`234,999` is a **transaction** count, not an event bound) and block-vs-event sparsity (block-presence is the wrong completeness population). The completeness gate is now a three-leg gate (independent tx-anchor + scout structural event-ratio conformance + contiguity proof), the wrong-population sufficiency bar is replaced, the STATS-01 envelope is corrected to ~165k `RequestCreated` events, plus the remaining MEDIUMs (contiguity-independence, deep-history retention probe, tx-granularity ordering surface, sample-vs-skip-bar consistency) and the LOW (subgraph-compatible paid swap). Everything else the reviewers confirmed holds was left untouched. Awaiting planning-review pipeline re-run.

## Current Position

- **Milestone:** M1
- **Phase:** Pre-roadmap-review (no phase active)
- **Plan:** None
- **Status:** Roadmap revised four times (2026-05-25 HIGH/MEDIUM/LOW fixes; 2026-05-29 #1 DATA-SOURCE-01 + arrival-periodicity; 2026-05-29 #2 DATA-SOURCE-01 rigor hardening; 2026-05-29 #3 scout-addendum fold-in resolving the two coupled HIGH findings + remaining MEDIUMs/LOW); awaiting Studio Producer re-dispatch + Reality Checker + dynamic-primary re-review per CLAUDE.md three-step pipeline.

**Progress:** `[░░░░░░] 0/6 phases complete`

## Roadmap Summary

| Phase | Goal | Requirements | Status |
|---|---|---|---|
| 1. Data-Sourcing Gate, Pre-flight Addendum & Schema Foundations | DATA-SOURCE-01 **provisional** free-vs-paid verdict with four numeric sufficiency bars + Ormi free-tier probe rows + machine-checkable `data_sourcing_matrix.yaml` (gates INDEX-01, absorbs RPC-capability probe) + resolve PITFALLS A3/B1/C1 + commit all schema artifacts (arrival-timing fields first-class, ordered on `(block_number, log_index)`) + KPD-09-docs finality semantics | DATA-SOURCE-01, EVENT-01, SHARED-SCHEMA-01 | Not started |
| 2. Topic & Implementation Provenance | Per-impl ABI resolver + admin-event registration + per-`(impl, topic0)` indexed-dynamic-field enumeration | TOPIC-01, IMPL-01 | Not started |
| 3. Subgraph Indexing | Indexer (on DATA-SOURCE-01-selected source) deployed with parity (agreement, mechanism per capability matrix) + **binding completeness gate (independent transaction-coverage anchor on `234,999` txs + scout-addendum structural event-ratio conformance + indexer-internal contiguity proof)** + tx-granularity ordering/granularity verification + liveness + KPD-09-empirical rollback observation | INDEX-01 | Not started |
| 4. Parallel Cost Inputs | Rebate (4a-pre concurrent with Phase 3; 4a-validate after Phase 3) + gas panel + FX series | BYTECODE-01, GAS-01, FX-01 | Not started |
| 5. Panel Materialization | Partitioned Parquet panel with schema-artifact discipline + KPD-11b runtime validation + KPD-PANEL-JOIN 6-way contract | PANEL-01 | Not started |
| 6. Statistics, Arrival-Periodicity Precursor & M2 Ingest Contract | Per-inequality binding + descriptive arrival-periodicity precursor (`(block_number, log_index)`-ordered inter-arrival + `gap_status`-censored `Var(N)/E[N]` dispersion) + executable M2 acceptance test (incl. arrival series) | STATS-01 | Not started |

**Coverage:** 11/11 v1 requirements mapped. No orphans.

## Performance Metrics

- v1 requirements: 11
- Phases: 6
- Success criteria total: 30 (up from 27 — Phase 1 +2 [DATA-SOURCE-01 verdict SC#7, arrival-timing schema SC#8]; Phase 6 +1 [arrival-periodicity precursor SC#6])
- KPDs forward-propagated: **22 active line items** (23 table rows incl. the retired struck-through KPD-20) = 19 original concerns + KPD-PANEL-JOIN (= 20 distinct concerns) + 2 split-induced extras (KPD-09→09-docs/09-empirical, KPD-11→11a/11b; KPD-18 spans Phase 1+Phase 2 but is one row) − 0 (KPD-20 retired into DATA-SOURCE-01, contributes 0). (Prior "21" under-counted the KPD-09/KPD-11 splits.)
- PITFALLS surfaces covered: 5 / 5 (A, B, C, D, E)
- Parallelization waves: 1 (Phase 4 — three independent sub-workstreams; 4a-pre additionally overlapping Phase 3)

## Accumulated Context

### Decisions (locked at revision time)
- **(2026-05-29) DATA-SOURCE-01 folded into Phase 1 as headline deliverable, GATING Phase 3 INDEX-01.** Chosen over a new Phase 0 because Phase 1 already hosts the chain-capability probes and DATA-SOURCE-01 absorbs the RPC-capability probe; a new phase would fragment the "probe reality before building" coherence and force renumbering of all six phases. Deliverable `research/DATA_SOURCING.md` (verified capability matrix + free-vs-paid verdict, each claim with `source_url + utc_fetch_ts`, coherence-checked against x402 SUMMARY.md + somnia_cost_extraction.md).
- **(2026-05-29) Former KPD-20 RPC-capability probe absorbed into DATA-SOURCE-01's capability matrix** — `eth_getBlockReceipts` availability is now a provenance-stamped matrix row, not a free-standing KPD. Phase 3 SC#2 parity mechanism is conditional on that row. No orphan KPD remains.
- **(2026-05-29) Arrival-periodicity primacy reflected in two phases:** Phase 1 EVENT-01 arrival-timing fields (`block_ts_utc`, `block_number`, `log_index` ordering, gap-detection metadata) made first-class non-nullable load-bearing columns (new Phase 1 SC#8); Phase 6 STATS-01 gains a descriptive arrival-periodicity precursor (inter-arrival series + intensity-over-time + `Var(N)/E[N]` overdispersion + periodicity, new Phase 6 SC#6). Descriptive ONLY — no NHPP/Hawkes fit, no convex-demand verdict (both M2). M2 ingest acceptance test (KPD-13 / Phase 6 SC#2) now loads the arrival series.
- **(2026-05-29) M1→M2 consumer contract updated** from "parametric fit" to "arrival-process estimation + convex-demand verdict" throughout ROADMAP + REQUIREMENTS.
- **(2026-05-29 #2, HIGH-1) Block-cadence timing corrected + arrival-ordering key fixed.** Replaced "block-level ~1 s timing" everywhere with native ~72 ms/block cadence (scout-measured: 316M blocks ≈ 1.2M/day, ~14× finer than 1 s). DATA-SOURCE-01 records whether `block.timestamp` is whole-second or sub-second; if whole-second, up to ~14 events share a timestamp so **`(block_number, log_index)` is the arrival-ordering key, `block_ts_utc` is a coarse secondary**. EVENT-01 + the Phase 6 arrival series carry ordering on `(block_number, log_index)`.
- **(2026-05-29 #2, HIGH-2/HIGH-3) Completeness gate reframed + numeric sufficiency bars.** DATA-SOURCE-01's pre-INDEX verdict is now a **PROVISIONAL source selection** ("completeness re-confirmed in Phase 3"); the ~1% Ormi-vs-RPC sample bounds **agreement, not completeness** (shares the 1000-block cap). **[Superseded in part by #3 below — see the count-anchor unit correction.]**
- **(2026-05-29 #3, HIGH-1+HIGH-2 — scout-addendum fold-in) Count-anchor unit mismatch + block-vs-event sparsity resolved.** Using `.planning/scout/2026-05-29/event_count_addendum.md`: `234,999` is a **transaction** count (exact, independent, getLogs-cap-free), re-labelled the **transaction-coverage anchor** — NOT an event bound. Phase 3 SC#6 is now a **three-leg gate**: (a) reconcile the indexer's distinct proxy-targeted `tx_hash` count against `234,999`; (b) **event completeness via the scout's structural ratios** (total ≈ 2.15 × txs; topic0s 1:1:1; `RequestCreated` ≈ 0.7 × txs ≈ ~165k; response `0xb623…` 1–5×/tx) within a plan-phase tolerance, as a sanity check — this **REPLACES the "≥99.9% blocks present" bar** (block-presence measured the wrong population at ~0.0016 events/block); (c) the indexer-internal contiguity proof, which proves only host cursor advancement and is completeness-bearing **only when conjoined with the tx-anchor (a)** (a host can advance its cursor over under-indexed blocks). The exact `RequestCreated` lifetime count is an **INDEX-01 output** cross-checked against (a)+(b), not a pre-known constant; no cheap EXACT independent event-count oracle exists pre-indexing. The Phase-1 sufficiency bars (SC#7(ii)) likewise switch from block-presence to distinct-tx-coverage + structural-ratio conformance, restricted to what the ~1% sample can test; the no-contiguous-skip check is demoted to the Phase-3 full-range contiguity proof. STATS-01's envelope corrected from "5,000 under 234,999" to the **~165k `RequestCreated`-event envelope** (clears the 5,000 floor by ~30×).
- **(2026-05-29 #3, MEDIUM) Contiguity-independence + deep-history retention + tx-granularity ordering + sample-vs-skip consistency.** `_meta`/`COUNT(DISTINCT block_number)` cursor contiguity proves only host belief, not event emission — completeness-bearing only with the tx-anchor (SC#2 + SC#6 leg c). Ormi retention is answered by an **explicit deep-history probe at the deployment block** (~320M blocks back, confirm non-empty correct response), a **distinct row + distinct method** from the throughput-only backfill-throttle probe. Phase 3 SC#7 ordering is testable at **tx granularity** (a single request tx emits the request pair + 1–5 response events in one block with ascending `log_index`); multi-`RequestCreated`-block verification is not a hard stratification requirement (sparsity). The no-contiguous-skip bar is evaluated by the Phase-3 contiguity proof, NOT the ~1% Phase-1 sample.
- **(2026-05-29 #3, LOW) Paid swap constrained to a subgraph-compatible host** (paid Ormi/Protofire) to preserve the SC#2 parity mechanism + KPD-08 mapping surface; a non-subgraph paid archive instead triggers an INDEX-01 **sub-plan re-author** (pre-costed), not a phase re-decomposition. Scout addendum recorded as a KPD-16 provenance row (DATA-SOURCE-01 matrix + PROJECT.md scout table).
- **(2026-05-29 #2, HIGH-4) Ormi free-tier probe rows + pre-costed paid fallback.** Capability matrix now carries explicit Ormi free-tier rows: retention/history depth (to deployment block?), backfill throttle / rate cap, subgraph-deployment permission (deploy vs read-only) — each by vendor doc OR bounded live backfill-rate probe. Free-tier selection is provisional-pending-Phase-3-backfill-confirmation with the paid alternative **pre-costed**, so a Phase-3 backfill failure swaps in the paid source without phase re-decomposition.
- **(2026-05-29 #2, MEDIUM) Phase 3 SC#7 ordering/granularity verification** (multi-event-block `(block_number, log_index)` tuple-for-tuple vs RPC, `block_ts_utc` exact match); **Phase 6 SC#6 `gap_status` column** (`observed`/`indexer_gap_censored`/`source_gap`) with `Var(N)/E[N]` gap-censoring; **KPD-14 gap threshold set in blocks** not days; **`eth_getBlockReceipts`-absent fallback** weakens completeness to contiguity proof (Phase 3 SC#2) and feeds back to the free-vs-paid verdict; **Phase 1 SC#7(iv) coherence check is a real budget-vs-completeness reconciliation**, not a one-line citation.
- **(2026-05-29 #2, LOW) Machine-checkable `research/data_sourcing_matrix.yaml`** (fixed schema) emitted alongside `DATA_SOURCING.md`; **KPD line-item count corrected 21 → 23**; **wall-clock honesty** — the live Ormi backfill-rate probe is the unbounded-by-doc-read tail most likely to consume the Phase 1 1.5–2× allowance.
- Phase 4a (BYTECODE-01) is split across the Phase 3 boundary: 4a-pre (Tier-B decompilation) runs concurrently with Phase 3; 4a-validate (Tier-C empirical residual + Tier-B holdout) depends strictly on Phase 3 INDEX-01.
- KPD-09 split into KPD-09-docs (Phase 1 — doc-side finality semantics) + KPD-09-empirical (Phase 3 — ≥1-hour rollback observation).
- KPD-11 split into KPD-11a (Phase 1 — batch manifest schema artifact) + KPD-11b (Phase 5 — runtime JSON-schema validation on every batch write).
- KPD-18 split into Phase 1 (schema column reservation for `agent_class_keccak`/`agent_class_string`) + Phase 2 (per-`(impl, topic0)` enumeration in resolved ABIs).
- KPD-02 (`gas_payment_bucket`) ownership: authored in 4a-validate, consumed by Phase 5 + Phase 6.
- KPD-PANEL-JOIN (PANEL-01 6-way join contract: join order, multiplicity, responses-sidecar rule, `decode_status` propagation) in Phase 5.
- M1 reference environment defined at top of ROADMAP.md (≥32 GB RAM, NVMe, Linux, polars ≥1.20); all read-time SLOs assert against M1 env not M2 env.
- Phase 5 SC#5 uses local `tests/fixtures/abrigo_analytics_consumer_emulation.py`; cross-repo coordination is out of M1 acceptance scope.
- Phase 1 KPD-16 ordered FIRST inside Phase 1 (scout-artifact integrity surfaced before any other Phase 1 work).
- Phase 1 sizing acknowledged: 1.5–2× per-phase median wall-clock allowance (DATA-SOURCE-01 + 7 KPDs, 8 success criteria, 3 PITFALLS pre-conditions). Not split further.
- STATS-01 re-entry rule on `Upgraded` events (MEDIUM-4): conditional re-entry into Phase 2 + Phase 4a-validate before Phase 6 STATS-01 is valid for blocks ≥ upgrade_block.
- KPD-12 extended (MEDIUM-1) to cover total-count shortfall (<5,000 RequestCreated rows) in addition to per-class shortfall.

### TODOs (carried into plan-phase)
- Phase 1 plan-phase executes DATA-SOURCE-01: produce `research/DATA_SOURCING.md` (prose) + `research/data_sourcing_matrix.yaml` (machine-checkable, fixed schema) — capability matrix incl. `eth_getLogs` 1000-block cap, `eth_getBlockReceipts` probe, median block cadence + `block.timestamp` whole-second flag, Ormi retention/throttle/deployment rows; four numeric sufficiency bars; PROVISIONAL free-vs-paid verdict with pre-costed paid fallback; real budget-vs-completeness coherence check vs x402 SUMMARY.md + somnia_cost_extraction.md. Pin `N` (no-skip range) + `gap_block_threshold` in plan-phase. INDEX-01 plan-phase blocks on this verdict; the binding completeness re-confirmation is a Phase-3 deliverable (contiguity proof + count anchor), not Phase 1.
- Phase 1 plan-phase must resolve the contract deployment block from creator EOA `0x320362C7…fdE88936`'s earliest tx — needed for INDEX-01 manifest `startBlock`.
- Phase 1 plan-phase executes KPD-09-docs (doc-side finality verdict); Phase 3 plan-phase executes KPD-09-empirical (≥1-hour rollback observation).
- Phase 1 plan-phase commits EVENT-01 DDL with arrival-timing fields as first-class non-nullable load-bearing columns + ordering/dedup guarantees (arrival-periodicity primacy).
- Phase 3 plan-phase may sub-split into 3a (manifest scaffold + admin-event handlers, parallel with Phase 2) and 3b (business-event handlers + agreement parity + liveness + rollback + **binding completeness gate: contiguity proof + Blockscout count anchor** + ordering/granularity verification) per MEDIUM-2 slack; parity mechanism selected by the DATA-SOURCE-01 capability matrix (`eth_getBlockReceipts`-absent fallback weakens completeness assurance and feeds back to the free-vs-paid verdict).
- Phase 4a-validate plan-phase must close out Tier-B as `tier_b_status = 'inconclusive'` if both decompilers return unusable output OR holdout fails ±2% threshold (already committed; not an open question).
- Phase 5 plan-phase must implement the KPD-PANEL-JOIN 6-way join contract.
- Phase 6 plan-phase produces the descriptive arrival-periodicity precursor (`(block_number, log_index)`-ordered inter-arrival + intensity-over-time + `gap_status`-censored `Var(N)/E[N]` overdispersion + periodicity, gap-audited via KPD-14 block-count threshold) and the M2 ingest test that loads the arrival series; reports `impl_segments_observed = 1` collapsed-dimension clause when no `Upgraded` event observed.

### Blockers
None at roadmap-revision time. (Phase-3 INDEX-01 is gated by DATA-SOURCE-01, but that gate is internal to the roadmap ordering, not an external blocker.)

### Open Questions
ROADMAP.md "Open Questions Flagged for Plan-Phase" — 2 items: (1) deployment-block resolution; (2) plan-phase parameter pins `N` (no-skip range, default 1000) + `gap_block_threshold` (calibration values, not design choices). The former RPC-parity open question's residual is now a DATA-SOURCE-01 capability-matrix deliverable.

## Session Continuity

**Last action:** Roadmap re-revision 2026-05-29 #3 — scout-addendum fold-in resolving the two coupled HIGH findings (count-anchor unit mismatch + block-vs-event sparsity) + remaining MEDIUMs/LOW. `234,999` re-labelled as the transaction-coverage anchor; Phase 3 SC#6 rebuilt as a three-leg gate (tx-anchor + scout structural event-ratio conformance + contiguity proof); the "≥99.9% blocks present" sufficiency bar replaced by distinct-tx-coverage + structural-ratio conformance (Phase 1 SC#7(ii) + Phase 3 SC#6); STATS-01 envelope corrected to ~165k `RequestCreated` events; contiguity-independence stated (SC#2 + SC#6); deep-history retention probe split from the throttle probe (SC#7(i)); tx-granularity ordering surface (SC#7); subgraph-compatible paid swap (SC#7(iii)); scout addendum logged as a KPD-16 provenance row. ROADMAP.md + PROJECT.md + REQUIREMENTS.md + this STATE.md edited surgically in place; coverage still 11/11; all #2 fixes + six prior HIGH-fixes untouched.
**Next action:** Re-run the full planning-review pipeline (Studio Producer selector → parallel Reality Checker + dynamic primary reviewer) against the hardened `.planning/ROADMAP.md` + PROJECT.md + REQUIREMENTS.md. If both PASS, proceed to commit + `/gsd:plan-phase 1`.
**Resume hint:** Read `.planning/PROJECT.md` (amended: DATA-SOURCE-01, arrival-periodicity banner, sharpened EVENT-01/STATS-01) → `.planning/REQUIREMENTS.md` (11 v1 reqs, traceability 11/11) → `.planning/ROADMAP.md` (revised) → this file.

---
*State initialized: 2026-05-25 alongside ROADMAP.md.*
*State revised: 2026-05-25 after planning-review NEEDS WORK iteration #1.*
*State revised: 2026-05-29 — DATA-SOURCE-01 gate + arrival-periodicity sharpening; coverage 11/11.*
*State revised: 2026-05-29 #2 — DATA-SOURCE-01 rigor hardening (native ~72 ms/block timing + `(block_number, log_index)` ordering key; provisional source selection + Phase-3 contiguity/count-anchor completeness gate; four numeric sufficiency bars; Ormi free-tier probe rows + pre-costed paid fallback; `gap_status` censoring + block-count gap threshold; machine-checkable capability matrix; KPD count 23). Coverage 11/11; six prior HIGH-fixes preserved.*
*State revised: 2026-05-29 #3 — scout-addendum fold-in (resolves the two coupled HIGH findings + remaining MEDIUMs/LOW): `234,999` = transaction-coverage anchor (not event bound); Phase 3 SC#6 three-leg gate (tx-anchor + scout structural event-ratio conformance [total ≈ 2.15 × txs, topic0s 1:1:1, `RequestCreated` ≈ 0.7 × txs ≈ ~165k] + contiguity proof); block-presence sufficiency bar replaced by distinct-tx-coverage + structural-ratio; STATS-01 envelope corrected to ~165k events; contiguity-independence + deep-history retention probe + tx-granularity ordering surface + sample-vs-skip consistency (MEDIUM); subgraph-compatible paid swap (LOW); scout addendum as KPD-16 provenance row. Coverage 11/11; all #2 fixes + six prior HIGH-fixes intact.*

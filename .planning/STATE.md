---
gsd_state_version: 1.0
milestone: v2.1
milestone_name: — Live Agent Integration
status: blocked
stopped_at: 18-01 DOCUMENTED BLOCK — Somnia validator LLM-inference callbacks not landing; runner built+committed, no mandate landed, nothing fabricated/published
last_updated: "2026-06-08T14:08:04Z"
last_activity: "2026-06-07 — completed 14-03-PLAN.md (cornerstone Wave 3: `resolveFromMandate` FORK-PROVEN — a `HedgeMandate` flows through the deterministic regime-conditional geometry → the Fix-C sink → a real wCOP/USDC Panoptic position at the EXACT structural K_hi tick 360360 + `numberOfLegs(exec)>0`; the 8-param `ExecutorDecided` honesty flag (`nonErgodicDisclosed==true`) + TEMPLATE caveat decoded from the live mint; behavioral LLM-independence via `MockRevertingPlatform`; the direct-path size>127 guard. UNBLOCKED by a gate-passed `volToWidth` even-width invariant — odd widths snap to even so symmetric Panoptic leg bounds stay tickSpacing-aligned — landed as a real RED→GREEN evm-TDD split (`f92b0f7` test → `e686d4d` fix, per-file ancestry verified). demo fork 6/6, fork EXEC 7/7, Representativeness 17/17, onResult 4/4, fork-free 114/114, build exit 0)"
progress:
  total_phases: 12
  completed_phases: 9
  total_plans: 36
  completed_plans: 31
  percent: 100
---

# Project State: abrigo-somnia v2.1 — Live Agent Integration

> **v2.1 (current), 2026-06-07.** Backend deploy serving the frontend live-Agent-1 path: deploy the
> two-leg `MacroHedgeStrategist` (`StrategistDecided` API) live on Somnia 50312, prove on-chain, publish
> the artifact. Phases continue at **17**. Source: `docs/FRONTEND-REQUEST-2026-06-07-strategist-live-deploy.md`.
> **v2.0 (Convex Instrument) remains OPEN** (parallel track — cornerstone E2E-01 video pending, phases 9–10
> deferred); the v2.0 state below is preserved verbatim and is NOT closed by v2.1.

## Current Position (v2.1 — Live Agent Integration)

- **Milestone:** v2.1 — Live Agent Integration (Somnia two-leg strategist live deploy)
- **Phase:** 18 — On-chain decision-moves proof + publish (**BLOCKED** — 0/1 plan landed; LIVEDEP-02/03/04/05 unverified)
- **Plan:** 18-01 **DOCUMENTED BLOCK (2026-06-08)** — runner FULLY built + committed (`8190564` Task 1/2 two-leg `run_two_leg` + divergent run-2 + idempotent persist; `187e347` Rule-3 conditional oracle refresh) and `bash -n` clean, ABI extracted (local-only, has `StrategistDecided`). BUT the live proof is BLOCKED: **Somnia testnet validator LLM-inference callbacks are not landing.** Against the reused live CONSUMER `0xf0570CcB1271FFaFf4caCA628F3632257f177b1D`, all FREE surface gates + freshness gate PASS, the school-leg request fired TWICE (real on-chain txs `0x2a8230cc…` 180s + `0xb1ff1d17…` 300s, both `status 0x1`, deterministic decisionId `0x…b24ac1afbcefc708`) but `decisionState` stayed `(false,false,0,"")` and `getMandate` all-zero — no `StrategistDecided`/`DecisionFailed`, a silent validator no-show. Earlier the json-fetch oracle refresh also `MacroFailed` (keeper-proxy 404). NO full mandate ⇒ **did NOT** publish `somnia-strategist-deployment.json` (would need 3 real tx hashes) nor reverse the §6 prohibition (would be a false "✅ LIVE — proven on-chain" claim) — an unverifiable claim is explicitly unacceptable. ~0.63 STT spent (bounded, within reserve). NOT fabricated. chainId=137 untouched, CONSUMER reused (no `forge create`), no secret committed. (NB: Phase-17 saw a live school callback earlier today — infra is INTERMITTENT; re-run when it recovers.)
- **Status:** BLOCKED — re-run `CONSUMER=0xf0570CcB…7b1D MACRO_ORACLE=0xAcA751…983f bash script/macro-hedge-strategist-e2e.sh` once the Somnia validator inference layer + keeper-proxy recover (pre-flight a cheap liveness probe first). Frontend live-Agent-1 path stays blocked until then.
- **Next phase:** 18-02 (or re-run 18-01) — execute the bounded proof + publish when validator infra is live; consumes the already-built runner.
- **Source of truth:** `docs/FRONTEND-REQUEST-2026-06-07-strategist-live-deploy.md` (§2/§3/§4)
- **Spend-gate reason for the split:** §4 mandates re-confirming the volatile `LLM_AGENT_ID`/platform BEFORE spending STT — Phase 17 (deploy + cheap school-leg liveness probe) gates Phase 18 (the full STT-spending decision-moves prove + publish).
- **Guardrails:** keep v1 `0xfA428171…` reachable; do NOT alter the join's `chainId = 137`; funded STT key in `contracts/.env`; real tx hashes mandatory.
- **Runner to adapt (not author):** `contracts/script/macro-hedge-strategist-e2e.sh` (currently v1).

---

---

# (preserved) Project State: abrigo-somnia v2.0 — Convex Instrument

> **Parallel-track note.** The M1 donor-transfer *econometrics* milestone is **PARKED mid-Phase-3**
> and preserved verbatim at `.planning/{STATE,ROADMAP,REQUIREMENTS}-M1-donor-transfer-2026-06-01.md`
> (resumable; M1 phase folders `phases/01..03/` untouched). This STATE now tracks the
> **convex-instrument engineering** milestone (phases 7–10; numbering starts at 7 to avoid
> collision with the existing `phases/01..03/` on disk). M1 later calibrates this instrument's
> params (K, k, σ_CPI, the CPI→FX coefficient). Do NOT edit the M1 snapshots.

## Project Reference

See: `.planning/PROJECT.md` (§ Current Milestone: v2.0, updated 2026-06-01)

**Core value (this milestone):** A TE-sized long-gamma cCOP/USD hedge on borrowed-Panoptic-V2-data-model
contracts (Base fork against our own cCOP/USDC UniV4 pool); premium = upfront collateral with
data-cost-weighted reimbursement; post-Keynesian/Shiller-grounded; strict evm-tdd.
**Current focus:** Phase 7 executing — Plans 01 (toolchain), 02 (borrowed Panoptic V2 core + `IPanopticData` seam + MockCcop), 03 (Base-fork harness) and 04 (our own cCOP/USDC UniV4 pool: initialize + full-range LP + state read, FORK-02) complete; next is 07-05 (seam test: `factory.deployNewPool` against the initialized+seeded pool, then non-reverting `dispatch`/`getAccumulatedFeesAndPositionsData` via `IPanopticData`).

## Current Position

- **Milestone:** v2.0 — convex-instrument
- **Phase:** 8 — LongGammaWrapper cash-flow (PLANS COMPLETE — 7/7; phase-level gsd-verifier runs next)
- **Plan:** 08-07 complete (two named fuzz invariants, hand-written against the Plan-01 `invariants.tree`): `invariant_residualNeverExceedsHoldings` (SINGLE claim: `lastPaid_i <= preClaimSurviving_i+1`) + `invariant_userClaimsBackedByCollateral` (CUMULATIVE non-inverted: `totalPaid_i <= cumPreClaimSurviving_i+1`); a bounded `LongGammaWrapperHandler` drives deposit→swap→sync→close→claim (stores `address(this)` as `user` so `close()`'s NotUser gate passes), maintains two independent CLAIM-TIME payout ledgers; both pass on the Base fork at the additive `[invariant]` floor (runs=16/depth=16) with 0 reverts, proven NON-vacuous by mutation; 8-canonical-tree set complete; 30/30 existing wrapper tests un-regressed
- **Status:** Milestone complete
- **Progress:** [██████████] 100%
- **Last activity:** 2026-06-07 — completed 14-03-PLAN.md (cornerstone Wave 3: `resolveFromMandate` FORK-PROVEN — a `HedgeMandate` flows through the deterministic regime-conditional geometry → the Fix-C sink → a real wCOP/USDC Panoptic position at the EXACT structural K_hi tick 360360 + `numberOfLegs(exec)>0`; the 8-param `ExecutorDecided` honesty flag (`nonErgodicDisclosed==true`) + TEMPLATE caveat decoded from the live mint; behavioral LLM-independence via `MockRevertingPlatform`; the direct-path size>127 guard. UNBLOCKED by a gate-passed `volToWidth` even-width invariant — odd widths snap to even so symmetric Panoptic leg bounds stay tickSpacing-aligned — landed as a real RED→GREEN evm-TDD split (`f92b0f7` test → `e686d4d` fix, per-file ancestry verified). demo fork 6/6, fork EXEC 7/7, Representativeness 17/17, onResult 4/4, fork-free 114/114, build exit 0)
- **Cornerstone (phases 11–15) tracker:** 11 ✅ · 12 ✅ · 13 ✅ · **14 ✅ COMPLETE (14-01 ✅, 14-02 ✅, 14-03 ✅ of 3 plans)** · 15 pending — MVP remaining: **Phase 15 (UI E2E + CI — the judges' artifact)**

### Phase 8 — LongGammaWrapper cash-flow

- **2026-06-02 (08-01):** Interface-first surface LOCKED. `ICostMeter.cost(address)->(cost0,cost1)` external-meter seam (zero-address⇒(0,0) caller convention). `IPanopticData` extended with `getOracleTicks()` (+`OraclePack` import) for involuntary-branch monitoring; stale `@L221`→`L431` drive-by fix (NatSpec + inline). `LongGammaWrapper.sol` skeleton compiles with the swap seam intact (NO borrowed concrete import — grep-guard==0; P1 no-streamia-constant guard clear): `State{Uninitialized,Open,Closed,Claimed}`, single-record ledger, 4 events, 4 errors, 5 locked entrypoints; `setCostMeter` (owner+Uninitialized-only) and `_costOf` (zero-address⇒(0,0)) implemented now, the four fork-dependent entrypoints revert `UNIMPLEMENTED` (Plans 02–04). `LongGammaWrapper.invariants.tree` = the 8th canonical tree, names both fuzz invariants, committed before any `.t.sol` (Iron Law). `forge build` green. Commits: `ef0e5a2` (interfaces), `01fd897` (wrapper), `7ec4a1f` (tree). **Deviations (3, all Rule-3 blocking-to-pass-the-plan's-own-gates):** invariants.tree → bulloak-0.9.2 `when/it` keyword form (bare `invariant_*` leaf labels do not parse, same 07-03/07-04 tree-deviation precedent); removed `@types/`/`panoptic-borrowed` literals from wrapper NatSpec (solc 6546 tag error + grep-guard); single-lined `getOracleTicks`/`enum State` to satisfy literal-signature grep ACs. **Metric (08-01):** 3 tasks, 4 files, ~35 min.
- **2026-06-02 (08-02):** Wave-0 test harness shipped (2 files, both new). `helpers/V4SwapHelper.sol` — an `IUnlockCallback->PoolManager.swap` deterministic fee generator (swap analogue of the Phase-7 `V4LpHelper`): `swapExactIn(key,zeroForOne,amountSpecified)` moves `feeGrowthInside` so an in-range chunk earns LP fees — the WRAP-03 streamia source, since block advance alone accrues nothing (RESEARCH Pitfall 2). `SwapParams` = `IPoolManager.SwapParams`; price limits `TickMath.MIN_SQRT_PRICE+1`/`MAX_SQRT_PRICE-1`; inlined sync->transfer->settle / take (no v4-core test settler util, 07-04 type-identity). `LongGammaWrapperBase.sol` — the M-3 deploy-isolation base: forks Base @46700000, runs `PanopticV2DeployHelper.deployPanopticV2()` BEHIND the base (so units that EXTEND it import no borrowed concrete and no helper — grep-guard `panoptic-borrowed==0`), rebuilds the hookless fee-500 swap `PoolKey`, wires a `V4SwapHelper`, and **seeds a SELLER SHORT** (isLong=0, `SELLER_SIZE=10 ether >= LONG_SIZE=1 ether`) at the OTM (STRIKE_OFFSET=2000) tickSpacing-aligned (width=2) chunk via `pool.dispatch` — the WRAP-02 `NotEnoughLiquidityInChunk()` prerequisite. Shared for Plans 03-06: `_longTokenId()` (long at the SAME chunk), `_oneLegArgs(id,size)`, `_closeSellerShort()` (burns the short to free the pool-wide `maxRedeem` cap before claim, B4); seller tokenId + address exposed. **Smoke-verified on the LIVE Base fork** (throwaway test, deleted post-gate): deploy handles populated, `numberOfLegs(seller)>0`, seller holds ct0/ct1 4626 shares, `_closeSellerShort()` -> `numberOfLegs==0`. Commits: `5dfc838` (V4SwapHelper), `501fb78` (LongGammaWrapperBase). **Deviations (3, all Rule-3):** `int24(uint24(CHUNK_WIDTH))` cast at both `addLeg` sites (uint8 constant has no implicit int24 conversion — the seam test's bare literal `2` converted, a typed constant does not); reworded NatSpec to drop the literal `panoptic-borrowed` token (grep-guard==0, same 08-01 precedent); single-lined `swapExactIn` signature + reworded settler-util comments to satisfy the literal grep ACs (`CurrencySettler` count==0). **Note:** executed on branch `feat/macro-hedge-agent` (branching=none, same Phase-7/11 in-place precedent), not the STATE-referenced `feat/keeper-vercel-buildoutput`. **Metric (08-02):** 2 tasks, 2 files, ~5 min.
- **2026-06-02 (08-03):** Open lifecycle SHIPPED + WRAP-01/02 fork-proven (5/5 green). `LongGammaWrapper.deposit(address _user, uint256 assets0, uint256 assets1, TokenId longId, uint128 longSize, int24[3] limits)` IMPLEMENTED (the Plan-01 3-arg stub SUPERSEDED — the long-leg chunk params are caller-supplied because the wrapper has no view of the seller's seeded chunk; this 6-arg signature is now LOCKED for Plans 04-07): one-shot `WrongState` guard, `transferFrom`+`approve`+`ct.deposit(assets, address(this))` so the WRAPPER owns the 4626 shares (`ct.asset()` yields the underlying — no concrete `token0()`), `pool.dispatch(list, finalIds, sizes, lim, false, 0)` minting `isLong=1` against the seeded same-chunk seller short, ledger + last-observation `lastSurviving0/1` checkpoints, `Uninitialized->Open`, `PositionOpened`. **WRAP-01 custody PROVEN:** `ct.balanceOf(wrapper)>0 && ct.balanceOf(user)==0 && numberOfLegs(wrapper)>0` + a length-1 `PositionBalance[]` with `positionSize>0` from `getAccumulatedFeesAndPositionsData` (NO `positionIdList` getter — RESEARCH correction #1). **WRAP-02 PROVEN:** stored `positionTokenId.isLong(0)==1`. Over-funded collateral cap (P3): test deposits `type(uint104).max` both tokens so the 18-dp tracker backs a 1-ether long. `open.tree` committed FIRST (Iron-Law ancestry verified: `ba0fc57` tree -> `afe9b75` impl -> `a31f93e` test). `forge test --match-path test/instrument/LongGammaWrapper.open.t.sol --fork-url $BASE_RPC_URL` = **5 passed, 0 failed** on the live Base fork. Swap-seam grep-guard==0 on BOTH the wrapper and `open.t.sol`; P1 streamia-constant guard clear; `forge build` exit 0. **Continuation note:** the three task commits + on-disk artifacts pre-existed from a prior session that died before docs (same 11-02 socket-death pattern); this run independently re-verified every acceptance gate before producing SUMMARY/STATE — no rework, no deviations. **Deviations (0):** plan executed exactly as written. **Metric (08-03):** 3 tasks, 3 files, ~20 min (incl. re-verification).
- **2026-06-02 (08-04):** Streamia READ SHIPPED + WRAP-03 fork-proven (6/6 green). `LongGammaWrapper.recordStreamia() public view returns (uint128 streamia0, uint128 streamia1)` IMPLEMENTED as a ZERO-arithmetic passthrough READ of `pool.getAccumulatedFeesAndPositionsData(address(this), true, [positionTokenId]).longPremium` (`rightSlot()`=token0, `leftSlot()`=token1), gated `if (state != State.Open) revert WrongState();`. **WRAP-03 proven on the Base fork via FOUR legs:** (a) READ-FIDELITY (`recordStreamia()` == the SAME getter's `longPremium` slots at the SAME tick, RHS pinned to the CORRECT-tokenId long slot — a shortPremium/wrong-leg/multiplier wrapper diverges); (b) NON-ZERO FLOOR (`rec0>0||rec1>0` after a ~2e31 price-UP swap `zeroForOne=false` selling token1/cCOP into the +2000 OTM chunk band — block advance accrues nothing, Pitfall 2/4); (c) DIRECTIONAL/MONOTONIC (more fee-generating swaps ⇒ recorded strictly increases); (d) STATE-GATE PRE-OPEN (Uninitialized wrapper ⇒ `recordStreamia()` reverts `WrongState`). **NO cross-tick `assertEq(recorded, OptionBurnt.premiaByLeg)` gate** (`recordStreamia`@currentTick L437/L445 vs `premiaByLeg`@tick=0 under COMMIT_LONG_SETTLED L1159-1161 diverge — a gating equality would revert; available-premium cap L1186-1235 is short-branch-only, NOT the cause). `streamia.tree` committed FIRST (Iron-Law ancestry verified: `891c4e6` tree -> `c233b01` impl -> `2d239a0` test). `forge test --match-path test/instrument/LongGammaWrapper.streamia.t.sol --fork-url $BASE_RPC_URL` = **6 passed, 0 failed** on the live Base fork. P1 streamia-constant grep-guard (`SPREAD_MULTIPLIER`/`perBlock`/`streamiaPerBlock`==0) + swap-seam grep-guard (`panoptic-borrowed`==0) hold on BOTH the wrapper and the test; `OK_NO_BAD_GATE`. **Approved relocation:** the POST-Claimed state-gate leg moved 08-04→08-05 `claimResidual.t.sol` — `close()` is UNIMPLEMENTED at wave 4, so pulling it forward would break the Iron Law; `streamia.t.sol` asserts ONLY the pre-Open WrongState leg (`grep -c expectRevert == 1`), no `// TODO(plan-05)` marker. Full state-gate = 08-04 (pre-Open) + 08-05 (post-Claimed). **Continuation note:** Tasks 1-2 (`891c4e6`/`c233b01`) + an uncommitted on-disk `streamia.t.sol` pre-existed from a prior session; this run removed the stale `spent.close()` POST-OPEN-WINDOW block (the relocation blocker), re-ran every gate, and committed Task 3. **Deviations (0):** plan executed exactly as the relocation-edited spec prescribes. **Metric (08-04):** 3 tasks (2 verified + 1 executed), 3 files, ~12 min (continuation).
- **2026-06-02 (08-05):** Voluntary exit SHIPPED + WRAP-04 fork-proven (close **6/6** + claimResidual **7/7** green). `close()` (user-gated `NotUser` voluntary burn via `pool.dispatch(list, emptyFinal, [0], lim, false, 0)` — the Phase-7-proven voluntary args; requires `numberOfLegs(wrapper)==0`; `Open->Closed`; closes the trapped-funds path / ROADMAP SC-5 / BLOCKER N1), `_reconcile()` internal + `syncResidual()` permissionless poke (re-reads surviving, emits `ResidualEroded` on a per-interval drop, last-OBSERVATION re-checkpoint, AND promotes `Open->Closed` on `numberOfLegs==0` — the **involuntary-close detection authored HERE**, the shared enabler for the Plan-06 forceExercise/liquidation branches), and `claimResidual()` (caller-agnostic; **CEI** — `claimed`+`state=Claimed`+`_reconcile` BEFORE the redeem; per-token `residual_i = max(convertToAssets(balanceOf(wrapper)) - _costOf_i, 0)`; **cap-aware + dust-guarded** `_redeemCapped` = `min(previewWithdraw(residual), maxRedeem(wrapper))` with the `previewRedeem(shares)==0` BelowMinimumRedemption guard so the claim NEVER reverts; pays the stored `user`; idempotent). **NO streamia double-count** (Pattern 7: streamia+commission already netted into the share balance by `settleBurn`; the ONLY deduction is `_costOf()`==(0,0) for the v1 zero-address meter). **WRAP-04 PROVEN:** the primary proof perturbs fees, restores the price, `close()`s, `_closeSellerShort()`s (frees the pool-wide `maxRedeem` cap, B4), then a RANDOM caller's `claimResidual()` pays the stored `user` a surviving-bounded figure (`assertLe(paid, surviving+1)`, `assertGt(paid,0)`); second claim reverts `AlreadyClaimed`; WrongState-while-Open; surviving-at-close DIVERGES from `deposited` (streamia netted in) and `surviving<=deposit`. **RELOCATED 08-04 leg verified here:** `test_claim_recordStreamiaRevertsPostClaimed` (full lifecycle to `Claimed`, then `recordStreamia()` reverts `WrongState`) — full state-gate = 08-04 (pre-Open) + 08-05 (post-Claimed). Both trees committed FIRST (Iron-Law ancestry verified: `fb85a98` trees -> `e967f88` impl -> `8c4f422` tests; `44984e5` idempotency-ordering fix between impl and tests). Swap-seam grep-guard (`panoptic-borrowed`==0) + P1 streamia-constant guard==0 on both new test files and the wrapper; `bulloak check` co-location clean; `forge build` exit 0; open 5/5 + streamia 6/6 NOT regressed. **Deviations (3, all auto-fixed):** (1) Rule-1 — `claimed` checked BEFORE the `state!=Closed` gate so a second claim reverts `AlreadyClaimed` not `WrongState` (the first claim sets `state=Claimed`); commit `44984e5`. (2) Rule-3 — the ~2e31 fee-seeding up-swap trips `PriceImpactTooLarge` on the burn (current-vs-median tick delta > 2·tickDeltaLiquidation, PanopticPool L679-682), fixed by a calibrated `_restorePrice(4e15)` USDC down-swap returning spot to ~+309 of the 359268 start tick (prototyped on the live fork; fee growth stays accrued, cumulative). (3) Rule-1 — `residualMovesWithFees` premise corrected: a swap alone does not move `convertToAssets(balanceOf)`, so it now asserts surviving-at-close diverges from `deposited` (Pattern 7) + `surviving<=deposit`. **Metric (08-05):** 3 tasks, 5 files, ~11 min.
- **2026-06-02 (08-06):** Three `dispatchFrom` involuntary branches SHIPPED + WRAP-03 FULLY fork-proven (settleLong **2/2** + forceExercise **2/2** + liquidation **2/2** green). **TEST-ONLY** — NO src edit; consumes (never re-authors) the 08-05 `_reconcile()` `Open->Closed` involuntary-close detection (src L247 confirmed present). **settleLongPremium** (dispatchFrom `toLen==finalLen`, solvent → `_settlePremium`, NO `_burnOptions`): position STAYS Open (`numberOfLegs` UNCHANGED, `state==Open`) — the load-bearing settle-stays-Open distinction; `ResidualEroded` conditional (m1 coverage-gap NOTE flags it). **forceExercise** (dispatchFrom `finalLen==toLen-1`, solvent → `_forceExercise` burns the leg): `numberOfLegs→0`, `syncResidual` promotes Open→Closed, `claimResidual` pays a surviving-derived residual to the stored user (never overpays, pre-claim snapshot). **liquidation** (dispatchFrom `finalLen==0`, insolvent at all 4 ticks → `_liquidate`): burns all → Closed; residual floors at ≥0; never overpays. **Two reusable fork-driving recipes:** (1) **pokeOracle TWAP-EMA convergence** — a price-UP fee-seeding swap moves `currentTick` ~1982 ticks but the slow TWAP EMA lags, so the settle `dispatchFrom` reverts `StaleOracle` (`|currentTick-twapTick|>tickDeltaLiquidation=513`, L1386-1389); ~30 `vm.warp(≥120s)`+`pokeOracle()` steps (clamped ±149/step, MAX_CLAMP_DELTA) converge `twapTick` UP to `currentTick` WITHOUT moving price (fees stay accrued). (2) **vm.store both-CT collateral-shrink** for liquidation insolvency — a single large adverse swap trips `StaleOracle` (L1388) before liquidation is reachable, so instead shrink the wrapper's CollateralTracker SHARE balance on BOTH `ct0` AND `ct1` (one-sided may not flip `solvent==0` at all 4 ticks) at `keccak(wrapper, BAL_SLOT)` where `BAL_SLOT=1` (DERIVED via `forge inspect CollateralTracker storage-layout`: `_internalSupply` slot 0, `balanceOf` slot 1 — NEVER slot 0; probe-and-verified) → `convertToAssets(balanceOf)≈0` at all 4 ticks with zero price movement. Trees committed FIRST (Iron-Law ancestry: `2c0ef11` trees → `cfea71a` settleLong+forceExercise tests → `53adf7d` liquidation test). Swap-seam grep-guard (`panoptic-borrowed`==0) + P1 streamia-constant guard==0 on all three files; `bulloak check` co-location clean (the `IPokeOracle` helper interface declared AFTER the test contract so bulloak anchors the root correctly); `forge build` exit 0; close 6/6 + claimResidual 7/7 + streamia 6/6 + open 5/5 NOT regressed. **WRAP-03 marked complete in REQUIREMENTS.md** (all three branches genuinely fork-proven). **Deviations (6, all auto-fixed):** (1) Rule-1 — forceExercise out-of-range precondition over-specified (the disambiguation gates only on solvent + `finalLen==toLen-1` + `validateIsExercisable()!=0`, which is 1 for any long with width; "out-of-range" only scales the exercise FEE), driving price past the chunk tripped `PriceImpactTooLarge`+`StaleOracle` → open at-price, assert the genuine gate. (2) Rule-3 — `StaleOracle` on the settle dispatchFrom after the up-swap → pokeOracle TWAP convergence (the 08-05 `_restorePrice` targets a different gate and was ineffective here). (3) Rule-3 — forceExercise refund leg `NotEnoughTokens` (exercisor had no CT account) → fund the exercisor. (4) Rule-1 — dispatchFrom `positionIdListFrom` must be the CALLER's OWN list (tail `_validateSolvency(msg.sender, …)`); an exercisor/liquidator that owns nothing passes an EMPTY list (else `InputListFail` L1859). (5) Rule-1 — `ResidualEroded` made conditional for forceExercise (an at-price OTM debits ~0; delegate/revoke can leave surviving marginally higher). (6) Rule-3 — bulloak co-location broke when `IPokeOracle` was declared BEFORE the test contract → moved it after. **Metric (08-06):** 3 tasks, 6 files, ~19 min.

- **2026-06-02 (08-07):** Two named fuzz invariants SHIPPED — Phase 8 plans 7/7 complete. `LongGammaWrapper.invariants.t.sol` (1 file created) is the full-stem `.t.sol` satisfying the Plan-01 `invariants.tree` (root `LongGammaWrapperInvariants`), completing the **8-canonical-tree set** (open/streamia/close/forceExercise/settleLong/liquidation/claimResidual/invariants). A bounded `LongGammaWrapperHandler` (`act_open`/`act_swap`/`act_sync`/`act_close`/`act_claim`, each `bound()`+state-guarded so the fuzzer never aborts a run) drives the lifecycle and stores `address(this)` as the wrapper's `user` — **load-bearing**, else `close()`'s `if (msg.sender != user) revert NotUser();` blocks the close path and the invariants go vacuous. It maintains TWO INDEPENDENT claim-time ledgers from live reads (never the deposit `a0/a1`): `preClaimSurviving_i` (live `convertToAssets(balanceOf(wrapper))` snapshotted just before the redeem) + `lastPaid_i` (the realized user-balance delta), plus their sums. **`invariant_residualNeverExceedsHoldings`** (SINGLE claim): `assertLe(lastPaid_i, preClaimSurviving_i+1)`. **`invariant_userClaimsBackedByCollateral`** (CUMULATIVE, NON-inverted): `assertLe(totalPaid_i, cumPreClaimSurviving_i+1)` — TRUE on fuzz step 1 (`0<=0+1`), NEVER the deposit-as-backing form (B1) nor the `max(surv-cost,0)<=surv` tautology (W4). Both pass on the live Base fork at the **additive `[invariant]` floor (runs=16/depth=16, fail_on_revert=false)** with **0 reverts** (`act_open` 49 / `act_close` 47 / `act_claim` 64 — lifecycle genuinely exercised). **NON-vacuity PROVEN by mutation:** zeroing the backing accumulator while the payout accumulates the real positive figure FAILS the unchanged assertion (`9.99e17 > 1` cumulative; `4.4e28 > 1` single-claim) — both restored to green. A live diagnostic confirmed a positive `~1.24e30` payout lands with `totalPaid == preClaimSurviving` exactly (tightest bound). Seam grep-guard (`panoptic-borrowed`==0) + P1 (`SPREAD_MULTIPLIER`/`perBlock`/`streamiaPerBlock`/`VEGOID`==0) + inverted-backing guard (`promised|>= a0|>= deposit|liveSurv...>=`==0) + tautology guard (`owed0<=surv0|max(surv`==0) all hold; bulloak co-location clean; 30/30 existing wrapper tests un-regressed. Iron Law preserved (tree from 08-01 pre-dates this `.t.sol`). Commit `2c5b5ef`. **Deviations (3):** (1) Rule-1 — `act_open` bound raised from the plan's `[1e6,1e14]` to the OVER-FUNDED `[1e18, type(uint104).max]`: at ~2e6 collateral the 1-ether long mint underflows (0x11) in the SFPM solvency math, so `act_open` always reverts and the invariants go VACUOUS (the tell: a zeroed-backing mutant PASSED until the bound was fixed). (2) Rule-3 — `@types` NatSpec literal trips solc 6546 + `max(surv` literal trips the tautology grep-guard → reworded (same 08-01/02/06 precedent). (3) Rule-3 — bulloak anchors the tree root on the FIRST contract, so `LongGammaWrapperInvariants` declared BEFORE `LongGammaWrapperHandler` (08-06 `IPokeOracle` precedent). **RPC note:** rapid successive fork runs 429'd (Alchemy rate-limit); each invariant run separately with short backoff clears it. **Metric (08-07):** 1 task, 2 files, ~13 min.

## Phase 16 — Shiller-differentiated representativeness (POST-MVP, side-track)

> Phase 16 adds a SECOND geometry branch (SHILLER surprise-driven convex) beside the fork-proven PKE path. The `progress:` frontmatter tracks the v2.0 convex-instrument phases (7–10); Phase 16 progress is tracked here and in ROADMAP.md. **Wave 1 = 16-01 (the pure fork-free substrate); Wave 2 = 16-02 (the `resolveFromMandate` SHILLER branch, consumes this); Wave 3 = 16-03 (the MacroWorkflow whole-workflow JOIN, consumes the branched executor).** Phase 16 COMPLETE 3/3 (SHILLER-01 + SHILLER-02 fork-proven) — goes to the phase-goal verifier.

- **2026-06-07 (16-01):** Wave-1 substrate SHIPPED — the pure, fork-free half of SHILLER-01 (lib fns + oracle + ctor; the branch itself is 16-02). `contracts/src/interfaces/ISurpriseOracle.sol` (new): `latestSurprise() → (int256 actualWad, int256 consensusWad, uint256 sigmaWad, uint64 observedAt)` mirroring `IRegimeOracle`'s `observedAt==0`-unset staleness contract. `contracts/test/mocks/MockSurpriseOracle.sol` (new): `set(actual,consensus,sigma)` (fresh observedAt) + `setStaleAt(...,ts)` (0=unset, old=stale), mirroring `MockRegimeOracle`. `contracts/src/libraries/Representativeness.sol` (ADDITIVE — PKE region byte-identical): four pure SHILLER fns + `shillerStale` + six TEMPLATE constants — `shillerSurprise = (actual−consensus)·WAD/σ` (signed WAD); the CONVEX `shillerOptionRatio = clamp(SCALE·max(|s|−k,0)²/WAD², 1, 127)` (the load-bearing differentiator from PKE's LINEAR notional size; UNIT CARE /WAD/WAD); the SIGN-driven further-OTM `shillerStrikeTick` (base 15% + 5%/σ, REUSES `structuralStrikeTick` — no hand-rolled snap); the `|s|`-scaled `shillerWidth` (REUSES `VolToWidthLib.volToWidth` even-snap); `shillerStale` (35-day monthly-CPI window, distinct from PKE's 1h — open-Q2 resolved). **Exact asserted outputs:** ratios s=1σ→2 / 1.5σ→10 / 2σ→22 / 2.5σ→40 / 3σ→62 / 3.5σ→90 / 4σ→122 (≤0.5σ→1, ≤127); strikes (×60-aligned) +1σ→360780 / +2σ→361200 / +3σ→361620 / −1σ→356760 / −2σ→356100 (upside > PKE anchor 360360, downside <); width even-snapped + STRICT `width(2σ)>width(0)`; staleness BOTH unset (observedAt==0) AND aged (ts<now−35d) → s=0 minimal stance. **Atomic 10-arg ctor migration** (ONE compiling commit): `ISurpriseOracle surpriseOracle` immutable appended after `regimeOracle`, migrated across **6 sites** (5 enumerated + a 6th `script/ProvisionBuildBearDemo.s.sol` added by Phase-15 after this plan was written — the documented Rule-3 deviation); fork sites pass a live `MockSurpriseOracle` (reused via a shared field on the two under-funded twins), the onResult DecodeProbe passes `ISurpriseOracle(address(0))` (never runs the mandate path). **evm-TDD Iron Law honored:** the `.tree` (bulloak 0.9.2 when/it, 5 leaves) + the failing unit suite committed FIRST as a SEPARATE RED commit (`0b89a1f`) before the GREEN impl (`e1b1ab5`); `git merge-base --is-ancestor 0b89a1f e1b1ab5` → true, and the RED commit touched NO lib/executor impl. **Proven green:** Shiller unit **10/10** (5 bulloak-anchored leaves + 5 assertion fns), PKE `Representativeness.t.sol` **17/17 un-regressed** (PKE region byte-identical), `onResult` **4/4** (decode-isolation intact through the 10-arg DecodeProbe), `forge build` exit 0 project-wide. **Cosmetic notes (gate):** strike σ-quantization — `|s|/WAD` floors to whole σ so +3σ/+3.5σ share tick 361620 (the convex SIZE 62 vs 90 differentiates them); `regimeZt=0` on the SHILLER path is a documented N/A (`0==Regime.Unknown`, the SHILLER arm doesn't read the regime). Commits `1856921` (Task 1) → `0b89a1f` (RED) → `e1b1ab5` (GREEN). **Continuation:** all three task commits + an on-disk SUMMARY pre-existed from a prior session that died before STATE/ROADMAP (the documented socket-death pattern); this run independently re-verified EVERY acceptance gate (Iron-Law ancestry, build, all three suites, exact assertions, PKE-byte-identical, the 6 ctor sites) before completing the docs — no rework, no faked-green. **SHILLER-01 stays Pending** in REQUIREMENTS (the multi-plan req needs the 16-02 branch; reverted a premature mark-complete to keep the checkbox honest). **Deviations (1, auto-fixed prior session):** Rule-3 — the 6th ctor site in `ProvisionBuildBearDemo.s.sol` (folded into the atomic GREEN commit). **Metric (16-01):** 3 tasks (TDD), 10 files, ~9 min (continuation re-verify).
- **2026-06-07 (16-02):** Wave-2 SHIPPED — **SHILLER-01 COMPLETE**; `resolveFromMandate` now BRANCHES on the economic school, fork-proven on the live Polygon fork. `contracts/src/MacroHedgeExecutor.sol`: a single `if (address(mandate.economicTheory) == address(uint160(0x5))) return _resolveFromShillerMandate(...);` at the top of `resolveFromMandate` — the SHILLER arm; every other sentinel (incl. POST_KEYNESIAN 0x6) falls through to the EXISTING PKE body **byte-identical** (the if-arm returns, so the PKE lines are untouched, no else-brace wrapping). `_resolveFromShillerMandate` (new internal, extracted to keep the PKE frame byte-identical AND dodge stack-too-deep): reads `surpriseOracle.latestSurprise()` with the `shillerStale`→s=0 minimal-stance fail-safe (35-day window; never calls `shillerSurprise` when stale so σ>0 holds), derives the CONVEX size (`shillerOptionRatio`), the SIGN-driven further-OTM strike (`shillerStrikeTick`, reusing the Fix-C snap), and the |s|-scaled even-snapped width (`shillerWidth`), then emits the 8-param `ExecutorDecided` with `regimeZt=0` (N/A — reads the surprise oracle, not the regime), |s| in the inflationAdjustment slot, `nonErgodicDisclosed=true`, and the new `SHILLER_RATIONALE` TEMPLATE constant (carries "Shiller" + "UNVALIDATED"), minting through the SHARED unchanged Fix-C `_resolveAndMintAtStrike` sink. **open-Q3 RESOLVED by LIVE FORK EVIDENCE:** path A (the s<0 K_lo appreciation strike 356100, BELOW spot) was tried first per the plan and **panicked arithmetic-underflow (0x11) inside Panoptic `dispatch`** (the appreciation leg-upper crosses spot on the opposite side of the proven K_hi clearance, RESEARCH Pitfall 8) → the impl switched to **depreciation-only-v1** (`if (s < 0) s = 0;` ⇒ s<0 collapses to the K_hi minimal stance 360360); the two-sided Carr-Madan strip is the documented deferred-v2 stretch (the 356100 constant retained as the v2 target). **evm-TDD Iron Law:** the three SHILLER fork tests + the bulloak `test_When*` delegating leaves + the `.tree` committed FIRST as a SEPARATE RED commit (`2d5ffe0`, RED-verified failing on the live fork — SHILLER mandate hit the PKE path → minted 360360 not the SHILLER strikes) before the GREEN branch (`2e42f12`); `git merge-base --is-ancestor 2d5ffe0 2e42f12` → true. **Fork-proven:** `test_branch_shillerDiffersFromPke` (SHILLER +2σ mints EXACT 361200 ≠ PKE 360360, executor owns the leg), `test_executorDecided_perSchoolHonesty` (SHILLER rationale Shiller+UNVALIDATED, PKE post-Keynesian, BOTH nonErgodic), `test_branch_shillerDownsideFork` (s=−2σ → 360360 minimal stance), `test_resolveFromMandate_mintsThroughExecutor` STILL mints EXACT 360360 (the byte-identical PKE regression anchor). **Proven green:** demo fork **13/13** on the live Polygon fork (4 SHILLER/PKE-anchor + 3 bulloak leaves + 6 Phase-13/14 demo — no regression), `Representativeness.t.sol` **17/17 un-regressed** (PKE lib byte-identical), `onResult` **4/4** (decode-isolation intact), `forge build` exit 0 project-wide. **Note (bulloak):** `bulloak check` on the fork `.tree` WARNS — it anchors the root on the FIRST contract (`ICollateralDeposit` helper) which must precede the test contract by Solidity ordering; the Task-1 verify gates on the three `--match-test` name greps (all pass) + the three named `test_When*` leaves run green (the 14-03 "this fork file has no .tree" reality — the tree is best-effort BTT docs here). **Deviations (2, both Rule-3 blocking):** (1) `_decodeExecutorDecided` tuple-destructure arity mismatch (6 skip-slots for the 7-tuple non-indexed tail) → added the missing `width` skip slot (folded into the RED commit before it was committed); (2) stack-too-deep when the SHILLER arm was inlined → extracted `_resolveFromShillerMandate` (cleaner + byte-identity, the GREEN commit). Commits `2d5ffe0` (RED) → `2e42f12` (GREEN). **Metric (16-02):** 3 tasks (TDD), 3 files, ~12 min.
- **2026-06-07 (16-03):** Wave-3 SHIPPED — **SHILLER-02 COMPLETE; Phase 16 done 3/3.** The agent-interaction-layer differentiation proof (the layer just below the UI). `contracts/test/instrument/MacroWorkflow.fork.t.sol` (new) + `MacroWorkflow.tree` (new): a single fork test contract runs **Agent-1** (`MacroHedgeStrategist` + `MockPlatform`, in-VM — the LLM legs are fork-free) to assemble a `HedgeMandate` (school `inferString` → `0x5`/`0x6` sentinel, notional `inferNumber` → 50_000), bridges it via `getMandate`, then drives **Agent-2** (`MacroHedgeExecutor.resolveFromMandate`) to mint on the live Polygon fork. **4 Colombian macro-risk scenarios × 2 schools, 6/6 green:** CPI upside (+2σ → SHILLER 361200/size22 vs PKE 360360/size50), CPI downside (−2σ → SHILLER 360360/size1 depreciation-only-v1 vs PKE 360360/size50), fiscal-slippage tail (+3.5σ → SHILLER 361620/size90), carry-unwind (+3σ → SHILLER 361620/size62). **`test_workflow_sameInputDifferentGeometry`** asserts strike OR size differs per school for every scenario + both mint. **ANTI-TAUTOLOGY (the load-bearing point — a stubbed school-keyed constant would FAIL):** (a) INTRA-SCHOOL SIZE monotonicity — +3σ and +3.5σ floor to the SAME tick 361620 so the proof uses SIZE: `assertGt(sSize35, sSize3)` → 90 > 62; (b) FLIP-ONLY-THE-SENTINEL — `_flipSentinelRun` seeds IDENTICAL +2σ surprise + STRESS regime oracles and flips ONLY `economicTheory` 0x5↔0x6 on an otherwise byte-identical mandate; the geometry STILL differs (SHILLER 361200 vs PKE 360360) → the SCHOOL branch drives it, not the oracle. Per-school TEMPLATE honesty on `ExecutorDecided` (Shiller/UNVALIDATED vs post-Keynesian, both `nonErgodicDisclosed==true`). **evm-TDD Iron Law:** the tree + suite committed FIRST as a SEPARATE RED commit (`83f7fca`) before the GREEN integration wiring (`2d83a5c`); `git merge-base --is-ancestor 83f7fca 2d83a5c` → true. **Proven green:** MacroWorkflow **6/6** on the live Polygon fork (3 VALIDATION tests + 3 bulloak-anchored delegating leaves); PKE anchor `test_resolveFromMandate_mintsThroughExecutor` STILL 360360; `Representativeness` **17/17**; `ShillerRepresentativeness` **10/10**; `onResult` **4/4**; demo fork **13/13** (16-02 SHILLER tests un-regressed); `forge build` exit 0 project-wide. **Deviations (3, all auto-fixed):** (1) Rule-3 — `_init_world()` was never called (pool `0x0` → `collateralToken0()` on a non-contract); hoisted to ONCE per test (a double-call reverts `AlreadyInitialized`), each `_runWorkflow` run deploys its OWN funded executor against the shared pool (the demo per-test pool-isolation pattern). (2) Rule-3 — non-ASCII σ in assertion-message string literals (solc 8936) → ASCII-only. (3) Rule-1 — prior-session draft had a dead tuple-assignment + an `IMacroThesisLike` cast that could not assign to the `IMacroThesis economicTheory` field → plain `uint256 sSizeD = 1` + import the real `IMacroThesis` (folded into the RED commit). **Note (bulloak):** `bulloak check MacroWorkflow.tree` WARNS (the `.fork.t.sol` suffix is load-bearing for the 15-02 CI `*fork*` match-path gate; renaming to `.t.sol` would break CI) — the 3 derived leaf names verified via `bulloak scaffold`, all run green (same 16-02 documented reality). **Continuation:** both files pre-existed on disk (uncommitted) from a prior session that died before the fork run/docs; this run fixed the compile/integration blockers, RED→GREEN split them, and ran every gate. Commits `83f7fca` (RED) → `2d83a5c` (GREEN). **Metric (16-03):** 2 tasks (TDD), 2 files, ~14 min.

## Phase 15 — Cornerstone E2E (UI → contracts) + CI (the deliverable, Agentathon cornerstone)

> Phase 15 is the integrating ribbon — NO new contracts. Wave 1 = three disjoint-file plans in parallel: 15-01 (contract-leg packaging + video checkpoint), 15-02 (polygon-fork CI gate, THIS entry), 15-03 (UI-AGENT-HANDOFF docs refresh). The `progress:` frontmatter tracks the v2.0 convex-instrument phases (7–10); Phase 15 progress is tracked here and in ROADMAP.md.

- **2026-06-07 (15-02):** Polygon-fork CI gate SHIPPED — E2E-02 satisfied. `.github/workflows/contracts-ci.yml` gains a `polygon` job (placed after `fork`, before `somnia-e2e`) that gates the repo on the Polygon-fork mint suite, mirroring the proven base `fork` job's cache+retry+secret-skip posture but keyed on **`ALCHEMY_API_KEY`** (the var `foundry.toml`'s `polygon` rpc_endpoint + the fork tests read via `vm.envString` — gating on `BASE_RPC_URL` would silently skip every Polygon test) and the **`foundry-rpc-polygon-86900000-v1`** cache (pinned block 86_900_000). Run step: two `::warning::` lines + `exit 0` when the secret is unset, else `forge test --match-path 'test/**/*fork*' --retries 2 --delay 3 -vvv`. NO invented `--shard` flag (Foundry has none — the pinned-block RPC cache + retries are the honest 429 defense, documented in the job comment as "test-sharding flag"). The keyless `build-and-spec` job (`forge build` + per-file bulloak loop + fork-excluded keyless suite) and the `workflow_dispatch`-only `somnia-e2e` (live STT) stay BYTE-unchanged. **Keyless gate proven un-regressed locally:** `forge build` exit 0, per-file `bulloak check` over `test/fork/*.tree test/instrument/*.tree` all clean (17 trees), `forge test --no-match-path 'test/**/*fork*'` = **118 passed / 0 failed across 15 suites** with no RPC secret. YAML parses; `jobs` = {build-and-spec, fork, polygon, somnia-e2e}. **Continuation:** Task 1's commit (`7a413db`) + the on-disk SUMMARY pre-existed from a prior session that died before STATE/ROADMAP (the documented socket-death pattern); this run independently re-verified every Task-1 AC + ran every Task-2 keyless gate before updating docs — no rework, no edits. **Human follow-up (user_setup):** the maintainer adds the `ALCHEMY_API_KEY` repo secret (Settings → Secrets and variables → Actions) for the job to run green on a runner; until then it skips with a warning by design. Commit `7a413db` (feat, Task 1). **Deviations (0 this run):** the SUMMARY records one prior-session Rule-3 auto-fix (reworded the `--shard` comment tokens in the new job so the AC holds; base job's line-112 comment left intact). **Metric (15-02):** 2 tasks (1 code-committed prior session, 1 regression-verify this session), 1 file, ~6 min (continuation).

## Phase 14 — Representativeness derivation (Agent 2 brain) (Agentathon cornerstone, side-track)

> Phase 14 is the genuinely-new representativeness math of the cornerstone arc (ROADMAP § Phase 14):
> the PKE regime-conditional asymmetric passthrough core that turns a `HedgeMandate` into geometry,
> reusing the shipped Phase-13 mint sink. The `progress:` frontmatter block tracks the v2.0
> convex-instrument phases (7–10) and is intentionally left at its prior value; Phase 14 progress is
> tracked here and in ROADMAP.md. **Wave 1 = 14-01 (the pure substrate, this entry); Wave 2 = 14-02
> (executor wiring) imports it; Wave 3 = 14-03 (fork mint).**

- **2026-06-07 (14-01):** Wave-1 substrate SHIPPED — the deterministic representativeness core, fork-free + mutation-proven (REPR-01 + REPR-02 unit halves; 17/17 green). `contracts/src/libraries/Representativeness.sol` (new, pure): the PKE regime-conditional ASYMMETRIC passthrough `inflationAdjustment(β₁,dev) = β₁·dev/WAD`, the fail-safe `effectiveRegime` (stale/unset/Unknown ⇒ STRESS, §3.6), the TICK-SPACE `regimeVol`/`regimeWidth` (STRESS scales baseVol up by the asymmetric excess `M(β₁stress)−M(β₁tranquil)`; TRANQUIL == the GBM baseline by construction) + the `gbmBaselineWidth` comparator (the ≥1-param-≠-GBM binding constraint), **Fix-C `structuralStrikeTick`** (the decimal-gap-correct pre-snapped `int24` = `snap(getTickAtSqrtPrice(sqrt(humanRate·1e12·Q192)))`, ZERO inversion — the broken WAD-to-sink inversion is DELETED, absent from every file this plan touches), the DEFINED monotone `feasibleOptionRatio = clamp(notional/NOTIONAL_PER_RATIO,1,127)` (NOTIONAL_PER_RATIO=1_000 TEMPLATE), and the honesty output (`nonErgodicDisclosed`==true + the `TEMPLATE_RATIONALE` marker). `contracts/src/interfaces/IRegimeOracle.sol` (new): `Regime{Unknown,Tranquil,Stress}` (Unknown==0 default) + `latestRegime() → (Regime, uint64 observedAt)`, mirroring the `MacroOracle.deliveredAt==0 ⇒ unset` precedent. `contracts/test/mocks/MockRegimeOracle.sol` (new, the gate-corrected `test/mocks/` location alongside MockMacroOracle/MockPlatform/MockCcop): settable double (`set` seeds a non-zero observedAt; `setStaleAt` pins an explicit ts). **The Fix-C ticks were verified to the wei BEFORE any assertion was baked** (throwaway forge probe against the REAL TickMath+FixedPointMathLib): `structuralStrikeTick(4485,60)==360360`, `(3900,60)==358980`, `(4095,60)==359460` — exactly matching RESEARCH, so the exact-equality canary (NOT a band) GREENs on Fix C and would RED on the deleted inversion (361680 at 4485, off by 1320). **The β₁-collapse mutation was proven non-vacuous then restored** (the prior session left the mutant ACTIVE — both `stressCap`/`tranquilCap` read `beta1TranquilWad` — which is exactly why the on-disk suite was 13/17 on resume): CONTINUOUS `test_regimeVol_stressExceedsBaseline` FAILS `14400 <= 14400` and QUANTIZED `test_regimeWidth_differsFromGbmBaseline` FAILS (wStress==wGbm==20) under the collapse; after restoring `beta1StressWad`, regimeVol(STRESS)=15156>14400 and wStress=21≠wGbm=20. **bulloak-vs-VALIDATION naming resolved by delegation** (verified, in-repo-precedented): bulloak 0.9.2 `check` derives the required `test_When*/test_Given*` name from the `when`/`given` clause AND tolerates ADDITIONAL functions, so each anchored leaf delegates to a VALIDATION-`--match-test`-named function carrying the assertions — `bulloak check` exits 0 AND every `--match-test` string (`test_structuralStrike_exactAt4485`, `test_regimeVol_stressExceedsBaseline`, `test_regimeWidth_differsFromGbmBaseline`, `test_beta1_stressExceedsTranquil`, `test_staleOracle_defaultsToStress`, `test_feasibleOptionRatio_monotoneFromNotional`, `test_resolveFromMandate_derivesExpectedGeometry`, `test_mutation_collapseBeta1_breaksNonVacuity`) resolves. **Whole fork-free tree green: 114/114 across 14 suites** (Representativeness 17/17 + PolygonPools 3/3 + MacroHedgeExecutor onResult 4/4 + OperationalCostManagement 10/10 + MacroHedgeStrategist 19/19 — no sibling regressions); `forge build` exit 0. **NO MacroHedgeExecutor/PriceGrids file touched** (per the coordination note — the Fix-C sink split + ctor 4→9 + Phase-13 probe/fork migration is Plan 02's). Iron-Law ancestry `ac1a6e1` (tree) → `e5254cb` (RED, lists tree+.t.sol+interface+mock, NOT impl) → `04471fa` (GREEN impl, strict descendant). **Continuation:** the two RED commits + an UNTRACKED on-disk impl pre-existed from a prior session that died before GREEN/docs (the documented 08-03/12-01 socket-death pattern); this run re-verified EVERY Task-1 gate before completing Task 2 (restore→GREEN→commit) — no rework, no faked-green. **Deviations (2, both auto-fixed):** (1) Rule-1 — restored the left-active β₁-collapse mutant in `regimeVol` (line 90 `beta1TranquilWad`→`beta1StressWad`); this IS the Task-2 GREEN step (mutation already proven, only the restore was missing). (2) Rule-3 — reworded two NatSpec comments to drop the literal `strikeWadForSink`/`exchangeRateToSqrtPriceX96` tokens that false-failed the `! grep -q` Fix-C gates (the 08-01/12-01 NatSpec-vs-grep precedent). **Metric (14-01):** 2 tasks (TDD; RED prior session, GREEN this session), 5 files (all created), ~4 min (continuation).
- **2026-06-07 (14-02):** Wave-2 executor wiring SHIPPED — the additive Agent-2 front-end + the Fix-C sink split, in ONE compiling commit (REPR-01 + REPR-02 derivation/surfacing halves on the deployable contract). `contracts/src/MacroHedgeExecutor.sol`: (1) **Fix-C sink split** — `_resolveAndMint(HedgeLegParams,uint256,uint128,uint256)` renamed to `_resolveAndMintAtStrike(...,int24 strike)` with the mint BODY (the require guards, width/asset/riskPartner, pid/addLeg, the short-then-long two-`pool.dispatch` block, the two emits) lifted BYTE-FOR-BYTE verbatim; the `strikeWAD→tick` line (`exchangeRateToSqrtPriceX96(legParams.strikeWAD)`) moved UP into the public `resolveAndMint` AND `_onResult` so the Phase-13 demo/direct path is byte-unchanged (dispatch count stays 2, the `optionRatio<=127` guard stays the sink's first statement). (2) **additive `resolveFromMandate(HedgeMandate,legIndex,positionSize)`** — reads Z_t via `regimeOracle.latestRegime()` with the staleness→STRESS `effectiveRegime` fail-safe, computes `inflationAdjustment` + the regime-conditional `regimeWidth`/`regimeVol`, maps `targetNotional`→`feasibleOptionRatio`<=127, builds a well-formed `HedgeLegParams` (`strikeWAD` UNUSED→0), surfaces the 8-param `ExecutorDecided` (the honesty flag `nonErgodicDisclosed==true` + the `TEMPLATE_RATIONALE` caveat), and mints through the shared sink passing the EXACT pre-snapped K_hi tick `structuralStrikeTick(CANONICAL_COP_USD*115/100, 60)==360360` directly (ZERO inversion; `strikeWadForSink` ABSENT everywhere; NO dead `getCurrentTick` read). (3) **ctor 4→9** (Pattern 3): `beta1TranquilWad`/`beta1StressWad` immutable pair (TEMPLATE 0.10e18/0.35e18), `targetDevaluationWad` (0.15e18), `baseVol` (TICK-SPACE 14_400 — NO `baseVolWad`), `IRegimeOracle regimeOracle` — the exact 9-arg order Plan-03's `_deployExecutorWith` already uses. **The breaking fan-out CLOSED in the SAME commit** (the gate's two prior-cycle BLOCKERs): `contracts/test/instrument/MacroHedgeExecutor.onResult.t.sol` — the `MacroHedgeExecutorDecodeProbe` override migrated to `_resolveAndMintAtStrike(HedgeLegParams,uint256,uint128,uint256,int24) internal override` (decode-isolation preserved — the rerouted `_onResult` call lands on it, records the decoded params, ignores the strike, skips dispatch) + its ctor forwards the 9-arg base ctor (template β₁/dev/baseVol + a zero-address `IRegimeOracle`, never read on the decode path); `contracts/test/fork/MacroHedgeExecutor.fork.t.sol` — all THREE `new MacroHedgeExecutor(...)` sites (`_init_world`, the BTT-leaf under-funded twin, `test_margin`) migrated to the 9-arg ctor with a shared contract-field `MockRegimeOracle` (import `../mocks/MockRegimeOracle.sol`, set to Stress). **Proven green:** `forge build` exit 0 PROJECT-WIDE (only the 3 edited files recompiled — no intermediate red build); keyless onResult **4/4** (decode-isolation intact) + Representativeness **17/17** (consumes only Plan-01's locked lib surface — no new helper); the migrated fork EXEC suite **7/7 on the live Polygon fork** (block 86_900_000 — `test__takeDemoPosition__Succeeds`, the AccountInsolvent atomic gate, the size guard all mint through the 9-arg ctor sites); full fork-free regression **114/114** (no sibling regressions — PolygonPools 3/3, OperationalCostManagement 10/10, MacroHedgeStrategist 19/19). Commit `135f378` (feat, all 3 files atomic). **Deviations (1, auto-fixed):** Rule-3 — reworded the `resolveFromMandate` NatSpec to drop the literal `getCurrentTick` token (no `getCurrentTick` CODE exists; the comment NAMED it to explain its deliberate absence, false-failing the `! grep -q "getCurrentTick"` gate — the 08-01/12-01/14-01 NatSpec-vs-grep precedent). **Metric (14-02):** 1 task (type=auto, atomic 3-file commit), 3 files (all modified), ~13 min.
- **2026-06-07 (14-03):** Wave-3 fork mint SHIPPED — **Phase 14 COMPLETE (3/3)**; `resolveFromMandate` FORK-PROVEN end-to-end on the live Polygon fork (REPR-01 surfacing + REPR-02 integration halves, the cornerstone Agent-2 brain now mints a real position from a mandate). `contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol` (+219, TEST): the four Wave-3 tests reuse `_init_world` verbatim and wire the 9-arg executor (`receiver=address(exec)`, TICK-SPACE baseVol 14_400, STRESS oracle). **`test_resolveFromMandate_mintsThroughExecutor`** — a `HedgeMandate{POST_KEYNESIAN sentinel 0x6, wcopUsdcKey.toId(), targetNotional 50_000, 137, isLong}` flows through `resolveFromMandate` → the deterministic regime-conditional geometry → the Fix-C sink → a real wCOP/USDC Panoptic position; the mint SUCCEEDS, `positionId.strike(0) == 360360` EXACT (the pre-snapped structural K_hi tick — minted==emitted==asserted, zero drift, NOT a band), `countLegs()==1`, `isLong(0)==1`, `numberOfLegs(address(exec))>0` (executor-owned). **`test_executorDecided_surfacesHonestyFlag`** — decodes the 8-param `ExecutorDecided(uint256,uint8,uint256,int24,int24,bool,bool,string)` from `vm.getRecordedLogs()`, asserts `nonErgodicDisclosed==true` + `bytes(rationale)` contains `"TEMPLATE"` (the Davidson honesty split surfaces for the UI). **`test_resolveFromMandate_llmIndependentGeometry`** — deploys the executor with a `MockRevertingPlatform` (every external fn reverts) and `resolveFromMandate` STILL mints the identical geometry (strike 360360) — BEHAVIORAL LLM-independence (a geometry touching the agent surface would revert; it does not). **`test_resolveAndMint_sizeOver127_reverts`** — the direct-path `resolveAndMint(size=128)` reverts `"optionRatio overflow"` (the shared sink's %128 guard; the deliberate twin of the EXEC-file `test_resolveAndMint_sizeGuard`). **THE BLOCKER + FIX (the resolved Rule-4 checkpoint):** the prior session's fork suite RED'd `InvalidTickBound()` on the 3 mandate-path tests — under STRESS `regimeVol=14_940 → volToWidth(14_940,100,60)=21` (ODD) → a symmetric leg `strike ± width*tickSpacing/2` is tick-misaligned (`360360−630=359730`, `%60=30≠0`) at `SemiFungiblePositionManagerV4:833`; the Phase-13 demo never hit it (only ever vol 14_400 → width 20 EVEN). The prior executor correctly escalated it as a Rule-4 architectural checkpoint (shipped Wave-1 src, a shared library every Panoptic leg flows through). User chose fix **"A + two-reviewer gate"**; the patch PASSED the CLAUDE.md gate (Reality Checker PASS + Solidity Smart Contract Engineer PASS). **The fix** (`contracts/src/libraries/VolToWidth.sol`, +7): after the `[1,4095]` clamp, `if ((raw & 1) == 1) raw = raw < 4095 ? raw + 1 : raw - 1;` snaps odd widths to even (DOWN only at the ceiling) so Panoptic's asymmetric `getRangesFromStrike` collapses both ranges to `(width/2)*tickSpacing` — an exact tickSpacing multiple → bounds stay aligned. STRESS width `21→22`, bounds `359700/361020` (both `%60=0`) → mints at `360360`. **DECISION — the even-width invariant is a DOCUMENTED property of `volToWidthLib`** (comment block VolToWidth.sol:26-31; even width is sufficient on EVERY tickSpacing, snap chosen at the single chokepoint over `RepresentativenessLib.regimeWidth` so ALL callers are protected). **evm-TDD Iron Law honored with a REAL RED→GREEN split despite the gate-passed fix being on disk:** the failing fork test committed FIRST with the fix stashed (`f92b0f7` test — RED-verified: 3 mandate-path tests fail `InvalidTickBound()` at ~12M gas, NOT a compile error/different revert; the size-guard + 2 demo tests pass), then the fix restored + committed SEPARATELY (`e686d4d` fix — GREEN); `git merge-base --is-ancestor f92b0f7 e686d4d` → true. **Proven green:** demo fork **6/6** on the live Polygon fork (the 4 Wave-3 + the 2 Phase-13 demo, `test__takeDemoPosition__Succeeds` + the vol-aware round-trip STAY green — no Fix-C-split regression); the executor's OWN migrated fork EXEC suite **7/7**; Wave-1 `Representativeness` **17/17** (the `wStress!=wGbm` divergence HOLDS — the snap makes STRESS 22 vs GBM 20, MORE divergent); `onResult` **4/4**; fork-free tree **114/114**; `forge build` exit 0 project-wide. `deferred-items.md` marked RESOLVED (fix commit refs folded in). REPR-01 + REPR-02 carry the 14-03 fork-proof evidence annotations in REQUIREMENTS.md. **Deviations (0):** plan executed as written, as a continuation from the resolved Rule-4 checkpoint (the one permitted src edit IS the gate-passed even-snap; the plan's pre-checkpoint TEST-ONLY `! grep "^src/"` framing is superseded by the resolved checkpoint). The three untracked `src/types/{CalldataReader,OptionType,Underlying}.sol` (separate macro-hedge WIP) left untouched. **Metric (14-03):** 1 task (RED→GREEN evm-TDD split), 2 files (1 test, 1 src), ~4 min (continuation).

## Phase 15 — Cornerstone E2E + CI (Agentathon judges' artifact)

> Phase 15 packages the fork-proven mint into the judges' demo via a HOSTED Polygon fork
> (BuildBear sandbox, chainId 31337) the Vercel UI can reach. Wave 1 = 15-01 (contract leg
> + BuildBear provisioning, this entry, PAUSED at a blocking checkpoint), 15-02 (CI yaml ✅),
> 15-03 (UI-AGENT-HANDOFF docs ✅).

- **2026-06-07 (15-01 — PAUSED at blocking checkpoint T2):** Tasks 1–2 committed; execution STOPPED at the first blocking `checkpoint:human-verify` (T2, the Wave-0 cold-deploy spike). **T1 (auto, tdd):** `test_quoteMargin_basicReadAfterMint` — the E2E-01 in-VM BASIC-read leaf (mint via `resolveFromMandate`, then `exec.quoteMargin(positionId, positionId.strike(0))` returns a `BalanceDelta` without reverting, no magnitude/PnL assertion, `numberOfLegs(exec)>0`; the four existing mandate tests untouched). Committed `42b62d9` (prior session, strict descendant of the fork-proven `f92b0f7`); leaf re-verified present on resume. **T2 build half:** `contracts/script/ProvisionBuildBearDemo.s.sol` `spike()` — INLINES the V4 leg of the `DeployProtocol` body (SFPM/Guardian/BuilderFactory/RiskEngine/CollateralTrackerV2/PanopticPoolV2/PanopticFactoryV4) under ONE broadcast, reads back + `console2.log`s `FACTORY_ADDRESS`/`RISK_ENGINE_ADDRESS`, `require`s code>0; NO nested-broadcast (does not instantiate-and-run the DeployProtocol script object), NO key/RPC literal, V3 leg omitted (V4-only demo). `forge build` exit 0; all T2 automated gates green. Committed `92a7e5d`. **BLOCKER:** the live spike (run `spike()` against a hosted BuildBear sandbox) cannot be performed by Claude — `BUILDBEAR_RPC_URL` + `BUILDBEAR_DEPLOYER_PK` are ABSENT from `contracts/.env` (the env note said pre-staged; they are not present). Surfacing this gap IS part of the spike checkpoint. Tasks 3 (full `run()` + `.env.example`), 4 (the runner), 5 (the video) are GATED on the spike passing. **Deviations (2):** Rule-3 reworded the spike header to clear the `! grep 'new DeployProtocol'` gate (NatSpec-vs-grep precedent); scope decision to inline only the V4 leg. **Metric (15-01, partial):** 2/5 tasks, 2 files, paused at checkpoint.

## Phase 13 — MacroHedgeExecutor (Agent 2) mint (Agentathon cornerstone, side-track)

> Phase 13 is the deployable-mint ribbon of the cornerstone arc (ROADMAP § Phase 13). The `progress:`
> frontmatter block tracks the v2.0 convex-instrument phases (7–10) and is intentionally left at its
> prior value; Phase 13 progress is tracked here and in ROADMAP.md. **Wave 1 runs 13-01 + 13-03 in
> parallel** ({PoolId-anchor, cost-ledger}); 13-02 (executor mint) is Wave 2.

- **2026-06-06 (13-01):** Wave-0 substrate SHIPPED — the STRAT-02 pool-id anchor + `IMacroThesis` compile-stub + the Polygon fork-cache (the smallest self-contained unblock for the Plan-02 executor; NO dependency on the executor or the cost ledger). `contracts/src/libraries/PolygonPools.sol` (new): `wcopUsdcKey()` reproduces the demo's runtime key EXACTLY (currency0=USDC `0x3c49…` 6dp, currency1=wCOP `0x8a1D…` 18dp, fee 3000, tickSpacing 60, hookless — `DemoMacroHedgeExecutor.fork.t.sol:215-221`), and `POLYGON_WCOP_USDC_POOL_ID()` returns `wcopUsdcKey().toId()` (a **pure function**, NOT a `constant` state var — `PoolKey`/`PoolId` are not compile-time constant-expressible; RESEARCH §4). Co-located BTT (`PolygonPools.tree` → `PolygonPools.t.sol`, root `PolygonPoolsTestpoolIdAnchor`, bulloak-0.9.2 `when/it` form) proves all four observable claims: currency ordering USDC<wCOP; fee/tickSpacing/hooks; `PoolId.unwrap(constant) == PoolId.unwrap(localKey.toId())` (constant == the demo's runtime key.toId()); and `panopticPoolIdFromUniV4PoolId(constant, 4, 60) == panopticPoolIdFromUniV4PoolId(localKey.toId(), 4, 60)` (sample vegoid 4 per 07-05 §G; mapping pure/deterministic). 3/3 PASS. `IMacroThesis` confirmed compiling as an empty marker (resolver never reads `economicTheory`; concrete shape is Phase-12 STRAT-01) — committed with the substrate so Plan 02 can import it. `foundry.toml` `rpc_storage_caching` chains → `[8453, 137]` (additive; Base retained) for the Pitfall-6 429 defense on Plan-02's Polygon fork reruns. Iron-Law ancestry: `3de3c5e` (tree) → `a91a708` (impl+test) → `ebea287` (foundry.toml). **Deviations (1, auto-fixed):** Rule-3 — `assertLt` has no `address` overload (forge-std ADL Error 9322 on `Currency.unwrap`); cast both unwrapped currencies to `uint160` before `assertLt`. **Parallel-wave note:** a whole-`test/`-tree `forge build`/`forge test` is blocked by the sibling 13-03 agent's in-flight `OperationalCostManagement.t.sol` (untracked, references a not-yet-exposed `cummCostSomi()`); `--no-match-path` only filters execution, not compilation, so this plan's gates were proven in isolation via `FOUNDRY_TEST=test/instrument/PolygonPools.t.sol` (a test-selection env var, NOT a code change) — `forge build` exit 0, `bulloak check` exit 0, 3/3 tests PASS, `grep 137 foundry.toml`. The whole-tree compile re-greens once 13-03 lands its impl (out of scope — coordination note forbids touching the sibling's files). **Metric (13-01):** 3 tasks, 5 files (4 created, 1 modified), ~4 min.
- **2026-06-06 (13-03):** `OperationalCostManagement` cummCost ledger SHIPPED + EXEC-03 satisfied (10/10 green, conservation invariant at the `[invariant]` floor). The empty stub promoted (SPDX MIT, pragma `^0.8.24`) to: `CostLine{agentCostSomi, dataCost}`, `costOf` keyed by `decisionId`, `cummCostSomi`+`cummDataCost` global accumulators, the **canonical 3-arg** `accrueAgentCost(bytes32 decisionId, uint8 leg, uint256 somi)` (the §7 2-arg quote REFINED — `leg` makes per-`(decisionId,leg)` idempotency enforceable: Agent 1 = leg 0, Agent 2 = leg 1, each accrues once under one `decisionId`), `accrueDataCost(bytes32,uint256)` (idempotent per decision), and the `totalCost(bytes32)` exit read. Idempotency = `revert AlreadyAccrued` under the `agentLegAccrued[d][leg]` / `dataAccrued[d]` guards. NatSpec documents the **budgeted-not-realized** SOMI boundary (realized `executionCost` structurally absent on Somnia — memory `project-realized-executioncost-structurally-absent`) + the **no-settlement** (XCHAIN-01 deferred) boundary + per-component units. Co-located BTT (`OperationalCostManagement.tree` → `.t.sol`, root `OperationalCostManagementTest`, bulloak-0.9.2 `when/it` form, exit 0). `invariant_costConserved` (`cummCostSomi == Σ_d costOf[d].agentCostSomi` AND `cummDataCost == Σ_d costOf[d].dataCost`, read from the ACTUAL ledger lines + cross-checked against an INDEPENDENT handler mirror) passes at the additive `[invariant]` floor (runs=16/depth=16, fail_on_revert=false) — **16 runs / 256 calls / 0 reverts**, handler genuinely exercised (`act_accrueAgent` 121 / `act_accrueData` 135). **BOTH non-vacuity mutations proven (the M3 gate finding — TWO DISTINCT mutations, TWO DISTINCT assertions):** (1) CONSERVATION — comment out `costOf[d].agentCostSomi +=` (asymmetric drift, global moves not line) → `invariant_costConserved` FAILS `882397197328767578143505 != 0`; (2) IDEMPOTENCY — drop the `agentLegAccrued[d][leg]` guard (symmetric double-count, BOTH sides → 14) → `invariant_costConserved` STILL PASSES (`14 == 14`, blind to symmetric double-count, verified via a throwaway probe then deleted) but the SEPARATE `test_accrueSameLegTwice_cummCostSomiUnchanged` leaf FAILS `14 != 7`. Each restored to green. PURE in-memory unit/fuzz — NO fork (does NOT 429); reused the `[invariant]` floor already in foundry.toml (did NOT touch it — 13-01 owned it this wave). Iron-Law ancestry: `e3b9dc4` (tree+RED) → `ceb5057` (impl). The whole-`test/`-tree compile (blocked during 13-01 by this plan's then-in-flight `.t.sol`) now re-greens. **Deviations (2, both auto-fixed, both on my own test file):** (1) Rule-1 — the conservation invariant initially compared global-vs-mirror, which mutation-1 did NOT break; rewrote to sum the ACTUAL `Σ_d costOf[d]` ledger lines + retain the mirror cross-check. (2) Rule-1 — an in-invariant-body `assertGt(callCount > 0)` false-failed Foundry's setup-time run-0 evaluation; moved non-vacuity to a dedicated `test_conservationInvariant_isNonVacuous_*` leaf (the proven `LongGammaWrapper.invariants` shape). **Metric (13-03):** 2 tasks (TDD RED+GREEN), 3 files (all created), ~8 min.
- **2026-06-06 (13-02):** Deployable `MacroHedgeExecutor is SomniaAgentConsumer` SHIPPED — EXEC-01 + EXEC-02 satisfied, **Phase 13 Wave 2 complete (the cornerstone mint is now contract-owned)**. The 34-line stub PROMOTED: ctor `(address platform, PanopticPoolV2 _pool, RiskManagement _riskManager, uint8 _vegoid)` (immutables `pool`/`riskManager`/`vegoid`; the unused `ISemiFungiblePositionManager` field dropped; `using TokenIdLibrary for TokenId`), a SHARED internal `_resolveAndMint(HedgeLegParams, legIndex, positionSize, requestId)` sink whose FIRST statement is `require(legParams.size <= 127, "optionRatio overflow")` (the §Pitfall-4 `% 128`-mask guard on the ORIGINAL uint256 size, covering BOTH entrypoints) followed by the demo resolver body migrated VERBATIM (width/asset/riskPartner/strike derivation + `pid = panopticPoolIdFromUniV4PoolId(legParams.underlyingMarket, vegoid, tickSpacing)` + `addLeg` + the short-then-long two-`dispatch` block with `fl=[shortId,positionId]`/`sl=[positionSize*2]` load-bearing), then `emit RepresentativenessAssessed(requestId, stub, true)` + `emit PositionMinted(address(this), positionId, positionSize)`. The external `resolveAndMint` passes `requestId = 0` (the sentinel); `_onResult` FIXED to `abi.decode(responses[0].result, (HedgeLegParams))` (the `abi.encode(response)` type error gone), guards `status == Success`, routes to the same sink with the live requestId. **EXEC-01 fork-proven** through the CONTRACT (`test__takeDemoPosition__Succeeds` lineage): `_init_world` deploys a `MockPlatform(0.01 ether)` for the base ctor (the mint path never touches `PLATFORM`), `new MacroHedgeExecutor(...)`, funds + deposits collateral with **`receiver = address(executor)`** (the §1 ownership shift), DROPS the demo's vestigial `address(this)` deposit — `numberOfLegs(executor) > 0`, both events emitted (requestId == 0). **EXEC-02 HONEST**: (a) post-mint `quoteCollateralRequirements(owner: executor, …)` returns a `BalanceDelta` without reverting (informational); (b) the real gate is the protocol-native atomic `AccountInsolvent` — a SECOND executor funded `DEFAULT_FUND_*/1000` (10e6 USDC / 10e18 WCOP — nonzero so the deposit succeeds, NOT `BelowMinimumRedemption`; far below requirement so `_validateSolvency` `:1142` fires) with the IDENTICAL `legParams`/`legIndex`/`positionSize` → reverts `AccountInsolvent(0,1)`, `numberOfLegs == 0` after (the funded twin's `numberOfLegs > 0` makes the gate non-vacuous + provably collateral-driven, not a param/liquidity revert). **`_onResult` unit-proven in ISOLATION** via `MacroHedgeExecutorDecodeProbe` (a test-only `seedPending`→`_sendRequest`→MockPlatform wrapper registers `pendingRequests`; the probe OVERRIDES the `virtual` mint sink to RECORD `legParams` + SKIP `pool.dispatch`): the decode records the demo struct (`responses[0].result`, not the wrapper), `NotPlatform`/`UnknownRequest`(unregistered + replay) hold. **7/7 fork + 4/4 onResult unit leaves green; both `bulloak check` exit 0; no sibling regressions** (PolygonPools 3/3, OperationalCostManagement 10/10, MacroHedgeStrategist 17/17). **LIMITATION (honest):** the production `_onResult`→real-mint edge is COMPILED but UNEXECUTED — Task-3 probe skips `dispatch`, Task-2 fork calls `resolveAndMint` directly; the live Somnia→Polygon join + the live `llm-inference` representativeness are Phase-14 `workflow_dispatch` STRETCH. Iron-Law ancestry: `322cab4` (trees + failing fork test, RED) → `e85c2fc` (impl) → `de22865` (unit suite) — `merge-base --is-ancestor` confirmed. **Deviations (2, both auto-fixed Rule-3, both on test files, both preserving the plan's exact intent):** (1) forge 1.5.1 `vm.expectRevert(bytes4)` requires an EXACT data match and rejects an error carrying args (`AccountInsolvent(0,1) != 0xcdef092d`) → switched both negative-gate sites to `vm.expectPartialRevert(Errors.AccountInsolvent.selector)` (matches the selector, tolerates ANY overload args — the plan's stated "bare selector, all 3 overloads" intent; verified against a throwaway forge-1.5.1 probe). (2) bulloak anchored the tree root on the `ICollateralDeposit` interface declared before the test contract → moved it AFTER (the 08-06/08-07 helper-after precedent). **Metric (13-02):** 3 tasks, 12 files (11 created, 1 modified), ~12 min.

## Phase 12 — MacroHedgeStrategist (Agent 1) prompt → hedge mandate (Agentathon cornerstone, side-track)

> Phase 12 re-semantics the live Phase-11 v1 strategist so Agent 1 emits a `HedgeMandate` (inferred
> economic school + direction + target notional), NOT a finalized `HedgeLegParams` — the moneyness/
> strike/width/feasible-size geometry is explicitly Agent 2's representativeness output (Phase 14).
> The `progress:` frontmatter block tracks the v2.0 convex-instrument phases (7–10) and is intentionally
> left at its prior value; Phase 12 progress is tracked here and in ROADMAP.md. **Wave 1 = 12-01
> (substrate); 12-02 (the strategist re-semantic, evm-TDD) is Wave 2 and imports both.**

- **2026-06-06 (12-01):** Wave-0 substrate SHIPPED — the honest two-type Agent-1/Agent-2 seam (the smallest unblock the Plan-02 evm-TDD failing test imports). `contracts/src/types/HedgeMandate.sol` (new): the Agent-1 output value type carrying hedge INTENT only — five LOCKED intent-only fields `economicTheory` (IMacroThesis handle), `underlyingMarket` (PoolId anchor = `PolygonPools.POLYGON_WCOP_USDC_POOL_ID()`), `targetNotional` (uint256, **whole USD notional units** — NOT bps, NOT the resolver's `[1,127]` optionRatio; the size bound lives in Plan-02/Phase-14), `chainId` (uint32), `isLong` (bool). The four pass-through field TYPES mirror `HedgeLegParams` BYTE-FOR-BYTE (grep-diff confirmed: `IMacroThesis economicTheory` / `PoolId underlyingMarket` / `uint32 chainId` / `bool isLong`) so a Phase-14 `HedgeLegParams` scratch-copy needs ZERO translation. **DELIBERATELY no geometry-field declarations** (type-prefixed grep `uint256 (strikeWAD|size)|PayoffTerms payoffTerms` == 0) — the whole point of the two-type seam; NatSpec describes the omission descriptively (no verbatim geometry-field names, which would false-fail its own gate). `contracts/src/interfaces/IMacroThesis.sol` (promoted from the empty marker): the `interface IMacroThesis {}` marker is KEPT (so `HedgeLegParams.economicTheory` + `HedgeMandate.economicTheory` stay valid handle types) and a concrete **`library MacroThesisRegistry`** added (Fork B — handle-resolving over enum-indexed, Pitfall 5): `schoolLabels()` (the SINGLE source of truth for both the `inferString` `allowedValues` guardrail and the `_mapSchool` keccak compare — exactly the two demo schools `SHILLER_MACRO_RISK`/`POST_KEYNESIAN`, extensible = one array entry + one branch), `thesisOf(label) → (IMacroThesis handle, bool ok)` (resolves a label to a NON-DEPLOYABLE sentinel handle `address(uint160(0x5))`/`0x6` — satisfies ONLY the `!= address(0)` well-formedness check, holds NO code, NEVER call/delegatecall/staticcall'd; `ok==false` on no-match mirroring the v1 `_mapAction(...,false)` no-default-write contract), `promptBias(label)` (deterministic per-school prompt-framing fragment, empty string on no-match). `forge build` exit 0 — the compile gate proving both Task-1 imports resolve; only the pre-existing benign `MacroOracle.sol named-struct-fields` notes remain (no new errors). Iron-Law-irrelevant (type=execute, not TDD); both artifacts ready for Plan 02 to import. Task commits: `59dcc0d` (HedgeMandate type) → `1d4387f` (IMacroThesis registry promotion). **Deviations (0):** plan executed exactly as written — both files landed verbatim against the prescribed struct + registry code, no Rule 1-3 auto-fixes, no Rule 4 architectural decision. **Continuation note:** the two task commits + on-disk artifacts (and a complete, accurate `12-01-SUMMARY.md`) pre-existed from a prior session that died before the docs/state finalization (the documented 08-03/11-02 socket-death pattern); this run independently re-verified EVERY acceptance gate (all Task-1/Task-2 greps PASS, `forge build` exit 0, field-type alignment byte-for-byte, both commits present in the log) before finalizing docs — no rework, no faked-green. **Metric (12-01):** 2 tasks, 2 files (1 created, 1 modified), ~4 min (prior session) + re-verification (this session).
- **2026-06-06 (12-02):** Wave-2 SHIPPED — Phase 12 plans 2/2 complete (STRAT-01 + STRAT-02). The live Phase-11 `MacroHedgeStrategist` (CONSUMER `0xfA428171…` on Somnia testnet) was SURGICALLY re-semantic'd to emit a **`HedgeMandate`** instead of an action/sizeBps decision, keeping the v1 spine VERBATIM and changing ONLY three things. **evm-TDD Iron Law honored** (ancestry `git merge-base --is-ancestor` verified): the re-labeled `.tree` + the FAILING unit suite committed FIRST (`6fe4c32`, RED — the suite fails to compile against the old API NAMING the missing `requestSchoolDecision`, M3 RED-for-the-right-reason, NOT a typo), THEN the impl (`7101acb`, GREEN), THEN the doc reconcile (`68e34d0`). **The three changes:** (1) Leg 1 `requestSchoolDecision(string userIntent, bytes32 dataKey, int256 consensus)` fires `inferString` over `MacroThesisRegistry.schoolLabels()` (the structural guardrail — the model cannot return out-of-set) → the school callback resolves `thesisOf` to a non-zero `IMacroThesis` handle (no default write on no-match) and writes `schoolLabel`+`economicTheory` IN THE CALLBACK (provenance discipline); (2) Leg 2 `requestNotionalDecision(decisionId)` fires `inferNumber([MIN_NOTIONAL=1_000, MAX_NOTIONAL=100_000_000])` whole-USD behind the **B2 `schoolSet` gate** (reverts `UnknownDecision` unless the school completed first); (3) the join assembles the embedded `HedgeMandate` (anchor `POLYGON_WCOP_USDC_POOL_ID()`, `chainId=137`, `isLong=true`), sets `decidedAt`, and emits `StrategistDecided(decisionId, school, HedgeMandate)` — exposed via **B1 SINGLE-struct getters** `getMandate(id)→HedgeMandate memory` + `decisionState(id)→DecisionState memory` (member access compiles; the positional-tuple auto-getter does NOT, solc 0.8.24 Error 9582 — the v1 `getDecision` lesson). **M2 behavior change vs v1:** the clamp `raw <= int256(MIN_NOTIONAL) ? MIN_NOTIONAL : (uint256(raw) > MAX_NOTIONAL ? MAX_NOTIONAL : uint256(raw))` floors BOTH negatives AND positive-below-floor up to `MIN_NOTIONAL` (v1 floored small positives to 0). **Spine kept verbatim** (grep-proven): `decisionId = bytes32(requestId)` block-independent join, `try this.decodeString` decode-safety, `result.length != 32` notional guard, base `handleResponse` NOT re-declared (count 0 — only `_onResult` overridden). **Old API removed** (count 0): `MAX_SIZE_BPS`/`HedgeAction`/`HedgeDecisionMade`/`_mapAction`. **Fork A** (caller free-text intent 4B) + **Fork B** (handle-resolving registry, from Plan 01) documented. `bulloak check` exit 0; **strategist suite 19/19** (v1's 17 + the M2 below-MIN floor-UP leaf + the STRAT-02 `when a mandate is assembled` well-formedness leaf, which copies the four mandate fields into a `HedgeLegParams` scratch struct — a compile-time Phase-14 hand-off proof, NO geometry); **full fork-free suite 97/97, 0 failed** via the `--no-match-path 'test/**/*{fork,invariants}*'` PATH glob (empirically verified to exclude ONLY the Base-fork `LongGammaWrapper.invariants.t.sol` while KEEPING the in-memory `OperationalCostManagement.invariant_costConserved`); no sibling regressions (PolygonPools 3/3, OperationalCostManagement 10/10). `docs/UI-AGENT-HANDOFF.md` §3/§4/§5 reconciled to the HedgeMandate-shaped `StrategistDecided` with a dated 2026-06-06 frontend-coordination note (Pitfall 7, documentation-only). **LIVE-PROOF DEFERRAL (M4):** the live "different prompt → different mandate on Somnia testnet" run (adapting `script/macro-hedge-strategist-e2e.sh` from the v1 `requestActionDecision`/`requestSizeDecision`/`getDecision` to the new API) is a **DEFERRED Manual-Only `workflow_dispatch` follow-up, NOT a CI gate** — re-confirm `LLM_AGENT_ID` 12847293847561029384 + the testnet platform `0x037Bb9…6776` against the Agent Explorer before spending STT (CLAUDE.md stop-gap caveat). **Deviations (1, in-scope documentation):** also updated the §3 status-table row for MacroHedgeStrategist (it still described the v1 `HedgeDecision{action,sizeBps}` + the pre-correction `HedgeLegParams` target, which the landed work contradicts) — same Pitfall-7 doc-coordination item, no contract change. **Metric (12-02):** 3 tasks, 4 files (0 created, 4 modified), ~6 min.

## Phase 11 — MacroHedgeStrategist (Agentathon POC, side-track)

> Phase 11 is an additive Agentathon POC layered on top of the v2.0 arc (see ROADMAP § Phase 11).
> The `progress:` frontmatter block above tracks the v2.0 convex-instrument phases (7–10) and is
> intentionally left at its prior value; Phase 11 progress is tracked here and in ROADMAP.md.

- **Plan:** 11-03 complete (4/4: 01, 02, 03, 04 all done). **Phase 11 COMPLETE** — all four AGENT-01..04 satisfied. Agentathon POC demo-video evidence captured (addresses + two HedgeDecisionMade tx hashes below).
- **2026-06-02 (11-01):** `ILLMAgent` interface vendored into `contracts/src/interfaces/ISomniaAgents.sol` with ONLY `inferString` + `inferNumber` (`inferChat`/`inferToolsChat` dropped — out of scope per 11-CONTEXT § Deferred). `calldata` for string/array params matches the existing `IJsonApiAgent` convention; the 4-byte selector is data-location-invariant, so Plan 02's `abi.encodeWithSelector(ILLMAgent.inferString.selector, …)` is unaffected. `forge build` green (only pre-existing unrelated `named-struct-fields` lint notes from `MacroOracle.sol`). Committed `faffaec`.
- **2026-06-02 (11-02):** `MacroHedgeStrategist is SomniaAgentConsumer` shipped with the BLOCKER-1 join fix: `decisionId = bytes32(actionRequestId)` (block-INDEPENDENT — NOT `keccak(...block.number)`), so the size leg in a later block joins the same struct. TWO entrypoints, one infer per tx (msg.value-forwarding footgun): `requestActionDecision(bytes32,int256)` returns the decisionId, `requestSizeDecision(bytes32)` binds the size leg (guard = `actionSet==true && !sizeSet`, NOT a `macroValue==0` sentinel since zero is a legit signed macro print). `_onResult` decodes string→enum behind try/catch + `_mapAction` keccak compare (no enum-zero write on no-match → `DecisionFailed`, `actionSet` stays false) and int256→clamp `[0, MAX_SIZE_BPS]` behind a 32-byte length guard. `HedgeDecisionMade` fires only on both-set. Tree (`71d3bec`), contract (`0853bfc`), suite (`f778012`). bulloak 0.9.2 clean; `forge test --match-path test/instrument/MacroHedgeStrategist.t.sol` 17/17 green incl. the `vm.roll` cross-block-join leaf, int-blob/string-blob payload-type containment, both-end clamp, and the inherited `NotPlatform`/`UnknownRequest` auth+replay guards.
- **2026-06-02 (11-02 continuation deviation):** prior executor's socket died mid-Task-3; resumed, reconciled the `.t.sol` to a SINGLE contract (`MacroHedgeStrategistlifecycle`) importing the standalone `test/mocks/MockMacroOracle.sol` (de-orphaned) so bulloak anchors correctly, with no-op `when*` branch modifiers mirroring `CcopUsdcPool.t.sol`. Both gates already green on disk on resume; verified before committing.
- **AGENT-01 status:** COMPLETE — interface (11-01) + contract compiles & calls the LLM agent (ID `12847293847561029384`) via `encodeWithSelector(ILLMAgent.infer*.selector,…)`. Marked complete in REQUIREMENTS.md.
- **AGENT-02 status:** COMPLETE — decision behavior + bounds guardrail forge-proven at unit level. Marked complete in REQUIREMENTS.md.
- **AGENT-04 status:** COMPLETE — `contracts-ci.yml` gates the Foundry suite (secret-free build+spec proven green via the renamed fork test, RPC-cached/sharded/retried fork job, workflow_dispatch-only Somnia e2e). Marked complete in REQUIREMENTS.md (11-04).
- **AGENT-03 status:** COMPLETE — UNIT half (auth/replay/DecisionFailed/payload-type-containment, 11-02) + LIVE Somnia-testnet half (11-03) both done. Marked complete in REQUIREMENTS.md.
- **2026-06-02 (11-03):** `contracts/script/macro-hedge-strategist-e2e.sh` authored (`f150f96`) — cast-driven Somnia-testnet runner, class-correct deposits (JSON_DEPOSIT=FLOOR+0.09 STT json-fetch; LLM_DEPOSIT=FLOOR+0.21 STT each llm-inference leg), MacroOracle deploy/seed precondition (slash-terminated PROXY_BASE), explicit decisionId hand-off, MacroReceived-awaited, in-enum/in-range asserts. **LIVE run APPROVED** across two runs on Somnia testnet (chain 50312). Deployed: MACRO_ORACLE `0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f` (PROXY_BASE `https://keeper-eta-pied.vercel.app/`), CONSUMER (MacroHedgeStrategist) `0xfA428171E1F5B56f92C67C002De1d8e90B053EE1`, PLATFORM `0x037Bb9C718F3f7fe5eCBDB0b600D607b52706776`. LLM_AGENT_ID `12847293847561029384` confirmed on-chain (AgentRequested target `0xb24ac1afbcefc708`). json-fetch sourced LIVE TE CPI macroValue=568 (Colombia 5.68%) via the no-cache keeper-proxy. **Decision-moves-with-consensus PROVEN** (same macro 568, different consensus): run #1 consensus=500 → ADD_LONG_GAMMA/6800 (HedgeDecisionMade tx `0x2a8ec99452956fb94ad3b138844957409b298daa05e2d9986b34676d643c36a5`; size-leg tx `0x887f16705aec6df12cb6c643edb70a32264f7212ad6380be6f3e2d59c7677ecd`); run #2 consensus=900 → REDUCE/568 (tx `0x5057f803d214aa549e16a6c8ce3745610f0ce407a3bac06c1a6f643807dc3575`). Both action and size differ ⇒ genuine reasoning, not a constant. Two e2e-script bugs found+fixed DURING the live run (orchestrator-driven debug), committed `f008a99`: (1) decisionId parsed as topics[0] (event sig) instead of topics[2] → false-negative "callback did not land"; (2) balance gate overflowed bash 64-bit int on a 20-digit wei value. No contract changes. **Metric (11-03):** 2 tasks (1 auto + 1 human-verify), 1 file, ~90 min incl. live run + debug.
- **2026-06-02 (11-04):** `.github/workflows/contracts-ci.yml` shipped (modeled on `keeper-ci.yml`). THREE jobs: (1) `build-and-spec` — NO secret/NO RPC: `forge build` + `bulloak check` loop over `test/fork/*.tree` + `test/instrument/*.tree`, NON-blocking check of the parseable-but-orphaned `handleResponse.tree`, then `forge test --no-match-path 'test/**/*fork*'` (exits 0 keylessly). A comment names exactly the three un-parseable spec trees skipped (`MacroOracle.tree`, `sendRequest.tree`, `sweep.tree`); no `test/spec/*.tree` glob. (2) `fork` — `secrets.BASE_RPC_URL` on that job only + `actions/cache@v4` (`~/.foundry/cache/rpc`, key `foundry-rpc-base-46700000-v1`) + shard `1/2` + `--retries 2 --delay 3` + best-effort warm step (cold-cache 429 defense). (3) `somnia-e2e` — `if: workflow_dispatch` only (never push/PR; no auto STT spend). `permissions: contents: read`; safe `pull_request` event only. **BLOCKER-2 fix:** `git mv` CcopUsdcPool.{tree,t.sol} → `.fork.{tree,t.sol}` (the test does `vm.createSelectFork`), so `*fork*` honestly excludes it and the no-secret job is green — proven `.env`-INDEPENDENTLY via `forge test --list --no-match-path 'test/**/*fork*' | grep -q CcopUsdcPool` returning non-zero (NOT `env -u BASE_RPC_URL`, which forge defeats by auto-loading `contracts/.env`). `foundry.toml [profile.default]` gained `rpc_storage_caching = { chains = [8453], endpoints = "all" }`. Commits: rename+toml `0dc1587`, workflow `c291305`. AGENT-04 marked complete in REQUIREMENTS.md.
- **Metric (11-01):** 1 task, 1 file, ~5 min. **Metric (11-02):** 3 tasks, 4 src/test files, ~15 min (continuation). **Metric (11-04):** 2 tasks, 4 files, ~3 min.

## Decisions Log (v2.0)

- **2026-06-07 (14-01):** The representativeness substrate LOCKED as a **pure library with the deterministic geometry decoupled from any LLM** + **Fix C as the only strike path**. (1) **Representativeness is NOT a correlation** — it is the regime-conditional ASYMMETRIC β₁ passthrough: `regimeVol(STRESS) = baseVol·(WAD + (M(β₁stress)−M(β₁tranquil)))/WAD` where `M(β₁,dev)=β₁·dev/WAD`; **TRANQUIL == the GBM baseline by construction**, so the suite asserts STRESS-vs-baseline, NEVER TRANQUIL-vs-baseline. The **load-bearing binding constraint is the CONTINUOUS `regimeVol(STRESS) > baseVol`** (tuning-invariant); the QUANTIZED `regimeWidth(STRESS) != gbmBaselineWidth` is secondary (one ceil-unit at the TEMPLATE vintage, since `volToWidth` ceil-snaps). (2) **base vol is TICK-SPACE everywhere** (the demo's 14_400 `PayoffTerms.vol` scale, NOT a WAD — a WAD ~1e16 sqrt-clamps to 4095); `gbmBaselineWidth(baseVol) == volToWidth(uint88(baseVol),horizon,ts)` EXACTLY pins this. (3) **Fix C is localized to `RepresentativenessLib.structuralStrikeTick`** = `snap(getTickAtSqrtPrice(sqrt(humanRate·1e12·Q192)))`, a 0-error fixed point returning the EXACT tick that gets minted; **the broken WAD-to-sink inversion (`strikeWadForSink` through the raw 1:1-decimal converter) is DELETED — absent from every file this plan touches.** The canary asserts EXACT equality (`structuralStrikeTick(4485,60)==360360`, `(3900,60)==358980`, verified to the wei against the real libs BEFORE baking), NOT a band — a 22-tickSpacing emitted-vs-minted lie is impossible. (4) **`feasibleOptionRatio` is a DEFINED monotone map** `clamp(notional/NOTIONAL_PER_RATIO,1,127)` (NOTIONAL_PER_RATIO=1_000 TEMPLATE so a mid-band notional changes the ratio), NOT a vague "clamp to [1,127]" — a notional-ignored regression REDs. (5) **fail-safe `effectiveRegime`**: stale (beyond MAX_STALENESS=1h)/unset(observedAt==0)/Unknown ⇒ STRESS (§3.6), with `IRegimeOracle` mirroring the `MacroOracle.deliveredAt==0 ⇒ unset` precedent. (6) **Mutation non-vacuity is proven by a dedicated leaf + a real collapse** (β₁stress→β₁tranquil ⇒ continuous `regimeVol(STRESS)==baseVol` 14400<=14400 AND quantized width 20==20), the 13-03 discipline; the prior session left this mutant ACTIVE in `regimeVol`, which IS why the on-disk suite was 13/17 on resume — restoring `beta1StressWad` is the Task-2 GREEN step. (7) **bulloak-vs-VALIDATION naming resolved by delegation** (verified): bulloak 0.9.2 `check` derives the required `test_When*/test_Given*` name from the clause AND tolerates extra functions, so each anchored leaf delegates to its VALIDATION-`--match-test`-named assertion function — both gates pass simultaneously. **NO MacroHedgeExecutor/PriceGrids file touched** (the Fix-C sink split is Plan 02's). Whole fork-free tree 114/114. Iron-Law ancestry `ac1a6e1`→`e5254cb`→`04471fa`.
- **2026-06-06 (13-01):** The STRAT-02 pool-id anchor LOCKED as a **pure function**, not a `constant` state var: `PolygonPools.POLYGON_WCOP_USDC_POOL_ID()` returns `wcopUsdcKey().toId()` because `PoolKey`/`PoolId` are not compile-time constant-expressible in Solidity (RESEARCH §4). `wcopUsdcKey()` reproduces the demo's runtime key VERBATIM — currency0=USDC (`0x3c49…`, 6dp) < currency1=wCOP (`0x8a1D…`, 18dp), fee 3000, tickSpacing 60, hookless — and the ordering is load-bearing (any drift breaks the whole TokenId derivation). Both agents reference the cornerstone pool by this stable value, never a recomputed keccak. The unit test proves the round-trip against an inline-rebuilt `localKey.toId()` (the demo's exact construction) AND that `panopticPoolIdFromUniV4PoolId` matches on both, with sample vegoid 4 (the 07-05 §G `RiskEngine.vegoid()` constant; the mapping is pure so any sample feeds both sides identically). `IMacroThesis` stays an empty marker interface — the resolver never reads `economicTheory`, the demo passes `IMacroThesis(address(0))`, and the concrete economic-school shape is Phase-12 STRAT-01; Phase 13 only needs it to compile (no edit made). `foundry.toml` gains chain 137 in `rpc_storage_caching` (additive, Base/8453 retained) for the Pitfall-6 429 defense. **Parallel-wave isolation:** when a sibling Wave-1 agent's in-flight test blocks the whole-`test/`-tree compile, prove this plan's gates with `FOUNDRY_TEST=<my test path>` (compiles only the file's source closure) rather than touching the sibling's files. Iron-Law ancestry `3de3c5e`→`a91a708`→`ebea287`.
- **2026-06-06 (13-03):** The EXEC-03 `cummCost` ledger LOCKED with the **M3 conservation≠idempotency distinction codified as two distinct mutations against two distinct assertions** — the load-bearing finding. CONSERVATION (`invariant_costConserved`: `cummCostSomi == Σ_d costOf[d].agentCostSomi`) is proven NON-VACUOUS by an ASYMMETRIC mutation (add to the global WITHOUT the line → FAILS `882…3505 != 0`); it is provably BLIND to SYMMETRIC double-counting (dropping the per-leg guard doubles BOTH sides → `14 == 14`, still passes). IDEMPOTENCY is therefore proven by a SEPARATE leaf `test_accrueSameLegTwice_cummCostSomiUnchanged` (re-accrue same `(d,leg)` → `cummCostSomi` UNCHANGED), broken by the DISTINCT dropped-guard mutation (`14 != 7`). The conservation invariant CANNOT prove idempotency — recorded both mutations verbatim. **Canonical signature** `accrueAgentCost(bytes32 decisionId, uint8 leg, uint256 somi)` (3-arg; the RESEARCH §7 2-arg quote was the starting point, REFINED — `leg` makes per-`(decisionId,leg)` idempotency enforceable; Agent 1 = leg 0, Agent 2 = leg 1). Idempotency = `revert AlreadyAccrued` under `agentLegAccrued[d][leg]`/`dataAccrued[d]` (the plan permitted revert OR no-op; tests `try/catch` the 2nd accrue so the unchanged assertion holds either way). **The conservation invariant MUST read the ACTUAL ledger lines** (`Σ_d costOf[d]` via `totalCost`), not just the handler mirror — the global-vs-mirror-only form did NOT catch the asymmetric mutation (the global side still matched the mirror). Cross-check the independent mirror too (catches a lockstep global+line drift). **Non-vacuity is asserted in a DEDICATED unit leaf, NOT inside the invariant body** — an in-body `assertGt(callCount>0)` false-fails Foundry's setup-time run-0 evaluation before any handler call (the proven `LongGammaWrapper.invariants` shape asserts only the equality in-body). Ledger ACCRUES + CONSERVES the BUDGETED forwarded SOMI only (realized `executionCost` structurally absent on Somnia); NO real transfer/settlement (XCHAIN-01 deferred) — documented in NatSpec. Demo-scoped, no access control (Plan 02 wires it as a settable `costLedger`, not a ctor requirement). PURE in-memory unit/fuzz — NO fork (does NOT 429); reused the `[invariant]` floor in foundry.toml without touching it (13-01 owned it this wave). Iron-Law ancestry `e3b9dc4`→`ceb5057`.
- **2026-06-06 (13-02):** The deployable executor LOCKED as **promote-don't-invent, one-sink-two-entrypoints**: the demo harness resolver body migrated VERBATIM into a shared internal `_resolveAndMint` (the `% 128`-mask `require(size<=127)` as its FIRST statement, covering BOTH `resolveAndMint` and `_onResult`); the external `resolveAndMint` (direct, MVP fork-proven) and `_onResult` (live decode, unit-proven in isolation) both route there. The **collateral-ownership shift is the crux**: the executor is BOTH the `dispatch` caller AND the ERC4626 share owner (`ct.deposit(assets, address(executor))`), so `s_positionBalance[executor]` holds the mint and `_validateSolvency` checks the executor's shares. **EXEC-02 is honest** — NO pre-mint solvency quote (it reverts `PositionNotOwned`); the gate is the protocol-native atomic `AccountInsolvent` AFTER `_mintOptions` writes state. **The non-vacuous negative-gate recipe codified:** an identical-params funded twin (`numberOfLegs>0`) PLUS an under-funded twin pinned to a SPECIFIC nonzero `DEFAULT_FUND_*/1000` (nonzero clears `BelowMinimumRedemption`; far-below makes `_validateSolvency` the first collateral-sensitive failure) → reducing ONLY collateral cannot introduce a NEW liquidity revert, so the `AccountInsolvent` is provably collateral-driven, not param/liquidity. **forge 1.5.1 lesson (load-bearing for any future custom-error-with-args gate):** `vm.expectRevert(bytes4)` does an EXACT revert-data match and REJECTS args — use `vm.expectPartialRevert(Error.selector)` to match the selector and tolerate the overload args (this is what the GSD "bare selector, all overloads" guidance actually requires under 1.5.1). **bulloak co-location reminder:** declare any helper interface / probe subclass AFTER the root test contract (it anchors the tree on the FIRST contract). The test-only `MacroHedgeExecutorDecodeProbe` seeds the internal `pendingRequests` via a `seedPending`→`_sendRequest`→MockPlatform wrapper (the production executor exposes NO public request entrypoint) and overrides the `virtual` mint sink to prove the decode without a fork. **The live `_onResult`→mint join + the live representativeness inference are explicitly NOT proven this phase** (Phase-14 `workflow_dispatch` STRETCH) — do not claim production routing end-to-end. The demo-session `src/` closure (`HedgeLegParams`/`PositionInfo`/`PayoffTerms` type+lib/`PriceGrids`/`VolToWidth`) was uncommitted and got committed alongside the impl; the orphan `CalldataReader`/`OptionType`/`Underlying` (no importers) were left out and logged to `deferred-items.md`. Iron-Law ancestry `322cab4`→`e85c2fc`→`de22865`.
- **2026-06-02 (08-07):** Named fuzz invariants LOCKED as the INDEPENDENT-LEDGER-vs-LIVE-STATE form, completing Phase 8's plan set + the 8-canonical-tree set. The handler MUST store `address(this)` (itself) as the wrapper's `user` so `close()`'s `NotUser` gate passes — otherwise the close/claim path never fuzzes and the invariants are vacuous. The handler tracks two INDEPENDENT claim-time ledgers from live reads, NEVER the gross deposit `a0/a1` (the mint commission burn erodes surviving below the deposit by far more than +1, B1, so the deposit-as-backing `liveSurv + totalPaid + 1 >= deposit` form is INVERTED and false-reds a correct wrapper on step 1): `preClaimSurviving_i` = live `convertToAssets(balanceOf(wrapper))` just before the redeem; `lastPaid_i` = the realized user-balance delta. `invariant_residualNeverExceedsHoldings` = `assertLe(lastPaid_i, preClaimSurviving_i+1)` (single claim); `invariant_userClaimsBackedByCollateral` = `assertLe(totalPaid_i, cumPreClaimSurviving_i+1)` (cumulative non-overpayment, TRUE on step 1 `0<=0+1`, NON-vacuous — proven by mutants that fail with real `9.99e17`/`4.4e28` payouts). NEITHER is the `max(surv-cost,0)<=surv` tautology (W4: v1's zero-address meter makes `_costOf`=(0,0), so no cost term enters). **The deposit bound MUST be over-funded `[1e18, type(uint104).max]`** (P3, the 08-03/05 cap) — the plan's `[1e6,1e14]` underflows (0x11) the 1-ether long mint, making `act_open` always revert and the invariants vacuous. `foundry.toml` gains an ADDITIVE `[invariant]` section (runs=16, depth=16, fail_on_revert=false): the base toml had none, so Foundry's 256x500 defaults would silently overrun the fork run; `fail_on_revert=false` is needed because the one-shot wrapper makes post-open `act_open` calls legitimately no-op. Both pass on the Base fork with 0 reverts; commit `2c5b5ef`.
- **2026-06-02 (08-06):** Three `dispatchFrom` involuntary branches LOCKED + WRAP-03 fully fork-proven. The branch route is disambiguated live by `(solvent, toLen, finalLen)` (PanopticPool L1410-1465): **settle** = solvent & `toLen==finalLen` (hashes equal) → `_settlePremium` (NO `_burnOptions`, STAYS Open); **forceExercise** = solvent & `toLen==finalLen+1` & `validateIsExercisable()!=0` → `_forceExercise` → `numberOfLegs→0`; **liquidation** = insolvent-at-all-4-ticks & `finalLen==0` → `_liquidate` → closes. **forceExercise opens the long AT-PRICE (no displacement)** — the disambiguation does NOT require the price be past the chunk ("out-of-range" only scales the exercise fee; `validateIsExercisable` returns 1 for ANY long with width, TokenId L523-533); displacing the price tripped `PriceImpactTooLarge` (burn cumulative tick delta > 2·tickDeltaLiquidation, L677-682) + `StaleOracle`. **settle clears StaleOracle via pokeOracle TWAP-EMA convergence** (NOT the 08-05 `_restorePrice`, which targets a different gate): the up-swap accrues fees + moves `currentTick`, then ~30 `pokeOracle()` steps (≥120s apart, clamped ±149/step) pull `twapTick` UP to `currentTick` with NO price move so `|currentTick-twapTick|≤513`; fees stay accrued. **liquidation insolvency = both-CT `vm.store` collateral-shrink** at the DERIVED `balanceOf` slot (`BAL_SLOT=1` via `forge inspect storage-layout`; slot 0 is `_internalSupply` — NEVER slot 0; probe-and-verified), applied to BOTH `ct0` AND `ct1` (one-sided may not flip `solvent==0` at all 4 ticks), no price move → no StaleOracle (a single large adverse swap is rejected, L1388). **dispatchFrom `positionIdListFrom` is the CALLER's OWN list** — the tail `_validateSolvency(msg.sender, positionIdListFrom)` hashes it vs `s_positionsHash[caller]`; an exercisor/liquidator that owns nothing passes an EMPTY list (a non-owned list reverts `InputListFail` L1859); settle's caller is the seller (owns `sellerShortId`). The **forceExercise refund leg** (`ct.refund(account, msg.sender, …)`, L1657-1658) needs the exercisor to hold a CT deposit (else `NotEnoughTokens`). `ResidualEroded`-on-involuntary-debit is asserted CONDITIONALLY for settle (Pitfall 3 cap) AND forceExercise (at-price OTM debits ~0); the UNCONDITIONAL load-bearing proofs are settle-STAYS-OPEN and forceExercise/liquidation TERMINAL-CLOSE + never-overpay (pre-claim snapshot). TEST-ONLY (no src edit; consumes the 08-05 `_reconcile` Open→Closed detection). Iron-Law ancestry `2c0ef11`→`cfea71a`→`53adf7d`.
- **2026-06-02 (08-05):** Voluntary-exit cash-flow LOCKED. `close()` is the user-gated (`NotUser`) voluntary burn via `pool.dispatch(list, emptyFinal, [0], lim, false, 0)` (size 0 ≠ stored ⇒ burn; `false`/`0` voluntary args keep `OptionBurnt.premiaByLeg` unperturbed) — `Open->Closed` once `numberOfLegs(wrapper)==0`; the ONLY voluntary exit (closes the trapped-funds path, ROADMAP SC-5 / BLOCKER N1). The `Open->Closed` promotion lives in the shared internal `_reconcile()` (`if (state==State.Open && numberOfLegs(wrapper)==0) state=Closed;`) — the **involuntary-close detection authored HERE**, so Plan-06's `dispatchFrom` branches (which drop `numberOfLegs` WITHOUT running wrapper code) are reconcilable; `_reconcile()` is non-modifier so both `syncResidual()` and `claimResidual()` (each `nonReentrant`) can call it without double-tripping the guard. `claimResidual()` is **CEI** (`claimed`+`state=Claimed`+`_reconcile` BEFORE the redeem) and checks `claimed` BEFORE the `state!=Closed` gate so a second claim reverts `AlreadyClaimed` (the first claim already set `Claimed`, which would otherwise mask the intent as `WrongState`). Residual is per-token `max(convertToAssets(balanceOf(wrapper)) - _costOf_i, 0)` — the ONLY deduction is `_costOf()` ((0,0) for the v1 zero-address meter); streamia+commission are already netted by `settleBurn` (NO double-count, Pattern 7). `_redeemCapped` = `min(previewWithdraw(residual), maxRedeem(wrapper))` with the `previewRedeem(shares)==0` BelowMinimumRedemption dust-guard so the claim NEVER reverts on a binding cap/dust (remainder stays as shares, B4); the test `_closeSellerShort()`s first to free the pool-wide `s_depositedAssets-1` cap. **Fork gotcha:** the big fee-seeding up-swap trips `PriceImpactTooLarge` on the burn (current-vs-median tick delta > 2·tickDeltaLiquidation), so the test restores spot near the oracle median (`4e15` USDC down-swap, prototyped: 359268 start ⇒ ~359577) before `close()`; fee growth stays accrued (cumulative). WRAP-04 proven via close 6/6 + claimResidual 7/7 on the Base fork; surviving-at-close DIVERGES from `deposited` (proves it tracks surviving, not the deposit). Iron-Law ancestry `fb85a98`→`e967f88`→`8c4f422`.
- **2026-06-01 (07-05):** FORK-03 keystone green — the borrowed Panoptic V2 working pool comes EXCLUSIVELY from `PanopticFactory.deployNewPool` (CWIA proxies); the only `new PanopticPool`/`new CollateralTracker` are master copies (`poolReference`/`collateralReference`). `vegoid` is read inside the factory as `re.vegoid()` (constant 4, §G), never a per-pool param. `PanopticV2DeployHelper` runs the §B+§D choreography verbatim and returns a seam-safe `Deployed` struct.
- **2026-06-01 (07-05):** M-3 deploy-isolation via a base contract (`PanopticDataSeamBase`) absorbs ALL deploy coupling so the seam test imports NEITHER `panoptic-borrowed` NOR `PanopticV2DeployHelper` (both grep guards hold) while the pool is genuinely deployed. ct0/ct1 typed `IERC4626` (B-1) so `.deposit()` is reachable without the concrete `CollateralTracker`; `poolId` handed across as a plain `uint64` so the seam test builds a concrete `TokenId` via the `@types` value-type builder.
- **2026-06-01 (07-05):** The minted short leg must be OUT OF THE MONEY and `width=2`. `width=1` (`r=5`) leaves chunk ticks un-aligned to tickSpacing=10 ⇒ `InvalidTickBound`; `width=2` (`r=10`) aligns them. An AT-the-money strike straddles the active tick and underflows (0x11) the SFPM's ERC6909-claim burn at mint — pushing the strike +2000 ticks above current makes the short single-sided and it mints/burns cleanly against the 1,000,000-ether full-range seed. `addLeg` arg order confirmed verbatim vs vendored `TokenIdLibrary` @fe55774.
- **2026-06-01 (07-05):** `PanopticDataSeam.fork.tree` co-located in `test/instrument/` (bulloak 0.9.2 same-dir + full-stem rule: `PanopticDataSeam.fork.tree → PanopticDataSeam.fork.t.sol`), not the plan's `test/spec/PanopticDataSeam.tree` — same deviation as 07-04. `deployPanopticV2` returns a single `Deployed` struct (+ `_deployInfra` split) to dodge Stack-too-deep under the non-viaIR cancun/200-runs profile.
- **2026-06-02 (07-01):** Single `cancun`/`0.8.24` profile supersedes FORK-01's roadmap multi-version-solc-matrix wording (07-RESEARCH §5; Panoptic V2 is `^0.8.24` everywhere) — recorded in NOTICE + SUMMARY so the checker does not flag a false gap.
- **2026-06-02 (07-01):** v4-periphery NOT installed and NO v4-periphery remapping (B-2) — its StateView/IStateView pull the undefined `@uniswap/v4-core/` alias + permit2; pool-state reads go via the already-borrowed `V4StateReader` + v4-core `StateLibrary` under the `v4-core/` alias.
- **2026-06-02 (07-01):** `lib/` deps kept OUT of git (gitignored, restorable via `foundry.lock`); reverted a stray prior forge-install submodule registration to match the plan's restore model.
- **2026-06-02 (07-02):** Borrowed V2 internal imports already use the §C `@`-aliases (Plan 01 repointed them at `panoptic-borrowed/`) ⇒ ZERO import rewrites (mn-A confirmed); only third-party prefixes were checked, all matched the §C remapping RHS verbatim. Borrowed via `gh api .../contents?ref=fe55774` raw to keep BUSL/GPL bytes intact.
- **2026-06-02 (07-02):** `IPanopticData` kept to exactly the six §E-verified V2 functions (`dispatch`/`dispatchFrom`-payable/`getAccumulatedFeesAndPositionsData`/`getCurrentTick`/`getTWAP`/`numberOfLegs`); optional `getOracleTicks` deferred to Phase 8. FORK-03 compile-time conformance proof = `forge build` green (interface compiles vs borrowed V2 types), NOT an `IPanopticData(addr)` cast.
- **2026-06-02 (07-02):** Phase 7 lives on branch `feat/keeper-vercel-buildoutput` (07-01 commits reachable only there), not `rescope/somi-leg-donor-transfer` as the prompt/STATE implied; executed + committed 07-02 in place (branching=none).
- **2026-06-02 (07-03):** `BASE_FORK_BLOCK = 46700000` pinned as a Solidity `uint256 constant` in the harness source (M-4, NOT an env var) — archive-verified (`cast code <PoolManager>` = 48020 hex chars at that height; head ≈46789746). PoolManager touched via borrowed `V4StateReader.getSqrtPriceX96(IPoolManager, PoolId)` under cancun — NOT v4-periphery StateView (B-2, not installed). `forge test --fork-url "$BASE_RPC_URL"` 2/2 green = FORK-01 fork-run proof.
- **2026-06-02 (07-03):** bulloak 0.9.2 infers the matching `.t.sol` STRICTLY same-dir as the `.tree` (no subtree search / path flag) ⇒ co-located `BaseForkHarness.tree` + `.t.sol` in `test/fork/` (deviation from the plan's `test/spec/` tree dir) so `bulloak check` exits 0 while the harness stays on the VALIDATION-verbatim `--match-path test/fork/...` path. The pre-existing `test/spec/*.tree` (MacroOracle, SomniaAgentConsumer.*) are un-parseable in 0.9.2 (`/`/`.` in branch text) — out-of-scope for FORK-01, so the full-glob `bulloak check test/spec/*.tree` is NOT a passing gate in this repo.
- **2026-06-02 (07-04):** Our own cCOP/USDC UniV4 pool deployed on the Base fork via `PoolManager.initialize(key, sqrtPriceX96)` (FORK-02). `PoolKeyLib.buildCcopUsdcKey` is the SHARED PoolKey/sqrtPriceX96/ordering builder (mn-C, reused by Plan 05) — `ccopIsCurrency0 = ccop < usdc` decided at RUNTIME; sqrtPriceX96 bakes the 1e12 (18dp/6dp) gap for HUMAN_RATE=4000, and `decodeHumanRate` round-trips to [3000,5000] (mn-3, catches a 1e12 ordering error). **sqrtPriceX96 literals:** ccop=currency0 → `1252707241875239655932` (→4000); ccop=currency1 → `5010828967500958623728276031392126461` (→3999).
- **2026-06-02 (07-04):** Full-range LP (M-1) via a minimal `V4LpHelper` (`IUnlockCallback`→`PoolManager.modifyLiquidity`, ticks rounded to spacing). **Seeded `SEEDED_LIQUIDITY = 1_000_000 ether`** (helper funded `type(uint128).max` of BOTH tokens via `deal`/`mint` before `unlock`) so `StateLibrary.getLiquidity(id) > 0` — **Plan 05 must size its mint FAR below 1,000,000 ether to clear `_validateSolvency`** (07-RESEARCH-DEPLOY §D). NOT `PositionManager.modifyLiquidities` / `SFPMV4.mintTokenizedPosition` (SFPM.initializeAMMPool not run yet).
- **2026-06-02 (07-04):** Inlined the BalanceDelta settlement (`sync`→`transfer`→`settle` / `take`) in `V4LpHelper` instead of v4-core's `test/utils/CurrencySettler.sol` — the latter's `../../src/...` relative imports resolve to a DISTINCT compiler type from the `v4-core/`-remapped `Currency`/`IPoolManager` (Error 9553 invalid implicit conversion). Read path stays `V4StateReader.getSqrtPriceX96` + `StateLibrary.getLiquidity` — NO StateView (B-2). Tree+test co-located `test/instrument/CcopUsdcPool.{tree,t.sol}` (stem+same-dir bulloak rule; plan's `test/spec/` + `.fork.t.sol` would not match).
- **2026-06-02 (08-04):** Streamia is a zero-arithmetic READ. `recordStreamia()` returns `getAccumulatedFeesAndPositionsData(address(this),true,[positionTokenId]).longPremium.rightSlot()/leftSlot()` verbatim, gated to Open (reverts WrongState off Open). The WRAP-03 P1 proof is READ-FIDELITY (recorded == the getter's OWN slots from the SAME call at the SAME tick, RHS pinned to the long slot) — NOT read-vs-formula and NOT a cross-tick read-vs-`OptionBurnt.premiaByLeg` (which diverges: currentTick vs tick=0, so a gating assertEq reverts). Discrimination against a shortPremium/wrong-leg/multiplier wrapper is carried by the pinned-long RHS + non-zero/monotonic + the P1 grep-guard, not the equality alone. Streamia is non-zero ONLY after price-UP swaps (sell token1/cCOP, `zeroForOne=false`) cross into the seller's +2000 OTM chunk band (~2e31, deep up-direction; block advance accrues nothing — Pitfall 2/4). **State-gate split (approved relocation):** pre-Open WrongState proven in 08-04 `streamia.t.sol`; post-Claimed leg relocated to 08-05 `claimResidual.t.sol` because `close()` is UNIMPLEMENTED at wave 4 and its tree lives in 08-05 — pulling it forward would break the Iron Law. 6/6 green on the Base fork; Iron-Law ancestry `891c4e6`→`c233b01`→`2d239a0`.
- **2026-06-02 (08-03):** Open lifecycle LOCKED. `LongGammaWrapper.deposit()` SUPERSEDED the Plan-01 3-arg stub to the 6-arg `deposit(address,uint256,uint256,TokenId,uint128,int24[3])` — the long-leg chunk params (`longId`, `longSize`, `limits`) are CALLER-supplied because the wrapper reaches the pool only via `IPanopticData` and so has no view of the seeded seller chunk; this is the binding entrypoint for Plans 04-07. Custody is the wrapper-owns-everything pattern (P2): `ct.deposit(assets, address(this))` routes 4626 shares to the wrapper, `pool.dispatch(...,false,0)` keys `s_positionBalance` to `msg.sender==wrapper`, the user holds nothing. Custody PROVEN via `numberOfLegs(wrapper)>0` + a length-1 `PositionBalance[]` (`positionSize>0`), NOT the non-existent `positionIdList(address)` getter (RESEARCH correction #1; ROADMAP SC-1 literal superseded). `ct.asset()` (OZ IERC4626) yields the underlying token without a concrete `token0()`/`token1()` call — seam-preserving collateral pull. Over-funded collateral cap (P3): the 18-dp cCOP tracker needs `>=1e18` collateral to back a 1-ether long, so the test deposits `type(uint104).max`. 5/5 green on the live Base fork (WRAP-01 `test_open_wrapperOwnsCollateralAndPosition` + WRAP-02 `test_open_mintsLongGamma`); Iron-Law ancestry verified; swap-seam + P1 grep-guards hold on both files.
- **2026-06-02 (08-02):** Wave-0 harness substrate. `V4SwapHelper` is the WRAP-03 streamia source — premium accrues ONLY via swaps moving `feeGrowthInside` (block advance does nothing, Pitfall 2); it mirrors `V4LpHelper`'s import set + unlock/settle shape exactly, only `manager.swap` (vs `modifyLiquidity`) differs. `LongGammaWrapperBase` extends the 07-05 M-3 pattern AND seeds the seller short BEFORE any long mint (WRAP-02 hard prerequisite: a naked long reverts `NotEnoughLiquidityInChunk()`), size ≥ long, at the wrapper's target chunk, CLOSEABLE (`_closeSellerShort`) before the wrapper's claim to free the pool-wide `maxRedeem` cap (`s_depositedAssets−1`, B4). `addLeg(_width)` is `int24` ⇒ a `uint8` width constant needs an explicit `int24(uint24(...))` cast (untyped literals convert; typed constants do not). The base carries NO behavioral assertions — Plans 03-06 `is LongGammaWrapperBase` and inherit the long counterparty + swap helper + one-leg builder, re-deriving no deploy/seed logic. Smoke-verified live on the Base fork.
- **2026-06-02 (08-01):** Phase-8 contract surface LOCKED interface-first. `ICostMeter.cost(address)->(uint256 cost0,uint256 cost1)` with zero-address⇒(0,0) as a CALLER convention (`_costOf` short-circuits; the interface itself stays minimal). `IPanopticData.getOracleTicks` is the ONLY V2 interface addition (the one deferred from 07-02), `OraclePack` value type resolves via `@types/`. Wrapper swap seam: imports ONLY `IPanopticData`+`ICostMeter` (local) + OZ `IERC4626`/`IERC20` (`@openzeppelin/contracts/interfaces/...`, the proven Phase-7 path) + `@types/TokenId.sol`; NO borrowed concrete (grep-guard==0). Inline `nonReentrant` (no OZ ReentrancyGuard) keeps the import set minimal. `setCostMeter` frozen at first deposit (owner+`Uninitialized`-only) — owner's ONLY lever, not transferable/renounceable in v1. `lastSurviving` documented as last-OBSERVATION (not high-water mark) for Plan-03 erosion semantics. invariants.tree must use bulloak-0.9.2 `when/it` keyword form — bare `invariant_*` branch labels throw `unexpected token` (bulloak always prefixes scaffolded fn names `test_When…`, so Plan 05 writes the `invariant_*`-named `.t.sol` by hand). `forge build` green is the Plan-01 conformance gate (no fork tests yet).

## Roadmap Summary (v2.0)

| Phase | Goal | Requirements | Depends on |
|---|---|---|---|
| **7. Base-fork harness + borrowed Panoptic V2 + pool** | Foundry Base-fork (UniV4 PoolManager), borrowed Panoptic-V2-lite behind `IPanopticData`, our own cCOP/USDC UniV4 demo pool, BUSL NOTICE + bulloak | FORK-01, FORK-02, FORK-03 | — (first) |
| **8. LongGammaWrapper cash-flow** | Wrapper owns the position; deposit collateral → mint long-gamma → streamia accrues (read from contract) → burn → residual from surviving collateral, all involuntary-close branches | WRAP-01, WRAP-02, WRAP-03, WRAP-04 | Phase 7 |
| **9. Premium split + data-cost reimbursement** | `PremiumSplitter` (π_panoptic + μ_LP + φ_data) + ERC-4626 `CapitalRemunerationVault` (mutualized $199) + no-double-count conservation invariant + data-cost-weighted residual | FEE-01, FEE-02, FEE-03 | Phase 8 |
| **10. Oracle surprise route + position sizing** | `MacroOracle` CPI surprise (consensus + σ → `s_t`); `PositionBuilder` sizes notional/strike, `linkage_validated:false` | SIZE-01, SIZE-02 | Phase 8 (oracle modify can parallelize) |

**Deferred (no active phase):** PAY-01 (x402 entry), XCHAIN-01 (Reactive cross-chain), HEDGE-01 (live delta-hedge).
**Coverage:** 12/12 v1 requirements mapped, 0 orphans.

## Process Gates (carry into every phase)

- **evm-tdd `.tree`-before-impl** per phase; named fuzz invariants (`invariant_userClaimsBackedByCollateral`,
  `invariant_residualNeverExceedsHoldings`, `invariant_dataCostConserved`) with a fixed CI fuzz-run floor.
- **Three-step planning-review gate** (CLAUDE.md): every phase PLAN passes Studio-Producer selection →
  Reality Checker + selected domain reviewer → verdict gate, before execution.
- **BUSL NOTICE**: borrowed Panoptic V2 files retain their header; repo NOTICE documents provenance;
  v2 strictly fork/non-production.

## Accumulated Context

### Roadmap Evolution
- **Phase 16 added (2026-06-07):** Shiller-differentiated representativeness (Agent-2 brain branches on economic school). Surfaced by `/gsd:verify-work 15` — SHILLER_MACRO_RISK is *selected* by Agent-1 but Agent-2's geometry is regime-driven/PKE/TEMPLATE (school-agnostic); the school is a label, not a differentiated model. SHILLER-01 (school-branching Shiller geometry) + SHILLER-02 (whole-workflow integration tests across both schools × Colombian macro-risks). Grounded in `research/macro-markets-colombia/`. POST-cornerstone-MVP — does NOT gate the June-11 submission (Phase-15 mint proven onchain; only the demo video remains).

- **Built + proven (this session, on master):** `SomniaAgentConsumer` + `SomniaProbe` + `MacroOracle` —
  live on Somnia testnet (`fetchUint` 775 / `fetchInt` −84); keeper-proxy live on Vercel (key-hidden, via CI).
- **Research (v2.0):** `research/v2-convex-instrument/{SUMMARY,STACK,ARCHITECTURE,PITFALLS}.md`;
  earlier `research/macro-markets-colombia/{RESEARCH,FEASIBILITY-v1,INSTRUMENT-v1}.md`; `DRAFT.md`; `MATH.md`.
- **Decisive pivot (2026-06-01):** Panoptic V2 (UniV4), not V1 (EOL); Base fork (not Celo) with our own
  cCOP/USDC UniV4 demo pool; BUSL-1.1 non-blocker because demo/testnet only (NOTICE).
- **Honest constraints:** borrowed data model (own contracts behind `IPanopticData`, swap-to-canonical-V2
  later); streamia READ from the contract, never re-derived; deposit is upfront COLLATERAL (over-funded cap),
  residual from surviving collateral at actual close (forceExercise/settleLongPremium/liquidation branches);
  two disjoint data costs (mutualized φ_data vs per-position hedge metering) under a conservation invariant;
  CPI→FX linkage `linkage_validated:false` (M1 calibrates later); delta-hedge is a stubbed external keeper in v1.
- **Deployer wallet (testnet):** `0xF3c30FBe0D987116BE592E7c62626b5F4Df90a90` (key in gitignored `contracts/.env`).

## Session Continuity

- **Next action:** Phase 14 **Wave 1 (14-01) is COMPLETE** — the deterministic representativeness substrate (`RepresentativenessLib` pure core + `IRegimeOracle` + `MockRegimeOracle`) is committed + importable (Iron-Law ancestry `ac1a6e1`→`e5254cb`→`04471fa`; REPR-01/02 unit halves green 17/17; whole fork-free tree 114/114). **Next is 14-02 (Wave 2 — executor wiring):** add `resolveFromMandate(HedgeMandate)` + the `ExecutorDecided` event + the **Fix-C sink split** — rename the shared body to `_resolveAndMintAtStrike(..., int24 strike)`, move the `strikeWAD→tick` line up into the public `resolveAndMint` (Phase-13 RUNTIME byte-unchanged), have `resolveFromMandate` pass `structuralStrikeTick(CANONICAL_COP_USD*115/100, ts)` — and MIGRATE the COMPILE-BREAKING Phase-13 sites ATOMICALLY (the onResult `MacroHedgeExecutorDecodeProbe` override rename + its ctor 4→9 forward, plus the three fork ctor sites in `MacroHedgeExecutor.fork.t.sol`). Then 14-03 (Wave 3 — the Polygon-fork mint asserting `strike==360360` + `numberOfLegs>0` + the honesty flag + the `MockRevertingPlatform` LLM-independence). **Locked signatures (do not drift):** `structuralStrikeTick(uint256 humanRate, int24 ts)`, `feasibleOptionRatio(uint256 notional)`, `Regime{Unknown,Tranquil,Stress}`, `MockRegimeOracle.set/setStaleAt`. **Live STRETCH (deferred):** the live `inferToolsChat` representativeness round-trip (`workflow_dispatch`, `14-VALIDATION` Manual-Only). **RPC caution:** the live Alchemy Polygon RPC 429s on rapid fork reruns — 14-03 reuses `fork-state/polygon-panoptic.json` + the cached chain-137 `rpc_storage_caching`; run isolated with short backoff. (Phase 13 was fully complete: PolygonPools 3/3, OperationalCostManagement 10/10, MacroHedgeExecutor fork 7/7 + onResult 4/4.)
- **Stopped at:** Phase 16 context gathered (Shiller-differentiated representativeness)
- **Files:** `.planning/ROADMAP.md` (v2.0), `.planning/REQUIREMENTS.md` (v1 reqs + traceability),
  `.planning/PROJECT.md` (§ Current Milestone: v2.0); `08-01-SUMMARY.md` for the locked signatures.
- **Do not touch:** the `*-M1-donor-transfer-2026-06-01.md` snapshots, the `phases/01..03/` M1 folders.

---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: planning
last_updated: "2026-05-29T23:05:51.828Z"
progress:
  total_phases: 6
  completed_phases: 2
  total_plans: 7
  completed_plans: 7
---

# Project State: abrigo-somnia M1

**Last updated:** 2026-05-29 #3 (scout-addendum fold-in — resolves the two coupled HIGH findings: `234,999` = **transaction** anchor not event bound; event completeness via the scout's structural ratios [total ≈ 2.15 × txs, topic0s 1:1:1, `RequestCreated` ≈ 0.7 × txs ≈ ~165k]; block-presence sufficiency bar replaced; STATS-01 envelope corrected to ~165k; deep-history retention probe; tx-granularity ordering surface; subgraph-compatible paid swap; awaiting planning-review re-run)

## Project Reference

**Project:** abrigo-somnia — Empirical Somnia Cost Panel (M1)
**Core Value:** Which caller-side inequalities in `somnia_cost_extraction.md §6` (rows #1, #2, #4, #5, #6, #7) bind empirically, where are they slack, and what is the realized `executionCost_i` distribution per agent class versus the docs-quoted `{0.03, 0.07, 0.10}` SOMI prices — across all observed proxy implementation versions?
**M1 primary concern:** Capture the `IAgentRequester` **arrival series** (per-event timing, ordering, completeness) robustly enough that M2's arrival-process estimation (NHPP vs Hawkes) + convex-demand verdict is robust. M1 runs neither the estimation nor the verdict.
**Current Focus:** Scout-addendum (2026-05-29) folded in to resolve the two coupled HIGH findings the last review round left — the count-anchor unit mismatch (`234,999` is a **transaction** count, not an event bound) and block-vs-event sparsity (block-presence is the wrong completeness population). The completeness gate is now a three-leg gate (independent tx-anchor + scout structural event-ratio conformance + contiguity proof), the wrong-population sufficiency bar is replaced, the STATS-01 envelope is corrected to ~165k `RequestCreated` events, plus the remaining MEDIUMs (contiguity-independence, deep-history retention probe, tx-granularity ordering surface, sample-vs-skip-bar consistency) and the LOW (subgraph-compatible paid swap). Everything else the reviewers confirmed holds was left untouched. Awaiting planning-review pipeline re-run.

## Current Position

- **Milestone:** M1
- **Phase:** 02 — Topic & Implementation Provenance (COMPLETE, 2/2 plans)
- **Plan:** 02-01 COMPLETE (TOPIC-01, Wave 1) + 02-02 COMPLETE (IMPL-01, Wave 2). Phase 2 done; Phase 3 (INDEX-01) is next.
- **Status:** Ready to plan

**Plan progress (Phase 2):** [██████████] 2/2 plans complete (02-01 TOPIC-01 + 02-02 IMPL-01 done)
**Phase progress:** [███░░░] 2/6 phases complete

## Roadmap Summary

| Phase | Goal | Requirements | Status |
|---|---|---|---|
| 1. Data-Sourcing Gate, Pre-flight Addendum & Schema Foundations | DATA-SOURCE-01 **provisional** free-vs-paid verdict with four numeric sufficiency bars + Ormi free-tier probe rows + machine-checkable `data_sourcing_matrix.yaml` (gates INDEX-01, absorbs RPC-capability probe) + resolve PITFALLS A3/B1/C1 + commit all schema artifacts (arrival-timing fields first-class, ordered on `(block_number, log_index)`) + KPD-09-docs finality semantics | DATA-SOURCE-01, EVENT-01, SHARED-SCHEMA-01 | **Complete (5/5)** |
| 2. Topic & Implementation Provenance | Per-impl ABI resolver + admin-event registration + per-`(impl, topic0)` indexed-dynamic-field enumeration | TOPIC-01, IMPL-01 | **Complete (2/2)** — 02-01 TOPIC-01 + 02-02 IMPL-01 done |
| 3. Subgraph Indexing | Indexer (on DATA-SOURCE-01-selected source) deployed with parity (agreement, mechanism per capability matrix) + **binding completeness gate (independent transaction-coverage anchor on `234,999` txs + scout-addendum structural event-ratio conformance + indexer-internal contiguity proof)** + tx-granularity ordering/granularity verification + liveness + KPD-09-empirical rollback observation | INDEX-01 | Not started |
| 4. Parallel Cost Inputs | Rebate (4a-pre concurrent with Phase 3; 4a-validate after Phase 3) + gas panel + FX series | BYTECODE-01, GAS-01, FX-01 | Not started |
| 5. Panel Materialization | Partitioned Parquet panel with schema-artifact discipline + KPD-11b runtime validation + KPD-PANEL-JOIN 6-way contract | PANEL-01 | Not started |
| 6. Statistics, Arrival-Periodicity Precursor & M2 Ingest Contract | Per-inequality binding + descriptive arrival-periodicity precursor (`(block_number, log_index)`-ordered inter-arrival + `gap_status`-censored `Var(N)/E[N]` dispersion) + executable M2 acceptance test (incl. arrival series) | STATS-01 | Not started |

**Coverage:** 11/11 v1 requirements mapped. No orphans.

## Performance Metrics

- v1 requirements: 11
- Phases: 6
- Success criteria total: 30 (up from 27 — Phase 1 +2 [DATA-SOURCE-01 verdict SC#7, arrival-timing schema SC#8]; Phase 6 +1 [arrival-periodicity precursor SC#6])
- KPDs forward-propagated: **22 active line items** (23 table rows incl. the retired struck-through KPD-20) = 19 original concerns + KPD-PANEL-JOIN (= 20 distinct concerns) + 2 split-induced extras (KPD-09→09-docs/09-empirical, KPD-11→11a/11b; KPD-18 spans Phase 1+Phase 2 but is one row) − 0 (KPD-20 retired into DATA-SOURCE-01, contributes 0). (Prior "21" under-counted the KPD-09/KPD-11 splits.)
- PITFALLS surfaces covered: 5 / 5 (A, B, C, D, E)
- Parallelization waves: 1 (Phase 4 — three independent sub-workstreams; 4a-pre additionally overlapping Phase 3)

## Accumulated Context

### Decisions (locked at revision time)
- **(2026-05-29, 02-02 execution) IMPL-01 discharged — Phase 2 COMPLETE (2/2 plans); Phase 3 INDEX-01 impl-segmentation surface unblocked.** `schemas/impl_history_v1.md` (`schema_version: impl_history_v1`, DESIGN-only — Phase 3 materializes the parquet) locks the 9-column contract (`chain_id` Int64, `implementation_address`/`set_by_tx_hash`/`impl_bytecode_keccak`/`schema_version` Utf8, `impl_first_seen_block`/`impl_last_seen_block`/`verified_at_head_block` UInt64, `set_by_event` Enum[upgraded,deploy_floor,bytecode_backstop]; uint256/hash columns Utf8-only per the DTYPE SCOPE RULE; `impl_last_seen_block` NULL = open head). **Floor row [283417317, ∞)** with `implementation_address = 0x9af59c5683bb8686596b0d56e4f67655c6b73edd`, `set_by_event = 'upgraded'` — a REAL `Upgraded(address)` fires AT the deploy block (corroborated, NOT synthetic); the `deploy_floor` synthetic fallback (`set_by_tx_hash` NULL) is documented for a future deployment lacking a deploy-time event. PANEL-01 join `block_number BETWEEN impl_first_seen_block AND COALESCE(impl_last_seen_block, head)`. **Head-row bytecode backstop** = `keccak(eth_getCode(impl@head))` = `0x13e721a63c4b1c87655c94a9765a602d4b55703a3cc313d07a7bc1a9fe7f3b44` — hashes the **18,507-byte IMPL**, NEVER the **130-byte proxy delegatecall stub** ("hash the impl, never the proxy"); it is the INDEPENDENT logic-change detector that makes Upgraded-only listening safe (a missed Upgraded trips the head-block hash mismatch). **`## Supersedes`:** IMPL-01 registers `Upgraded(address)` ONLY — `AdminChanged` + `BeaconUpgraded` NOT registered (deliberate PITFALLS-A2 override: admin ≠ logic; BeaconUpgraded structurally impossible on this plain EIP-1967 proxy per KPD-17). **PITFALLS A1** ±10-block `unresolved_impl_transition` quarantine encoded as `is_quarantined(upgrade_block, candidate_block, deploy_block=283417317) == (upgrade_block > deploy_block) AND abs(candidate_block - upgrade_block) <= 10` — the deploy-block Upgraded establishes the floor and is NOT quarantined (else genesis requests censored); only a strictly-post-deploy Upgraded triggers the band (empty for M1). `tests/test_impl_history.py` (211 lines, **7 tests**: floor row, backstop value + "never the proxy"/130-byte/18,507-byte doc assertions, A1 predicate excludes deploy block + applies ±10 post-deploy, Upgraded-only Supersedes note, Upgraded topic0 reuses the Plan 02-01 keccak harness) — synthetic in-Python frame, NO parquet read, no live RPC in CI. `02-FORWARD-NOTES.md` records three cross-phase hand-offs: (1) `responses` table is **state-fill-only** via `getRequest(uint256)` selector `0xc58343ef` (no `ResponseReceived` event) → closes the EVENT-01 open question, Phase-3 INDEX-01 must state-fill or defer; (2) `CommitteeDepositFailed` is a **structural invariant** (fires 1:1 on every finalization, budget restored) → BYTECODE-01/Phase-4 rebate-residual prior, NOT an error to filter; (3) **$75/mo Ormi Production no-auto-spend payment protocol** → Phase-3 INDEX-01 (notify user with entity-count evidence, user confirms before provisioning, disable free-tier auto-upgrade at signup). Full suite **56 green** (49 baseline + 7 IMPL-01). Three atomic commits `db52d50` (feat design)/`a321c52` (test)/`f465753` (docs forward-notes). IMPL-01 requirement marked complete; ROADMAP Phase-2 row → Complete (2/2). No deviations. (gsd-tools `state advance-plan` again failed on this STATE.md heading format — Current Position/status/progress-bars/Roadmap-Summary-row/decision/session edited manually; `state update-progress` [100%, 7/7, completed_phases 2] + `requirements mark-complete IMPL-01` succeeded via tooling; `roadmap update-plan-progress 02` computed Complete but did NOT rewrite the Progress-Table row — row edited by hand to 2/2 Complete.)
- **(2026-05-29, 02-01 execution) TOPIC-01 discharged — per-`(impl, topic0)` ABI resolver committed; Phase 3 INDEX-01 decode surface unblocked.** `schemas/topic0_map_v1.json` (`schema_version: topic0_map_v1`, 5 resolver rows) resolves all 3 observed `IAgentRequester` topic0s by EXACT keccak-256 match against the blob-SHA-pinned ABI `e15d4e94…`: `RequestCreated(uint256,uint256,uint256,bytes,address[])`=`0xb623…`, `RequestFinalized(uint256,uint8)`=`0x65db…` (**enum ResponseStatus → uint8** — the single most likely keccak mistake, test-pinned; enum-name form asserted NOT to collide), `CommitteeDepositFailed(uint256,uint256)`=`0x5c09…`. The 2 defined-but-unobserved events (`SubcommitteePaid`=`0x1586…`, `NativeTransferFailed`=`0xa5b0…`) registered cost-zero to prevent a future `unresolved_abi` false positive (RESEARCH Open Q2). **`field_layout_hash`** is `keccak('Name(type:I|D,…)')` in SOURCE order with `:I`/`:D` suffixes (independent key from topic0) — RequestCreated=`0x9b58ba75…`, the other four derived from the ABI and re-recomputed in tests. **SC#5 confirm-and-document (NOT a migration):** every indexed arg across all 5 events is a value type (uint256/address) → `INDEXED_DYNAMIC_FIELD_COUNT==0`, `SCHEMA_V2_BUMP_REQUIRED is False` → no `event_schema_v1.md → v2` bump. `decode_status` enum domain `{resolved, unresolved_abi, unresolved_impl_transition}` (all 5 rows `resolved`). `schemas/unresolved_topics_v1.md` documents the KPD-06 quarantine parquet design (columns/dtypes, escalation order, `pct_logs_unresolved = Σ observed_count / total_proxy_logs × 100` ship gate `< 1.0`, **expected 0.0% for M1**; the total-log denominator is an INDEX-01/Phase-3 output — Phase 2 fixes the numerator pipeline). Keccak tooling `eth-hash[pycryptodome]` self-proves against the EIP-1967 `Upgraded(address)` constant `0xbc7cd75a…` (and `test_keccak_is_not_sha3_256` proves NIST sha3_256 diverges to `0x0e51260f…`) — **NEVER `hashlib.sha3_256`**. Doc-accuracy fix: `e15d4e9` relabelled a **git BLOB SHA** (not commit; `git hash-object references/interfaces/IAgentRequester.sol` reproduces it) in CLAUDE.md + PROJECT.md; pin value unchanged. `references/interfaces/PROVENANCE.sha256` pins the blob SHA. Cross-phase finding for Phase 3 / EVENT-01-v-next: the `responses` child table is **state-fill-only** (no `ResponseReceived` event; `getRequest().responses` only); `CommitteeDepositFailed` is a structural invariant (fires 1:1 on every finalization), relevant to BYTECODE-01's rebate residual. 12 topic-resolution tests; full suite 49 green. Three atomic commits `0c0d035`/`393d457`/`648c7e8`. TOPIC-01 requirement marked complete; ROADMAP Phase-2 row → In Progress (1/2). No deviations. **02-02 (IMPL-01, Wave 2) remains.** (gsd-tools `state advance-plan`/`record-metric`/`record-session`/`add-decision` again did not match this STATE.md heading format — position/status/progress-bars/decision/session edited manually; `update-progress` [86%, 6/7], `roadmap update-plan-progress 02` [In Progress], `requirements mark-complete TOPIC-01` succeeded via tooling.)
- **(2026-05-29, 01-05 execution) SHARED-SCHEMA-01 discharged — Phase 1 COMPLETE (5/5 plans).** `schemas/abrigo_cost_panel_intersection_v1.json` (JSON-Schema **draft 2020-12**, `check_schema`-valid; 11 columns each carrying an `x-polars-dtype` annotation so consumers map dtypes WITHOUT inferring — PITFALLS E1; `required` = 7 NOT-NULL columns; `x-schema-anchor: v1-K_AI-anchored` + `x-intersection-pk` pinned machine-readably) + `schemas/abrigo_cost_panel_intersection_v1.md` (prose: source-agnostic **Supersedes** note — a free-vs-paid source swap does NOT bump the schema; verbatim DTYPE SCOPE RULE; PK `(chain_id, tx_hash, request_id)`; **v1-K_AI-anchored breakage budget** — stable contracts vs what version-bumps vs explicit cross-version read filter PITFALLS E4) + `schemas/abrigo_cost_panel_k_ai_extensions_v1.md` (K_AI **SIDECAR** joined on `(chain_id, tx_hash)`; `request_id_kai` Utf8-only, `per_agent_budget_native` wei Utf8/Decimal(38,0); responses stay a SEPARATE child table per KPD-03/KPD-PANEL-JOIN; three agent classes json-fetch 0.03 / llm-inference 0.07 / llm-parse-website 0.10). **request_id / request_id_kai are pl.Utf8 ONLY** (78-digit uint256 > Decimal128's 38 — proven empirically: `pl.Series([2**256-1], dtype=pl.Decimal(38,0))` raises); wei amounts may be Utf8 OR Decimal(38,0). Dtypes consistent with EVENT-01 (`block_number` UInt64 NOT NULL, `block_ts_utc` coarse-secondary, uint256-not-Int64 in both files — N-drift fix test-enforced). Two new test modules (6 tests: draft-2020-12 validity, RUNTIME polars dtype round-trip, required-non-nullable; PK/join-key/EVENT-01 dtype consistency). Full Phase-1 suite green (**37 tests**). Three atomic commits `564ebcd`/`9d7fe57`/`0917233`. SHARED-SCHEMA-01 requirement marked complete → read-ready for PANEL-01 (Phase 5). No deviations.
- **(2026-05-29, 01-03 execution) DATA-SOURCE-01 discharged — provisional free-vs-paid verdict LANDED; Phase 3 INDEX-01 unblocked.** `research/data_sourcing_matrix.yaml` (fixed 7-key schema `source,capability,value,threshold,pass,source_url,utc_fetch_ts`; 18 provenance-stamped rows) + `research/DATA_SOURCING.md` recommendation memo. **Verdict: `ormi-free-developer` $0, provisional, confirmed_in phase-3.** A free pick commits no spend → **no sign-off required** (`sign_off_required: false`); a FORCED paid crossing (Ormi Production $75/mo if the 300k-entity free cap is exceeded, or GetBlock Starter $39/mo archive → INDEX-01 sub-plan re-author) is surfaced as the `user_setup` item and gated on explicit sign-off BEFORE provisioning — NO auto-spend. Disable Ormi free-tier auto-upgrade at signup regardless (Pitfall 5). All paid options < $390 ceiling EXCEPT the explicitly-flagged GetBlock Pro $399. **Leg-(b) tolerance band = raw round-half-up Wilson 95% CI [0.628, 0.790] (basis `wilson_95ci_n116`, n=116, point 83/116≈0.716); `cross_epoch_widening: deferred_to_phase_3` is a SEPARATE sibling note, NEVER folded into the interval.** Four sufficiency bars encoded; verdict logic free-sufficient iff ALL FOUR pass (`test_free_sufficient_iff_all_pass` proves flipping any single bar to fail flips to paid). **SC#7(iv) is a REAL reconciliation** — x402 free-tier discipline = paid-API BUDGET gate (modeled-not-paid) vs DATA-SOURCE-01 = COMPLETENESS-sufficiency gate; paying for real SOMI data is a JUSTIFIED DEPARTURE (real-or-nothing), not a violation; free-vs-paid does NOT change source-agnostic SHARED-SCHEMA-01 intersection columns; abrigo-analytics no-oracle + stop-gap findings cross-checked (both stand). Supersedes note records the 36.3M-block / deploy-283417317 / Ormi-preferred-not-mandatory corrections. Three deferred items pinned (leg-b tolerance, RequestCreated column def → TOPIC-01, cross-epoch drift → Phase-3 stratified). Three atomic commits `8c1dd94`/`abb9f0f`/`d731e9f`; full suite green (31 tests).
- **(2026-05-29, 01-04 execution) EVENT-01 + KPD-11a discharged; foundational schema LOCKED.** `schemas/event_schema_v1.md` fixes the load-bearing dtypes: **uint256 id columns (`request_id`/`agent_id`) are `pl.Utf8` ONLY** — NEVER Int64/UInt64 AND NEVER `Decimal(38,0)` (uint256 = up to 78 digits > Decimal128's 38; `pl.Series([2**256-1], dtype=pl.Decimal(38,0))` raises `OverflowError`, verified empirically on polars 1.41.2). `Decimal(38,0)` PERMITTED only for bounded wei amounts (`per_agent_budget_native`, `gross_cost_native` ≈ 17–18 digits). The verbatim DTYPE SCOPE RULE is in the DDL. **Arrival key `(block_number, log_index)`; `block_ts_utc` COARSE SECONDARY** (whole-second source); dedup `(chain_id, tx_hash, log_index)`. **responses child table reserved** PK `(chain_id, tx_hash, log_index, member_index)` FK `(chain_id, tx_hash)→requests`; population path (event-derivable vs state-readable) **OPEN for Phase 2** — no `ResponseReceived` event in interface. **N4 naming:** `requestId→request_id`, `agentId→agent_id` (snake_case parquet columns; ABI camelCase only for raw event). KPD-18 columns `agent_class_keccak`/`agent_class_string` reserved (recovery LIKELY `agentId→class` registry, NOT keccak→string). Three topic0 hashes recorded as domain, **roles UNASSIGNED** (TOPIC-01 keccak-resolves vs pinned `e15d4e9`; 3-topic/1120-byte `0xb623…` is leading RequestCreated candidate, NOT hard-coded). KPD-11a `batch_manifest_v1.yaml` + `.schema.json` (draft 2020-12, 11 required fields) committed — **Phase 5 / KPD-11b invokes the validator at write time**. `test_uint256_not_int64` is a RUNTIME polars round-trip (not grep-only). Full suite green (24 tests). **EVENT-01 requirement marked complete; DATA-SOURCE-01 stays open** (matrix + verdict are 01-03's deliverables).
- **(2026-05-29, 01-02 execution) Three pre-flight verdicts discharged with primary-source provenance.** KPD-17 **HAPPY PATH** — impl `0x9AF59C5683bb8686596B0D56e4F67655C6B73EdD` is plain EIP-1967 (BEACON_SLOT `0xa3f0…3d50`, IMPLEMENTATION_SLOT, EIP-2535 diamond `0xc8fc…c131c` all `0x` empty); **IMPL-01 tracks ONLY the proxy EIP-1967 slot**, ROADMAP SC#2a satisfied, SC#2b re-scope does not fire. KPD-09-docs **MEDIUM branch-(a) provisional** — Somnia docs assert sub-second PBFT finality but never irreversibility → `safe_block_depth = 1` provisionally accepted; KPD-09-empirical (≥1-hour rollback obs) deferred to Phase 3. KPD-19 **candle-CLOSE** — CoinGecko OHLC ts marks candle close; FX LOCF MUST subtract candle duration (else 1-candle look-ahead poisons inequality #1); free hourly via `market_chart/range` (OHLC hourly is paid-only). Artifacts: `beacon_diamond_probe.md`, `somnia_finality_semantics.md`, `coingecko_convention.md`, `adapters/fx/coingecko_config.yaml` (`timestamp_convention: close`), `tests/fixtures/fx_candle_convention.py` (stdlib-only no-look-ahead helper for Phase 4c, encodes ROADMAP Phase 4 SC#4). N3 closed: all three scout files appended to `PROVENANCE.sha256` (set-equality test holds at 7 `.md` files; `sha256sum -c` OK). DATA-SOURCE-01 pre-flight surface complete.
- **(2026-05-29, 01-01 execution) Wave 0 test infra + N2 fixture contract locked.** uv-managed Python `>=3.12` (this repo's floor — deliberately NOT abrigo-analytics' `>=3.13`, recorded in pyproject comments to block a future false-parity edit) + pytest harness (dev group pytest/jsonschema/pyyaml/polars, no watch-mode). The N2 contract for ALL of Plans 01-01..01-05: shared helpers are `@pytest.fixture`s — `scout_dir`/`schemas_dir`/`research_dir` return `Path`, `load_yaml`/`load_json`/`read_text` return callables — consumed as test PARAMETERS, not module imports.
- **(2026-05-29, 01-01 execution) KPD-16 discharged.** Scout archive consolidated under `.planning/scout/2026-05-29/` (canonical; the never-committed upstream `2026-05-25/` path reconciled in `.planning/scout/README.md`), every fact provenance-stamped (`source_url` + `utc_fetch_ts`), sha256-pinned via `PROVENANCE.sha256`. `test_scout_archive.py::test_provenance_manifest_valid` is a `.md`-scoped SET-EQUALITY check, so Plan 02's three appended files (`beacon_diamond_probe.md`, `somnia_finality_semantics.md`, `coingecko_convention.md`) MUST be re-pinned or the test fails by design. Reusable probe tooling (`probes/somnia_rpc.py`, `probes/blockscout.py`) is stdlib-only and network-touching only in `__main__` (never in CI).
- **(2026-05-29, 01-01 execution) INDEX-01 startBlock RESOLVED = 283417317.** Recorded in `deployment_block.md` from the proxy creation tx; backfill span corrected to ~36.3M blocks (~36,287 getLogs windows). Supersedes the ROADMAP/PROJECT "~320M blocks / ~270 days" figure (conflated chain-age-since-TGE with the ~42-day-old proxy). On-chain event roles recorded as SHAPES only — scout role labels likely INVERTED (the 3-topic/1120-byte `0xb623…` matches `RequestCreated`); TOPIC-01 (Phase 2) keccak-resolves definitively against pinned commit `e15d4e9`.
- **(2026-05-29) DATA-SOURCE-01 folded into Phase 1 as headline deliverable, GATING Phase 3 INDEX-01.** Chosen over a new Phase 0 because Phase 1 already hosts the chain-capability probes and DATA-SOURCE-01 absorbs the RPC-capability probe; a new phase would fragment the "probe reality before building" coherence and force renumbering of all six phases. Deliverable `research/DATA_SOURCING.md` (verified capability matrix + free-vs-paid verdict, each claim with `source_url + utc_fetch_ts`, coherence-checked against x402 SUMMARY.md + somnia_cost_extraction.md).
- **(2026-05-29) Former KPD-20 RPC-capability probe absorbed into DATA-SOURCE-01's capability matrix** — `eth_getBlockReceipts` availability is now a provenance-stamped matrix row, not a free-standing KPD. Phase 3 SC#2 parity mechanism is conditional on that row. No orphan KPD remains.
- **(2026-05-29) Arrival-periodicity primacy reflected in two phases:** Phase 1 EVENT-01 arrival-timing fields (`block_ts_utc`, `block_number`, `log_index` ordering, gap-detection metadata) made first-class non-nullable load-bearing columns (new Phase 1 SC#8); Phase 6 STATS-01 gains a descriptive arrival-periodicity precursor (inter-arrival series + intensity-over-time + `Var(N)/E[N]` overdispersion + periodicity, new Phase 6 SC#6). Descriptive ONLY — no NHPP/Hawkes fit, no convex-demand verdict (both M2). M2 ingest acceptance test (KPD-13 / Phase 6 SC#2) now loads the arrival series.
- **(2026-05-29) M1→M2 consumer contract updated** from "parametric fit" to "arrival-process estimation + convex-demand verdict" throughout ROADMAP + REQUIREMENTS.
- **(2026-05-29 #2, HIGH-1) Block-cadence timing corrected + arrival-ordering key fixed.** Replaced "block-level ~1 s timing" everywhere with native ~72 ms/block cadence (scout-measured: 316M blocks ≈ 1.2M/day, ~14× finer than 1 s). DATA-SOURCE-01 records whether `block.timestamp` is whole-second or sub-second; if whole-second, up to ~14 events share a timestamp so **`(block_number, log_index)` is the arrival-ordering key, `block_ts_utc` is a coarse secondary**. EVENT-01 + the Phase 6 arrival series carry ordering on `(block_number, log_index)`.
- **(2026-05-29 #2, HIGH-2/HIGH-3) Completeness gate reframed + numeric sufficiency bars.** DATA-SOURCE-01's pre-INDEX verdict is now a **PROVISIONAL source selection** ("completeness re-confirmed in Phase 3"); the ~1% Ormi-vs-RPC sample bounds **agreement, not completeness** (shares the 1000-block cap). **[Superseded in part by #3 below — see the count-anchor unit correction.]**
- **(2026-05-29 #3, HIGH-1+HIGH-2 — scout-addendum fold-in) Count-anchor unit mismatch + block-vs-event sparsity resolved.** Using `.planning/scout/2026-05-29/event_count_addendum.md`: `234,999` is a **transaction** count (exact, independent, getLogs-cap-free), re-labelled the **transaction-coverage anchor** — NOT an event bound. Phase 3 SC#6 is now a **three-leg gate**: (a) reconcile the indexer's distinct proxy-targeted `tx_hash` count against `234,999`; (b) **event completeness via the scout's structural ratios** (total ≈ 2.15 × txs; topic0s 1:1:1; `RequestCreated` ≈ 0.7 × txs ≈ ~165k; response `0xb623…` 1–5×/tx) within a plan-phase tolerance, as a sanity check — this **REPLACES the "≥99.9% blocks present" bar** (block-presence measured the wrong population at ~0.0016 events/block); (c) the indexer-internal contiguity proof, which proves only host cursor advancement and is completeness-bearing **only when conjoined with the tx-anchor (a)** (a host can advance its cursor over under-indexed blocks). The exact `RequestCreated` lifetime count is an **INDEX-01 output** cross-checked against (a)+(b), not a pre-known constant; no cheap EXACT independent event-count oracle exists pre-indexing. The Phase-1 sufficiency bars (SC#7(ii)) likewise switch from block-presence to distinct-tx-coverage + structural-ratio conformance, restricted to what the ~1% sample can test; the no-contiguous-skip check is demoted to the Phase-3 full-range contiguity proof. STATS-01's envelope corrected from "5,000 under 234,999" to the **~165k `RequestCreated`-event envelope** (clears the 5,000 floor by ~30×).
- **(2026-05-29 #3, MEDIUM) Contiguity-independence + deep-history retention + tx-granularity ordering + sample-vs-skip consistency.** `_meta`/`COUNT(DISTINCT block_number)` cursor contiguity proves only host belief, not event emission — completeness-bearing only with the tx-anchor (SC#2 + SC#6 leg c). Ormi retention is answered by an **explicit deep-history probe at the deployment block** (~320M blocks back, confirm non-empty correct response), a **distinct row + distinct method** from the throughput-only backfill-throttle probe. Phase 3 SC#7 ordering is testable at **tx granularity** (a single request tx emits the request pair + 1–5 response events in one block with ascending `log_index`); multi-`RequestCreated`-block verification is not a hard stratification requirement (sparsity). The no-contiguous-skip bar is evaluated by the Phase-3 contiguity proof, NOT the ~1% Phase-1 sample.
- **(2026-05-29 #3, LOW) Paid swap constrained to a subgraph-compatible host** (paid Ormi/Protofire) to preserve the SC#2 parity mechanism + KPD-08 mapping surface; a non-subgraph paid archive instead triggers an INDEX-01 **sub-plan re-author** (pre-costed), not a phase re-decomposition. Scout addendum recorded as a KPD-16 provenance row (DATA-SOURCE-01 matrix + PROJECT.md scout table).
- **(2026-05-29 #2, HIGH-4) Ormi free-tier probe rows + pre-costed paid fallback.** Capability matrix now carries explicit Ormi free-tier rows: retention/history depth (to deployment block?), backfill throttle / rate cap, subgraph-deployment permission (deploy vs read-only) — each by vendor doc OR bounded live backfill-rate probe. Free-tier selection is provisional-pending-Phase-3-backfill-confirmation with the paid alternative **pre-costed**, so a Phase-3 backfill failure swaps in the paid source without phase re-decomposition.
- **(2026-05-29 #2, MEDIUM) Phase 3 SC#7 ordering/granularity verification** (multi-event-block `(block_number, log_index)` tuple-for-tuple vs RPC, `block_ts_utc` exact match); **Phase 6 SC#6 `gap_status` column** (`observed`/`indexer_gap_censored`/`source_gap`) with `Var(N)/E[N]` gap-censoring; **KPD-14 gap threshold set in blocks** not days; **`eth_getBlockReceipts`-absent fallback** weakens completeness to contiguity proof (Phase 3 SC#2) and feeds back to the free-vs-paid verdict; **Phase 1 SC#7(iv) coherence check is a real budget-vs-completeness reconciliation**, not a one-line citation.
- **(2026-05-29 #2, LOW) Machine-checkable `research/data_sourcing_matrix.yaml`** (fixed schema) emitted alongside `DATA_SOURCING.md`; **KPD line-item count corrected 21 → 23**; **wall-clock honesty** — the live Ormi backfill-rate probe is the unbounded-by-doc-read tail most likely to consume the Phase 1 1.5–2× allowance.
- Phase 4a (BYTECODE-01) is split across the Phase 3 boundary: 4a-pre (Tier-B decompilation) runs concurrently with Phase 3; 4a-validate (Tier-C empirical residual + Tier-B holdout) depends strictly on Phase 3 INDEX-01.
- KPD-09 split into KPD-09-docs (Phase 1 — doc-side finality semantics) + KPD-09-empirical (Phase 3 — ≥1-hour rollback observation).
- KPD-11 split into KPD-11a (Phase 1 — batch manifest schema artifact) + KPD-11b (Phase 5 — runtime JSON-schema validation on every batch write).
- KPD-18 split into Phase 1 (schema column reservation for `agent_class_keccak`/`agent_class_string`) + Phase 2 (per-`(impl, topic0)` enumeration in resolved ABIs).
- KPD-02 (`gas_payment_bucket`) ownership: authored in 4a-validate, consumed by Phase 5 + Phase 6.
- KPD-PANEL-JOIN (PANEL-01 6-way join contract: join order, multiplicity, responses-sidecar rule, `decode_status` propagation) in Phase 5.
- M1 reference environment defined at top of ROADMAP.md (≥32 GB RAM, NVMe, Linux, polars ≥1.20); all read-time SLOs assert against M1 env not M2 env.
- Phase 5 SC#5 uses local `tests/fixtures/abrigo_analytics_consumer_emulation.py`; cross-repo coordination is out of M1 acceptance scope.
- Phase 1 KPD-16 ordered FIRST inside Phase 1 (scout-artifact integrity surfaced before any other Phase 1 work).
- Phase 1 sizing acknowledged: 1.5–2× per-phase median wall-clock allowance (DATA-SOURCE-01 + 7 KPDs, 8 success criteria, 3 PITFALLS pre-conditions). Not split further.
- STATS-01 re-entry rule on `Upgraded` events (MEDIUM-4): conditional re-entry into Phase 2 + Phase 4a-validate before Phase 6 STATS-01 is valid for blocks ≥ upgrade_block.
- KPD-12 extended (MEDIUM-1) to cover total-count shortfall (<5,000 RequestCreated rows) in addition to per-class shortfall.

### TODOs (carried into plan-phase)
- Phase 1 plan-phase executes DATA-SOURCE-01: produce `research/DATA_SOURCING.md` (prose) + `research/data_sourcing_matrix.yaml` (machine-checkable, fixed schema) — capability matrix incl. `eth_getLogs` 1000-block cap, `eth_getBlockReceipts` probe, median block cadence + `block.timestamp` whole-second flag, Ormi retention/throttle/deployment rows; four numeric sufficiency bars; PROVISIONAL free-vs-paid verdict with pre-costed paid fallback; real budget-vs-completeness coherence check vs x402 SUMMARY.md + somnia_cost_extraction.md. Pin `N` (no-skip range) + `gap_block_threshold` in plan-phase. INDEX-01 plan-phase blocks on this verdict; the binding completeness re-confirmation is a Phase-3 deliverable (contiguity proof + count anchor), not Phase 1.
- Phase 1 plan-phase must resolve the contract deployment block from creator EOA `0x320362C7…fdE88936`'s earliest tx — needed for INDEX-01 manifest `startBlock`.
- Phase 1 plan-phase executes KPD-09-docs (doc-side finality verdict); Phase 3 plan-phase executes KPD-09-empirical (≥1-hour rollback observation).
- Phase 1 plan-phase commits EVENT-01 DDL with arrival-timing fields as first-class non-nullable load-bearing columns + ordering/dedup guarantees (arrival-periodicity primacy).
- Phase 3 plan-phase may sub-split into 3a (manifest scaffold + admin-event handlers, parallel with Phase 2) and 3b (business-event handlers + agreement parity + liveness + rollback + **binding completeness gate: contiguity proof + Blockscout count anchor** + ordering/granularity verification) per MEDIUM-2 slack; parity mechanism selected by the DATA-SOURCE-01 capability matrix (`eth_getBlockReceipts`-absent fallback weakens completeness assurance and feeds back to the free-vs-paid verdict).
- Phase 4a-validate plan-phase must close out Tier-B as `tier_b_status = 'inconclusive'` if both decompilers return unusable output OR holdout fails ±2% threshold (already committed; not an open question).
- Phase 5 plan-phase must implement the KPD-PANEL-JOIN 6-way join contract.
- Phase 6 plan-phase produces the descriptive arrival-periodicity precursor (`(block_number, log_index)`-ordered inter-arrival + intensity-over-time + `gap_status`-censored `Var(N)/E[N]` overdispersion + periodicity, gap-audited via KPD-14 block-count threshold) and the M2 ingest test that loads the arrival series; reports `impl_segments_observed = 1` collapsed-dimension clause when no `Upgraded` event observed.

### Blockers
None at roadmap-revision time. (Phase-3 INDEX-01 is gated by DATA-SOURCE-01, but that gate is internal to the roadmap ordering, not an external blocker.)

### Open Questions
ROADMAP.md "Open Questions Flagged for Plan-Phase" — 2 items: (1) deployment-block resolution; (2) plan-phase parameter pins `N` (no-skip range, default 1000) + `gap_block_threshold` (calibration values, not design choices). The former RPC-parity open question's residual is now a DATA-SOURCE-01 capability-matrix deliverable.

## Session Continuity

**Last action (2026-05-29):** Executed Plan 02-02 (Wave 2 — IMPL-01). Three atomic commits — `db52d50` (feat: `impl_history_v1.md` 9-column design — floor row `[283417317, ∞)` corroborated by the deploy-block Upgraded, IMPL-not-proxy bytecode backstop `0x13e721a6…`, Supersedes-A2 Upgraded-only note, A1 ±10-block quarantine), `a321c52` (test: `tests/test_impl_history.py` — 7 fixture tests, synthetic frame, `is_quarantined()` predicate, reuses 02-01 keccak harness), `f465753` (docs: `02-FORWARD-NOTES.md` — responses state-fill-only / CommitteeDepositFailed invariant / $75/mo Ormi no-auto-spend protocol). `uv run pytest tests/` green (**56 tests**; `test_impl_history.py` 7 passed). **Phase 2 COMPLETE (2/2 plans).** IMPL-01 requirement marked complete; ROADMAP Phase-2 Progress-Table row → Complete (2/2). SUMMARY at `.planning/phases/02-…/02-02-SUMMARY.md`. No deviations. Commits are LOCAL only — push to `origin` deferred to the user. (Tooling note: `state advance-plan` again did not match this STATE.md heading format — Current Position/status/progress-bars/Roadmap-Summary-row/decision/session edited manually; `state update-progress` [100%, 7/7], `requirements mark-complete IMPL-01` succeeded; `roadmap update-plan-progress 02` reported Complete but did not rewrite the Progress-Table row — fixed by hand.)
**Prior action (2026-05-29):** Executed Plan 02-01 (Wave 1 — TOPIC-01). Three atomic commits — `0c0d035` (chore: keccak dep + ABI blob-SHA provenance + topic test harness), `393d457` (feat: `topic0_map_v1.json` resolver + `unresolved_topics_v1.md` design + tests), `648c7e8` (docs: blob-vs-commit `e15d4e9` fix in CLAUDE.md + PROJECT.md). `uv run pytest tests/ -q` green (**49 tests**; `test_topic_resolution.py` 12 passed). TOPIC-01 requirement marked complete; ROADMAP Phase-2 row → In Progress (1/2). SUMMARY at `.planning/phases/02-…/02-01-SUMMARY.md`. No deviations. (Execution note: the three task commits were authored in the prior session 18:38–18:42 -0400; this session verified them against the full success criteria, confirmed the suite green, and completed the wrap-up — SUMMARY + STATE + ROADMAP + requirement marking.) Note: `state advance-plan`/`record-metric`/`record-session`/`add-decision` again did not match this STATE.md's heading format — position/status/progress-bars/decision/session edited manually; `state update-progress` (86%, 6/7), `roadmap update-plan-progress 02` (In Progress), and `requirements mark-complete TOPIC-01` succeeded via tooling.
**Next action:** Phase 2 complete (TOPIC-01 + IMPL-01 discharged). Run `/gsd:plan-phase 3` for INDEX-01 (subgraph indexing) — it consumes `topic0_map_v1.json` (decode surface), `impl_history_v1.md` (impl segmentation), `unresolved_topics_v1.md` (quarantine), and the three `02-FORWARD-NOTES.md` hand-offs (responses state-fill-only, CommitteeDepositFailed invariant, $75/mo Ormi no-auto-spend payment protocol). Push to `origin` deferred to the user.
**Prior action (2026-05-29):** Executed Plan 01-02 (Wave 2 — pre-flight verdicts). Three atomic commits — `54d3541`, `25dcc10`, `afbcb6a`. 13 tests green; KPD-17/09-docs/19 verdicts discharged. SUMMARY at `.planning/phases/01-…/01-02-SUMMARY.md`.
**Prior action (2026-05-29):** Executed Plan 01-01 (Wave 0 + KPD-16). Three atomic commits — `6818fd8`, `66e3dc9`, `4991329` (+ `002ae74` docs). 6 tests green; KPD-16 discharged; INDEX-01 startBlock 283417317.

**Prior action:** Roadmap re-revision 2026-05-29 #3 — scout-addendum fold-in resolving the two coupled HIGH findings (count-anchor unit mismatch + block-vs-event sparsity) + remaining MEDIUMs/LOW. `234,999` re-labelled as the transaction-coverage anchor; Phase 3 SC#6 rebuilt as a three-leg gate (tx-anchor + scout structural event-ratio conformance + contiguity proof); the "≥99.9% blocks present" sufficiency bar replaced by distinct-tx-coverage + structural-ratio conformance (Phase 1 SC#7(ii) + Phase 3 SC#6); STATS-01 envelope corrected to ~165k `RequestCreated` events; contiguity-independence stated (SC#2 + SC#6); deep-history retention probe split from the throttle probe (SC#7(i)); tx-granularity ordering surface (SC#7); subgraph-compatible paid swap (SC#7(iii)); scout addendum logged as a KPD-16 provenance row. ROADMAP.md + PROJECT.md + REQUIREMENTS.md + this STATE.md edited surgically in place; coverage still 11/11; all #2 fixes + six prior HIGH-fixes untouched.
**Next action:** Re-run the full planning-review pipeline (Studio Producer selector → parallel Reality Checker + dynamic primary reviewer) against the hardened `.planning/ROADMAP.md` + PROJECT.md + REQUIREMENTS.md. If both PASS, proceed to commit + `/gsd:plan-phase 1`.
**Resume hint:** Read `.planning/PROJECT.md` (amended: DATA-SOURCE-01, arrival-periodicity banner, sharpened EVENT-01/STATS-01) → `.planning/REQUIREMENTS.md` (11 v1 reqs, traceability 11/11) → `.planning/ROADMAP.md` (revised) → this file.

---
*State initialized: 2026-05-25 alongside ROADMAP.md.*
*State revised: 2026-05-25 after planning-review NEEDS WORK iteration #1.*
*State revised: 2026-05-29 — DATA-SOURCE-01 gate + arrival-periodicity sharpening; coverage 11/11.*
*State revised: 2026-05-29 #2 — DATA-SOURCE-01 rigor hardening (native ~72 ms/block timing + `(block_number, log_index)` ordering key; provisional source selection + Phase-3 contiguity/count-anchor completeness gate; four numeric sufficiency bars; Ormi free-tier probe rows + pre-costed paid fallback; `gap_status` censoring + block-count gap threshold; machine-checkable capability matrix; KPD count 23). Coverage 11/11; six prior HIGH-fixes preserved.*
*State revised: 2026-05-29 #3 — scout-addendum fold-in (resolves the two coupled HIGH findings + remaining MEDIUMs/LOW): `234,999` = transaction-coverage anchor (not event bound); Phase 3 SC#6 three-leg gate (tx-anchor + scout structural event-ratio conformance [total ≈ 2.15 × txs, topic0s 1:1:1, `RequestCreated` ≈ 0.7 × txs ≈ ~165k] + contiguity proof); block-presence sufficiency bar replaced by distinct-tx-coverage + structural-ratio; STATS-01 envelope corrected to ~165k events; contiguity-independence + deep-history retention probe + tx-granularity ordering surface + sample-vs-skip consistency (MEDIUM); subgraph-compatible paid swap (LOW); scout addendum as KPD-16 provenance row. Coverage 11/11; all #2 fixes + six prior HIGH-fixes intact.*

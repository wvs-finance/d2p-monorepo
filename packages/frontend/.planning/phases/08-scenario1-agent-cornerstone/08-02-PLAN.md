---
phase: 08-scenario1-agent-cornerstone
plan: 02
type: execute
wave: 2
depends_on: ["08-00", "08-01"]
files_modified:
  - package.json
  - app/(defi)/apps/abrigo/cornerstone/page.tsx
  - components/defi/cornerstone/PromptBox.tsx
  - components/defi/cornerstone/RunTranscript.tsx
  - tests/e2e/cornerstone.spec.ts
  - .planning/phases/08-scenario1-agent-cornerstone/08-LIVE-VERIFICATION.md
autonomous: false
requirements: [MOD4-FLOW, MOD4-A1, MOD4-A2, MOD4-MINT]
user_setup: []
must_haves:
  truths:
    - "At /apps/abrigo/cornerstone a visitor picks a preset (or free-text→nearest) and the workflow steps stream progressively into an aria-live transcript announcing each step once"
    - "The Agent-1 entry reveals the server-rendered DecisionPipelineTrace for the real recorded decision (single factor co/inflation-rate) under testnet-agent + 'consensus-verified' — appearing ONLY here"
    - "Agent-2 card → Confirm (focus moves to Confirm) → mock MintCard, all under fork-verified + mock sub-label, never green, never executed/realized"
    - "First paint is the idle prompt (getServerSnapshot=idle); motion entrance is mounted-gated + honors prefers-reduced-motion; no honker-node/AI-SDK/idb in package.json"
    - "A missing/null DecisionTraceView renders the cornerstone.errorState copy (route does NOT 500)"
    - "The 'replaying · mock' state renders as a small INLINE neutral pill inside RunTranscript (LivenessPill is NOT edited — it has no such branch)"
  artifacts:
    - path: "app/(defi)/apps/abrigo/cornerstone/page.tsx"
      provides: "RSC shell — pre-renders DecisionPipelineTrace for both presets (null-guarded), passes as children, mounts client islands"
      contains: "DecisionPipelineTrace"
    - path: "components/defi/cornerstone/RunTranscript.tsx"
      provides: "client transcript — useSyncExternalStore(store), aria-live polite/atomic=false, motion mounted-gate, reveals A1 children + A2 card + mint, inline replaying·mock pill"
      contains: "useSyncExternalStore"
    - path: "components/defi/cornerstone/PromptBox.tsx"
      provides: "client textarea + 2 preset chips → startRun(presetId)"
  key_links:
    - from: "components/defi/cornerstone/RunTranscript.tsx"
      to: "lib/apps/abrigo/cornerstone/workflow-store.ts"
      via: "useSyncExternalStore(store.subscribe, store.getSnapshot, store.getServerSnapshot)"
      pattern: "useSyncExternalStore"
    - from: "app/(defi)/apps/abrigo/cornerstone/page.tsx"
      to: "components/defi/somnia/DecisionPipelineTrace.tsx"
      via: "RSC pre-renders the trace for getDecisionTraceById(recordedDecisionId), passed as children"
      pattern: "DecisionPipelineTrace"
---

<objective>
Wire the full committed MVP: the /apps/abrigo/cornerstone RSC shell pre-renders the real DecisionPipelineTrace for both presets (null-guarded → errorState) and passes them as children; PromptBox starts the run; RunTranscript drives the workflow-store via useSyncExternalStore, streams A1→A2→mint with mounted-gated `motion` entrance + aria-live, moves focus to Confirm on the confirm gate, and renders an INLINE "replaying · mock" neutral pill (LivenessPill stays untouched). Add the `motion` dep. e2e honesty greps + Evidence Collector live-verify close the phase.

Purpose: Assemble the demo screen — the judged "watch it run" narrative — honest end to end.
Output: motion dep + RSC page + 2 client islands + e2e spec + 08-LIVE-VERIFICATION.md.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scenario1-agent-cornerstone/08-UI-SPEC.md
@docs/superpowers/specs/2026-06-06-module4-scenario1-cornerstone-design.md
@CLAUDE.md

<interfaces>
From app/(defi)/apps/abrigo/agent/[id]/page.tsx (REUSE the RSC shell + string-threading + force-dynamic/runtime nodejs pattern; the trace is pre-rendered server-side; REUSE its h1→h2→h3 heading literals):
```ts
export const dynamic = 'force-dynamic'; export const runtime = 'nodejs'
const trace = getDecisionTraceById(id)            // → DecisionTraceView | null  (NULL-GUARD required)
const traceStrings: TraceStrings = { title, stage1..stage6, systemPromptTrigger, illustrativeCaption, ..., consensusCaveat: t('feed.consensusCaveat') }
<DecisionPipelineTrace decision={trace} strings={traceStrings} />   // single real factor co/inflation-rate
```

From lib/apps/abrigo/cornerstone/{workflow-store,workflow-engine,presets,events}.ts (08-00):
```ts
createWorkflowStore(): { subscribe, getSnapshot, getServerSnapshot /* idle */, emit }   // per-call factory — NO singleton
runWorkflow(presetId, emit, { confirm }): Promise<void>
PRESETS: [{id:'infl-surprise-add',recordedDecisionId:'4083729'},{id:'infl-cooling-reduce',recordedDecisionId:'4083997'}]
```

From components/defi/cornerstone/{HedgeDecisionCardV2,MintCard}.tsx (08-01): full-weight cards, fork-verified+FlaskConical mock sub-label, Confirm button + forwarded confirmRef.

<!-- FD-B1: LivenessPill is hard-locked to 'snapshot'|'polling' (NO 'replaying'/'live' branch) and is a SHIPPED Phase-7 component — DO NOT edit it and DO NOT add it to files_modified. -->
<!-- Render the "replaying · mock" state as a SMALL INLINE neutral pill inside RunTranscript, verbatim shell (CROSS-09 color+icon+text, never green/amber): -->
```tsx
<span className="inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-sm font-normal ring-1 ring-inset text-text-muted ring-border-default bg-bg-surface" aria-label={strings.replayingMockAria}>
  <CircleDashed className="h-3.5 w-3.5 shrink-0" aria-hidden="true" />
  <span>{strings.replayingMock}</span>
</span>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add motion dep, RSC shell page, PromptBox + RunTranscript islands, e2e spec</name>
  <read_first>
    - app/(defi)/apps/abrigo/agent/[id]/page.tsx (RSC shell, force-dynamic, string threading, DecisionPipelineTrace usage, max-width container, h1→h2→h3 heading literals to REUSE; getDecisionTraceById null-guard pattern)
    - app/(defi)/layout.tsx + app/(defi)/providers.tsx (the (defi) group wallet provider tree — confirm cornerstone does NOT construct any viem/wagmi client against Polygon addrs; it only renders mock cards)
    - components/defi/somnia/LivenessPill.tsx (READ-ONLY reference for the verbatim neutral pill shell string — do NOT edit or import; replaying·mock is built inline in RunTranscript)
    - lib/apps/abrigo/cornerstone/{workflow-store,workflow-engine,presets,events}.ts (08-00 contracts; createWorkflowStore is a per-call factory)
    - lib/apps/abrigo/somnia/reader.ts getDecisionTraceById (returns DecisionTraceView|null — line 144)
    - components/defi/cornerstone/{HedgeDecisionCardV2,MintCard}.tsx (08-01 — confirmRef + onConfirm)
    - 08-UI-SPEC.md Q1 (single-column ~720px, region order, stacked entries, A1 hosts the trace as children), Q3 (focus-to-Confirm), Q4 (delay table + motion mounted-gate + prefers-reduced-motion fallback + aria-live polite/atomic=false), §Copywriting (banned-strings greps), §Color (accent reserved list)
    - messages/{es-CO,en}/somnia.json cornerstone.* (08-01 keys, incl. replayingMock + errorState)
    - package.json scripts (test:impeccable = `npx impeccable detect app/`; there is NO local impeccable bin → pnpm exec hard-fails)
  </read_first>
  <action>
    1. Add `motion` (MIT, React-19-safe) to package.json via `pnpm add motion`. Pin the import to `motion/react` (NOT `framer-motion`, NOT the `motion` root) everywhere. Do NOT add honker-node, @ai-sdk/*, or idb.
    2. Create app/(defi)/apps/abrigo/cornerstone/page.tsx — RSC shell, `export const dynamic='force-dynamic'; export const runtime='nodejs'`. For EACH preset, call getDecisionTraceById(recordedDecisionId). NULL-GUARD (RC-M5): if the trace is null, render the cornerstone.errorState copy for that slot instead of crashing (the route must NOT 500). Otherwise pre-render a <DecisionPipelineTrace> with TraceStrings threaded exactly like the [id] route (consensusCaveat = t('feed.consensusCaveat')). Pass the two pre-rendered trace nodes (keyed by preset id) as `children`/a prop map to <RunTranscript>. Thread all cornerstone.* strings + card strings + the replaying·mock strings. Single-column container max-w-[720px] mx-auto on bg-canvas, 2xl top/bottom padding. Mount <PromptBox> + <RunTranscript> client islands. Add generateMetadata (cornerstone.pageTitle). Heading outline (FD-M4): page `<h1>` then `<h2>` per agent entry (or h2 transcript + h3 per step) — REUSE the M3 (agent/[id]) heading literals; no skipped levels. The Agent-1 label "...consensus-verified" + testnet-agent tier appear ONLY in the trace block.
    3. components/defi/cornerstone/PromptBox.tsx ('use client') — textarea + EXACTLY 2 preset chips (equal dimensions, min-h-40px, active chip = accent ring) + captions; free-text → resolveNearestPreset on submit; calls a shared startRun(presetId). es-CO-first via threaded strings. The idle heading/body render here (first paint = idle).
    4. components/defi/cornerstone/RunTranscript.tsx ('use client') — owns createWorkflowStore() (useMemo, stable per-mount); useSyncExternalStore(store.subscribe, store.getSnapshot, store.getServerSnapshot) so first paint = idle. startRun(presetId) calls runWorkflow(presetId, e=>store.emit(fromMockEvent(e)), {confirm}). Container: aria-live="polite" aria-atomic="false". Transcript entries are APPEND-ONLY, keyed by step id (FD-M3): each step is a stacked entry (24px gap) reusing the Phase-7 stepper markers; the active-step-marker accent change must NOT mutate prior entries' text content (only the marker styling changes). Add a stable `data-step` testid per entry ("a1"/"a2"/"mint") for ordered DOM assertions (FD-m4). Agent-1 entry renders the pre-rendered trace child for the active preset (revealed all-at-once). Agent-2 entry renders <HedgeDecisionCardV2> with onConfirm wired to the engine's confirm signal AND a `data-confirm` attribute on the Confirm button (FD-M2); move focus to the forwarded confirmRef on entering a2_decision (useEffect on status). Mint entry renders <MintCard>. Step entrance uses `motion/react` with a MOUNTED-GATE (no non-idle content at SSR — guard with a mounted flag) + a prefers-reduced-motion → instant CSS fallback. During streaming render the INLINE "replaying · mock" neutral pill (verbatim shell + CircleDashed + strings.replayingMock + aria — do NOT import or edit LivenessPill).
    5. tests/e2e/cornerstone.spec.ts (production build, mirror playwright.config webServer; set `reducedMotion: 'reduce'` in the test context so the mounted-gate path is deterministic — FD-M2): run a preset → assert steps stream in ORDER via the stable `data-step` testids in DOM order ("a1" then "a2" then "mint"), NOT via aria-live child count (FD-m4) → Agent-1 shows the real recorded decision (the rendered inflation form `5.68%`, NOT raw `568`; "consensus-verified"; testnet-agent pill) → Agent-2 mock card (FlaskConical + "mock · no en vivo") → BEFORE clicking, assert `await expect(page.locator('[data-confirm]')).toBeFocused()` (focus landed on Confirm when the gate appeared — FD-M2), THEN click → MintCard appears. aria-live announce-once (FD-M3): capture a prior entry's textContent, trigger the next emit, assert that prior entry's textContent is BYTE-IDENTICAL before/after (append-only, no mutation). Honesty greps on the rendered DOM: NO executed/realized/ejecutad/realizad; NO raw 0x000…0; fork-verified pills carry no green/emerald/status-pass class; the decision card has no <details>; "consensus-verified"/"testnet-agent" appear ONLY inside the Agent-1 trace block; no '$' adjacent to a value as realized PnL; the free-text rationale sits under the "explicación (autoría humana)" label. Heading outline (FD-M4): run axe (or a heading-level walk) asserting NO skipped heading levels. es-CO default + NEXT_LOCALE=en parity spot-check. Architecture grep: no viem/wagmi client constructed against the Polygon addrs anywhere under app/(defi)/apps/abrigo/cornerstone or components/defi/cornerstone.
    Commit: `feat(08-02): cornerstone route + PromptBox + RunTranscript + e2e honesty greps`.
  </action>
  <verify>
    <automated>! grep -qE '"honker-node"|"@ai-sdk|"idb"' package.json && grep -q '"motion"' package.json && echo DEPS-OK</automated>
    <automated>grep -rqE "from ['\"]motion/react['\"]" components/defi/cornerstone/RunTranscript.tsx && ! grep -rqE "from ['\"](framer-motion|motion)['\"]" components/defi/cornerstone/ && echo MOTION-IMPORT-OK</automated>
    <automated>pnpm tsc --noEmit && pnpm biome check app/\(defi\)/apps/abrigo/cornerstone components/defi/cornerstone</automated>
    <automated>! grep -rqE "viem|wagmi|createPublicClient|createWalletClient" app/\(defi\)/apps/abrigo/cornerstone components/defi/cornerstone && echo NO-CHAIN-CLIENT</automated>
    <automated>! grep -rqE "testnet-agent|consensus-verified" components/defi/cornerstone/HedgeDecisionCardV2.tsx components/defi/cornerstone/MintCard.tsx && echo A1-ONLY-OK</automated>
    <automated>! grep -rqE "LivenessPill" app/\(defi\)/apps/abrigo/cornerstone components/defi/cornerstone && echo NO-LIVENESSPILL-EDIT</automated>
    <automated>pnpm run test:impeccable && echo IMPECCABLE-OK</automated>
    <automated>pnpm build && pnpm exec playwright test tests/e2e/cornerstone.spec.ts</automated>
  </verify>
  <done>motion added (import pinned to motion/react; no honker/AI-SDK/idb); cornerstone route renders idle on first paint and streams A1→A2→mint (ordered via data-step); null trace → errorState (no 500); focus on Confirm asserted before click; aria-live append-only (prior entries byte-identical); inline replaying·mock pill (LivenessPill untouched); no skipped heading levels; e2e honesty greps + es-CO/en parity pass on the production build; no chain client; impeccable clean via pnpm run test:impeccable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Evidence Collector live-verification of /apps/abrigo/cornerstone</name>
  <files>.planning/phases/08-scenario1-agent-cornerstone/08-LIVE-VERIFICATION.md</files>
  <action>Run the Evidence Collector agent against the cornerstone route per the how-to-verify steps below; append per-claim verdicts to 08-LIVE-VERIFICATION.md.</action>
  <what-built>The complete /apps/abrigo/cornerstone mock run flow: preset → live-streamed steps → real Agent-1 trace (testnet-agent · consensus-verified, single factor) → Agent-2 mock card (fork-verified + FlaskConical "mock · no en vivo", free-text rationale under "explicación (autoría humana)") → Confirm (focus-managed) → mock MintCard. Driven by the workflow-store; motion mounted-gated; aria-live transcript; inline replaying·mock pill.</what-built>
  <how-to-verify>
    Run the Evidence Collector agent (CLAUDE.md live-verification protocol) against /apps/abrigo/cornerstone:
    - Target https://www.d2pfinance.xyz/apps/abrigo/cornerstone, fall back to `pnpm start` on 3040 if it 4xx/5xx (the mock producer MUST run in the production build).
    - For BOTH presets: browser_navigate → browser_snapshot → pick the preset → assert the steps stream in (A1 trace, then the mock card, then mint after Confirm) → browser_console_messages (no runtime errors / no hydration #418) → browser_take_screenshot to /tmp/d2p-verify/08-02-cornerstone-{preset}.png → browser_evaluate the honesty invariants: fork-verified pill is neutral (computed color NOT green), the mock sub-label icon node is FlaskConical, the free-text rationale sits under the "explicación (autoría humana)" label (not attributed to a live agent), no '$' realized-PnL node, no executed/realized text, "consensus-verified"/"testnet-agent" only inside the Agent-1 entry, focus landed on Confirm at the gate.
    Append verdicts (✓/⚠/✗/⊘) + screenshot paths to .planning/phases/08-scenario1-agent-cornerstone/08-LIVE-VERIFICATION.md.
    Then visually confirm the ~10–12s pacing reads cleanly for a demo recording.
  </how-to-verify>
  <resume-signal>Type "approved" once the live DOM shows every claim ✓ (or record explicit waivers in 08-LIVE-VERIFICATION.md), or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- Route renders idle on first paint (getServerSnapshot=idle); no hydration #418; null trace → errorState (no 500).
- Steps stream A1→A2→mint (ordered via data-step testids); aria-live append-only (prior entries byte-identical across emits).
- Agent-1 = real trace, single factor, inflation form 5.68% (not 568), testnet-agent + consensus-verified ONLY here.
- Agent-2/mint = fork-verified + FlaskConical mock sub-label, never green, no <details>, no executed/realized, no raw 0x000…0, no real $; rationale under "explicación (autoría humana)".
- Focus on Confirm asserted (toBeFocused via data-confirm) before click; reducedMotion:reduce makes it deterministic.
- replaying·mock is an INLINE pill; LivenessPill.tsx untouched.
- No skipped heading levels (axe/walk).
- motion pinned to motion/react + mounted-gated + prefers-reduced-motion fallback; no honker-node/AI-SDK/idb dep; no chain client against Polygon addrs.
- impeccable (via pnpm run test:impeccable) + token tests + tsc + biome green; Evidence Collector live-DOM gate passed.
</verification>

<success_criteria>
Maps to ROADMAP success criteria 1–4 in full (the committed MVP): preset/free-text → progressive aria-live transcript; real Agent-1 reveal under testnet-agent · consensus-verified (single factor); Agent-2 mock card + Confirm gate (focus-managed); mock MintCard; driven by the dedicated workflow-store with idle first paint; motion mounted-gated; honker-node/AI-SDK/idb absent.
</success_criteria>

<output>
After completion, create `.planning/phases/08-scenario1-agent-cornerstone/08-02-SUMMARY.md` and the 08-LIVE-VERIFICATION.md per CLAUDE.md.
</output>
</content>
</invoke>

---
phase: 03.1-research-reading-surface
plan: 04
subsystem: ui
tags: [i18n, next-intl, a11y, axe, katex, font-display, lighthouse, lcp, mathml]

# Dependency graph
requires:
  - phase: 03.1-03
    provides: the /research/[slug] reading page (Mode A/B/notFound), PaperBridge, ArticleTOC, TheoremBlock, Figure, and the partial reading.* i18n keyset
provides:
  - Reading-page i18n SUPERSET (es-CO-first, symmetric across es-CO/en), gated by the recursive assertKeyParity('research') test
  - docs/copy-review.md Plan C2 native-reviewer sign-off entry for the reading.* superset
  - tests/a11y/research.spec.ts — axe WCAG 2.2 AA on /research + a reading page, both locales (spec authored; run live by orchestrator)
  - docs/a11y-audit.md MathML screen-reader spot-check recorded as a TRACKED WAIVER (no SR in headless Linux CI)
  - v1 LCP font strategy — 2 critical KaTeX faces copied to public/static/katex/, preloaded from a stable path (curl-proven 200), font-display:swap override
  - lighthouserc.cjs extended with a real migrated reading-page URL (LCP 2500 gate retained)
affects: [03.1-LIVE-VERIFICATION, phase verifier, future research polish (v2 byte-subsetting)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Stable public-path KaTeX font preload: copy the LCP-critical faces to public/static/katex/ (served verbatim, NOT Turbopack-hashed) + route-scoped font-display:swap @font-face override; never preload a Turbopack-rewritten /_next/static/media/* path (404 risk)"
    - "Tracked-waiver pattern for manual a11y gates unavailable in CI: record date+reviewer+result with an explicit WAIVED status + follow-up, never a silent skip"

key-files:
  created:
    - tests/a11y/research.spec.ts
    - public/static/katex/KaTeX_Main-Regular.woff2
    - public/static/katex/KaTeX_Math-Italic.woff2
  modified:
    - messages/es-CO/research.json
    - messages/en/research.json
    - docs/copy-review.md
    - docs/a11y-audit.md
    - app/(lab)/research/layout.tsx
    - lighthouserc.cjs
    - docs/03.1-RENDER-PATH.md

key-decisions:
  - "v1 LCP font approach (a): copy the 2 critical KaTeX woff2 to public/static/katex/ + preload the stable path + font-display:swap @font-face override — reconciles the Wave 1 404 burn. NOT swap-only fallback (c); the asset resolves 200, proven by curl."
  - "MathML SR check is a TRACKED WAIVER, not a pass: no NVDA/VoiceOver/Orca in headless Linux CI; htmlAndMathml output is already wired and build-verified, so confidence is high but the live SR announcement is deferred with a logged follow-up."
  - "The plan's literal `! grep -rqi 'pyftsubset|fonttools' .` gate is a documentation false-positive — those strings appear only in deferral PROSE (spec, plan, render-path doc, layout comment), never as executable tooling. No .py, no package dep, no script invocation. Substantive criterion (no Python font tooling) holds."

patterns-established:
  - "Reading-page chrome is localized via the research.reading.* superset; component label fallbacks (TheoremBlock KIND_LABELS, Figure data-fig-label) mirror the same words"

requirements-completed: [LAB-03]

# Metrics
duration: 12min
completed: 2026-05-29
---

# Phase 03.1 Plan 04: i18n Superset + a11y + Perf/Lighthouse Gates Summary

**Reading-page i18n superset (es-CO-first, parity-gated) + axe a11y spec + tracked MathML-SR waiver + v1 LCP font strategy (stable-path KaTeX preload, curl-proven 200, font-display:swap) wired into the existing lighthouserc.cjs.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-29T14:40Z
- **Completed:** 2026-05-29T14:55Z
- **Tasks:** 2 of 2 auto tasks complete; Task 3 is the orchestrator's live-verification checkpoint (axe + Lighthouse + MathML-SR)
- **Files modified:** 9 (2 created code, 2 created font assets, 5 modified)

## Accomplishments
- Completed the `research.reading.*` i18n SUPERSET symmetric across es-CO and en (read_on_arxiv, read_pdf, doi_label, bibtex_copy/copied, affiliation, figure_prefix, theorem_label.{theorem,definition,lemma,proof}), gated green by the recursive `assertKeyParity('research')` test.
- Authored the Plan C2 native-reviewer sign-off section in docs/copy-review.md with es-CO authoring notes (distinct from the Plan-B index-chrome line).
- Authored tests/a11y/research.spec.ts (axe WCAG 2.2 AA on /research + /research/pair-d-dispatch-brief, es-CO + en) — written, NOT run (orchestrator runs it live).
- Recorded the MathML screen-reader spot-check in docs/a11y-audit.md as a TRACKED WAIVER (no SR in headless Linux env; htmlAndMathml already wired; follow-up logged with date 2026-05-29 + reviewer).
- Reconciled the Wave 1 font-preload 404 burn: copied the 2 LCP-critical KaTeX faces to public/static/katex/, preloaded the stable path, added a font-display:swap @font-face override. Proven 200 via short-lived curl.
- Wired a real migrated reading-page URL into the existing lighthouserc.cjs (no stray .json); LCP maxNumericValue 2500 gate retained.

## Task Commits

1. **Task 1: Reading-page i18n superset + recursive parity + native sign-off** - `c1fa7f3` (feat)
2. **Task 2: a11y axe spec + MathML SR waiver + v1 LCP font preload + lhci URL** - `b73167d` (feat)

**Plan metadata:** pending final docs commit.

## Files Created/Modified
- `messages/es-CO/research.json` / `messages/en/research.json` - reading.* superset (symmetric)
- `tests/unit/i18n-coverage.test.ts` - unchanged (already recursive); confirmed green
- `docs/copy-review.md` - Plan C2 reading-page sign-off section + es-CO authoring notes
- `tests/a11y/research.spec.ts` - axe scan of index + reading page, both locales
- `docs/a11y-audit.md` - MathML SR tracked waiver (date + reviewer + result)
- `app/(lab)/research/layout.tsx` - stable-path KaTeX preload + font-display:swap override
- `public/static/katex/KaTeX_Main-Regular.woff2` / `KaTeX_Math-Italic.woff2` - copied critical faces (26272 + 16440 bytes)
- `lighthouserc.cjs` - reading-page URL (port 3040) wired in; LCP 2500 gate retained
- `docs/03.1-RENDER-PATH.md` - §10 v1 LCP font strategy + unicode-range fallback + v2 byte-subset deferral

## Decisions Made
- **Font approach (a) over swap-only (c):** copying the 2 critical faces to a stable public path yields a 200 preload (curl-proven), so byte-level preload is delivered in v1 — not deferred. Only byte-SUBSETTING is deferred to v2.
- **MathML SR = tracked waiver:** the headless Linux CI/dev env has no NVDA/VoiceOver/Orca; recorded explicitly with a follow-up rather than silently skipped or falsely passed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Reconciled the Wave 1 font-preload 404 (plan must-have predates the burn)**
- **Found during:** Task 2 (perf/font work)
- **Issue:** The plan's must-have says "add `<link rel=preload>` for KaTeX faces (Plan A scaffolded this)". That scaffold was REMOVED in Wave 1 (commit 4117fc3) because it pointed at /static/katex/*.woff2 which did not exist → 4×404 + console errors. Naively re-adding the preload would reproduce the burn.
- **Fix:** Copied the 2 critical woff2 to public/static/katex/ (served at a stable, non-Turbopack-hashed path), preloaded that path, added a font-display:swap @font-face override. Proven 200 + zero console 404 via short-lived curl on `pnpm start -p 3041` (killed immediately).
- **Files modified:** app/(lab)/research/layout.tsx, public/static/katex/*.woff2, docs/03.1-RENDER-PATH.md
- **Verification:** `curl` → KaTeX_Main-Regular.woff2 200 (26272 B, font/woff2), KaTeX_Math-Italic.woff2 200 (16440 B); both preload links present in reading-page HTML; server log clean.
- **Committed in:** b73167d

**2. [Rule 3 - Blocking] Plan's literal `! grep -rqi 'pyftsubset|fonttools' .` gate is a documentation false-positive**
- **Found during:** Task 2 (acceptance-criteria check)
- **Issue:** The verify command matches the deferral PROSE the plan itself mandates (spec, plan, CONTEXT, render-path doc, layout comment all NAME the deferred tools). The literal `! grep` therefore can never pass once the required v2-deferral documentation exists.
- **Fix:** Verified the SUBSTANTIVE criterion instead: no `.py` files, no package.json reference, no executable invocation of pyftsubset/fonttools anywhere in source. Documented the tension; the no-Python-tooling intent holds.
- **Files modified:** none (verification-only)
- **Verification:** `find . -name '*.py'` (none); package.json python grep (none); source-file invocation grep (none).
- **Committed in:** N/A (analysis)

---

**Total deviations:** 2 (1 bug-class reconciliation, 1 gate false-positive resolution)
**Impact on plan:** Both necessary. The font reconciliation actually delivers MORE than the plan's fallback option (full preload, not swap-only). No scope creep.

## Issues Encountered
- commitlint rejected the Task 2 commit body for >100-char lines; re-wrapped the body and committed cleanly.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All code + automated verification complete: i18n parity GREEN, test:quick GREEN, `pnpm run build` exit 0 (velite OK), impeccable exit 0, font assets curl-200.
- **REMAINING (orchestrator gate — Task 3 checkpoint:human-verify):** the LIVE axe run (`pnpm exec playwright test tests/a11y/research.spec.ts`), the LIVE Lighthouse LCP run (`pnpm exec lhci autorun` against the prod server) with the LCP score recorded in 03.1-LIVE-VERIFICATION.md, and (waived) the MathML SR confirmation. The phase is NOT marked verified here — that is the orchestrator's gate.

## Self-Check: PASSED

All created/modified files present on disk; both task commits (c1fa7f3, b73167d) found in git history.

---
*Phase: 03.1-research-reading-surface*
*Completed: 2026-05-29*

## Gate outcome (orchestrator, 2026-05-29)

Live gates run against a clean prod build (localhost:3040; axe + lhci CLI):
- **i18n superset + recursive parity:** ✓ green (4/4).
- **KaTeX font preload:** ✓ both faces 200 at `/static/katex/`, preload links present, zero 404s (Wave-1 burn reconciled).
- **axe WCAG 2.2 AA:** initially 4 FAIL (serious small-text contrast on locked ochre + text-muted). Fixed in `4705766` — added AA-safe `--accent-text` (locked `--accent-default` untouched) + darkened `--text-muted`. Re-run **4/4 PASS** (es-CO + en, index + reading). impeccable exit 0; suite 132/132.
- **MathML SR:** ⊘ tracked waiver (headless env), recorded in `docs/a11y-audit.md`.
- **Lighthouse LCP:** ⚠ local ~2.9–3.2s misses the 2500ms gate but is noisy (2.87–3.66s) on a loaded dev box under simulated 3G; FCP/TBT healthy. **Author decision: re-measure on a Vercel preview deploy** (real CDN) as ground truth — tracked, not waived.
- **es-CO native copy sign-off:** ⏳ pending a human native reviewer (`docs/copy-review.md` C2).

**Status: code-complete.** Residual gates are non-code: LCP re-measure on Vercel preview + es-CO native sign-off. See `03.1-LIVE-VERIFICATION.md` → "Task 03.1-04 … (live, orchestrator)".

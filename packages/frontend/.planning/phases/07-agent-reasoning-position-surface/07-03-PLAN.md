---
phase: 07-agent-reasoning-position-surface
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - app/(defi)/apps/abrigo/agent/[id]/page.tsx
  - app/(defi)/apps/abrigo/agent/[id]/not-found.tsx
  - app/(defi)/apps/abrigo/agent/page.tsx
  - components/defi/somnia/HedgeDecisionFeed.tsx
  - components/defi/somnia/DecisionTraceLink.tsx
  - messages/es-CO/somnia.json
  - messages/en/somnia.json
  - tests/e2e/agent-decision-detail.spec.ts
  - .planning/phases/07-agent-reasoning-position-surface/07-LIVE-VERIFICATION.md
autonomous: false
requirements: [MOD3-TRACE, MOD3-POS, MOD3-MANAGE, MOD3-LIVE, CROSS-01, CROSS-09, CROSS-10]

must_haves:
  truths:
    - "At /apps/abrigo/agent/[id] a visitor sees the deterministic decision-pipeline trace + the not-deployed position panel + the disabled management controls + the liveness pill, for a real decision (requestId 4083729 / 4083997)."
    - "Each Phase-6 decision feed card links to its detail route via an accent-text label + ChevronRight + underline (CROSS-09: color is not the sole link signal); the equal-weight invariant is preserved across all cards."
    - "An unknown id triggers notFound() → the dedicated not-found.tsx renders the honest error state (trace.errorNotFound) + a return link, returning HTTP 404; no fabricated decision and no inline-200 error path."
    - "The rendered detail DOM contains no executed/realized/ejecutad/realizad, no fabricated dollar notional, no green token on the fork-verified tier, and no live liveness pill (live is deferred)."
    - "es-CO and en parity holds on the detail route; the page renders read-first (no wallet gate); the 6 pipeline-stage marker rings + titles share identical computed width/height/font-size (bounding-box equal weight, not grep-only)."
  artifacts:
    - path: "app/(defi)/apps/abrigo/agent/[id]/page.tsx"
      provides: "The per-decision detail route assembling trace + position panel + management + liveness; notFound() on unknown id"
      contains: "DecisionPipelineTrace"
    - path: "app/(defi)/apps/abrigo/agent/[id]/not-found.tsx"
      provides: "The dedicated 404 boundary carrying trace.errorNotFound + back link (instruments-route precedent)"
      contains: "errorNotFound"
    - path: "components/defi/somnia/DecisionTraceLink.tsx"
      provides: "The master->detail link affordance (accent-text + ChevronRight + underline)"
      contains: "ChevronRight"
    - path: "tests/e2e/agent-decision-detail.spec.ts"
      provides: "e2e honesty greps + render assertions + bounding-box equal-weight on the detail route (es-CO + en)"
      contains: "pipeline-trace"
  key_links:
    - from: "app/(defi)/apps/abrigo/agent/[id]/page.tsx"
      to: "lib/apps/abrigo/somnia/reader.ts"
      via: "getDecisionTraceById(id) (await params); notFound() when null"
      pattern: "getDecisionTraceById"
    - from: "components/defi/somnia/HedgeDecisionFeed.tsx"
      to: "app/(defi)/apps/abrigo/agent/[id]/page.tsx"
      via: "DecisionTraceLink to /apps/abrigo/agent/[id] using decision.decisionId (the requestId)"
      pattern: "/apps/abrigo/agent/"
    - from: "app/(defi)/apps/abrigo/agent/[id]/page.tsx"
      to: "components/defi/somnia/PositionPanel.tsx"
      via: "mounting the not-deployed position panel"
      pattern: "PositionPanel"
---

<objective>
Wire the per-decision detail route /apps/abrigo/agent/[id]: assemble the pipeline trace (07-01) + position panel + disabled management + liveness pill (07-02) into one read-first RSC page, add the master->detail link affordance on the Phase-6 feed cards, handle the unknown-id error via notFound() + a dedicated not-found.tsx (HTTP 404, matching the instruments-route precedent), and prove the honesty invariants with an e2e suite (route-scoped fraction + consensus assertions + bounding-box equal weight) + the Evidence Collector live-verification gate.

Purpose: This is the rendered surface the whole phase exists for. The honesty greps (no executed/realized/ejecutad/realizad, no fabricated dollar, no green on the fork-verified tier, no live pill) run against the real built DOM here, and the Evidence Collector confirms the live route.

Output: app/(defi)/apps/abrigo/agent/[id]/page.tsx, app/(defi)/apps/abrigo/agent/[id]/not-found.tsx, DecisionTraceLink.tsx, the feed-card link wiring, the e2e suite, and the LIVE-VERIFICATION.md section.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-agent-reasoning-position-surface/07-UI-SPEC.md
@docs/superpowers/specs/2026-06-02-module3-agent-reasoning-position-surface-design.md

<interfaces>
From 07-01: DecisionPipelineTrace (DecisionTraceView + TraceStrings; TraceStrings includes consensusCaveat threaded from feed.consensusCaveat), the trace.* somnia copy.
From 07-02: PositionPanel (position.* strings), ManagementControls (manage.* strings), LivenessPill (LivenessSource + liveness.* strings; getServerSnapshot stable first paint).
From 07-00: getDecisionTraceById(id): DecisionTraceView | null; snapshotSource(seed); WrapperPositionView/getWrapperPosition (deployed:false default). DecisionTraceView.requestId = the route key (= the snapshot decisionId / size-leg requestId).
Existing page to extend: app/(defi)/apps/abrigo/agent/page.tsx (Phase-6 overview; export const dynamic='force-dynamic', runtime='nodejs'; string-threading pattern; getLocale/getTranslations('somnia')).
Existing feed: components/defi/somnia/HedgeDecisionFeed.tsx (maps decisions to HedgeDecisionCard via key={decision.decisionId}; HedgeDecisionView.decisionId carries the requestId — the [id] route key, UNCHANGED per BLOCKER-7).
generateMetadata precedent: app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx — `generateMetadata({ params }: { params: Promise<{...}> })` then `await params`. (BLOCKER-4: the agent route title is the ROUTE-LEVEL STATIC trace.title — NOT decision-specific — so there is NO null branch; the instruments null-branch is for its registry lookup, which the agent route does not need at metadata time.)
notFound() + not-found.tsx precedent: instruments/[id]/[chain]/page.tsx calls notFound() for unknown ids (WAIVER-05-04) → the (defi) segment's not-found boundary renders. The agent [id] route adds its OWN app/(defi)/apps/abrigo/agent/[id]/not-found.tsx.

Real decision ids (route keys): 4083729 (ADD_LONG_GAMMA, sizeBps 6800, macro 568, consensus 500) and 4083997 (REDUCE, sizeBps 568, macro 568, consensus 900).
bridge.ts fraction strings (verified — REUSE, do NOT hardcode 0.68): formatFractionOfMax(6800n) === "68%"; formatFractionOfMax(568n) === "6%" (Number(568)/100 = 5.68 → Math.round). The trace stage 6 renders these (07-01 reuses bridge.ts). The e2e asserts the route-correct string per id.
</interfaces>

<ui_spec_bindings>
Master->detail link (UI-SPEC Component & Layout Contract 5 + Color accent reserved-for 1):
- Each HedgeDecisionCard (or its container) gains a link to /apps/abrigo/agent/[id].
- Affordance: `text-accent-text hover:text-accent-hover` label (es: Ver la traza de decisión / en: View decision trace) + a lucide-react ChevronRight icon + underline-on-hover. CROSS-09: color is NOT the sole link signal (icon + underline present).
- The link affordance is IDENTICAL on every card regardless of action (equal-weight invariant preserved).

Detail route (UI-SPEC IA + Component & Layout Contract):
- Page header: route title (es: Traza de la decisión / en: Decision trace) + the LivenessPill near the title (describes the whole surface freshness).
- SR heading outline (MINOR-16): the page uses an <h1> route title; the DecisionPipelineTrace section is introduced by an <h2> ("Decision trace" / "Traza de la decisión" — may reuse trace.title or a section label) and each of the 6 stages carries an <h3> stage title (so screen-reader users can navigate the stepper by heading level).
- Body order: DecisionPipelineTrace (centerpiece) -> PositionPanel (not-deployed empty state) -> ManagementControls (disabled).
- Read-first: no wallet gate; RSC; await params (Next 16) to read id.
- Error state (id not found): call notFound() (NOT an inline-200 render) → the dedicated not-found.tsx renders the honest message (es: No se encontró la decisión solicitada. Vuelve al panel del agente para ver las decisiones registradas. / en: The requested decision was not found. Return to the agent panel to see recorded decisions.) + a link back to /apps/abrigo/agent. This returns HTTP 404 (MAJOR-6).
- Liveness pill: snapshot by default; polling when SOMNIA_LIVE (source selection mirrors the reader lazy-env pattern, but the pill stays snapshot in default CI). NEVER a live pill.

generateMetadata (BLOCKER-4): `export async function generateMetadata({ params }: { params: Promise<{ id: string }> }): Promise<Metadata>` then `await params` (id read only if needed for description; title is the static route-level trace.title). NO null branch — the title does not depend on a found decision.

Add copy keys under somnia (es-CO first / en):
- trace.errorNotFound: No se encontró la decisión solicitada. Vuelve al panel del agente para ver las decisiones registradas. / The requested decision was not found. Return to the agent panel to see recorded decisions.
- trace.linkLabel: Ver la traza de decisión / View decision trace
- trace.backToPanel: Volver al panel del agente / Back to the agent panel
</ui_spec_bindings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DecisionTraceLink + feed-card wiring + the /[id] detail page + not-found.tsx (404)</name>
  <read_first>
    - app/(defi)/apps/abrigo/agent/page.tsx (the overview page — string-threading, dynamic/runtime exports, feedStrings construction)
    - app/(defi)/apps/abrigo/instruments/[id]/[chain]/page.tsx (the generateMetadata Promise<params>+await pattern + notFound() precedent to mirror)
    - components/defi/somnia/HedgeDecisionFeed.tsx (the file being modified — where each card is mapped via key={decision.decisionId}; add the link affordance per card)
    - components/defi/somnia/HedgeDecisionCard.tsx (the equal-weight CARD_CLASS — the link must not break equal weight)
    - components/defi/somnia/DecisionPipelineTrace.tsx (07-01 — the trace component + TraceStrings shape incl. consensusCaveat)
    - components/defi/somnia/PositionPanel.tsx + ManagementControls.tsx + LivenessPill.tsx (07-02 — the panel/controls/pill + their strings; LivenessPill needs a snapshotSource)
    - lib/apps/abrigo/somnia/reader.ts (getDecisionTraceById) + liveness.ts (snapshotSource)
    - messages/es-CO/somnia.json + messages/en/somnia.json (add trace.errorNotFound/linkLabel/backToPanel; feed.consensusCaveat already exists for reuse)
    - 07-UI-SPEC.md Component & Layout Contract 5 + IA + Copywriting Contract; the <ui_spec_bindings> above
  </read_first>
  <action>
    Create components/defi/somnia/DecisionTraceLink.tsx (RSC): a Next.js <Link href={`/apps/abrigo/agent/${requestId}`}> with `text-accent-text hover:text-accent-hover hover:underline` + a lucide-react ChevronRight icon + the trace.linkLabel text (color + icon + underline; CROSS-09). Identical markup for every card. Props: requestId, label.

    Modify components/defi/somnia/HedgeDecisionFeed.tsx: render a DecisionTraceLink inside each card mapping (or its wrapper), using decision.decisionId (the requestId — UNCHANGED Phase-6 field) as the route key. Thread the linkLabel through the feed strings. Do NOT alter the equal-weight CARD_CLASS or add action-specific emphasis — the link affordance is identical per card. Update the agent overview page.tsx feedStrings to supply linkLabel = t('trace.linkLabel') (keeps the overview compiling).

    Create app/(defi)/apps/abrigo/agent/[id]/page.tsx (RSC; export const dynamic='force-dynamic', runtime='nodejs'; mirror the overview + instruments-route conventions): await params to read id; call getDecisionTraceById(id). If null → call notFound() (NOT an inline-200 render; MAJOR-6). If found, render: an <h1> route title (trace.title) + a LivenessPill (source = snapshotSource of the trace freshness; snapshot default) near the title; then an <h2>-introduced DecisionPipelineTrace section (threaded TraceStrings incl. consensusCaveat = t('feed.consensusCaveat'), each stage an <h3> — MINOR-16); then PositionPanel (getWrapperPosition() -> deployed:false empty state, threaded position strings); then ManagementControls (threaded manage strings). getLocale + getTranslations('somnia') and thread all strings (NO getTranslations inside leaf components — the Phase-6 pattern). Add generateMetadata per BLOCKER-4: `{ params }: { params: Promise<{ id: string }> }`, await params, title = the STATIC route-level trace.title (no null branch). Add trace.errorNotFound/linkLabel/backToPanel to both locale somnia.json files (es-CO first, parity).

    Create app/(defi)/apps/abrigo/agent/[id]/not-found.tsx (RSC): renders the trace.errorNotFound copy + a <Link> back to /apps/abrigo/agent carrying trace.backToPanel. getLocale + getTranslations('somnia') for the copy. This is the 404 boundary the notFound() call triggers (HTTP 404 — matches instruments-route precedent). Remove any inline-200 error path from page.tsx.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - file exists: app/(defi)/apps/abrigo/agent/[id]/page.tsx and app/(defi)/apps/abrigo/agent/[id]/not-found.tsx
    - grep -q getDecisionTraceById 'app/(defi)/apps/abrigo/agent/[id]/page.tsx'
    - grep -q 'notFound' 'app/(defi)/apps/abrigo/agent/[id]/page.tsx' (MAJOR-6: notFound() not inline-200) and ! grep -qE 'errorNotFound' 'app/(defi)/apps/abrigo/agent/[id]/page.tsx' (the error copy lives in not-found.tsx, NOT inline in page)
    - grep -q 'errorNotFound' 'app/(defi)/apps/abrigo/agent/[id]/not-found.tsx' and grep -q 'backToPanel' 'app/(defi)/apps/abrigo/agent/[id]/not-found.tsx'
    - grep -q DecisionPipelineTrace 'app/(defi)/apps/abrigo/agent/[id]/page.tsx' and grep -q PositionPanel '...page.tsx' and grep -q ManagementControls '...page.tsx' and grep -q LivenessPill '...page.tsx'
    - grep -q 'await params' 'app/(defi)/apps/abrigo/agent/[id]/page.tsx' (Next 16 async params) and grep -q 'Promise<' 'app/(defi)/apps/abrigo/agent/[id]/page.tsx' (params typed as Promise — BLOCKER-4)
    - generateMetadata present with no null/early-return title branch (BLOCKER-4: static route-level title) — reviewer/test confirms the title is t('trace.title') unconditionally
    - grep -q 'feed.consensusCaveat' 'app/(defi)/apps/abrigo/agent/[id]/page.tsx' (MAJOR-9: reuse, threaded into TraceStrings)
    - grep -qE '<h2|<h3' 'app/(defi)/apps/abrigo/agent/[id]/page.tsx' (MINOR-16 heading outline; or the trace component renders the h3s — confirm the stage titles are h3)
    - grep -q ChevronRight components/defi/somnia/DecisionTraceLink.tsx and grep -q '/apps/abrigo/agent/' components/defi/somnia/DecisionTraceLink.tsx
    - grep -q text-accent-text components/defi/somnia/DecisionTraceLink.tsx (accent link) and grep -q underline components/defi/somnia/DecisionTraceLink.tsx (non-color signal)
    - grep -q DecisionTraceLink components/defi/somnia/HedgeDecisionFeed.tsx (link wired into the feed)
    - grep -q errorNotFound messages/es-CO/somnia.json and grep -q errorNotFound messages/en/somnia.json
    - pnpm build succeeds (route compiles, no ssr:false-in-RSC error, no missing-module error)
    - tsc --noEmit exits 0; biome check . exits 0
  </acceptance_criteria>
  <done>The /[id] route assembles trace + position panel + disabled management + liveness pill with an h1/h2/h3 heading outline, calls notFound() (→ not-found.tsx, HTTP 404) on unknown id, has generateMetadata with a static route-level title (no null branch), reuses feed.consensusCaveat, and the feed cards carry an identical accent+chevron+underline link; build/tsc/biome pass.</done>
</task>

<task type="auto">
  <name>Task 2: e2e honesty greps + route-scoped render assertions + bounding-box equal weight (es-CO + en)</name>
  <read_first>
    - tests/e2e/somnia-decision-feed.spec.ts (mirror this e2e shape: production webServer, NEXT_LOCALE cookie for en, networkidle, getByText/locator patterns, AND the card-equality bounding-box pattern to mirror for MAJOR-11)
    - playwright.config.ts (the production webServer pnpm build && pnpm start -p 3040 — the Phase-2 burn class decision)
    - app/(defi)/apps/abrigo/agent/[id]/page.tsx + not-found.tsx (the route under test, from Task 1)
    - lib/apps/abrigo/somnia/bridge.ts (the verified fraction strings: 6800n → "68%", 568n → "6%" — the exact per-route assertions)
    - docs/superpowers/specs/2026-06-02-module3-agent-reasoning-position-surface-design.md §8 (the exact e2e honesty greps required)
    - 07-UI-SPEC.md Binding honesty invariants + Copywriting Contract (the exact strings to assert)
  </read_first>
  <action>
    Create tests/e2e/agent-decision-detail.spec.ts (against the production webServer). Routes under test: /apps/abrigo/agent/4083729 (ADD_LONG_GAMMA, sizeBps 6800, consensus 500) and /apps/abrigo/agent/4083997 (REDUCE, sizeBps 568, consensus 900). Assertions (es-CO default + en via NEXT_LOCALE cookie):
    - the pipeline-trace testid is visible and contains exactly 6 pipeline-stage nodes;
    - ROUTE-SCOPED built-prompt assertions (BLOCKER-3): on /4083729 the built-prompt block contains "Actual macro print (scaled int): 568. Consensus expectation (scaled int): 500"; on /4083997 it contains "Consensus expectation (scaled int): 900" (consensus 900, NOT 500 — pin the route-correct consensus term);
    - ROUTE-SCOPED stage-6 fraction (BLOCKER-3, REUSE bridge.ts output — do NOT hardcode 0.68 for both): on /4083729 stage 6 shows "68%"; on /4083997 stage 6 shows the bridge string for 568n (verify formatFractionOfMax(568n) → "6%" at execution and assert that EXACT string; also assert the REDUCE direction marker). The page contains NO dollar character anywhere;
    - the position-panel testid is visible and the fork-verified pill is present; every position dd is an em-dash;
    - the management-controls testid is visible; exactly three buttons are disabled (assert the disabled property on all three) and EACH has aria-describedby pointing at the caption id (MAJOR-12);
    - the liveness pill renders snapshot text (es: instantánea / en: snapshot) and the page contains NO live pill text;
    - MAJOR-11 EQUAL-WEIGHT BOUNDING-BOX (mirror the Phase-6 card-equality e2e): collect the 6 pipeline-stage marker rings + the 6 stage <h3> titles via browser bounding-box/computed-style and assert all 6 markers share identical width + height, and all 6 titles share identical font-size (not grep-only);
    - HONESTY GREPS on body.textContent (MAJOR-13 mechanical — case-insensitive): body.textContent does NOT match /executed|realized|ejecutad|realizad/; body.textContent does NOT contain a "$" character (this is the textContent assertion, NOT a source grep -F which trips on ${} template literals); the fork-verified pill className does NOT match /status-pass|green|emerald/ (assert via evaluate on the pill node); the liveness pill text does NOT contain a live state (precise check, not naive matching of "liveness");
    - the master->detail link: navigate /apps/abrigo/agent, click the first DecisionTraceLink, assert the URL becomes /apps/abrigo/agent/4083729 (or 4083997) and the trace renders;
    - the unknown-id 404 (MAJOR-6): navigate /apps/abrigo/agent/does-not-exist — assert the response status is 404 (page.goto response.status() === 404) AND the errorNotFound copy + the back link render (the not-found.tsx boundary), NOT an inline-200;
    - en parity: the en route title and key strings render under the NEXT_LOCALE=en cookie.
  </action>
  <verify>
    <automated>pnpm playwright test tests/e2e/agent-decision-detail.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - file exists: tests/e2e/agent-decision-detail.spec.ts
    - grep -q pipeline-trace tests/e2e/agent-decision-detail.spec.ts and grep -q position-panel tests/e2e/agent-decision-detail.spec.ts and grep -q management-controls tests/e2e/agent-decision-detail.spec.ts
    - grep -qE 'executed|realized|ejecutad|realizad' tests/e2e/agent-decision-detail.spec.ts (the honesty NEGATIVE grep is asserted — these words appear only inside a not.toMatch assertion)
    - grep -qE 'status-pass|green|emerald' tests/e2e/agent-decision-detail.spec.ts (the no-green NEGATIVE assertion is present)
    - grep -q '68%' tests/e2e/agent-decision-detail.spec.ts and grep -q '900' tests/e2e/agent-decision-detail.spec.ts and grep -q '500' tests/e2e/agent-decision-detail.spec.ts (route-scoped fraction + consensus assertions per BLOCKER-3)
    - grep -q 4083729 tests/e2e/agent-decision-detail.spec.ts and grep -q 4083997 tests/e2e/agent-decision-detail.spec.ts (both real decision ids tested)
    - grep -qE 'boundingBox|getComputedStyle|evaluate' tests/e2e/agent-decision-detail.spec.ts (MAJOR-11 bounding-box equal-weight present, not grep-only)
    - grep -qE 'status\\(\\)|=== 404|toBe\\(404' tests/e2e/agent-decision-detail.spec.ts (MAJOR-6: 404 status asserted, not inline-200)
    - grep -q 'NEXT_LOCALE' tests/e2e/agent-decision-detail.spec.ts (en parity covered)
    - pnpm playwright test tests/e2e/agent-decision-detail.spec.ts --project=chromium passes
  </acceptance_criteria>
  <done>e2e suite asserts the trace + not-deployed panel + disabled management + snapshot liveness render for both real decisions with route-scoped consensus (500/900) + route-scoped bridge fraction (68%/6%), the 6 stages equal-weight by bounding-box, the master->detail navigation works, the unknown-id 404 (status 404 + not-found.tsx copy) renders, and the honesty greps (no executed/realized/ejecutad/realizad, no dollar in textContent, no green on fork-verified, no live pill) all pass in es-CO and en.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Evidence Collector live-verification of /apps/abrigo/agent/[id] (CLAUDE.md ground-truth gate)</name>
  <what-built>The per-decision detail route /apps/abrigo/agent/[id] rendering the deterministic pipeline trace (6 stages), the not-deployed position panel under the fork-verified neutral tier, the disabled management controls, and the snapshot liveness pill — for the two real decisions 4083729 and 4083997.</what-built>
  <how-to-verify>
    Per CLAUDE.md, launch the Evidence Collector agent (subagent_type="Evidence Collector") in live-verification mode:
    - Working dir: /home/jmsbpp/apps/d2p/frontend
    - Target: https://www.d2pfinance.xyz/apps/abrigo/agent/4083729 (fall back to local pnpm start on port 3040 if the deployed route 4xx/5xx — the route is new, so local fallback is expected until deploy propagates)
    - Use the mcp__plugin_playwright_playwright__* tools.
    - Routes affected: /apps/abrigo/agent/4083729, /apps/abrigo/agent/4083997, and /apps/abrigo/agent (the feed link source).
    - Claims to verify against the live DOM (default to skepticism; screenshots required):
      1. pipeline-trace present with 6 pipeline-stage nodes at equal visual weight (bounding-box check on the 6 marker rings + titles);
      2. the built-prompt block shows the route-correct prompt: /4083729 → "...scaled int): 568. Consensus expectation (scaled int): 500..."; /4083997 → "...Consensus expectation (scaled int): 900...";
      3. stage 6 shows the bridge fraction "68%" on /4083729 and "6%" on /4083997 (read the rendered text), and there is NO dollar character anywhere on the page;
      4. both legs show real requestIds (action-leg derived requestId present); the action-leg TIMESTAMP on /4083729 is an em-dash (out-of-window — honest), and both timestamps present on /4083997;
      5. the fork-verified pill is present with the ShieldCheck icon (read the icon node) and its computed color is NEUTRAL — assert the className/computed color does NOT resolve to a green/status-pass token;
      6. the position panel renders the not-deployed empty state — every value an em-dash;
      7. the three management buttons are disabled (read the disabled property + aria-disabled + per-button aria-describedby) with a Lock icon and the persistent caption;
      8. the liveness pill reads snapshot (es: instantánea / en: snapshot), NOT live;
      9. body text contains no executed/realized/ejecutad/realizad;
      10. clicking the decision-trace link on /apps/abrigo/agent lands on the detail route;
      11. /apps/abrigo/agent/does-not-exist returns HTTP 404 and renders the not-found.tsx error copy + back link.
    - Save screenshots to /tmp/d2p-verify/07-03-{slug}.png.
    - Append a section to .planning/phases/07-agent-reasoning-position-surface/07-LIVE-VERIFICATION.md with verdicts per claim and screenshot paths.
    A claim is complete only when every verdict is PASS or has an explicit recorded waiver.
  </how-to-verify>
  <files>.planning/phases/07-agent-reasoning-position-surface/07-LIVE-VERIFICATION.md, /tmp/d2p-verify/07-03-*.png</files>
  <action>Launch the Evidence Collector subagent against the live (or local pnpm start :3040 fallback) /apps/abrigo/agent/[id] route for both real decisions 4083729 and 4083997, assert each of the 11 claims above against the live DOM with screenshots, and append the verdict section to 07-LIVE-VERIFICATION.md. This is the CLAUDE.md ground-truth gate — typecheck + unit + e2e green are necessary but not sufficient; the live DOM is the ground truth.</action>
  <verify>Evidence Collector report shows every one of the 11 claims as PASS (or an explicit waiver recorded), with screenshot paths under /tmp/d2p-verify/, appended to 07-LIVE-VERIFICATION.md.</verify>
  <done>07-LIVE-VERIFICATION.md contains a 07-03 section with a PASS (or waived) verdict for all 11 claims and referenced screenshots; the user has typed approved.</done>
  <resume-signal>Type "approved" once the Evidence Collector reports all claims PASS (or a waiver is recorded in 07-LIVE-VERIFICATION.md), or describe the issues found.</resume-signal>
</task>

</tasks>

<verification>
- pnpm build succeeds; tsc --noEmit exits 0; biome check . exits 0
- pnpm playwright test tests/e2e/agent-decision-detail.spec.ts --project=chromium — GREEN (es-CO + en; route-scoped 500/900 consensus + 68%/6% fraction; bounding-box equal weight; 404 status)
- Phase-6 e2e regression: pnpm playwright test tests/e2e/somnia-decision-feed.spec.ts --project=chromium — still GREEN
- Evidence Collector live-verification recorded in 07-LIVE-VERIFICATION.md with all claims PASS or waived
</verification>

<success_criteria>
- /apps/abrigo/agent/[id] renders the trace + not-deployed position panel + disabled management + snapshot liveness for real decisions (phase success criteria 1, 2, 3, 4)
- Master->detail link is identical per card, color-not-sole-signal (phase success criterion 1, CROSS-09)
- Unknown id → notFound() → not-found.tsx, HTTP 404 (MAJOR-6)
- Honesty greps pass on the real built DOM: no executed/realized/ejecutad/realizad, no fabricated dollar, no green on fork-verified, no live pill; route-scoped consensus + bridge fraction correct (honesty invariants, all 4 criteria)
- es-CO + en parity; read-first no wallet gate (CROSS-01/CROSS-10); 6 stages equal-weight by bounding-box (CROSS-09)
</success_criteria>

<output>
After completion, create .planning/phases/07-agent-reasoning-position-surface/07-03-SUMMARY.md, and ensure the Evidence Collector section is appended to 07-LIVE-VERIFICATION.md (the CLAUDE.md ground-truth gate for the rendered route).
</output>

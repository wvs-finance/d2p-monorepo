---
phase: 09-cornerstone-live-tx-integration
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/e2e/cornerstone-replay-smoke.spec.ts
  - lib/apps/abrigo/cornerstone/mode.ts
  - tests/unit/cornerstone/mode.test.ts
  - wagmi.config.ts
  - lib/contracts/generated.ts
  - lib/apps/abrigo/cornerstone/buildbear-deployments.json
  - lib/apps/abrigo/cornerstone/artifact-loader.ts
  - lib/apps/abrigo/cornerstone/buildbear.ts
  - lib/apps/abrigo/cornerstone/balance-delta.ts
  - lib/apps/abrigo/cornerstone/token-id.ts
  - app/api/cornerstone/rpc/route.ts
  - tests/unit/cornerstone/balance-delta.test.ts
  - tests/unit/cornerstone/token-id.test.ts
  - tests/unit/cornerstone/artifact-loader.test.ts
  - tests/unit/cornerstone/rpc-proxy.test.ts
  - tsconfig.json
autonomous: true
requirements: [MOD5-MODES, MOD5-ABI, MOD5-CHAIN, MOD5-LIVE]
must_haves:
  truths:
    - "replay mode (real captured receipts) is the GUARANTEED in-phase demo artifact: it renders snapshot.json end-to-end with the fork RPC UNREACHABLE/expired (replay never reads the live RPC at render), and the git tag cornerstone-replay-safe marks the working build"
    - "the replay smoke test is the CI gate; it must stay green through all later live work and is TTL- and RPC-independent"
    - "ABIs for MacroHedgeStrategist, MacroHedgeExecutor, IPanopticData, and the MacroOracle latest(dataKey) read are wagmi-generated from contracts/out/, not hand-typed"
    - "the BuildBear fork (31337) is registered as a 6th wagmi chain via defineChain sourced from the mirrored artifact rpcUrl (Somnia is NOT a browser chain)"
    - "a Next Route-Handler JSON-RPC proxy (app/api/cornerstone/rpc/route.ts) ships regardless; an eth_chainId browser-probe decides direct-vs-proxy at runtime"
    - "decodeBalanceDelta sign-extends the low word so a negative amount1 decodes correctly; extractStrike(positionId) returns 360360 with the bit offset (76) cross-checked against panoptic-sdk generated.ts (not a reverse-fit fixture)"
    - "isExpired returns false for a fresh capturedAt and true after capturedAt + 3 days"
  artifacts:
    - path: "tests/e2e/cornerstone-replay-smoke.spec.ts"
      provides: "T0 replay smoke — the CI gate guaranteeing replay renders snapshot end-to-end with the fork RPC down"
      min_lines: 20
    - path: "lib/apps/abrigo/cornerstone/mode.ts"
      provides: "CornerstoneMode = live|replay|mock + default/parse helper (replay frozen first)"
      exports: ["CornerstoneMode", "DEFAULT_MODE"]
    - path: "lib/contracts/generated.ts"
      provides: "wagmi-generated ABIs incl. macroHedgeStrategistAbi, macroHedgeExecutorAbi, iPanopticDataAbi, + the MacroOracle latest read"
      contains: "macroHedgeStrategistAbi"
    - path: "lib/apps/abrigo/cornerstone/buildbear-deployments.json"
      provides: "mirrored fork artifact (addresses, rpcUrl, capturedAt)"
      contains: "rpcUrl"
    - path: "app/api/cornerstone/rpc/route.ts"
      provides: "JSON-RPC proxy forwarding browser reads/writes to the fork RPC (CORS fallback)"
      exports: ["POST", "runtime"]
    - path: "lib/apps/abrigo/cornerstone/balance-delta.ts"
      provides: "decodeBalanceDelta sign-extending decoder"
      exports: ["decodeBalanceDelta"]
    - path: "lib/apps/abrigo/cornerstone/artifact-loader.ts"
      provides: "typed loader + isExpired(nowMs)"
      exports: ["deployment", "isExpired"]
    - path: "lib/apps/abrigo/cornerstone/buildbear.ts"
      provides: "createBuildBearChain + public client from artifact rpcUrl"
      exports: ["createBuildBearChain", "createBuildBearPublicClient", "BuildBearChainId"]
    - path: "lib/apps/abrigo/cornerstone/token-id.ts"
      provides: "extractStrike(positionId) Panoptic TokenId leg-0 strike (offset 76, sdk-cross-checked)"
      exports: ["extractStrike"]
  key_links:
    - from: "lib/apps/abrigo/cornerstone/buildbear.ts"
      to: "lib/apps/abrigo/cornerstone/artifact-loader.ts"
      via: "rpcUrl sourced from deployment artifact, never a literal"
      pattern: "deployment\\.rpcUrl|createBuildBearChain\\(.*rpcUrl"
    - from: "tests/e2e/cornerstone-replay-smoke.spec.ts"
      to: "app/(defi)/apps/abrigo/cornerstone/page.tsx"
      via: "navigate in replay mode with the fork RPC unreachable + assert snapshot decision/mint rendered"
      pattern: "replay|cornerstone"
    - from: "app/api/cornerstone/rpc/route.ts"
      to: "lib/apps/abrigo/cornerstone/artifact-loader.ts deployment.rpcUrl"
      via: "JSON-RPC proxy forwards to the artifact rpcUrl"
      pattern: "deployment\\.rpcUrl|fetch\\("
---

<objective>
T0 + T1 (governing plan §4): FREEZE the replay fallback FIRST (the GUARANTEED in-phase artifact), then lay the live data-layer foundation. Tag the working build `cornerstone-replay-safe`, add the replay smoke test that becomes the CI gate AND proves replay renders with the fork RPC down, introduce the `live | replay | mock` mode type (replay default), then generate the real ABIs via wagmi codegen (incl. the MacroOracle latest read), register the BuildBear fork as a 6th wagmi chain, mirror the fork artifact, ship the JSON-RPC CORS proxy, and write the pure decoders (BalanceDelta sign-extension, Panoptic TokenId strike at the sdk-cross-checked offset 76, isExpired) — all TDD, failing-first.

Purpose: replay is the GUARANTEED in-phase artifact (the live on-chain RUN is gated ⊘ on the external Somnia validator-callback outage); nothing in the live build may break replay, and replay must not depend on a reachable RPC or an unexpired TTL. The decoders + ABIs + typed artifact + RPC proxy are the load-bearing edges (the BigInt/int24 burn class) every downstream live task depends on.
Output: replay smoke spec + git tag, mode.ts, lib/contracts/generated.ts (wagmi), mirrored buildbear-deployments.json, app/api/cornerstone/rpc/route.ts, balance-delta.ts, token-id.ts, artifact-loader.ts, buildbear.ts + 5 unit/e2e test files.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/cornerstone-live-twochain-PLAN.md
@docs/superpowers/specs/2026-06-07-module5-cornerstone-live-tx-design.md
@.planning/phases/09-cornerstone-live-tx-integration/09-RESEARCH.md
@.planning/phases/09-cornerstone-live-tx-integration/09-VALIDATION.md

<interfaces>
<!-- Existing project patterns the executor reuses directly. -->

From lib/apps/abrigo/somnia/chain.ts (the defineChain + createPublicClient template):
```typescript
export const SomniaChainId = 50312 as const
export const somniaTestnet = defineChain({ /* id, name, nativeCurrency, rpcUrls */ })
export const somniaClient = createPublicClient({ chain: somniaTestnet, transport: http(...) })
```

From wagmi.config.ts (Phase-1 placeholder to repoint — currently points at ../abrigo, out/):
```typescript
export default defineConfig({ out: 'lib/contracts/generated.ts',
  plugins: [ foundry({ project: '../abrigo', artifacts: 'out/' }), react() ] })
```

Existing replay/snapshot data: lib/apps/abrigo/somnia/snapshot.json (the real captured Somnia trace the cornerstone reveals).
Existing cornerstone page: app/(defi)/apps/abrigo/cornerstone/page.tsx (Phase 8 shell + RunTranscript). Existing e2e: tests/e2e/cornerstone.spec.ts.
Source artifact to MIRROR: /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/script/out/buildbear-deployments.json
Compiled ABIs: /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/out/{MacroHedgeStrategist.sol,MacroHedgeExecutor.sol,IPanopticData.sol} + the MacroOracle latest(bytes32) read (codegen include OR the minimal inline ABI tuple documented in 09-02).
Existing Somnia keeper-proxy Route Handler (the CORS-proxy pattern to mirror): grep app/api for the existing JSON-RPC forward route.
Panoptic TokenId strike(0) offset: contracts/lib/panoptic-sdk/src/generated.ts — the spec/RESEARCH pin offset 76; cross-check against the generated source, do NOT reverse-fit from the fixture.
package.json already defines "contracts:gen": "wagmi generate".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: T0 — freeze replay FIRST (git tag + RPC-independent replay smoke = CI gate) + mode type</name>
  <read_first>
    - docs/cornerstone-live-twochain-PLAN.md T0 (§4) + §8 risk note (replay is the guaranteed fallback; live work must never break it)
    - spec v5 reframe block (replay = GUARANTEED in-phase artifact, TTL- AND RPC-independent; the live RUN is ⊘ DEFERRED) + governance block (three modes live|replay|mock; replay frozen first via git tag cornerstone-replay-safe)
    - app/(defi)/apps/abrigo/cornerstone/page.tsx (FULL — how the current build renders the recorded snapshot trace end-to-end)
    - tests/e2e/cornerstone.spec.ts (existing Phase-8 specs; playwright.config.ts uses a PRODUCTION webServer — e2e runs the prod build)
    - lib/apps/abrigo/somnia/snapshot.json (the real captured receipts replay renders)
  </read_first>
  <action>
    1. Tag the current working build as the guaranteed artifact: `git tag -a cornerstone-replay-safe -m "frozen replay fallback before Phase 9 live work"`. Do NOT push (tags pushed only when the user asks).
    2. Create `lib/apps/abrigo/cornerstone/mode.ts`: `export type CornerstoneMode = 'live' | 'replay' | 'mock'`; `export const DEFAULT_MODE: CornerstoneMode = 'replay'` (replay frozen first — the guaranteed default); `export function parseMode(raw: string | null | undefined): CornerstoneMode` returning 'live'|'mock' only for exact matches, else DEFAULT_MODE.
    3. Create `tests/unit/cornerstone/mode.test.ts`: assert `DEFAULT_MODE === 'replay'`; `parseMode('live')==='live'`; `parseMode('mock')==='mock'`; `parseMode('garbage')==='replay'`; `parseMode(null)==='replay'`.
    4. Create `tests/e2e/cornerstone-replay-smoke.spec.ts` — the CI gate, RPC-INDEPENDENT. Navigate to the cornerstone route in REPLAY mode (the current default rendering of the recorded snapshot). To prove replay does NOT read the live RPC at render, block/stub the fork RPC at the network layer for the test (Playwright `page.route` → abort any request to the artifact rpcUrl host) BEFORE navigation; the page must STILL render the recorded Agent-1 decision AND the recorded mint evidence end-to-end from snapshot.json. PIN the assertion anchors in this plan (do not let the executor invent them): assert the recorded school/decision thesis string AND the recorded mint strike "360360" both appear in the DOM (read snapshot.json to confirm these exact strings are what the page renders; if the page formats the strike, assert the formatted "5.68% (tick 360360)" anchor instead — pin whichever the page actually renders, but it MUST include 360360). This test MUST pass against the current build WITH the fork RPC aborted, and is the gate later live work cannot break.
    Document in the SUMMARY: this smoke test is the CI gate; it asserts replay is RPC-independent; live work is verified separately and must keep this green.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone/mode.test.ts && pnpm build && pnpm exec playwright test tests/e2e/cornerstone-replay-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git tag` lists `cornerstone-replay-safe` (run `git tag | grep cornerstone-replay-safe`)
    - mode.ts exports `CornerstoneMode`, `DEFAULT_MODE` (=== 'replay'), `parseMode` (grep all three)
    - mode.test.ts asserts DEFAULT_MODE is 'replay' and garbage/null fall back to replay
    - cornerstone-replay-smoke.spec.ts aborts the fork RPC host via `page.route` BEFORE navigation (grep `page.route` + `abort`) and STILL asserts the snapshot decision + "360360" mint anchor render
    - the replay smoke test passes against the current production build WITH the fork RPC unreachable (the CI gate is green BEFORE any live change, and is RPC-independent)
  </acceptance_criteria>
  <done>Replay is frozen (tag + green RPC-independent smoke gate) and the live|replay|mock mode type exists with replay as the guaranteed default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: T1 — wagmi-codegen ABIs (3 contracts + MacroOracle latest) + register fork as 6th wagmi chain + mirror artifact (RED then GREEN)</name>
  <read_first>
    - docs/cornerstone-live-twochain-PLAN.md T1 (§4) + §2 D2/D3 (fork = 6th wagmi chain from artifact rpcUrl; Somnia NOT a browser chain; wagmi codegen include filter)
    - spec §2 D3 (codegen) + the v5 fix-5 note (the MacroOracle latest(dataKey).deliveredAt read needs an ABI — add IMacroOracleLatest to the codegen include filter OR document the minimal inline ABI tuple for the single-field read; the 09-02 route uses it for the oracle-freshness pre-check)
    - wagmi.config.ts (the Phase-1 placeholder) + package.json scripts (contracts:gen = wagmi generate)
    - lib/apps/abrigo/somnia/chain.ts (defineChain + createPublicClient template) + lib/wagmi/config.ts (FULL — getDefaultConfig, 5-chain array, transports, ssr:false)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/out/ (confirm MacroHedgeStrategist.sol/, MacroHedgeExecutor.sol/, IPanopticData.sol/ + the MacroOracle/IMacroOracleLatest artifact exist)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/script/out/buildbear-deployments.json (source to copy)
    - 09-RESEARCH.md Pattern 1 + Pattern 7 (artifact loader, isExpired, Turbopack-safe static import); spec §0.3 (no hardcoded fork addrs; 3-day TTL)
    - MEMORY (STATE.md): static `import x from './x.json'` not dynamic require — Turbopack JSON bundling lesson
  </read_first>
  <action>
    RED: write `tests/unit/cornerstone/artifact-loader.test.ts` asserting: `deployment.chainId === 31337`; `deployment.executor`/`deployment.pool`/`deployment.rpcUrl`/`deployment.capturedAt` present non-empty; `isExpired(capturedAtMs + 2*86400e3) === false`; `isExpired(capturedAtMs + 3*86400e3 + 1) === true`. Run, confirm fail (module missing).
    GREEN:
    1. Repoint `wagmi.config.ts`: `project: '../abrigo/abrigo-somnia/contracts'`, `artifacts: 'out/'`, `include: ['MacroHedgeStrategist.json', 'MacroHedgeExecutor.json', 'IPanopticData.json', 'MacroOracle.json']` (or the IMacroOracleLatest interface that carries `latest(bytes32)`; if the oracle ABI can't be generated cleanly, document the minimal inline ABI tuple `latest(bytes32) returns (..., uint64 deliveredAt, int256 scaledValue, ...)` in a comment for 09-02 to use). Keep `out: 'lib/contracts/generated.ts'` + `react()`. Run `pnpm contracts:gen`; commit the generated file. Confirm exports `macroHedgeStrategistAbi` (with `StrategistDecided`, `HedgeDecisionRequested`, `DecisionFailed`, `requestSchoolDecision`, `requestNotionalDecision`, `decisionState`), `macroHedgeExecutorAbi` (8-field `ExecutorDecided`, `PositionMinted`, `resolveFromMandate`, `quoteMargin`), `iPanopticDataAbi` (`numberOfLegs`), and the MacroOracle `latest` read. Do NOT add `contracts:gen` to `prebuild`.
    2. Copy the source `buildbear-deployments.json` to `lib/apps/abrigo/cornerstone/buildbear-deployments.json` (committed, values unedited).
    3. Create `lib/apps/abrigo/cornerstone/artifact-loader.ts`: `import rawDeployment from './buildbear-deployments.json'` (static relative — Turbopack-safe). Export typed `BuildBearDeployment` (chainId, executor, pool, riskManagement, rpcUrl, mintTxHash, mintedStrike, capturedAt, source). Validate required fields at module load (throw if executor/pool/rpcUrl/capturedAt missing). Export `deployment` + `isExpired(nowMs): boolean` with `TTL_MS = 3*24*60*60*1000`.
    4. Create `lib/apps/abrigo/cornerstone/buildbear.ts` (mirror somnia/chain.ts): `export const BuildBearChainId = 31337 as const`; `export function createBuildBearChain(rpcUrl: string)` = `defineChain({ id:31337, name:'BuildBear Polygon Fork', nativeCurrency:{name:'MATIC',symbol:'MATIC',decimals:18}, rpcUrls:{default:{http:[rpcUrl]}} })`; `export function createBuildBearPublicClient(rpcUrl: string)` = `createPublicClient({ chain:createBuildBearChain(rpcUrl), transport:http(rpcUrl) })`. rpcUrl ALWAYS passed in (from `deployment.rpcUrl`).
    5. Register the fork as the 6th wagmi chain in `lib/wagmi/config.ts` (D2): `const buildBearFork = createBuildBearChain(deployment.rpcUrl)`; append to the `chains` tuple AND the getDefaultConfig `chains` array AND add a `transports` entry `[BuildBearChainId]: http(deployment.rpcUrl)`. Widen the exported `SupportedChainId`/chains type to include 31337. rpcUrl ONLY from `deployment.rpcUrl` — no literal endpoint. Do NOT add Somnia (50312) — it stays server-side only.
  </action>
  <verify>
    <automated>pnpm contracts:gen && grep -q "macroHedgeStrategistAbi" lib/contracts/generated.ts && grep -q "iPanopticDataAbi" lib/contracts/generated.ts && grep -q "numberOfLegs" lib/contracts/generated.ts && pnpm vitest run tests/unit/cornerstone/artifact-loader.test.ts && grep -qE "31337|BuildBearChainId" lib/wagmi/config.ts</automated>
  </verify>
  <acceptance_criteria>
    - generated.ts exports `macroHedgeStrategistAbi`, `macroHedgeExecutorAbi`, `iPanopticDataAbi`; grep `StrategistDecided`, `decisionState`, `ExecutorDecided`, `numberOfLegs` all present
    - the MacroOracle `latest` read is available — either generated in generated.ts (grep `latest`) OR a documented minimal inline ABI tuple comment in wagmi.config.ts / artifact for 09-02 to consume
    - wagmi.config.ts `project` points at the abrigo-somnia contracts dir (grep `abrigo-somnia/contracts`); `contracts:gen` NOT in `prebuild`
    - buildbear-deployments.json present, byte-identical to source (chainId 31337, rpcUrl present)
    - artifact-loader.ts uses static `import ... from './buildbear-deployments.json'` (grep, NOT `require(`); exports `deployment` + `isExpired`; boundary tests pass (false at +2d, true at +3d+1ms)
    - buildbear.ts exports BuildBearChainId/createBuildBearChain/createBuildBearPublicClient; grep `rpc.buildbear.io` in buildbear.ts AND lib/wagmi/config.ts both empty (no hardcoded endpoint)
    - lib/wagmi/config.ts chains array length is 6 incl. 31337; transports has a 31337 entry sourced from `deployment.rpcUrl`; Somnia 50312 NOT added
  </acceptance_criteria>
  <done>Real ABIs (incl. the MacroOracle latest read) are wagmi-generated; the fork is a registered 6th wagmi chain (rpcUrl from artifact); the artifact is mirrored with a typed loader + isExpired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BalanceDelta decoder + Panoptic TokenId strike extractor (offset 76, sdk-cross-checked) — RED then GREEN</name>
  <read_first>
    - 09-RESEARCH.md Pattern 4 (decodeBalanceDelta low-word sign-extension) + "TokenId.strike(0) extraction" + Open Question 1 + line 515 (pin offset against panoptic-sdk generated.ts)
    - spec §2 M1 (amount0 = asIntN(128, v>>128n); amount1 = asIntN(128, v & ((1n<<128n)-1n))) + §6 acceptance (negative BalanceDelta + negative int24) + v5 EVM-validated note (TokenId strike offset 76)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia/contracts/lib/panoptic-sdk/src/generated.ts — CROSS-CHECK the leg-0 strike bit offset is 76 against the generated source (do NOT reverse-fit it from the fixture)
    - the mirrored artifact's mintTxHash + mintedStrike 360360 (the assertion anchor)
  </read_first>
  <action>
    RED: write `tests/unit/cornerstone/balance-delta.test.ts` and `tests/unit/cornerstone/token-id.test.ts`.
    balance-delta.test.ts: helper `toBalanceDelta(a0,a1) = (a0 << 128n) | BigInt.asUintN(128, a1)`; assert `decodeBalanceDelta(toBalanceDelta(100n,-50n)) === {amount0:100n, amount1:-50n}`; `amount1 = -1n` and `amount1 = -(1n<<127n)` (min int128) round-trip; positive `{100n,50n}`.
    token-id.test.ts: `extractStrike(<recorded positionId>) === 360360`. First read panoptic-sdk generated.ts to CONFIRM the leg-0 strike bit offset is 76 (cite the source line in a comment) + the recorded positionId for the mintTxHash; encode the recorded positionId as a const with a source-citing comment. Run both — confirm fail.
    GREEN:
    1. `lib/apps/abrigo/cornerstone/balance-delta.ts`: `export function decodeBalanceDelta(rawInt256: bigint): { amount0: bigint; amount1: bigint }` — `amount0 = BigInt.asIntN(128, rawInt256 >> 128n)`, `amount1 = BigInt.asIntN(128, rawInt256 & ((1n<<128n)-1n))`.
    2. `lib/apps/abrigo/cornerstone/token-id.ts`: `export function extractStrike(positionId: bigint): number` — leg-0 int24 strike at offset 76, `Number(BigInt.asIntN(24, (positionId >> 76n) & ((1n<<24n)-1n)))`. Document the OFFSET=76 const with the panoptic-sdk generated.ts source line (the cross-check, not a reverse-fit).
    If a Wave-0 RED stub imports a not-yet-created module, exclude it from tsconfig per the project's documented stub pattern, re-include once GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone/balance-delta.test.ts tests/unit/cornerstone/token-id.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `decodeBalanceDelta(toBalanceDelta(100n,-50n))` returns `{amount0:100n, amount1:-50n}`; min-int128 `-(1n<<127n)` round-trips
    - `extractStrike(recordedPositionId) === 360360` passes
    - balance-delta.ts uses `BigInt.asIntN(128, ...)` for the low word (grep `asIntN(128`); NOT a bare `& mask` returned
    - token-id.ts pins offset 76 with a panoptic-sdk generated.ts source-citing comment (grep `76` + the sdk citation; NOT reverse-fit)
    - `pnpm tsc --noEmit` passes (no leaked stubs)
  </acceptance_criteria>
  <done>Both sign-correct decoders implemented and unit-tested incl. the negative fixtures and the 360360 strike anchor (offset 76 cross-checked against the sdk).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: CORS Route-Handler JSON-RPC proxy (eth_chainId probe + forward) — RED then GREEN</name>
  <read_first>
    - spec §3 "Server proxy" (if BuildBear RPC is CORS-blocked from the browser, verify with one eth_chainId fetch on mount, route reads/writes through a Next Route Handler forwarding JSON-RPC; mirror the existing Somnia keeper-proxy pattern) + v5 fix-3 (the proxy ships REGARDLESS; the eth_chainId probe decides direct-vs-proxy)
    - the existing Somnia keeper/JSON-RPC proxy Route Handler in app/api (grep app/api for the forward pattern) + app/api/health/route.ts (Route Handler shape)
    - lib/apps/abrigo/cornerstone/artifact-loader.ts (deployment.rpcUrl — the forward target, from the artifact, never a literal)
  </read_first>
  <action>
    RED: write `tests/unit/cornerstone/rpc-proxy.test.ts` (mock global fetch): POST a JSON-RPC body `{jsonrpc:'2.0',id:1,method:'eth_chainId'}` to the route's POST handler; assert it forwards the body to `deployment.rpcUrl` (assert the mocked fetch was called with that URL + the JSON body) and returns the upstream JSON response; on an upstream non-200 / network throw it returns a typed JSON-RPC-shaped error (never throws an unhandled 500). Run, confirm fail.
    GREEN: create `app/api/cornerstone/rpc/route.ts`:
    - `export const runtime = 'nodejs'`.
    - `export async function POST(req)`: read the JSON body; forward it via `fetch(deployment.rpcUrl, { method:'POST', headers:{'content-type':'application/json'}, body: JSON.stringify(payload) })`; return the upstream JSON. Wrap in try/catch → return a `{ jsonrpc:'2.0', id, error:{ code:-32000, message } }` shaped body with a 200/502 status (never an unhandled crash). Forward target ALWAYS `deployment.rpcUrl` (from the artifact). Optionally allowlist read-ish methods, but for the demo forward verbatim.
    Document in the SUMMARY: the page does one `eth_chainId` browser probe on mount against the direct fork RPC; on CORS/network failure it falls back to POSTing through this proxy (wired in 09-04).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/cornerstone/rpc-proxy.test.ts && grep -q "runtime = 'nodejs'" app/api/cornerstone/rpc/route.ts && grep -q "deployment.rpcUrl" app/api/cornerstone/rpc/route.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/api/cornerstone/rpc/route.ts exists with `runtime = 'nodejs'` and a POST handler that forwards to `deployment.rpcUrl` (grep both)
    - rpc-proxy.test.ts asserts the eth_chainId body is forwarded to the artifact rpcUrl and the upstream response is returned
    - upstream failure returns a JSON-RPC-shaped error, not an unhandled 500 (test asserts)
    - no hardcoded RPC endpoint in the route (grep `rpc.buildbear.io` empty; forward target is `deployment.rpcUrl`)
  </acceptance_criteria>
  <done>The JSON-RPC CORS proxy ships and forwards to the artifact rpcUrl with typed error handling; the mount-time eth_chainId probe (wired in 09-04) decides direct-vs-proxy.</done>
</task>

</tasks>

<verification>
- `git tag` includes `cornerstone-replay-safe`; replay smoke spec green on the prod build WITH the fork RPC aborted (the RPC-independent CI gate).
- `pnpm contracts:gen` regenerates the ABIs (incl. MacroOracle latest); `pnpm vitest run tests/unit/cornerstone/` green (mode + decoder/loader + rpc-proxy files).
- `pnpm tsc --noEmit` passes. grep: no hardcoded `rpc.buildbear.io` in buildbear.ts, lib/wagmi/config.ts, or the rpc route; no `require(` in artifact-loader.ts.
- lib/wagmi/config.ts has 6 chains (incl. 31337); Somnia 50312 NOT added. app/api/cornerstone/rpc/route.ts forwards to deployment.rpcUrl.
</verification>

<success_criteria>
Replay is frozen first (tag + green RPC-INDEPENDENT CI gate); the live|replay|mock mode type exists (replay default); the ABIs (incl. MacroOracle latest) are wagmi-generated; the fork is a 6th wagmi chain (rpcUrl from artifact); the JSON-RPC CORS proxy ships; artifact mirrored + isExpired honored; BalanceDelta + TokenId decoders sign-correct and test-covered (negative amount1 + strike===360360 at sdk-cross-checked offset 76).
</success_criteria>

<output>
After completion, create `.planning/phases/09-cornerstone-live-tx-integration/09-01-SUMMARY.md`.
The replay smoke spec is a rendered-route assertion (already covered by Playwright against the prod build) — no separate Evidence Collector pass needed for this pure data-layer/config/test plan; note so per CLAUDE.md "When to skip". The RPC proxy is verified by `browser_network_request`/route unit test, not a rendered surface. Record the cornerstone-replay-safe tag + that the smoke test is the RPC-independent CI gate.
</output>
</content>
</invoke>

---
phase: 04-agent-surface-mcp
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/openapi/schemas.ts
  - lib/openapi/generate.ts
  - app/.well-known/openapi.yaml/route.ts
  - app/llms.txt/route.ts
  - tests/api/openapi.test.ts
autonomous: true
requirements: [AGENT-08, AGENT-09]
must_haves:
  truths:
    - "/.well-known/openapi.yaml serves a valid OpenAPI 3.1.0 document generated from the SAME canonical Zod schemas in lib/mcp-tools/contract.ts (NOT re-declared here), documenting /api/dashboard, /api/status, /api/health, and /api/mcp with response examples"
    - "The OpenAPI spec is generated from the same Zod schemas the routes conform to (proven by the Plan-01 conformance test), so it cannot drift from actual response shapes (B3 single source of truth)"
    - "extendZodWithOpenApi(z) is NOT called here — it was called once in contract.ts (M6); this module imports the already-extended schemas"
    - "/api/health example uses runtime:'node' to match the live route (M5), not 'nodejs'"
    - "/llms.txt lists primary entry URLs (no stale /dashboard or /status page 404s), content licensing, and the MCP streamable-http endpoint pointer (/api/mcp/mcp)"
  artifacts:
    - path: "lib/openapi/generate.ts"
      provides: "generateOpenApiYaml(): string — OpenApiGeneratorV31 → js-yaml dump"
      exports: ["generateOpenApiYaml"]
    - path: "lib/openapi/schemas.ts"
      provides: "OpenAPIRegistry registering the canonical schemas IMPORTED from contract.ts + registerPath calls (no schema re-declaration, no second extendZodWithOpenApi)"
      exports: ["registry"]
    - path: "app/.well-known/openapi.yaml/route.ts"
      provides: "GET returning generateOpenApiYaml() with application/yaml content-type"
  key_links:
    - from: "lib/openapi/schemas.ts"
      to: "lib/mcp-tools/contract.ts"
      via: "import DashboardResponseSchema/StatusResponseSchema/HealthResponseSchema/etc — single source of truth"
      pattern: "from '@/lib/mcp-tools/contract'"
    - from: "lib/openapi/generate.ts"
      to: "@asteasolutions/zod-to-openapi OpenApiGeneratorV31 + js-yaml"
      via: "generator.generateDocument() then yaml.dump()"
      pattern: "OpenApiGeneratorV31"
---

<objective>
Replace the hand-written OpenAPI YAML stub with a drift-proof spec generated from the canonical Zod schemas defined in Plan 01's `lib/mcp-tools/contract.ts` (NOT a fresh re-declaration — reviewer B3 single source of truth), documenting every public REST endpoint with response examples (AGENT-08), and update `/llms.txt` to reflect the now-live MCP + OpenAPI surface with correct URLs (AGENT-09).

Purpose: A generated spec sharing ONE Zod definition with the routes (and proven equal by the Plan-01 conformance test) cannot diverge from actual route responses — directly addressing the Phase-2/3 burn class where hand-maintained boundary artifacts drifted from runtime shapes.
Output: lib/openapi/{schemas,generate}.ts, the openapi.yaml + llms.txt routes, openapi test green.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-agent-surface-mcp/04-RESEARCH.md

<interfaces>
From lib/mcp-tools/contract.ts (Plan 01 — the canonical Zod schemas to IMPORT, never re-declare):
```typescript
export const DashboardResponseSchema   // version:1, app, status, chains[], fetchedAt
export const StatusResponseSchema      // version:1, status, build, timestamp, chains[], apps record
export const ChainAggregationResultSchema, InstrumentStateSchema, ChainHealthSchema
export const HealthResponseSchema       // { status, build, runtime, timestamp } — runtime is a string; LIVE value is 'node'
// extendZodWithOpenApi(z) was called ONCE in contract.ts. DO NOT call it again here (M6).
```
GROUND TRUTH — app/api/health/route.ts returns `runtime: 'node'` (NOT 'nodejs'). The OpenAPI example MUST use 'node' (M5).
@asteasolutions/zod-to-openapi@7.3.4 (04-RESEARCH § Pattern 4 / Code Examples):
```typescript
import { OpenApiGeneratorV31, OpenAPIRegistry } from '@asteasolutions/zod-to-openapi'
// Do NOT import or call extendZodWithOpenApi here — contract.ts owns the single call.
```
GROUND TRUTH — current app/.well-known/openapi.yaml/route.ts: `export const dynamic='force-static'; export function GET()` returning a hardcoded YAML string with only /api/health (`runtime: { example: node }`). REPLACE the body source.
GROUND TRUTH — current app/llms.txt/route.ts lists: `https://d2pfinance.xyz/dashboard (live protocol state — coming Phase 3)` (STALE — real route is `/apps/abrigo/dashboard`; there is NO top-level `/dashboard`), `/api/mcp/mcp`, `/api/mcp/sse`, `/.well-known/openapi.yaml`, `/.well-known/mcp.json`. There is NO page at `/status` (only `/api/status`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenAPI schemas (import canonical) + generator from Zod (AGENT-08)</name>
  <files>lib/openapi/schemas.ts, lib/openapi/generate.ts, tests/api/openapi.test.ts</files>
  <read_first>
    - lib/mcp-tools/contract.ts (the canonical DashboardResponseSchema/StatusResponseSchema/HealthResponseSchema/etc — IMPORT these; do NOT redefine; extendZodWithOpenApi already called there)
    - app/api/health/route.ts (GROUND TRUTH: returns runtime:'node' — the example must match)
    - 04-RESEARCH.md § "Pattern 4: OpenAPI Generation from Zod" + § "Code Examples / @asteasolutions/zod-to-openapi v7 pattern" + § "Pitfall 6: force-static"
    - tests/api/openapi.test.ts (Wave-0 scaffold from Plan 01 — un-fixme its assertions)
    - tests/api/openapi-conformance.test.ts (Plan 01 — un-fixme it now that schemas are wired; it proves spec ≡ live route)
  </read_first>
  <action>
    `lib/openapi/schemas.ts`: `import { OpenAPIRegistry } from '@asteasolutions/zod-to-openapi'`. Import the canonical schemas from the single source: `import { DashboardResponseSchema, StatusResponseSchema, HealthResponseSchema, ChainAggregationResultSchema, ChainHealthSchema, InstrumentStateSchema } from '@/lib/mcp-tools/contract'`. DO NOT import or call `extendZodWithOpenApi` here, and DO NOT redefine any of these schemas (M6 + B3 — re-declaration is the drift bug we are eliminating; the architecture test in Plan 01 asserts there is no second `extendZodWithOpenApi(` in this file). `export const registry = new OpenAPIRegistry()`. `registry.register('DashboardResponse', DashboardResponseSchema)`, etc. for each.
    Then `registry.registerPath(...)` for FOUR paths with a `200` response containing `content['application/json'].schema` AND an `example`:
      - GET `/api/health` → HealthResponseSchema, example `{ status:'ok', build:'local', runtime:'node', timestamp:'2026-05-29T00:00:00.000Z' }` (M5: `runtime:'node'`, matching the live route — NOT 'nodejs')
      - GET `/api/dashboard` (query param `app`, default 'abrigo') → DashboardResponseSchema, example showing a chain with `status:'empty'` and empty `instruments:[]` (honest pre-launch — no fabricated balances)
      - GET `/api/status` → StatusResponseSchema, example with a healthy + a degraded chain
      - POST `/api/mcp/mcp` → the MCP streamable-http JSON-RPC endpoint. OpenAPI cannot usefully type a JSON-RPC envelope, so DESCRIBE it in prose in the path `description` and give ONE example request body. Use a clean prose description plus an example JSON-RPC body, e.g. request `application/json` example `{ jsonrpc:'2.0', id:1, method:'tools/list', params:{} }` and a note that the response is `application/json` or `text/event-stream` framing a JSON-RPC result. Do NOT register a garbled schema name — no leftover tokens (the prior draft had a malformed "Mthe McpJsonRpc"; replace it entirely with the prose+example approach).
    `lib/openapi/generate.ts`: `import { OpenApiGeneratorV31 } from '@asteasolutions/zod-to-openapi'; import yaml from 'js-yaml'; import { registry } from './schemas'`. `export function generateOpenApiYaml(): string { const doc = new OpenApiGeneratorV31(registry.definitions).generateDocument({ openapi:'3.1.0', info:{ title:'d2p Finance Public API', version:'0.1.0', description:'…' }, servers:[{ url:'https://www.d2pfinance.xyz', description:'Production' }] }); return yaml.dump(doc) }`. Use `js-yaml`'s `dump` (NOT hand-rolled string concat — 04-RESEARCH "Don't Hand-Roll").
    Un-fixme the assertions in `tests/api/openapi.test.ts` (`// @vitest-environment node`): call `generateOpenApiYaml()`, assert the string contains `openapi: 3.1.0`, `/api/dashboard`, `/api/status`, `/api/health`, `/api/mcp`, AND `runtime` example value `node` (not `nodejs`); assert it parses back via `yaml.load()` without throwing. ALSO un-fixme `tests/api/openapi-conformance.test.ts` (the B3 proof — live /api/dashboard + /api/status responses parse against the imported canonical schemas).
  </action>
  <verify>
    <automated>vitest run tests/api/openapi.test.ts tests/api/openapi-conformance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/openapi/schemas.ts` contains `OpenAPIRegistry`, `registerPath`, `from '@/lib/mcp-tools/contract'`, and all four path strings `/api/health` `/api/dashboard` `/api/status` `/api/mcp`
    - `lib/openapi/schemas.ts` does NOT contain `extendZodWithOpenApi` (grep no match — M6) and does NOT re-declare `DashboardResponseSchema`/`StatusResponseSchema` (no `const DashboardResponseSchema =` in this file)
    - `lib/openapi/schemas.ts` does NOT contain the garbled token `Mthe McpJsonRpc` (grep no match)
    - the /api/health example uses `node` not `nodejs` (grep the file: no `nodejs` near the health example)
    - `lib/openapi/generate.ts` contains `OpenApiGeneratorV31`, `'3.1.0'`, and `yaml.dump`
    - `tests/api/openapi.test.ts` and `tests/api/openapi-conformance.test.ts` no longer contain `test.fixme`
    - `vitest run tests/api/openapi.test.ts tests/api/openapi-conformance.test.ts` exits 0
  </acceptance_criteria>
  <done>generateOpenApiYaml() emits a valid OpenAPI 3.1.0 doc from the canonical (imported) Zod schemas covering all four endpoints with examples (health runtime:'node'); the conformance test proves spec ≡ live route; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire openapi.yaml route + refresh llms.txt (AGENT-08, AGENT-09)</name>
  <files>app/.well-known/openapi.yaml/route.ts, app/llms.txt/route.ts</files>
  <read_first>
    - app/.well-known/openapi.yaml/route.ts (current hardcoded GET to replace)
    - app/llms.txt/route.ts (current URL list to refresh — note the STALE /dashboard and the absence of a /status page)
    - lib/openapi/generate.ts (Task 1 — generateOpenApiYaml)
    - tests/e2e/agent-stubs.spec.ts (the live openapi.yaml/llms.txt tests that must stay green — they assert `openapi: 3.1.0`, content-type yaml, and llms.txt contains `/api/mcp`)
    - 04-RESEARCH.md § "Pitfall 6: force-static" (keep force-static; schemas are static)
  </read_first>
  <action>
    `app/.well-known/openapi.yaml/route.ts`: replace the hardcoded YAML body. Keep `export const dynamic = 'force-static'`. `import { generateOpenApiYaml } from '@/lib/openapi/generate'`. Compute `const body = generateOpenApiYaml()` at module level (executed once at build), then `export function GET(){ return new Response(body, { status:200, headers:{ 'Content-Type':'application/yaml; charset=utf-8' } }) }`. The generated `body` MUST still satisfy the existing e2e assertion `body.toContain('openapi: 3.1.0')` (js-yaml emits it as a top-level scalar).
    `app/llms.txt/route.ts`: keep `force-static`. Update the body:
      - REMOVE the stale `https://d2pfinance.xyz/dashboard (live protocol state — coming Phase 3)` line entirely — there is NO top-level `/dashboard` route (it would 404). Replace with the real `https://www.d2pfinance.xyz/apps/abrigo/dashboard` (Abrigo live protocol state).
      - Do NOT advertise a `/status` page URL (there is no page at `/status`, only the JSON route `/api/status`). If you want to surface status, list `/api/status` under a machine/API section, not as a primary human URL.
      - Under "## Agent endpoints": mark `/api/mcp/mcp` as the primary (canonical) streamable-http transport; note `/api/mcp/sse` returns 404 (SSE disabled, no Redis). Keep `/.well-known/openapi.yaml` and `/.well-known/mcp.json` listed.
      - Ensure the body still contains `d2p Finance` and `/api/mcp` (the existing e2e test asserts both).
  </action>
  <verify>
    <automated>pnpm test:quick</automated>
  </verify>
  <acceptance_criteria>
    - `app/.well-known/openapi.yaml/route.ts` contains `generateOpenApiYaml` and `force-static`; does NOT contain a hardcoded `paths:` YAML block anymore (grep `paths:` returns no match in this file)
    - `app/llms.txt/route.ts` contains `/api/mcp/mcp`, mentions SSE disabled/404, and `/apps/abrigo/dashboard`
    - `app/llms.txt/route.ts` does NOT contain the stale top-level URL `d2pfinance.xyz/dashboard` (grep no match) and does NOT advertise a primary `/status` PAGE url
    - `pnpm test:quick` exits 0
  </acceptance_criteria>
  <done>openapi.yaml route serves the generated spec; llms.txt reflects the live MCP/OpenAPI surface with correct URLs (no /dashboard or /status page 404); quick suite green.</done>
</task>

</tasks>

<verification>
- `vitest run tests/api/openapi.test.ts tests/api/openapi-conformance.test.ts` exits 0
- the openapi.yaml route returns the generated spec containing `openapi: 3.1.0` and all four paths; health example uses runtime:'node'
- schemas.ts imports canonical schemas from contract.ts, no second extendZodWithOpenApi, no re-declaration, no garbled token
- llms.txt lists the canonical `/api/mcp/mcp` endpoint and `/apps/abrigo/dashboard`; no stale `/dashboard`/`/status` page URLs
</verification>

<success_criteria>
- AGENT-08: drift-proof OpenAPI 3.1 generated from the SINGLE canonical Zod source (B3), four endpoints + examples (health runtime:'node' — M5), JSON-RPC documented in prose+example (no garbled token)
- AGENT-09: llms.txt points at the live MCP endpoint + OpenAPI spec, correct URLs only
- No hand-rolled YAML; js-yaml dump from OpenApiGeneratorV31; no double extendZodWithOpenApi (M6)
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-surface-mcp/04-04-SUMMARY.md`
</output>

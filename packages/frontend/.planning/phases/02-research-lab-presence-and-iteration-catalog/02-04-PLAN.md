---
phase: 02-research-lab-presence-and-iteration-catalog
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - content/iterations/pair-d/v1.mdx
  - content/iterations/fx-vol-on-cpi-surprise/v1.mdx
  - content/iterations/sample/v1.mdx
  - content/iterations/abrigo-y3-carbon-basket/v1.mdx
  - content/iterations/pair-b-bittensor/v1.mdx
autonomous: true
agent: Frontend Developer
requirements: [ITER-05, ITER-06, ITER-08]
must_haves:
  truths:
    - "ITER-05: content/iterations/pair-d/v1.mdx exists with frontmatter status: PASS, beta: 0.13670985, p_value: 1.46e-8 (real value from abrigo dispatch brief), and 5-section Spec → Data → Estimation → Tests → Disposition narrative in both es-CO and en"
    - "ITER-06: content/iterations/fx-vol-on-cpi-surprise/v1.mdx exists with status: FAIL, beta: -0.000685, ci_lower: -0.003635, ci_upper: 0.002265, sample_size: 947, disposition_memo present (required by Velite refine when status === FAIL)"
    - "ITER-08: slugs are content-addressable, human-readable, lowercase-kebab-case — never UUIDs or numeric IDs"
    - "content/iterations/sample/v1.mdx is DELETED (Phase 1 fixture no longer needed)"
    - "≥ 2 additional iterations seeded as IN_PROGRESS or PARKED placeholders so the catalog renders all 4 statuses"
    - "All β / CI / p / N / replication_hash values come from abrigo source files — NO invented values"
    - "If replication_hash is unknown for an iteration, the field is OMITTED (Velite schema allows optional); never invented"
    - "Velite build (pnpm velite build) succeeds and emits .velite/iterations.json containing ≥ 4 entries"
  artifacts:
    - path: content/iterations/pair-d/v1.mdx
      provides: "Pair D PASS iteration — real content from abrigo notebooks + dispatch brief"
      contains: "status: PASS"
    - path: content/iterations/fx-vol-on-cpi-surprise/v1.mdx
      provides: "FX-vol-on-CPI-surprise FAIL iteration with full disposition memo"
      contains: "status: FAIL"
    - path: content/iterations/abrigo-y3-carbon-basket/v1.mdx
      provides: "Placeholder IN_PROGRESS iteration (real header from abrigo scratch)"
      contains: "status: IN_PROGRESS"
    - path: content/iterations/pair-b-bittensor/v1.mdx
      provides: "Placeholder PARKED iteration (P1 Bittensor SN18 — real from abrigo README)"
      contains: "status: PARKED"
  key_links:
    - from: content/iterations/pair-d/v1.mdx
      to: ".velite/iterations.json"
      via: "Velite build pipeline"
      pattern: "pair-d"
    - from: content/iterations/fx-vol-on-cpi-surprise/v1.mdx
      to: ".velite/iterations.json"
      via: "Velite build pipeline (refine() validates disposition_memo present)"
      pattern: "fx-vol-on-cpi-surprise"
---

<objective>
Author 4 real iteration MDX files from abrigo source materials (no paraphrase-from-memory, no invented values), delete the Phase 1 sample fixture, and ensure Velite build succeeds with all 4 statuses represented so downstream Plan 02-05 (catalog) and Plan 02-06 (detail) have a realistic content corpus.

Purpose: ITER-05 (Pair D PASS) and ITER-06 (FX-vol FAIL) are the two demo-critical iteration detail pages — the Uniswap Hook Incubator judges will load these URLs. The other two iterations (IN_PROGRESS + PARKED) ensure the catalog renders all 4 statuses, which is required by ITER-02 (epistemic equality across statuses). ITER-08 (content-addressable slugs) is delivered by the filename → URL mapping convention.

Output: 4 MDX files committed to `content/iterations/`, the Phase 1 sample fixture deleted, `pnpm velite build` succeeds, and `.velite/iterations.json` contains 4 entries with all 4 statuses present.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-CONTEXT.md
@.planning/phases/02-research-lab-presence-and-iteration-catalog/02-RESEARCH.md
@velite.config.ts
@content/iterations/sample/v1.mdx

<interfaces>
<!-- Iteration schema from Plan 01-06 / velite.config.ts (extended in Plan 02-01) -->

```typescript
const iterationSchema = s.object({
  slug: s.string().regex(/^[a-z0-9-]+$/),
  version: s.number().int().positive(),
  status: s.enum(['PASS', 'FAIL', 'PARKED', 'IN_PROGRESS']),
  title_es: s.string().min(1),
  title_en: s.string().min(1),
  beta: s.number().optional(),
  ci_lower: s.number().optional(),
  ci_upper: s.number().optional(),
  ci_level: s.number().optional(), // 0.95 or 0.90
  p_value: s.number().optional(),
  sample_size: s.number().int().positive().optional(),
  replication_hash: s.string().regex(/^(sha256:)?[a-f0-9]{64}$/).optional(),
  notebook_url: s.string().url().optional(),
  dataset_ref: s.string().optional(),
  analysis_date: s.coerce.date(),
  disposition_memo: s.string().optional(),
}).refine(d => d.status !== 'FAIL' || !!d.disposition_memo, {
  message: 'FAIL iterations require disposition_memo',
})
```

URL pattern: file `content/iterations/{slug}/v{n}.mdx` → URL `/apps/abrigo/iterations/{slug}/v{n}` (ITER-08).
</interfaces>

<abrigo_source_facts>
<!-- VERBATIM facts from abrigo source files — to be embedded in MDX with no invention. -->

PAIR D (from /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/scratch/2026-04-30-stage-2-m-sketch-dispatch-brief-pair-d.md):
- Title context: Pair D Stage 2 M-sketch dispatch — Colombian young-worker services × COP/USD lagged 6-12mo
- β_composite = +0.13670985
- HAC SE = 0.02465
- t = +5.5456
- p_one (one-sided) ≈ 1.46×10⁻⁸ (~1.5e-8)
- 95% CI ≈ [0.0884, 0.1850] (computed: β ± 1.96 × SE = 0.13671 ± 0.04832 = [0.0884, 0.1850])
- Status: PASS
- Notebook: /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/notebooks/pair_d_stage_2_path_a/03_tests_and_sensitivity.ipynb
- GitHub notebook URL: https://github.com/wvs-finance/abrigo-analytics/blob/main/notebooks/pair_d_stage_2_path_a/03_tests_and_sensitivity.ipynb
- Disposition: PASS — Stage-2 M-sketch unblocked

FX-VOL-ON-CPI-SURPRISE (from /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/notebooks/fx_vol_cpi_surprise/Colombia/README.md):
- Title context: FX volatility response to CPI surprise — Colombia
- β̂_CPI = -0.000685
- 90% CI = [-0.003635, 0.002265] (note: 90%, not 95%)
- n = 947
- Verdict: FAIL (CI contains 0; effect statistically indistinguishable from zero at 90% level)
- Notebook: /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/notebooks/fx_vol_cpi_surprise/Colombia/
- GitHub notebook URL: https://github.com/wvs-finance/abrigo-analytics/tree/main/notebooks/fx_vol_cpi_surprise/Colombia
- Disposition: FAIL — Effect statistically zero; hypothesis rejected at 90% confidence; retained as worked example of disciplined failure

OTHER (from abrigo-analytics README):
- "dev-AI Stage-1: in progress (Section J × COP/USD)" → maps to slug `abrigo-y3-carbon-basket` OR `dev-ai-stage-1-section-j` (use the former if dispatched brief references Y3 × Carbon basket; otherwise use the latter). VERIFY by reading the README before authoring.
- "P1 Bittensor SN18: PARKED" → slug `pair-b-bittensor`. Status: PARKED. No β/CI/p — those fields omitted.
- "Phase-A.0 remittance: CLOSED EXIT_NON_REMITTANCE" → could be a 3rd FAIL or a separate status. For Phase 2 simplicity, do NOT create this iteration; the catalog already has 4 statuses with Pair D / FX-vol / Y3 / Bittensor.
</abrigo_source_facts>
</context>

<tasks>

<task type="auto" agent="Frontend Developer">
  <name>Task 1: Author Pair D PASS iteration MDX from abrigo dispatch brief</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/scratch/2026-04-30-stage-2-m-sketch-dispatch-brief-pair-d.md (DISPATCH BRIEF — read fully; this is the source of every numeric value)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/scratch/simple-beta-pair-d/ (read the README or summary file if present for additional context)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/notebooks/pair_d_stage_2_path_a/03_tests_and_sensitivity.ipynb (read cell outputs; jupyter notebooks are JSON — `cat` then inspect `cells[].outputs[].text` for printed values)
    - /home/jmsbpp/apps/d2p/frontend/velite.config.ts (current iterationSchema — frontmatter must satisfy)
    - /home/jmsbpp/apps/d2p/frontend/content/iterations/sample/v1.mdx (Phase 1 fixture pattern for frontmatter/body shape)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-CONTEXT.md ("Pair D + FX-vol content fidelity" — Spec → Data → Estimation → Tests → Disposition 5-section pattern)
  </read_first>
  <files>content/iterations/pair-d/v1.mdx</files>
  <action>
    1. Read the abrigo dispatch brief and tests notebook BEFORE composing. Confirm:
       - β_composite = +0.13670985
       - HAC SE = 0.02465
       - p_one (one-sided) ≈ 1.46×10⁻⁸
       - 95% CI = [0.0884, 0.1850]  (compute from β ± 1.96 × HAC SE)
       - Sample size N (read from notebook — it is the number of monthly observations in the pooled panel; if the notebook does not state it explicitly, omit `sample_size` from frontmatter and document the omission)
       - replication_hash: if there is an `estimates/gate_verdict.json` or equivalent in the abrigo scratch/simple-beta-pair-d/ directory, read the hash; if absent, OMIT the field (do not invent)

    2. Create content/iterations/pair-d/v1.mdx with frontmatter:
       ```yaml
       ---
       slug: pair-d
       version: 1
       status: PASS
       title_es: "Pair D — servicios jóvenes Colombia × COP/USD (6–12 meses rezagado)"
       title_en: "Pair D — Colombian young-worker services × COP/USD lagged 6–12mo"
       beta: 0.13670985
       ci_lower: 0.0884
       ci_upper: 0.1850
       ci_level: 0.95
       p_value: 1.46e-8
       # sample_size: N    # OMIT if not confirmed from notebook
       # replication_hash: # OMIT if not confirmed from abrigo source
       notebook_url: "https://github.com/wvs-finance/abrigo-analytics/blob/main/notebooks/pair_d_stage_2_path_a/03_tests_and_sensitivity.ipynb"
       dataset_ref: "abrigo-analytics/notebooks/pair_d_stage_2_path_a"
       analysis_date: "2026-04-30"
       ---
       ```

    3. Body: 5 sections in both es-CO and en, parallel content, each section 100-200 words. Use `## Spec` / `## Data` / `## Estimation` / `## Tests` / `## Disposition` H2 headings. Within each, write in the anti-fishing register — direct, evidence-cited, no marketing prose.

       Suggested structure (each section as parallel Spanish-then-English paragraphs, separated by blank lines):

       ```mdx
       ## Spec / Especificación

       (es-CO) Pre-comprometemos que la elasticidad de los servicios jóvenes en Colombia respecto al tipo de cambio COP/USD rezagado entre 6 y 12 meses tiene signo positivo y magnitud detectable a nivel mensual. La hipótesis nula H₀: β ≤ 0; la alterna H₁: β > 0. Umbral: t > 2 con SE robusto HAC; horizonte 2006-01 a 2025-12.

       (en) We pre-commit that the elasticity of Colombian young-worker services to COP/USD lagged 6 to 12 months has positive sign and detectable magnitude at monthly frequency. H₀: β ≤ 0; H₁: β > 0. Threshold: t > 2 with HAC robust SE; horizon 2006-01 to 2025-12.
       ```

       Repeat for Data (panel definition, FRED/Banco de la República sources, transformations), Estimation (OLS structural with HAC SE; specification used; controls), Tests (sensitivity to window, sub-panel, transform; trio checkpoint result), and Disposition (PASS — β = +0.137, p ≈ 1.5×10⁻⁸; Stage-2 M-sketch unblocked).

       Author rule: read the dispatch brief sections in full before writing each MDX section. Synthesize, don't paraphrase headlines. The anti-fishing tone is non-negotiable — banned phrases (per UI-SPEC §Copywriting Contract): "empower", "cutting-edge", "unlock", "leverage our", "best-in-class", "transform your".

    4. Save the file. Test by running `pnpm velite build` — must succeed without schema errors.

    5. Do NOT delete `content/iterations/sample/v1.mdx` in this task; deletion is Task 4 (after all 4 real iterations are in place to verify build at each step).
  </action>
  <acceptance_criteria>
    - `test -f content/iterations/pair-d/v1.mdx`.
    - Frontmatter contains `status: PASS`, `beta: 0.13670985`, `p_value:` with value ≈ 1.46e-8 or 1.5e-8 (acceptable scientific notation).
    - Frontmatter contains `notebook_url:` ending in `notebooks/pair_d_stage_2_path_a/03_tests_and_sensitivity.ipynb`.
    - Body contains `## Spec` (or `## Especificación`), `## Data` (or `## Datos`), `## Estimation` (or `## Estimación`), `## Tests` (or `## Pruebas`), `## Disposition` (or `## Disposición`) — at least 5 H2 headings total.
    - Body word count ≥ 500 (5 sections × ~100 words minimum each, both locales together ≥ 1000 words).
    - No banned phrases: `! grep -iE "empower|cutting-edge|unlock|leverage our|best-in-class|transform your" content/iterations/pair-d/v1.mdx`.
    - `pnpm velite build` exits 0 and `.velite/iterations.json` contains an entry with `"slug":"pair-d"`.
  </acceptance_criteria>
  <verify>
    <automated>test -f content/iterations/pair-d/v1.mdx && grep -q 'status: PASS' content/iterations/pair-d/v1.mdx && grep -q 'beta: 0.13670985' content/iterations/pair-d/v1.mdx && grep -qE 'p_value:\s*1\.4?6?e-?08?|p_value:\s*1\.5e-8' content/iterations/pair-d/v1.mdx && grep -q 'notebook_url:' content/iterations/pair-d/v1.mdx && grep -q 'pair_d_stage_2_path_a' content/iterations/pair-d/v1.mdx && grep -cE '^## ' content/iterations/pair-d/v1.mdx | awk '{exit ($1>=5?0:1)}' && ! grep -iE "empower|cutting-edge|unlock|leverage our|best-in-class|transform your" content/iterations/pair-d/v1.mdx && pnpm velite build && node -e "const f=require('./.velite/iterations.json'); if(!f.find(i=>i.slug==='pair-d'))throw new Error('pair-d not in velite output')"</automated>
  </verify>
  <done>Pair D MDX exists at content/iterations/pair-d/v1.mdx with frontmatter matching dispatch-brief values (β = 0.13670985, p ≈ 1.46e-8, status: PASS); body has 5 H2 sections (Spec / Data / Estimation / Tests / Disposition) authored in es-CO and en; velite build succeeds; no marketing slop.</done>
</task>

<task type="auto" agent="Frontend Developer">
  <name>Task 2: Author FX-vol-on-CPI-surprise FAIL iteration MDX from abrigo notebook README</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/notebooks/fx_vol_cpi_surprise/Colombia/README.md (FULL READ — this is the source of every numeric value)
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/notebooks/fx_vol_cpi_surprise/Colombia/ (list directory; check for gate_verdict.json or estimates/ for replication_hash if present)
    - /home/jmsbpp/apps/d2p/frontend/velite.config.ts (note the refine() — FAIL status REQUIRES disposition_memo field)
    - /home/jmsbpp/apps/d2p/frontend/content/iterations/pair-d/v1.mdx (created in Task 1 — pattern reference for body structure)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-CONTEXT.md ("FAIL iteration treatment" — DispositionMemo at same visual weight)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/02-research-lab-presence-and-iteration-catalog/02-UI-SPEC.md (§Component Inventory — DispositionMemo composition)
  </read_first>
  <files>content/iterations/fx-vol-on-cpi-surprise/v1.mdx</files>
  <action>
    1. Read the Colombia README in full. Confirm:
       - β̂_CPI = -0.000685
       - 90% CI = [-0.003635, 0.002265]  (NOTE: 90%, not 95% — frontmatter MUST set `ci_level: 0.90`)
       - n = 947
       - Verdict: FAIL (CI contains 0)
       - replication_hash: check `estimates/gate_verdict.json` if present. If absent, OMIT the field.

    2. Create content/iterations/fx-vol-on-cpi-surprise/v1.mdx with frontmatter:
       ```yaml
       ---
       slug: fx-vol-on-cpi-surprise
       version: 1
       status: FAIL
       title_es: "Volatilidad FX ante sorpresa CPI — Colombia"
       title_en: "FX volatility response to CPI surprise — Colombia"
       beta: -0.000685
       ci_lower: -0.003635
       ci_upper: 0.002265
       ci_level: 0.90
       sample_size: 947
       # p_value: omit if README does not state it explicitly
       # replication_hash: omit if gate_verdict.json absent
       notebook_url: "https://github.com/wvs-finance/abrigo-analytics/tree/main/notebooks/fx_vol_cpi_surprise/Colombia"
       dataset_ref: "abrigo-analytics/notebooks/fx_vol_cpi_surprise/Colombia"
       analysis_date: "2026-XX-XX"  # use date from README header if present; else use 2026-04-01 as conservative
       disposition_memo: |
         (es-CO) Esta iteración fue rechazada. El estimado β̂_CPI = -0.000685 con IC 90% = [-0.003635, 0.002265] sobre n = 947 observaciones contiene el cero. El efecto de la sorpresa CPI sobre la volatilidad FX en Colombia es estadísticamente indistinguible de cero al 90% de confianza. Se conserva como ejemplo de fracaso disciplinado: la hipótesis fue pre-comprometida, los datos fueron extraídos antes de ver resultados, y el rechazo se documenta con el mismo peso narrativo que las aprobaciones del laboratorio.

         (en) This iteration was rejected. The estimate β̂_CPI = -0.000685 with 90% CI = [-0.003635, 0.002265] over n = 947 observations contains zero. The effect of CPI surprise on FX volatility in Colombia is statistically indistinguishable from zero at 90% confidence. Retained as a worked example of disciplined failure: the hypothesis was pre-committed, data was pulled before results were inspected, and the rejection is documented with the same narrative weight as the lab's passes.
       ---
       ```

       NOTE: disposition_memo MUST be present (Velite refine() rejects FAIL without it).

    3. Body: 5 H2 sections (Spec / Data / Estimation / Tests / Disposition) in both es-CO and en, per the same pattern as Pair D. The Disposition section in the body should EXPAND on the disposition_memo with the full failure narrative — what was learned, why CPI surprise is a poor predictor of FX vol in this sample (e.g., CPI surprise signal-to-noise issues, regime-dependence, etc., as documented in the abrigo README).

    4. Save the file. Test with `pnpm velite build` — must succeed.

    5. No banned marketing phrases.
  </action>
  <acceptance_criteria>
    - `test -f content/iterations/fx-vol-on-cpi-surprise/v1.mdx`.
    - Frontmatter contains `status: FAIL`, `beta: -0.000685`, `ci_lower: -0.003635`, `ci_upper: 0.002265`, `ci_level: 0.90`, `sample_size: 947`.
    - Frontmatter contains `disposition_memo:` with non-empty content (REQUIRED by Velite refine()).
    - Frontmatter contains `notebook_url:` ending in `fx_vol_cpi_surprise/Colombia`.
    - Body contains ≥ 5 H2 headings.
    - No banned phrases: `! grep -iE "empower|cutting-edge|unlock|leverage our|best-in-class|transform your" content/iterations/fx-vol-on-cpi-surprise/v1.mdx`.
    - `pnpm velite build` exits 0 and `.velite/iterations.json` contains an entry with `"slug":"fx-vol-on-cpi-surprise"` and `"status":"FAIL"` and `"disposition_memo"` populated.
  </acceptance_criteria>
  <verify>
    <automated>test -f content/iterations/fx-vol-on-cpi-surprise/v1.mdx && grep -q 'status: FAIL' content/iterations/fx-vol-on-cpi-surprise/v1.mdx && grep -q 'beta: -0.000685' content/iterations/fx-vol-on-cpi-surprise/v1.mdx && grep -q 'ci_lower: -0.003635' content/iterations/fx-vol-on-cpi-surprise/v1.mdx && grep -q 'ci_upper: 0.002265' content/iterations/fx-vol-on-cpi-surprise/v1.mdx && grep -q 'ci_level: 0.90' content/iterations/fx-vol-on-cpi-surprise/v1.mdx && grep -q 'sample_size: 947' content/iterations/fx-vol-on-cpi-surprise/v1.mdx && grep -q 'disposition_memo:' content/iterations/fx-vol-on-cpi-surprise/v1.mdx && grep -q 'fx_vol_cpi_surprise/Colombia' content/iterations/fx-vol-on-cpi-surprise/v1.mdx && grep -cE '^## ' content/iterations/fx-vol-on-cpi-surprise/v1.mdx | awk '{exit ($1>=5?0:1)}' && ! grep -iE "empower|cutting-edge|unlock|leverage our|best-in-class|transform your" content/iterations/fx-vol-on-cpi-surprise/v1.mdx && pnpm velite build && node -e "const f=require('./.velite/iterations.json'); const it=f.find(i=>i.slug==='fx-vol-on-cpi-surprise'); if(!it)throw new Error('fx-vol not found'); if(it.status!=='FAIL')throw new Error('wrong status'); if(!it.disposition_memo)throw new Error('disposition_memo empty')"</automated>
  </verify>
  <done>FX-vol-on-CPI-surprise MDX exists with FAIL status, β̂ = -0.000685, 90% CI [-0.003635, 0.002265], n = 947; disposition_memo populated (refine() satisfied); body has 5 H2 sections in both locales; velite build succeeds.</done>
</task>

<task type="auto" agent="Frontend Developer">
  <name>Task 3: Seed placeholder IN_PROGRESS + PARKED iterations from abrigo README + delete sample fixture</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/abrigo/abrigo-analytics/README.md (snapshot of empirical state — list of iterations + statuses)
    - /home/jmsbpp/apps/d2p/frontend/content/iterations/sample/v1.mdx (Phase 1 fixture to delete)
    - /home/jmsbpp/apps/d2p/frontend/velite.config.ts
    - /home/jmsbpp/apps/d2p/frontend/tests/unit/velite-schema.test.ts (verify deletion does not break schema tests — they use a hardcoded object, not the file)
  </read_first>
  <files>
    content/iterations/abrigo-y3-carbon-basket/v1.mdx,
    content/iterations/pair-b-bittensor/v1.mdx,
    content/iterations/sample/v1.mdx
  </files>
  <action>
    1. From abrigo-analytics README, confirm:
       - "dev-AI Stage-1: in progress (Section J × COP/USD)" → create as IN_PROGRESS. Choose slug `abrigo-y3-carbon-basket` if the actual investigation is the Y3 × Carbon basket arm; OR `dev-ai-stage-1-section-j` if it is more accurately the dev-AI Section J effort. Read the README context to disambiguate. If both exist, pick the one explicitly named IN_PROGRESS.
       - "P1 Bittensor SN18: PARKED" → slug `pair-b-bittensor`. PARKED.

    2. Create content/iterations/abrigo-y3-carbon-basket/v1.mdx (or the chosen slug for IN_PROGRESS) with minimal frontmatter — placeholders are acceptable since this is not a demo-critical iteration; the body can be a 1-paragraph stub in both locales explaining that the analysis is in progress:
       ```yaml
       ---
       slug: abrigo-y3-carbon-basket
       version: 1
       status: IN_PROGRESS
       title_es: "Abrigo Y3 × canasta de carbono — exposición sintética (en progreso)"
       title_en: "Abrigo Y3 × carbon basket — synthetic exposure (in progress)"
       analysis_date: "2026-05-01"
       dataset_ref: "abrigo-analytics/scratch/y3-carbon-basket"
       ---
       ```
       Body: short status note in both locales — what is being investigated, current stage, target completion. No invented β/CI/p values.

    3. Create content/iterations/pair-b-bittensor/v1.mdx with frontmatter:
       ```yaml
       ---
       slug: pair-b-bittensor
       version: 1
       status: PARKED
       title_es: "P1 Bittensor SN18 — exposición a subnet de IA (pausada)"
       title_en: "P1 Bittensor SN18 — AI subnet exposure (parked)"
       analysis_date: "2026-03-15"
       dataset_ref: "abrigo-analytics/scratch/p1-bittensor-sn18"
       ---
       ```
       Body: short note explaining why parked (Bittensor SN18 instrument selection criteria not yet stable; revisit when subnet emission schedule is finalized). Both locales.

    4. DELETE content/iterations/sample/v1.mdx — this is a Phase 1 fixture, no longer needed (per CONTEXT.md decision). Use `rm content/iterations/sample/v1.mdx` (and `rmdir content/iterations/sample` if empty).

    5. Run `pnpm velite build` — must succeed and emit `.velite/iterations.json` with exactly 4 entries (Pair D, FX-vol, Y3 OR dev-ai, P1 Bittensor) — sample fixture absent.

    6. Run `pnpm vitest run tests/unit/velite-schema.test.ts` — must still pass (it uses hardcoded objects, not the file).
  </action>
  <acceptance_criteria>
    - `test -f content/iterations/abrigo-y3-carbon-basket/v1.mdx` OR `test -f content/iterations/dev-ai-stage-1-section-j/v1.mdx` (whichever slug was chosen).
    - `test -f content/iterations/pair-b-bittensor/v1.mdx`.
    - `! test -f content/iterations/sample/v1.mdx` (sample deleted).
    - Frontmatter of new IN_PROGRESS file contains `status: IN_PROGRESS`.
    - Frontmatter of new PARKED file contains `status: PARKED`.
    - `pnpm velite build` exits 0.
    - `.velite/iterations.json` contains ≥ 4 entries.
    - All 4 statuses (PASS / FAIL / PARKED / IN_PROGRESS) appear at least once across the 4 entries.
    - `pnpm vitest run tests/unit/velite-schema.test.ts` exits 0 (Phase 1 test not regressed).
    - `pnpm biome check content/` exits 0 (MDX frontmatter is YAML — biome won't lint it; the check is on any TS in content/ which should be empty).
  </acceptance_criteria>
  <verify>
    <automated>( test -f content/iterations/abrigo-y3-carbon-basket/v1.mdx || test -f content/iterations/dev-ai-stage-1-section-j/v1.mdx ) && test -f content/iterations/pair-b-bittensor/v1.mdx && ! test -f content/iterations/sample/v1.mdx && grep -q 'status: PARKED' content/iterations/pair-b-bittensor/v1.mdx && pnpm velite build && node -e "const f=require('./.velite/iterations.json'); if(f.length<4)throw new Error('expected >=4 iterations, got '+f.length); const statuses=new Set(f.map(i=>i.status)); ['PASS','FAIL','PARKED','IN_PROGRESS'].forEach(s=>{if(!statuses.has(s))throw new Error('missing status '+s)}); if(f.find(i=>i.slug==='sample'))throw new Error('sample still present')" && pnpm vitest run tests/unit/velite-schema.test.ts</automated>
  </verify>
  <done>4 iteration MDX files in content/iterations/; sample fixture deleted; all 4 statuses represented (PASS / FAIL / PARKED / IN_PROGRESS); velite build green; Phase 1 schema test still passes; demo-critical Pair D + FX-vol pages have real content from abrigo source files.</done>
</task>

</tasks>

<verification>
- ITER-05: Pair D MDX exists with real values from abrigo dispatch brief (β = 0.13670985, p ≈ 1.46e-8)
- ITER-06: FX-vol MDX exists with FAIL status, real values (β̂ = -0.000685, 90% CI [-0.003635, 0.002265], n = 947), disposition_memo populated
- ITER-08: All slugs are content-addressable kebab-case (pair-d, fx-vol-on-cpi-surprise, abrigo-y3-carbon-basket, pair-b-bittensor) — no UUIDs
- All 4 statuses represented (PASS / FAIL / PARKED / IN_PROGRESS)
- content/iterations/sample/v1.mdx deleted
- pnpm velite build succeeds
- pnpm vitest run tests/unit/velite-schema.test.ts still passes
</verification>

<success_criteria>
The Velite content corpus has 4 real iterations sourced from abrigo notebooks/scratch with zero invented values. The demo-critical Pair D PASS and FX-vol FAIL iterations have full Spec → Data → Estimation → Tests → Disposition narratives in both es-CO and en. Plans 02-05 (catalog) and 02-06 (detail pages) can compose against this corpus immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/02-research-lab-presence-and-iteration-catalog/02-04-SUMMARY.md` documenting:
- Verbatim frontmatter committed for each of the 4 iterations
- Source-file reference for each numeric value (β / CI / p / N / hash) in Pair D and FX-vol
- Any field omitted (e.g., replication_hash if not in abrigo source) with reason
- Sample-fixture deletion confirmed
- Velite build output: number of entries + status distribution
</output>

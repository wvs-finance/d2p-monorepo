---
phase: 15-cornerstone-e2e-ci
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/UI-AGENT-HANDOFF.md
autonomous: true
requirements: [E2E-01]
user_setup: []

must_haves:
  truths:
    - "docs/UI-AGENT-HANDOFF.md describes the SHIPPED MacroHedgeExecutor (deployable, fork-proven) — the stale '34-line STUB' claim is gone"
    - "The handoff carries the 8-param ExecutorDecided shape, replacing the stale 6-param one"
    - "The handoff carries the shipped PositionMinted shape (address owner, TokenId positionId, uint128 positionSize) and points margins at quoteMargin, not at PositionMinted fields"
    - "Section 6 no longer says 'build against mocks only / do NOT connect to a chain' — it documents the Phase-15 swap-to-real for the Agent-2 leg (mint on a HOSTED BuildBear Sandbox Polygon fork at chainId 31337, addresses from buildbear-deployments.json) while Agent-1 stays LIVE on real Somnia testnet (the recorded v1 trace)"
    - "The honest no-bridge label + the Agent-1-is-v1-recorded-and-live-on-Somnia reality are stated; the frontend real-mint wiring is scoped as a sibling-repo deliverable (its own git at /home/jmsbpp/apps/d2p/frontend)"
  artifacts:
    - path: "docs/UI-AGENT-HANDOFF.md"
      provides: "The refreshed frontend-to-contracts contract reflecting the shipped Phase 13/14 executor + the Phase-15 hosted-fork real-leg seam"
      contains: "resolveFromMandate"
  key_links:
    - from: "docs/UI-AGENT-HANDOFF.md"
      to: "MacroHedgeExecutor.resolveFromMandate + quoteMargin + ExecutorDecided (8-param)"
      via: "the refreshed section-3 status table + section-5 events + section-6 swap-to-real"
      pattern: "resolveFromMandate"
---

<objective>
Refresh the STALE `docs/UI-AGENT-HANDOFF.md` so the frontend agent integrates against reality, not against the pre-Phase-13 stub description. Three load-bearing corrections: (1) the executor is the SHIPPED deployable `MacroHedgeExecutor` with `resolveFromMandate`/`quoteMargin`/`_onResult` all implemented and fork-proven — NOT a "34-line STUB"; (2) the events are the shipped 8-param `ExecutorDecided` + 3-field `PositionMinted` (margins read via `quoteMargin`, not in the mint event); (3) section 6 flips from "mocks only, do NOT connect to a chain" to the Phase-15 swap-to-real for the Agent-2 leg — minting on a HOSTED BuildBear Sandbox (a Polygon fork at chainId 31337) the Vercel UI can reach, with the deployed addresses + sandbox RPC read from `contracts/script/out/buildbear-deployments.json`, Agent-1 staying LIVE on real Somnia testnet (the honest recorded v1 trace) and the explicit no-bridge label.

Purpose: E2E-01's UI leg + its judges' artifact depend on the frontend agent wiring the CORRECT shapes AND the CORRECT demo env. The research flags the handoff as stale "in load-bearing ways" — a wrong handoff doc is failure mode #1. The post-gate decision moved the Agent-2 demo env to a hosted forked testnet (resolving the funding + core-deploy blockers); the env was further switched from Tenderly to BuildBear after Tenderly's free tier proved unusable (no API/public-RPC on free), while BuildBear free forks Polygon + exposes `buildbear_ERC20Faucet` on its single sandbox RPC + ships a per-sandbox explorer. This doc is the contract that makes the sibling-repo frontend real-mint wiring (committed in `/home/jmsbpp/apps/d2p/frontend`, NOT here) feasible.
Output: the corrected handoff doc (this repo); the frontend code changes it enables are sibling-repo work, not abrigo-somnia commits.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-cornerstone-e2e-ci/15-RESEARCH.md
@.planning/phases/15-cornerstone-e2e-ci/15-VALIDATION.md

# The doc to refresh.
@docs/UI-AGENT-HANDOFF.md
# The shipped contract that the doc must now describe accurately.
@contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol

<interfaces>
<!-- The shipped shapes the refreshed handoff must carry. Extracted from contracts/src/MacroHedgeExecutor.sol (SHIPPED). -->

```solidity
// the 8-param honesty event (MacroHedgeExecutor.sol:94) — replaces the stale 6-param section-5 row:
event ExecutorDecided(
    uint256 indexed requestId, uint8 regimeZt, uint256 inflationAdjustmentWad,
    int24 strikeTick, int24 regimeWidth, bool parametricHedged, bool nonErgodicDisclosed, string rationale
); // nonErgodicDisclosed == true + a "TEMPLATE: …" rationale — render these.

// the shipped mint event (MacroHedgeExecutor.sol:111) — replaces the stale section-5 PositionMinted:
event PositionMinted(address indexed owner, TokenId indexed positionId, uint128 positionSize);
// margins are NOT in this event — read them via:
function quoteMargin(TokenId id, int24 strike) external view returns (BalanceDelta);

// the Agent-2 mint entrypoint the frontend calls on the BuildBear sandbox:
function resolveFromMandate(HedgeMandate calldata mandate, uint256 legIndex, uint128 positionSize)
    external returns (TokenId positionId); // mints at the EXACT structural K_hi tick 360360
```

Reality (research Pitfall 1 + State-of-the-Art table + the post-gate Demo-Env DECISION):
- The deployed Somnia strategist 0xfA428171 is still the Phase-11 v1 (HedgeDecisionMade(action,sizeBps)); the Phase-12 StrategistDecided(decisionId, school, HedgeMandate) is source-only + unit-proven, NOT deployed for this flow. Agent-1-live = the RECORDED v1 trace, LIVE on real Somnia testnet 50312 (never forked — Somnia's inference primitives need live validators + the keeper-proxy).
- Panoptic V2 + the executor are fork-only (BUSL-1.1 non-production) — Agent-2 mints on a HOSTED BuildBear Sandbox Polygon fork (chainId 31337; provisioned by contracts/script/provision-buildbear-demo.sh → contracts/script/out/buildbear-deployments.json), never a public mainnet chain.
- The frontend is a SEPARATE git repo at /home/jmsbpp/apps/d2p/frontend.
- BuildBear's sandbox has a SINGLE RPC that carries both standard JSON-RPC and the `buildbear_*` cheat methods (funding via `buildbear_ERC20Faucet`); there is NO separate admin RPC. The browser uses that same RPC for reads/writes. If browser→BuildBear CORS is blocked (verify with one eth_chainId fetch), the frontend proxies the RPC through a Next.js route (the existing Somnia keeper-proxy pattern). Funding (`buildbear_ERC20Faucet`) stays server-side in the provisioning runner.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the stale section-3 status table + section-5 event shapes in UI-AGENT-HANDOFF.md</name>
  <files>docs/UI-AGENT-HANDOFF.md</files>
  <read_first>
    - docs/UI-AGENT-HANDOFF.md (the whole file — section 3 status table at the MacroHedgeExecutor/Monitoring rows; section 5 event ABIs)
    - contracts/src/MacroHedgeExecutor.sol lines 94-111 (the shipped ExecutorDecided 8-param + PositionMinted 3-field) — already extracted in the interfaces block
  </read_first>
  <action>
    Edit `docs/UI-AGENT-HANDOFF.md` in place:
    1. Section-3 status table — the MacroHedgeExecutor row (currently the "STUB / 34 lines / `_onResult` is pseudo-code / working mint logic lives ONLY in a fork test" row): replace with a 🟡 row reading "Phase 13/14 done — the SHIPPED deployable MacroHedgeExecutor (resolveFromMandate / resolveAndMint / _onResult / quoteMargin all implemented + fork-proven on a Polygon fork; mints the real wCOP/USDC position at strike 360360). Fork-verified, NOT deployed to a public mainnet chain (BUSL-1.1, fork-only)." DELETE the "34-line" / "34 lines" / "pseudo-code" wording entirely.
    2. Section-3 status table — the Monitoring row (currently "Phase 14 — not built"): replace with "DEFERRED (MON-01) — the demo shows a BASIC position READ (mark/margin via quoteMargin + numberOfLegs), NOT a monitoring agent. Phase 14 became the representativeness brain."
    3. Section-5 events — ExecutorDecided: replace the stale 6-param line with the shipped 8-param signature `ExecutorDecided(uint256 indexed requestId, uint8 regimeZt, uint256 inflationAdjustmentWad, int24 strikeTick, int24 regimeWidth, bool parametricHedged, bool nonErgodicDisclosed, string rationale)`. Add a one-line note: render nonErgodicDisclosed (true) + the rationale (carries "TEMPLATE: …") on the Agent-2 decision card (the Davidson honesty split).
    4. Section-5 events — PositionMinted: replace the stale `PositionMinted(TokenId, address, int256, int256)` with the shipped `PositionMinted(address indexed owner, TokenId indexed positionId, uint128 positionSize)`. Add a note: margins are NOT in this event — read them via `quoteMargin(positionId, strike) -> BalanceDelta`.
    5. Section-5 events — PerformanceUpdated: mark it DEFERRED (MON-01) — keep the type documented but state the demo does NOT emit it; the basic read replaces it.
    Keep all other sections intact. Preserve the markdown table structure.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia && grep -c regimeZt docs/UI-AGENT-HANDOFF.md && grep -c nonErgodicDisclosed docs/UI-AGENT-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'regimeZt' docs/UI-AGENT-HANDOFF.md` (the 8-param ExecutorDecided landed)
    - `grep -q 'nonErgodicDisclosed' docs/UI-AGENT-HANDOFF.md` (the honesty flag is documented)
    - `grep -q 'uint128 positionSize' docs/UI-AGENT-HANDOFF.md` (the shipped PositionMinted shape landed)
    - `grep -q 'quoteMargin' docs/UI-AGENT-HANDOFF.md` (margins pointed at quoteMargin)
    - `grep -c '34 lines' docs/UI-AGENT-HANDOFF.md` returns 0 AND `grep -c '34-line' docs/UI-AGENT-HANDOFF.md` returns 0 (the stub claim is gone)
  </acceptance_criteria>
  <done>Section 3 describes the shipped deployable executor (no "34-line STUB"); section 5 carries the 8-param ExecutorDecided + the 3-field PositionMinted with margins pointed at quoteMargin; the monitoring rows are marked DEFERRED/basic-read.</done>
</task>

<task type="auto">
  <name>Task 2: Flip section 6 to the Phase-15 swap-to-real on the BuildBear sandbox + add the no-bridge / Agent-1-live-on-Somnia honesty + the sibling-repo boundary</name>
  <files>docs/UI-AGENT-HANDOFF.md</files>
  <read_first>
    - docs/UI-AGENT-HANDOFF.md section 6 (the "Build against MOCKS only — do NOT connect to a chain" section) + section 7 scope guardrails
    - .planning/phases/15-cornerstone-e2e-ci/15-RESEARCH.md § "Pattern 1: Frontend-as-orchestrator carries the HedgeMandate across chains" (the verbatim no-bridge label) + § "Phase-15 Demo-Env DECISION (revised post-gate)" (the hosted-fork decision + the CORS caveat)
    - .planning/phases/15-cornerstone-e2e-ci/15-RESEARCH-fork-env-landscape.md § "FREE-tier Polygon-fork options" (BuildBear #1: single sandbox RPC + buildbear_ERC20Faucet + per-sandbox explorer; the wagmi defineChain → sandbox RPC → deployments-artifact read-back wiring)
    - NOTE: contracts/script/provision-buildbear-demo.sh + contracts/script/out/buildbear-deployments.json are authored in Plan 01 (Wave 1, parallel). Reference them by name + role (the provisioning runner that funds via buildbear_ERC20Faucet, deploys + reads back, and writes the deployments artifact carrying executor/pool/rpcUrl/mintTxHash); do NOT assume their file contents here, only their documented output contract.
  </read_first>
  <action>
    Edit `docs/UI-AGENT-HANDOFF.md`:
    1. Rewrite section 6 (currently "Build against MOCKS only — do NOT connect to a chain"): Phase 15 IS the swap-to-real moment for the Agent-2 leg. State: the frontend submits `resolveFromMandate(mandate, legIndex, positionSize)` against a HOSTED **BuildBear Sandbox** (a Polygon fork at **chainId 31337** — BuildBear re-chains the fork) reachable from the Vercel-deployed UI. The deployed executor/pool addresses + the sandbox RPC URL come from the deployments artifact `contracts/script/out/buildbear-deployments.json` (written by `contracts/script/provision-buildbear-demo.sh`), which also carries the verifiable `mintTxHash` — NOT a hardcoded constant. After the mint, read back `numberOfLegs(executor)` + `quoteMargin(positionId, strike)` for the basic position read. Keep the real-chain path ALONGSIDE the existing mock (the documented `fromMockEvent` isolation seam) so the demo degrades gracefully to the mock if the sandbox is down. State the RPC model: BuildBear's sandbox has a SINGLE RPC that carries both standard JSON-RPC and the `buildbear_*` cheats; the browser uses that RPC for reads/writes, while funding (`buildbear_ERC20Faucet`) is done server-side by the provisioning runner — never in client code. Note the CORS caveat: if browser→BuildBear CORS is blocked (verify with one eth_chainId fetch), proxy the RPC through a Next.js route handler (the existing Somnia keeper-proxy pattern). Add the verbatim on-screen honesty label: "Agent 1 runs live on Somnia testnet; Agent 2 mints on a hosted Polygon fork (BuildBear) — the hedge mandate is carried between them by this app; there is NO cross-chain bridge in this demo (deferred)."
    2. Correct the Agent-1 reality (research Pitfall 1): the deployed Somnia strategist is still the v1 (`HedgeDecisionMade(action, sizeBps)`); the Phase-12 `StrategistDecided(decisionId, school, HedgeMandate)` is source-only/unit-proven and is NOT deployed for this flow. So Agent-1-live in the demo = the real RECORDED v1 trace the frontend already renders, LIVE on real Somnia testnet 50312 (Somnia is NEVER forked — its consensus-validated inference + keeper-proxy do not survive a fork); the `HedgeMandate` is carried as the Agent-1-to-Agent-2 hand-off VALUE OBJECT, labeled "intent derived for this demo". Do NOT instruct the frontend to subscribe to a live `StrategistDecided` on Somnia or to deploy the Phase-12 strategist.
    3. Add an explicit CROSS-REPO BOUNDARY note: the frontend is a SEPARATE git repo at `/home/jmsbpp/apps/d2p/frontend` with its own commit discipline; the real-mint wiring (a `defineChain({ id: 31337, rpc: BUILDBEAR_RPC_URL })` for the BuildBear Polygon-fork chain added to `lib/wagmi/config.ts` alongside the live Somnia chain, a mint panel calling `resolveFromMandate` + reading the position back, addresses/RPC sourced from `buildbear-deployments.json` mirrored into the frontend, real `ExecutorDecided`/`PositionMinted` reader/writer added alongside `fromMockEvent`, repointing `wagmi.config.ts` at `contracts/out/` + `pnpm contracts:gen`) is a deliverable committed in the FRONTEND repo, NOT in abrigo-somnia. The abrigo-somnia MVP deliverable is the BuildBear provisioning script + the deployments artifact + this refreshed doc + the in-VM quoteMargin assertion + the CI — achievable + verifiable (the mint tx hash) even before the frontend wiring lands.
    4. Update section 7 scope guardrails to keep the deferred boundaries explicit: MON-01 (no monitoring agent — basic read only), HEDGE-01 (no delta-hedge), XCHAIN-01 (no bridge — carried by the app), no public/mainnet Panoptic deploy (hosted fork only).
    Add a dated refresh note at the bottom: "Refreshed 2026-06-07 (Phase 15) — executor shipped + fork-proven; Agent-2 leg mints on a hosted BuildBear Polygon-fork sandbox at chainId 31337 (addresses from buildbear-deployments.json); Agent-1 stays the recorded v1 trace live on Somnia; no bridge."
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia && grep -ci 'buildbear' docs/UI-AGENT-HANDOFF.md && grep -ci 'no cross-chain bridge' docs/UI-AGENT-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi 'buildbear' docs/UI-AGENT-HANDOFF.md` (section 6 points the Agent-2 leg at the hosted BuildBear sandbox)
    - `grep -q 'buildbear-deployments.json' docs/UI-AGENT-HANDOFF.md` (the addresses/RPC come from the deployments artifact, not a constant)
    - `grep -qi 'no cross-chain bridge' docs/UI-AGENT-HANDOFF.md` (the verbatim no-bridge honesty label is present)
    - `grep -q 'recorded' docs/UI-AGENT-HANDOFF.md` (Agent-1 is described as the recorded v1 trace live on Somnia, not a live HedgeMandate)
    - `grep -q '/home/jmsbpp/apps/d2p/frontend' docs/UI-AGENT-HANDOFF.md` (the cross-repo boundary is stated)
    - `! grep -q 'serve-polygon-fork-demo.sh' docs/UI-AGENT-HANDOFF.md` (the OLD local-anvil node reference is gone — the demo env is the hosted BuildBear sandbox)
    - `! grep -qi 'tenderly' docs/UI-AGENT-HANDOFF.md` (no stale Tenderly reference — the env is BuildBear)
    - `grep -q 'swap' docs/UI-AGENT-HANDOFF.md` (section 6 documents the swap-to-real, no longer a blanket mocks-only directive for the Agent-2 leg)
  </acceptance_criteria>
  <done>Section 6 documents the Phase-15 swap-to-real for the Agent-2 leg (mint on the hosted BuildBear sandbox at chainId 31337, addresses/RPC from buildbear-deployments.json + the basic quoteMargin read, single-RPC model + CORS-proxy note), Agent-1 is the recorded v1 trace live on Somnia, the verbatim no-bridge label is present, the old serve-polygon-fork-demo.sh + Tenderly references are gone, and the frontend real-mint wiring is scoped as a sibling-repo deliverable with its own git.</done>
</task>

</tasks>

<verification>
- `grep -q 'regimeZt' docs/UI-AGENT-HANDOFF.md` and `grep -q 'uint128 positionSize' docs/UI-AGENT-HANDOFF.md` (shipped event shapes).
- `grep -c '34 lines' docs/UI-AGENT-HANDOFF.md` returns 0 (the stub claim is gone).
- `grep -qi 'buildbear' docs/UI-AGENT-HANDOFF.md` and `grep -q 'buildbear-deployments.json' docs/UI-AGENT-HANDOFF.md` and `grep -qi 'no cross-chain bridge' docs/UI-AGENT-HANDOFF.md` (the hosted-fork swap-to-real + the no-bridge honesty).
- `! grep -q 'serve-polygon-fork-demo.sh' docs/UI-AGENT-HANDOFF.md` and `! grep -qi 'tenderly' docs/UI-AGENT-HANDOFF.md` (the old local-anvil + the stale Tenderly references are removed).
- `grep -q '/home/jmsbpp/apps/d2p/frontend' docs/UI-AGENT-HANDOFF.md` (the sibling-repo boundary).
</verification>

<success_criteria>
- E2E-01 UI provenance row supported: the frontend agent has the correct shipped shapes (8-param ExecutorDecided, 3-field PositionMinted, quoteMargin reads) + the honest Agent-2-real-on-hosted-BuildBear-fork / Agent-1-recorded-live-on-Somnia / no-bridge framing + the deployments-artifact address source.
- The frontend real-mint wiring (BuildBear fork chain via defineChain({id:31337}) + the mint panel + the Playwright e2e extension) is clearly scoped as sibling-repo work (committed in /home/jmsbpp/apps/d2p/frontend), NOT abrigo-somnia commits — the MVP deliverable stands on the provisioning script + the deployments artifact + the verifiable mint tx hash + the video without them.
- No monitoring agent, no bridge, no public mainnet deploy in the handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/15-cornerstone-e2e-ci/15-03-SUMMARY.md`
</output>

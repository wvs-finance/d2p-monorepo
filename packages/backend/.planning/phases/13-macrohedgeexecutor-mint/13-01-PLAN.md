---
phase: 13-macrohedgeexecutor-mint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/src/libraries/PolygonPools.sol
  - contracts/src/interfaces/IMacroThesis.sol
  - contracts/foundry.toml
  - contracts/test/instrument/PolygonPools.tree
  - contracts/test/instrument/PolygonPools.t.sol
autonomous: true
requirements: [EXEC-01]
must_haves:
  truths:
    - "A stable POLYGON_WCOP_USDC_POOL_ID constant exists and equals the demo's wcopUsdcKey().toId() (currency0=USDC 6dp, currency1=wCOP 18dp, fee 3000, tickSpacing 60, hookless)"
    - "PoolIdMappersLib.panopticPoolIdFromUniV4PoolId(POLYGON_WCOP_USDC_POOL_ID, vegoid, 60) yields the same uint64 panoptic pool id the demo computes at runtime"
    - "foundry.toml caches the polygon (chain 137) fork-state so rapid executor fork runs do not 429"
    - "IMacroThesis compiles as a marker interface that HedgeLegParams.economicTheory can be typed against (IMacroThesis(address(0)) is a valid argument, as the demo passes)"
  artifacts:
    - path: "contracts/src/libraries/PolygonPools.sol"
      provides: "wcopUsdcKey() canonical PoolKey getter + POLYGON_WCOP_USDC_POOL_ID() PoolId constant (the STRAT-02 anchor)"
      contains: "POLYGON_WCOP_USDC_POOL_ID"
    - path: "contracts/test/instrument/PolygonPools.t.sol"
      provides: "Round-trip proof: constant == demo key.toId(); currency ordering USDC<wCOP holds"
    - path: "contracts/foundry.toml"
      provides: "polygon chain in rpc_storage_caching"
      contains: "137"
  key_links:
    - from: "contracts/src/libraries/PolygonPools.sol"
      to: "wcopUsdcKey().toId()"
      via: "PoolIdLibrary.toId on the canonical PoolKey"
      pattern: "toId\\(\\)"
---

<objective>
Lay the Wave-0 substrate that unblocks the executor promotion (Plan 02): the canonical `POLYGON_WCOP_USDC_POOL_ID` constant (the STRAT-02 anchor — Agent 1 cannot produce a runtime PoolId, so both agents reference the cornerstone pool by a stable constant), the `IMacroThesis` compile-stub confirmation, and the `polygon` fork-state cache in foundry.toml (the demo currently caches only Base/chain 8453, so rapid Polygon fork runs 429 — Pitfall 6).

Purpose: Deliver the stable pool-id anchor and the fork-cache so Plan 02's executor compiles and its fork test reruns cheaply. This is the smallest self-contained unblock; it has NO dependency on the executor or the cost ledger and runs in Wave 1 in parallel with Plan 03.
Output: `contracts/src/libraries/PolygonPools.sol` (new), a co-located unit `.tree`+test proving the constant round-trips the demo's runtime key, the `polygon` cache entry in foundry.toml, and `IMacroThesis` confirmed compiling.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-macrohedgeexecutor-mint/13-RESEARCH.md
@.planning/phases/13-macrohedgeexecutor-mint/13-VALIDATION.md

<interfaces>
<!-- Key contracts the executor (Plan 02) references the constant by. Extracted from the green demo. -->

From contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol (the proven runtime derivation this constant must reproduce):
```solidity
// :180-185 — the canonical addresses + the live PoolKey the constant must equal:
address constant POLYGON_USDC = 0x3c499c542cEF5E3811e1192ce70d8cC03d5c3359; // 6 decimals
address constant POLYGON_WCOP = 0x8a1D45e102e886510e891d2Ec656a708991e2D76; // 18 decimals
// :215-221 — wcopUsdcKey (currency0=USDC, currency1=wCOP; USDC addr < wCOP addr holds):
wcopUsdcKey = PoolKey({
    currency0: Currency.wrap(POLYGON_USDC), currency1: Currency.wrap(POLYGON_WCOP),
    fee: 3000, tickSpacing: 60, hooks: IHooks(address(0))
});
// :84 / :305 — the runtime mapping the constant feeds:
uint64 pid = PoolIdMappersLib.panopticPoolIdFromUniV4PoolId(wcopUsdcKey.toId(), vegoid, tickSpacing_);
```

From contracts/src/libraries/PoolIdMappers.sol (the consumer of the constant):
```solidity
function panopticPoolIdFromUniV4PoolId(PoolId uniV4PoolId, uint8 vegoid, int24 tickSpacing)
    internal pure returns(uint64 panopticPoolId);
```

From lib/v4-core/src/types/PoolId.sol (toId is `using PoolIdLibrary for PoolKey`):
```solidity
function toId(PoolKey memory poolKey) internal pure returns (PoolId poolId);
```

From contracts/src/interfaces/IMacroThesis.sol (current — empty marker interface, must stay compiling):
```solidity
interface IMacroThesis {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BTT .tree for the PoolId constant (committed FIRST, Iron Law)</name>
  <files>contracts/test/instrument/PolygonPools.tree</files>
  <read_first>
    - contracts/test/instrument/MacroHedgeStrategist.tree (the repo's bulloak-0.9.2 `when/it` keyword form — bare leaf labels do NOT parse; follow this exact style)
    - 13-RESEARCH.md §4 "POLYGON_WCOP_USDC_POOL_ID constant" (the currency ordering is load-bearing: 0x3c49…(USDC) < 0x8a1D…(wCOP), so currency0=USDC; a drift breaks the whole TokenId derivation)
    - contracts/test/fork/DemoMacroHedgeExecutor.fork.t.sol:180-221 (the runtime key the constant must equal)
  </read_first>
  <action>
    Create `contracts/test/instrument/PolygonPools.tree` (CO-LOCATED with the `.t.sol` per bulloak 0.9.2 same-dir + full-stem rule). Root contract name `PolygonPoolsTest`. Use the `when/it` keyword form (NOT bare `it_*` leaves — they throw `unexpected token` in 0.9.2, the 08-01 precedent). Cover exactly these observable claims:
    - when the canonical key is built → it should order currency0 as USDC and currency1 as wCOP (USDC address strictly less than wCOP address)
    - when the canonical key is built → it should set fee 3000, tickSpacing 60, and the zero hooks address
    - when POLYGON_WCOP_USDC_POOL_ID is read → it should equal the demo wcopUsdcKey toId computed inline from the same fields
    - when the panoptic pool id is mapped → it should equal PoolIdMappersLib panopticPoolIdFromUniV4PoolId of the constant with a sample vegoid and tickSpacing sixty
    Commit this `.tree` in a SEPARATE commit BEFORE any `.sol` (evm-tdd Iron Law). Commit message: `test(13): PolygonPools.tree BTT for the wCOP/USDC pool-id anchor`.
  </action>
  <acceptance_criteria>
    - `cd contracts && bulloak check test/instrument/PolygonPools.tree` — must NOT error on parse (it WILL report the missing `.t.sol` until Task 2; the parse itself must be clean — confirm via exit-code/stderr having no "unexpected token").
    - `git log --oneline -1 -- contracts/test/instrument/PolygonPools.tree` shows a commit that predates the `.t.sol` and `PolygonPools.sol` commits (Iron-Law ancestry).
  </acceptance_criteria>
  <verify>
    <automated>cd contracts && bulloak check test/instrument/PolygonPools.tree 2>&1 | grep -vq "unexpected token"</automated>
  </verify>
  <done>`PolygonPools.tree` exists, parses cleanly under bulloak 0.9.2, and is committed before any source file.</done>
</task>

<task type="auto">
  <name>Task 2: PolygonPools library (the constant) + IMacroThesis compile-stub + FAILING-then-GREEN unit test</name>
  <files>contracts/src/libraries/PolygonPools.sol, contracts/src/interfaces/IMacroThesis.sol, contracts/test/instrument/PolygonPools.t.sol</files>
  <read_first>
    - 13-RESEARCH.md §4 (the recommended `PolygonPools` shape — `wcopUsdcKey()` getter + `POLYGON_WCOP_USDC_POOL_ID()`; reproduce the EXACT currency ordering from the demo or the TokenId derivation drifts)
    - contracts/src/interfaces/IMacroThesis.sol (the marker interface — keep it compiling; the resolver never reads `economicTheory`, the demo passes `IMacroThesis(address(0))` at :162)
    - contracts/src/libraries/PoolIdMappers.sol:8-11 (panopticPoolIdFromUniV4PoolId — reads low 40 bits + vegoid<<40 + tickSpacing<<48)
    - contracts/remappings.txt (v4-core/ = lib/v4-core/src; the demo imports PoolKey/PoolId/Currency/IHooks from `v4-core/types/...` and `v4-core/interfaces/IHooks.sol`)
  </read_first>
  <action>
    1. Create `contracts/src/libraries/PolygonPools.sol` (pragma `^0.8.24`, SPDX MIT). `library PolygonPools` with:
       - `using PoolIdLibrary for PoolKey;`
       - `function wcopUsdcKey() internal pure returns (PoolKey memory)` returning EXACTLY the demo key: `currency0: Currency.wrap(0x3c499c542cEF5E3811e1192ce70d8cC03d5c3359)` (USDC 6dp), `currency1: Currency.wrap(0x8a1D45e102e886510e891d2Ec656a708991e2D76)` (wCOP 18dp), `fee: 3000`, `tickSpacing: 60`, `hooks: IHooks(address(0))`.
       - `function POLYGON_WCOP_USDC_POOL_ID() internal pure returns (PoolId)` returning `wcopUsdcKey().toId()`.
       Imports: `{PoolKey} from "v4-core/types/PoolKey.sol"`, `{PoolId, PoolIdLibrary} from "v4-core/types/PoolId.sol"`, `{Currency} from "v4-core/types/Currency.sol"`, `{IHooks} from "v4-core/interfaces/IHooks.sol"`. (A function-returning constant — not a `constant` state var — because `PoolKey`/`PoolId` are not compile-time constant-expressible; this matches the §4 recommendation.)
    2. Confirm `contracts/src/interfaces/IMacroThesis.sol` compiles as-is (empty `interface IMacroThesis {}`). It is already a valid marker; do NOT add members (its concrete shape is Phase 12's STRAT-01 concern, RESEARCH Open Q2). No edit needed unless `forge build` flags it.
    3. Write `contracts/test/instrument/PolygonPools.t.sol` (co-located, scaffolded from the Task-1 tree; pure unit — NO fork). Import `PolygonPools`, `PoolIdMappersLib`, and the v4-core types. Assert:
       - `Currency.unwrap(PolygonPools.wcopUsdcKey().currency0) < Currency.unwrap(PolygonPools.wcopUsdcKey().currency1)` AND `currency0 == USDC`, `currency1 == wCOP`.
       - `key.fee == 3000 && key.tickSpacing == 60 && address(key.hooks) == address(0)`.
       - `PoolId.unwrap(PolygonPools.POLYGON_WCOP_USDC_POOL_ID()) == PoolId.unwrap(localKey.toId())` where `localKey` is rebuilt inline from the same literal fields (proves the constant equals the demo's runtime `wcopUsdcKey.toId()`).
       - `PoolIdMappersLib.panopticPoolIdFromUniV4PoolId(PolygonPools.POLYGON_WCOP_USDC_POOL_ID(), 4, 60)` equals the same call on `localKey.toId()` (sample vegoid 4 per the 07-05 §G constant; mapping is deterministic).
    Commit the impl + test AFTER the tree (Iron-Law ancestry). The tree predates this commit.
  </action>
  <acceptance_criteria>
    - `cd contracts && forge build` exits 0 (PolygonPools + IMacroThesis compile under cancun/0.8.24).
    - `cd contracts && bulloak check test/instrument/PolygonPools.tree` exits 0 (the `.t.sol` now satisfies the tree, co-located).
    - `cd contracts && forge test --match-path "test/instrument/PolygonPools.t.sol" -vv` — all tests PASS (the constant round-trips the demo key + the mapping matches).
  </acceptance_criteria>
  <verify>
    <automated>cd contracts && forge build && bulloak check test/instrument/PolygonPools.tree && forge test --match-path "test/instrument/PolygonPools.t.sol" -vv</automated>
  </verify>
  <done>`PolygonPools.sol` exports the `POLYGON_WCOP_USDC_POOL_ID` constant equal to the demo's runtime key; `forge build` green; the co-located unit test passes; `bulloak check` exit 0; `IMacroThesis` confirmed compiling.</done>
</task>

<task type="auto">
  <name>Task 3: Add polygon (chain 137) to foundry.toml rpc_storage_caching</name>
  <files>contracts/foundry.toml</files>
  <read_first>
    - contracts/foundry.toml:15 (current `rpc_storage_caching = { chains = [8453], endpoints = "all" }` — Base only)
    - 13-RESEARCH.md §Pitfall 6 (Alchemy 429s on rapid Polygon fork runs; the fork-state cache `fork-state/polygon-panoptic.json` is present; add a `polygon` cache entry so chain-137 RPC reads warm across attempts)
    - 13-VALIDATION.md Wave-0 Requirements (last checkbox: "Add `polygon` to `rpc_storage_caching` chains")
  </read_first>
  <action>
    Edit `contracts/foundry.toml:15` to add Polygon's chain id (137) to the cached chains: `rpc_storage_caching = { chains = [8453, 137], endpoints = "all" }`. Do NOT touch the `[invariant]` section, the `skip` list, the solc/evm_version, or the `[rpc_endpoints] polygon = ...` line (already present at :26). This is additive — Base/8453 stays cached for the Phase-7/8 tests.
    Commit: `chore(13): cache polygon (137) fork-state in foundry.toml (429 defense)`.
  </action>
  <acceptance_criteria>
    - `cd contracts && grep -q "chains = \[8453, 137\]" foundry.toml` (the cache now includes Polygon).
    - `cd contracts && forge build` exits 0 (toml still parses; no regression).
  </acceptance_criteria>
  <verify>
    <automated>cd contracts && grep -q "137" foundry.toml && forge build</automated>
  </verify>
  <done>foundry.toml caches chain 137; `forge build` green; the existing Base cache + invariant section are untouched.</done>
</task>

</tasks>

<verification>
- `cd contracts && forge build` — green (PolygonPools + IMacroThesis compile).
- `cd contracts && bulloak check test/instrument/PolygonPools.tree` — exit 0 (co-located, full-stem).
- `cd contracts && forge test --match-path "test/instrument/PolygonPools.t.sol" -vv` — PASS (constant == demo key.toId(); mapping matches; currency ordering correct).
- `grep -q "137" contracts/foundry.toml` — polygon cached.
- Iron-Law ancestry: the `.tree` commit predates the `.sol`/`.t.sol` commits.
</verification>

<success_criteria>
- `POLYGON_WCOP_USDC_POOL_ID()` exists in `contracts/src/libraries/PolygonPools.sol` and equals the demo's `wcopUsdcKey.toId()` (verified by the co-located unit test).
- The currency ordering (currency0=USDC 6dp, currency1=wCOP 18dp, fee 3000, tickSpacing 60, hookless) reproduces the demo key exactly — the STRAT-02 anchor is stable.
- `PoolIdMappersLib.panopticPoolIdFromUniV4PoolId(constant, vegoid, 60)` matches the runtime derivation (Plan 02's executor relies on this).
- foundry.toml caches Polygon (137) so Plan 02's fork test reruns cheaply (Pitfall 6).
- `IMacroThesis` compiles as a marker interface (Plan 02's executor imports `HedgeLegParams` which types `economicTheory` as `IMacroThesis`).
</success_criteria>

<output>
After completion, create `.planning/phases/13-macrohedgeexecutor-mint/13-01-SUMMARY.md`
</output>

---
phase: 02-topic-implementation-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_topic_resolution.py
  - schemas/topic0_map_v1.json
  - schemas/unresolved_topics_v1.md
  - references/interfaces/PROVENANCE.sha256
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [TOPIC-01]
must_haves:
  truths:
    - "A consumer can query topic0_map_v1.json for any of the three observed topic0 hashes and receive a (signature, implementation_address, field_layout_hash, decode_status) tuple resolved by keccak match against the pinned IAgentRequester blob e15d4e94…"
    - "The keccak tooling reproduces the EIP-1967 Upgraded(address) constant 0xbc7cd75a… exactly (proves the resolver uses keccak-256, not NIST sha3_256)"
    - "RequestFinalized canonicalizes its ResponseStatus enum to uint8 → topic0 0x65db… (the single most likely keccak mistake is pinned by test)"
    - "field_layout_hash is deterministic: the committed RequestCreated hash equals keccak('RequestCreated(uint256:I,uint256:I,uint256:D,bytes:D,address[]:D)') = 0x9b58ba75…; a reordered-arg input yields a different hash"
    - "SC#5: the resolved ABI has zero indexed-dynamic fields → no event_schema_v1.md → v2 bump is triggered (confirm-and-document, not a migration)"
    - "unresolved_topics_v1.md design documents the KPD-06 column set + the <1% pct_logs_unresolved ship gate + the decode_status enum {resolved, unresolved_abi, unresolved_impl_transition}"
    - "CLAUDE.md and PROJECT.md no longer call e15d4e9 a 'commit' — they call it a git blob SHA (git hash-object reproduces it byte-for-byte)"
  artifacts:
    - path: "references/interfaces/IAgentRequester.sol"
      provides: "The recovered, blob-SHA-verified ABI — already committed; source of truth for every event signature"
      contains: "event RequestCreated"
    - path: "schemas/topic0_map_v1.json"
      provides: "Per-(impl, topic0) ABI resolver with the 3 resolved entries + the 2 unobserved-but-registered entries, each with field_layout_hash + decode_status + abi_source.blob_sha"
      contains: "0xb62339927ed9948fd837358a55f5b9a824f7b047043faece66965593ed726889"
    - path: "schemas/unresolved_topics_v1.md"
      provides: "unresolved_topics.parquet design (KPD-06 columns, decode_status enum, <1% gate computation)"
      contains: "pct_logs_unresolved"
    - path: "tests/test_topic_resolution.py"
      provides: "Wave-0 + TOPIC-01 unit tests (keccak verify, 3-topic0 resolution, enum-uint8, field_layout_hash determinism, no-indexed-dynamic SC#5, decode_status enum, <1% gate)"
      min_lines: 80
    - path: "references/interfaces/PROVENANCE.sha256"
      provides: "Blob-SHA provenance row for the recovered ABI so resolver abi_source.blob_sha is checkable"
      contains: "IAgentRequester.sol"
  key_links:
    - from: "tests/test_topic_resolution.py"
      to: "schemas/topic0_map_v1.json"
      via: "load_json fixture reads the committed map; test recomputes keccak(signature) == topic0 for every resolved row"
      pattern: "load_json.*topic0_map_v1"
    - from: "schemas/topic0_map_v1.json"
      to: "references/interfaces/IAgentRequester.sol"
      via: "abi_source.blob_sha e15d4e94… pins the source-of-truth ABI"
      pattern: "e15d4e94ef9a0c09c8971ac1061098b929325028"
---

<objective>
Resolve the three observed `IAgentRequester` topic0 hashes to event signatures by exact keccak match against the recovered, blob-SHA-pinned ABI, and commit the per-`(impl, topic0)` resolver `topic0_map_v1.json` (KPD-01) plus the `unresolved_topics.parquet` design (KPD-06). Add the keccak tooling to the dev group (Wave 0). Confirm-and-document SC#5 (zero indexed-dynamic fields → no schema v2 bump). Correct the "commit `e15d4e9`" → "blob `e15d4e9`" wording in CLAUDE.md + PROJECT.md.

Purpose: TOPIC-01 is the resolver every downstream phase decodes events through. Phase 3 INDEX-01's AssemblyScript mappings reference these resolved field names; Phase 5 PANEL-01 joins on `(implementation_address, topic0)`; STATS-01 reports `pct_logs_unresolved < 1%` against this resolver's quarantine pipeline.

Output: `schemas/topic0_map_v1.json` (5 resolved/registered events), `schemas/unresolved_topics_v1.md` (quarantine design), `tests/test_topic_resolution.py`, the `eth-hash[pycryptodome]` dev dependency, `references/interfaces/PROVENANCE.sha256`, and the doc-accuracy fix.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-topic-implementation-provenance/02-CONTEXT.md
@.planning/phases/02-topic-implementation-provenance/02-RESEARCH.md
@.planning/phases/02-topic-implementation-provenance/02-VALIDATION.md
@references/interfaces/IAgentRequester.sol
@schemas/event_schema_v1.md
@tests/conftest.py

<interfaces>
<!-- Contracts the executor needs. Extracted from the recovered ABI + Phase-1 harness. Use directly — no codebase exploration needed. -->

The five events in `references/interfaces/IAgentRequester.sol` (blob SHA `e15d4e94ef9a0c09c8971ac1061098b929325028`; `git hash-object references/interfaces/IAgentRequester.sol` reproduces it byte-for-byte). Canonical signature → computed topic0 (all verified live 2026-05-29):

| event_name | canonical signature (enum → uint8) | topic0 | observed on-chain? |
|---|---|---|---|
| RequestCreated | `RequestCreated(uint256,uint256,uint256,bytes,address[])` | `0xb62339927ed9948fd837358a55f5b9a824f7b047043faece66965593ed726889` | YES (3-topic, 896–5920B data) |
| RequestFinalized | `RequestFinalized(uint256,uint8)` | `0x65db1ef5b3bcd84fe4fb8dbbe1cadc9fe6643bb261ab2e01d65c281c3d466af2` | YES (2-topic, 32B) |
| CommitteeDepositFailed | `CommitteeDepositFailed(uint256,uint256)` | `0x5c090ef48df2b4d8a01bd0639355d62c318b623aed749bdd12325f789e37a2cf` | YES (2-topic, 32B) |
| SubcommitteePaid | `SubcommitteePaid(uint256,uint256,uint256)` | `0x15863241ef82702f42fe12b9bc93f3fbc82b50dbc2a3962c70f3249939db605e` | NO (register anyway) |
| NativeTransferFailed | `NativeTransferFailed(address,uint256)` | `0xa5b05eec8040da65485bf3ab248b47c42271aa50a9234ed8f116e568dc285cc6` | NO (register anyway) |

Indexed args per event (all VALUE types — no indexed-dynamic field exists anywhere):
- RequestCreated: `requestId` (uint256), `agentId` (uint256) indexed; `perAgentBudget` (uint256), `payload` (bytes), `subcommittee` (address[]) in data.
- RequestFinalized: `requestId` (uint256) indexed; `status` (ResponseStatus→uint8) in data.
- CommitteeDepositFailed: `requestId` (uint256) indexed; `attemptedAmount` (uint256) in data.
- SubcommitteePaid: `requestId` (uint256) indexed; `totalPaid`, `perMember` (uint256) in data.
- NativeTransferFailed: `recipient` (address) indexed; `amount` (uint256) in data.

The single observed impl for M1: `implementation_address = 0x9af59c5683bb8686596b0d56e4f67655c6b73edd` (lowercased).

field_layout_hash canonical string form (Claude's-discretion definition, RESEARCH §Pattern 2):
`<EventName>(<type0>:<I|D>,<type1>:<I|D>,…)` where types are canonical ABI types (enum→uint8, struct→tuple), `I`=indexed, `D`=data. Then `field_layout_hash = "0x" + keccak(canonical_string.encode()).hex()`.
Worked: `RequestCreated(uint256:I,uint256:I,uint256:D,bytes:D,address[]:D)` → `0x9b58ba757cb55c0f041e54b88e3bfa0fe42457e16705c55bafb82c312fd39dd2`.

Keccak tooling: `from eth_hash.auto import keccak` (pycryptodome backend). NEVER `hashlib.sha3_256` (NIST SHA3 padding differs — `sha3_256("Upgraded(address)")` = `0x0e51260f…` ≠ the EIP-1967 constant).

Phase-1 conftest fixtures (consume as test params, NOT module imports): `schemas_dir` (→ `Path("schemas")`), `load_json` (→ callable `fn(path) → json`), `read_text` (→ callable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — add keccak dep, commit ABI provenance, scaffold test module with verified keccak harness</name>
  <files>pyproject.toml, references/interfaces/PROVENANCE.sha256, tests/test_topic_resolution.py</files>
  <read_first>
    - references/interfaces/IAgentRequester.sol (the recovered ABI — already committed)
    - tests/conftest.py (fixtures: schemas_dir, load_json, read_text)
    - .planning/phases/02-topic-implementation-provenance/02-RESEARCH.md §Standard Stack, §Code Examples, §Validation Architecture
    - pyproject.toml (dev group; modern-python skill convention is `uv add --group dev`)
  </read_first>
  <action>
    1. Add the keccak dependency to the dev group via uv (modern-python skill: NEVER edit pyproject.toml by hand to add deps):
       `uv add --group dev "eth-hash[pycryptodome]"`
       This updates pyproject.toml + uv.lock. Confirm `eth-hash` lands in `[dependency-groups].dev`.
    2. Create `references/interfaces/PROVENANCE.sha256` recording the ABI's git blob SHA so the resolver's `abi_source.blob_sha` is independently checkable. Format: one line
       `e15d4e94ef9a0c09c8971ac1061098b929325028  IAgentRequester.sol`
       (this is the git blob SHA, reproduced by `git hash-object references/interfaces/IAgentRequester.sol` — NOT a sha256sum; label the file's first comment line `# git blob SHA (git hash-object), content-addressed ABI pin`). Do NOT touch `.planning/scout/2026-05-29/PROVENANCE.sha256` — that is a separate KPD-16 set-equality manifest scoped to scout `.md` files; Phase 2 adds no scout `.md`, so leave it untouched.
    3. Create `tests/test_topic_resolution.py` with, at minimum, the verified keccak harness as the FIRST test (the non-negotiable tooling gate):
       ```python
       from eth_hash.auto import keccak
       def topic0(sig: str) -> str:
           return "0x" + keccak(sig.encode()).hex()
       def test_keccak_tooling_verified():
           assert topic0("Upgraded(address)") == \
               "0xbc7cd75a20ee27fd9adebab32041f755214dbc6bffa90cc0225b39da2e5c2d3b"
       ```
       Add the helper `field_layout_hash(name, args)` per the §interfaces canonical-string form. The remaining tests (Task 2) consume `schemas/topic0_map_v1.json` via the `load_json` conftest fixture — scaffold their function stubs now so the module imports cleanly, but they MAY assert against the map that Task 2 commits (run order is Task1→Task2 in the same plan).
  </action>
  <verify>
    <automated>uv run pytest tests/test_topic_resolution.py::test_keccak_tooling_verified -x</automated>
  </verify>
  <done>`eth-hash[pycryptodome]` is in the dev group; `uv run pytest tests/test_topic_resolution.py::test_keccak_tooling_verified -x` passes (keccak reproduces the EIP-1967 Upgraded constant); `references/interfaces/PROVENANCE.sha256` contains the blob SHA `e15d4e94ef9a0c09c8971ac1061098b929325028`; the scout PROVENANCE.sha256 is unchanged (full suite scout set-equality test still green).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit topic0_map_v1.json resolver + unresolved_topics design; assert keccak resolution, field_layout_hash, SC#5, decode_status, <1% gate</name>
  <files>schemas/topic0_map_v1.json, schemas/unresolved_topics_v1.md, tests/test_topic_resolution.py</files>
  <read_first>
    - references/interfaces/IAgentRequester.sol (event signatures + indexed/data arg layout)
    - .planning/phases/02-topic-implementation-provenance/02-RESEARCH.md §Architecture Patterns (Pattern 1/2), §unresolved_topics.parquet design, §Common Pitfalls 2/3
    - schemas/event_schema_v1.md §KPD-18 reservations + §topic0 domain (the reservations SC#5 enumerates against)
    - tests/test_topic_resolution.py (the keccak + field_layout_hash helpers from Task 1)
  </read_first>
  <behavior>
    - Test: each of the 3 observed topic0s (`0xb623…`, `0x65db…`, `0x5c09…`) appears in the map's `resolver` list with the exact signature from §interfaces, `decode_status: "resolved"`, `implementation_address: "0x9af59c5683bb8686596b0d56e4f67655c6b73edd"`.
    - Test: for EVERY resolved row, `keccak(row.signature) == row.topic0` (recompute, do not trust the file).
    - Test: RequestFinalized's signature is `RequestFinalized(uint256,uint8)` (enum→uint8) and hashes to `0x65db…` — assert the row signature contains `uint8`, NOT `ResponseStatus`.
    - Test: RequestCreated's `field_layout_hash == 0x9b58ba757cb55c0f041e54b88e3bfa0fe42457e16705c55bafb82c312fd39dd2`; recompute it from the row's args and assert equality; a reordered-args input yields a DIFFERENT hash (determinism + sensitivity).
    - Test (SC#5): across all 5 events, no indexed arg is a dynamic type (string/bytes/dynamic array). Assert every entry's `indexed_args` resolve to value types only → a module-level constant `INDEXED_DYNAMIC_FIELD_COUNT == 0` and `SCHEMA_V2_BUMP_REQUIRED is False`.
    - Test: `decode_status` enum domain across the map ⊆ {resolved, unresolved_abi, unresolved_impl_transition}; the 3 observed + 2 registered are all `resolved`.
    - Test (<1% gate): a synthetic counter `{total_logs: 10000, unresolved_count: 50}` → `pct_logs_unresolved == 0.5` and the ship gate `< 1.0` passes; `unresolved_count: 150` → `1.5` fails the gate (prove the threshold flips).
  </behavior>
  <action>
    1. Write `schemas/topic0_map_v1.json` per RESEARCH §Pattern 1 structure: top-level `{"schema_version": "topic0_map_v1", "resolver": [ ... ]}`. Include ALL FIVE events as resolver rows (the 3 observed + SubcommitteePaid + NativeTransferFailed — register the 2 unobserved-but-defined ones too, cost-zero, prevents a future `unresolved_abi` false positive per RESEARCH Open Question 2). Each row: `implementation_address` (= `0x9af59c5683bb8686596b0d56e4f67655c6b73edd`), `topic0`, `signature`, `event_name`, `indexed_args` (list of `{name, type}`), `data_args` (list of `{name, type}`), `field_layout_hash`, `decode_status: "resolved"`, `abi_source: {"blob_sha": "e15d4e94ef9a0c09c8971ac1061098b929325028", "path": "references/interfaces/IAgentRequester.sol"}`. Compute each `topic0` and `field_layout_hash` from the canonical strings in §interfaces — DO NOT transcribe blindly; the test recomputes. For the 5 field_layout_hashes, derive each from its canonical `name(type:I|D,…)` string. RequestCreated's is given (`0x9b58ba75…`); compute the other four with `uv run python -c` and the helper.
    2. Write `schemas/unresolved_topics_v1.md` documenting the `unresolved_topics.parquet` DESIGN (not materialized — Phase 3 fills it). Columns per RESEARCH §unresolved_topics.parquet design: `topic0` (pl.Utf8), `implementation_address` (pl.Utf8), `first_seen_block` (pl.UInt64), `first_seen_tx` (pl.Utf8), `observed_count` (pl.UInt64), `raw_data_sample` (pl.Utf8), `decode_status` (pl.Enum(["unresolved_abi","unresolved_impl_transition"])). Document the full decode_status domain {resolved, unresolved_abi, unresolved_impl_transition}, the escalation order (prior blobs of emrestay/somnia-agents-skills → newer Somnia DevRel repos → 4byte.directory → Tier-B decompiled event tables), and the `<1%` gate: `pct_logs_unresolved = (Σ observed_count) / total_proxy_log_count × 100`, ship gate `< 1.0`, **expected 0.0% for M1** (all topic0s resolved; the total-log denominator is an INDEX-01/Phase-3 output — Phase 2 fixes the numerator pipeline). State that uint256/hash columns are `pl.Utf8` only per the event_schema_v1.md DTYPE SCOPE RULE.
    3. Add an SC#5 confirm-and-document block to `schemas/unresolved_topics_v1.md` (or a short `## SC#5` section): the resolved ABI has ZERO indexed-dynamic fields (every indexed arg across all 5 events is `uint256`/`address` — a value type), therefore **no `event_schema_v1.md → v2` bump is triggered**. Note the `agent_class_keccak`/`agent_class_string` reservations stay as cheap insurance with no keccak→string referent (likely recovery is `agentId → class` uint registry or `bytes payload` decode, per event_schema_v1.md §KPD-18).
    4. Fill in the Task-1 test stubs to satisfy the <behavior> above, consuming `schemas/topic0_map_v1.json` via `load_json` + `schemas_dir`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_topic_resolution.py -x</automated>
  </verify>
  <done>`topic0_map_v1.json` has 5 resolver rows; every resolved row satisfies `keccak(signature)==topic0`; RequestFinalized uses `uint8`; RequestCreated field_layout_hash == `0x9b58ba75…`; SC#5 asserts 0 indexed-dynamic fields + no v2 bump; decode_status enum + <1% gate tests pass; `uv run pytest tests/test_topic_resolution.py -x` green.</done>
</task>

<task type="auto">
  <name>Task 3: Doc-accuracy fix — correct "commit e15d4e9" → "blob SHA e15d4e9" in CLAUDE.md + PROJECT.md</name>
  <files>CLAUDE.md, .planning/PROJECT.md</files>
  <read_first>
    - CLAUDE.md line 78 (`commit e15d4e9, fetched 2026-05-23`)
    - .planning/PROJECT.md lines 107, 128 (`commit e15d4e9` / `e15d4e9 schema`)
    - .planning/phases/02-topic-implementation-provenance/02-RESEARCH.md §Summary + §Common Pitfalls 1 (the blob-vs-commit finding + the byte-for-byte git hash-object proof)
  </read_first>
  <action>
    The pin itself is sound (`git hash-object references/interfaces/IAgentRequester.sol` = `e15d4e94ef9a0c09c8971ac1061098b929325028`, prefix `e15d4e9`); only the WORDING is wrong — `GET /commits/e15d4e9` returns HTTP 422 "No commit found". Make these surgical edits (preserve all surrounding meaning; do NOT change the pin value):
    - CLAUDE.md line 78: change `commit ` + backtick-`e15d4e9`-backtick to `blob SHA ` + backtick-`e15d4e9`-backtick. Add a parenthetical: `(git blob SHA of references/interfaces/IAgentRequester.sol — not a commit SHA; reproduced by git hash-object)`.
    - PROJECT.md line 49: `emrestay/somnia-agents-skills@e15d4e9` is acceptable (the `@blob` ref form is ambiguous) but append `(blob SHA, not commit)` after it.
    - PROJECT.md line 107: change `commit e15d4e9` → `blob SHA e15d4e9`.
    - PROJECT.md line 128: the phrase `never silently typed as e15d4e9 schema` reads fine but for accuracy change `e15d4e9 schema` → `the e15d4e9-blob ABI schema`.
    Use the `Edit` tool for each — these are targeted string replacements, not rewrites.
  </action>
  <verify>
    <automated>! grep -n "commit \`e15d4e9\`" CLAUDE.md .planning/PROJECT.md && grep -q "blob SHA" CLAUDE.md && git hash-object references/interfaces/IAgentRequester.sol | grep -q e15d4e94ef9a0c09c8971ac1061098b929325028</automated>
  </verify>
  <done>No occurrence of "commit `e15d4e9`" remains in CLAUDE.md or PROJECT.md; CLAUDE.md says "blob SHA"; the blob-SHA pin still reproduces via git hash-object (pin value unchanged).</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_topic_resolution.py -v` — all TOPIC-01 + Wave-0 tests green.
- `uv run pytest tests/ -q` — full Phase-1 + Phase-2 suite green (scout set-equality test STILL passes — Phase 2 added no scout `.md`).
- `python -c "import json; m=json.load(open('schemas/topic0_map_v1.json')); print(len(m['resolver']))"` → 5.
- `grep -c "commit \`e15d4e9\`" CLAUDE.md .planning/PROJECT.md` → 0.
</verification>

<success_criteria>
- TOPIC-01 SC#1: `topic0_map_v1.json` resolves all 3 observed topic0s to `(signature, impl_address, field_layout_hash)` by keccak match against the e15d4e94… blob.
- TOPIC-01 SC#4: `unresolved_topics_v1.md` documents the quarantine columns + `pct_logs_unresolved < 1%` gate (expected 0.0% for M1).
- SC#5: confirmed zero indexed-dynamic fields → no `event_schema_v1.md → v2` bump (documented, not migrated).
- Doc-accuracy: "commit `e15d4e9`" corrected to "blob SHA" in CLAUDE.md + PROJECT.md; pin value unchanged.
- Keccak tooling pinned against the EIP-1967 Upgraded constant; no `hashlib.sha3_256` anywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/02-topic-implementation-provenance/02-01-SUMMARY.md`.
</output>

---
phase: 18-on-chain-decision-moves-proof-+-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/script/macro-hedge-strategist-e2e.sh
  - contracts/script/out/somnia-strategist-deployment.json  # gitignored (unanchored out/) → MUST `git add -f` (MINOR-5)
  - contracts/script/out/MacroHedgeStrategist.abi.json       # gitignored (unanchored out/) → MUST `git add -f` (MINOR-5)
  - docs/UI-AGENT-HANDOFF.md
autonomous: true
requirements: [LIVEDEP-02, LIVEDEP-03, LIVEDEP-04, LIVEDEP-05]
user_setup:
  - service: somnia-testnet-stt
    why: "The proof fires TWO full two-leg runs PLUS retry headroom (each run = 1 conditional json refresh + 2 LLM-inference deposits). The Step-0b gate pre-reserves the FULL profile (RUNS=2 + MAX_RETRY=1) × NEED_PER_RUN before the first irreversible send; an under-funded wallet FAILS the gate (it never strands a half-open decision mid-flow)."
    env_vars:
      - name: SOMNIA_TESTNET_PK
        source: "contracts/.env (already present from Phase 17 — funded wallet 0xF3c3…0a90, key NEVER committed)"
      - name: SOMNIA_TESTNET_ADDRESS
        source: "contracts/.env (already present)"
    dashboard_config:
      - task: "Ensure the wallet holds the FULL-profile reserve: (RUNS=2 + MAX_RETRY=1) × (json refresh 0.09 + 2×0.21 LLM) ≈ 3 × ~0.51 STT ≈ ~1.6 STT plus the platform floor headroom. FRONTEND-REQUEST §4 confirms a >50 STT funded account is available, so this is comfortably covered. Faucet is browser-only."
        location: "https://testnet.somnia.network/"

must_haves:
  truths:
    - "A StrategistDecided tx from 0xf0570CcB1271FFaFf4caCA628F3632257f177b1D on Somnia 50312 carries a non-empty school + a HedgeMandate with economicTheory != 0x0 and targetNotional in [1000, 100000000]"
    - "decisionState(decisionId) for run 1 returns schoolSet==true AND notionalSet==true (a FULL two-leg mandate landed — not just the school leg)"
    - "Run 2 uses a GENUINELY divergent input (different USER_INTENT2 targeting post-Keynesian/regime risk AND a different consensus); every per-run log capture is bound to that run's decisionId (the StrategistDecided indexed topic / the leg requestId topic), so run-2's poll can never re-read run-1's log on the reused CONSUMER — the decision-moves comparison is run-1 vs run-2, never run-1 vs itself"
    - "LIVEDEP-03 outcome: PASS if the divergent input yields a different (school OR targetNotional); OTHERWISE a DOCUMENTED no-move finding (both decisionIds + both mandates + an explicit 'decision did not move for these inputs — agent-sensitivity finding, NOT a silent pass' note) surfaced to the user — never a hang, never unbounded spend, never a fabricated pass"
    - "contracts/script/out/somnia-strategist-deployment.json parses as JSON and carries the live strategist address + three real, explorer-resolvable run-1 tx hashes (schoolTx, notionalTx, strategistDecidedTx), each bound to run-1's decisionId"
    - "docs/UI-AGENT-HANDOFF.md marks the two-leg strategist LIVE with 0xf0570CcB…7b1D and BOTH §6 prohibition sentences (the lowercase 'do NOT subscribe to a live' AND the 'Do NOT instruct the frontend to subscribe') are REPLACED by a LIVE-subscribe instruction"
  artifacts:
    - path: "contracts/script/macro-hedge-strategist-e2e.sh"
      provides: "Two-leg StrategistDecided runner: run_two_leg() drives school→notional→join with decisionId-bound captures throughout, fired TWICE on DIVERGENT inputs; balance precheck reserves the FULL run profile; run-1 persisted to a state file before run-2 (idempotent re-invoke); REUSES the live CONSUMER (no redeploy)"
      contains: "requestNotionalDecision"
    - path: "contracts/script/out/somnia-strategist-deployment.json"
      provides: "The machine-readable artifact the frontend mirrors (addresses + 3 run-1 tx hashes + call inputs)"
      contains: "strategistDecidedTx"
    - path: "contracts/script/out/MacroHedgeStrategist.abi.json"
      provides: "The committed strategist ABI (copied from the gitignored contracts/out/ build dir) for wagmi.config.ts"
      contains: "StrategistDecided"
    - path: "docs/UI-AGENT-HANDOFF.md"
      provides: "The reversed §6 guidance (BOTH prohibition sentences replaced) + the LIVE two-leg-strategist row"
      contains: "0xf0570CcB1271FFaFf4caCA628F3632257f177b1D"
  key_links:
    - from: "macro-hedge-strategist-e2e.sh"
      to: "0xf0570CcB1271FFaFf4caCA628F3632257f177b1D (CONSUMER env)"
      via: "CONSUMER reuse (idempotent re-run path — NO forge create)"
      pattern: "CONSUMER"
    - from: "macro-hedge-strategist-e2e.sh per-run capture"
      to: "this run's decisionId (StrategistDecided indexed topic + leg requestId topic)"
      via: "cast logs filtered by the decisionId topic (NOT address-only) so run-2 cannot capture run-1's log"
      pattern: "StrategistDecided\\(bytes32,string.*\" \"\\$decision_id\""
    - from: "macro-hedge-strategist-e2e.sh notional leg"
      to: "StrategistDecided event + decisionState notionalSet"
      via: "requestNotionalDecision(bytes32) then poll member-2 of decisionState"
      pattern: "requestNotionalDecision"
    - from: "somnia-strategist-deployment.json"
      to: "the three on-chain txs"
      via: "jq-readable schoolTx/notionalTx/strategistDecidedTx fields written by the runner"
      pattern: "strategistDecidedTx"
---

<objective>
Prove the live two-leg `MacroHedgeStrategist` (deployed in Phase 17 at `0xf0570CcB1271FFaFf4caCA628F3632257f177b1D` on Somnia 50312) ON-CHAIN end-to-end, then publish the artifact the frontend mirrors.

This phase: (1) extends the Phase-17 runner with the NOTIONAL leg so a FULL `HedgeMandate` lands (`StrategistDecided` emitted, `decisionState == (schoolSet, notionalSet) == (true, true)`); (2) runs a SECOND, genuinely-divergent-input run (different `USER_INTENT2` toward post-Keynesian/regime risk + a different consensus) and tests whether the mandate MOVES — a bounded proof whose negative branch is a DOCUMENTED no-move finding, not a silent pass; (3) writes `somnia-strategist-deployment.json` to the exact FRONTEND-REQUEST §2.4 schema + commits the generated ABI + reverses BOTH §6 handoff prohibition sentences.

Purpose: unblock the frontend live-Agent-1 path — it cannot decode a `HedgeMandate` until a real `StrategistDecided` lands and the address/ABI/inputs are published.
Output: an extended runner, a published JSON artifact, a committed ABI, and a reversed handoff doc — all backed by real Somnia 50312 tx hashes.

GUARDRAILS (non-negotiable, from FRONTEND-REQUEST §4 + CLAUDE.md):
- REUSE the existing deploy (`CONSUMER=0xf0570C…7b1D`, `MACRO_ORACLE=0xAcA751…983f`). NEVER `forge create` a new strategist — that strands STT and breaks the frontend mirror.
- Real tx hashes are MANDATORY. A recorded run is acceptable; an unverifiable claim is NOT. If the notional callback persistently does not land, that is a DOCUMENTED BLOCK to surface (the full mandate IS required here — unlike Phase 17), not a silent pass.
- Decision-moves is NOT assumed — school+notional are LLM outputs, so run-2 uses a deliberately divergent intent+consensus to MAXIMIZE the chance of movement; if it still does not move after the bounded attempt, that is a recorded agent-sensitivity finding, never fabricated.
- Secrets only in gitignored `contracts/.env`. Never echo/commit the PK. The JSON holds addresses + tx hashes + call inputs — NOT the key.
- Do NOT alter the contract's `POLYGON_CHAIN_ID = 137` join constant (it is the mandate's chainId; the frontend's 31337 override is a frontend-side accommodation).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@docs/FRONTEND-REQUEST-2026-06-07-strategist-live-deploy.md
@.planning/phases/17-live-deploy-pre-flight-surface-verification/17-01-SUMMARY.md
@contracts/script/macro-hedge-strategist-e2e.sh
@contracts/script/out/buildbear-deployments.json

<interfaces>
<!-- Verified from contracts/src/instrument/MacroHedgeStrategist.sol — use these EXACTLY, no exploration needed. -->

Live deploy (Phase 17, REUSE — do NOT redeploy):
- CONSUMER (strategist): 0xf0570CcB1271FFaFf4caCA628F3632257f177b1D  (Somnia 50312, ≠ v1 0xfA428171…)
- MACRO_ORACLE: 0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f
- PLATFORM: 0x037Bb9C718F3f7fe5eCBDB0b600D607b52706776
- LLM_AGENT_ID: 12847293847561029384
- RPC: https://api.infra.testnet.somnia.network  (chain-id 50312)

Two-leg flow (the runner drives this in order PER run):
- requestSchoolDecision(string userIntent, bytes32 dataKey, int256 consensus) payable returns (bytes32 decisionId)
    emits HedgeDecisionRequested(uint256 indexed requestId, bytes32 indexed decisionId, uint8 leg=0/School).
    decisionId = topics[2] of HedgeDecisionRequested (requestId=topics[1]); the contract derives
    decisionId = bytes32(requestId), so for the SCHOOL leg requestId-as-topic == decisionId value.
- requestNotionalDecision(bytes32 decisionId) payable returns (uint256 requestId)
    GUARD: reverts UnknownDecision(decisionId) unless schoolSet==true && notionalSet==false.
    So the SCHOOL callback MUST have landed (schoolSet==true) BEFORE this is sent.
    emits HedgeDecisionRequested(requestId, decisionId, uint8 leg=1/Notional) synchronously.
- On the notional callback landing, _onResult JOINs both legs and emits:
    event StrategistDecided(bytes32 indexed decisionId, string school, HedgeMandate mandate)  (event L127)
    decisionId is the INDEXED topic[1] → cast-logs can filter by it (the BLOCKER-1 binding).
- DecisionFailed(uint256 indexed requestId, ResponseStatus status) (L126) — requestId indexed (NOT
    decisionId); bind a per-run failure poll via the leg's requestId topic.

Reads (cast call against CONSUMER):
- decisionState(bytes32 decisionId) returns ((bool schoolSet, bool notionalSet, uint64 decidedAt, string schoolLabel))
    member-1 schoolSet, member-2 notionalSet, member-4 schoolLabel.
- getMandate(bytes32 decisionId) returns ((address economicTheory, bytes32 underlyingMarket, uint256 targetNotional, uint32 chainId, bool isLong))
    economicTheory = the IMacroThesis sentinel handle (0x5 SHILLER / 0x6 POST_KEYNESIAN, NON-zero when set);
    targetNotional clamped to [1000, 100000000].
    POSITIONAL (MINOR-4): economicTheory = the 1st tuple field (address), targetNotional = the 3rd
    field (uint256) — extract with explicit awk -F',' $1 / $3, never a naive split.

Constants: MIN_NOTIONAL=1_000, MAX_NOTIONAL=100_000_000, POLYGON_CHAIN_ID=137.
Registry sentinels: SHILLER 0x5 (surprise/convex), POST_KEYNESIAN 0x6 (structural/regime) — run-1's
default intent ("rate-hike surprise") resolved SHILLER, so run-2's intent is worded toward
post-Keynesian regime risk to maximize divergence.

Phase-17 runner steps already present (REUSE): Step 0/0b/0c FREE surface gate, Step 1 oracle
deploy-or-reuse, Step 2 + 2b oracle refresh + freshness gate, Step 3 deploy-or-REUSE (CONSUMER env
short-circuits forge create), poll_log helper. The Phase-17 single-school-probe body was refactored
into run_two_leg() and extended with the notional leg + join.

ARTIFACT shape to MIRROR (contracts/script/out/buildbear-deployments.json): a flat JSON object,
addresses + rpcUrl + tx hash + capturedAt + source.

CRITICAL gitignore reality (MINOR-5): the repo root .gitignore has an UNANCHORED `out/` rule, so BOTH
contracts/out/ AND contracts/script/out/ are gitignored. somnia-strategist-deployment.json + the ABI
are therefore local-only / NOT git-tracked. To satisfy LIVEDEP-04 the executor MUST `git add -f`
them — a plain `git add` silently no-ops, and the commit step must not trust it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the runner — run_two_leg() with the notional leg → full mandate + decisionId-bound StrategistDecided (LIVEDEP-02)</name>
  <files>contracts/script/macro-hedge-strategist-e2e.sh</files>
  <read_first>
    Read contracts/script/macro-hedge-strategist-e2e.sh in full. Note it REUSES CONSUMER via the
    `if [ -z "${CONSUMER:-}" ]` short-circuit at Step 3 and exposes a `run_two_leg()` function +
    `poll_log` helper. Re-read the §4 guardrails in
    docs/FRONTEND-REQUEST-2026-06-07-strategist-live-deploy.md.
  </read_first>
  <action>
    Implement the FULL single-run two-leg flow inside `run_two_leg() { local intent="$1" consensus="$2"; … }`
    against the LIVE deploy (this is already drafted in the runner; verify/keep these properties):

    1. Budget (MAJOR-1): `NEED_PER_RUN=$(( JSON_DEPOSIT + LLM_DEPOSIT + LLM_DEPOSIT ))` (json refresh +
       TWO LLM legs), and `NEED_TOTAL=$(( (RUNS + MAX_RETRY) * NEED_PER_RUN ))` (RUNS=2, MAX_RETRY=1).
       The Step-0b balance gate compares BAL against NEED_TOTAL (the FULL profile) BEFORE the first
       irreversible send, big-int-safe by digit-count. Reconcile the user_setup STT estimate to this.

    2. cast-send shape probe (MINOR-3): BEFORE any STT spend, self-test the tx-hash extractor —
       `extract_txhash()` reads `.transactionHash // .hash` from `cast send/receipt --json`; a literal
       self-test (`{"transactionHash":"0xabc"}` → `0xabc`) must pass or the script exits 0-cost. This
       guarantees the three artifact hashes are non-empty AFTER STT is spent.

    3. SCHOOL leg: capture `school_tx` from `cast send … requestSchoolDecision … --json` via
       extract_txhash. Resolve `decision_id` from THAT tx's receipt (`cast receipt $school_tx --json`)
       — the HedgeDecisionRequested log's `topics[2]` on $CONSUMER. This binds the decisionId to this
       run's send (NOT address-only). Then poll schoolSet==true (decisionState member-1), with a
       DecisionFailed poll BOUND to this decisionId (`cast logs … "DecisionFailed(uint256,uint8)" $decision_id …`,
       valid because school requestId==uint256(decisionId)). On timeout/fail: documented BLOCK (return 1).

    4. NOTIONAL leg: capture FROMBLK_N, send `requestNotionalDecision(bytes32) $decision_id --json`,
       capture `notional_tx`. Resolve the notional leg's requestId (topics[1] of the
       HedgeDecisionRequested log in the notional receipt whose topics[2]==$decision_id) and use it to
       bind the notional-leg DecisionFailed poll. Poll notionalSet==true (decisionState member-2). On
       timeout WITH no DecisionFailed: `BLOCK: notionalSet never landed … full mandate REQUIRED for
       LIVEDEP-02 (documented BLOCK, not a pass)` → return 1.

    5. Full-mandate assertion (MINOR-4): `getMandate(...)` → economicTheory (field $1) != 0x0;
       targetNotional (field $3) integer in [1000,100000000] (plain -ge/-le, 64-bit-safe); schoolLabel
       (decisionState member-4 $4) non-empty. Each its own BLOCK/return-1 branch.

    6. StrategistDecided capture, decisionId-BOUND (BLOCKER-1): resolve the log via
       `cast logs --address $CONSUMER "StrategistDecided(bytes32,string,(address,bytes32,uint256,uint32,bool))" $decision_id --from-block $FROMBLK_N`
       — filtered by the indexed decisionId topic so run-2 can NEVER capture run-1's log on the reused
       CONSUMER. Parse the transactionHash → `strategist_tx`; guard non-empty (else BLOCK).

    7. Echo EXACTLY one parseable line on success:
       `RESULT decisionId=<id> school=<label> notional=<n> schoolTx=<h> notionalTx=<h> strategistTx=<h>`.

    Keep the FREE Step-0c surface gate, the oracle-freshness gate, and secret discipline ($PK from
    .env, never echoed). Do NOT touch the contract source or chainId=137.
  </action>
  <verify>
    <automated>cd contracts &amp;&amp; bash -n script/macro-hedge-strategist-e2e.sh &amp;&amp; grep -q 'requestNotionalDecision(bytes32)' script/macro-hedge-strategist-e2e.sh &amp;&amp; grep -q 'StrategistDecided(bytes32,string' script/macro-hedge-strategist-e2e.sh &amp;&amp; grep -qF 'NEED_PER_RUN=$(( JSON_DEPOSIT + LLM_DEPOSIT + LLM_DEPOSIT ))' script/macro-hedge-strategist-e2e.sh &amp;&amp; grep -qF 'NEED_TOTAL=$(( (RUNS + MAX_RETRY) * NEED_PER_RUN ))' script/macro-hedge-strategist-e2e.sh &amp;&amp; grep -q 'extract_txhash' script/macro-hedge-strategist-e2e.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` parses clean.
    - grep finds the notional send, the `StrategistDecided(bytes32,string,…)` log capture filtered by
      `$decision_id` (decisionId-bound, NOT address-only), and a notionalSet poll reading decisionState
      member-2.
    - The captured strategistTx is bound to $DECISION_ID — the `cast logs StrategistDecided … $decision_id`
      call (and the DecisionFailed polls bound by requestId topic) are present (BLOCKER-1).
    - Budget covers the FULL profile: `NEED_PER_RUN = json + 2×LLM` AND `NEED_TOTAL = (RUNS+MAX_RETRY)×NEED_PER_RUN`,
      and Step-0b gates on NEED_TOTAL (MAJOR-1).
    - The tx-hash extractor self-test runs BEFORE the first STT spend (MINOR-3).
    - CONSUMER reuse intact: the only `forge create … MacroHedgeStrategist` stays inside the
      `if [ -z "${CONSUMER:-}" ]` guard.
  </acceptance_criteria>
  <done>run_two_leg() drives school→notional→StrategistDecided against the reused live CONSUMER with every capture bound to that run's decisionId, asserts the full mandate (economicTheory!=0x0, targetNotional∈[1000,1e8], non-empty school via positional awk $1/$3/$4), pre-reserves the full-profile budget, self-tests the tx-hash extractor pre-spend, and surfaces a documented BLOCK on a missing notional callback.</done>
</task>

<task type="auto">
  <name>Task 2: Second DIVERGENT-input decision-moves run (bounded, no-move documented) + run both LIVE (LIVEDEP-02, LIVEDEP-03)</name>
  <files>contracts/script/macro-hedge-strategist-e2e.sh</files>
  <read_first>
    The Task-1 run_two_leg(). Note CONSENSUS (run-1, default 500), CONSENSUS2 (run-2, default 900),
    USER_INTENT (run-1, rate-hike-surprise → SHILLER), USER_INTENT2 (run-2, structural post-Keynesian
    regime risk → POST_KEYNESIAN) are env knobs. Decision-moves is NOT assumed (school+notional are LLM
    outputs) — run-2 maximizes divergence on BOTH the prompt-feeding userIntent AND the consensus.
  </read_first>
  <action>
    1. Call `run_two_leg "$USER_INTENT" "$CONSENSUS"` as RUN 1; parse its RESULT into RUN1_* via
       `parse_result RUN1`.

    2. IDEMPOTENCY (MAJOR-2): persist run-1's RESULT block (decisionId + school + notional + 3 tx
       hashes + consensus + userIntent) to `script/out/.run1-state.env` IMMEDIATELY after run-1
       completes, BEFORE run-2's first send. On re-invoke, if that file exists, SKIP run-1 entirely
       (source the file, no re-spend, no new decisionId) and resume at run-2. State the exact path
       (`script/out/.run1-state.env`) and the `[ -f "$RUN1_STATE" ]` skip-guard in the runner.

    3. RUN 2 — GENUINELY DIVERGENT INPUT (BLOCKER-2(a)): `run_two_leg "$USER_INTENT2" "$CONSENSUS2"`
       (default a post-Keynesian/regime-risk intent + consensus 900, vs run-1's SHILLER-leaning intent
       + 500). userIntent feeds BOTH prompts directly, so this maximizes the chance the mandate moves.
       Parse into RUN2_*.

    4. BOUNDED proof, NO unbounded spend (BLOCKER-2(b)/(c)): the proof is a SMALL bounded number of
       attempts (RUNS=2, MAX_RETRY=1 budget pre-reserved at Step 0b). After the bounded run-2:
       - If `RUN1_school != RUN2_school` OR `RUN1_notional != RUN2_notional`: DECISION_MOVED=true,
         echo `== DECISION-MOVES PROVEN: run1(...) != run2(...) ==`.
       - ELSE (mandate did NOT move) OR run-2 produced no RESULT (BLOCK): do NOT exit-1-and-burn-more,
         do NOT loop, do NOT fabricate. Record a DOCUMENTED outcome — both decisionIds + both mandates
         + a `MOVE_NOTE` ("decision did not move for these inputs — agent-sensitivity finding, NOT a
         silent pass" / "run-2 BLOCK surfaced above"). Echo `== DOCUMENTED NO-MOVE: … ==` and surface
         it; exit 0 with the verdict recorded so the SUMMARY/user owns the honest outcome.

    5. Persist BOTH run blocks (RUN1_* + RUN2_* + DECISION_MOVED + MOVE_NOTE + CONSUMER/oracle/inputs)
       to `script/out/.runs-state.env` for Task 3's publish + the SUMMARY.

    6. RUN IT LIVE: execute
       `CONSUMER=0xf0570CcB1271FFaFf4caCA628F3632257f177b1D MACRO_ORACLE=0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f bash script/macro-hedge-strategist-e2e.sh`
       against Somnia 50312. Capture the real tx hashes + decisionIds + mandates from BOTH runs. If the
       notional callback hits the documented BLOCK, surface it; do not fabricate. Record the captured
       values for Task 3.
  </action>
  <verify>
    <automated>cd contracts &amp;&amp; bash -n script/macro-hedge-strategist-e2e.sh &amp;&amp; grep -q 'run_two_leg' script/macro-hedge-strategist-e2e.sh &amp;&amp; grep -qE 'CONSENSUS2' script/macro-hedge-strategist-e2e.sh &amp;&amp; grep -qE 'USER_INTENT2' script/macro-hedge-strategist-e2e.sh &amp;&amp; grep -q 'DECISION-MOVES' script/macro-hedge-strategist-e2e.sh &amp;&amp; grep -q 'DOCUMENTED NO-MOVE' script/macro-hedge-strategist-e2e.sh &amp;&amp; grep -q 'RUN1_STATE' script/macro-hedge-strategist-e2e.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` clean; run_two_leg + CONSENSUS2 + USER_INTENT2 (divergent run-2 input) + the bounded
      decision-moves verdict (DECISION-MOVES proven branch AND a DOCUMENTED NO-MOVE branch) present.
    - Idempotency (MAJOR-2): the `script/out/.run1-state.env` persist-before-run-2 + the
      `[ -f "$RUN1_STATE" ]` skip-on-re-invoke guard are present; re-invoking after a run-1 success
      does NOT re-fire requestSchoolDecision (no new decisionId / new STT for run-1).
    - LIVE EVIDENCE (recorded in the run log + carried to Task 3): for run 1,
      `cast call 0xf0570CcB1271FFaFf4caCA628F3632257f177b1D "decisionState(bytes32)((bool,bool,uint64,string))" <run1_decisionId> --rpc-url https://api.infra.testnet.somnia.network`
      returns member-1==true AND member-2==true.
    - LIVEDEP-03 outcome is one of: (PASS) RUN1 vs RUN2 differ in school OR notional with both
      decisionIds bound to distinct runs; OR (DOCUMENTED FINDING) a no-move/run-2-block recorded with
      both decisionIds + both mandates + the explicit no-silent-pass note. Never a hang, never a
      fabricated pass.
    - Three real run-1 tx hashes (schoolTx, notionalTx, strategistTx) are non-empty and resolve via
      `cast tx <hash> --rpc-url https://api.infra.testnet.somnia.network` (mined).
  </acceptance_criteria>
  <done>Two runs executed LIVE on Somnia 50312 against the reused deploy; run 1 has decisionState==(true,true) + a full mandate, run-1 persisted before run-2 (idempotent); run 2 uses a divergent intent+consensus and EITHER moves the mandate (LIVEDEP-03 PASS) OR yields a DOCUMENTED no-move finding surfaced to the user; all run-1 tx hashes captured, decisionId-bound, and on-chain-resolvable.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the §2.4 JSON artifact + commit the ABI + reverse BOTH §6 handoff prohibition sentences (LIVEDEP-04, LIVEDEP-05)</name>
  <files>contracts/script/out/somnia-strategist-deployment.json, contracts/script/out/MacroHedgeStrategist.abi.json, docs/UI-AGENT-HANDOFF.md</files>
  <read_first>
    docs/FRONTEND-REQUEST-2026-06-07-strategist-live-deploy.md §2.4 (the EXACT json field set) and §5.
    docs/UI-AGENT-HANDOFF.md line ~120 — it has TWO prohibition sentences: a lowercase
    "...do NOT subscribe to a live `StrategistDecided`..." AND a capital "Do NOT instruct the frontend
    to subscribe to a live `StrategistDecided`..." — BOTH must go (BLOCKER-3). Also line ~49 (the 🟡
    MacroHedgeStrategist row). Recall: contracts/out/ AND contracts/script/out/ are gitignored
    (unanchored `out/`) → `git add -f` is REQUIRED (MINOR-5).
  </read_first>
  <action>
    Using the LIVE run-1 values from `script/out/.runs-state.env` (Task 2):

    1. Write `contracts/script/out/somnia-strategist-deployment.json` with the EXACT §2.4 schema (all
       fields present, real values — NO placeholders, NO private key):
       ```json
       {
         "strategist": "0xf0570CcB1271FFaFf4caCA628F3632257f177b1D",
         "platform": "0x037Bb9C718F3f7fe5eCBDB0b600D607b52706776",
         "llmAgentId": "12847293847561029384",
         "macroOracle": "0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f",
         "chainId": 50312,
         "rpcUrl": "https://api.infra.testnet.somnia.network",
         "dataKey": "<run-1 DATA_KEY (keccak256 of co/inflation-rate)>",
         "consensus": 500,
         "userIntent": "<exact run-1 USER_INTENT free text>",
         "schoolDepositSTT": "<run-1 LLM_DEPOSIT wei>",
         "notionalDepositSTT": "<run-1 LLM_DEPOSIT wei>",
         "decisionId_example": "<run-1 decisionId>",
         "schoolTx": "<run-1 schoolTx>",
         "notionalTx": "<run-1 notionalTx>",
         "strategistDecidedTx": "<run-1 strategistTx>"
       }
       ```
       Optionally append `capturedAt` + `source`. Verify it parses: `jq . contracts/script/out/somnia-strategist-deployment.json`.

    2. Generate + commit the ABI: `cd contracts && forge build`, then
       `jq '.abi' contracts/out/MacroHedgeStrategist.sol/MacroHedgeStrategist.json > contracts/script/out/MacroHedgeStrategist.abi.json`.
       Verify: `jq -e '.[] | select(.name=="StrategistDecided")' contracts/script/out/MacroHedgeStrategist.abi.json`.

    3. Reverse docs/UI-AGENT-HANDOFF.md §6 (BLOCKER-3 — REPLACE the ENTIRE paragraph, BOTH prohibition
       sentences):
       - Line ~120: replace the whole "Agent-1 reality (do NOT subscribe…)" paragraph — including BOTH
         the lowercase "...do NOT subscribe to a live `StrategistDecided`..." sentence AND the capital
         "Do NOT instruct the frontend to subscribe to a live `StrategistDecided`..." sentence — with a
         LIVE-subscribe instruction naming the new address, e.g.:
         "**Agent-1 LIVE (DO subscribe to `StrategistDecided` on Somnia 50312).** The two-leg
         `MacroHedgeStrategist` is deployed LIVE at `0xf0570CcB1271FFaFf4caCA628F3632257f177b1D`
         (Somnia testnet 50312) and emits `StrategistDecided(decisionId, school, HedgeMandate)` on a
         real `requestSchoolDecision` → `requestNotionalDecision` flow (proven on-chain, Phase 18).
         Subscribe to it and decode the `HedgeMandate`; addresses/ABI/inputs are in
         `contracts/script/out/somnia-strategist-deployment.json`. The v1 `0xfA428171…` stays reachable
         as the recorded-trace fallback." Keep the no-bridge honesty label intact.
       - Line ~49 (the 🟡 MacroHedgeStrategist row): flip 🟡 → ✅, text "LIVE on Somnia 50312 at
         0xf0570CcB…7b1D; emits StrategistDecided".
       - Update the status legend / footer date note if it claims the strategist is not deployed.

    4. Stage with force (MINOR-5): `git add -f contracts/script/out/somnia-strategist-deployment.json contracts/script/out/MacroHedgeStrategist.abi.json`
       and `git add contracts/script/macro-hedge-strategist-e2e.sh docs/UI-AGENT-HANDOFF.md`. Do NOT
       commit contracts/.env, any key, or the `.run1-state.env`/`.runs-state.env` working files. Leave
       the actual commit to the workflow's commit step.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/abrigo/abrigo-somnia &amp;&amp; jq -e '.strategist=="0xf0570CcB1271FFaFf4caCA628F3632257f177b1D" and (.schoolTx|startswith("0x")) and (.notionalTx|startswith("0x")) and (.strategistDecidedTx|startswith("0x")) and .chainId==50312' contracts/script/out/somnia-strategist-deployment.json &amp;&amp; jq -e '.[] | select(.name=="StrategistDecided")' contracts/script/out/MacroHedgeStrategist.abi.json &amp;&amp; grep -q '0xf0570CcB1271FFaFf4caCA628F3632257f177b1D' docs/UI-AGENT-HANDOFF.md &amp;&amp; ! grep -qi 'subscribe to a live' docs/UI-AGENT-HANDOFF.md &amp;&amp; ! grep -q 'Do NOT instruct the frontend to subscribe' docs/UI-AGENT-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - `jq` confirms the json has the live strategist address, chainId 50312, and three `0x…` tx fields
      (schoolTx, notionalTx, strategistDecidedTx) all non-empty.
    - The three tx hashes resolve on-chain:
      `cast tx <strategistDecidedTx> --rpc-url https://api.infra.testnet.somnia.network` returns a mined tx.
    - `contracts/script/out/MacroHedgeStrategist.abi.json` parses and contains the `StrategistDecided`
      event entry.
    - docs/UI-AGENT-HANDOFF.md CONTAINS `0xf0570CcB1271FFaFf4caCA628F3632257f177b1D` and BOTH
      prohibition sentences are GONE: case-insensitive `! grep -qi 'subscribe to a live'` AND
      `! grep -q 'Do NOT instruct the frontend to subscribe'` (BLOCKER-3).
    - `git status --short` shows the json + abi staged (force-added past the gitignore) and NO
      contracts/.env / `.run1-state.env` / `.runs-state.env` staged.
  </acceptance_criteria>
  <done>somnia-strategist-deployment.json matches §2.4 with real run-1 addresses + 3 on-chain tx hashes; the ABI is extracted to a committable path and force-staged; UI-AGENT-HANDOFF.md marks the strategist ✅ LIVE at 0xf0570C…7b1D and BOTH §6 prohibition sentences are replaced by a LIVE-subscribe instruction.</done>
</task>

</tasks>

<verification>
- LIVEDEP-02: `cast call 0xf0570CcB1271FFaFf4caCA628F3632257f177b1D "decisionState(bytes32)((bool,bool,uint64,string))" <run1_decisionId> --rpc-url https://api.infra.testnet.somnia.network` → member-1 (schoolSet)==true AND member-2 (notionalSet)==true; `getMandate(...)` economicTheory!=0x0 and targetNotional∈[1000,100000000]; a decisionId-bound StrategistDecided log exists on the address with a non-empty school string.
- LIVEDEP-03: run-2 uses a divergent USER_INTENT2+CONSENSUS2 and EITHER yields a different (school OR targetNotional) — PASS — OR a DOCUMENTED no-move finding (both decisionIds + mandates + the no-silent-pass note); the runner is bounded (RUNS+MAX_RETRY budget) and never hangs/fabricates. Every per-run capture is decisionId-bound so run-1 is never compared to itself.
- LIVEDEP-04: `jq` parses somnia-strategist-deployment.json with the live strategist + 3 real run-1 tx hashes; each resolves via `cast tx`; MacroHedgeStrategist.abi.json contains StrategistDecided; both force-staged (`git add -f`).
- LIVEDEP-05: `grep 0xf0570CcB1271FFaFf4caCA628F3632257f177b1D docs/UI-AGENT-HANDOFF.md` matches AND BOTH `grep -i "subscribe to a live"` AND `grep "Do NOT instruct the frontend to subscribe"` find nothing.
- Guardrails: no new `forge create` of the strategist (CONSUMER reused); contract `POLYGON_CHAIN_ID = 137` untouched; no PK echoed/committed; the json holds addresses+hashes+inputs only.
</verification>

<success_criteria>
A real, on-chain-resolvable two-leg StrategistDecided on Somnia 50312 from 0xf0570CcB…7b1D with a full HedgeMandate (decisionState==(true,true)); a second DIVERGENT-input run that either proves the decision moves or records a documented no-move finding (never a fake pass), with every capture decisionId-bound; somnia-strategist-deployment.json published to the §2.4 schema with three real tx hashes + the committed ABI; and docs/UI-AGENT-HANDOFF.md reversed to mark the strategist LIVE and instruct the frontend to subscribe (BOTH prohibition sentences gone). All four LIVEDEP-02/03/04/05 grep/cast/jq-verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/18-on-chain-decision-moves-proof-+-publish/18-01-SUMMARY.md` recording: the two runs' decisionIds + mandates (school + notional each), all six+ real tx hashes (school/notional/strategist × 2 runs), the exact STT spent, the published json path + ABI path, the reversed handoff lines, AND the LIVEDEP-03 verdict (decision moved, OR the documented no-move/agent-sensitivity finding). If the notional callback hit the documented BLOCK on any attempt, record the BLOCK + the outcome honestly (an unverifiable claim is not acceptable).
</output>

---
phase: 17-live-deploy-pre-flight-surface-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/script/macro-hedge-strategist-e2e.sh
autonomous: true
requirements: [LIVEDEP-01]
user_setup: []

must_haves:
  truths:
    - "The two-leg StrategistDecided strategist has live bytecode at a NEW Somnia-50312 address (≠ the v1 0xfA428171…)"
    - "The deployed strategist's constructor-wired surface reads back the live platform, LLM agent id, and MacroOracle"
    - "A single cheap requestSchoolDecision school-leg probe returns a real validator callback (schoolSet==true, NOT DecisionFailed / no-callback) — the volatile LLM_AGENT_ID + platform are LIVE"
    - "platform-has-code + v1-reachability + deploy-network chain-id(50312) are re-confirmed FREE before any STT spend; the LLM_AGENT_ID liveness is proven BY the single cheap school-leg probe (which spends STT BY DESIGN and precedes the full Phase-18 prove run) — a wrong id surfaces as DecisionFailed/no-callback and rebates most of the deposit"
    - "The v1 contract 0xfA428171E1F5B56f92C67C002De1d8e90B053EE1 still has its bytecode (reachable, not decommissioned)"
  artifacts:
    - path: "contracts/script/macro-hedge-strategist-e2e.sh"
      provides: "Adapted two-leg runner (requestSchoolDecision/requestNotionalDecision/getMandate/decisionState/StrategistDecided), with a pre-spend FREE surface gate (platform code + v1 code + chain-id 50312) and a scripted oracle-freshness gate"
      contains: "requestSchoolDecision"
  key_links:
    - from: "macro-hedge-strategist-e2e.sh FREE surface gate (Step 0c)"
      to: "the FIRST STT spend (requestMacro refresh / cast send / forge create)"
      via: "ordering — the gate runs and must pass FIRST"
      pattern: "chain-id|cast code .*PLATFORM"
    - from: "deployed strategist immutables"
      to: "live platform 0x037Bb9… / agent 12847293847561029384 / oracle 0xAcA75144…"
      via: "cast call read-back equality"
      pattern: "PLATFORM\\(\\)|ORACLE\\(\\)|LLM_AGENT_ID\\(\\)"
---

<objective>
Deploy the source-complete, offline-proven (Phase 12, 19/19) two-leg `MacroHedgeStrategist`
(`StrategistDecided` API) LIVE to Somnia testnet 50312 at a NEW address, wired to the live
platform / LLM agent / MacroOracle, and re-confirm the FREE testnet surface (platform-has-code,
v1-reachable, deploy chain-id == 50312) BEFORE any STT spend; then prove the volatile LLM_AGENT_ID
liveness BY a single cheap school-leg probe (which spends STT by design, ahead of the full Phase-18 run).

Purpose: §4 of the FRONTEND-REQUEST mandates re-confirming the FREE surface BEFORE spending STT, and
is async-tolerant (a recorded run is acceptable). Agent-ID liveness is NOT free-provable — it is proven
by the one cheap probe. A `DecisionFailed`/no-callback means a constant is wrong (and rebates most of the
deposit). This phase is the spend-gate: FREE gate → deploy → ONE cheap school-leg probe. NO new contract
development — the source is complete.

Output: a NEW deployed strategist address with non-empty bytecode + verified immutables; a non-failed
`requestSchoolDecision` probe callback (OR a documented PARTIAL — deploy+immutables+v1-reachable PROVEN,
school-callback DEFERRED with the captured request tx); the adapted runner; v1 0xfA428171… still
reachable. The artifact publish + full decision-moves prove run are Phase 18 — NOT here.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/FRONTEND-REQUEST-2026-06-07-strategist-live-deploy.md
@CLAUDE.md

# The contract to DEPLOY (source-complete, do NOT edit it):
@contracts/src/instrument/MacroHedgeStrategist.sol
# The v1 runner to ADAPT (this plan edits this file):
@contracts/script/macro-hedge-strategist-e2e.sh

<interfaces>
<!-- Contracts the executor works against. Do NOT modify the .sol; only adapt the runner. -->

MacroHedgeStrategist.sol (DEPLOY this; constructor + read surface):
```solidity
// constructor(address platform, address oracle) SomniaAgentConsumer(platform) { ORACLE = oracle; }
//   forge create constructor-args ORDER: <platform> <oracle>   (NO agent id — it is a constant)
uint256 public constant LLM_AGENT_ID = 12847293847561029384;   // VOLATILE — liveness proven by the probe, not a free check
uint256 public constant MIN_NOTIONAL = 1_000;
uint256 public constant MAX_NOTIONAL = 100_000_000;
uint32  public constant POLYGON_CHAIN_ID = 137;                 // do NOT alter this join constant (NOT the deploy chain-id)
address public immutable ORACLE;                                // read back via ORACLE()
// PLATFORM is an immutable inherited from SomniaAgentConsumer — read back via PLATFORM()

function requestSchoolDecision(string calldata userIntent, bytes32 dataKey, int256 consensus)
    external payable returns (bytes32 decisionId);              // school leg = the liveness probe
function requestNotionalDecision(bytes32 decisionId) external payable returns (uint256 requestId);
function getMandate(bytes32 decisionId) external view returns (HedgeMandate memory);
function decisionState(bytes32 decisionId)
    external view returns (DecisionState memory);               // {bool schoolSet; bool notionalSet; uint64 decidedAt; string schoolLabel;}

// EVENTS the runner polls:
event HedgeDecisionRequested(uint256 indexed requestId, bytes32 indexed decisionId, uint8 leg);  // emitted SYNCHRONOUSLY in requestSchoolDecision — decisionId = topics[2]
event DecisionFailed(uint256 indexed requestId, ResponseStatus status);                          // FAILURE signal (TWO causes — see below)
event StrategistDecided(bytes32 indexed decisionId, string school, HedgeMandate mandate);        // join (both legs) — Phase 18 territory
```

DecisionFailed (MacroHedgeStrategist `_onResult` L227-242) has TWO distinct causes, and the probe MUST
distinguish them:
  (a) validator `status != Success` / wrong agent-id / dead platform  → the agent path itself is broken;
  (b) a landed-but-UNMAPPED school label (`MacroThesisRegistry.thesisOf(s).ok == false`, or a malformed
      payload caught by the try/catch) → the agent IS LIVE, it answered, but the registry has no match.
On a wrong agent id / dead platform the callback typically never lands (poll timeout). A landed
`DecisionFailed` with an unmapped school means the agent is live — re-run with an adjusted intent.
`decisionState(id).schoolSet == true` is the affirmative liveness proof.

MacroOracle.sol (REUSE the live one; freshness gate target):
```solidity
mapping(bytes32 => MacroDatum) public latest;   // auto-getter: latest(bytes32) returns (bytes32,int256,uint64,uint64)
// struct MacroDatum { bytes32 dataKey; int256 scaledValue; uint64 observedAt; uint64 deliveredAt; }
//   -> the cast tuple sig is FOUR members: latest(bytes32)((bytes32,int256,uint64,uint64))
//   -> deliveredAt is the 4TH member (index 3). deliveredAt != 0 ⇔ the datum has been delivered.
```

Known live constants (from the FRONTEND-REQUEST §2 + STATE 11-03):
- PLATFORM   = 0x037Bb9C718F3f7fe5eCBDB0b600D607b52706776
- LLM agent  = 12847293847561029384
- MacroOracle (REUSE the live one) = 0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f
- v1 strategist (keep reachable)   = 0xfA428171E1F5B56f92C67C002De1d8e90B053EE1
- Somnia 50312 RPC = https://api.infra.testnet.somnia.network  (deploy-network chain-id MUST be 50312)
- keeper-proxy = https://keeper-eta-pied.vercel.app/
- env (contracts/.env, gitignored): SOMNIA_TESTNET_PK, SOMNIA_TESTNET_ADDRESS

Phase-17 TRUE STT cost (single-probe footprint — NOT the v1 demo's 2×(json+2×llm)):
  deploy gas + ONE json-fetch oracle refresh (0.09 STT, only if the datum is stale)
  + ONE llm-inference school deposit (~0.21 STT). Size the balance check to this single probe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt the v1 e2e runner to the two-leg StrategistDecided API + add the FREE pre-spend surface gate, the scripted oracle-freshness gate, and the idempotent re-run path</name>
  <files>contracts/script/macro-hedge-strategist-e2e.sh</files>
  <read_first>
    - contracts/script/macro-hedge-strategist-e2e.sh (the WHOLE v1 runner — Steps 0–6; keep the deposit-math, poll_log helper, big-int balance compare, and the MacroOracle PROXY_BASE guard byte-for-byte)
    - contracts/src/instrument/MacroHedgeStrategist.sol L148-181 (requestSchoolDecision — emits HedgeDecisionRequested synchronously; decisionId = topics[2]), L189-207 (requestNotionalDecision), L227-242 (the DecisionFailed dual cause), L295-303 (decisionState shape), L127 (StrategistDecided sig)
    - contracts/src/MacroOracle.sol L70-75 (MacroDatum 4 members) + L141 (`latest` auto-getter)
    - docs/FRONTEND-REQUEST-2026-06-07-strategist-live-deploy.md §2.3 (the exact re-point list) + §4 (async-tolerant; recorded run acceptable)
  </read_first>
  <action>
    Edit `contracts/script/macro-hedge-strategist-e2e.sh` in place. PRESERVE the shebang, `set -euo pipefail`,
    the RPC/PLATFORM/LLM_AGENT_ID/PROXY_BASE/SUBSIZE constants, the per-class deposit TERMS, the `.env`
    source (`SOMNIA_TESTNET_PK`/`SOMNIA_TESTNET_ADDRESS`), the deposit math (Step 0), the `poll_log` helper,
    and the MacroOracle PROXY_BASE BadProxyBase guard — all verbatim. Adapt the balance check per change (6) below.

    Make these EXACT changes:

    1. Add a hardcoded MacroOracle DEFAULT and a v1-address constant near the top constants:
       `MACRO_ORACLE="${MACRO_ORACLE:-0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f}"` (REUSE the live oracle — do NOT redeploy it), and
       `V1_STRATEGIST="0xfA428171E1F5B56f92C67C002De1d8e90B053EE1"`.

    2. INSERT a NEW FREE pre-spend "Step 0c: surface gate (free reads only)" AFTER Step 0b (balance) and
       BEFORE Step 1 (oracle) — this is the §4 ordering guarantee, it MUST run before the FIRST STT spend
       (the first of requestMacro / cast send / forge create). ALL checks here are free reads:
         - assert deploy-network chain-id == 50312 with an EXPLICIT equality+FAIL branch (NOT a bare echo of
           the literal): write the test in the shape
           `[ "$(cast chain-id --rpc-url "$RPC")" = "50312" ] || { echo "FAIL: deploy-network chain-id != 50312 (Somnia testnet) — wrong RPC"; exit 1; }`
           (the `!= … exit 1` form is equally acceptable). (This is the DEPLOY chain-id; it is DISTINCT from the
           contract's POLYGON_CHAIN_ID=137 join constant — do NOT conflate.)
         - assert `PLATFORM` has code: `cast code "$PLATFORM" --rpc-url "$RPC"` length > 2 (not "0x"); FAIL
           "platform $PLATFORM has no code — volatile platform constant is wrong" else.
         - assert the v1 strategist is still reachable (the §4 keep-reachable guardrail):
           `cast code "$V1_STRATEGIST" --rpc-url "$RPC"` length > 2; FAIL "v1 $V1_STRATEGIST unreachable" else.
         - echo an EXPLICIT human-verify reminder (NOT an automated gate): "REMINDER: LLM_AGENT_ID
           $LLM_AGENT_ID liveness is NOT free-provable — it is proven BY the cheap school-leg probe at
           Step 4 (which spends STT by design). A wrong id surfaces there as DecisionFailed/no-callback."
         - print "== surface gate PASS (chain-id 50312 + platform code + v1 reachable) — proceeding to spend ==".

    3. ADD a SCRIPTED "Step 2b: oracle-freshness gate" immediately AFTER Step 2's oracle refresh and BEFORE
       Step 3 deploy / Step 4 probe — so a stale datum cannot burn the LLM deposit via an UnknownKey/stale
       read at requestSchoolDecision:
         - `FRESH=$(cast call "$MACRO_ORACLE" "latest(bytes32)((bytes32,int256,uint64,uint64))" "$DATA_KEY" --rpc-url "$RPC")`
         - parse the 4TH tuple member (`deliveredAt`, index 3) and assert it is `!= 0`; FAIL with
           "oracle datum stale (deliveredAt==0) for $DATA_KEY — refusing to spend the LLM deposit" else.
         - print "== oracle-freshness gate PASS (deliveredAt != 0) ==".

    4. Re-point Step 3 deploy: constructor-args become `"$PLATFORM" "$MACRO_ORACLE"` (already correct in v1 —
       confirm the order is platform THEN oracle). Capture the new address into `CONSUMER` exactly as v1 does,
       BUT guard the deploy actually mined before any read-back: assert `cast code "$CONSUMER" --rpc-url "$RPC"`
       length > 2; FAIL "deploy not mined — $CONSUMER has no code" else. THEN add the immutable read-backs (SC-1):
         - `cast call "$CONSUMER" "PLATFORM()(address)" --rpc-url "$RPC"` equals `$PLATFORM` (case-insensitive compare via `tr A-Z a-z`); FAIL else.
         - `cast call "$CONSUMER" "ORACLE()(address)" --rpc-url "$RPC"` equals `$MACRO_ORACLE`; FAIL else.
         - `cast call "$CONSUMER" "LLM_AGENT_ID()(uint256)" --rpc-url "$RPC"` equals `$LLM_AGENT_ID`; FAIL else.
         - assert `$CONSUMER` (lowercased) != `0xfa428171e1f5b56f92c67c002de1d8e90b053ee1` (NEW address, not v1); FAIL else.
       NOTE on idempotent re-run (MAJOR-6): the v1 `if [ -z "${CONSUMER:-}" ]` reuse guard MUST be preserved
       so that passing `CONSUMER=$NEW_CONSUMER` skips a second `forge create`. Likewise the `if [ -z "${MACRO_ORACLE:-}" ]`
       guard means passing `MACRO_ORACLE=…` reuses the oracle (no second refresh deploy). On a retry the runner
       is invoked with BOTH set, so it ONLY re-fires the probe — no double-deploy, no STT strand.

    5. Re-point Step 4 to the SCHOOL leg (this is the cheap liveness probe; Phase 18 does the notional+join):
         - call `cast send "$CONSUMER" "requestSchoolDecision(string,bytes32,int256)" "$USER_INTENT" "$DATA_KEY" "$CONSENSUS" --value "$LLM_DEPOSIT" --private-key "$PK" --rpc-url "$RPC"`
           where `USER_INTENT="${USER_INTENT:-Hedge COP depreciation from a rate-hike surprise}"`.
         - poll `HedgeDecisionRequested(uint256,bytes32,uint8)` (emitted synchronously) and parse `decisionId`
           from topics[2] using the EXISTING awk topic-parse (topics[0]=sig, topics[1]=requestId, topics[2]=decisionId).
         - then poll for the SCHOOL callback landing: loop on `decisionState(bytes32)((bool,bool,uint64,string))`
           on `$CONSUMER`. POSITION-EXPLICIT read (MINOR-4): the tuple's TWO leading bools are
           {schoolSet, notionalSet}; do NOT `grep '\btrue\b'` blindly (that is positional-blind — notionalSet
           is also a bool and a future both-true state would spuriously match). Instead decode the FIRST member
           into a dedicated var (e.g. `SCHOOL_SET=$(cast call … | awk 'NR==1{print $1}')` — member 1 only) and
           loop until `[ "$SCHOOL_SET" = "true" ]`. (Safe today since this school-only probe leaves notionalSet
           false, but the read is pinned to member 1 regardless.) On the FAILURE branches, DISTINGUISH
           the two causes (MINOR-8):
             * a `DecisionFailed(uint256,uint8)` log present → message: "DecisionFailed — agent ANSWERED but the
               school label is UNMAPPED in MacroThesisRegistry (agent LIVE; re-run with an adjusted USER_INTENT)";
             * a `$TIMEOUT_S` timeout with NO callback → message: "no callback / timeout — LLM_AGENT_ID
               $LLM_AGENT_ID / platform likely DEAD or WRONG (§4)".
         - on success: `echo "== probe PASS: requestSchoolDecision callback landed (schoolSet=true) — volatile surface LIVE =="`.

    6. DELETE the v1 Step 5 (requestSizeDecision) and Step 6 (action/sizeBps assertions) — those legs + the
       full join + the decision-moves assertions are Phase 18. RE-SIZE the Step-0b balance check to the
       SINGLE-PROBE footprint. Make ALL of these EXACT edits to the Step-0/Step-0b block (v1 lines 75–92):
         (a) Re-point `NEED_PER_RUN`: `NEED_PER_RUN=$(( JSON_DEPOSIT + LLM_DEPOSIT ))` (one json refresh + ONE
             school-leg infer — NOT the v1 `JSON_DEPOSIT + 2*LLM_DEPOSIT`).
         (b) DELETE the v1 `NEED_DEMO=$(( 2 * NEED_PER_RUN ))` line ENTIRELY (the 2-run demo budget is Phase 18;
             nothing in Phase 17 may reference `NEED_DEMO`).
         (c) Change the Step-0b balance comparison operands FROM `NEED_DEMO` TO `NEED_PER_RUN` — both the
             digit-count test and the lexical test (v1 line 89 becomes:
             `if [ "${#BAL}" -lt "${#NEED_PER_RUN}" ] || { [ "${#BAL}" -eq "${#NEED_PER_RUN}" ] && [[ "$BAL" < "$NEED_PER_RUN" ]]; }; then`).
         (d) Update the Step-0b echo/labels + the FAIL message to reference `NEED_PER_RUN` and the single probe
             (v1 lines 81/83/90/91): drop the "need_demo(2 runs)" echo, drop the "2-run demo" wording, and the
             FAIL line reads "need >= $NEED_PER_RUN wei, have $BAL" (NOT `$NEED_DEMO`).
       SPEND-ACCOUNTING HONESTY NOTE (MAJOR-2): keep `JSON_DEPOSIT` inside `NEED_PER_RUN` — it is a deliberate
       CONSERVATIVE OVER-RESERVE. Add a comment above `NEED_PER_RUN` stating: the JSON_DEPOSIT covers the
       cold/first run where the oracle datum is stale and Step 2 fires a json-fetch refresh; on the idempotent
       fresh-retry path (MACRO_ORACLE reused + datum already fresh, deliveredAt != 0) NO json-fetch fires, so
       the actual probe spends LESS than NEED_PER_RUN — the over-reserve only ever leaves headroom, it never
       strands funds and the conditional refresh is honestly the only json spend.
       End this runner after the school-probe success with a final line:
       `echo "== Phase-17 done: CONSUMER=$CONSUMER decisionId=$DECISION_ID schoolSet=true =="`
       and a comment: `# Phase 18 adapts THIS runner further: add requestNotionalDecision + StrategistDecided join + the somnia-strategist-deployment.json publish.`

    7. Do NOT introduce any new secret literal; the key stays sourced from `contracts/.env` as `$SOMNIA_TESTNET_PK`/`$PK`.
       Never `echo` the PK. Do NOT alter the contract's `POLYGON_CHAIN_ID = 137` join constant (it is not in this
       script; confirm no edit references it — the chain-id you assert in Step 0c is the deploy network 50312).

    After editing, tail-check the file ends cleanly (no stray tags) and `bash -n contracts/script/macro-hedge-strategist-e2e.sh` parses.
  </action>
  <verify>
    <automated>bash -n contracts/script/macro-hedge-strategist-e2e.sh && grep -q "requestSchoolDecision(string,bytes32,int256)" contracts/script/macro-hedge-strategist-e2e.sh && grep -q 'latest(bytes32)((bytes32,int256,uint64,uint64))' contracts/script/macro-hedge-strategist-e2e.sh && grep -q "cast chain-id" contracts/script/macro-hedge-strategist-e2e.sh && ! grep -qE "requestActionDecision|requestSizeDecision|HedgeDecisionMade|getDecision" contracts/script/macro-hedge-strategist-e2e.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n contracts/script/macro-hedge-strategist-e2e.sh` exits 0 (syntactically valid).
    - `grep -c "requestSchoolDecision(string,bytes32,int256)" contracts/script/macro-hedge-strategist-e2e.sh` >= 1.
    - `grep -cE "requestActionDecision|requestSizeDecision|HedgeDecisionMade|getDecision" contracts/script/macro-hedge-strategist-e2e.sh` == 0 (all v1 sigs re-pointed/removed).
    - `grep -q "decisionState(bytes32)" contracts/script/macro-hedge-strategist-e2e.sh` (the schoolSet poll uses the new getter).
    - `grep -q 'latest(bytes32)((bytes32,int256,uint64,uint64))' contracts/script/macro-hedge-strategist-e2e.sh` (the 4-member oracle-freshness cast; NO 5-member tuple).
    - `! grep -q 'latest(bytes32)((bytes32,int256,uint64,uint64,uint256))' contracts/script/macro-hedge-strategist-e2e.sh` (the bogus 5-member tuple is GONE).
    - The deploy chain-id has an EXPLICIT equality-or-FAIL branch (not a bare literal that also appears in comments/constants): `grep -qE '\[ "\$\(cast chain-id --rpc-url "\$RPC"\)" (=|!=) "?50312"? \]' contracts/script/macro-hedge-strategist-e2e.sh` AND the same line (or its `||` continuation) contains an `exit 1` FAIL — i.e. the assertion is a real test+branch, not just a grep for the literal `50312`.
    - `grep -q 'PLATFORM()(address)' contracts/script/macro-hedge-strategist-e2e.sh && grep -q 'ORACLE()(address)' contracts/script/macro-hedge-strategist-e2e.sh && grep -q 'LLM_AGENT_ID()(uint256)' contracts/script/macro-hedge-strategist-e2e.sh` (the three immutable read-backs present).
    - The FREE surface gate precedes the FIRST spend (ordering smoke check, comment-anchored per MINOR-5 — strip
      comment lines first so a `# forge create …` note above the header can't false-PASS): `grep -vE '^\s*#' contracts/script/macro-hedge-strategist-e2e.sh | awk '/Step 0c/{g=NR} (/requestMacro/||/cast send/||/forge create/){if(!s)s=NR} END{exit !(g>0 && g<s)}'` exits 0 (Step 0c precedes the first real spend command line).
    - The oracle-freshness gate precedes the probe (comment-anchored, MINOR-5): `grep -vE '^\s*#' contracts/script/macro-hedge-strategist-e2e.sh | awk '/Step 2b/{f=NR} /requestSchoolDecision/{if(!p)p=NR} END{exit !(f>0 && f<p)}'` exits 0. (These remain ordering smoke checks, not proofs; the per-step FAIL branches enforce the gates at runtime.)
    - `grep -q "0xfA428171E1F5B56f92C67C002De1d8e90B053EE1" contracts/script/macro-hedge-strategist-e2e.sh` (v1-reachable guard present).
    - Balance check re-sized to single probe: `grep -q 'NEED_PER_RUN=$(( JSON_DEPOSIT + LLM_DEPOSIT ))' contracts/script/macro-hedge-strategist-e2e.sh` (no 2×(json+2×llm) demo budget).
    - The stale 2-run demo budget is GONE: `! grep -q 'NEED_DEMO' contracts/script/macro-hedge-strategist-e2e.sh` (the `NEED_DEMO=` line deleted; no reference survives).
    - The Step-0b balance compare gates on the single-probe budget: `grep -qE '\$\{#BAL\}.*\$\{#NEED_PER_RUN\}' contracts/script/macro-hedge-strategist-e2e.sh` AND `grep -qE '"\$BAL" < "\$NEED_PER_RUN"' contracts/script/macro-hedge-strategist-e2e.sh` (both operands of the digit-count + lexical compare reference `NEED_PER_RUN`, not the deleted `NEED_DEMO`).
    - Secret discipline (MINOR-9): the PK is referenced ONLY as the env var (`grep -qE '\$(SOMNIA_TESTNET_)?PK' contracts/script/macro-hedge-strategist-e2e.sh`) AND no echo of it exists (`! grep -qE 'echo.*\$(SOMNIA_TESTNET_)?PK' contracts/script/macro-hedge-strategist-e2e.sh`).
  </acceptance_criteria>
  <done>The runner targets the two-leg StrategistDecided API, deploys with the correct constructor order, guards the deploy mined, reads back all three immutables, runs the FREE surface gate (chain-id 50312 + platform code + v1-reachable) BEFORE the first STT spend, runs the scripted oracle-freshness gate (4-member tuple, deliveredAt != 0) before the probe, distinguishes the two DecisionFailed causes, and is idempotent on re-run (CONSUMER+MACRO_ORACLE reuse). v1 sigs are gone; balance sized to a single probe.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy the two-leg strategist live to Somnia 50312, verify immutables + the school-leg liveness probe (PARTIAL allowed on no-callback), and re-confirm v1 reachable</name>
  <files>contracts/script/macro-hedge-strategist-e2e.sh</files>
  <read_first>
    - contracts/script/macro-hedge-strategist-e2e.sh (the adapted runner from Task 1 — the exact steps to execute)
    - contracts/.env (confirm SOMNIA_TESTNET_PK + SOMNIA_TESTNET_ADDRESS are present; NEVER echo the PK)
    - docs/FRONTEND-REQUEST-2026-06-07-strategist-live-deploy.md §4 (async-tolerant; recorded run acceptable; surface FIRST)
  </read_first>
  <action>
    This task SPENDS real testnet STT from the key in `contracts/.env`. Execute in this strict order
    (the runner enforces it; this task RUNS the runner and captures evidence):

    1. PRE-FLIGHT (no spend, redundant safety on top of the runner's Step 0c):
       `cd contracts && cast chain-id --rpc-url https://api.infra.testnet.somnia.network` == `50312`, AND
       `cast code 0x037Bb9C718F3f7fe5eCBDB0b600D607b52706776 --rpc-url …` non-empty (platform alive), AND
       `cast code 0xfA428171E1F5B56f92C67C002De1d8e90B053EE1 --rpc-url …` non-empty (v1 still reachable —
       the §4 keep-reachable guardrail, captured BEFORE we spend). If ANY fails, STOP — do NOT spend.

    2. Confirm the live MacroOracle datum freshness so `requestSchoolDecision` does not read a stale/unset datum:
       `cast call 0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f "latest(bytes32)((bytes32,int256,uint64,uint64))" $(cast keccak "co/inflation-rate") --rpc-url https://api.infra.testnet.somnia.network`.
       If the 4TH member (`deliveredAt`, index 3) reads 0, the runner's Step 2 oracle-refresh + Step 2b gate
       will populate/assert it (json-fetch spend 0.09 STT); otherwise the populated datum is reused. (The
       runner handles this — do not hand-roll the 5-member tuple; the struct is 4 members.)

    3. RUN the adapted runner against the LIVE testnet, capturing full stdout AND the runner's real exit code
       (pipefail masks a mid-script exit 1 behind tee — capture `${PIPESTATUS[0]}`):
       `MACRO_ORACLE=0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f CONSENSUS=500 bash script/macro-hedge-strategist-e2e.sh 2>&1 | tee /tmp/17-deploy-probe.log; RC=${PIPESTATUS[0]}`.
       The runner: FREE surface gate → (refreshes oracle if needed) → oracle-freshness gate → deploys the
       strategist (new CONSUMER) → guards deploy mined → asserts the three immutables → fires
       `requestSchoolDecision` → polls until `schoolSet==true` (or fails on DecisionFailed/timeout).

    4. CALLBACK-TIMEOUT / RE-RUN policy (MAJOR-5 + MAJOR-6): Somnia validator-callback latency is variable.
       Parse the deployed `NEW_CONSUMER` from /tmp/17-deploy-probe.log (the `CONSUMER=` line). If the school
       callback TIMED OUT (no-callback) on the first run, re-run the probe ONLY — pass BOTH the deployed
       consumer and the oracle so NOTHING redeploys and the oracle is NOT refreshed again:
       `CONSUMER=$NEW_CONSUMER MACRO_ORACLE=0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f CONSENSUS=500 bash script/macro-hedge-strategist-e2e.sh 2>&1 | tee /tmp/17-deploy-probe-retry.log; RC=${PIPESTATUS[0]}`.
       Run the retry AT MOST ONCE.
         - If a landed `DecisionFailed` shows an UNMAPPED school (agent answered) → agent is LIVE; this is a
           registry/intent miss, NOT a wrong constant — note it and treat liveness as PROVEN-by-answer.
         - If STILL a hard no-callback after the single retry → record a DOCUMENTED PARTIAL (see step 5),
           do NOT spend further.

    5. TERMINAL BRANCHES — capture evidence per outcome:
       a. SUCCESS: `decisionState(decisionId).schoolSet == true`. Capture: NEW_CONSUMER address;
          `cast code <NEW_CONSUMER>` non-empty; the three immutable read-backs; decisionId + school-leg tx hash.
       b. PARTIAL (allowed by §4 async-tolerance): deploy + immutables + v1-reachable are PROVEN but the school
          callback never landed after the single retry. Record: NEW_CONSUMER + immutables PROVEN, v1-reachable
          PROVEN, and the REAL requestSchoolDecision REQUEST tx hash with `schoolSet liveness DEFERRED
          (no-callback)`. This is NOT a hard-fail; do NOT strand the freshly deployed contract.
       Record the chosen outcome + all real tx hashes in the SUMMARY (the published JSON artifact is Phase 18).

    DO NOT commit any key. DO NOT write a key into any file or on-chain. DO NOT echo the PK. DO NOT decommission v1.
    A recorded run with real tx hashes is acceptable; an unverifiable claim is NOT.
  </action>
  <verify>
    <automated>cd contracts && test "$(cast code "$NEW_CONSUMER" --rpc-url https://api.infra.testnet.somnia.network | wc -c)" -gt 2 && test "$(cast chain-id --rpc-url https://api.infra.testnet.somnia.network)" = "50312" && test "$(cast code 0xfA428171E1F5B56f92C67C002De1d8e90B053EE1 --rpc-url https://api.infra.testnet.somnia.network | wc -c)" -gt 2</automated>
  </verify>
  <acceptance_criteria>
    - `cast chain-id --rpc-url https://api.infra.testnet.somnia.network` == `50312` (deploy network is Somnia testnet, distinct from the contract's POLYGON_CHAIN_ID=137). *(SC-1)*
    - `cast code <NEW_CONSUMER> --rpc-url …` returns bytecode length > 2 (deploy mined), and the address lowercased != `0xfa428171e1f5b56f92c67c002de1d8e90b053ee1` (NEW address). *(SC-1)*
    - `cast call <NEW_CONSUMER> "PLATFORM()(address)" --rpc-url <somnia-50312>` == `0x037Bb9C718F3f7fe5eCBDB0b600D607b52706776` (case-insensitive). *(SC-1)*
    - `cast call <NEW_CONSUMER> "ORACLE()(address)" --rpc-url <somnia-50312>` == `0xAcA75144f644220f1dEAD5F989C350D8e0Cc983f`. *(SC-1)*
    - `cast call <NEW_CONSUMER> "LLM_AGENT_ID()(uint256)" --rpc-url <somnia-50312>` == `12847293847561029384`. *(SC-1)*
    - SC-2 liveness — EITHER: `cast call <NEW_CONSUMER> "decisionState(bytes32)((bool,bool,uint64,string))" <decisionId> --rpc-url <somnia-50312>` shows the FIRST member `schoolSet == true` (no DecisionFailed for that requestId); OR a documented PARTIAL: deploy+immutables proven AND the real requestSchoolDecision REQUEST tx hash is recorded with `schoolSet liveness DEFERRED (no-callback after one retry)`. *(SC-2)*
    - `cast code 0xfA428171E1F5B56f92C67C002De1d8e90B053EE1 --rpc-url …` STILL returns non-empty v1 bytecode. *(SC-3)*
    - The SUMMARY records the NEW address + real tx hashes (verifiable on the Somnia 50312 explorer). Secret discipline: PK referenced only as `$SOMNIA_TESTNET_PK`/`$PK` and never echoed (`! grep -qE 'echo.*\$(SOMNIA_TESTNET_)?PK' contracts/script/macro-hedge-strategist-e2e.sh`).
  </acceptance_criteria>
  <done>A NEW Somnia-50312 strategist address holds non-empty bytecode with the three immutables wired to the live platform/agent/oracle; the school-leg probe EITHER returned schoolSet==true OR recorded a documented PARTIAL (deploy+immutables+v1 PROVEN, liveness DEFERRED with the real request tx). v1 0xfA428171… still reachable. Real tx hashes captured. No artifact JSON published (Phase 18).</done>
</task>

</tasks>

<verification>
- LIVEDEP-01 SC-1: deploy chain-id == 50312; `cast code <new>` non-empty at a NEW address (≠ v1); immutables
  read back as platform `0x037Bb9…6776`, LLM agent `12847293847561029384`, oracle `0xAcA75144…`.
- LIVEDEP-01 SC-2: one cheap `requestSchoolDecision` probe returned a real validator callback
  (`decisionState(id).schoolSet == true`), OR a documented PARTIAL (deploy+immutables proven; liveness
  deferred with the real request tx hash) — §4 async-tolerance.
- §4 ordering: the FREE surface gate (chain-id 50312 + platform code + v1 code) ran and passed BEFORE the
  FIRST STT spend; the scripted oracle-freshness gate (deliveredAt != 0) passed before the LLM-deposit probe.
- §4 guardrail: v1 `0xfA428171…` still returns non-empty bytecode (reachable, not decommissioned).
- §4 guardrail: no key in any committed file (PK only as `$SOMNIA_TESTNET_PK`, never echoed); the contract's
  `POLYGON_CHAIN_ID = 137` join constant is untouched (distinct from the asserted deploy chain-id 50312).
</verification>

<success_criteria>
- A NEW Somnia-50312 strategist address with non-empty bytecode and correct immutables (cast-verifiable).
- A non-failed `requestSchoolDecision` callback (`schoolSet == true`) — OR a documented PARTIAL — proving
  (or deferring with evidence) the volatile LLM_AGENT_ID + platform before Phase 18 spends more STT.
- v1 `0xfA428171…` reachable; the adapted runner re-points all v1 sigs to the two-leg API and gates spend.
- Real tx hashes captured for the SUMMARY (the published JSON artifact + full decision-moves prove = Phase 18).
</success_criteria>

<output>
After completion, create `.planning/phases/17-live-deploy-pre-flight-surface-verification/17-01-SUMMARY.md`
recording: the NEW strategist address, the three immutable read-backs, the deploy chain-id (50312), the
school-leg decisionId + real tx hash, the `schoolSet==true` confirmation OR the documented PARTIAL (request
tx hash + DEFERRED note), and the v1-reachable re-check. (Do NOT write the `somnia-strategist-deployment.json`
artifact here — that is Phase 18.)
</output>

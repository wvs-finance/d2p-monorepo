---
phase: 07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool
plan: 04
type: execute
wave: 4
depends_on: ["07-02", "07-03"]
files_modified:
  - contracts/test/spec/CcopUsdcPool.tree
  - contracts/test/instrument/helpers/PoolKeyLib.sol
  - contracts/test/instrument/helpers/V4LpHelper.sol
  - contracts/test/instrument/CcopUsdcPool.fork.t.sol
autonomous: true
requirements: [FORK-02]
must_haves:
  truths:
    - "A fork test deploys MockCcop, builds a cCOP/USDC UniV4 PoolKey (currencies ascending, hooks=address(0)) via the shared PoolKeyLib, and calls PoolManager.initialize(key, sqrtPriceX96)"
    - "A consumer reads the initialized state via V4StateReader.getSqrtPriceX96(poolId) > 0 (pool exists/initialized) — NOT v4-periphery's StateView (B-2)"
    - "The decoded (sqrtP/2^96)^2 maps — through the RUNTIME-determined currency ordering (depends on address(mockCcop) < BASE_USDC) — to a human cCOP/USD rate in [3000,5000] (mn-3 round-trip; catches a 1e12 currency-ordering error assertGt(sqrtP,0) misses)"
    - "Full-range liquidity is LP'd via a minimal V4LpHelper (IUnlockCallback → PoolManager.modifyLiquidity) so StateLibrary.getLiquidity(poolId) > 0 is observable; the CONCRETE seeded amount is stated so Plan 05 can size against it (M-1)"
  artifacts:
    - path: "contracts/test/spec/CcopUsdcPool.tree"
      provides: "BTT spec for the pool deploy + state read + round-trip + LP"
      contains: "CcopUsdcPool"
    - path: "contracts/test/instrument/helpers/PoolKeyLib.sol"
      provides: "shared cCOP/USDC PoolKey + sqrtPriceX96 builder (runtime currency ordering) reused by Plan 04 and Plan 05 (mn-C)"
      contains: "function buildCcopUsdcKey"
    - path: "contracts/test/instrument/helpers/V4LpHelper.sol"
      provides: "minimal IUnlockCallback helper minting full-range liquidity via PoolManager.modifyLiquidity (M-1)"
      contains: "unlockCallback"
    - path: "contracts/test/instrument/CcopUsdcPool.fork.t.sol"
      provides: "deploy+initialize cCOP/USDC pool, LP full-range, read sqrtPriceX96/liquidity, assert human-rate round-trip"
      contains: "test_ccopUsdcPool_initialized_state_readable"
  key_links:
    - from: "contracts/test/instrument/CcopUsdcPool.fork.t.sol"
      to: "PoolManager.initialize(key, sqrtPriceX96)"
      via: "IPoolManager.initialize on the fork"
      pattern: "initialize\\("
    - from: "contracts/test/instrument/CcopUsdcPool.fork.t.sol"
      to: "V4StateReader.getSqrtPriceX96(poolId) + StateLibrary.getLiquidity(poolId)"
      via: "consumer-side state read + round-trip rate assertion (B-2 read path)"
      pattern: "getSqrtPriceX96"
    - from: "contracts/test/instrument/helpers/V4LpHelper.sol"
      to: "PoolManager.modifyLiquidity (full-range mint)"
      via: "IUnlockCallback.unlockCallback"
      pattern: "modifyLiquidity"
---

<objective>
Deploy our OWN cCOP/USDC UniV4 pool on the Base fork and prove a consumer can read its initialized state (FORK-02). Behind a committed `.tree` BTT spec: deploy `MockCcop`, build a hookless `PoolKey` via a shared `PoolKeyLib` (mn-C), call `PoolManager.initialize(key, sqrtPriceX96)` encoding cCOP/USD ~ 1/4000, LP full-range liquidity via a minimal `V4LpHelper` (M-1), then read `sqrtPriceX96 > 0` (via `V4StateReader.getSqrtPriceX96`) and `liquidity > 0` (via v4-core `StateLibrary.getLiquidity`) — AND assert the decoded price round-trips (through the runtime currency ordering) to a human cCOP/USD rate in [3000,5000].

Purpose: FORK-02's proof is `V4StateReader.getSqrtPriceX96(poolId) > 0` after our `initialize` = our own cCOP/USDC pool exists. This establishes the read-pool-state-never-derive-it path (07-RESEARCH Pattern 3) that Phase 8's streamia read reuses. B-2: the read path is `V4StateReader.getSqrtPriceX96` / `StateLibrary.getLiquidity` (resolving through the `v4-core/` alias) — NOT v4-periphery's `StateView`/`IStateView`, which is not installed (its imports pull the undefined `@uniswap/v4-core/` alias + permit2). The round-trip assertion (mn-3) catches a 1e12 currency-ordering error that `assertGt(sqrtP,0)` silently misses. M-1: the full-range LP path uses a minimal `V4LpHelper` (`IUnlockCallback` → `PoolManager.modifyLiquidity`) — NOT `PositionManager.modifyLiquidities` (nontrivial Actions-encoding) and NOT `SFPMV4.mintTokenizedPosition` (needs `SFPM.initializeAMMPool`, which has not run at Plan 04 stage). The LP is PRESCRIBED (not optional) and seeded at a stated large amount because Plan 05's mint solvency sizing and Phase 8's streamia both need observable liquidity. evm-tdd Iron Law: `.tree` + failing test before the deploy implementation.
Output: `contracts/test/spec/CcopUsdcPool.tree`, `contracts/test/instrument/helpers/PoolKeyLib.sol`, `contracts/test/instrument/helpers/V4LpHelper.sol`, `contracts/test/instrument/CcopUsdcPool.fork.t.sol`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-RESEARCH.md
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-RESEARCH-DEPLOY.md
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-VALIDATION.md
@contracts/test/fork/BaseForkHarness.t.sol
@contracts/test/mocks/MockCcop.sol
@contracts/test/spec/MacroOracle.tree

<interfaces>
<!-- FORK-02 deploy+read recipe — 07-RESEARCH §"Code Examples" + §2; B-2 read path = V4StateReader/StateLibrary. -->
address constant BASE_POOL_MANAGER = 0x498581fF718922c3f8e6A244956aF099B2652b2b;
address constant BASE_POSITION_MGR = 0x7C5f5A4bBd8fD63184577525326123B519429bDc; // (informational — NOT used for the LP; see V4LpHelper)
address constant BASE_USDC         = 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913; // native USDC (6 dp)
// NO BASE_STATE_VIEW constant — v4-periphery StateView is dropped (B-2).

// MockCcop ccop = new MockCcop();  (18 dp)
// CURRENCY ORDERING IS RUNTIME-DETERMINED (do NOT assume): currencies ASCENDING by address.
// mn-C: build the PoolKey + sqrtPriceX96 via the shared PoolKeyLib so Plan 05 reproduces identical ordering.
(PoolKey memory key, uint160 sqrtPriceX96, bool ccopIsCurrency0) =
    PoolKeyLib.buildCcopUsdcKey(address(ccop), BASE_USDC);  // fee 500, tickSpacing 10, hooks address(0)
// sqrtPriceX96 encodes cCOP/USD ~ 1/4000; price = (sqrtP/2^96)^2 = amount1/amount0 (RAW units).
// USDC 6dp vs cCOP 18dp: bake the decimal scale into the chosen ratio; the round-trip below verifies it.
IPoolManager(BASE_POOL_MANAGER).initialize(key, sqrtPriceX96);

// Read (B-2 — V4StateReader + StateLibrary, both resolve through the v4-core/ alias):
PoolId id = key.toId();
uint160 sqrtP = V4StateReader.getSqrtPriceX96(IPoolManager(BASE_POOL_MANAGER), id);  // borrowed lib (Plan 02)
assertGt(sqrtP, 0);  // initialized
using StateLibrary for IPoolManager;                                                // v4-core/libraries/StateLibrary.sol
uint128 liq = IPoolManager(BASE_POOL_MANAGER).getLiquidity(id);                      // > 0 after the full-range LP
</interfaces>

<interfaces>
<!-- M-1: minimal V4LpHelper — mint full-range liquidity via IUnlockCallback → PoolManager.modifyLiquidity.
     This is the audited test's V4RouterSimple shape; do NOT use PositionManager.modifyLiquidities (Actions
     encoding) nor SFPMV4.mintTokenizedPosition (needs SFPM.initializeAMMPool, not run yet). -->

// Full-range ticks rounded to spacing (use v4-core/v3-core TickMath):
int24 tickLower = (TickMath.MIN_TICK / key.tickSpacing) * key.tickSpacing;
int24 tickUpper = (TickMath.MAX_TICK / key.tickSpacing) * key.tickSpacing;

// Seed amount — STATE this concretely so Plan 05 sizes against it:
//   SEEDED_LIQUIDITY target ~ 1_000_000 ether-scale (the audited test's full-range seed).
//   Fund the helper with BOTH tokens BEFORE unlock: deal(BASE_USDC, helper, <large>); ccop.mint(helper, <large>).
// V4LpHelper implements IUnlockCallback:
//   function unlockCallback(bytes calldata data) external returns (bytes memory) {
//       // decode (key, tickLower, tickUpper, liquidityDelta), call:
//       manager.modifyLiquidity(key, ModifyLiquidityParams({tickLower, tickUpper, liquidityDelta, salt:0}), "");
//       // settle the resulting BalanceDelta (transfer/settle both currencies, take any owed).
//   }
//   function addFullRangeLiquidity(PoolKey key, int128 liquidityDelta) external { manager.unlock(abi.encode(...)); }
</interfaces>

<notes>
- The required test NAME is `test_ccopUsdcPool_initialized_state_readable` (07-VALIDATION FORK-02 `--match-test`). Use this exact name.
- B-2 READ PATH (Blockchain Security Auditor BLOCKER): read pool state via the borrowed `V4StateReader.getSqrtPriceX96(IPoolManager(BASE_POOL_MANAGER), id)` (Plan 02 vendored it) + v4-core `StateLibrary.getLiquidity` (`v4-core/libraries/StateLibrary.sol`, resolves through the DEFINED `v4-core/` alias). Do NOT import `IStateView`/`StateView` from v4-periphery and do NOT reference the `0xA3c0…7A71` StateView address — v4-periphery is not installed (Plan 01) and its imports pull the undefined `@uniswap/v4-core/` alias + permit2. Confirm the exact `V4StateReader.getSqrtPriceX96` arg order against the vendored `panoptic-borrowed/libraries/V4StateReader.sol` in read_first.
- mn-C (shared PoolKey/currency ordering, Blockchain Security Auditor MINOR): "reuse Plan 04's PoolKey/sqrtPriceX96" is infeasible across Solidity files (no cross-file constant import). Extract the PoolKey + sqrtPriceX96 construction into a shared `test/instrument/helpers/PoolKeyLib.sol` library function `buildCcopUsdcKey(address ccop, address usdc) returns (PoolKey, uint160 sqrtPriceX96, bool ccopIsCurrency0)` that determines currency ordering at RUNTIME. Plan 04 AND Plan 05 both call this same fn — guaranteeing identical PoolKey + ordering across the two plans. This file is added to `files_modified`.
- mn-3 ROUND-TRIP ASSERTION (Solidity MINOR): `assertGt(sqrtP, 0)` passes even for a price off by 1e12 (the 6dp/18dp decimal gap). ADD an assertion that decodes `(sqrtP/2^96)^2`, maps it THROUGH THE ACTUAL CURRENCY ORDERING (cCOP may be currency0 OR currency1 depending on `address(mockCcop) < BASE_USDC` — `ccopIsCurrency0` returned by PoolKeyLib), un-scales the 6dp/18dp decimals, and asserts the resulting human cCOP/USD rate is in [3000,5000].
- M-1 FULL-RANGE LP via V4LpHelper (Reality-Checker + Blockchain Security Auditor MAJOR): the previous "PositionManager.modifyLiquidities or SFPMV4.mintTokenizedPosition" path is INFEASIBLE at this stage (Actions-encoding / SFPM.initializeAMMPool not run). Write a minimal `test/instrument/helpers/V4LpHelper.sol` implementing `IUnlockCallback.unlockCallback` that calls `IPoolManager.modifyLiquidity` to mint FULL-RANGE liquidity (ticks rounded to spacing per the M-1 interfaces block). Fund the helper with both tokens (`deal()` USDC + `ccop.mint()`) BEFORE `unlock`. STATE the concrete seeded amount (target ~`1_000_000 ether`-scale) in a comment + the SUMMARY so Plan 05 sizes its small position against it. This file is added to `files_modified` (and noted as the §F LP-helper extension). Alternatively vendor `V4RouterSimple` from the audit test utils — if so, add it to §F and note the provenance.
- Extend the Plan-03 fork pattern: same `vm.createSelectFork(vm.rpcUrl("base"), BASE_FORK_BLOCK)` (the M-4 Solidity constant), same Base address constants. Reuse the harness's pinned block.
- BULLOAK/GIT ORDERING (mn-B): commit the `.tree` FIRST (separate commit) before `bulloak scaffold`; do NOT rename scaffolded functions before `bulloak check`.
- The executor MUST `export BASE_RPC_URL` (from `.env`) before the fork test — `--fork-url "$BASE_RPC_URL"` (07-VALIDATION verbatim) expands to that value.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPECIFY — CcopUsdcPool.tree (commit FIRST) + scaffolded FAILING fork test</name>
  <files>contracts/test/spec/CcopUsdcPool.tree, contracts/test/instrument/CcopUsdcPool.fork.t.sol</files>
  <read_first>
    - contracts/test/spec/MacroOracle.tree (BTT `.tree` format template)
    - 07-RESEARCH.md §2 + §"Code Examples" (the deploy/initialize/read recipe + the decimal-gap note)
    - 07-VALIDATION.md FORK-02 row (exact command + the required test name `test_ccopUsdcPool_initialized_state_readable`)
    - contracts/test/fork/BaseForkHarness.t.sol (the fork-select + Base-address-constant pattern + the Solidity-constant BASE_FORK_BLOCK to reuse)
    - contracts/test/mocks/MockCcop.sol (the mock token to deploy + mint)
  </read_first>
  <action>
    Create `contracts/test/spec/CcopUsdcPool.tree` (one behavioral unit), e.g.:
    ```
    CcopUsdcPool::initializeAndReadState
    ├── given MockCcop deployed and a hookless cCOP/USDC PoolKey (currencies ascending by runtime address via PoolKeyLib, fee 500, tickSpacing 10)
    │   ├── when PoolManager.initialize(key, sqrtPriceX96) is called with sqrtPriceX96 encoding cCOP/USD ~ 1/4000
    │   │   └── it should not revert (the pool is created on the fork).
    │   └── given the pool is initialized
    │       ├── it should read sqrtPriceX96 > 0 via V4StateReader.getSqrtPriceX96(poolId).
    │       └── it should round-trip (sqrtP/2^96)^2 — through the runtime currency ordering + 6dp/18dp scale — to a human cCOP/USD rate in [3000,5000].
    └── given full-range liquidity is LP'd via V4LpHelper
        └── it should read liquidity > 0 via StateLibrary.getLiquidity(poolId).
    ```
    BULLOAK/GIT ORDERING (mn-B): COMMIT the `.tree` FIRST (separate commit). THEN `bulloak scaffold` it into `contracts/test/instrument/CcopUsdcPool.fork.t.sol` WITHOUT renaming scaffolded functions (`// SPDX-License-Identifier: MIT`, `pragma ^0.8.24;`, `import {Test} from "forge-std/Test.sol";`, `@dev BTT spec: test/spec/CcopUsdcPool.tree` tag). The primary happy-path test MUST be named `test_ccopUsdcPool_initialized_state_readable`. Pin the Base address constants verbatim (NO StateView — B-2) and reuse the harness's `BASE_FORK_BLOCK` constant. At SPECIFY the bodies are stubs that FAIL (no `initialize` yet). `bulloak check` must pass on the scaffolded (un-renamed) file.
  </action>
  <verify>
    <automated>cd contracts && test -f test/spec/CcopUsdcPool.tree && test -f test/instrument/CcopUsdcPool.fork.t.sol && bulloak check test/spec/CcopUsdcPool.tree && bulloak check test/spec/*.tree && grep -q "test_ccopUsdcPool_initialized_state_readable" test/instrument/CcopUsdcPool.fork.t.sol && grep -q "BTT spec: test/spec/CcopUsdcPool.tree" test/instrument/CcopUsdcPool.fork.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `test -f contracts/test/spec/CcopUsdcPool.tree` succeeds and contains `CcopUsdcPool::`
    - `bulloak check test/spec/CcopUsdcPool.tree` exits 0 (names NOT renamed — mn-B)
    - `bulloak check test/spec/*.tree` exits 0 (full-glob phase-gate — covers MacroOracle + BaseForkHarness trees too)
    - `grep -q "test_ccopUsdcPool_initialized_state_readable" contracts/test/instrument/CcopUsdcPool.fork.t.sol` succeeds (the exact required test name)
    - `grep -q "BTT spec: test/spec/CcopUsdcPool.tree" contracts/test/instrument/CcopUsdcPool.fork.t.sol` succeeds
    - `.tree` committed FIRST in a separate commit before filled bodies (mn-B / git history)
  </acceptance_criteria>
  <done>`CcopUsdcPool.tree` is committed FIRST (mn-B) and passes per-file + full-glob `bulloak check`; the scaffolded fork test carries the spec tag, the required test name, the Base address constants (no StateView — B-2), and branches for the round-trip rate + the V4LpHelper full-range LP; function names un-renamed; bodies are failing stubs — spec precedes impl.</done>
</task>

<task type="auto">
  <name>Task 2: IMPLEMENT — PoolKeyLib + V4LpHelper + deploy/initialize/LP/read/round-trip, green</name>
  <files>contracts/test/instrument/helpers/PoolKeyLib.sol, contracts/test/instrument/helpers/V4LpHelper.sol, contracts/test/instrument/CcopUsdcPool.fork.t.sol</files>
  <read_first>
    - contracts/test/instrument/CcopUsdcPool.fork.t.sol (the stub from Task 1)
    - contracts/test/spec/CcopUsdcPool.tree (the spec the bodies satisfy)
    - 07-RESEARCH.md §"Code Examples" (FORK-02) (the full PoolKey + initialize code; the decimal-note on the raw-unit ratio)
    - 07-RESEARCH.md §"Open Questions" #1 and #2 (sqrtPriceX96 literal computation; the LP path for liquidity > 0)
    - 07-RESEARCH-DEPLOY.md §D solvency-sizing note (why the seeded LP matters for Plan 05's mint) + §F (V4StateReader is the borrowed read lib)
    - contracts/panoptic-borrowed/libraries/V4StateReader.sol (confirm the exact `getSqrtPriceX96(IPoolManager, PoolId)` arg order — B-2 read path)
    - lib/v4-core (the real `IPoolManager`, `PoolKey`, `Currency`, `PoolId`, `IHooks`, `IUnlockCallback`, `ModifyLiquidityParams`, `BalanceDelta`, `StateLibrary`, `TickMath` symbol paths under the `v4-core/` alias — verify against the installed lib) and lib/v3-core (TickMath if needed)
  </read_first>
  <action>
    Create the two shared helpers, then fill the test bodies (do NOT rename functions — mn-B).

    A) `contracts/test/instrument/helpers/PoolKeyLib.sol` (mn-C — shared, reused by Plan 05): a library with
       `function buildCcopUsdcKey(address ccop, address usdc) internal pure returns (PoolKey memory key, uint160 sqrtPriceX96, bool ccopIsCurrency0)`:
       - Determine ordering at RUNTIME: `ccopIsCurrency0 = ccop < usdc;` order `(c0,c1)` ascending.
       - Build `PoolKey({currency0: c0, currency1: c1, fee: 500, tickSpacing: 10, hooks: IHooks(address(0))})`.
       - Compute `sqrtPriceX96` for cCOP/USD ~ 1/4000 baking 6dp USDC vs 18dp cCOP into the RAW-unit ratio (price = amount1/amount0), branching on `ccopIsCurrency0`. Document the chosen literal + human rate in a comment.

    B) `contracts/test/instrument/helpers/V4LpHelper.sol` (M-1 — minimal full-range LP): implement `IUnlockCallback`:
       - `function addFullRangeLiquidity(PoolKey memory key, int128 liquidityDelta) external` → `manager.unlock(abi.encode(key, tickLower, tickUpper, liquidityDelta))`.
       - `function unlockCallback(bytes calldata data) external returns (bytes memory)`: decode, call `manager.modifyLiquidity(key, ModifyLiquidityParams({tickLower, tickUpper, liquidityDelta, salt: bytes32(0)}), "")`, then SETTLE the returned `BalanceDelta` (sync/settle both currencies owed, take any credit). Full-range ticks: `tickLower = (TickMath.MIN_TICK / key.tickSpacing) * key.tickSpacing; tickUpper = (TickMath.MAX_TICK / key.tickSpacing) * key.tickSpacing;`. Confirm `IPoolManager`/`ModifyLiquidityParams`/`BalanceDelta`/`IUnlockCallback` symbol paths against `lib/v4-core` in read_first. (Or vendor the audit's `V4RouterSimple` and note it in §F.)

    C) Fill `CcopUsdcPool.fork.t.sol`:
    - `vm.createSelectFork(vm.rpcUrl("base"), BASE_FORK_BLOCK);` (reuse the harness's Solidity-constant block).
    - `MockCcop ccop = new MockCcop();`
    - `(PoolKey memory key, uint160 sqrtPriceX96, bool ccopIsCurrency0) = PoolKeyLib.buildCcopUsdcKey(address(ccop), BASE_USDC);`
    - `IPoolManager(BASE_POOL_MANAGER).initialize(key, sqrtPriceX96);`
    - Read (B-2): `PoolId id = key.toId(); uint160 sqrtP = V4StateReader.getSqrtPriceX96(IPoolManager(BASE_POOL_MANAGER), id); assertGt(sqrtP, 0);`
    - ROUND-TRIP (mn-3): decode `(sqrtP/2^96)^2`, branch on `ccopIsCurrency0` to convert to a human cCOP/USD rate, un-scale the 6dp/18dp decimals, and `assertGe(rate, 3000); assertLe(rate, 5000);`.
    - FULL-RANGE LP (M-1, PRESCRIBED): deploy `V4LpHelper`, fund it with both tokens (`deal(BASE_USDC, address(lp), AMT); ccop.mint(address(lp), AMT);` with `AMT` a stated large value, e.g. `1_000_000 ether`-scale), then `lp.addFullRangeLiquidity(key, int128(SEEDED_LIQUIDITY));`. Read `using StateLibrary for IPoolManager; uint128 liq = IPoolManager(BASE_POOL_MANAGER).getLiquidity(id); assertGt(liq, 0);`. STATE the concrete `SEEDED_LIQUIDITY` / funded amount in a comment + the SUMMARY (Plan 05 sizes its mint against this).
    Make `test_ccopUsdcPool_initialized_state_readable` green. Record the `sqrtPriceX96` literal + human rate + the seeded liquidity amount in the SUMMARY.
  </action>
  <verify>
    <automated>cd contracts && forge build && forge test --match-test test_ccopUsdcPool_initialized_state_readable --fork-url "$BASE_RPC_URL" && bulloak check test/spec/CcopUsdcPool.tree && grep -q "getLiquidity" test/instrument/CcopUsdcPool.fork.t.sol && grep -q "getSqrtPriceX96" test/instrument/CcopUsdcPool.fork.t.sol && test -f test/instrument/helpers/V4LpHelper.sol && test -f test/instrument/helpers/PoolKeyLib.sol && grep -q "modifyLiquidity" test/instrument/helpers/V4LpHelper.sol && ! grep -rq "StateView" test/instrument/CcopUsdcPool.fork.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-test test_ccopUsdcPool_initialized_state_readable --fork-url "$BASE_RPC_URL"` exits 0 (FORK-02 proof, command verbatim from 07-VALIDATION)
    - `grep -q "initialize(" contracts/test/instrument/CcopUsdcPool.fork.t.sol` and `grep -q "getSqrtPriceX96" ...` succeed (deploy + B-2 read path present — V4StateReader, not StateView)
    - `! grep -rq "StateView" contracts/test/instrument/CcopUsdcPool.fork.t.sol` succeeds (B-2: no v4-periphery StateView)
    - `grep -q "assertGt" contracts/test/instrument/CcopUsdcPool.fork.t.sol` succeeds (sqrtPriceX96 > 0 asserted)
    - `grep -qE "assert(Ge|Le|GtDecimal|ApproxEqAbs)" contracts/test/instrument/CcopUsdcPool.fork.t.sol` succeeds AND a `3000`/`5000` (or equivalent band) literal present — the mn-3 human-rate round-trip band assertion
    - `grep -q "getLiquidity" contracts/test/instrument/CcopUsdcPool.fork.t.sol` succeeds AND the test asserts liquidity > 0 (the prescribed full-range LP via V4LpHelper — Plan 05 / Phase 8 dependency)
    - `test -f contracts/test/instrument/helpers/V4LpHelper.sol` AND `grep -q "modifyLiquidity" ...` AND `grep -q "unlockCallback" ...` succeed (M-1 minimal LP helper present)
    - `test -f contracts/test/instrument/helpers/PoolKeyLib.sol` AND `grep -q "function buildCcopUsdcKey" ...` succeed (mn-C shared builder present)
    - `grep -qE "ccopIsCurrency0|address\(ccop\) *< *BASE_USDC|ccop *< *usdc" contracts/test/instrument/helpers/PoolKeyLib.sol` succeeds (currency ordering determined at runtime, not assumed)
    - `bulloak check test/spec/CcopUsdcPool.tree` exits 0
    - The chosen `sqrtPriceX96` literal + human rate + the concrete seeded liquidity amount recorded in the SUMMARY (M-1 — Plan 05 sizes against it)
  </acceptance_criteria>
  <done>The fork test deploys MockCcop, builds the PoolKey via the shared PoolKeyLib (runtime ordering), initializes a hookless cCOP/USDC UniV4 pool at ~1/4000, LPs full-range liquidity via the minimal V4LpHelper (IUnlockCallback → modifyLiquidity, seeded at a stated large amount), and a consumer reads `sqrtPriceX96 > 0` (V4StateReader) + `liquidity > 0` (StateLibrary) AND asserts the price round-trips (through the runtime ordering) to a human rate in [3000,5000]; no StateView (B-2); `test_ccopUsdcPool_initialized_state_readable` passes against the Base fork; `bulloak check` clean.</done>
</task>

</tasks>

<verification>
- `forge test --match-test test_ccopUsdcPool_initialized_state_readable --fork-url "$BASE_RPC_URL"` green (FORK-02 proof). Executor MUST `export BASE_RPC_URL` first.
- Read path is `V4StateReader.getSqrtPriceX96` + `StateLibrary.getLiquidity` (B-2) — NO v4-periphery StateView.
- The round-trip rate band [3000,5000] is asserted (mn-3) — not just `assertGt(sqrtP,0)`.
- Full-range liquidity is LP'd via the minimal `V4LpHelper` (M-1) and `getLiquidity(id) > 0` asserted; the seeded amount is stated for Plan 05.
- `PoolKeyLib.buildCcopUsdcKey` is the shared PoolKey/ordering builder (mn-C) reused by Plan 05.
- `bulloak check test/spec/CcopUsdcPool.tree` clean (`.tree` preceded impl in a separate commit — mn-B).
- `forge build` green (single cancun profile unaffected).
</verification>

<success_criteria>
- `CcopUsdcPool.tree` committed FIRST before its implementation (Iron Law + mn-B).
- A consumer reads `sqrtPriceX96 > 0` (via `V4StateReader.getSqrtPriceX96`) from our own freshly-initialized cCOP/USDC UniV4 pool on the Base fork — NO v4-periphery StateView (B-2).
- The price round-trips through the RUNTIME currency ordering + 6dp/18dp scale to a human rate in [3000,5000] (mn-3).
- Full-range liquidity LP'd via the minimal `V4LpHelper` (M-1); `getLiquidity > 0` asserted; the concrete seeded amount documented for Plan 05 solvency sizing + Phase 8 streamia.
- `PoolKeyLib` (mn-C) shares the PoolKey + currency-ordering construction with Plan 05.
- The `sqrtPriceX96` literal + human rate + LP seeded amount documented.
</success_criteria>

<output>
After completion, create `.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-04-SUMMARY.md`
</output>

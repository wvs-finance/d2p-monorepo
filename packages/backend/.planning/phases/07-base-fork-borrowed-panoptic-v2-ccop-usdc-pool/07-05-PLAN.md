---
phase: 07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool
plan: 05
type: execute
wave: 5
depends_on: ["07-02", "07-04"]
files_modified:
  - contracts/test/spec/PanopticDataSeam.tree
  - contracts/test/instrument/helpers/PanopticV2DeployHelper.sol
  - contracts/test/instrument/PanopticDataSeam.fork.t.sol
autonomous: true
requirements: [FORK-03]
must_haves:
  truths:
    - "Compile-time truth: `forge build` exit 0 — IPanopticData compiles against the borrowed V2 types and the helper's returned `address` is consumed as `IPanopticData` (the real compile-time proof; an unchecked cast proves nothing)"
    - "The deploy helper builds a REAL pool via the factory choreography (07-RESEARCH-DEPLOY §B+§D): master copies + RiskEngine + PanopticFactoryV4 with the §B constructor signatures, deploys its OWN MockCcop, builds a FRESH PoolKey via the shared PoolKeyLib (mn-C), PoolManager.initialize, seeds full-range LP via V4LpHelper, then factory.deployNewPool — NOT `new PanopticPool(...)`"
    - "Runtime truth: the fork test mints ONE position via dispatch (PositionBalance==0 ⇒ mint), reads PositionBalance[] length 1 via getAccumulatedFeesAndPositionsData, then dispatches size 0 (≠ stored ⇒ burn)"
    - "ct0/ct1 are typed as IERC4626 (from @openzeppelin/contracts/interfaces/IERC4626.sol — resolves through the OZ-into-v4-core remapping) so the seam test calls .deposit() WITHOUT importing the concrete CollateralTracker (B-1 seam-guard)"
    - "The seam test imports NO panoptic-borrowed file AND no PanopticV2DeployHelper — it touches Panoptic only through IPanopticData (no transitive coupling); BOTH grep guards hold"
  artifacts:
    - path: "contracts/test/spec/PanopticDataSeam.tree"
      provides: "BTT spec for the mint/burn-through-interface seam"
      contains: "PanopticDataSeam"
    - path: "contracts/test/instrument/helpers/PanopticV2DeployHelper.sol"
      provides: "factory-choreography deploy (own MockCcop + PoolKeyLib + V4LpHelper → PoolManager.initialize → seed LP → deployNewPool); returns deployed PanopticPool address + ct0/ct1 + token addresses"
      contains: "deployNewPool"
    - path: "contracts/test/instrument/PanopticDataSeam.fork.t.sol"
      provides: "mint+burn ONE position via IPanopticData only; ct0/ct1 deposited via IERC4626"
      contains: "test_mintBurn_single_position_through_IPanopticData"
  key_links:
    - from: "contracts/test/instrument/helpers/PanopticV2DeployHelper.sol"
      to: "PanopticFactoryV4.deployNewPool(poolKey, riskEngine, salt)"
      via: "factory clone+init choreography (§B/§D)"
      pattern: "deployNewPool\\("
    - from: "contracts/test/instrument/PanopticDataSeam.fork.t.sol"
      to: "IPanopticData.dispatch"
      via: "mint/burn through the interface"
      pattern: "\\.dispatch\\("
    - from: "contracts/test/instrument/PanopticDataSeam.fork.t.sol"
      to: "IERC4626.deposit (ct0/ct1)"
      via: "collateral deposit via IERC4626 — no concrete CollateralTracker import"
      pattern: "IERC4626"
---

<objective>
Prove the FORK-03 swap seam against the REAL deploy mechanics: the borrowed Panoptic V2 contracts are ClonesWithImmutableArgs proxies, so a working pool comes ONLY from `PanopticFactoryV4.deployNewPool` (07-RESEARCH-DEPLOY §A). A SEPARATE deploy helper runs the factory choreography (§B+§D) — deploying its OWN `MockCcop`, building a FRESH `PoolKey` via the shared `PoolKeyLib` (mn-C), initializing the UniV4 pool, seeding full-range LP via `V4LpHelper`, then `factory.deployNewPool` — and returns the deployed pool as an `address` plus `ct0`/`ct1` (typed `IERC4626`) and the token addresses. The seam test holds only an `IPanopticData` + `IERC4626` collateral handles, mints and burns ONE position via `dispatch`, and reads it back via `getAccumulatedFeesAndPositionsData` — never importing the borrowed concrete nor the helper. Behind a committed `.tree` BTT spec.

Purpose: FORK-03's proof has four parts. COMPILE-TIME: `forge build` exit 0 with the helper's returned `address` consumed as `IPanopticData` (an `IPanopticData(addr)` cast alone proves nothing — §B-3). RUNTIME: (a) one position minted via `dispatch` (PositionBalance==0 ⇒ mint) → `getAccumulatedFeesAndPositionsData` returns a non-reverting `PositionBalance[]` of length 1; (b) `dispatch` with size 0 (≠ stored) burns it. SEAM: BOTH static greps prove NO `panoptic-borrowed` AND no `PanopticV2DeployHelper` import in the seam test → the consumer depends on the interface only, no transitive coupling. B-1: the seam test deposits collateral via `ct0`/`ct1` typed as `IERC4626` (the OZ interface, which HAS `deposit(uint256,address)`) — NOT the concrete `CollateralTracker` (whose import would pull `panoptic-borrowed/` and VIOLATE the seam guard) and NOT `IERC20Partial` (which has no `deposit()`). This is the keystone that lets a future canonical V2 deployment drop in by repointing `IPanopticData`.
Output: `contracts/test/spec/PanopticDataSeam.tree`, `contracts/test/instrument/helpers/PanopticV2DeployHelper.sol`, `contracts/test/instrument/PanopticDataSeam.fork.t.sol`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-RESEARCH-DEPLOY.md
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-RESEARCH.md
@.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-VALIDATION.md
@contracts/src/instrument/interfaces/IPanopticData.sol
@contracts/test/instrument/CcopUsdcPool.fork.t.sol
@contracts/test/instrument/helpers/PoolKeyLib.sol
@contracts/test/instrument/helpers/V4LpHelper.sol
@contracts/test/spec/MacroOracle.tree

<interfaces>
<!-- 07-RESEARCH-DEPLOY §A.4 + §B — the REAL factory-choreography deploy path, lifted from the audited
     V4 test _deployPanopticPool()/setUp(). The contracts are CWIA proxies; deploy ONLY via the factory.
     Quote the §B constructor signatures VERBATIM. mn-C: the helper builds its OWN MockCcop + a FRESH
     PoolKey via PoolKeyLib (NO cross-file constant import from Plan 04). -->

// ---- ONE-TIME INFRASTRUCTURE (in the helper) ----
IPoolManager manager = IPoolManager(BASE_POOL_MANAGER);                 // live Base PoolManager on the fork (0x498581...2b2b)
SemiFungiblePositionManager sfpm =
    new SemiFungiblePositionManager(manager, 10**13, 10**13, 0);        // §B: (IPoolManager, uint256 _minEnforcedTickFillCost, uint256 _nativeEnforcedTickFillCost, uint256 _supplyMultiplierTickFill)

// master-copy implementations the factory clones:
address poolReference       = address(new PanopticPool(ISemiFungiblePositionManager(address(sfpm)))); // §B: (ISemiFungiblePositionManager)
address collateralReference = address(new CollateralTracker(10));                                      // §B: (uint256 _commissionFee)

PanopticFactory factory = new PanopticFactory(                          // §B: 7 args
    sfpm, manager, poolReference, collateralReference,
    new bytes32[](0), new uint256[][](0), new Pointer[][](0)            // empty metadata arrays OK for demo
);
RiskEngine re = new RiskEngine(10_000_000, 10_000_000, address(0), address(0)); // §B: (uint256, uint256, address guardian, address builderFactory) — 0/0 OK for demo

// ---- PER-POOL (helper builds its OWN pool — mn-C) ----
MockCcop ccop = new MockCcop();
(PoolKey memory poolKey, uint160 sqrtPriceX96, bool ccopIsCurrency0) =
    PoolKeyLib.buildCcopUsdcKey(address(ccop), BASE_USDC);             // SAME builder Plan 04 uses ⇒ identical ordering
manager.initialize(poolKey, sqrtPriceX96);                             // REQUIRED first or deployNewPool reverts PoolNotInitialized
// seed full-range UniV4 liquidity via V4LpHelper (Plan 04's helper) so the mint clears solvency:
V4LpHelper lp = new V4LpHelper(manager);
deal(BASE_USDC, address(lp), AMT); ccop.mint(address(lp), AMT);        // AMT = the stated Plan-04-scale seed
lp.addFullRangeLiquidity(poolKey, int128(SEEDED_LIQUIDITY));
// factory does SFPM-init + clone2 both CTs + clone3 the pool + initialize all three:
PanopticPool pp = factory.deployNewPool(poolKey, re, uint96(block.timestamp /* salt */));
// vegoid is a RiskEngine CONSTANT (=4) per §G — never a per-pool param.

// helper RETURNS to the seam test (all as seam-safe types — B-1):
//   address pool;                 // consumed as IPanopticData
//   IERC4626 ct0; IERC4626 ct1;   // pp.collateralToken0()/collateralToken1() handed back as IERC4626
//   address token0; address token1; address ccop;
</interfaces>

<interfaces>
<!-- 07-RESEARCH-DEPLOY §D — the mint-ONE-position init + collateral call sequence (VERBATIM, ordered).
     The seam test consumes the helper's returned address ONLY as IPanopticData; ct0/ct1 as IERC4626 (B-1). -->

import {IERC4626} from "@openzeppelin/contracts/interfaces/IERC4626.sol";  // B-1: resolves through the OZ-into-v4-core remapping; HAS deposit(uint256,address) — no panoptic-borrowed import
IPanopticData pano = IPanopticData(deployedPoolAddress);               // helper-returned address; assignment + use is the compile-time proof

// 1. FUND + APPROVE COLLATERAL (per actor)
deal(BASE_USDC, actor, type(uint104).max);                            // USDC 6dp on Base
ccop.mint(actor, type(uint104).max);                                  // MockCcop 18dp (Plan 02)
vm.startPrank(actor);
IERC20Partial(token0).approve(address(pano), type(uint256).max);
IERC20Partial(token1).approve(address(pano), type(uint256).max);
IERC20Partial(token0).approve(address(ct0),  type(uint256).max);      // ct0 = IERC4626 collateral (B-1)
IERC20Partial(token1).approve(address(ct1),  type(uint256).max);      // ct1 = IERC4626 collateral (B-1)

// 2. DEPOSIT COLLATERAL (ERC-4626; deposit(uint256 assets, address receiver); assets <= uint104) — via IERC4626 (B-1)
ct0.deposit(type(uint104).max, actor);
ct1.deposit(type(uint104).max, actor);

// 3. MINT ONE POSITION via dispatch (PanopticPool.dispatch @L572)
//    M-2: build a concrete one-leg short TokenId aligned to tickSpacing (confirm addLeg arg order in read_first):
int24 strike = int24((pano.getCurrentTick() / poolKey.tickSpacing) * poolKey.tickSpacing); // aligned to tickSpacing
TokenId built = TokenId.wrap(0)
    .addPoolId(pano.poolId())                 // OR the poolId surfaced by the helper if not on IPanopticData
    .addLeg(0 /*legIndex*/, 1 /*optionRatio*/, 0 /*asset*/, 0 /*isLong=short*/, 0 /*tokenType*/, 0 /*riskPartner*/, strike, 1 /*width=1 tickSpacing*/);
TokenId[]  memory mintList = new TokenId[](1); mintList[0] = built;
TokenId[]  memory finalIds = new TokenId[](1); finalIds[0] = built;
uint128[]  memory sizes    = new uint128[](1); sizes[0]    = positionSize;   // small vs seeded LP (solvency)
int24[3][] memory limits   = new int24[3][](1);
limits[0][0] = tickLimitLow; limits[0][1] = tickLimitHigh; limits[0][2] = int24(uint24(effectiveLiquidityLimitX32));
pano.dispatch(mintList, finalIds, sizes, limits, false, 0);

// 4. READ PREMIUM (never derive; PanopticPool.getAccumulatedFeesAndPositionsData @L221)
(,, PositionBalance[] memory bals) = pano.getAccumulatedFeesAndPositionsData(actor, true, finalIds);
// assert bals.length == 1

// 5. BURN (size 0 != stored ⇒ burn path). FIX the array literal — NO inline [0]:
uint128[] memory burnSizes = new uint128[](1); burnSizes[0] = 0;
TokenId[] memory emptyFinal = new TokenId[](0);
pano.dispatch(mintList, emptyFinal, burnSizes, limits, false, 0);
</interfaces>

<notes>
- 07-RESEARCH-DEPLOY §A/§B/§D SUPERSEDE the old plan's `new PanopticPool(...)` direct-deploy and its "verify signatures at execute time" hand-wave. The constructor signatures (§B) and the call sequence (§D) are PINNED — lift them verbatim, no re-discovery.
- B-1 (Blockchain Security Auditor BLOCKER — the seam-guard trap): the §D sequence calls `ct0.deposit(...)`/`ct1.deposit(...)`, but `IERC20Partial` has NO `deposit()`. Do NOT reach for the concrete `CollateralTracker` to get `.deposit()` — importing it pulls `panoptic-borrowed/` and VIOLATES the seam guard. Instead type ct0/ct1 as `IERC4626` (`import {IERC4626} from "@openzeppelin/contracts/interfaces/IERC4626.sol";` — resolves through the OZ-into-v4-core remapping configured in Plan 01; NO panoptic-borrowed import). The helper hands ct0/ct1 back AS `IERC4626` (or as plain `address` the seam test wraps `IERC4626(addr)`). Acceptance adds `grep -q "IERC4626" test/instrument/PanopticDataSeam.fork.t.sol`. KEEP both existing seam guards (`! grep import.*panoptic-borrowed`, `! grep import.*PanopticV2DeployHelper`).
- THE FORK-03 STATIC GUARD (three greps now — KEEP all): the seam test must NOT import anything matching `panoptic-borrowed`, must NOT import `PanopticV2DeployHelper`, AND must use `IERC4626` for the collateral deposit (B-1). 07-VALIDATION commands: `! grep -E "import.*panoptic-borrowed" ...` AND `! grep -E "import.*PanopticV2DeployHelper" ...` AND `grep -q "IERC4626" ...`.
- §M-3 recommended PRIMARY seam-clean path (KEEP): a setUp/base contract calls the helper and hands the seam test back ONLY an `address` typed to `IPanopticData`, the ct0/ct1 as `IERC4626`, and the token/ccop addresses as plain `address`. The seam test file imports neither the helper nor any panoptic-borrowed file. (The helper file DOES import the concretes + factory + PoolKeyLib + V4LpHelper — that is correct/intended for the deploy seam.)
- mn-C (shared PoolKey/ordering): "reuse Plan 04's PoolKey/sqrtPriceX96" is infeasible across Solidity files. The helper deploys its OWN `MockCcop`, determines runtime currency ordering, and builds a FRESH PoolKey + sqrtPriceX96 by calling the SAME `PoolKeyLib.buildCcopUsdcKey(...)` Plan 04 uses — guaranteeing identical ordering between the two plans. NO cross-file constant import.
- M-2 (Blockchain Security Auditor MAJOR — TokenId construction): an unspecified TokenId makes `dispatch` revert. Build a concrete one-leg short TokenId via the GPL `@types/TokenId.sol` builder (a value type — importable into the helper/setUp without coupling; the SEAM test obtains `built` from setUp/base, not by importing the concrete). Use the snippet in the second `<interfaces>` block: a short (`isLong=0`), `optionRatio=1`, `width=1`-tickSpacing leg with `strike` aligned to `tickSpacing` at the current tick. CONFIRM the exact `addLeg` arg order against the vendored `panoptic-borrowed/types/TokenId.sol` (the `TokenIdLibrary` at the pinned commit) in read_first — adjust if it differs. A short 1-tickSpacing-width leg at the current tick always has counterparty liquidity from the seeded full-range LP, so `_validateSolvency` passes given the uint104-max collateral.
- The burn array-literal fix (MINOR mn-4 / RC MINOR): use `uint128[] memory burnSizes = new uint128[](1); burnSizes[0]=0;` — NOT the inline `[0]` (Solidity has no `uint128[]` literal coercion there).
- `vegoid` is a RiskEngine CONSTANT (=4) per §G — do NOT treat it as a per-pool param anywhere in the helper or test.
- Solvency sizing (§D): deposit `type(uint104).max` collateral and choose `positionSize` small relative to the seeded UniV4 liquidity (Plan 04's stated full-range seed) so `_validateSolvency` clears on the first try.
- The required test NAME is `test_mintBurn_single_position_through_IPanopticData` (07-VALIDATION FORK-03 `--match-test`). Use it exactly.
- Streamia is READ, never re-derived (07-RESEARCH Pattern 3) — premium values come from `getAccumulatedFeesAndPositionsData`, never a local formula.
- BULLOAK/GIT ORDERING (mn-B): commit the `.tree` FIRST (separate commit) before `bulloak scaffold`; do NOT rename scaffolded functions before `bulloak check`.
- This is the hardest FORK plan. evm-tdd Iron Law: 3 tasks — Task 1 SPECIFY (`.tree` + FAILING seam test), Task 2 factory-deploy helper IMPLEMENT, Task 3 seam mint/burn IMPLEMENT. Keep mint+burn to ONE position.
- Executor MUST `export BASE_RPC_URL` (from `.env`) before fork tests; the `--fork-url "$BASE_RPC_URL"` form expands to that value (07-VALIDATION standardized).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPECIFY — PanopticDataSeam.tree (commit FIRST) + scaffolded FAILING fork test (interface-only)</name>
  <files>contracts/test/spec/PanopticDataSeam.tree, contracts/test/instrument/PanopticDataSeam.fork.t.sol</files>
  <read_first>
    - contracts/test/spec/MacroOracle.tree (BTT `.tree` format)
    - 07-RESEARCH-DEPLOY.md §D (the dispatch mint→read→burn sequence the spec branches encode) + §A.4 (the factory deploy the helper provides)
    - 07-VALIDATION.md FORK-03 rows (the commands: `--match-test test_mintBurn_single_position_through_IPanopticData --fork-url "$BASE_RPC_URL"`; `! grep -E "import.*panoptic-borrowed" ...`; `! grep -E "import.*PanopticV2DeployHelper" ...`)
    - contracts/src/instrument/interfaces/IPanopticData.sol (the interface the test depends on exclusively)
  </read_first>
  <action>
    Create `contracts/test/spec/PanopticDataSeam.tree` (one behavioral unit), e.g.:
    ```
    PanopticDataSeam::mintBurnThroughInterface
    ├── given a borrowed Panoptic pool deployed via the factory choreography, referenced only as IPanopticData, with collateral via IERC4626
    │   └── it should compile with the helper-returned address consumed as IPanopticData (forge build green — the compile-time proof).
    ├── when one position is minted via IPanopticData.dispatch (stored balance 0 ⇒ mint)
    │   ├── it should not revert.
    │   └── it should read PositionBalance[] of length 1 via getAccumulatedFeesAndPositionsData.
    └── when the same position is dispatched with size 0 (≠ stored ⇒ burn)
        └── it should close the position (subsequent read reflects the burn).
    ```
    BULLOAK/GIT ORDERING (mn-B): COMMIT the `.tree` FIRST (separate commit). THEN `bulloak scaffold` it into `contracts/test/instrument/PanopticDataSeam.fork.t.sol` WITHOUT renaming scaffolded functions. The happy-path test MUST be named `test_mintBurn_single_position_through_IPanopticData`. CRITICAL (B-1 + seam guards): the seam test imports ONLY `forge-std/Test.sol`, `IPanopticData` (from `src/instrument/interfaces/`), `IERC4626` (from `@openzeppelin/contracts/interfaces/IERC4626.sol` — for the ct0/ct1 collateral deposit, B-1), `IERC20Partial`/`@types/` value types — NEVER anything matching `import.*panoptic-borrowed` AND NEVER `PanopticV2DeployHelper`. The borrowed concrete is deployed in the Task-2 helper (outside this file); the seam test references the deployed address only as an `IPanopticData` and ct0/ct1 only as `IERC4626`. Add the `@dev BTT spec:` tag. Bodies are failing stubs. `bulloak check` must pass on the scaffolded (un-renamed) file.
  </action>
  <verify>
    <automated>cd contracts && test -f test/spec/PanopticDataSeam.tree && test -f test/instrument/PanopticDataSeam.fork.t.sol && bulloak check test/spec/PanopticDataSeam.tree && bulloak check test/spec/*.tree && grep -q "test_mintBurn_single_position_through_IPanopticData" test/instrument/PanopticDataSeam.fork.t.sol && ! grep -E "import.*panoptic-borrowed" test/instrument/PanopticDataSeam.fork.t.sol && ! grep -E "import.*PanopticV2DeployHelper" test/instrument/PanopticDataSeam.fork.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `test -f contracts/test/spec/PanopticDataSeam.tree` succeeds and contains `PanopticDataSeam::`
    - `bulloak check test/spec/PanopticDataSeam.tree` exits 0 (per-file check; names NOT renamed — mn-B)
    - `bulloak check test/spec/*.tree` exits 0 (full-glob phase-gate command from 07-VALIDATION; covers the pre-existing MacroOracle.tree + the Plan 03/04 trees, catching cross-tree drift before phase close)
    - `grep -q "test_mintBurn_single_position_through_IPanopticData" contracts/test/instrument/PanopticDataSeam.fork.t.sol` succeeds (exact required name)
    - `! grep -E "import.*panoptic-borrowed" contracts/test/instrument/PanopticDataSeam.fork.t.sol` succeeds (FORK-03 static guard — verbatim 07-VALIDATION command — even at SPECIFY)
    - `! grep -E "import.*PanopticV2DeployHelper" contracts/test/instrument/PanopticDataSeam.fork.t.sol` succeeds (transitive-import seam guard)
    - `grep -q "BTT spec: test/spec/PanopticDataSeam.tree" contracts/test/instrument/PanopticDataSeam.fork.t.sol` succeeds
    - `.tree` committed FIRST in a separate commit before filled bodies (mn-B / git history)
  </acceptance_criteria>
  <done>`PanopticDataSeam.tree` is committed FIRST (mn-B) and passes both the per-file and full-glob `bulloak check`; the scaffolded fork test carries the spec tag, the required test name, and imports NEITHER `panoptic-borrowed` NOR `PanopticV2DeployHelper`; function names un-renamed; bodies are failing stubs — spec precedes impl.</done>
</task>

<task type="auto">
  <name>Task 2: IMPLEMENT — factory-choreography deploy helper (§B+§D, own MockCcop + PoolKeyLib + V4LpHelper), NOT `new PanopticPool(...)`</name>
  <files>contracts/test/instrument/helpers/PanopticV2DeployHelper.sol</files>
  <read_first>
    - 07-RESEARCH-DEPLOY.md §A.4 "The REAL deploy path (copy-pasteable)" + §A.3 (USE the real factory, do NOT replicate) — THE AUTHORITY
    - 07-RESEARCH-DEPLOY.md §B "Exact constructor signatures (verbatim @fe55774)" — the table for SFPMV4 (4 args), CollateralTracker (1 arg commissionFee), RiskEngine (4 args, guardian/builderFactory = address(0) OK), PanopticPool (1 arg), PanopticFactoryV4 (7 args, empty metadata arrays OK)
    - 07-RESEARCH-DEPLOY.md §A.4 step list (what deployNewPool does internally: SFPM.initializeAMMPool → clone2 CTs → clone3 pool → 3× initialize) + the PoolNotInitialized precondition (PoolManager.initialize FIRST)
    - 07-RESEARCH-DEPLOY.md §G (`vegoid` is a RiskEngine CONSTANT = 4, NOT a per-pool param)
    - contracts/panoptic-borrowed/PanopticFactoryV4.sol, PanopticPool.sol, CollateralTracker.sol, RiskEngine.sol, SemiFungiblePositionManagerV4.sol, types/Pointer.sol (confirm the §B signatures byte-for-byte against the vendored files; confirm `collateralToken0()/collateralToken1()` return types so they can be handed back as IERC4626 — B-1)
    - contracts/test/instrument/helpers/PoolKeyLib.sol (mn-C — the SHARED PoolKey/sqrtPriceX96 builder the helper calls to match Plan 04's ordering) + contracts/test/instrument/helpers/V4LpHelper.sol (M-1 — the full-range LP helper to seed liquidity) + the Plan-04 SUMMARY's stated seeded amount
    - contracts/src/instrument/interfaces/IPanopticData.sol (so the helper's returned address is known-assignable + usable as it)
  </read_first>
  <action>
    Create `contracts/test/instrument/helpers/PanopticV2DeployHelper.sol`. This file DOES import the borrowed concretes + factory + `Pointer` + `PoolKeyLib` + `V4LpHelper` (correct/intended — it is the deploy seam, NOT the test). Run the FACTORY CHOREOGRAPHY from §B+§D — do NOT call `new PanopticPool(...)` directly as the working pool (the contracts are CWIA proxies; a hand-constructed PanopticPool has no immutable args and is non-functional). The only `new PanopticPool(...)`/`new CollateralTracker(...)` allowed are the MASTER COPIES assigned into `poolReference`/`collateralReference`. Lift the §B signatures VERBATIM:
    1. `SemiFungiblePositionManager sfpm = new SemiFungiblePositionManager(IPoolManager(BASE_POOL_MANAGER), 10**13, 10**13, 0);` (4 args)
    2. master copies: `address poolReference = address(new PanopticPool(ISemiFungiblePositionManager(address(sfpm))));` (1 arg); `address collateralReference = address(new CollateralTracker(10));` (1 arg `commissionFee`)
    3. `PanopticFactory factory = new PanopticFactory(sfpm, IPoolManager(BASE_POOL_MANAGER), poolReference, collateralReference, new bytes32[](0), new uint256[][](0), new Pointer[][](0));` (7 args; empty metadata arrays OK)
    4. `RiskEngine re = new RiskEngine(10_000_000, 10_000_000, address(0), address(0));` (4 args; guardian & builderFactory = address(0) OK)
    5. mn-C — build the helper's OWN pool: `MockCcop ccop = new MockCcop();` then `(PoolKey memory poolKey, uint160 sqrtPriceX96, bool ccopIsCurrency0) = PoolKeyLib.buildCcopUsdcKey(address(ccop), BASE_USDC);` (the SAME builder Plan 04 uses ⇒ identical ordering). `IPoolManager(BASE_POOL_MANAGER).initialize(poolKey, sqrtPriceX96);` (REQUIRED first — else deployNewPool reverts PoolNotInitialized)
    6. M-1 — seed full-range liquidity via `V4LpHelper`: `V4LpHelper lp = new V4LpHelper(IPoolManager(BASE_POOL_MANAGER)); deal(BASE_USDC, address(lp), AMT); ccop.mint(address(lp), AMT); lp.addFullRangeLiquidity(poolKey, int128(SEEDED_LIQUIDITY));` (AMT/SEEDED_LIQUIDITY = the Plan-04-scale seed so later mints clear solvency)
    7. `PanopticPool pp = factory.deployNewPool(poolKey, re, uint96(block.timestamp));` (the factory does SFPM.initializeAMMPool(key, re.vegoid()) → clone2 both CTs → clone3 the pool → initialize all three)
    Expose `deployPanopticV2(...) returns (address pool, IERC4626 ct0, IERC4626 ct1, address token0, address token1, address ccop)` — return `address(pp)` and `pp.collateralToken0()`/`collateralToken1()` cast to `IERC4626` (B-1: the seam test gets ct0/ct1 as `IERC4626`, never the concrete). Do NOT treat `vegoid` as a parameter — it is `re.vegoid()` (constant 4). Do NOT mint/read/burn here — that is Task 3.
  </action>
  <verify>
    <automated>cd contracts && test -f test/instrument/helpers/PanopticV2DeployHelper.sol && grep -q "deployNewPool(" test/instrument/helpers/PanopticV2DeployHelper.sol && grep -q "new PanopticFactory" test/instrument/helpers/PanopticV2DeployHelper.sol && grep -q "poolReference" test/instrument/helpers/PanopticV2DeployHelper.sol && grep -q "buildCcopUsdcKey" test/instrument/helpers/PanopticV2DeployHelper.sol && grep -q "addFullRangeLiquidity" test/instrument/helpers/PanopticV2DeployHelper.sol && forge build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f contracts/test/instrument/helpers/PanopticV2DeployHelper.sol` succeeds
    - `grep -q "deployNewPool(" contracts/test/instrument/helpers/PanopticV2DeployHelper.sol` succeeds (the REAL deploy path — factory, not a direct working-pool `new`)
    - `grep -q "new PanopticFactory" contracts/test/instrument/helpers/PanopticV2DeployHelper.sol` succeeds AND `grep -q "poolReference" ...` succeeds (the master-copy + factory setup is present)
    - `grep -q "buildCcopUsdcKey" contracts/test/instrument/helpers/PanopticV2DeployHelper.sol` succeeds (mn-C: own pool built via the SHARED PoolKeyLib — identical ordering to Plan 04)
    - `grep -q "addFullRangeLiquidity" contracts/test/instrument/helpers/PanopticV2DeployHelper.sol` succeeds (M-1: seeds liquidity via V4LpHelper for solvency)
    - `grep -q "IERC4626" contracts/test/instrument/helpers/PanopticV2DeployHelper.sol` succeeds (B-1: ct0/ct1 returned as IERC4626)
    - `grep -q "10\*\*13" contracts/test/instrument/helpers/PanopticV2DeployHelper.sol` succeeds (SFPMV4 §B args) AND `grep -q "address(0)" ...` succeeds (RiskEngine guardian/builderFactory = 0)
    - Source inspection confirms the pool the helper RETURNS comes from `factory.deployNewPool` — any `new PanopticPool(...)` is ONLY the master copy assigned into `poolReference`, never the working pool
    - `forge build` exits 0 — the factory choreography compiles AND the returned pool `address` is usable as `IPanopticData(deployedPool)` (compile-time conformance proof)
  </acceptance_criteria>
  <done>`PanopticV2DeployHelper.sol` runs the §B+§D factory choreography (master copies + RiskEngine + PanopticFactoryV4 with verbatim §B signatures → own MockCcop + PoolKeyLib FRESH PoolKey → PoolManager.initialize → V4LpHelper full-range seed → factory.deployNewPool) and returns the deployed `PanopticPool` address + ct0/ct1 as `IERC4626` + token addresses; `vegoid` read as the RiskEngine constant; `forge build` green and the returned address is compile-time assignable to `IPanopticData`. No mint/burn yet.</done>
</task>

<task type="auto">
  <name>Task 3: IMPLEMENT — mint+burn ONE position through IPanopticData only (§D sequence; ct0/ct1 via IERC4626; concrete TokenId), green</name>
  <files>contracts/test/instrument/PanopticDataSeam.fork.t.sol</files>
  <read_first>
    - contracts/test/instrument/PanopticDataSeam.fork.t.sol (the stub from Task 1)
    - contracts/test/spec/PanopticDataSeam.tree (the spec the bodies satisfy)
    - 07-RESEARCH-DEPLOY.md §D "Mint-ONE-position init + collateral call sequence" — THE AUTHORITY: deal/mint → approve(pp + ct0 + ct1) → ct.deposit(uint104, actor) → dispatch (mint) → getAccumulatedFeesAndPositionsData → dispatch(size 0) (burn); the burn-array fix; the dispatch disambiguation; the solvency-sizing note
    - contracts/test/instrument/helpers/PanopticV2DeployHelper.sol (the Task-2 helper's deploy entrypoint + returned (address, IERC4626 ct0, IERC4626 ct1, token0, token1, ccop) — the seam test obtains these via a setUp/base WITHOUT importing this file)
    - contracts/panoptic-borrowed/types/TokenId.sol (M-2 — confirm the exact `addLeg` arg order + `addPoolId`/`poolId` builder signatures of `TokenIdLibrary` at the pinned commit; adjust the snippet if it differs)
    - @openzeppelin/contracts/interfaces/IERC4626.sol (B-1 — the `deposit(uint256,address)` signature; resolves through the OZ-into-v4-core remapping)
  </read_first>
  <action>
    Fill the seam test body following the §D sequence VERBATIM. The deployed PanopticPool comes from the Task-2 helper, but the seam test must obtain it ONLY as an `IPanopticData` address and ct0/ct1 ONLY as `IERC4626` — NEVER by importing `PanopticV2DeployHelper` or any `panoptic-borrowed` file (§M-3 PRIMARY path: a setUp/base contract calls the helper and hands back ONLY `address` (→ `IPanopticData`), `IERC4626 ct0/ct1`, and the token/ccop addresses as plain `address`/`IERC20Partial`). The test holds `IPanopticData pano = IPanopticData(deployedPoolAddress);` and `IERC4626 ct0; IERC4626 ct1;`.
    - `vm.createSelectFork(vm.rpcUrl("base"), BASE_FORK_BLOCK);` (reuse the harness/Plan 04 block).
    - FUND + APPROVE (§D step 1): `deal(BASE_USDC, actor, type(uint104).max);` `ccop.mint(actor, type(uint104).max);` then `approve(pano)` + `approve(address(ct0))` + `approve(address(ct1))` for both tokens via `IERC20Partial`.
    - DEPOSIT (§D step 2, B-1): `ct0.deposit(type(uint104).max, actor); ct1.deposit(type(uint104).max, actor);` — ct0/ct1 are `IERC4626` (HAS `deposit(uint256,address)`); do NOT import the concrete CollateralTracker.
    - MINT (§D step 3, M-2): build a concrete one-leg short `TokenId` via the GPL `@types/TokenId.sol` builder (a value type — importable without coupling; or obtain the built TokenId from setUp): `int24 strike = int24((pano.getCurrentTick() / tickSpacing) * tickSpacing);` then `TokenId built = TokenId.wrap(0).addPoolId(poolId).addLeg(0,1,0,0,0,0,strike,1);` (CONFIRM addLeg arg order against the vendored TokenIdLibrary in read_first; `isLong=0` short, `width=1` tickSpacing). `mintList=[built]`, `finalIds=[built]`, `sizes=[positionSize]` sized small vs the seeded LP for solvency, `limits[0]=[tickLimitLow, tickLimitHigh, int24(uint24(effectiveLiquidityLimitX32))]`; `pano.dispatch(mintList, finalIds, sizes, limits, false, 0);`
    - READ (§D step 4): `(,, PositionBalance[] memory bals) = pano.getAccumulatedFeesAndPositionsData(actor, true, finalIds);` and `assertEq(bals.length, 1);` (runtime proof). Premium values are READ, never re-derived.
    - BURN (§D step 5, FIX the array literal — mn-4): `uint128[] memory burnSizes = new uint128[](1); burnSizes[0] = 0;` `TokenId[] memory emptyFinal = new TokenId[](0);` `pano.dispatch(mintList, emptyFinal, burnSizes, limits, false, 0);` then assert the position closed (e.g. a re-read reflects the burn). NEVER the inline `[0]` literal.
    Keep it to ONE position. Make `test_mintBurn_single_position_through_IPanopticData` green. Do NOT add any `import` of a `panoptic-borrowed` type OR `PanopticV2DeployHelper`; ct0/ct1 are `IERC4626` (B-1).
  </action>
  <verify>
    <automated>cd contracts && forge build && forge test --match-test test_mintBurn_single_position_through_IPanopticData --fork-url "$BASE_RPC_URL" && bulloak check test/spec/PanopticDataSeam.tree && grep -q "new uint128\[\](1)" test/instrument/PanopticDataSeam.fork.t.sol && grep -q "IERC4626" test/instrument/PanopticDataSeam.fork.t.sol && ! grep -E "import.*panoptic-borrowed" test/instrument/PanopticDataSeam.fork.t.sol && ! grep -E "import.*PanopticV2DeployHelper" test/instrument/PanopticDataSeam.fork.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-test test_mintBurn_single_position_through_IPanopticData --fork-url "$BASE_RPC_URL"` exits 0 (FORK-03 runtime proof, command verbatim from 07-VALIDATION)
    - `grep -q "\.dispatch(" contracts/test/instrument/PanopticDataSeam.fork.t.sol` succeeds (mint+burn through the interface)
    - `grep -q "getAccumulatedFeesAndPositionsData" contracts/test/instrument/PanopticDataSeam.fork.t.sol` succeeds (read through the interface)
    - `grep -q "IERC4626" contracts/test/instrument/PanopticDataSeam.fork.t.sol` succeeds (B-1: ct0/ct1 deposit via IERC4626, NOT the concrete CollateralTracker)
    - `grep -q "addLeg" contracts/test/instrument/PanopticDataSeam.fork.t.sol` OR the built TokenId is obtained from setUp — the M-2 concrete one-leg TokenId is constructed (dispatch does not revert on an empty/invalid TokenId)
    - `grep -q "new uint128\[\](1)" contracts/test/instrument/PanopticDataSeam.fork.t.sol` succeeds (burn array fixed — mn-4; no inline `[0]`)
    - `grep -q "\.deposit(" contracts/test/instrument/PanopticDataSeam.fork.t.sol` succeeds (ERC-4626 collateral deposit per §D)
    - `! grep -E "import.*panoptic-borrowed" contracts/test/instrument/PanopticDataSeam.fork.t.sol` succeeds (seam guard — verbatim 07-VALIDATION)
    - `! grep -E "import.*PanopticV2DeployHelper" contracts/test/instrument/PanopticDataSeam.fork.t.sol` succeeds (transitive-import seam guard)
    - `bulloak check test/spec/PanopticDataSeam.tree` exits 0 AND `forge build` exits 0
  </acceptance_criteria>
  <done>The fork test funds + ERC-4626-deposits collateral via `IERC4626` ct0/ct1 (B-1), mints ONE position through `IPanopticData.dispatch` with a concrete one-leg short TokenId (M-2; PositionBalance==0 ⇒ mint), reads `PositionBalance[]` length 1 via `getAccumulatedFeesAndPositionsData`, and burns it via `dispatch(size 0)` (proper `new uint128[](1)` literal) — all without importing the borrowed concrete OR the deploy helper; `test_mintBurn_single_position_through_IPanopticData` passes; `bulloak check` clean.</done>
</task>

</tasks>

<verification>
- `forge test --match-test test_mintBurn_single_position_through_IPanopticData --fork-url "$BASE_RPC_URL"` green (FORK-03 mint/burn runtime proof). Executor MUST `export BASE_RPC_URL` first.
- `grep -q "deployNewPool(" test/instrument/helpers/PanopticV2DeployHelper.sol` (the REAL factory deploy path — NOT `new PanopticPool` as the working pool); helper builds its own pool via `buildCcopUsdcKey` (mn-C) + seeds LP via `addFullRangeLiquidity` (M-1).
- `grep -q "IERC4626" test/instrument/PanopticDataSeam.fork.t.sol` (B-1: ct0/ct1 deposit via the OZ interface, no concrete import).
- `! grep -E "import.*panoptic-borrowed" test/instrument/PanopticDataSeam.fork.t.sol` AND `! grep -E "import.*PanopticV2DeployHelper" test/instrument/PanopticDataSeam.fork.t.sol` (both seam guards).
- `bulloak check test/spec/PanopticDataSeam.tree` clean (`.tree` preceded impl in a separate commit — mn-B) AND `bulloak check test/spec/*.tree` clean (full-glob, run in Task 1).
- `forge build` green (compile-time conformance: IPanopticData compiles against V2 types and the helper-returned address is consumed as IPanopticData — not an unchecked cast).
</verification>

<success_criteria>
- `PanopticDataSeam.tree` committed FIRST before its implementation (Iron Law + mn-B).
- The deploy helper uses the REAL factory choreography (§B+§D verbatim signatures) — the contracts are CWIA proxies, never `new`-constructed working pools — and builds its OWN pool via the shared `PoolKeyLib` (mn-C) + seeds full-range LP via `V4LpHelper` (M-1).
- COMPILE-TIME: `forge build` green with the helper-returned address consumed as `IPanopticData`. RUNTIME: one position minted with a concrete one-leg short TokenId (M-2; PositionBalance==0 ⇒ mint), read (length 1), and burned (size 0 ⇒ burn, proper array literal) through the interface.
- The seam test imports neither a borrowed-concrete type nor the deploy helper; collateral is deposited via `IERC4626` ct0/ct1 (B-1) — Panoptic touched only via `IPanopticData` (swap seam intact, no transitive coupling).
- `vegoid` treated as the RiskEngine constant (=4), never a per-pool param. Streamia READ, never re-derived.
</success_criteria>

<output>
After completion, create `.planning/phases/07-base-fork-borrowed-panoptic-v2-ccop-usdc-pool/07-05-SUMMARY.md`
</output>

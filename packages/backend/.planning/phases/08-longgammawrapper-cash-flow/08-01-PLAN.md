---
phase: 08-longgammawrapper-cash-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/src/instrument/interfaces/ICostMeter.sol
  - contracts/src/instrument/interfaces/IPanopticData.sol
  - contracts/src/instrument/LongGammaWrapper.sol
  - contracts/test/instrument/LongGammaWrapper.invariants.tree
autonomous: true
requirements: [WRAP-01, WRAP-02, WRAP-03, WRAP-04]
user_setup: []

must_haves:
  truths:
    - "The wrapper reaches the pool ONLY through IPanopticData and collateral ONLY through IERC4626/IERC20 (swap seam intact)"
    - "An external ICostMeter seam exists; zero-address meter resolves to (0,0) cost"
    - "IPanopticData exposes getOracleTicks for involuntary-branch state monitoring"
    - "forge build compiles the new interfaces + wrapper skeleton against borrowed V2 value types"
  artifacts:
    - path: "contracts/src/instrument/interfaces/ICostMeter.sol"
      provides: "cost(address)->(cost0,cost1) external-meter seam"
      contains: "function cost"
    - path: "contracts/src/instrument/interfaces/IPanopticData.sol"
      provides: "getOracleTicks extension + stale @L221 comment fixed to L431"
      contains: "function getOracleTicks"
    - path: "contracts/src/instrument/LongGammaWrapper.sol"
      provides: "Contract skeleton: state machine, storage, events, function stubs"
      contains: "contract LongGammaWrapper"
    - path: "contracts/test/instrument/LongGammaWrapper.invariants.tree"
      provides: "BTT spec for the two named fuzz invariants"
      contains: "invariant_residualNeverExceedsHoldings"
  key_links:
    - from: "contracts/src/instrument/LongGammaWrapper.sol"
      to: "contracts/src/instrument/interfaces/IPanopticData.sol"
      via: "import + IPanopticData(pool) typed handle"
      pattern: "import.*IPanopticData"
    - from: "contracts/src/instrument/LongGammaWrapper.sol"
      to: "contracts/src/instrument/interfaces/ICostMeter.sol"
      via: "import + ICostMeter storage var"
      pattern: "import.*ICostMeter"
---

<objective>
Establish the Phase-8 contract surface that every later plan implements against: the `ICostMeter` external-meter seam, the `getOracleTicks` extension to `IPanopticData` (drive-by fixing the stale `@L221` comment to `L431`), the `LongGammaWrapper.sol` skeleton (state machine + storage + events + function stubs), and the `invariants.tree` BTT spec. This is interface-first: contracts and stubs only — no behavioral implementation yet (that comes in Plans 02–05). It LOCKS the swap seam (no `panoptic-borrowed` import) and the two-token external-cost shape so downstream tasks have unambiguous contracts.

Purpose: Hands every downstream plan the exact function signatures, event signatures, state-machine enum, per-token residual formula, and the `ICostMeter` shape — eliminating the scavenger-hunt anti-pattern. The 8-canonical-tree denominator starts here with the `invariants.tree`.
Output: 3 source files (2 interfaces + 1 wrapper skeleton) + 1 `.tree` spec; `forge build` green.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-longgammawrapper-cash-flow/08-CONTEXT.md
@.planning/phases/08-longgammawrapper-cash-flow/08-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Use these directly — no codebase exploration needed. -->

EXISTING IPanopticData (contracts/src/instrument/interfaces/IPanopticData.sol) — already declares:
  dispatch(TokenId[] positionIdList, TokenId[] finalPositionIdList, uint128[] positionSizes, int24[3][] tickAndSpreadLimits, bool usePremiaAsCollateral, uint256 builderCode) external
  dispatchFrom(TokenId[] positionIdListFrom, address account, TokenId[] positionIdListTo, TokenId[] positionIdListToFinal, LeftRightUnsigned usePremiaAsCollateral) external payable
  getAccumulatedFeesAndPositionsData(address user, bool includePendingPremium, TokenId[] positionIdList) view returns (LeftRightUnsigned shortPremium, LeftRightUnsigned longPremium, PositionBalance[] balances)
  getCurrentTick() view returns (int24)
  getTWAP() view returns (int24)
  numberOfLegs(address user) view returns (uint256)
Imports already present: TokenId (@types/TokenId.sol), PositionBalance (@types/PositionBalance.sol), LeftRightUnsigned (@types/LeftRight.sol)

BORROWED-SOURCE getOracleTicks (panoptic-borrowed/PanopticPool.sol L1899-1917) — verbatim signature to mirror:
  function getOracleTicks() external view returns (int24 currentTick, int24 spotTick, int24 medianTick, int24 latestTick, OraclePack oraclePack)
OraclePack is `type OraclePack is uint256;` at panoptic-borrowed/types/OraclePack.sol L5.
Remapping `@types/=panoptic-borrowed/types/` resolves `@types/OraclePack.sol`.

PHASE-7 PROVEN: contracts/test/instrument/PanopticDataSeam.fork.t.sol imports ONLY IPanopticData + IERC4626 + @types — the seam pattern to preserve.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ICostMeter interface + IPanopticData getOracleTicks extension</name>
  <files>contracts/src/instrument/interfaces/ICostMeter.sol, contracts/src/instrument/interfaces/IPanopticData.sol</files>
  <read_first>
    - contracts/src/instrument/interfaces/IPanopticData.sol (the file being modified — the existing 6-fn seam + its `@types` imports + the stale `@L221` comment on L12 and L38)
    - panoptic-borrowed/PanopticPool.sol L1899-1917 (verbatim `getOracleTicks` signature to mirror)
    - panoptic-borrowed/types/OraclePack.sol L5 (`type OraclePack is uint256;`)
    - 08-CONTEXT.md §code_context "Integration Points" (ICostMeter shape: `cost(address)->(uint256 cost0, uint256 cost1)`, zero-address ⇒ (0,0); IPanopticData drive-by comment fix @L221→L431)
    - 08-RESEARCH.md §"Don't Hand-Roll / Interface gaps" (getOracleTicks is the ONLY interface addition; no streamia getter)
  </read_first>
  <action>
    Create `contracts/src/instrument/interfaces/ICostMeter.sol` (SPDX MIT, pragma `^0.8.24`):
    ```solidity
    interface ICostMeter {
        /// @notice Per-token realized cost for a position, in each token's NATIVE decimals.
        /// @dev v1 wires the zero address ⇒ callers treat a zero-address meter as (0,0); Phase 9 deploys the real meter.
        /// @return cost0 token0 cost (native dp), cost1 token1 cost (native dp)
        function cost(address position) external view returns (uint256 cost0, uint256 cost1);
    }
    ```
    Then EXTEND `IPanopticData.sol`:
    1. Add `import {OraclePack} from "@types/OraclePack.sol";` alongside the existing `@types` imports.
    2. Add the function (verbatim signature from L1899):
    ```solidity
    // oracle ticks for involuntary-branch health monitoring — EXISTS @L1899
    function getOracleTicks() external view returns (int24 currentTick, int24 spotTick, int24 medianTick, int24 latestTick, OraclePack oraclePack);
    ```
    3. DRIVE-BY FIX the stale comment: in the NatSpec block (currently "getAccumulatedFeesAndPositionsData (L221)") AND the inline `// streamia / premium READ — EXISTS @L221` comment, change `@L221`/`L221` to `@L431`/`L431` (the vendored source has the getter at L431; CONTEXT/RESEARCH already use L431). Do NOT change any function body or any other line number.
  </action>
  <verify>
    <automated>cd contracts && forge build 2>&1 | tail -5; grep -c "L221" src/instrument/interfaces/IPanopticData.sol</automated>
  </verify>
  <acceptance_criteria>
    - `cd contracts && forge build` exits 0 (interfaces compile against borrowed V2 value types incl. `OraclePack`).
    - `grep -n "function cost(address position) external view returns (uint256 cost0, uint256 cost1)" contracts/src/instrument/interfaces/ICostMeter.sol` matches.
    - `grep -n "function getOracleTicks() external view returns (int24 currentTick, int24 spotTick, int24 medianTick, int24 latestTick, OraclePack oraclePack)" contracts/src/instrument/interfaces/IPanopticData.sol` matches.
    - `grep -c "L221" contracts/src/instrument/interfaces/IPanopticData.sol` returns `0` (stale comment fixed).
    - `grep -n "import {OraclePack} from \"@types/OraclePack.sol\"" contracts/src/instrument/interfaces/IPanopticData.sol` matches.
  </acceptance_criteria>
  <done>ICostMeter exists with the locked `cost(address)->(cost0,cost1)` shape; IPanopticData declares `getOracleTicks` and the stale L221 comment is gone; `forge build` green.</done>
</task>

<task type="auto">
  <name>Task 2: LongGammaWrapper skeleton — state machine, storage, events, stubbed entrypoints</name>
  <files>contracts/src/instrument/LongGammaWrapper.sol</files>
  <read_first>
    - contracts/src/instrument/interfaces/IPanopticData.sol (the seam the wrapper imports — post Task 1)
    - contracts/src/instrument/interfaces/ICostMeter.sol (the cost seam — post Task 1)
    - contracts/test/instrument/PanopticDataSeam.fork.t.sol L48-110 (the proven deposit→dispatch flow the wrapper internalizes; ct0/ct1 are `IERC4626`)
    - 08-CONTEXT.md §decisions "Tenancy & lifecycle" (state machine `Uninitialized→Open→Closed→Claimed`; single-record ledger; explicit `user`; `close()` MANDATORY user-gated; `claimResidual` caller-agnostic; `syncResidual` permissionless; `setCostMeter` owner+pre-Open-only)
    - 08-CONTEXT.md §decisions "Events" (PositionOpened/ResidualEroded/ResidualClaimed/CostMeterSet signatures) + "Security guardrails" (nonReentrant + CEI)
    - 08-RESEARCH.md Pattern 1, Pattern 7 (custody + surviving-collateral residual)
  </read_first>
  <action>
    Create `contracts/src/instrument/LongGammaWrapper.sol` (SPDX MIT — NOTE: this is OUR code, not borrowed; pragma `^0.8.24`). Import ONLY: `IPanopticData` (local), `ICostMeter` (local), `IERC4626` + `IERC20` from `@openzeppelin/contracts/interfaces/IERC4626.sol` / `@openzeppelin/contracts/interfaces/IERC20.sol`, `TokenId` from `@types/TokenId.sol`. ABSOLUTELY NO `panoptic-borrowed` import (swap-seam grep-guard).

    Define:
    - `enum State { Uninitialized, Open, Closed, Claimed }`
    - Immutables set in constructor: `IPanopticData public immutable pool; IERC4626 public immutable ct0; IERC4626 public immutable ct1; address public immutable owner;` Constructor signature: `constructor(IPanopticData _pool, IERC4626 _ct0, IERC4626 _ct1)` setting `owner = msg.sender`.
    - Single-record storage (NOT a per-user mapping): `address public user; State public state; TokenId public positionTokenId; uint256 public deposited0; uint256 public deposited1; uint256 public lastSurviving0; uint256 public lastSurviving1; ICostMeter public costMeter; bool public claimed;`
    - A `_locked` reentrancy flag + `modifier nonReentrant()` (set-before / clear-after; the cheap-insurance guard per the security lock — implement inline, do NOT pull OZ ReentrancyGuard to keep the import set minimal).
    - Events (verbatim from CONTEXT §Events):
      `event PositionOpened(address indexed user, TokenId tokenId, uint256 deposited0, uint256 deposited1);`
      `event ResidualEroded(address indexed user, uint256 eroded0, uint256 eroded1, bytes32 cause);`
      `event ResidualClaimed(address indexed user, uint256 paid0, uint256 paid1);`
      `event CostMeterSet(address meter);`
    - Errors: `error NotOwner(); error NotUser(); error WrongState(); error AlreadyClaimed();`
    - STUBBED entrypoints with the LOCKED signatures + modifiers, bodies reverting `revert("UNIMPLEMENTED")` (Plans 02–04 fill them):
      `function deposit(address _user, uint256 assets0, uint256 assets1) external;`  (sets user, pulls/deposits collateral, mints long — Plan 02)
      `function close() external nonReentrant;`  (user-gated voluntary burn via `pool.dispatch` size→0 — Plan 03)
      `function syncResidual() external nonReentrant;`  (permissionless erosion poke — Plan 03)
      `function claimResidual() external nonReentrant;`  (caller-agnostic payout — Plan 03)
      `function setCostMeter(address meter) external;`  (owner + only while `state == Uninitialized`; sets `costMeter`, emits `CostMeterSet`; this ONE is implemented now since it has no fork dependency)
    - Internal view helper signatures (stub-revert except those usable now):
      `function _costOf() internal view returns (uint256 c0, uint256 c1)` — IMPLEMENT now: `if (address(costMeter) == address(0)) return (0,0); return costMeter.cost(address(this));` (the zero-address ⇒ (0,0) rule).

    IMPLEMENT `setCostMeter` fully now (it is config-only, no fork dep): `if (msg.sender != owner) revert NotOwner(); if (state != State.Uninitialized) revert WrongState(); costMeter = ICostMeter(meter); emit CostMeterSet(meter);`

    Document in a contract-level NatSpec: "owner's ONLY lever is `setCostMeter`, frozen at first deposit (state leaves Uninitialized); ownership is NOT assumed transferable/renounceable in v1" and "v1 assumes `user` is a callable EOA — the N1 callable-`user` precondition."
  </action>
  <verify>
    <automated>cd contracts && forge build 2>&1 | tail -5; grep -L "panoptic-borrowed" src/instrument/LongGammaWrapper.sol</automated>
  </verify>
  <acceptance_criteria>
    - `cd contracts && forge build` exits 0.
    - `grep -c "panoptic-borrowed" contracts/src/instrument/LongGammaWrapper.sol` returns `0` (swap-seam grep-guard — the foundational LOCKED constraint).
    - `grep -n "enum State { Uninitialized, Open, Closed, Claimed }" contracts/src/instrument/LongGammaWrapper.sol` matches.
    - All five entrypoint signatures present: `grep -c "function deposit\|function close\|function syncResidual\|function claimResidual\|function setCostMeter" contracts/src/instrument/LongGammaWrapper.sol` returns `5`.
    - The four events present: `grep -c "event PositionOpened\|event ResidualEroded\|event ResidualClaimed\|event CostMeterSet" contracts/src/instrument/LongGammaWrapper.sol` returns `4`.
    - `setCostMeter` body present (owner-gated, Uninitialized-gated): `grep -n "if (state != State.Uninitialized) revert WrongState();" contracts/src/instrument/LongGammaWrapper.sol` matches.
    - No streamia constant: `grep -Ei "SPREAD_MULTIPLIER|perBlock|streamiaPerBlock|VEGOID" contracts/src/instrument/LongGammaWrapper.sol` returns nothing (P1 grep-guard).
  </acceptance_criteria>
  <done>Wrapper skeleton compiles, imports only the seam interfaces (no panoptic-borrowed), declares the locked state machine + events + entrypoint signatures, and `setCostMeter` + `_costOf` (zero-address⇒(0,0)) are fully implemented.</done>
</task>

<task type="auto">
  <name>Task 3: invariants.tree — BTT spec for the two named fuzz invariants (Iron Law, tree-before-impl)</name>
  <files>contracts/test/instrument/LongGammaWrapper.invariants.tree</files>
  <read_first>
    - contracts/test/instrument/PanopticDataSeam.fork.tree (a same-repo `.tree` bulloak 0.9.2 parses cleanly while using `::` and free prose — mirror its `when/it` style)
    - contracts/test/instrument/MacroHedgeStrategist.tree (second reference for the parseable when/it nesting style)
    - 08-CONTEXT.md §code_context "Established Patterns" (canonical count = 8 trees; `invariants` is the 8th; bulloak 0.9.2 same-dir full-stem `X.tree ↔ X.t.sol`)
    - 08-VALIDATION.md §Per-Task Verification Map (the two invariant rows: `invariant_residualNeverExceedsHoldings`, `invariant_userClaimsBackedByCollateral`)
    - 08-RESEARCH.md §Validation Architecture (invariant semantics: payout ≤ convertToAssets(balanceOf(wrapper)); Σ user claims ≤ wrapper surviving collateral)
  </read_first>
  <action>
    Create `contracts/test/instrument/LongGammaWrapper.invariants.tree`. bulloak 0.9.2 parses it and (Plan 05) scaffolds `LongGammaWrapper.invariants.t.sol` same-dir, full-stem. Structure (root contract name `LongGammaWrapperInvariants`):
    ```
    LongGammaWrapperInvariants
    ├── invariant_residualNeverExceedsHoldings
    │   └── it never pays out more than convertToAssets of the wrapper share balance
    └── invariant_userClaimsBackedByCollateral
        └── it keeps the single user claim at or below wrapper surviving collateral
    ```
    Keep each leaf a single `it ...` line. The tree DEFINES the two named invariant functions Plan 05 implements; committing it now satisfies the Iron Law (tree before impl) for the invariants unit. The on-disk `PanopticDataSeam.fork.tree` confirms bulloak 0.9.2 parses prose `it`/`when` labels (and even `::`) fine — there is NO "no dot/slash in labels" rule; rely on `bulloak check` for parse-validity.
  </action>
  <verify>
    <automated>cd contracts && bulloak scaffold test/instrument/LongGammaWrapper.invariants.tree >/dev/null 2>&1 && echo PARSE_OK || echo PARSE_FAIL</automated>
  </verify>
  <acceptance_criteria>
    - REAL PARSE GATE (NOT `$?` — `bulloak check` exits 0 even on a genuine parse error, so an exit-code check is a silent no-op that would PASS a malformed tree): `cd contracts && bulloak scaffold test/instrument/LongGammaWrapper.invariants.tree >/dev/null 2>&1 && echo PARSE_OK` prints `PARSE_OK` (`bulloak scaffold` emits valid Solidity and exits 0 ONLY when the tree parses). Equivalent gate: `bulloak check test/instrument/LongGammaWrapper.invariants.tree 2>&1 | grep -qi "error occurred while parsing" && echo PARSE_FAIL || echo PARSE_OK` prints `PARSE_OK`. The absent companion `.t.sol` is the expected pre-impl state (resolved in Plan 05) and does NOT affect the scaffold parse gate.
    - `grep -c "invariant_residualNeverExceedsHoldings\|invariant_userClaimsBackedByCollateral" contracts/test/instrument/LongGammaWrapper.invariants.tree` returns `2`.
  </acceptance_criteria>
  <done>`LongGammaWrapper.invariants.tree` exists, is bulloak-0.9.2-parseable, names both ROADMAP invariants, and is committed before any invariant `.t.sol` (Iron Law).</done>
</task>

</tasks>

<verification>
- `cd contracts && forge build` exits 0 (all three source files compile).
- Swap seam grep-guard holds: `grep -c "panoptic-borrowed" contracts/src/instrument/LongGammaWrapper.sol` == 0.
- P1 grep-guard holds: no `SPREAD_MULTIPLIER`/`perBlock`/`VEGOID` in the wrapper.
- `bulloak scaffold test/instrument/LongGammaWrapper.invariants.tree >/dev/null 2>&1 && echo PARSE_OK` prints `PARSE_OK` (a REAL parse gate — `bulloak check`'s exit code is a silent no-op).
</verification>

<success_criteria>
The Phase-8 contract surface is LOCKED: `ICostMeter` (zero-address⇒(0,0)), `IPanopticData.getOracleTicks` (+L221→L431 fix), `LongGammaWrapper` skeleton (state machine + events + 5 entrypoints, `setCostMeter`/`_costOf` live), and the `invariants.tree`. Every downstream plan implements against these exact signatures. `forge build` green; seam + P1 grep-guards pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-longgammawrapper-cash-flow/08-01-SUMMARY.md`.
</output>

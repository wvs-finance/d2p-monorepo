# Copy to .env and fill in real values. .env is gitignored; this template is committed.

# Trading Economics REST API — format is `client:secret`, passed to the API as ?c=<client:secret>
TRADING_ECONOMICS_API_KEY=your_client:your_secret

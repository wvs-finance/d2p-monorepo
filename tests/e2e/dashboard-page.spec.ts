// Phase 3 Wave 0 — DASH-03 e2e stub (filled by Plan 03-02)
import { test } from '@playwright/test'

test.fixme(
  '/apps/abrigo/dashboard renders 4 labelled metric tiles per chain with dashed values + live banner',
  async () => {
    // When filled by Plan 03-02:
    //   1. Navigate to /apps/abrigo/dashboard
    //   2. Assert 4 metric tiles visible:
    //      - "Saldo del fondo" / "Pool balance" with value "—"
    //      - "Eventos de liquidación" / "Settlement events" with value "—"
    //      - "Posiciones LP" / "LP positions" with value "—"
    //      - "Último bloque sincronizado" / "Last block synced" with value "—"
    //   3. Assert live banner "En vivo una vez se desplieguen los contratos" is visible
    //   4. Assert response status 200
  },
)

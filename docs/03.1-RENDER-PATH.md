# 03.1 Render-Path Investigation — Math Pipeline + Custom-Component Injection

**Phase:** 03.1-research-reading-surface — Plan A (03.1-01)
**Date:** 2026-05-29
**Purpose:** Prove the build-time KaTeX render path and the custom-component injection path
work end-to-end before any downstream `/research` work begins.

---

## 1. Resolved Dependency Versions

| Package | Version | Notes |
|---------|---------|-------|
| `katex` | 0.17.0 | pinned exact |
| `remark-math` | 6.0.0 | pinned exact |
| `rehype-katex` | 7.0.1 | pinned exact |
| `remark-gfm` | 4.0.1 | pinned exact |
| `remark-directive` | 4.0.0 | pinned exact |
| `rehype-slug` | 6.0.0 | pinned exact |
| `rehype-autolink-headings` | 7.1.0 | pinned exact |
| `rehype-pretty-code` | — | **DROPPED from v1** (see §3) |

## 2. Unified Major Deduplication

`pnpm why unified` output confirms **one single version: unified@11.0.5** in the dependency tree.

All newly-installed plugins (remark-math@6, rehype-katex@7, remark-gfm@4, remark-directive@4,
rehype-slug@6, rehype-autolink-headings@7) resolve against `unified@11.0.5` — the same major
Velite@0.3.1 bundles via `@mdx-js/mdx@3.1.1`. **No duplicate unified majors; no pnpm overrides
needed.**

`mdast-util-from-markdown@2.0.3` is also deduplicated to a single version (confirmed via
`pnpm why mdast-util-from-markdown`). All mdast utilities resolve against the same major.

`@mdx-js/mdx` is NOT added as a top-level dependency (stays nested under velite@0.3.1 and
next-mdx-remote@6.0.0). Correct.

## 3. rehype-pretty-code — DROPPED from v1

Decision: `rehype-pretty-code` is **not included in v1**. Neither spike fixture
(`spike-katex.es.mdx`, `spike-katex.en.mdx`) contains a fenced code block. Adding Shiki
(the Vite/Node bundle required by rehype-pretty-code) adds meaningful LCP weight and is
deferred to v2 alongside the full reading-page polish.

The `rehypePlugins` array in `velite.config.ts` is:
`[rehypeSlug, rehypeAutolinkHeadings, [rehypeKatex, { output: 'htmlAndMathml', throwOnError: false }]]`

## 4. Velite Auto-GFM Suppression (`gfm: false`)

Verified against `node_modules/velite/dist/index.js` L4982–L4989:
```
enableGfm = options.gfm ?? mdx.gfm ?? true
if (enableGfm) remarkPlugins.push(remarkGfm)
// THEN: remarkPlugins.push(...options.remarkPlugins)
```

Without `gfm: false`, the effective remark order would be:
`[remarkGfm(auto), remarkRemoveComments, remarkCopyLinkedFiles, remarkMath, remarkGfm(dup), remarkDirective]`

With `gfm: false` set on `s.mdx({...})`, the effective order is:
`[remarkRemoveComments, remarkCopyLinkedFiles, remarkMath, remarkGfm, remarkDirective, remarkTheoremDirective]`
— math first, GFM registered exactly once. Spec §0 order honored.

## 5. Locale-Derivation Mechanism

`s.path()` in Velite returns the SOURCE FILE PATH relative to the `content/` root,
e.g. `research/spike-katex.es.mdx`. The derived `locale` field uses a `.transform()`:

```ts
locale: s.path().transform((p: string) => p.endsWith('.es.mdx') ? 'es' : 'en')
```

The `.es.mdx` / `.en.mdx` suffix is present in the `s.path()` value — confirmed by
velite@0.3.1 source (`meta.path` is the source file path). This is the correct mechanism.

## 6. Components-Prop Injection Finding

**Finding:** Velite@0.3.1 compiles MDX bodies with `outputFormat: 'function-body'` and
**passes NO `providerImportSource`**. The compiled default export resolves custom components
(JSX elements whose names start with uppercase letters, e.g. `<Figure>`, `<TheoremBlock>`)
via the `components` PROP passed to the evaluated function — NOT via `useMDXComponents()`.

A no-prop renderer (`return <C />`) therefore **throws at runtime** on any MDX body containing
custom components. The extended `MDXRenderer` passes the components map explicitly:
`return <C components={components} />`.

This is the critical path Plan C depends on: the component-map (h2/h3, Figure, TheoremBlock,
Sidenote) is passed as a prop to the compiled body's default export at render time.

## 7. next-mdx-remote/rsc Fallback Exercise

**Verdict: PASS** (exercised 2026-05-29)

The fallback path was exercised by running `compileMDX` from `next-mdx-remote/rsc.js`
directly against the `spike-katex.es.mdx` source with the same remark-math + rehype-katex
plugin chain:

```js
const result = await compileMDX({
  source,
  options: {
    parseFrontmatter: true,
    mdxOptions: {
      remarkPlugins: [remarkMath],
      rehypePlugins: [[rehypeKatex, { output: 'htmlAndMathml', throwOnError: false }]],
    },
  },
})
```

Result: `frontmatter.slug = 'spike-katex'`, `typeof content = 'object'` (React element). **PASS.**

**Finding:** The `next-mdx-remote/rsc` path compiles MDX at render time (pulling `@mdx-js/mdx`
into the render path) and returns a React element directly (unlike `s.mdx()` which returns a
function-body string for evaluation). The render paths are fundamentally different:
- **Primary path (s.mdx + MDXRenderer):** compile at Velite build time → store string → evaluate at render. Zero runtime MDX cost. Best for 3G LCP.
- **Fallback path (next-mdx-remote/rsc):** compile at request time → React element. Higher per-request cost; only used if s.mdx() fails.

The primary path (`s.mdx()` + MDXRenderer) is confirmed working; the fallback is confirmed
as a viable emergency option but is NOT used in the v1 implementation.

## 8. Dev-Turbopack Verdict

**Verdict: PASS** (verified 2026-05-29 with `pnpm dev --turbopack`)

Server started at `http://localhost:3000`.
Route `/research/spike` returned HTTP 200.

DOM inspection via `curl` + grep:
- `class="katex-display"` nodes: **1** (display equation rendered correctly)
- `class="katex-error"` nodes: **0** (burn-class guard passes)
- `data-testid="theorem-block"` nodes: **1** (TheoremBlock stub rendered)

The KaTeX styles loaded correctly (scoped via `app/(lab)/research/layout.tsx` import of
`katex/dist/katex.min.css`). Turbopack honors the Next.js layout CSS import without any
webpack workaround — confirmed safe by construction (KaTeX compiles at Velite build time,
in Node, not in the webpack/Turbopack resolver path).

## 9. Production-Build Spike Verdict

**Verdict: PASS** (verified 2026-05-29 via Playwright e2e, `pnpm build && pnpm start -p 3040`)

All 5 Playwright assertions in `tests/e2e/research-math-spike.spec.ts` passed:

| Test | Result |
|------|--------|
| Route `/research/spike` returns 200 | PASS |
| At least one `.katex` node present | PASS |
| BURN-CLASS GUARD: zero `.katex-error` nodes | PASS |
| `\tag{1}` equation number `(1)` present in DOM | PASS |
| `[data-testid="theorem-block"]` visible | PASS |

**Note on the `\tag` macro:** The initial single-line `$$...\tag{1}$$` form was treated
as inline math by remark-math (the inline-vs-display ambiguity when both delimiters are
on the same line). Fixed by using multi-line display form:
```
$$
\Pi(\sigma_T) \approx \hat{K} \cdot \sigma_T \tag{1}
$$
```
The multi-line form is unambiguously classified as display math by remark-math's parser.
**Authors must use multi-line `$$` for display equations with `\tag`.** This finding is
recorded here as a content authoring constraint for Plans B and C.
